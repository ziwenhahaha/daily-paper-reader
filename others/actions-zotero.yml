type: ActionsTagsBackup
author: zwwen0711
platformVersion: 7.0.30
pluginVersion: 2.2.4
timestamp: '2025-12-28T17:22:32.173Z'
actions:
  1766933168242-lENQeb2s:
    event: 1
    operation: 4
    data: >-
      // === 1. 定义标记 ===
    
      const MARKER_SUMMARY = "【🤖 AI Summary】";

      const MARKER_CHAT = "【💬 Chat History】";

      const MARKER_ORIG = "【📄 Original Abstract】";


      // 前端传来的占位符

      const BR_PLACEHOLDER = /__BR__/g; 


      // === 2. 获取条目（支持 item / items）===

      const targetItems = (typeof items !== 'undefined' && items && items.length > 0)
          ? items
          : ((typeof item !== 'undefined') ? [item] : []);


      if (!targetItems || targetItems.length === 0) {
          return "❌ 错误：未接收到有效条目 (请确保选中了条目)";
      }


      // === 3. 工具函数 ===

      function normalizeText(v) {
          return (v || "").toString();
      }


      function escapeHtml(v) {
          return normalizeText(v)
              .replace(/&/g, '&amp;')
              .replace(/</g, '&lt;')
              .replace(/>/g, '&gt;')
              .replace(/"/g, '&quot;')
              .replace(/'/g, '&#39;');
      }


      function toParagraphHtml(rawText) {
          return escapeHtml(rawText)
              .replace(BR_PLACEHOLDER, "\n")
              .replace(/\n/g, "<br>");
      }


      function splitSections(fullText) {
          const s = fullText.indexOf(MARKER_SUMMARY);
          const c = fullText.indexOf(MARKER_CHAT);
          const o = fullText.indexOf(MARKER_ORIG);

          if (s === -1) {
              return {
                  valid: false,
                  reason: "❌ 摘要里没找到 'AI Summary' 标记，跳过。"
              };
          }

          if (o === -1) {
              return {
                  valid: false,
                  reason: "❌ 找不到 Original Abstract 标记。"
              };
          }

          const summaryEnd = (c > -1) ? c : o;

          return {
              valid: true,
              summary: fullText.substring(s + MARKER_SUMMARY.length, summaryEnd).trim(),
              chat: c > -1 ? fullText.substring(c + MARKER_CHAT.length, o).trim() : "",
              original: fullText.substring(o + MARKER_ORIG.length).trim()
          };
      }


      function formatChatHistory(rawChatString) {
          if (!rawChatString) return "";

          let segments = rawChatString.split(/(?=👤 User:|🤖 AI:)/g);
          let html = '<div style="display: flex; flex-direction: column; gap: 20px;">';

          segments.forEach(seg => {
              seg = seg.trim();
              if (!seg) return;

              let cleanText = seg.replace(BR_PLACEHOLDER, "\n").trim();

              if (cleanText.startsWith("👤 User:")) {
                  let content = cleanText.replace("👤 User:", "").trim();
                  html += `
                      <div style="align-self: flex-end; max-width: 85%;">
                          <strong style="display:block; color:#0d47a1; font-size:0.8em; margin-bottom:4px; text-align:right;">👤 User</strong>
                          <pre style="
                              display: block !important;
                              background-color: #bbdefb;
                              color: #0d47a1;
                              padding: 15px;
                              border-radius: 15px 15px 0 15px;
                              white-space: pre-wrap;
                              font-family: -apple-system, BlinkMacSystemFont, sans-serif;
                              margin: 0;
                              font-size: 1em;
                              line-height: 1.5;
                              border: none;
                              box-shadow: 1px 1px 2px rgba(0,0,0,0.05);
                          ">${escapeHtml(content)}</pre>
                      </div>
                  `;
              } else if (cleanText.startsWith("🤖 AI:")) {
                  let content = cleanText.replace("🤖 AI:", "").trim();
                  html += `
                      <div style="align-self: flex-start; max-width: 90%;">
                          <strong style="display:block; color:#616161; font-size:0.8em; margin-bottom:4px;">🤖 AI</strong>
                          <pre style="
                              display: block !important;
                              background-color: #f5f5f5;
                              color: #333;
                              padding: 15px;
                              border-radius: 15px 15px 15px 0;
                              white-space: pre-wrap;
                              font-family: -apple-system, BlinkMacSystemFont, sans-serif;
                              margin: 0;
                              font-size: 1em;
                              line-height: 1.5;
                              border: 1px solid #e0e0e0;
                          ">${escapeHtml(content)}</pre>
                      </div>
                  `;
              }
          });

          html += '</div>';
          return html;
      }


      // === 4. 处理逻辑 ===

      async function processItem(targetItem) {
          const fullText = normalizeText(targetItem.getField('abstractNote'));

          if (!fullText) {
              return "⚠️ 跳过：条目摘要为空。";
          }

          const parsed = splitSections(fullText);
          if (!parsed.valid) {
              return parsed.reason;
          }

          const rawSummary = parsed.summary;
          const rawChat = parsed.chat;
          const rawOrig = parsed.original;

          const summaryHtml = toParagraphHtml(rawSummary);
          const chatHtml = formatChatHistory(rawChat);

          const title = targetItem.getField('title') || '[无标题]';

          const markdownSource = `${MARKER_SUMMARY}\n\n${normalizeText(rawSummary).replace(BR_PLACEHOLDER, "\n")}\n\n${rawChat ? `${MARKER_CHAT}\n\n${normalizeText(rawChat).replace(BR_PLACEHOLDER, "\n")}\n\n` : ''}${MARKER_ORIG}\n\n${normalizeText(rawOrig)}`;

          let noteHTML = `
              <div style="font-family: -apple-system, BlinkMacSystemFont, sans-serif; color: #333;">
                  <h2 style="
                      background: transparent; 
                      color: #0277bd; 
                      padding: 0; 
                      margin-bottom: 15px; 
                      border-bottom: 2px solid #e1f5fe; 
                      display: inline-block;
                  ">
                      🤖 AI Summary
                  </h2>
                  <div style="margin-bottom: 30px;">
                      <div style="
                          display: block;
                          background-color: transparent;
                          color: #333;
                          font-family: -apple-system, BlinkMacSystemFont, sans-serif;
                          font-size: 1em;
                          line-height: 1.6;
                          border: none;
                      ">${summaryHtml}</div>
                  </div>
          `;

          if (chatHtml) {
              noteHTML += `
                  <hr style="border: 0; border-top: 2px dashed #ccc; margin: 30px 0;">
                  <h3 style="
                      background: transparent; 
                      color: #444; 
                      padding: 0; 
                      margin-bottom: 20px;
                      border-bottom: 2px solid #eee;
                      display: inline-block;
                  ">
                      💬 Chat History
                  </h3>

                  ${chatHtml}
                  <h3 style="
                      background: transparent; 
                      color: #444; 
                      padding: 0; 
                      margin-top: 26px; 
                      margin-bottom: 8px;
                  ">
                      🧩 Source Snapshot (LaTeX/Markdown)
                  </h3>
                  <div style="font-size: 0.95em; color: #555;">
                      已保留原始文本块，便于你复制到 Better Notes 或手动二次处理。
                  </div>
              `;
          }

          noteHTML += `
                  <details style="margin-top: 16px;">
                      <summary style="cursor: pointer; color: #0277bd;">🧮 打开 LaTeX/Markdown 源码</summary>
                      <pre style="white-space: pre-wrap; background: #fafafa; border: 1px solid #e0e0e0; border-radius: 8px; padding: 12px;">${escapeHtml(markdownSource)}</pre>
                  </details>
              </div>`;


          let newNote = new Zotero.Item('note');
          newNote.setNote(noteHTML);
          newNote.parentID = targetItem.id;
          await newNote.saveTx();

          targetItem.setField('abstractNote', normalizeText(rawOrig).replace(BR_PLACEHOLDER, "\n"));
          await targetItem.saveTx();

          return `✅ [${escapeHtml(title)}] 处理完成（含 LaTeX/Markdown 源码折叠块）`;
      }


      const results = [];
      for (let i = 0; i < targetItems.length; i += 1) {
          results.push(await processItem(targetItems[i]));
      }

      return results.join("\n");
    shortcut: ''
    enabled: true
    menu: ''
    name: Process AI Summary
    showInMenu:
      item: false
      collection: false
      tools: false
      reader: false
      readerAnnotation: false
