# 可选：仅当需要本地 embedding/reranker fallback 时安装。
# 默认本地调试使用 zwwen.online 远程 embedding/rerank 服务，不需要这些重依赖。
-r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1+cpu
sentence-transformers
transformers
