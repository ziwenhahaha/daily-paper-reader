# Optional: Install only if local embedding/reranker fallback is required.

# By default, local debugging uses the zwwen.online remote embedding/rerank service, which does not require these heavy dependencies.
-r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.9.1
sentence-transformers
transformers
