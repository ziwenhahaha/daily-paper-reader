# Demo Paper (test.pdf)

本页面用于演示：从本地 test.pdf 抽取文本并作为聊天上下文使用。

对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`对应的文本文件：`demo-test-pdf.txt`

---

111111111111

| 维度         | 位置编码                  | 时序模型             |
| :----------- | :------------------------ | :------------------- |
| **并行性**   | **极高** (O(1) 顺序操作)  | **极低** (O(n) 顺序操作) |
| **长距离依赖** | **非常高效** (路径长度 O(1)) | **困难** (路径长度 O(n)，信息易丢失) |
| **位置表示** | **显式、灵活** (可设计相对位置) | **隐式、动态** (与内容融合) |
| **时序动态性** | **无** (静态处理)         | **有** (动态状态机)    |
| **长度外推性** | **有限** (受限于训练长度) | **理论上较强** (可无限循环) |
| **核心哲学** | **注入位置，全局关系计算** | **顺序处理，动态状态更新** |

（这里是正文区域，可以写摘要、笔记等内容。）
