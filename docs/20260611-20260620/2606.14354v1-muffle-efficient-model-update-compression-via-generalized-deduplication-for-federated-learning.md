---
title: "MUFFLe: Efficient Model Update Compression via Generalized Deduplication for Federated Learning"
title_zh: MUFFLe：通过广义去重实现联邦学习的高效模型更新压缩
authors: "Xiaobo Zhao, Daniel E. Lucani"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14354v1"
tags: ["query:dgnn-nas"]
score: 10.0
evidence: MUFFLe通过广义去重压缩模型更新，显著降低联邦学习通信开销
tldr: "在边缘联邦学习中，频繁上传模型更新导致上行通信开销巨大，成为限制其大规模部署的关键瓶颈。本文提出MUFFLe，将广义去重（GD）技术融入FedAvg流程，通过挖掘并消除更新向量中重复模式，实现固定速率、可变计数的压缩。在20客户端IID MNIST任务上，MUFFLe以仅38MB累计通信量达到92.93%精度，大幅优于8位量化（75MB）、Top-k稀疏化（86MB）及未压缩FedAvg（310MB）。初步实验证实GD可显著降低联邦学习通信成本，为高效边缘训练提供了新途径。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14354v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 659, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14354v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 842, \"height\": 419, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14354v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 914, \"height\": 382, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14354v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 218, \"label\": \"Table\"}]"
motivation: 联邦学习的上行通信成本严重制约其边缘部署，急需高效的更新压缩方案。
method: MUFFLe将广义去重引入FedAvg，通过识别更新向量中重复模式，实现固定压缩率与可变长编码。
result: "在20客户端IID MNIST上，MUFFLe以38MB累计上行通信达到92.93%准确率，优于8位量化(75MB)、Top-k(86MB)和未压缩(310MB)。"
conclusion: 广义去重能有效压缩联邦学习更新，大幅降低通信量，展示了其在通信高效训练中的潜力。
---

## 摘要
联邦学习非常适合边缘环境，但通常受限于传输模型更新的上行链路成本。这篇进行中的论文提出了MUFFLe，一种通信高效的更新压缩方案，将广义去重（GD）集成到FedAvg流水线中。MUFFLe对更新向量中重复的模式进行去重，产生了一种固定速率、可变计数的压缩方案。在包含20个客户端的IID MNIST上的初步实验表明，MUFFLe以38 MB的累计上行通信量达到了92.93%的目标准确率，相比之下，8位量化需要75 MB，Top-k稀疏化需要86 MB，未压缩的FedAvg则需要310 MB。这些结果证明了将GD应用于通信高效的联邦学习的可行性。

## Abstract
Federated learning is well suited to edge environments but is often limited by the uplink cost of transmitting model updates. This Work-in-Progress paper presents MUFFLe, a communication-efficient update compression scheme that integrates generalized deduplication (GD) into the FedAvg pipeline. MUFFLe deduplicates repeated patterns across the update vector, yielding a fixed-rate, variable-count compression scheme. Preliminary experiments on IID MNIST with 20 clients show that MUFFLe reaches the target accuracy of $92.93\%$ with 38~MB cumulative uplink communication, compared with 75~MB for 8-bit quantization, 86~MB for Top-$k$ sparsification, and 310~MB for uncompressed FedAvg. These results demonstrate the feasibility of applying GD to communication-efficient federated learning.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **核心问题**：在边缘计算环境中部署联邦学习时，客户端向服务器传输模型更新（上行链路）消耗大量带宽、延迟显著且增加设备能耗，成为系统的主要瓶颈。
- **整体含义**：本文提出了一种全新的更新压缩思路——利用更新向量内部跨元素的**冗余模式（重复模式）** 进行压缩，而非传统方法中针对单个元素降低精度（量化）或只传输部分元素（稀疏化）。其核心思想是将**广义去重（Generalized Deduplication, GD）** 技术引入联邦学习管道，通过消除数据中的重复基模式来大幅降低通信开销，证明了该方向的可行性。

### 2. 论文提出的方法论
- **核心思想**：将联邦学习中的浮点本地更新转化为固定长度的二进制符号（基），识别并去除这些符号最高有效位中的重复模式。
- **关键技术细节**：
    - **预处理**：将本地更新 $\Delta w_t^u$ 缩放并转化为无符号整数表示，以便于压缩。
    - **固定速率截断**：识别所有更新元素共用的常量比特，并仅为每个元素保留预定义的 $g$ 个非常量最高有效位（MSB），构成**基（base）**。
    - **去重与压缩**：检查所有元素的 $g$-bit MSB 模式，将相同的模式归为一个唯一的基，存储在字典中；然后为每个更新元素分配一个指向字典的基ID。由此，更新向量被表示为一组唯一基和一个基ID序列，从而跳过重复模式的多次传输。
    - **有损压缩**：与传统GD不同，MUFFLe 丢弃了表示细节的偏差位，实现有损近似。这是一种**固定速率、可变计数**的压缩方案。
- **传输成本计算**：总成本为 $n_b \cdot g + d \cdot \lceil \log_2(n_b) \rceil$，其中 $n_b$ 为唯一基的数量，$d$ 为向量维度，$g$ 为基的比特长度。第一项是字典成本，第二项是指向字典的索引成本。

### 3. 实验设计
- **数据集与场景**：MNIST 数据集，以独立同分布方式均匀分配给 `K=20` 个客户端。
- **联邦学习设置**：模型为轻量级多层感知器（MLP，含128单元ReLU隐藏层和10类Softmax输出层）。采用FedAvg算法，每轮随机选择 `C=0.2` 的客户端（即4个）参与，本地训练使用学习率 $\eta=0.01$ 的 SGD 训练 `E=1` 个 epoch。
- **对比 Baselines**：
    - **未压缩 FedAvg**：传输32位浮点更新。
    - **8-bit 量化**：每个元素量化为8比特。
    - **Top-k 稀疏化**：传输绝对值最大的 $k$ 个元素，$k$ 的预算与8-bit量化方案匹配。
- **评估指标**：主要指标是达到目标准确率（92.93%，即未压缩基线在200轮达到的精度）所需的**累计上行通信量**（以MB为单位）。

### 4. 资源与算力
- **文中未明确说明**所使用的GPU型号、数量或具体训练时长。这篇文章强调的是通信成本而非计算算力开销，因此省略了相关计算资源信息。

### 5. 实验数量与充分性
- **实验数量**：本文作为概念验证，实验规模较小。仅在一个数据集、一种模型和一种场景下进行了横向对比实验。
- **充分性与客观性**：
    - 实验固定了随机种子以保证可复现性，对比的 baseline 方法（量化、稀疏化）是常见标准，具有一定代表性。
    - 但实验仅限于IID数据划分，缺乏对联邦学习中更具挑战性的非独立同分布场景的验证，也未包含消融研究（如不同 $g$ 比特长度的影响）。因此，实验的充分性有限，结论的泛化能力有待后续扩展验证。

### 6. 论文的主要结论与发现
- MUFFLe 作为一种通信高效压缩方案是可行的，在相同测试精度下，其通信效率显著优于现有技术。
- **具体数据**：达到92.93%的目标准确率，MUFFLe 仅需 **38 MB** 累计上行通信量，相比未压缩FedAvg（310 MB）减少了 **8.3倍**，相比8-位量化（75 MB）和Top-k稀疏化（86 MB）也减少了 **2倍** 左右。
- 结果表明，挖掘并消除更新向量中重复的跨元素模式，比单纯降低元素精度或筛选部分元素更能节省通信成本。

### 7. 优点
- **创新性强**：开创性地将广义去重技术应用于联邦学习模型更新压缩，开辟了不同于量化和稀疏化的全新方向，利用数据的冗余结构实现压缩。
- **压缩效率显著**：在初步实验中，与传统方法和未压缩情况相比，通信量实现了数量级的降低。
- **方法能与其他方法互补**：论文明确指出MUFFLe与量化和稀疏化等方法*正交*，可以结合使用以追求更高的压缩率，显示出良好的可持续研究潜力。

### 8. 不足与局限
- **实验验证局限性大**：目前仅在简单的MNIST数据集和IID数据划分下得到了概念验证，未在更复杂的非IID场景、更大规模模型（如CNN， ResNet）或更具挑战的数据集上进行评估。
- **缺乏深度分析**：作为进行中的工作，缺少对压缩超参数（如 $g$ 的取值）的敏感性分析，也未分析GD去重效果在不同训练轮次的动态变化。
- **缺少误差补偿机制**：当前为有损压缩，丢弃了所有偏差信息且未应用任何误差反馈机制，可能会对收敛速度和最终精度造成潜在影响，文中尚未进行探索或实验。
- **性能上限不明确**：压缩效果强依赖于更新向量本身的内在冗余度，在不同任务和模型下的鲁棒性和性能上限尚未可知。

（完）
