---
title: "LLM2SMT: Building an SMT Solver with Zero Human-Written Code"
title_zh: LLM2SMT：构建零人工代码的 SMT 求解器
authors: "Mikoláš Janota, Mirek Olšák"
date: 2026-03-06
pdf: "https://arxiv.org/pdf/2603.06931v1"
tags: ["query:ahd"]
score: 8.0
evidence: LLM智能体在零人工代码下构建完整的推理求解器
tldr: 本研究探讨了大型语言模型（LLM）是否能编写具备推理能力的软件。通过LLM代码智能体，在无需人工编写代码的情况下，成功构建了一个完整的DPLL(T)风格的SMT求解器（针对QF_UF理论）。该求解器实现了Nieuwenhuis-Oliveras同余闭包算法，包含预处理功能，并能为不可满足实例生成Lean证明。实验表明，该求解器在SMT-LIB基准测试中具有竞争力，展示了LLM在构建复杂逻辑推理工具方面的潜力。
selection_source: carryover_cache
motivation: 旨在探索LLM是否能够独立编写出自身具备复杂逻辑推理能力的软件系统。
method: 利用LLM代码智能体自动实现包含同余闭包算法、预处理及Lean证明生成的DPLL(T)架构SMT求解器。
result: 成功构建了无需人工干预的QF_UF求解器，其在SMT-LIB标准测试集上表现出具有竞争力的性能。
conclusion: 证明了LLM具备构建复杂、高性能逻辑推理工具的能力，为自动化软件开发开辟了新方向。
---

## 摘要
大语言模型（LLM）是否能够推理或编写软件已引起广泛讨论，但它们是否能编写出自身具备推理能力的软件在很大程度上仍未被探索。我们展示了一个案例研究，其中一个 LLM 编程智能体在零人工代码的情况下，为 QF_UF 构建了一个完整的 DPLL(T) 风格的 SMT 求解器。该求解器实现了 Nieuwenhuis-Oliveras 同余闭包算法，包含预处理功能，并能为不可满足（unsatisfiable）的实例生成 Lean 证明。我们描述了开发过程和关键挑战，并展示了所得求解器在 SMT-LIB 基准测试中具有竞争力。

## Abstract
Whether LLMs can reason or write software is widely debated, but whether they can write software that itself reasons is largely unexplored. We present a case study in which an LLM coding agent builds a complete DPLL(T)-style SMT solver for QF_UF with zero human-written code. The solver implements the Nieuwenhuis-Oliveras congruence closure algorithm, includes preprocessing, and emits Lean proofs for unsatisfiable instances. We describe the development process and key challenges, and show that the resulting solver is competitive on SMT-LIB benchmarks.

---

## 论文详细总结（自动生成）

这篇论文《LLM2SMT: Building an SMT Solver with Zero Human-Written Code》展示了一个极具启发性的案例研究，探讨了大型语言模型（LLM）在构建复杂逻辑推理工具方面的潜力。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：尽管 LLM 在编写通用软件和辅助形式化证明方面表现出色，但它们是否能编写出“自身具备自动化推理能力”的复杂软件（如 SMT 求解器）仍是一个悬而未决的问题。
*   **核心目标**：验证 LLM 编程智能体能否在**零人工编写代码**的前提下，构建一个功能完整、性能达标且能生成形式化证明的 SMT（可满足性模理论）求解器。
*   **研究背景**：SMT 求解器是形式化验证的核心工具，其开发通常需要深厚的逻辑学背景和极高的代码正确性要求。

### 2. 方法论
*   **核心思想**：利用 Claude Code（基于 Sonnet 4.6 模型）作为编程智能体，通过自然语言指令引导其完成从解析器到核心算法的全部开发。
*   **关键技术细节**：
    *   **架构**：采用经典的 **DPLL(T)** 框架。
    *   **SAT 引擎**：通过 `IPASIR-UP` 接口集成现有的 **CaDiCaL** SAT 求解器。
    *   **理论求解器**：实现了 **Nieuwenhuis-Oliveras 同余闭包算法**，用于处理不解构函数理论（QF_UF）。
    *   **预处理**：LLM 自主设计了处理“等式钻石问题”（equational diamond problems）的预处理技术，通过提取不同分支的共同推论来加速求解。
    *   **证明生成**：求解器能输出 **Lean 4** 证明。它利用 Lean 的 `grind` 策略验证理论引理，利用 `bv_decide` 处理命题逻辑部分。
*   **开发流程**：人类仅提供高层指令和参考文献（如 PDF 论文），LLM 负责编写 C++20 代码、配置 CMake、编写解析器（ANTLR）以及调试。

### 3. 实验设计
*   **数据集**：使用 **SMT-LIB** 官方基准测试集中的 QF_UF（无量词不解构函数）非增量问题。
*   **对比方法**：与工业级成熟求解器 **Z3** 和 **cvc5** 进行对比。
*   **消融实验**：
    *   `llm2smt-no-prepro`：禁用预处理模块。
    *   `llm2smt-no-th-prop`：禁用理论传播（Theory Propagation）。
*   **验证机制**：对于 SAT 实例，通过参考求解器验证模型；对于 UNSAT 实例，通过 Lean 编译器验证生成的证明。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备两颗 **AMD EPYC 7513**（共 64 核心）处理器和 **514 GiB RAM** 的服务器上运行。
*   **运行配置**：100 个实例并行运行，每个实例设置 180 秒超时限制。
*   **开发工具**：使用了 Anthropic 的 Claude Code 智能体。文中未提及 LLM 训练的具体算力，仅聚焦于推理和代码生成阶段。

### 5. 实验数量与充分性
*   **实验规模**：测试了 SMT-LIB 中 QF_UF 目录下的全部 **7,500 个** 实例，样本量充足。
*   **充分性**：通过消融实验分析了预处理和理论传播对性能的影响。
*   **客观性**：使用了标准化的第三方基准测试和成熟的对比工具，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **可行性确认**：LLM 能够在零人工代码的情况下构建出具有竞争力的 SMT 求解器。
*   **性能表现**：`llm2smt` 成功解决了 7,468 个实例，非常接近 Z3（7,500）和 cvc5（7,494）的水平。
*   **意外发现**：在某些基准测试中，禁用理论传播反而提升了速度，这表明 LLM 编写的传播调度策略仍有优化空间。
*   **调试能力**：LLM 能够利用模糊测试（Fuzzing）和增量调试（Delta-debugging）工具自主修复复杂的逻辑漏洞。

### 7. 优点
*   **极高的自动化程度**：展示了从阅读学术论文到实现高效算法的端到端自动化能力。
*   **功能完备性**：不仅实现了求解功能，还涵盖了预处理、模型构造和形式化证明生成，超出了简单的代码补全范畴。
*   **实战价值**：证明了 LLM 可以作为快速原型开发和实验新推理技术的强大工具。

### 8. 不足与局限
*   **理论覆盖有限**：目前仅支持 QF_UF 这一种理论，尚未扩展到算术、位向量等更复杂的 SMT 理论。
*   **“参差不齐的智能”（Jagged Intelligence）**：LLM 在处理复杂算法时表现出色，但有时会忽略极简单的逻辑（如 `t=t` 的简化），需要人类细致的提示。
*   **证明生成的脆弱性**：Lean 证明的生成是开发中最困难的部分，由于 Lean 自身的资源限制和 LLM 对证明复杂度的理解不足，导致大量证明验证超时或失败。
*   **依赖专家引导**：虽然代码是零人工，但开发过程仍需要具备 SMT 知识的人类进行高水平的“提示工程”和调试方向引导。

（完）
