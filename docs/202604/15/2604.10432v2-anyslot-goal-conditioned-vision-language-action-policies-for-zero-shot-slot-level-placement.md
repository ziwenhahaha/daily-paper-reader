---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v2"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于精确操作的目标条件视觉-语言-动作（VLA）策略
tldr: AnySlot是一个针对零样本槽位级放置任务的视觉-语言-动作（VLA）框架。针对现有VLA在处理复杂指令和高精度放置时的不足，该框架通过引入显式空间视觉目标（场景标记）作为中间表示，将任务解耦为高层槽位选择和底层目标条件控制。此外，研究者还推出了包含九类任务的SlotBench基准测试。实验证明，AnySlot在零样本槽位放置任务中显著优于现有基准，实现了亚厘米级的执行精度。
selection_source: fresh_fetch
motivation: 现有的单体VLA策略在处理需要高精度和复杂语义理解的槽位级放置任务时，难以兼顾可靠的槽位定位与精确的动作执行。
method: 提出一种分层设计，先将语言指令转化为显式的场景标记作为视觉目标，再由目标条件的VLA策略驱动机器人完成精确放置。
result: 在SlotBench基准测试中，AnySlot在零样本场景下的表现显著优于传统的扁平化VLA模型和现有的模块化定位方法。
conclusion: 通过引入中间视觉目标来解耦语义理解与动作执行，能有效提升机器人策略在复杂、高精度放置任务中的泛化能力和鲁棒性。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。插槽级任务既需要可靠的插槽定位，也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，这是一个通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低组合复杂性的框架。AnySlot 通过生成场景标记将语言转化为显式的视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置方面显著优于扁平化 VLA 基准模型和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **AnySlot** 的框架，旨在解决机器人操作中极具挑战性的“插槽级放置”（Slot-level Placement）问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **核心挑战**：现有的视觉-语言-动作（VLA）策略在处理复杂的组合指令（如“将方块放入第二大的插槽”）和高精度放置（亚厘米级误差）时表现不佳。
*   **瓶颈分析**：
    *   **扁平化（Flat）策略**：将语义理解与动作控制耦合在一起，难以在未见过的复杂布局中同时实现准确的语义定位和精确的几何对齐。
    *   **模块化（Modular）策略**：通常依赖视觉语言模型（VLM）输出坐标，但坐标回归往往不够精确，且丢失了插槽边界等几何上下文。
*   **研究目标**：通过引入显式的“视觉目标”作为中间表示，解耦高层推理与底层控制，实现零样本（Zero-shot）下的高精度插槽放置。

### 2. 方法论：AnySlot 框架
AnySlot 采用两阶段分层设计：
*   **第一阶段：高层目标构建（视觉定位）**
    *   **核心思想**：利用现成的图像生成模型（如 Nano-Banana）根据语言指令在原始图像中渲染一个“蓝色球体”标记。
    *   **技术细节**：通过深度图将 2D 标记中心反投影到 3D 空间，形成世界坐标系下的锚点。随后，利用相机外参将该锚点投影到所有相机视角（如头部相机、手腕相机），生成视角一致的视觉目标覆盖层（Overlay）。
*   **第二阶段：底层目标条件化策略（动作执行）**
    *   **核心思想**：使用一个以目标为条件的 VLA 策略（基于 $\pi_{0.5}$，采用 PaliGemma-3B 骨干网络）。
    *   **执行逻辑**：该策略不再需要理解复杂的空间推理指令，而是接收固定的指令（如“根据蓝球引导移动物体”），专注于将物体精确放置到视觉标记指示的位置。

### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准 SlotBench，包含 9 类空间推理任务：序数推理、尺寸比较、高度比较、距离推理、组合关系、逻辑否定、模糊语言、功能性推理（Affordance）和常识推理。
*   **对比方法**：
    *   **扁平化 VLA**：Diffusion Policy, OpenVLA, $\pi_0, \pi_{0.5}$。
    *   **模块化方法**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle 基准**：由人类专家提供完美视觉目标的上限测试。
*   **评估指标**：成功率（SR）、指令准确率（IA，误差 < 0.02m）、粗略准确率（CA）。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA H200 GPU**。
*   **训练细节**：对底层策略 $\pi_{0.5}$ 进行了全参数微调，训练步数为 **20,000 步**，Batch Size 为 64。
*   **数据**：使用了在 SAPIEN 仿真器中收集的合成数据集 $D_{syn}$，包含随机化的物体位姿、尺寸和插槽几何形状。

### 5. 实验数量与充分性
*   **实验规模**：每个任务类别进行 50 次试验，总计评估了数百次任务。
*   **消融实验**：
    *   对比了不同的高层定位模块（GPT-4o, Gemini, 不同版本的图像生成模型）。
    *   对比了不同的底层策略架构（Diffusion Policy vs. VLA）。
    *   验证了合成数据集对训练的重要性。
*   **充分性评价**：实验设计非常全面，涵盖了从纯语义推理到纯几何控制的多个维度，并进行了真实世界的机器人部署验证，证明了方法的客观性和公平性。

### 6. 主要结论与发现
*   **AnySlot 性能卓越**：在 SlotBench 的 9 类任务中，AnySlot 实现了近 **90% 的平均成功率**，而传统的扁平化 VLA 几乎全部失败。
*   **视觉目标优于坐标回归**：图像生成模型提供的空间标记比 VLM 输出的坐标更具鲁棒性，能更好地引导底层策略进行闭环控制。
*   **解耦是关键**：将“去哪里（推理）”和“怎么去（控制）”分开，显著提升了模型在未见场景下的零样本泛化能力。

### 7. 优点与亮点
*   **创新表示**：将语言定位转化为“图像编辑/生成”任务，利用了生成模型强大的空间常识。
*   **高精度**：通过多视角一致的视觉覆盖层，实现了亚厘米级的放置精度。
*   **零样本能力**：无需针对特定插槽布局进行微调，即可处理复杂的组合逻辑指令。
*   **新基准**：SlotBench 填补了机器人领域缺乏高精度插槽级放置评测工具的空白。

### 8. 不足与局限
*   **推理延迟**：依赖外部图像生成模型会引入额外的计算开销，影响实时性（Nano-Banana 2 约需 11 秒生成目标）。
*   **依赖深度信息**：第一阶段的目标构建高度依赖准确的深度图和相机标定，在透明或高反光物体上可能失效。
*   **长程任务限制**：目前主要集中在单步放置任务，尚未扩展到更复杂的多阶段长程操作。

（完）
