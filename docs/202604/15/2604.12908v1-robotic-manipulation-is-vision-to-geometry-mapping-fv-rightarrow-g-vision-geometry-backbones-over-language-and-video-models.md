---
title: "Robotic Manipulation is Vision-to-Geometry Mapping ($f(v) \\rightarrow G$): Vision-Geometry Backbones over Language and Video Models"
title_zh: 机器人操纵是视觉到几何的映射 ($f(v) \rightarrow G$)：优于语言和视频模型的视觉-几何骨干网络
authors: "Zijian Song, Qichang Li, Jiawei Zhou, Zhenlong Yuan, Tianshui Chen, Liang Lin, Guangrun Wang"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12908v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 对VLA模型的批判并提出用于操作的视觉-几何主干网络
tldr: "本研究提出机器人操纵的核心是视觉到几何的映射（f(v) -> G）。针对现有VLA和视频模型依赖2D语义先验而缺乏3D几何一致性的问题，提出了VGA（Vision-Geometry-Action）模型。该模型利用预训练的3D世界模型作为骨干，通过渐进式体积调制模块将视觉输入直接映射为物理动作。实验证明，VGA在仿真和真实场景中均优于主流模型，尤其在跨视角泛化方面表现卓越，展示了原生3D表示在物理智能中的潜力。"
selection_source: fresh_fetch
motivation: 现有的视觉语言或视频模型主要依赖2D语义或时间像素先验，难以捕捉物理操纵所需的精确3D几何属性。
method: 提出VGA模型，利用预训练3D世界模型作为骨干，并结合渐进式体积调制模块和联合训练策略实现视觉到动作的直接映射。
result: VGA在仿真测试中优于pi_0.5和GeoVLA，并在真实世界任务中展现出卓越的跨视角零样本泛化性能。
conclusion: 采用原生3D表示而非语言或2D视频先验进行视觉-几何映射，是实现高泛化性机器人控制的有效方向。
---

## 摘要
机器人操纵的核心是一个视觉到几何映射（$f(v) \rightarrow G$）的问题。物理动作从根本上由 3D 位置和空间关系等几何属性定义。因此，我们认为通用机器人控制的基础应该是视觉-几何骨干网络，而非广泛采用的视觉-语言或视频模型。传统的 VLA（视觉-语言-动作）和视频预测模型依赖于在大规模 2D 图像-文本或时序像素数据上预训练的骨干网络。尽管这些模型有效，但其表示很大程度上受语义概念或 2D 先验的影响，在本质上并不符合物理操纵所需的精确 3D 几何特性。基于这一见解，我们提出了视觉-几何-动作（VGA）模型，该模型直接基于预训练的原生 3D 表示来生成动作。具体而言，VGA 用预训练的 3D 世界模型取代了传统的语言或视频骨干网络，建立了无缝的视觉到几何映射，将视觉输入直接转化为物理动作。为了进一步增强几何一致性，我们引入了渐进式体素调制（Progressive Volumetric Modulation）模块并采用了联合训练策略。广泛的实验验证了我们方法的有效性。在仿真基准测试中，VGA 的表现优于包括 $π_{0.5}$ 和 GeoVLA 在内的顶级 VLA 基线模型，证明了其在精确操纵方面的优越性。更重要的是，VGA 在真实世界的部署中对未见过的视角表现出卓越的零样本泛化能力，持续优于 $π_{0.5}$。这些结果表明，基于原生 3D 表示进行操作——而非通过语言或 2D 视频先验进行转换——是实现通用物理智能的一个极具前景的方向。

## Abstract
At its core, robotic manipulation is a problem of vision-to-geometry mapping ($f(v) \rightarrow G$). Physical actions are fundamentally defined by geometric properties like 3D positions and spatial relationships. Consequently, we argue that the foundation for generalizable robotic control should be a vision-geometry backbone, rather than the widely adopted vision-language or video models. Conventional VLA and video-predictive models rely on backbones pretrained on large-scale 2D image-text or temporal pixel data. While effective, their representations are largely shaped by semantic concepts or 2D priors, which do not intrinsically align with the precise 3D geometric nature required for physical manipulation. Driven by this insight, we propose the Vision-Geometry-Action (VGA) model, which directly conditions action generation on pretrained native 3D representations. Specifically, VGA replaces conventional language or video backbones with a pretrained 3D world model, establishing a seamless vision-to-geometry mapping that translates visual inputs directly into physical actions. To further enhance geometric consistency, we introduce a Progressive Volumetric Modulation module and adopt a joint training strategy. Extensive experiments validate the effectiveness of our approach. In simulation benchmarks, VGA outperforms top-tier VLA baselines including $π_{0.5}$ and GeoVLA, demonstrating its superiority in precise manipulation. More importantly, VGA exhibits remarkable zero-shot generalization to unseen viewpoints in real-world deployments, consistently outperforming $π_{0.5}$. These results highlight that operating on native 3D representations-rather than translating through language or 2D video priors-is a highly promising direction for achieving generalizable physical intelligence.

---

## 论文详细总结（自动生成）

这是一份关于论文《Robotic Manipulation is Vision-to-Geometry Mapping ($f(v) \rightarrow G$): Vision-Geometry Backbones over Language and Video Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机）
论文指出，机器人操纵的本质是**视觉到几何的映射（$f(v) \rightarrow G$）**。当前的视觉-语言-动作（VLA）模型和视频预测模型主要依赖于在 2D 图像-文本或时序像素数据上预训练的骨干网络。
*   **研究动机**：2D 骨干网络受限于语义概念或像素变化，缺乏对 3D 空间现实的内在理解。这导致了“3D-2D-3D”的信息瓶颈，即丰富的 3D 几何信息被压缩进扁平的 2D 潜空间，限制了模型在复杂环境下的泛化能力。
*   **核心目标**：建立一个直接在原生 3D 表示空间内进行感知、推理和动作生成的机器人基础模型。

### 2. 方法论：VGA 模型
论文提出了 **Vision-Geometry-Action (VGA)** 模型，其核心思想是利用 3D 世界模型作为控制的基础。
*   **核心骨干（VGGT）**：采用预训练的视觉几何接地变换器（VGGT）作为骨干网络，通过交替注意力机制（帧内局部注意力和跨帧全局注意力）提取原生 3D 表示。
*   **渐进式体积调制（PVM）**：为了解决 3D 表示与动作头之间的有效融合，引入 PVM 模块。它通过多层交叉模式转导，将视觉-语言条件和动作查询进行深度对齐，确保几何信息高保真地流向动作生成过程。
*   **联合训练与解耦推理**：
    *   **训练阶段**：模型同时预测机器人动作、相机参数（旋转、平移、焦距）和像素级深度图。这种多任务学习增强了表示的几何一致性。
    *   **推理阶段**：为了保证实时性，解耦并跳过几何预测头，仅保留动作头进行高效执行。

### 3. 实验设计
*   **仿真实验**：在 **LIBERO** 基准测试上进行，涵盖四个子集：LIBERO-Spatial（空间关系）、LIBERO-Object（物体泛化）、LIBERO-Goal（任务组合）和 LIBERO-Long（长程任务）。
*   **真实世界实验**：使用 Franka Panda 机械臂，执行“抓取方块”、“按按钮”和“叠方块”任务。
*   **对比基线**：
    *   **VLA 模型**：$\pi_{0.5}$、OpenVLA、Octo、UniVLA 等。
    *   **3D 增强模型**：SpatialVLA、GeoVLA、GeoAwareVLA。
    *   **世界动作模型（WAM）**：WorldVLA、UniMimic、Motus 等。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100-SXM4-80GB GPU**。
*   **训练时长**：最长的训练任务在 **60 GPU 小时**内完成。
*   **模型规模**：可训练参数约为 **500M**（总参数约 3.6B，大部分冻结或使用 LoRA 微调）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真中对 4 个套件共 40 个任务进行了 500 次 Rollouts。
    *   真实世界中每个任务进行 20 次试验，并特别设计了**跨视角（Out-of-Distribution）**泛化测试。
    *   进行了详尽的消融实验，验证了 PVM 模块、联合训练、LoRA 秩以及预训练权重的影响。
*   **评价**：实验设计较为充分且客观。通过对比不同类别的基线（2D VLA vs 3D VLA vs WAM），清晰地展示了 VGA 在空间精度上的优势。

### 6. 主要结论与发现
*   **空间精度更高**：VGA 在 LIBERO 仿真中平均成功率达到 98.1%，优于所有对比基线，证明了 3D 骨干网络在精确操纵中的优越性。
*   **卓越的跨视角泛化**：在真实世界中，VGA 在未见过的相机视角下表现出极强的零样本泛化能力，成功率比 $\pi_{0.5}$ 高出 6%。
*   **联合训练的价值**：辅助的几何预测任务（深度、相机参数）显著提升了动作预测的准确性和数据效率。

### 7. 优点（亮点）
*   **范式创新**：挑战了主流的“语言/视频为中心”的机器人控制思路，回归“几何为中心”的本质。
*   **架构高效**：通过解耦推理，在保持 3D 感知能力的同时，实现了约 10Hz 的实时控制频率。
*   **无需额外传感器**：虽然是 3D 驱动，但推理时仅需 RGB 输入，无需依赖昂贵或高噪声的深度传感器。

### 8. 不足与局限
*   **长程任务挑战**：在 LIBERO-Long 任务中，VGA 虽然表现优秀，但相比某些在大规模序列数据上预训练的 VLM 模型，其处理极长时序依赖的能力仍有提升空间。
*   **语义推理限制**：由于骨干网络并非 LLM，VGA 在处理需要复杂常识推理的指令（如“把方块放在泰勒·斯威夫特的照片上”）时可能不如传统 VLA 模型。
*   **数据依赖**：虽然使用了预训练 3D 模型，但仍需要一定量的任务演示数据进行微调。

（完）
