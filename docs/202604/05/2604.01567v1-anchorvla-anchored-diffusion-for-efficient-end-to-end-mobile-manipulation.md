---
title: "AnchorVLA: Anchored Diffusion for Efficient End-to-End Mobile Manipulation"
title_zh: AnchorVLA：用于高效端到端移动操作的锚定扩散模型
authors: "Jia Syuen Lim, Zhizhen Zhang, Peter Bohm, Brendan Tidd, Zi Huang, Yadan Luo"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01567v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于移动操作的基于扩散的 VLA 策略
tldr: 针对移动操作中扩散策略推理开销大以及动作分块导致的累积漂移问题，本文提出AnchorVLA。该方法结合轻量级VLA骨干网络与锚定扩散动作头，通过在锚定轨迹附近进行局部截断去噪，在保留多模态生成能力的同时大幅降低推理延迟。此外，引入轻量级残差修正模块实现高频自纠偏，增强了机器人在动态环境下的闭环控制能力。
selection_source: fresh_fetch
motivation: 旨在解决扩散策略在移动操作中推理效率低以及动作分块带来的计算开销与执行漂移之间的矛盾。
method: 采用锚定扩散机制在候选轨迹附近进行局部截断去噪，并利用残差修正模块进行高频、每步的动作微调。
result: 在多样化的移动操作任务中，AnchorVLA在保持低延迟推理的同时，显著提升了应对环境干扰和分布偏移的成功率。
conclusion: AnchorVLA证明了通过局部去噪和实时纠偏可以实现高效、鲁棒且具备多模态能力的端到端移动操作控制。
---

## 摘要
移动操作中的一个核心挑战是在执行过程中保持反应性的同时，保留多种合理的动作模型。在杂乱场景中，抓取瓶子通常有多种有效方式。鲁棒的行为取决于在场景演变时保留这种动作多样性并保持反应性。扩散策略（Diffusion policies）因其能够建模多模态动作分布而非坍缩至单一解而备受关注。但在实践中，完整的迭代去噪在控制时的计算成本很高。动作分块（Action chunking）虽有助于分摊推理开销，但也会导致部分开环行为，使得微小误差累积成漂移。我们提出了 AnchorVLA，这是一种基于扩散的移动操作 VLA 策略。其核心见解是：当采样从合理候选解流形附近开始时，无需进行大量的去噪即可恢复多模态的有效动作。AnchorVLA 将轻量级 VLA 适配主干网络与锚定扩散动作头相结合，利用截断扩散调度在锚定轨迹周围进行局部去噪。这在保留多模态动作生成能力的同时，降低了闭环控制的推理成本。关键在于，为减轻分块诱发的漂移，我们通过轻量级残差修正模块引入了测试时自校正机制，在执行过程中进行高频的逐步调整。在多种移动操作任务中，AnchorVLA 在保持低延迟推理的同时，提升了在扰动和分布偏移下的成功率与稳定性。源代码已在 https://github.com/jason-lim26/AnchorVLA 开源。

## Abstract
A central challenge in mobile manipulation is preserving multiple plausible action models while remaining reactive during execution. A bottle in a cluttered scene can often be approached and grasped in multiple valid ways. Robust behavior depends on preserving this action diversity while remaining reactive as the scene evolves. Diffusion policies are appealing because they model multimodal action distributions rather than collapsing to one solution. But in practice, full iterative denoising is costly at control time. Action chunking helps amortize inference, yet it also creates partially open-loop behavior, allowing small mismatches to accumulate into drift. We present AnchorVLA, a diffusion-based VLA policy for mobile manipulation built on the core insight that when sampling begins near a plausible solution manifold, extensive denoising is unnecessary to recover multimodal, valid actions. AnchorVLA combines a lightweight VLA adaptation backbone with an anchored diffusion action head, which denoises locally around anchor trajectories using a truncated diffusion schedule. This retains multimodal action generation while reducing inference cost for closed-loop control. Crucially, to mitigate chunking-induced drift, we introduce a test-time self-correction mechanism via a lightweight residual correction module that makes high-frequency, per-step adjustments during rollout. Across diverse mobile manipulation tasks, AnchorVLA improves success and stability under disturbances and distribution shifts while maintaining low-latency inference. The source code is made available at https://github.com/jason-lim26/AnchorVLA.

---

## 论文详细总结（自动生成）

# AnchorVLA: 用于高效端到端移动操作的锚定扩散模型论文总结

### 1. 核心问题与整体含义（研究动机和背景）
移动操作（Mobile Manipulation）要求机器人同时协调移动底盘和机械臂，在复杂动态环境中完成任务。其核心挑战在于：
*   **多模态性（Multimodality）：** 同一个指令（如“抓取瓶子”）存在多种有效的执行路径和姿态，传统的回归模型（如 $L_1$ 回归）容易产生“众数平均”效应，导致生成的动作无效。
*   **推理延迟与稳定性的矛盾：** 扩散模型（Diffusion Policy）虽能建模多模态分布，但全迭代去噪计算开销巨大；而常用的“动作分块（Action Chunking）”技术虽然能提高吞吐量，却会引入开环执行带来的累积漂移（Drift）。
*   **研究动机：** 开发一种既能保留多模态表达能力，又能实现低延迟闭环控制，且对执行漂移具有鲁棒性的 VLA（视觉-语言-动作）策略。

### 2. 论文提出的方法论
AnchorVLA 结合了轻量级 VLA 骨干网络与创新的动作生成头，核心思想是**“在专家先验附近进行局部精炼”**。
*   **锚定截断扩散（Anchored Truncated Diffusion）：** 
    *   首先通过对专家演示轨迹进行聚类，构建一个“轨迹词汇表（Trajectory Vocabulary）”作为锚点（Anchors）。
    *   推理时，不再从纯高斯噪声开始去噪，而是从这些锚点出发，在缩短的扩散调度（Truncated Schedule）内进行局部去噪。这极大地减少了采样步数（如从 50 步降至 5-10 步）。
*   **锚点评分头（Anchor Scoring Head）：** 
    *   模型同时对多个锚点进行去噪，并利用评分头预测每个候选轨迹与当前上下文的匹配得分，从而选择最优的动作分块。
*   **测试时残差修正模块（Residual Correction Module）：** 
    *   为了解决动作分块导致的开环漂移，引入了一个极轻量级的 MLP 模块。在执行分块动作的每一步，该模块根据实时观测对预定义的宏观轨迹进行微小的闭环调整（微调），确保执行精度。

### 3. 实验设计
*   **仿真环境：** 使用 **ManiSkill-HAB** 基准测试，包含 6 个复杂的移动操作任务（如：Pick/Place Apple/Bowl, Open Fridge, Close Counter）。
*   **真实世界：** 部署在 **Unitree Go2 Edu 四足机器人**上，配备 SO101 机械臂。四足底盘的振动和漂移为测试自纠偏机制提供了极佳的压力测试环境。
*   **对比基准（Baselines）：**
    *   基础模仿学习：ACT, Diffusion Policy (DP)。
    *   3D/大模型基准：DP3, $\pi_0$, RDT-1B。
    *   SOTA 移动操作模型：AC-DiT（基于点云的扩散 Transformer）。

### 4. 资源与算力
*   **模型规模：** 骨干网络采用 **Qwen-2.5 (0.5B)**，使用 LoRA（rank=64）进行参数高效微调。
*   **训练细节：** 训练步数为 100k 梯度步，Batch Size 为 16。
*   **算力说明：** 论文**未明确提及具体的 GPU 型号和数量**，但强调了推理效率。通过动作分块（$H=5$），相比于每步查询 VLA，计算量（TFLOPs）减少了约 **80%**。

### 5. 实验数量与充分性
*   **实验规模：** 仿真实验涵盖 6 类任务，每类任务进行 3 次独立试验，每次试验包含 100 个随机初始化的回合，提供了均值和标准差，具有统计学意义。
*   **消融实验：** 针对“锚点先验的影响”、“残差修正模块的有效性”以及“动作分块长度 $H$ 的权衡”进行了详细对比。
*   **公平性：** 在相同输入（RGB）和相似参数规模下对比了多种 SOTA 模型，并验证了模型在长执行周期下的稳定性。实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能领先：** AnchorVLA 在 ManiSkill-HAB 上取得了 **64.0%** 的平均成功率，显著优于 AC-DiT（55.6%）和 RDT（42.9%）。
*   **效率与鲁棒性：** 即使在较长的动作分块（$H=5$ 或 $H=8$）下，AnchorVLA 依然能保持稳定性能，而传统的 $L_1$ 回归模型在 $H>2$ 时性能会剧烈下降。
*   **纠偏能力：** 残差修正模块在仅增加 57K 参数的情况下，将任务成功率提升了约 9.3%，证明了高频微调对移动操作的重要性。

### 7. 优点
*   **创新的锚定机制：** 巧妙地利用专家轨迹聚类来缩小扩散搜索空间，解决了扩散模型推理慢的痛点。
*   **双层控制架构：** 将“高层多模态规划（扩散头）”与“底层高频纠偏（残差模块）”分离，兼顾了智能性与反应性。
*   **端到端协同：** 实现了底盘与机械臂的统一动作空间控制，而非简单的分级解耦。

### 8. 不足与局限
*   **数据依赖：** 性能高度依赖于离线专家演示数据的覆盖范围，对于超出分布（OOD）场景的泛化能力可能受限。
*   **缺乏在线恢复：** 目前主要基于模仿学习，如果机器人发生严重碰撞或陷入死循环，缺乏类似强化学习那样的在线探索和自我恢复机制。
*   **硬件限制：** 真实世界实验规模相对较小（2个任务，各10次测试），且主要针对特定硬件组合（Go2 + SO101），跨形态泛化能力有待进一步验证。

（完）
