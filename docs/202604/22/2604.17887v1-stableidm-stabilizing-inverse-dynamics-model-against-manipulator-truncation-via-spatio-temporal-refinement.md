---
title: "StableIDM: Stabilizing Inverse Dynamics Model against Manipulator Truncation via Spatio-Temporal Refinement"
title_zh: StableIDM：通过时空细化稳定应对机械臂截断的逆动力学模型
authors: "Kerui Li, Zhe Jing, Xiaofeng Wang, Zheng Zhu, Yukun Zhou, Guan Huang, Dongze Li, Qingkai Yang, Huaibo Huang"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17887v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 具身智能策略执行中的逆动力学模型
tldr: StableIDM 针对具身智能中逆动力学模型（IDM）在机械臂截断（部分观测）下性能下降的问题，提出了一种时空细化框架。该框架通过机器人中心掩码、定向特征聚合（DFA）和时间动力学细化（TDR）来增强空间推理和运动连续性。实验证明，StableIDM 在动作预测准确率、真实机器人任务成功率以及下游 VLA 数据标注质量上均有显著提升，为具身智能提供了稳健的执行与标注基础。
selection_source: fresh_fetch
motivation: 现有的逆动力学模型在机械臂因截断导致部分观测时，面临状态恢复困难和控制不稳定的严峻挑战。
method: 该框架集成了机器人中心掩码、用于几何感知空间推理的定向特征聚合（DFA）以及用于平滑预测的时间动力学细化（TDR）。
result: "实验显示 StableIDM 在严重截断下的动作准确率提升了 12.1%，真实机器人任务成功率提升 9.7%，并显著优化了 VLA 的标注成功率。"
conclusion: StableIDM 证明了通过时空细化可以有效增强 IDM 的鲁棒性，为具身智能的策略执行和数据生成提供了可扩展的解决方案。
---

## 摘要
逆动力学模型 (IDM) 将视觉观测映射到低级动作指令，是具身智能中数据标注和策略执行的核心组件。然而，在机械臂截断（一种常见的失效模式）下，其性能会严重下降，这使得状态恢复变得病态并导致控制不稳定。我们提出了 StableIDM，这是一个时空框架，通过细化视觉输入的特征，在上述部分可观测性下稳定动作预测。StableIDM 集成了三个互补组件：(1) 辅助的以机器人为中心的掩码，用于抑制背景杂波；(2) 用于几何感知空间推理的方向特征聚合 (DFA)，它沿着从可见机械臂推断出的方向提取各向异性特征；(3) 时序动力学细化 (TDR)，通过运动连续性来平滑和修正预测。广泛的评估验证了我们的方法：在 AgiBot 基准测试的严重截断情况下，StableIDM 将严格动作准确率提高了 12.1%，并在真机回放中将平均任务成功率提高了 9.7%。此外，在解码视频生成的规划时，它将端到端抓取成功率提升了 11.5%；作为自动标注器时，它将下游 VLA 真机成功率提升了 17.6%。这些结果表明，StableIDM 为具身智能中的策略执行和数据生成提供了鲁棒且可扩展的主干网络。

## Abstract
Inverse Dynamics Models (IDMs) map visual observations to low-level action commands, serving as central components for data labeling and policy execution in embodied AI. However, their performance degrades severely under manipulator truncation, a common failure mode that makes state recovery ill-posed and leads to unstable control. We present StableIDM, a spatio-temporal framework that refines features from visual inputs to stabilize action predictions under such partial observability. StableIDM integrates three complementary components: (1) auxiliary robot-centric masking to suppress background clutter, (2) Directional Feature Aggregation (DFA) for geometry-aware spatial reasoning, which extracts anisotropic features along directions inferred from the visible arm and (3) Temporal Dynamics Refinement (TDR) to smooth and correct predictions via motion continuity. Extensive evaluations validate our approach: StableIDM improves strict action accuracy by 12.1% under severe truncation on the AgiBot benchmark, and increases average task success by 9.7% in real-robot replay. Moreover, it boosts end-to-end grasp success by 11.5% when decoding video-generated plans, and improves downstream VLA real-robot success by 17.6% when functioning as an automatic annotator. These results demonstrate that StableIDM provides a robust and scalable backbone for both policy execution and data generation in embodied artificial intelligence.

---

## 论文详细总结（自动生成）

### StableIDM：通过时空细化稳定应对机械臂截断的逆动力学模型

#### 1. 论文的核心问题与整体含义
逆动力学模型（IDM）在具身智能中起着桥梁作用，负责将视觉观测（图像/视频）映射为机器人可执行的动作指令。然而，现有的 IDM 在面对**机械臂截断（Manipulator Truncation）**时表现极差——即当机械臂部分移出相机视野时，模型难以从残缺的视觉信息中推断完整的运动状态。这导致动作预测不稳定、控制精度下降。论文的核心动机是提出一种能够处理部分观测、具备时空推理能力的鲁棒 IDM 框架，以支持大规模数据标注和可靠的策略执行。

#### 2. 论文提出的方法论
StableIDM 采用了一个三阶段的时空特征细化流水线：
*   **机器人中心掩码（Robot-centric Masking）：** 利用 Segment Anything (SAM) 提取机械臂掩码，抑制背景杂波。这确保了模型在机械臂严重截断、背景占比增大时，仍能聚焦于关键的运动部件。
*   **定向特征聚合（Directional Feature Aggregation, DFA）：** 针对单目 RGB 缺乏深度信息的问题，DFA 利用机械臂的几何轴向特性，通过方向感知算子提取各向异性特征。它利用全局上下文（CLS token）动态调整不同方向特征的权重，补偿因截断导致的几何模糊。
*   **时序动力学细化（Temporal Dynamics Refinement, TDR）：**
    *   **时序融合（Temporal Fusion）：** 在 DFA 之前，通过相邻帧的特征对齐和可见性门控，利用历史信息“修复”当前帧缺失的几何结构。
    *   **时序回归器（Temporal Regressor）：** 在 DFA 之后，利用因果时间卷积网络（TCN）分析运动趋势，平滑动作输出，确保运动学的一致性。

#### 3. 实验设计
*   **数据集与场景：** 使用了 **AgiBot World** 基准测试，包含 100 个情节，涵盖 10 种日常操作任务（如微波炉操作、水槽清理、桌面取放）。
*   **评估维度：** 实验分为四个维度：离线动作预测（区分轻度/重度截断）、真实机器人回放（验证物理执行稳定性）、视频规划部署（配合视频生成模型执行）、以及作为 VLA 数据标注器的质量验证。
*   **对比方法：** 包括标准的 **ResNet-50**、方向感知的 **AnyPos** 以及针对遮挡优化的 **Vidar**。

#### 4. 资源与算力
论文中**未明确说明**具体的 GPU 型号、数量或训练时长。但提到使用了预训练的 DINOv2 作为视觉骨干网络，并采用了端到端的优化方式。

#### 5. 实验数量与充分性
论文进行了较为充分且多维度的实验：
*   **离线测试：** 针对不同截断比例进行了细分对比。
*   **消融实验：** 逐一验证了掩码、DFA 和 TDR 模块的贡献。
*   **真实世界验证：** 在物理机器人上进行了三种复杂场景（取放、微波炉、水槽）的测试。
*   **下游应用：** 验证了该模型对 VLA（Pi-0.5）模型训练的增益。
实验设计客观、公平，通过设置统一的输入模态、动作参数化和损失函数，确保了架构改进是性能提升的主因。

#### 6. 论文的主要结论与发现
*   **鲁棒性显著提升：** 在严重截断的情况下，StableIDM 的动作预测准确率比现有方法提升了 12.1%，L1 误差显著降低。
*   **真机执行更稳定：** 在真实机器人任务中，平均成功率提升了 9.7%，尤其在受限空间（如水槽）表现优异。
*   **数据标注价值：** 使用 StableIDM 自动标注生成的视频数据来训练 VLA 模型，可使下游任务成功率提升 17.6%，证明了其作为“数据引擎”的潜力。

#### 7. 优点
*   **时空协同：** 巧妙地结合了空间几何推理（DFA）和时序动态修复（TDR），解决了单帧 IDM 无法处理的病态恢复问题。
*   **实用性强：** 既能作为离线标注工具，也能作为在线执行策略，且具有固定的推理延迟，适合实时控制。
*   **模块化设计：** 掩码和特征细化模块可以较容易地集成到其他视觉骨干网络中。

#### 8. 不足与局限
*   **任务范围：** 目前主要针对单步或短程任务执行，对于需要极强顺序推理的长程多步操作支持尚不明确。
*   **解耦误差：** 视频生成模型与 IDM 是解耦训练的，这可能导致合成视频的伪影与动作解码之间存在表示不匹配（Representation Mismatch）。
*   **依赖掩码质量：** 虽然实验证明其对掩码噪声有一定鲁棒性，但在极端复杂的视觉环境下，SAM 的分割质量仍可能成为瓶颈。

（完）
