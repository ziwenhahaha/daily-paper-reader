---
title: "Seeing Isn't Believing: Mitigating Belief Inertia via Active Intervention in Embodied Agents"
title_zh: 眼见不一定为实：通过具身智能体中的主动干预缓解信念惯性
authors: "Hanlin Wang, Chak Tou Leong, Jian Wang, Wenjie Li"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17252v1"
tags: ["query:q12"]
score: 9.0
evidence: 具身智能体的主动干预以减轻信念惯性
tldr: 具身智能体在执行任务时常因“信念惯性”而忽视与先验不符的环境反馈，导致决策失误。本文提出EVU（预测-验证-更新）机制，通过主动干预将智能体从被动理解转为主动管理信念。该机制包含预测结果、验证差异及更新信念三个步骤，可集成至提示词或训练方法中。实验证明，EVU在三大具身基准测试中显著提升了成功率，有效缓解了信念惯性。
selection_source: fresh_fetch
motivation: 具身智能体在面对与内部信念冲突的环境反馈时，往往表现出顽固坚持错误先验的“信念惯性”问题。
method: 引入EVU机制，通过显式的“预测-验证-更新”流程，使智能体能够主动识别并修正与现实不符的内部信念。
result: 在三个主流具身智能基准测试中，EVU机制均显著提升了任务成功率，验证了其在复杂环境下的有效性。
conclusion: 主动信念干预是构建鲁棒具身智能体的关键，EVU为解决智能体认知偏差提供了通用的推理框架。
---

## 摘要
大语言模型（LLMs）的最新进展使智能体能够通过环境交互处理复杂的具身任务。然而，这些智能体仍然会做出次优决策并执行无效动作，因为它们经常忽视与内部信念不一致的关键环境反馈。通过正式的探测分析，我们将此特征化为“信念惯性”（belief inertia），即智能体尽管有明确的观察结果，却仍固执地坚持先验信念的现象。为了解决这一问题，我们提倡主动信念干预，从被动理解转向主动管理。我们引入了“估计-验证-更新”（EVU）机制，该机制赋予智能体预测预期结果、通过显式推理对照观察结果进行验证，并根据验证证据主动更新先验信念的能力。EVU 被设计为一种统一的干预机制，能够显式生成文本信念状态，并可集成到基于提示（prompting-based）和基于训练（training-based）的智能体推理方法中。在三个具身基准测试上的广泛实验表明，EVU 持续显著提高了任务成功率。进一步的分析验证了我们的方法有效缓解了信念惯性，推动了更鲁棒的具身智能体的发展。我们的代码可在 https://github.com/WangHanLinHenry/EVU 获取。

## Abstract
Recent advancements in large language models (LLMs) have enabled agents to tackle complex embodied tasks through environmental interaction. However, these agents still make suboptimal decisions and perform ineffective actions, as they often overlook critical environmental feedback that differs from their internal beliefs. Through a formal probing analysis, we characterize this as belief inertia, a phenomenon where agents stubbornly adhere to prior beliefs despite explicit observations. To address this, we advocate active belief intervention, moving from passive understanding to active management. We introduce the Estimate-Verify-Update (EVU) mechanism, which empowers agents to predict expected outcomes, verify them against observations through explicit reasoning, and actively update prior beliefs based on the verification evidence. EVU is designed as a unified intervention mechanism that generates textual belief states explicitly, and can be integrated into both prompting-based and training-based agent reasoning methods. Extensive experiments across three embodied benchmarks demonstrate that EVU consistently yields substantial gains in task success rates. Further analyses validate that our approach effectively mitigates belief inertia, advancing the development of more robust embodied agents. Our code is available at https://github.com/WangHanLinHenry/EVU.

---

## 论文详细总结（自动生成）

### 论文总结：眼见不一定为实——通过主动干预缓解具身智能体中的信念惯性

#### 1. 核心问题与整体含义（研究动机和背景）
具身智能体（Embodied Agents）在执行复杂任务时，通常依赖“感知-推理-行动”的循环。然而，研究者发现智能体普遍存在**“观察忽视”（Observational Neglect）**现象：即使环境反馈已经显示状态发生了变化（例如：已经看到了目标物体），智能体仍会执行冗余的搜索动作。
论文通过探测分析（Probing Analysis）揭示了这一现象的根源——**“信念惯性”（Belief Inertia）**。这是一种认知偏差，指智能体固执地坚持其对动作结果的先验预期，而忽视了与之矛盾的实际观察。这种信念与现实的脱节是导致具身任务失败的关键瓶颈。

#### 2. 方法论：核心思想与技术细节
为了将智能体从被动接收信息转为主动管理认知，论文提出了 **EVU（Estimate-Verify-Update，估计-验证-更新）** 机制。其核心思想是将信念维护与动作生成解耦，通过显式的文本信念状态进行干预。

*   **核心流程（公式化表达）**：
    1.  **Estimate（估计 $E_t$）**：在看到实际观察前，智能体先根据上一动作预测预期结果。
    2.  **Verify（验证 $V_t$）**：将预期 $E_t$ 与实际观察 $o_t$ 进行对比，生成显式的“惊喜信号”（Surprise Signal），识别一致性或矛盾点。
    3.  **Update（更新 $B_t$）**：结合先验信念、验证证据和观察，将内部信念更新为与现实对齐的后验信念。
*   **统一干预框架**：
    *   **基于提示（Prompting-based）**：通过特定指令引导 LLM 在决策前执行 EVU 思考链。
    *   **基于训练（Training-based）**：将 EVU 过程纳入自回归生成中，通过监督微调（SFT）或强化学习（PPO/GRPO）优化整个推理链，使模型内化这种主动更新能力。

#### 3. 实验设计
*   **数据集/场景**：
    *   **ALFWorld**：家庭清洁与物体放置任务。
    *   **VirtualHome**：复杂的日常家庭活动模拟。
    *   **ScienceWorld**：科学实验任务（如热力学、电路等），逻辑性要求极高。
*   **基准（Benchmark）**：分为“已见场景”（Seen）和“未见场景”（Unseen）以测试泛化性。
*   **对比方法**：
    *   **提示词类**：No-Thinking（直接行动）、Plan-and-Act（仅初始规划）、ReAct（标准推理-行动）。
    *   **训练类**：标准的 SFT、PPO 和 GRPO 算法。
    *   **变体对比**：包括直接生成信念（DBI）、基于观察的干预（OBI）、自我反思（ReflAct）等。

#### 4. 资源与算力
*   **模型使用**：提示词实验使用了 **DeepSeek V3.2**；训练实验使用了 **Qwen2.5-3B-Instruct** 和 **Qwen3-1.7B-Instruct**。
*   **算力细节**：文中提到使用了 `verl-agent` 训练框架。虽然列出了超参数（如 Batch Size 为 32，训练步数为 250 等），但**未明确说明具体的 GPU 型号、数量及总训练时长**。

#### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流基准上进行了大规模测试，涵盖了从 1.7B 到超大规模模型的不同参数量级。
*   **充分性**：
    *   进行了**探测分析**，量化证明了 EVU 确实改变了模型的内部信念值。
    *   进行了**难度分析**，证明任务越难，EVU 的提升越显著。
    *   进行了**计算开销分析**，证明 EVU 虽然增加了单步输出 Token，但通过压缩历史信息反而降低了总 Token 消耗和推理延迟。
*   **客观性**：实验对比了多种主流 RL 算法（PPO vs GRPO），并提供了 Unseen 集合的泛化性数据，结果具有较强的说服力。

#### 6. 主要结论与发现
*   **性能提升**：EVU 在所有基准和模型上均取得显著提升，尤其在 Unseen 场景下平均提升达 9.21%，证明了其强大的泛化能力。
*   **缓解惯性**：探测实验证实，EVU 能将智能体的信念值从错误区域推向正确区域，有效解决了“视而不见”的问题。
*   **效率优势**：相比于不断重复历史记录的 ReAct，EVU 通过维护简洁的“信念状态”，在长程任务中更具计算效率。
*   **训练红利**：EVU 在训练模式下的提升（+10.37%）远高于纯提示词模式（+3.28%），说明模型能够通过学习内化这种认知机制。

#### 7. 优点（亮点）
*   **认知深度**：首次形式化定义了具身智能体中的“信念惯性”问题，并提供了量化的探测手段。
*   **架构通用**：EVU 机制不依赖特定模型架构，可无缝集成到现有的提示词工程或强化学习流程中。
*   **逻辑严密**：通过“预测-验证”引入了类似人类的“预期误差”学习机制，比简单的自我反思（Reflection）更具针对性。

#### 8. 不足与局限
*   **观察依赖**：EVU 的效果高度依赖环境反馈的质量。如果环境观测本身存在严重噪声或歧义，该机制可能会失效。
*   **模型覆盖**：实验主要集中在 Qwen 和 DeepSeek 系列，尚未在更多样化的模型（如 Llama 或多模态原生模型）上广泛验证。
*   **辅助监督缺失**：目前信念更新完全依靠模型自发学习，未引入显式的状态辅助监督（Auxiliary Supervision）或密集的奖励塑造。

（完）
