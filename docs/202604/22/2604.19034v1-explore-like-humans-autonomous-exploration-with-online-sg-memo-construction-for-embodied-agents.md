---
title: "Explore Like Humans: Autonomous Exploration with Online SG-Memo Construction for Embodied Agents"
title_zh: 像人类一样探索：面向具身智能体的在线 SG-Memo 构建与自主探索
authors: "Xu Chen, Shichao Xie, Zhining Gu, Lu Jia, Minghua Luo, Fei Liu, Zedong Chu, Yanfen Shen, Xiaolong Wu, Mu Xu"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19034v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 具身智能体的自主探索与空间记忆
tldr: 针对具身智能体在复杂环境导航中空间记忆构建效率低、缺乏语义理解的问题，本文提出ABot-Explorer框架。该框架模仿人类探索逻辑，利用大视觉语言模型（VLM）在线提取语义导航启发（SNA）作为认知锚点，构建层次化场景图记忆（SG-Memo）。通过优先探索结构性过渡节点，实现了高效的环境覆盖。此外，研究还贡献了大规模标注数据集，并在多项任务中证明了该方法的优越性。
selection_source: fresh_fetch
motivation: 现有的空间记忆构建方法多为离线且偏重几何信息，导致智能体忽略了如门廊、楼梯等关键的语义导航锚点。
method: 提出一种基于RGB输入的在线探索框架，利用VLM提取语义导航启发（SNA）并动态构建层次化场景图记忆（SG-Memo）来引导智能体移动。
result: 实验表明ABot-Explorer在探索效率和环境覆盖率上显著优于现有最先进方法，且生成的SG-Memo能有效支持多种下游任务。
conclusion: 该研究通过将语义智能引入在线探索过程，成功实现了更具人类认知特征的高效自主导航与空间记忆构建。
---

## 摘要
构建结构化空间记忆对于在复杂的具身导航任务中实现长程推理至关重要。目前的记忆构建主要依赖于一种解耦的两阶段范式：智能体首先通过探索聚合环境数据，随后进行空间记忆的离线重建。然而，这种事后处理且以几何为中心的方法阻碍了智能体利用高层语义智能，往往导致它们忽略了导航中关键的地标（如门口和楼梯），而这些地标在人类认知地图中充当着基础语义锚点。为了弥补这一差距，我们提出了 ABot-Explorer，这是一个新颖的主动探索框架，它将记忆构建和探索统一为一个在线的、仅依赖 RGB 的过程。其核心在于，ABot-Explorer 利用大视觉语言模型（VLMs）提取语义导航可负担性（SNA），并将其作为认知对齐的锚点来引导智能体的移动。通过将这些 SNA 动态集成到分层 SG-Memo 中，ABot-Explorer 通过优先处理结构化过渡节点来模拟类人的探索逻辑，从而实现高效覆盖。为了支持该框架，我们贡献了一个大规模数据集，扩展了 InteriorGS 并增加了 SNA 和 SG-Memo 标注。实验结果表明，ABot-Explorer 在探索效率和环境覆盖率方面均显著优于当前最先进的方法，同时生成的 SG-Memo 被证明能有效支持多种下游任务。

## Abstract
Constructing structured spatial memory is essential for enabling long-horizon reasoning in complex embodied navigation tasks. Current memory construction predominantly relies on a decoupled, two-stage paradigm: agents first aggregate environmental data through exploration, followed by the offline reconstruction of spatial memory. However, this post-hoc and geometry-centric approach precludes agents from leveraging high-level semantic intelligence, often causing them to overlook navigationally critical landmarks (e.g., doorways and staircases) that serve as fundamental semantic anchors in human cognitive maps. To bridge this gap, we propose ABot-Explorer, a novel active exploration framework that unifies memory construction and exploration into an online, RGB-only process. At its core, ABot-Explorer leverages Large Vision-Language Models (VLMs) to distill Semantic Navigational Affordances (SNA), which act as cognitive-aligned anchors to guide the agent's movement. By dynamically integrating these SNAs into a hierarchical SG-Memo, ABot-Explorer mirrors human-like exploratory logic by prioritizing structural transit nodes to facilitate efficient coverage. To support this framework, we contribute a large-scale dataset extending InteriorGS with SNA and SG-Memo annotations. Experimental results demonstrate that ABot-Explorer significantly outperforms current state-of-the-art methods in both exploration efficiency and environment coverage, while the resulting SG-Memo is shown to effectively support diverse downstream tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Explore Like Humans: Autonomous Exploration with Online SG-Memo Construction for Embodied Agents》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在未知复杂室内环境中，使具身智能体能够像人类一样，利用高层语义信息进行高效的自主探索，并同步构建可用于长程推理的结构化空间记忆。
*   **研究背景**：
    *   **现有局限**：目前的空间记忆构建多采用“两阶段”范式（先盲目探索采集数据，再离线重建），且大多以几何信息（如占据栅格）为中心。
    *   **痛点**：这种方法缺乏语义深度，容易忽略导航中的关键节点（如门口、楼梯），导致探索效率低下，且生成的地图难以直接支持复杂的语义推理任务。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **ABot-Explorer** 框架，将“记忆构建”与“主动探索”统一为一个在线的、仅依赖 RGB 输入的过程。
*   **关键技术细节**：
    *   **语义导航启发（SNA）**：定义了四类具有结构意义的导航锚点：房间入口（Room entries）、楼梯（Stairs）、交叉口（Intersections）和普通节点（Normal nodes）。
    *   **SG-Memo（场景图记忆）**：一种层次化记忆表示。每个节点包含 SNA 类型、房间类别和可见物体实例；边则表示节点间的拓扑连通性。
    *   **在线构建流程**：
        1.  **感知**：利用微调后的视觉语言模型（VLM，如 Qwen2.5-VL-3B）从多视图 RGB 图像中直接预测局部 2D 场景图。
        2.  **投影与融合**：通过逆透视变换（IPM）将 2D 节点投影至 BEV 空间，并利用空间阈值和非极大值抑制（NMS）将其动态融合进全局 SG-Memo。
        3.  **规划**：VLM 作为高层规划器，根据 SG-Memo 的 JSON 序列化信息选择最优的未访问 SNA 节点作为子目标。
        4.  **执行**：调用低层导航策略（如 ABot-N0）进行局部避障和路径执行。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：基于 InteriorGS 扩展的大规模数据集（1,000个场景），包含 SNA 标注、房间类别和经过遮挡剔除的物体标注。
    *   **测试集**：涵盖 InteriorGS（50个见过的，50个未见过的场景）、HM3D（9个场景）和 MP3D（34个场景）。
*   **Benchmark（评估指标）**：
    *   **探索性能**：占据覆盖率（CR_occ）、SNA 节点召回率（CR_topo）以及对应的效率指标（AUC）。
    *   **记忆质量与下游任务**：评估房间识别准确率、物体导航（ObjectNav）的成功率（SR）和路径长度加权成功率（SPL）。
*   **对比方法**：经典采样算法 **RRT**，以及近年先进的学习型方法 **GLEAM** 和 **CogniPlan**。

### 4. 资源与算力
*   **模型训练**：对 Qwen2.5-VL-3B 进行了监督微调（SFT）。文中未明确给出训练所用的具体 GPU 数量和总训练时长。
*   **推理与部署**：在真机实验（MagicDog 四足机器人）中，使用离线的 **NVIDIA RTX 4090 GPU** 通过 WebSocket 接收图像并进行实时推理。

### 5. 实验数量与充分性
*   **实验规模**：在 143 个不同的室内场景中进行了测试，每个场景包含多个随机起始点。
*   **消融实验**：针对训练数据来源（InteriorGS vs HM3D）以及是否包含“边预测”目标进行了详细对比。
*   **充分性与公平性**：实验覆盖了单层、多层（跨楼层探索）以及真实世界环境。对比实验均在相同传感器配置下进行，评估指标兼顾了几何覆盖和语义理解，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **探索效率更高**：ABot-Explorer 在节点召回率和环境覆盖效率上显著优于以几何为中心的方法（如 CogniPlan）。
*   **语义引导的优势**：通过识别楼梯等 SNA，智能体能够完成复杂的跨楼层探索任务，而传统方法往往在楼梯处受阻。
*   **下游任务增益**：在线构建的 SG-Memo 在物体导航和空间推理任务中表现优异，证明了其作为“推理就绪型”知识库的价值。

### 7. 优点（亮点）
*   **认知对齐**：模仿人类优先寻找“门”、“走廊尽头”的逻辑，使探索过程更具目的性。
*   **RGB-Only**：不依赖昂贵的深度传感器或激光雷达，降低了硬件门槛。
*   **端到端语义集成**：将感知、建图与规划统一在 VLM 的语义框架下，解决了传统方法中语义与几何脱节的问题。

### 8. 不足与局限
*   **实时性挑战**：依赖大型 VLM 进行在线推理，对计算资源和网络延迟有一定要求，可能限制其在低算力嵌入式设备上的表现。
*   **几何假设**：投影过程依赖平面假设（IPM），在地面极度不平整或坡度较大的环境中可能存在定位偏差。
*   **动态环境**：论文主要关注静态场景的探索，未深入探讨在有移动行人或动态障碍物环境中的鲁棒性。

（完）
