---
title: "EmbodiedLGR: Integrating Lightweight Graph Representation and Retrieval for Semantic-Spatial Memory in Robotic Agents"
title_zh: EmbodiedLGR：为机器人智能体集成轻量级图表示与检索的语义空间记忆
authors: "Paolo Riva, Leonardo Gargani, Matteo Frosi, Matteo Matteucci"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.18271v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 使用VLM的机器人智能体语义空间记忆
tldr: 本研究针对具身智能体在复杂环境中高效构建与检索记忆的需求，提出了EmbodiedLGR-Agent架构。该架构采用视觉语言模型（VLM）驱动，通过混合方法将物体的低层位置信息存储在语义图中，同时利用检索增强生成（RAG）保留高层场景描述。实验表明，该方法在NaVQA数据集上取得了领先的推理与查询速度，且准确率表现优异。此外，该系统已成功在物理机器人上实现本地部署，验证了其在实时人机交互中的实用性。
selection_source: fresh_fetch
motivation: 机器人需要在复杂环境中高效构建和检索语义空间记忆，以实现快速响应的人机交互。
method: 提出一种混合架构，利用轻量级视觉语言模型构建包含物体位置的语义图，并结合检索增强生成（RAG）保留高层场景描述。
result: 在NaVQA数据集上实现了最先进的推理和查询速度，同时保持了具有竞争力的准确率，并成功在实体机器人上本地部署。
conclusion: 该研究证明了轻量级图表示与检索结合的方案能有效提升具身智能体的环境理解与实时交互能力。
---

## 摘要
随着应用于机器人的代理式人工智能领域的发展，对能够高效构建和检索记忆与观测结果的智能体的需求日益增长。在复杂环境中运行的机器人必须构建记忆结构，通过利用当前操作上下文的记忆表示来实现有效的人机交互。与机器人交互的人们可能期望具身智能体提供有关位置、事件或物体的信息，这要求智能体在类人的推理时间内提供精确答案，以表现出良好的响应性。我们提出了具身轻量级图检索智能体（EmbodiedLGR-Agent），这是一种由视觉语言模型（VLM）驱动的智能体架构，可构建机器人操作环境的密集且高效的表示。EmbodiedLGR-Agent 通过提供一种基于参数高效 VLM 的混合构建-检索方法，直接解决了对环境高效记忆表示的需求；该方法在语义图中存储有关物体及其位置的底层信息，同时利用传统的检索增强架构保留观测场景的高层描述。EmbodiedLGR-Agent 在流行的 NaVQA 数据集上进行了评估，在具身智能体的推理和查询时间方面达到了最先进的性能，同时在全局任务上保持了相对于当前最先进方法具有竞争力的准确性。此外，EmbodiedLGR-Agent 已成功部署在实体机器人上，通过人机交互展示了在现实世界场景中的实际应用价值，且其视觉语言模型和构建-检索流水线均在本地运行。

## Abstract
As the world of agentic artificial intelligence applied to robotics evolves, the need for agents capable of building and retrieving memories and observations efficiently is increasing. Robots operating in complex environments must build memory structures to enable useful human-robot interactions by leveraging the mnemonic representation of the current operating context. People interacting with robots may expect the embodied agent to provide information about locations, events, or objects, which requires the agent to provide precise answers within human-like inference times to be perceived as responsive. We propose the Embodied Light Graph Retrieval Agent (EmbodiedLGR-Agent), a visual-language model (VLM)-driven agent architecture that constructs dense and efficient representations of robot operating environments. EmbodiedLGR-Agent directly addresses the need for an efficient memory representation of the environment by providing a hybrid building-retrieval approach built on parameter-efficient VLMs that store low-level information about objects and their positions in a semantic graph, while retaining high-level descriptions of the observed scenes with a traditional retrieval-augmented architecture. EmbodiedLGR-Agent is evaluated on the popular NaVQA dataset, achieving state-of-the-art performance in inference and querying times for embodied agents, while retaining competitive accuracy on the global task relative to the current state-of-the-art approaches. Moreover, EmbodiedLGR-Agent was successfully deployed on a physical robot, showing practical utility in real-world contexts through human-robot interaction, while running the visual-language model and the building-retrieval pipeline locally.

---

## 论文详细总结（自动生成）

这是一份关于论文《EmbodiedLGR: Integrating Lightweight Graph Representation and Retrieval for Semantic-Spatial Memory in Robotic Agents》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：具身智能体（机器人）在复杂环境中需要构建和检索记忆，以实现自然的人机交互（如回答“物体在哪里”、“发生了什么”）。
*   **研究动机**：现有的方法（如基于 RAG 的向量数据库或复杂的 3D 场景图）往往存在冗余度高、计算开销大、推理延迟高等问题，难以满足人类对机器人“实时响应”的期望。
*   **目标**：开发一种轻量级、高效的记忆表示架构，使机器人能在本地实时构建环境理解并快速检索信息。

### 2. 方法论
EmbodiedLGR-Agent 采用了一种**双层混合记忆架构**：
*   **核心思想**：将底层物体信息（标签、精确位置）存储在**语义图（Memory Graph）**中，而将高层场景描述（全局视觉特征）存储在**向量数据库（Vector Database）**中。
*   **关键技术细节**：
    *   **感知层**：使用轻量级视觉语言模型 **Florence-2**。它同时执行物体检测（生成标签）和详细描述（生成字幕）。
    *   **记忆构建**：
        *   **语义图更新**：当检测到相同物体时，通过距离阈值（$\delta_P$）和语义相似度阈值（$\delta_E$）判断是否为同一实体。若是，则使用**移动平均法**更新其坐标，避免了重复条目的堆积。
        *   **向量库填充**：存储场景的高层语义描述及其对应的时间戳和位姿。
    *   **检索机制**：LLM 智能体（如 GPT-4o）拥有三类图检索工具（语义、空间、时间搜索）。对于简单的原子查询，直接访问语义图；对于复杂的语义查询，则“回退”（Fallback）到向量数据库。

### 3. 实验设计
*   **数据集/场景**：
    *   **NaVQA 数据集**：用于评估导航相关的问答能力，包含空间和时间维度的查询。
    *   **仿真环境**：使用 Gazebo 模拟仓库环境。
    *   **现实世界部署**：在 AgileX Bunker Pro 移动机器人上进行实地测试。
*   **Benchmark 与对比方法**：
    *   以 **ReMEmbR**（纯向量数据库架构）作为主要对比基准。
    *   对比了不同规模的 VLM（Florence-2-base 和 Florence-2-large）。

### 4. 资源与算力
*   **模型规模**：使用了 Florence-2-base (0.22B) 和 Florence-2-large (0.77B)，远小于主流的 7B 或 13B 模型。
*   **硬件设备**：
    *   **离线测试**：AMD Ryzen 7 3700X CPU, NVIDIA RTX 3060 (12GB) GPU。
    *   **物理机器人**：**Nvidia Jetson AGX Orin (64GB)**，所有 VLM 推理和记忆构建流水线均在本地运行。
    *   **LLM 接口**：通过 API 调用 GPT-4o。
*   **训练时长**：文中未提及模型训练时长，因为主要使用的是预训练模型进行推理和在线记忆构建。

### 5. 实验数量与充分性
*   **实验规模**：对 NaVQA 数据集中的 120 个问题进行了详细评估。
*   **消融实验**：分别测试了“仅语义图”、“仅向量库（ReMEmbR）”以及“混合架构”的表现。
*   **充分性评价**：实验涵盖了准确率（空间/时间）、推理延迟、回退率等多个维度。虽然样本量（120个问题）不算巨大，但结合了物理机器人的实地部署验证，证明了系统在真实动态环境中的鲁棒性和实时性，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **推理速度显著提升**：在仅使用语义图时，响应延迟比纯向量库架构降低了约 **50%**。
*   **准确性优势**：混合架构在空间准确率上优于基准模型，因为语义图提供了更精细的实体位置表示。
*   **VLM 规模的影响**：使用更大的 Florence-2-large 模型显著降低了向向量数据库“回退”的比例（从 93% 降至 80%），说明更强的语义提取能力能让智能体更依赖高效的图结构。
*   **本地化可行性**：证明了在边缘计算设备（Jetson Orin）上运行完整具身记忆流水线的可行性。

### 7. 优点
*   **轻量化与实时性**：通过轻量级 VLM 和图结构更新算法，极大地减少了冗余数据，适合嵌入式部署。
*   **混合检索策略**：巧妙地平衡了“简单查询的快速响应”与“复杂查询的深度理解”。
*   **端到端集成**：在 ROS 2 环境下实现了从感知到导航（Nav2）的完整闭环。

### 8. 不足与局限
*   **LLM 依赖性**：系统性能仍受限于底层 LLM 调用工具的准确性，偶尔会出现误判。
*   **VLM 粒度限制**：为了追求速度使用的 Florence-2 模型在描述极度复杂的场景细节时，可能不如 13B 以上的大模型丰富。
*   **运动模糊影响**：在物理机器人快速移动时，VLM 对物体的识别准确率会下降，导致记忆构建出现偏差。
*   **空间推理深度**：目前主要处理原子级查询，对于需要多层逻辑推理的复杂空间关系（如“在 A 和 B 之间且靠近 C 的物体”）仍有提升空间。

（完）
