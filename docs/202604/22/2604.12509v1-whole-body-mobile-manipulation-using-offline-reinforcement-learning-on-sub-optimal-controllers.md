---
title: Whole-Body Mobile Manipulation using Offline Reinforcement Learning on Sub-optimal Controllers
title_zh: 基于次优控制器的离线强化学习全身移动操作
authors: "Snehal Jauhri, Vignesh Prasad, Georgia Chalvatzaki"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12509v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 使用离线强化学习进行全身协调和移动操作
tldr: 针对移动操作（MoMa）中全身协调控制的难题，本文提出WHOLE-MoMa框架。该方法利用次优的全身控制器（WBC）作为结构先验生成多样化演示数据，随后通过改进的离线强化学习算法（结合Q-chunking的隐式Q学习）对行为进行优化与缝合。实验表明，该方法在模拟和真实环境的复杂任务中均显著优于传统WBC和行为克隆，实现了无需真机训练数据的零样本迁移。
selection_source: fresh_fetch
motivation: 传统的全身控制器调参复杂且脆弱，而现有的学习方法又极度依赖昂贵的遥操作数据或繁琐的奖励工程。
method: 提出两阶段流程：先通过随机化轻量级WBC生成多样化演示，再利用扩展了Q-chunking的离线隐式Q学习（IQL）提取并优化策略。
result: "在TIAGo++机器人上实现了双臂抽屉操作80%和橱柜放置68%的成功率，且策略可直接从仿真迁移至现实。"
conclusion: 证明了利用次优控制器作为数据生成器并结合离线强化学习，是解决复杂全身移动操作任务的一种高效且无需人工演示的有效途径。
---

## 摘要
对关节物体（如开门、抽屉和橱柜）的移动操作（MoMa）要求机器人的底座和机械臂之间进行同步的全身协调。经典的全身控制器（WBC）可以通过分层优化解决此类问题，但需要大量的手动调优，且鲁棒性较差。另一方面，基于学习的方法表现出强大的泛化能力，但通常依赖昂贵的全身遥操作数据或繁重的奖励工程。我们观察到，即使是次优的 WBC 也是一种强大的结构先验：它可以用于在状态-动作空间中受限且与任务相关的区域收集数据，并且其行为仍可以通过离线强化学习得到改进。基于此，我们提出了 WHOLE-MoMa，这是一个两阶段流水线，首先通过随机化轻量级 WBC 生成多样化的演示，然后应用离线强化学习通过奖励信号识别并缝合改进的行为。为了支持复杂协调任务所需的具有表现力的动作分块扩散策略，我们通过 Q-chunking 扩展了离线隐式 Q 学习（IQL），用于块级评论家评估和优势加权策略提取。在仿真环境中使用 TIAGo++ 移动操作机器人的三个难度递增的任务中，WHOLE-MoMa 的表现显著优于 WBC、行为克隆和几种离线强化学习基准方法。策略无需微调即可直接迁移到真实机器人，在双臂抽屉操作中实现了 80% 的成功率，在同时开启橱柜和放置物体中实现了 68% 的成功率，且所有过程均无需任何遥操作或真实世界训练数据。

## Abstract
Mobile Manipulation (MoMa) of articulated objects, such as opening doors, drawers, and cupboards, demands simultaneous, whole-body coordination between a robot's base and arms. Classical whole-body controllers (WBCs) can solve such problems via hierarchical optimization, but require extensive hand-tuned optimization and remain brittle. Learning-based methods, on the other hand, show strong generalization capabilities but typically rely on expensive whole-body teleoperation data or heavy reward engineering. We observe that even a sub-optimal WBC is a powerful structural prior: it can be used to collect data in a constrained, task-relevant region of the state-action space, and its behavior can still be improved upon using offline reinforcement learning. Building on this, we propose WHOLE-MoMa, a two-stage pipeline that first generates diverse demonstrations by randomizing a lightweight WBC, and then applies offline RL to identify and stitch together improved behaviors via a reward signal. To support the expressive action-chunked diffusion policies needed for complex coordination tasks, we extend offline implicit Q-learning with Q-chunking for chunk-level critic evaluation and advantage-weighted policy extraction. On three tasks of increasing difficulty using a TIAGo++ mobile manipulator in simulation, WHOLE-MoMa significantly outperforms WBC, behavior cloning, and several offline RL baselines. Policies transfer directly to the real robot without finetuning, achieving 80% success in bimanual drawer manipulation and 68% in simultaneous cupboard opening and object placement, all without any teleoperated or real-world training data.

---

## 论文详细总结（自动生成）

### 论文总结：基于次优控制器的离线强化学习全身移动操作 (WHOLE-MoMa)

#### 1. 核心问题与整体含义（研究动机和背景）
移动操作（Mobile Manipulation, MoMa）要求机器人的底座与机械臂高度协同，尤其是在处理门、抽屉、橱柜等关节物体时。
*   **研究动机**：传统的全身控制器（WBC）依赖手动调优且难以应对长时程交互；而基于学习的方法（如模仿学习）通常需要昂贵的全身遥操作数据，或面临高维状态-动作空间下的奖励工程难题。
*   **核心思想**：本文提出，即使是一个**次优的 WBC** 也可以作为强大的“结构先验”。通过随机化 WBC 参数生成多样化的演示数据，再利用**离线强化学习（Offline RL）**从这些次优数据中识别并“缝合”出更优的行为，从而实现无需人工演示数据的复杂全身协同控制。

#### 2. 方法论：核心思想与关键技术
WHOLE-MoMa 采用两阶段流水线：
*   **第一阶段：数据生成**
    *   使用基于分层二次规划（HQP）的轻量级 WBC。
    *   通过随机化参数（如姿态权重、抓取阈值、步长等）在仿真中生成约 3000 条多样化轨迹。这些数据虽然包含失败尝试，但覆盖了任务相关的状态空间。
*   **第二阶段：离线强化学习优化**
    *   **Q-chunking 扩展**：为了适配具有强大表达能力的“动作分块扩散策略（Diffusion Policy）”，作者扩展了隐式 Q 学习（IQL）。
    *   **算法流程**：
        1.  **隐式 Q 学习**：在不访问未见动作的情况下，通过非对称分位数回归学习状态值函数 $V$ 和分块 Q 函数 $Q$。
        2.  **优势加权回归 (AWR)**：计算动作块的优势函数 $A = Q - V$，并以此作为权重对扩散策略进行加权行为克隆（Weighted BC）。
    *   **策略架构**：采用基于 Transformer 的扩散策略，能够处理多峰分布并保持动作序列的时间一致性。

#### 3. 实验设计
*   **实验平台**：TIAGo++ 双臂移动操作机器人（仿真环境为 NVIDIA Isaac Sim）。
*   **任务场景（难度递增）**：
    1.  **Door**：推门并穿过。
    2.  **Drawer**：双臂协同，同时关闭一个抽屉并打开另一个。
    3.  **Cupboard**：最难任务，单臂开门的同时另一只手将物体放入柜中。
*   **对比基准 (Benchmarks)**：
    *   **WBC-policy**：原始的全身控制器。
    *   **BC (Diffusion Policy)**：纯行为克隆。
    *   **RL (TD3)**：从零开始的在线强化学习。
    *   **离线 RL 变体**：IQL+DDPG BC、IDQL、RISE 等。

#### 4. 资源与算力
*   **算力支持**：论文提到使用了埃尔朗根国家高性能计算中心（NHR）的资源。
*   **具体细节**：文中**未明确列出**具体的 GPU 型号、数量或确切的训练时长。但提到控制频率为 40Hz，且为了在真机上实时运行扩散策略（推理耗时约 80-100ms），采用了异步推理方案。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   每个任务生成 3000 条仿真轨迹。
    *   仿真评估：每个任务进行 50 次独立实验，并计算 95% 置信区间。
    *   真机评估：在两个复杂任务上各进行 25 次实验。
*   **充分性评价**：实验设计较为充分。不仅对比了多种 SOTA 离线 RL 算法，还进行了详细的**消融实验**（包括 Transformer vs U-Net 架构、Q-chunking 的有效性、以及视觉位姿跟踪 vs 动捕标记的对比），客观地展示了算法在不同维度下的性能增益。

#### 6. 主要结论与发现
*   **性能超越**：WHOLE-MoMa 在所有任务中均显著优于原始 WBC 和纯 BC。在最难的橱柜任务中，成功率比 IDQL/RISE 高出约 14%。
*   **行为缝合**：离线 RL 成功从次优数据中提取了更连贯的协同动作，弥补了 WBC 无法进行长时程规划的缺陷。
*   **零样本迁移**：策略无需任何真机数据或微调，即可直接从仿真迁移到真实 TIAGo++ 机器人，在橱柜任务中达到 68% 的成功率。

#### 7. 优点与亮点
*   **无需遥操作**：完全摆脱了对昂贵的人工全身遥操作数据的依赖，降低了数据获取门槛。
*   **结构先验结合**：巧妙地将经典控制器的物理约束（如自碰撞规避）作为先验，与深度学习的泛化能力结合。
*   **技术创新**：引入 Q-chunking 使 IQL 能够有效支持分块扩散策略，解决了高维连续动作空间下的离线学习稳定性问题。

#### 8. 不足与局限
*   **精度瓶颈**：真机实验表明，系统对状态估计（位姿跟踪）的精度非常敏感。当视觉跟踪出现微小误差时，策略容易停滞。
*   **动力学间隙**：虽然实现了迁移，但仿真与现实之间的动力学差异（如关节顺应性、摩擦力）仍导致真机成功率低于仿真。
*   **应用限制**：目前仍依赖于关节物体的状态估计（如门的角度），尚未实现完全端到端的视觉输入控制。

（完）
