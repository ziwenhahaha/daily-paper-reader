---
title: "Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA"
title_zh: 开环规划，闭环验证：面向 VLA 的投机验证
authors: "Zihua Wang, Zhitao Lin, Ruibo Li, Yu Zhang, Xu Yang, Siya Mi, Xiu-Shen Wei"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02965v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 针对操纵任务的VLA模型优化与闭环验证
tldr: 针对视觉-语言-动作（VLA）模型推理成本高且开环执行鲁棒性差的问题，本文提出SV-VLA框架。该框架利用大型VLA模型作为低频宏观规划器生成动作块，并引入轻量级验证器进行高频闭环监控。验证器根据当前观测和规划上下文判断是否需要重新规划，从而在保持高效推理的同时，显著提升了机器人在动态环境中的执行可靠性。
selection_source: fresh_fetch
motivation: 现有的VLA模型动作分块技术虽能降低计算开销，但因缺乏闭环反馈，难以应对环境变化和误差累积。
method: 提出一种投机验证框架，由重型VLA模型生成动作序列及规划上下文，并由轻量级验证器实时比对当前观测与规划动作，仅在必要时触发重规划。
result: 实验表明，SV-VLA在动态环境中结合了分块预测的高效性与闭环控制的鲁棒性，实现了更可靠的机器人控制。
conclusion: SV-VLA通过引入轻量级在线验证机制，有效解决了VLA模型在效率与闭环反馈之间的权衡问题。
---

## 摘要
视觉-语言-动作（VLA）模型作为具身控制的大型基础模型，在操作任务中展现出强大的性能。然而，其高性能是以高昂的推理成本为代价的。为了提高效率，近期的方法采用了动作分块（action chunking）技术，即预测一系列未来动作以进行开环执行。尽管开环执行在减少计算量方面行之有效，但由于缺乏闭环反馈，它对环境变化十分敏感，且容易导致误差累积。针对这一局限性，我们提出了面向 VLA 控制的投机验证（SV-VLA）框架，该框架将高效的开环长程规划与轻量级的闭环在线验证相结合。具体而言，SV-VLA 使用重型 VLA 作为低频宏观规划器来生成动作分块及规划上下文，同时利用轻量级验证器根据最新观测结果持续监控执行情况。在当前观测和规划上下文的共同作用下，验证器将计划动作与闭环参考动作进行对比，并仅在必要时触发重新规划。实验表明，SV-VLA 兼具分块预测的高效性与闭环控制的鲁棒性，能够在动态环境中实现高效且可靠的基于 VLA 的控制。代码地址：https://github.com/edsad122/SV-VLA。

## Abstract
Vision-Language-Action (VLA) models, as large foundation models for embodied control, have shown strong performance in manipulation tasks. However, their performance comes at high inference cost. To improve efficiency, recent methods adopt action chunking, which predicts a sequence of future actions for open-loop execution. Although effective for reducing computation, open-loop execution is sensitive to environmental changes and prone to error accumulation due to the lack of close-loop feedback. To address this limitation, we propose Speculative Verification for VLA Control (SV-VLA), a framework that combines efficient open-loop long-horizon planning with lightweight closed-loop online verification. Specifically, SV-VLA uses a heavy VLA as a low-frequency macro-planner to generate an action chunk together with a planning context, while a lightweight verifier continuously monitors execution based on the latest observations. Conditioned on both the current observation and the planning context, the verifier compares the planned action against a closed-loop reference action and triggers replanning only when necessary. Experiments demonstrate that SV-VLA combines the efficiency of chunked prediction with the robustness of closed-loop control, enabling efficient and reliable VLA-based control in dynamic environments. Code is available: https://github.com/edsad122/SV-VLA.

---

## 论文详细总结（自动生成）

以下是对论文《Open-Loop Planning, Closed-Loop Verification: Speculative Verification for VLA》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：视觉-语言-动作（VLA）模型虽然强大，但推理延迟极高，难以实现高频闭环控制。
*   **研究背景**：
    *   **动作分块（Action Chunking）**：通过一次推理预测多个动作来提高效率，但属于“开环执行”，无法应对环境扰动，容易产生误差累积。
    *   **投机采样（Speculative Decoding）**：在NLP中有效，但在机器人领域，未来的观测依赖于当前动作的执行（环境状态转移），导致传统的并行验证机制失效。
*   **动机**：开发一种既能享受动作分块的高效率，又能具备闭环控制鲁棒性的框架。

### 2. 方法论：SV-VLA 框架
*   **核心思想**：**“昂贵但低频的规划”与“廉价但高频的验证”相结合**。将重型 VLA 作为宏观规划器，轻量级模型作为在线验证器。
*   **关键技术细节**：
    1.  **宏观规划器（Macro-Planner）**：使用冻结的重型 VLA（如 OpenVLA），在规划边界生成动作块 $A_{macro}$，并提取倒数第二层的特征作为“规划上下文” $F_0$。
    2.  **轻量级验证器（Verifier）**：由极小的视觉骨干（如 ViT-Tiny）和 MLP 组成。它接收当前实时观测 $I_t$ 和规划上下文 $F_0$，预测一个参考动作 $a'_t$。
    3.  **基于偏差的重规划（Deviation-based Replanning）**：
        *   计算计划动作 $a_t$ 与参考动作 $a'_t$ 之间的 L1 距离。
        *   若偏差超过预设阈值 $\tau$，则判定当前计划失效，立即丢弃剩余动作块，触发重型 VLA 进行重规划。
        *   若偏差在范围内，则继续执行原计划，保持高效。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 仿真基准测试，包含三个子任务集：
    *   **LIBERO-Goal**：考察目标达成能力。
    *   **LIBERO-Object**：考察对不同物体的操作。
    *   **LIBERO-Spatial**：考察空间关系处理。
*   **对比方法（Baselines）**：
    *   **BASE (K=8)**：短动作块开环基准（高频重规划，速度慢）。
    *   **BASE (K=64)**：长动作块开环基准（低频重规划，速度快但易失败）。
    *   **Speculative Decoding (SpecVLA)**：传统的投机采样 VLA 方法。

### 4. 资源与算力
*   **训练算力**：使用了 **4 张 NVIDIA A100 GPU**。
*   **推理/评估算力**：在 **1 张 NVIDIA V100 GPU** 上进行在线测试，以确保公平的延迟对比。
*   **训练参数**：AdamW 优化器，学习率 8e-4，Batch Size 为 64。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 LIBERO 的三大任务集，进行了大量的在线 Rollout 测试。
*   **消融实验**：非常充分，分别验证了“规划上下文 $F_0$”、“实时观测 $I_t$”以及“重规划机制”的必要性。
*   **敏感度分析**：测试了不同阈值 $\tau$ 对成功率和推理速度的影响。
*   **客观性**：通过对比不同 K 值的基准线，清晰地展示了该方法在“效率-性能”曲线上的优越位置。

### 6. 主要结论与发现
*   **性能提升**：在长动作块（K=64）设置下，SV-VLA 将平均成功率从 79.5% 提升至 **90.9%**。
*   **效率优势**：相比于频繁调用大模型的 BASE (K=8)，SV-VLA 实现了 **2.17 倍** 的加速，同时保持了相近的成功率。
*   **验证器作用**：实验证明，验证器必须同时结合“全局规划意图”和“局部实时观测”才能准确判断是否需要重规划。

### 7. 优点与亮点
*   **架构解耦**：规划与验证分离，使得框架可以适配任何现有的冻结 VLA 模型，无需对大模型进行微调。
*   **即时纠错**：定性实验显示，SV-VLA 能在抓取失败等关键时刻及时发现并重规划，而传统开环方法会盲目执行错误动作。
*   **实用性强**：通过调节阈值 $\tau$，用户可以根据任务需求灵活平衡机器人的“反应速度”与“计算开销”。

### 8. 不足与局限
*   **阈值依赖**：阈值 $\tau$ 目前是静态设置的，对于不同复杂度的任务，可能需要手动调优。
*   **纠错手段单一**：目前验证器发现偏差后只能触发昂贵的重型模型重规划，未来若能让轻量级模型进行“局部微调修正”会更高效。
*   **环境局限**：实验主要在 LIBERO 仿真环境中完成，尚未在复杂的真实世界动态环境（如有人干扰的场景）中进行大规模验证。

（完）
