---
title: Learning Structured Robot Policies from Vision-Language Models via Synthetic Neuro-Symbolic Supervision
title_zh: 通过合成神经符号监督从视觉语言模型中学习结构化机器人策略
authors: "Alessandro Adami, Tommaso Tubaldo, Marco Todescato, Ruggero Carli, Pietro Falco"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02812v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 从视觉语言模型中学习结构化机器人策略
tldr: 本研究针对端到端视觉语言模型在机器人策略中缺乏透明度的问题，提出了一种神经符号化方法。通过自动化的合成数据生成流程，训练VLM从视觉观察和自然语言指令中直接合成可执行的行为树策略。该方法结合了基础模型的高维感知能力与符号控制的可解释性，并在真实机器人实验中验证了其从合成数据到物理系统的有效迁移能力。
selection_source: fresh_fetch
motivation: 解决当前端到端机器人策略黑盒化、难以分析且在安全关键应用中受限的问题，寻求兼具感知能力与可解释性的结构化策略。
method: 提出一种神经符号框架，利用基础模型自动生成包含场景随机化和指令-行为树对的合成数据集，并据此训练VLM合成结构化策略。
result: 实验证明，完全在合成数据上训练的结构化策略能够成功迁移至两种不同的真实机器人操作臂上。
conclusion: 基础模型可以被有效适配以生成可解释的结构化机器人策略，为多模态机器人决策提供了一种优于不透明端到端方法的新途径。
---

## 摘要
视觉语言模型（VLM）最近在将多模态观测映射到机器人行为方面展现出了强大的能力。然而，目前大多数方法依赖于端到端的视觉运动策略，这些策略仍然是不透明且难以分析的，限制了它们在安全关键型机器人应用中的使用。相比之下，经典的机器人系统通常依赖于结构化的策略表示，这些表示提供了可解释性、模块化和反应式执行。本研究探讨了如何使基础模型专门化，以生成基于多模态感知的结构化机器人策略，从而架起高维学习与符号控制之间的桥梁。我们提出了一种神经符号方法，其中 VLM 根据视觉观测、自然语言指令和结构化系统规范合成可执行的行为树（Behavior Tree）策略。为了在无需人工标注的情况下实现可扩展的监督，我们引入了一个自动化流水线，该流水线生成了一个合成多模态数据集，其中包含领域随机化的场景以及由基础模型生成的指令-策略示例。在两台机器人机械臂上进行的真实世界实验表明，完全从合成监督中学习到的结构化策略能够成功迁移到物理系统。结果表明，基础模型可以经过调整以产生可解释且结构化的机器人策略，为多模态机器人决策提供了一种替代不透明端到端方法的新方案。

## Abstract
Vision-language models (VLMs) have recently demonstrated strong capabilities in mapping multimodal observations to robot behaviors. However, most current approaches rely on end-to-end visuomotor policies that remain opaque and difficult to analyze, limiting their use in safety-critical robotic applications. In contrast, classical robotic systems often rely on structured policy representations that provide interpretability, modularity, and reactive execution. This work investigates how foundation models can be specialized to generate structured robot policies grounded in multimodal perception, bridging high-dimensional learning and symbolic control. We propose a neuro-symbolic approach in which a VLM synthesizes executable Behavior Tree policies from visual observations, natural language instructions, and structured system specifications. To enable scalable supervision without manual annotation, we introduce an automated pipeline that generates a synthetic multimodal dataset of domain-randomized scenes paired with instruction-policy examples produced by a foundation model. Real-world experiments on two robotic manipulators show that structured policies learned entirely from synthetic supervision transfer successfully to physical systems. The results indicate that foundation models can be adapted to produce interpretable and structured robot policies, providing an alternative to opaque end-to-end approaches for multimodal robot decision making.

---

## 论文详细总结（自动生成）

以下是对论文《Learning Structured Robot Policies from Vision-Language Models via Synthetic Neuro-Symbolic Supervision》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的视觉-语言-动作模型（VLA）大多采用端到端的视觉运动策略，这种“黑盒”模式缺乏透明度、难以分析且难以施加安全约束，不适用于安全关键型机器人任务。
*   **研究动机**：传统的机器人控制依赖于**行为树（Behavior Trees, BTs）**等结构化表示，具有模块化、可解释和反应式执行的优点。本研究旨在结合视觉语言模型（VLM）的高维感知能力与符号控制的逻辑严谨性，通过合成数据监督，使模型能够直接从视觉和指令中合成可执行的结构化策略。

### 2. 方法论
*   **核心思想**：提出一种**神经符号（Neuro-Symbolic）**框架，利用大型基础模型自动生成大规模合成数据集，用于微调中型 VLM（如 Pixtral-12B），使其能够输出符合机器人约束的 JSON 格式行为树。
*   **关键技术细节**：
    *   **自动化合成流水线**：使用 MuJoCo 物理引擎生成 10,000 个领域随机化的桌面场景，提取对象元数据（位姿、颜色等）。
    *   **合成监督生成**：利用高性能基础模型（如 Gemini 3 Flash）作为“教师”，根据场景图像和元数据自动生成自然语言指令及对应的行为树（BT）。
    *   **结构化约束编码**：在生成的 BT 中强制执行：
        *   **反应式守护（Reactive Guarding）**：动作前必须有条件检查。
        *   **空间偏移（Spatial Offsetting）**：自动计算接近路径的 Z 轴偏移。
        *   **扁平化层级（Flat Hierarchy）**：防止逻辑深度过深导致的执行错误。
    *   **模型微调**：使用 LoRA（低秩适配）技术对 Pixtral-12B 进行监督微调（SFT），使其学习从多模态输入到 JSON 符号结构的映射。

### 3. 实验设计
*   **实验场景**：桌面操作任务（抓取、放置、堆叠等）。
*   **数据集**：10,000 个合成的 MuJoCo 场景及配对的指令-BT 序列。
*   **对比基准（Benchmark）**：
    *   **模型对比**：对比了 GPT-5 和 Gemini 3 Flash 在 Zero-shot 和 One-shot 模式下的表现。
    *   **消融实验**：设计了 17 种变体（A1-A17），测试了 LoRA Rank、微调模块（Attention vs MLP）、上下文窗口长度、学习率、数据集规模等参数的影响。
*   **硬件验证**：在 **Franka Emika Panda** 和 **UR5e** 两种不同的工业机械臂上进行了真实世界测试，验证跨平台迁移能力。

### 4. 资源与算力
*   **GPU 型号**：单张 **NVIDIA A40 (48 GiB VRAM)**。
*   **训练细节**：
    *   使用 8-bit Paged AdamW 优化器和 Bfloat16 精度。
    *   训练时长：基准模型（A1）在 10k 数据集上训练 1 个 epoch 耗时约 **15.12 小时**。
    *   上下文窗口限制为 4,400 tokens 以防止显存溢出。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **消融实验**：17 组详细的超参数对比，涵盖了从模型架构到数据规模的全方位评估。
    *   **真实世界测试**：在两种不同动力学特性的机器人上验证，并测试了“左侧物体”等空间推理任务。
    *   **泛化性测试**：包括对未见过的语义任务（如“交换位置”）和未见过的末端执行器（吸盘）的 Zero-shot 测试。
*   **充分性评价**：实验设计较为充分且客观。通过消融实验明确了 MLP 层微调和上下文窗口对逻辑合成的重要性，真实世界实验证明了从合成数据到物理环境的“零样本逻辑迁移”是可行的。

### 6. 主要结论与发现
*   **合成数据有效性**：完全在合成数据上训练的模型可以成功迁移到真实世界，且对真实图像具有良好的鲁棒性。
*   **模型规模与能力**：经过微调的 12B 模型在特定任务（生成 JSON 格式 BT）上的表现优于未微调的超大规模模型（如 GPT-5），因为后者在 Zero-shot 下难以严格遵守复杂的机器人语法约束。
*   **结构化先验的重要性**：将机器人动作分解为原子基元（Atomic Primitives）并嵌入行为树，显著提高了执行的鲁棒性和可解释性。
*   **局限性发现**：模型在处理复杂的长程逻辑（如需要中转位的“交换”任务）和物理交互常识（如吸盘需接触后才能吸取）方面仍存在挑战。

### 7. 优点
*   **无需人工标注**：通过自动化的合成流水线解决了机器人数据获取难、标注贵的痛点。
*   **可解释性强**：输出的是符号化的行为树，人类可以预先检查逻辑，且执行过程每一步都透明。
*   **硬件无关性**：生成的策略基于抽象基元，同一套逻辑可直接运行在 Panda 和 UR5e 上。

### 8. 不足与局限
*   **模式退化（Pattern Backsliding）**：对于训练集中未涵盖的复杂语义逻辑（如 Swap 任务），模型倾向于退化为简单的堆叠逻辑。
*   **功能接地间隙（Functional Grounding Gap）**：模型虽然能理解新词汇（如“吸盘”），但缺乏对新工具物理特性的 Zero-shot 理解。
*   **感知依赖**：目前假设存在一个外部感知骨干来提供物体标签，若感知层出错，整个符号逻辑将失效。

（完）
