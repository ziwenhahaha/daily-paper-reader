---
title: Reliability-Aware Geometric Fusion for Robust Audio-Visual Navigation
title_zh: 面向鲁棒视听导航的可靠性感知几何融合
authors: "Teng Liu, Yinfeng Yu"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02391v1"
tags: ["query:q13"]
score: 9.0
evidence: 利用多种感官模态进行视听导航
tldr: 针对视听导航（AVN）中复杂声学环境导致双耳音频信号不可靠的问题，本文提出RAVN框架。该框架引入声学几何推理器（AGR），通过异方差高斯负对数似然目标学习观测相关的可靠性线索，并利用可靠性感知几何调制（RAGM）动态校准视听融合。实验表明，RAVN在处理未听过声音的挑战性场景中表现出显著的鲁棒性和导航性能提升。
selection_source: fresh_fetch
motivation: 解决在复杂声学环境或面对未知声源时，双耳音频线索间歇性不可靠导致的导航失败问题。
method: 提出包含AGR和RAGM的RAVN框架，利用几何代理监督学习音频可靠性线索，并将其转化为软门控以动态调节跨模态特征融合。
result: 在SoundSpaces的Replica和Matterport3D环境下的实验证明，该方法在导航成功率和效率上均有提升，尤其在未知声音设置下表现优异。
conclusion: 通过显式建模音频信号的可靠性并动态调整多模态融合策略，可以显著增强具身智能体在复杂环境下的导航鲁棒性。
---

## 摘要
视听导航 (AVN) 要求具身智能体利用视觉和双耳音频向声源导航。在复杂的声学环境中，双耳线索会变得间歇性不可靠，特别是在泛化到以前未听过的声音类别时，这带来了一个核心挑战。为了解决这一问题，我们提出了 RAVN（可靠性感知视听导航），该框架以音频衍生的可靠性线索为条件进行跨模态融合，动态校准音频和视觉输入的集成。RAVN 引入了一个声学几何推理器 (AGR)，该推理器采用几何代理监督进行训练。通过使用异方差高斯负对数似然 (NLL) 目标，AGR 学习依赖于观测的离散度作为一种实用的可靠性线索，从而在推理阶段无需几何标签。此外，我们还引入了可靠性感知几何调制 (RAGM)，它将学习到的线索转换为软门控来调制视觉特征，从而缓解跨模态冲突。我们在 SoundSpaces 的 Replica 和 Matterport3D 环境中对 RAVN 进行了评估，结果表明其导航性能得到了一致提升，且在具有挑战性的未听过声音设置中表现出显著的鲁棒性。

## Abstract
Audio-Visual Navigation (AVN) requires an embodied agent to navigate toward a sound source by utilizing both vision and binaural audio. A core challenge arises in complex acoustic environments, where binaural cues become intermittently unreliable, particularly when generalizing to previously unheard sound categories. To address this, we propose RAVN (Reliability-Aware Audio-Visual Navigation), a framework that conditions cross-modal fusion on audio-derived reliability cues, dynamically calibrating the integration of audio and visual inputs. RAVN introduces an Acoustic Geometry Reasoner (AGR) that is trained with geometric proxy supervision. Using a heteroscedastic Gaussian NLL objective, AGR learns observation-dependent dispersion as a practical reliability cue, eliminating the need for geometric labels during inference. Additionally, we introduce Reliability-Aware Geometric Modulation (RAGM), which converts the learned cue into a soft gate to modulate visual features, thereby mitigating cross-modal conflicts. We evaluate RAVN on SoundSpaces using both Replica and Matterport3D environments, and the results show consistent improvements in navigation performance, with notable robustness in the challenging unheard sound setting.

---

## 论文详细总结（自动生成）

以下是对论文《Reliability-Aware Geometric Fusion for Robust Audio-Visual Navigation》的结构化总结：

### 1. 论文的核心问题与研究动机
*   **核心问题**：在复杂的室内声学环境中，由于障碍物遮挡、衍射和混响，双耳音频线索（Binaural Cues）往往具有间歇性的不可靠性。现有的视听导航（AVN）方法大多采用简单的特征拼接（Naive Fusion），假设音频信号始终可靠，这导致智能体在面对未知声源（Unheard Sounds）或复杂声学干扰时，容易被错误的音频线索误导，产生导航偏差。
*   **研究动机**：借鉴人类直觉——当声音模糊或环境嘈杂时，人类会降低对听觉的信任并转而依赖视觉确认。论文旨在赋予具身智能体“感知音频可靠性”的能力，通过动态调整视听融合权重来提升导航的鲁棒性。

### 2. 核心方法论
论文提出了 **RAVN (Reliability-Aware Audio-Visual Navigation)** 框架，主要包含以下核心组件：
*   **声学几何推理器 (AGR)**：
    *   **异方差不确定性估计**：利用异方差高斯负对数似然（NLL）目标函数，将距离预测建模为高斯分布 $d_t \sim N(\mu_t, \sigma_t^2)$。其中方差 $\sigma_t^2$ 被用作观测相关的可靠性线索（不确定性越高，可靠性越低）。
    *   **几何代理监督**：通过辅助任务学习相对距离和方位角（Azimuth）。方位角采用包裹式 Smooth-L1 损失处理角度周期性。
*   **可靠性感知几何调制 (RAGM)**：
    *   **动态软门控**：将 AGR 提取的几何特征通过 MLP 和 Sigmoid 激活函数转化为调制掩码（Reliability Mask）$m_t$。
    *   **特征校准**：利用掩码对视觉特征进行逐元素哈达玛积运算（$f_v' = f_v \odot m_t$），在音频不可靠时抑制可能导致错误决策的视觉/听觉冲突，实现动态特征校准。
*   **策略学习**：采用循环神经网络（GRU）聚合调制后的特征，并使用 PPO 强化学习算法进行端到端训练，总损失函数结合了导航奖励损失和几何辅助任务损失。

### 3. 实验设计
*   **数据集与场景**：
    *   **Replica**：包含 18 个扫描的公寓和办公室场景，网格粒度 0.5m。
    *   **Matterport3D (Mp3D)**：包含 85 个大型住宅环境，网格粒度 1m。
*   **实验设置**：分为“已听过声音（Heard）”和“未听过声音（Unheard）”两种设置，后者用于评估模型的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了随机代理、前沿路标法（Frontier Waypoints）、方向跟随者（Direction Follower）以及经典的 **AV-Nav**（SoundSpaces 基准方法）和 Gan 等人的概率方法。
*   **评估指标**：成功率 (SR)、路径长度加权成功率 (SPL) 和动作数加权成功率 (SNA)。

### 4. 资源与算力
*   **训练细节**：使用 Adam 优化器，学习率为 $2.5 \times 10^{-4}$。
*   **训练时长**：在 Replica 上训练了 4800 万步（48M steps），在 Matterport3D 上训练了 6000 万步（60M steps）。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但从训练步数和场景规模来看，属于中等规模的具身智能训练任务。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个主流的大型 3D 仿真数据集，并针对 102 种声音进行了 Heard/Unheard 划分。
*   **消融实验**：论文对比了“无几何推理”、“使用 MSE 损失的确定性几何推理”以及“使用 NLL 损失的可靠性感知推理”等变体，充分验证了 AGR 和 RAGM 模块的有效性。
*   **客观性**：通过可视化轨迹图展示了 RAVN 在拐角和长廊等易产生声学歧义区域的优势，实验设计遵循了 AVN 领域的标准协议，对比具有公平性。

### 6. 主要结论与发现
*   **性能提升**：RAVN 在所有设置下均优于基准方法。在最困难的 Replica Unheard 设置下，SR 提升了 12.3%，SPL 提升了 9.5%。
*   **鲁棒性验证**：实验证明，显式建模不确定性（NLL 损失）比单纯追求度量精度（MSE 损失）在复杂声学环境下更有效。
*   **决策稳定性**：SNA 指标的显著提升表明，可靠性感知机制减少了智能体在模糊环境下的犹豫和无效旋转，使决策更加果断。

### 7. 优点与亮点
*   **创新性地引入不确定性**：将异方差回归引入 AVN 的跨模态融合，解决了音频信号质量波动导致的策略失效问题。
*   **无需推理标签**：虽然训练时需要几何监督，但推理时仅靠音频输入即可推断可靠性，具有较强的实用性。
*   **即插即用**：RAGM 模块作为一种软门控机制，可以较容易地集成到其他多模态导航框架中。

### 8. 不足与局限
*   **现实迁移挑战**：实验完全在仿真环境（SoundSpaces）中进行，尚未在物理机器人或存在真实环境噪声（如风声、机械噪声）的场景中验证。
*   **极端退化评估不足**：虽然考虑了复杂声学，但未专门针对传感器完全失效或极端高分贝噪声干扰进行极限压力测试。
*   **计算开销**：虽然增加了辅助头，但未详细讨论增加 AGR 和 RAGM 模块对实时推理延迟的具体影响。

（完）
