---
title: "VAG: Dual-Stream Video-Action Generation for Embodied Data Synthesis"
title_zh: VAG：用于具身数据合成的双流视频-动作生成
authors: "Xiaolei Lang, Yang Wang, Yukun Zhou, Chaojun Ni, Kerui Li, Jiagang Zhu, Tianze Liu, Jiajun Lv, Xingxing Zuo, Yun Ye, Guan Huang, Xiaofeng Wang, Zheng Zhu"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09330v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 用于机器人策略学习的视频动作生成
tldr: 针对具身智能领域高质量数据获取成本高的问题，本文提出VAG框架，这是一种基于流匹配的双流生成模型，旨在同步生成视频及其对应的动作轨迹。VAG通过同步去噪过程和自适应3D池化机制，实现了视频上下文与动作分支的高效对齐，克服了现有模型在跨模态一致性及生成效率上的不足。实验表明，VAG生成的合成数据能显著增强下游策略的泛化性能，为具身数据合成提供了实用且高效的解决方案。
selection_source: fresh_fetch
motivation: 现有的世界模型缺乏对齐的动作输出，而两阶段生成方法存在效率低下和误差累积的问题，限制了合成数据在机器人策略学习中的应用。
method: 提出一种基于流匹配的双流框架，利用同步去噪和自适应3D池化机制将视频全局上下文传递至动作分支，实现视频与动作的联合生成。
result: 在仿真和真实世界任务中，VAG能够生成高质量且对齐的视频-动作对，并能通过合成数据预训练显著提升下游策略的泛化能力。
conclusion: VAG证明了联合生成视频与动作在具身数据合成中的潜力，为构建高性能的机器人世界-动作模型提供了新路径。
---

## 摘要
最近，在大型人类遥操作数据上训练的机器人基础模型的进展，使得机器人能够执行日益复杂的现实世界任务。然而，扩展这些系统仍然很困难，因为收集特定任务的演示数据既昂贵又费力。合成数据，特别是生成的视频，提供了一个充满前景的方向，但现有的世界模型（WMs）并不直接适用于策略学习，因为它们不提供成对的动作轨迹。世界-动作（WA）模型通过在输出视觉结果的同时预测动作，部分解决了这一问题，但往往缺乏强大的视频-动作对齐；而先生成视频再推断动作的两阶段流水线则会引入低效和误差累积。为了解决这些局限性，我们提出了 VAG，这是一个基于流匹配（flow-matching）的统一双流框架，能够在视觉和语言条件的约束下共同生成视频和动作。通过同步两个分支的去噪过程，并使用自适应 3D 池化机制将紧凑的全局视频上下文传输到动作分支，VAG 提高了生成过程中的跨模态一致性。在模拟和真实世界设置中，VAG 都能产生具有竞争力的预测质量且对齐的视频-动作对，支持可执行的轨迹回放，并提供有用的合成预训练数据以提高下游策略的泛化能力，这表明了其作为具身数据合成的实用世界-动作模型的潜力。

## Abstract
Recent advances in robot foundation models trained on large-scale human teleoperation data have enabled robots to perform increasingly complex real-world tasks. However, scaling these systems remains difficult because collecting task-specific demonstrations is expensive and labor-intensive. Synthetic data, especially generated videos, offer a promising direction, but existing World Models (WMs) are not directly suitable for policy learning since they do not provide paired action trajectories. World-Action (WA) models partially address this by predicting actions with visual outputs, yet often lack strong video-action alignment, while two-stage pipelines that generate video first and then infer actions introduce inefficiency and error accumulation. To address these limitations, we propose VAG, a unified flow-matching-based dual-stream framework that jointly generates video and action under visual and language conditioning. By synchronizing denoising in both branches and using an adaptive 3D pooling mechanism to transfer compact global video context to the action branch, VAG improves cross-modal consistency during generation. Across both simulated and real-world settings, VAG produces aligned video-action pairs with competitive prediction quality, supports executable trajectory replay, and provides useful synthetic pretraining data that improves downstream policy generalization, indicating its potential as a practical world-action model for embodied data synthesis.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **VAG (Dual-Stream Video-Action Generation)**，一个旨在解决具身智能（机器人）数据瓶颈问题的生成式框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：扩展机器人基础模型（如 VLA）需要海量的遥操作数据，但人工采集成本极高。
*   **研究背景**：
    *   **世界模型 (WM)**：能生成逼真视频，但缺乏配对的动作轨迹，无法直接用于策略训练。
    *   **世界-动作模型 (WA)**：现有模型多将视频生成作为动作预测的辅助任务，或采用“先生成视频、再提取动作”的两阶段方案，这会导致跨模态不一致、效率低下及累积误差。
*   **整体含义**：VAG 旨在通过一个统一的生成框架，**同步、对齐地生成长时程（约10秒）的视频和动作对**，从而为机器人策略学习提供高质量的合成数据。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：基于**流匹配 (Flow-Matching)** 理论，构建双流（视频流与动作流）并行架构，在去噪过程中实现时空同步。
*   **关键技术细节**：
    *   **双流架构**：视频分支基于 Diffusion Transformer (DiT)，动作分支基于 1D U-Net。两者共享相同的流匹配公式和去噪步数。
    *   **自适应 3D 池化 (Adaptive 3D Pooling)**：这是连接两者的桥梁。在每个去噪步中，将视频分支预测的“清晰”潜变量（latent）通过 3D 池化压缩为全局嵌入（Embedding），作为动作分支的条件输入。
    *   **同步去噪**：视频和动作在相同的噪声强度下同步演化，确保生成的视觉画面与物理动作在语义和时序上严格对齐。
    *   **条件控制**：模型同时接收初始帧图像和文本指令（通过 T5-XXL 编码）作为引导。

### 3. 实验设计
*   **数据集/场景**：
    *   **AgiBot 数据集**：大型真实世界机器人操作数据（G1 双足机器人）。
    *   **LIBERO 数据集**：模拟环境下的基准测试，包含空间、目标、长程等多种任务。
    *   **自采集数据集**：使用 Agilex 机器人采集的餐具拾放任务数据。
*   **对比方法 (Baselines)**：
    *   **视频生成**：对比 SVD、Wan2.2、Cosmos-Predict2。
    *   **动作提取**：对比“VAG视频 + ResNet/AnyPos”的两阶段动作回归方案。
    *   **下游应用**：对比使用与不使用 VAG 合成数据预训练的 $\pi_{0.5}$ (VLA 模型)。

### 4. 资源与算力
*   **算力配置**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练时长**：在 AgiBot 数据集上训练了 **40,000 次迭代**（Iteration），在 LIBERO 上训练了 20,000 次。
*   **推理设置**：推理时采用 35 步去噪，生成频率为 10Hz，视频分辨率为 480P。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从视频质量评估（FVD, FID, SSIM 等）到动作准确性评估（欧氏距离, 成功率），再到真实机器人部署。
*   **充分性与客观性**：
    *   实验在模拟器和真实世界中均得到了验证。
    *   通过消融实验证明了双流同步生成优于传统的两阶段提取方法。
    *   **局限性**：虽然在特定任务上表现良好，但实验任务的复杂度和多样性（如极端环境、多物体交互）仍有待进一步扩展。

### 6. 主要结论与发现
*   **对齐性强**：VAG 能够生成长达 10 秒且高度对齐的视频-动作序列，克服了累积误差。
*   **提升泛化性**：使用 VAG 生成的合成数据进行预训练，可将下游 VLA 模型在真实世界任务中的成功率从 **35% 提升至 55%**（绝对提升 20%）。
*   **可作为策略**：VAG 生成的动作轨迹可以直接在真实机器人上回放并完成任务，证明其具备作为“世界-动作策略”的潜力。

### 7. 优点（亮点）
*   **统一性**：将视频和动作生成整合进同一个流匹配框架，设计简洁高效。
*   **即插即用**：利用了预训练视频大模型（Cosmos）的先验知识，降低了从头训练的难度。
*   **实用性**：直接针对“数据合成”这一痛点，展示了合成数据对提升机器人泛化能力的实际价值。

### 8. 不足与局限
*   **单向引导**：目前仅实现了视频特征引导动作生成，动作信息尚未反向作用于视频分支（即视频生成不感知动作控制信号）。
*   **模型容量**：动作分支仍使用较小的 U-Net，未来可能需要升级为 DiT 以匹配视频分支的表达能力。
*   **数据规模**：虽然证明了有效性，但在更大规模、更通用的开放世界场景下的表现仍需验证。

（完）
