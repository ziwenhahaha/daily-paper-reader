---
title: "Harnessing Embodied Agents: Runtime Governance for Policy-Constrained Execution"
title_zh: 驾驭具身智能体：策略约束执行的运行时治理
authors: "Xue Qin, Simin Luan, John See, Cong Yang, Zhijun Li"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07833v2"
tags: ["query:q12"]
score: 9.0
evidence: 具身智能体策略约束执行的运行时治理
tldr: 随着具身智能体从推理转向实际执行，如何确保其运行时行为符合策略成为核心挑战。本文提出一种策略约束执行框架，通过将治理逻辑从智能体认知中分离，构建独立的运行时治理层，实现策略检查、监控、回滚及人工干预。实验证明，该框架能有效拦截未授权行为并显著提升系统安全性与恢复能力，为具身智能体系统设计提供了标准化治理的新范式。
selection_source: fresh_fetch
motivation: 现有的具身智能体安全逻辑多嵌入在智能体内部，导致执行控制难以标准化、审计和灵活调整。
method: 提出一种独立的运行时治理层，通过策略检查、能力准入、执行监控和回滚机制实现对智能体行为的外部化约束。
result: "实验表明该方法拦截了96.2%的未授权行为，将不安全执行率从100%降至22.2%，并实现了91.4%的合规恢复率。"
conclusion: 将运行时治理作为系统级问题并采用策略约束执行，是确保具身智能体安全可靠运行的关键设计原则。
---

## 摘要
具身智能体正从被动推理系统演变为与工具、机器人及物理环境交互的主动执行器。一旦被授予执行权限，核心挑战便在于如何在运行时保持动作的可治理性。现有方法将安全与恢复逻辑嵌入智能体循环内部，导致执行控制难以标准化、审计和适配。本文认为，具身智能不仅需要更强大的智能体，还需要更强大的运行时治理。我们提出了一个策略约束执行框架，将智能体认知与执行监督分离。治理功能被外部化到一个专门的运行时层，负责执行策略检查、能力准入、执行监控、回滚处理和人工干预。我们形式化了具身智能体、具身能力模块（ECMs）与运行时治理层之间的控制边界，并通过跨三个治理维度的 1000 次随机模拟实验进行了验证。结果显示，该框架拦截了 96.2% 的未授权动作，在运行时漂移情况下将不安全持续执行率从 100% 降低至 22.2%，并在完全符合策略的前提下实现了 91.4% 的恢复成功率，显著优于所有基准方法（p<0.001）。通过将运行时治理重新定义为一等系统问题，本文将策略约束执行定位为具身智能体系统的关键设计原则。

## Abstract
Embodied agents are evolving from passive reasoning systems into active executors that interact with tools, robots, and physical environments. Once granted execution authority, the central challenge becomes how to keep actions governable at runtime. Existing approaches embed safety and recovery logic inside the agent loop, making execution control difficult to standardize, audit, and adapt.   This paper argues that embodied intelligence requires not only stronger agents, but stronger runtime governance. We propose a framework for policy-constrained execution that separates agent cognition from execution oversight. Governance is externalized into a dedicated runtime layer performing policy checking, capability admission, execution monitoring, rollback handling, and human override.   We formalize the control boundary among the embodied agent, Embodied Capability Modules (ECMs), and runtime governance layer, and validate through 1000 randomized simulation trials across three governance dimensions. Results show 96.2% interception of unauthorized actions, reduction of unsafe continuation from 100% to 22.2% under runtime drift, and 91.4% recovery success with full policy compliance, substantially outperforming all baselines (p<0.001). By reframing runtime governance as a first-class systems problem, this paper positions policy-constrained execution as a key design principle for embodied agent systems.

---

## 论文详细总结（自动生成）

这是一份关于论文《Harnessing Embodied Agents: Runtime Governance for Policy-Constrained Execution》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
随着具身智能体（Embodied Agents）从单纯的对话推理转向在物理世界中操作工具和机器人，其执行权限的扩大带来了严峻的系统性挑战。
*   **研究动机**：现有的安全约束、故障恢复和决策限制通常被硬编码在智能体的 Prompt 或内部循环中。这种“内嵌式”设计导致执行控制难以标准化、难以审计，且无法灵活适配不同的部署环境（如从模拟环境迁移到真实人类协作环境）。
*   **核心问题**：当智能体拥有执行权时，如何确保其行为在运行时（Runtime）始终受到显式策略的约束，而不是完全依赖于智能体模型本身的“自觉”？

### 2. 方法论：核心思想与技术细节
论文提出了**策略约束执行（Policy-Constrained Execution）**框架，核心思想是**“认知与监督分离”**。
*   **三实体控制边界**：
    1.  **具身智能体（Agent）**：负责高层推理、规划和提出“能力调用提议”。
    2.  **能力包（Capability Package）**：模块化的执行单元（如抓取、导航），封装了具体的动作逻辑和元数据。
    3.  **运行时治理层（Governance Layer）**：独立的中间件，决定提议是否允许执行、如何修改以及何时干预。
*   **六大核心组件**：
    *   **能力准入（Admission）**：检查智能体是否有权调用特定能力。
    *   **策略守卫（Policy Guard）**：根据环境配置文件（Environment Profile）动态调整执行参数（如在人机共存区强制限速）。
    *   **执行观察者（Watcher）**：实时监控传感器流，检测漂移、超时或违反策略的行为。
    *   **恢复与回滚管理器**：当执行失败时，执行受控的重试或物理状态回滚。
    *   **人工覆盖接口（Human Override）**：高风险动作的强制人工审批。
    *   **审计与遥测**：记录全生命周期的执行轨迹，用于责任追溯。
*   **执行公式**：执行动作 $E_t$ 不再直接等于智能体的意图 $P_t$，而是治理函数 $G_{OV}$ 对意图、能力属性、当前策略和环境状态的综合映射结果。

### 3. 实验设计
*   **实验场景**：在 Gazebo Fortress 模拟器中构建，使用搭载 UR5e 机械臂的移动机器人。
*   **任务类型**：涵盖导航（区域限制）、操纵（力限制抓取）和长程复合任务（取物并送达）。
*   **Benchmark 与对比方法**：
    *   **B1 (Direct Execution)**：无治理，智能体直接控制。
    *   **B2 (Static Rule)**：仅有预执行检查，无运行时监控。
    *   **B3 (Capability-Internal)**：安全逻辑分散在各个能力模块内部，缺乏统一治理。
*   **评估维度**：未授权动作拦截率（UAIR）、运行时违规检测率（RVDR）、恢复成功率（RSR）及执行延迟。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **原因分析**：由于该研究侧重于系统架构和运行时逻辑验证，而非大规模模型训练，其核心开销在于模拟器的运行和中间件的逻辑推理。文中给出了详细的**延迟分析**：治理层的预执行开销小于 1.5μs，每步监控开销约 0.55μs，证明了该框架在实时控制系统中的可行性。

### 5. 实验数量与充分性
*   **实验规模**：进行了 **1000 次随机试验**（5 个随机种子 × 200 次试验），涵盖了 6 种违规类型（超力、超速、重试耗尽、后验失败、区域违规、人类接近）。
*   **充分性评价**：
    *   **消融实验**：逐一移除观察者、恢复管理器等组件，验证了各模块的独立贡献。
    *   **人工干预评估**：专门测试了在高风险场景下人工审批接口的拦截有效性。
    *   **统计显著性**：所有核心指标均进行了配对 t 检验（p < 0.001），结果具有统计学意义。实验设计较为客观、公平，覆盖了从准入到恢复的全流程。

### 6. 主要结论与发现
*   **高效拦截**：框架实现了 **96.2%** 的未授权动作拦截率，远高于静态规则（59.5%）。
*   **安全性提升**：在发生运行时漂移时，将不安全持续执行率从 100% 降低至 **22.2%**。
*   **可靠恢复**：实现了 **91.4%** 的恢复成功率，且所有恢复动作均符合安全策略。
*   **极低开销**：治理逻辑的引入对机器人控制循环（通常为 ms 级）几乎没有感知延迟（μs 级）。

### 7. 优点与亮点
*   **解耦设计**：首次将具身智能的“大脑”（规划）与“警察”（治理）在系统层面完全解耦，支持策略的动态加载而无需重新训练智能体。
*   **环境敏感性**：通过“环境配置文件”，实现了同一套智能体代码在模拟器（宽松）与现实世界（严格）之间的无缝策略切换。
*   **全生命周期覆盖**：不仅关注“能不能做”（准入），还关注“做得对不对”（监控）和“坏了怎么办”（恢复）。

### 8. 不足与局限
*   **实验环境局限**：所有验证均在**模拟环境**中完成，尚未在复杂的真实物理世界（存在传感器噪声、通信延迟等）中进行大规模部署验证。
*   **检测漏报风险**：运行时违规检测率（RVDR）为 61.3%，意味着仍有约 38% 的细微违规未能被及时捕获，这受限于观察者的灵敏度设置。
*   **适配性限制**：框架假设能力是模块化的（Capability Packages）。对于完全端到端（End-to-End）的视觉-动作策略（如直接输出关节扭矩的模型），该框架的切入点和治理粒度仍需进一步探索。
*   **策略编写依赖**：治理的效果高度依赖于人类专家定义的策略质量，若策略定义存在漏洞，框架可能失效。

（完）
