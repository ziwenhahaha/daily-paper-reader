---
title: Adaptive Action Chunking at Inference-time for Vision-Language-Action Models
title_zh: 视觉-语言-动作模型的推理时自适应动作分块
authors: "Yuanchang Liang, Xiaobo Wang, Kai Wang, Shuo Wang, Xiaojiang Peng, Haoyu Chen, David Kim Huat Chua, Prahlad Vadakkepat"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04161v2"
tags: ["query:wam-vla"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型的自适应动作分块
tldr: 针对视觉-语言-动作（VLA）模型中固定动作分块导致的反应迟钝或动作不连贯问题，本文提出了一种自适应动作分块（AAC）策略。该方法利用动作熵作为关键指标，在推理阶段动态调整动作序列的长度，以平衡模型的反应灵敏度与动作一致性。实验证明，AAC在多种仿真和真实机器人任务中显著提升了操作性能，解决了固定分块带来的局限性。
selection_source: fresh_fetch
motivation: 现有VLA模型多采用固定的动作分块长度，难以在应对环境变化的反应速度与动作执行的平滑性之间取得平衡。
method: 提出一种自适应动作分块（AAC）策略，利用动作熵作为线索，在推理时根据当前预测动态确定最优的动作分块大小。
result: 在大量模拟和真实世界的机器人操作实验中，该方法显著优于目前最先进的固定分块方案。
conclusion: 通过动态调整动作分块长度，AAC有效提升了VLA模型在复杂任务中的灵活性和执行效率。
---

## 摘要
在视觉-语言-动作（VLA）模型中，动作分块（即在不进行中间重新规划的情况下执行一系列动作）是提高机器人操纵能力的关键技术。然而，较大的分块尺寸会降低模型对新信息的响应能力，而较小的分块尺寸则会增加模态跳变（mode-jumping）的可能性，即由于分块之间的不连续性而导致的动作抖动。因此，选择最优的分块尺寸是平衡模型反应性与一致性的迫切需求。遗憾的是，当前 VLA 模型的主流趋势是在推理阶段采用经验性的固定分块长度，这阻碍了它们在各种操纵任务中的优越性和可扩展性。为了解决这一问题，我们提出了一种新颖的自适应动作分块（AAC）策略，该策略利用动作熵作为线索，根据当前的预测自适应地确定分块尺寸。在广泛的模拟和真实世界机器人操纵任务上进行的实验表明，我们的方法相比于现有的先进方案显著提高了性能。视频和源代码已在 https://lance-lot.github.io/adaptive-chunking.github.io/ 公开。

## Abstract
In Vision-Language-Action (VLA) models, action chunking (i.e., executing a sequence of actions without intermediate replanning) is a key technique to improve robotic manipulation abilities. However, a large chunk size reduces the model's responsiveness to new information, while a small one increases the likelihood of mode-jumping, jerky behavior resulting from discontinuities between chunks. Therefore, selecting the optimal chunk size is an urgent demand to balance the model's reactivity and consistency. Unfortunately, a dominant trend in current VLA models is an empirical fixed chunk length at inference-time, hindering their superiority and scalability across diverse manipulation tasks. To address this issue, we propose a novel Adaptive Action Chunking (AAC) strategy, which exploits action entropy as the cue to adaptively determine the chunk size based on current predictions. Extensive experiments on a wide range of simulated and real-world robotic manipulation tasks have demonstrated that our approach substantially improves performance over the state-of-the-art alternatives. The videos and source code are publicly available at https://lance-lot.github.io/adaptive-chunking.github.io/.

---

## 论文详细总结（自动生成）

这篇论文针对视觉-语言-动作（VLA）模型在机器人操纵中的“动作分块”策略提出了创新性的改进。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在 VLA 模型中，**动作分块（Action Chunking）**（即一次性执行序列动作而非逐帧规划）的长度选择存在两难困境。
    *   **大分块**：动作更平滑、推理效率高，但对环境变化的**反应性（Reactivity）**差。
    *   **小分块**：反应快，但容易导致动作不连贯、抖动（Mode-jumping），且计算开销大。
*   **研究动机**：现有模型（如 GR00T, $\pi_0$）通常在推理时使用固定的经验值，但不同任务（如搬运 vs. 精细旋转）对分块长度的需求不同。作者旨在开发一种无需额外训练、能在推理时动态调整分块长度的策略。

### 2. 方法论：自适应动作分块 (AAC)
*   **核心思想**：利用**动作熵（Action Entropy）**作为预测不确定性的指标。当模型对未来动作感到“困惑”（熵高）时，缩短执行长度并频繁重规划；当模型很“自信”（熵低）时，执行较长序列以保持一致性。
*   **关键技术细节**：
    1.  **并行采样**：在每个推理步，并行生成 $N$ 个候选动作分块。
    2.  **熵计算**：
        *   **连续动作**（平移/旋转）：计算高斯微分熵。
        *   **离散动作**（夹爪）：计算离散熵。
    3.  **最优长度确定**：计算不同分块长度下的平均动作熵，寻找熵增量最大的转折点（Maximum Differential Point）。
    4.  **最小动作约束 ($\xi$)**：引入动作幅值阈值，确保分块长度不会因微小扰动而过短，从而兼顾计算效率。
*   **算法流程**：输入 $N$ 个候选分块 $\rightarrow$ 计算每步熵值 $\rightarrow$ 寻找熵突变点 $\rightarrow$ 结合最小幅值约束输出最优长度 $h^*$ $\rightarrow$ 执行前 $h^*$ 个动作。

### 3. 实验设计
*   **数据集/场景**：
    *   **RoboCasa Kitchen**：包含 24 种厨房操纵任务（重定位、容器操作、旋转、按钮）。
    *   **LIBERO**：包含 4 个子集（空间、长程、物体、目标），共 40 个任务。
    *   **真实世界**：单臂机器人执行捡香蕉、按紧急按钮、抽屉收纳 3 类任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：GR00T N1.5（基于 Flow-matching 的 VLA 基础模型）。
    *   **对比策略**：固定分块长度（$h=2, 4, 8, 12, 16$）以及 $\pi_{0.5}$ 等不同骨干网络。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA A800 GPU** 进行混合精度微调。
*   **训练细节**：
    *   RoboCasa：微调 100 条演示轨迹，训练 90 个 Epoch。
    *   LIBERO：40 个任务联合训练 90 个 Epoch。
    *   真实世界：每个任务微调 300 个 Epoch。
*   **推理延迟**：默认采样数 $N=20$ 时，相比固定分块会增加约 **20ms** 的延迟，在高端 GPU 上影响较小。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了超过 64 个仿真任务和 3 个真实场景，每项任务进行 20 到 100 次 Rollouts 测试。
*   **充分性**：
    *   对比了多种固定长度，证明了“不存在一个通用的最优固定长度”。
    *   进行了消融实验（如采样数 $N$ 的影响）。
    *   验证了在不同骨干网络（GR00T 和 $\pi_{0.5}$）上的通用性。
    *   **客观性**：实验设计涵盖了分布外（OOD）扰动测试，评估了模型的鲁棒性。

### 6. 主要结论与发现
*   **性能提升**：AAC 在 RoboCasa 上平均提升 2.3% 成功率，在真实世界任务中平均提升 **15%**。
*   **语义对齐**：定性分析发现，AAC 自动选择的长度符合人类直觉：在**接近物体**（需精细操作）时分块变短，在**运输物体**（粗略移动）时分块变长。
*   **安全性**：在真实实验中，AAC 能有效减少夹爪与桌面的碰撞，因为它能通过高熵信号及时停止不确定的动作。

### 7. 优点与亮点
*   **即插即用**：无需修改模型架构，无需重新训练，直接应用于现有的扩散/流匹配 VLA 模型。
*   **动态平衡**：成功解决了反应性与一致性之间的固有矛盾。
*   **通用性强**：公式设计同时兼顾了连续和离散动作空间，适用于多种机器人形态。

### 8. 不足与局限
*   **计算开销**：虽然并行采样缓解了压力，但计算熵和多次采样仍比单次推理耗时，对于算力极度受限的嵌入式设备可能存在挑战。
*   **采样依赖**：AAC 的效果高度依赖于采样数 $N$ 的质量，如果模型本身预测极差，熵值可能无法提供有效引导。
*   **阈值超参**：最小动作幅值 $\alpha$ 仍需根据机器人硬件进行一定的经验设置。

（完）
