---
title: "Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization"
title_zh: 言行一致：通过多模态智能体策略优化弥合结合图像思考中的推理-行动差距
authors: "Wenhao Yang, Yu Xia, Jinlong Huang, Shiyin Lu, Qing-Guo Chen, Zhao Xu, Weihua Luo, Kaifu Zhang, Yuchen Zhou, Xiaobo Xia, Yuanyu Wan, Lijun Zhang, Tat-Seng Chua"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.06777v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 针对推理-动作差距的多模态智能体策略优化
tldr: 针对多模态大模型在利用视觉工具推理时存在的“言行不一”问题，即文本推理与视觉动作不匹配，本文提出了多模态智能体策略优化（MAPO）。该方法要求模型对工具获取的视觉内容生成文本描述，并将描述与实际观察的语义对齐度与任务奖励相结合，通过改进的优势估计缩小推理与行动间的差距。实验证明，MAPO在多个视觉推理基准上表现优异，并能有效降低训练梯度方差。
selection_source: fresh_fetch
motivation: 现有的强化学习方法仅依赖结果奖励，忽略了模型在多轮推理中可能出现的文本逻辑合理但视觉操作错误的问题。
method: 提出MAPO方法，通过强制模型生成视觉内容的文本描述，并将描述与实际观察的语义一致性融入优势估计函数中。
result: 实验结果显示，MAPO在多个视觉推理基准上取得了领先性能，并从理论和实践上证明了其能有效减少梯度方差。
conclusion: MAPO成功桥接了多模态推理中的文本逻辑与视觉行动，为提升智能体在复杂视觉任务中的可靠性提供了有效方案。
---

## 摘要
多模态大语言模型（MLLMs）的最新进展激励模型在多轮推理过程中通过主动调用视觉工具来“结合图像思考”。常见的依赖基于结果奖励的强化学习（RL）实践忽略了一个事实，即文本的合理性往往掩盖了执行层面的失败，这意味着模型在智能体推理轨迹中可能会表现出直观的文本推理，但执行的视觉动作却不精确或不相关。这种推理与行动之间的差异引入了在多轮推理过程中不断累积的噪声，严重降低了模型的多模态推理能力，并可能导致训练崩溃。在本文中，我们提出了多模态智能体策略优化（MAPO），旨在弥合模型在多模态思维链（MCoT）中生成的文本推理与视觉动作之间的差距。具体而言，MAPO 要求模型为通过工具使用获取的视觉内容生成显式的文本描述。随后，我们采用了一种新颖的优势估计方法，将这些描述与实际观测之间的语义对齐与任务奖励相结合。理论研究证明了 MAPO 背后的合理性，它本质上降低了梯度的方差；大量的实验表明，我们的方法在多个视觉推理基准测试中均取得了优越的性能。

## Abstract
Recent advancements in Multimodal Large Language Models (MLLMs) have incentivized models to ``think with images'' by actively invoking visual tools during multi-turn reasoning. The common Reinforcement Learning (RL) practice of relying on outcome-based rewards ignores the fact that textual plausibility often masks executive failure, meaning that models may exhibit intuitive textual reasoning while executing imprecise or irrelevant visual actions within their agentic reasoning trajectories. This reasoning-action discrepancy introduces noise that accumulates throughout the multi-turn reasoning process, severely degrading the model's multimodal reasoning capabilities and potentially leading to training collapse. In this paper, we introduce Multimodal Agentic Policy Optimization (MAPO), bridging the gap between textual reasoning and visual actions generated by models within their Multimodal Chain-of-Thought (MCoT). Specifically, MAPO mandates the model to generate explicit textual descriptions for the visual content obtained via tool usage. We then employ a novel advantage estimation that couples the semantic alignment between these descriptions and the actual observations with the task reward. Theoretical findings are provided to justify the rationale behind MAPO, which inherently reduces the variance of gradients, and extensive experiments demonstrate that our method achieves superior performance across multiple visual reasoning benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《Walk the Talk: Bridging the Reasoning-Action Gap for Thinking with Images via Multimodal Agentic Policy Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是多模态大语言模型（MLLM）在执行“结合图像思考”（Thinking with Images）任务时的**推理-行动差距（Reasoning-Action Gap）**。
*   **背景**：当前的MLLM（如OpenAI o3/o4-mini）已进化为能够主动调用视觉工具（如缩放、裁剪）进行多轮推理的智能体。
*   **核心问题**：现有的强化学习（RL）训练主要依赖“基于结果的奖励”（Outcome-based Reward），即只看最终答案是否正确。这导致模型可能出现“言行不一”：文本推理逻辑看起来很合理，但实际执行的视觉动作（如裁剪区域）却是错误或无关的。如果模型靠“瞎猜”碰巧答对，RL会错误地强化这种虚假的关联，导致训练不稳定、噪声大，甚至在长时程优化中出现训练崩溃。

### 2. 方法论：MAPO（多模态智能体策略优化）
MAPO的核心思想是强制模型“言行一致”，即视觉执行必须忠实于文本意图。
*   **核心流程**：
    1.  **强制标签生成（Talk）**：在调用工具（如zoom-in）时，强制模型生成一个描述性标签 $y_t$，明确说明它预期在目标区域看到什么。
    2.  **语义评分（Walk the Talk）**：使用轻量级预训练 CLIP 模型计算生成的标签 $y_t$ 与工具执行后返回的实际观察图像 $I_t$ 之间的余弦相似度。
    3.  **轨迹感知折扣**：引入折扣因子 $\lambda$，对多轮操作中的语义分数进行加权，既鼓励早期探索，又惩罚冗长无效的轨迹，防止“奖励作弊”（Reward Hacking）。
    4.  **优势估计集成**：将语义一致性得分与最终结果奖励结合，通过组相对策略优化（如GRPO/GSPO）进行策略更新。
*   **理论支撑**：论文证明了MAPO具有**双重方差削减**效应：
    *   **空间方差削减**：通过组聚合（Group Aggregation）过滤掉不同视觉提示带来的随机不确定性。
    *   **信号方差削减**：通过稠密的语义评分（过程监督）降低了稀疏结果奖励带来的信号噪声。

### 3. 实验设计
*   **数据集/场景**：专注于需要精细视觉推理的高分辨率基准测试：
    *   **V***：视觉搜索任务。
    *   **HR-Bench**：涵盖4K/8K超高分辨率图像的推理任务。
    *   **MME-Realworld-Lite**：复杂的真实世界视觉细节理解。
*   **对比方法**：
    *   **闭源模型**：GPT-5（预估版）、Gemini 2.5 Pro。
    *   **开源智能体模型**：DeepEyes、Thyme、Mini-o3、DeepEyesV2。
    *   **RL算法基准**：PPO、GRPO、DAPO、GSPO。
*   **实验底座**：统一使用 **Ovis2.5-9B** 作为基础模型进行RL微调。

### 4. 资源与算力
*   **框架**：基于 `verl` 强化学习框架和 `vLLM` 推理引擎。
*   **并行技术**：使用 FSDP（全分片数据并行）和优化器状态卸载（Offloading）。
*   **具体参数**：全局 Batch Size 为 96，每张 GPU 的 Micro Batch Size 为 4。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号（如 A100 或 H100）以及总的训练时长，但提到了使用了梯度检查点（Gradient Checkpointing）和 Triton 后端内核来优化吞吐量。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个主流高难度基准上进行了测试，并细分了不同分辨率（4K/8K）的子集。
*   **消融与分析**：
    *   对比了 5 种不同的 RL 算法在同一底座上的表现。
    *   绘制了详细的奖励曲线（Reward Curves），展示了训练稳定性。
    *   提供了推理轨迹的可视化对比。
*   **充分性评价**：实验设计较为充分且公平，通过在相同底座模型上对比不同 RL 算法，排除了模型参数量带来的干扰，客观地证明了 MAPO 在解决训练崩溃和提升语义一致性方面的有效性。

### 6. 主要结论与发现
*   **性能领先**：MAPO 在所有评估基准上均优于现有的开源智能体模型，并显著超过了 GRPO 等标准 RL 方法。
*   **训练稳定性**：标准 RL（如 GRPO）在长时程训练中容易出现性能剧降（崩溃），而 MAPO 保持了稳健的上升趋势，具有极佳的可扩展性。
*   **弥合差距**：可视化结果证实，MAPO 训练出的模型能够更精准地执行视觉动作，减少了“幻觉”和无效操作。

### 7. 优点
*   **简单高效**：无需昂贵的人工过程标注，利用预训练 CLIP 即可实现自动化的过程监督。
*   **理论严谨**：不仅有实验支撑，还通过数学证明解释了方差削减的原理。
*   **通用性强**：虽然本文针对视觉工具，但其“意图-执行”对齐的思想可扩展至 Web 浏览、代码执行等其他智能体领域。

### 8. 不足与局限
*   **标签依赖**：模型生成描述性标签的能力直接影响奖励质量，如果模型初始能力太弱，可能难以启动。
*   **CLIP 偏见**：语义评分受限于 CLIP 本身的视觉理解偏差，可能无法捕捉极度细微或超出 CLIP 训练分布的特征。
*   **工具范围**：目前主要验证了缩放和裁剪工具，对于更复杂的交互工具（如图像编辑、多图关联）的泛化性仍需进一步验证。

（完）
