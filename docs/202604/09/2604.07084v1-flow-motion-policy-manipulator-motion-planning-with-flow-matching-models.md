---
title: "Flow Motion Policy: Manipulator Motion Planning with Flow Matching Models"
title_zh: 流运动策略：基于流匹配模型的机械臂运动规划
authors: "Davood Soleymanzadeh, Xiao Liang, Minghui Zheng"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07084v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 使用流匹配模型的机械臂运动规划
tldr: 针对现有端到端神经运动规划器生成路径单一且缺乏推理优化的问题，本文提出了Flow Motion Policy (FMP)。该方法利用流匹配（Flow Matching）生成模型捕捉规划数据集的多模态特性，支持推理时的“N选1”采样策略。通过生成多条候选路径并筛选首个无碰撞解，FMP显著提升了机械臂在复杂环境下的规划成功率和效率，为端到端运动规划提供了更具鲁棒性的生成式方案。
selection_source: fresh_fetch
motivation: 现有神经运动规划器多为确定性输出，难以捕捉规划任务的多模态分布，且缺乏在推理阶段进行路径优化的能力。
method: 采用流匹配生成模型构建随机运动策略，通过建模可行路径分布实现多候选路径的并行生成与碰撞评估。
result: 在与传统采样算法及主流神经规划器的对比测试中，该方法在规划成功率和计算效率上均表现出显著优势。
conclusion: 研究证明了随机生成式模型结合推理时采样优化能有效提升机械臂端到端运动规划的性能和可靠性。
---

## 摘要
最近，开环端到端神经运动规划器被提出，用于改进机器人机械臂的运动规划。这些方法能够直接根据传感器观测进行规划，而无需在规划过程中依赖特权碰撞检测器。然而，许多现有方法在不同运行中针对给定工作空间仅生成单一路径，且未利用其开环结构进行推理时优化。为解决这一局限性，我们引入了流运动策略（Flow Motion Policy），这是一种用于机器人机械臂的开环端到端神经运动规划器，它利用流匹配方法的随机生成公式来捕捉规划数据集固有的多模态性。通过对可行路径的分布进行建模，流运动策略实现了高效的推理时 $N$ 选 1（best-of-$N$）采样。该方法生成多个端到端候选路径，在规划后评估其碰撞状态，并执行第一个无碰撞的解。我们将流运动策略与具有代表性的基于采样的运动规划方法和神经运动规划方法进行了基准测试。评估结果表明，流运动策略提高了规划成功率和效率，突显了随机生成策略在端到端运动规划和推理时优化中的有效性。实验评估视频可通过此链接获取。

## Abstract
Open-loop end-to-end neural motion planners have recently been proposed to improve motion planning for robotic manipulators. These methods enable planning directly from sensor observations without relying on a privileged collision checker during planning. However, many existing methods generate only a single path for a given workspace across different runs, and do not leverage their open-loop structure for inference-time optimization. To address this limitation, we introduce Flow Motion Policy, an open-loop, end-to-end neural motion planner for robotic manipulators that leverages the stochastic generative formulation of flow matching methods to capture the inherent multi-modality of planning datasets. By modeling a distribution over feasible paths, Flow Motion Policy enables efficient inference-time best-of-$N$ sampling. The method generates multiple end-to-end candidate paths, evaluates their collision status after planning, and executes the first collision-free solution. We benchmark the Flow Motion Policy against representative sampling-based and neural motion planning methods. Evaluation results demonstrate that Flow Motion Policy improves planning success and efficiency, highlighting the effectiveness of stochastic generative policies for end-to-end motion planning and inference-time optimization. Experimental evaluation videos are available via this \href{https://zh.engr.tamu.edu/wp-content/uploads/sites/310/2026/03/FMP-Website.mp4}{link}.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Flow Motion Policy (FMP)** 的新型神经运动规划方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的端到端神经运动规划器大多是**确定性**的，即对于给定的环境和目标，只能生成单一路径。这导致它们无法捕捉运动规划中固有的“多模态”特性（即到达目标有多种可行路径），也无法在推理阶段通过生成多个候选方案来优化成功率。
*   **研究背景**：传统的采样算法（如 RRT）虽然具有随机性，但碰撞检测计算量巨大（占 90% 时间）；而现有的神经规划器虽快，但缺乏灵活性。
*   **动机**：利用**流匹配（Flow Matching）**生成模型构建一个随机运动策略，实现在推理时进行高效的“N 选 1”（Best-of-N）采样优化，从而在不牺牲速度的前提下大幅提升复杂环境中的规划成功率。

### 2. 方法论
*   **核心思想**：将运动规划建模为一个条件生成任务。通过流匹配模型学习从高斯噪声到可行路径增量序列的连续变换。
*   **关键技术细节**：
    *   **输入编码**：使用 PointNet++ 提取机器人和环境的点云特征，使用 MLP 编码当前关节配置和目标配置。
    *   **特征融合**：采用 Transformer Encoder 作为骨干网络，将环境、机器人状态和目标信息作为 Token 进行全局注意力融合。
    *   **流匹配头（Flow Head）**：使用轻量级 MLP 预测向量场。相比扩散模型（Diffusion），流匹配在推理时所需的迭代步数更少，效率更高。
    *   **推理时优化（Best-of-N Sampling）**：在推理阶段，模型并行生成 $N$ 条候选路径。利用 GPU 加速的碰撞检测器（如 cuRobo）对这些路径进行评估，执行第一条无碰撞的路径。
*   **算法流程**：训练阶段通过回归目标向量场来学习路径分布；推理阶段通过 Euler 积分器从噪声中还原路径，并结合并行碰撞检测进行筛选。

### 3. 实验设计
*   **实验场景**：包括桌面（TableTop）、方块（Box）、箱子（Bins）和复杂货架（Shelf Task I/II/III）等多种模拟环境，并进行了 UR5e 机械臂的实机部署。
*   **Benchmark 与对比方法**：
    *   **传统采样算法**：Bi-RRT、BIT*。
    *   **神经启发式采样器**：MPNets、SIMPNet、GAIDE。
    *   **端到端神经规划器**：PerFACT、Neural MP（基于 GMM 的方法）。
*   **评估指标**：规划成功率（Success Rate）和规划耗时（Planning Time）。

### 4. 资源与算力
*   **硬件环境**：实验在一台配备 **NVIDIA RTX 4080 GPU** 的工作站上完成。
*   **算力细节**：文中提到利用 cuRobo 进行并行碰撞检测以加速数据采集和推理优化。虽然未明确给出总训练时长，但强调了模型参数量仅为 **1.4M**，远小于对比方法 Neural MP 的 20M，具有极高的计算效率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了跨 6 种不同复杂度场景的大规模测试。
    *   **消融实验**极其丰富：对比了不同的策略头架构（MLP、U-Net、Transformer、DiT）、对比了流匹配与扩散模型（Diffusion）的性能差异、测试了不同采样数 $N$ 和积分步数对结果的影响。
*   **充分性评价**：实验设计非常全面且客观。通过与多种 SOTA 方法对比，并结合实机验证，充分证明了该方法在处理多模态分布和推理效率方面的优势。

### 6. 主要结论与发现
*   **性能领先**：FMP 在保持极低规划耗时（通常小于 1 秒）的同时，成功率显著高于确定性神经规划器。
*   **流匹配的优越性**：相比基于 GMM 的 Neural MP，流匹配能更精准地建模复杂的路径分布；相比扩散模型，流匹配在推理速度上具有压倒性优势。
*   **推理优化的价值**：通过“N 选 1”采样，FMP 的成功率从 30%-40% 提升至 80% 以上，证明了随机生成模型在运动规划中的必要性。

### 7. 优点
*   **高效性**：模型轻量（1.4M 参数），推理速度快，支持大规模并行采样。
*   **多模态捕捉**：能够生成多样化的候选路径，有效避开局部最优或死胡同。
*   **端到端闭环**：直接从点云到动作，无需复杂的特权信息，易于部署。

### 8. 不足与局限
*   **动态环境限制**：目前的公式假设环境是静态的，未显式处理动态障碍物。
*   **路径质量**：生成的路径可能存在不够平滑的问题，在实机执行前可能仍需简单的后处理。
*   **泛化边界**：在极度狭窄或从未见过的复杂货架场景中，如果基础策略无法生成任何可行解，推理优化也难以奏效（如实验中 Shelf Task 的表现略低于传统算法）。

（完）
