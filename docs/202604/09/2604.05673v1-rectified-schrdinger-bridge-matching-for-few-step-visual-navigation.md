---
title: Rectified Schrödinger Bridge Matching for Few-Step Visual Navigation
authors: "Wuyang Luan, Junhui Li, Weiguang Zhao, Wenjian Zhang, Tieru Wu, Rui Ma"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05673v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 具身智能中的视觉导航
tldr: 针对具身智能视觉导航中生成式策略推理速度慢的问题，本文提出修正薛定谔桥匹配（RSBM）框架。该框架通过引入熵正则化参数统一了薛定谔桥与确定性最优传输，利用速度场结构不变性，使单一网络能适应不同正则化强度。RSBM 在减少积分步数的同时保持了多模态覆盖，仅需 3 步推理即可实现高精度导航，显著提升了实时控制性能。
selection_source: fresh_fetch
motivation: 现有的生成式导航策略因高方差随机传输需要大量推理步数，难以满足机器人实时控制的低延迟需求。
method: 提出 RSBM 框架，利用速度场结构不变性，通过调节熵正则化参数在多模态覆盖与路径平滑度之间取得平衡。
result: "在无需蒸馏的情况下，RSBM 仅需 3 步积分即可达到 92% 的导航成功率，并保持极高的动作预测相似度。"
conclusion: RSBM 有效缩小了高保真生成策略与具身智能实时性要求之间的差距，为高效视觉导航提供了新方案。
---

## Abstract
Visual navigation is a core challenge in Embodied AI, requiring autonomous agents to translate high-dimensional sensory observations into continuous, long-horizon action trajectories. While generative policies based on diffusion models and Schrödinger Bridges (SB) effectively capture multimodal action distributions, they require dozens of integration steps due to high-variance stochastic transport, posing a critical barrier for real-time robotic control. We propose Rectified Schrödinger Bridge Matching (RSBM), a framework that exploits a shared velocity-field structure between standard Schrödinger Bridges ($\varepsilon=1$, maximum-entropy transport) and deterministic Optimal Transport ($\varepsilon\to 0$, as in Conditional Flow Matching), controlled by a single entropic regularization parameter $\varepsilon$. We prove two key results: (1) the conditional velocity field's functional form is invariant across the entire $\varepsilon$-spectrum (Velocity Structure Invariance), enabling a single network to serve all regularization strengths; and (2) reducing $\varepsilon$ linearly decreases the conditional velocity variance, enabling more stable coarse-step ODE integration. Anchored to a learned conditional prior that shortens transport distance, RSBM operates at an intermediate $\varepsilon$ that balances multimodal coverage and path straightness. Empirically, while standard bridges require $\geq 10$ steps to converge, RSBM achieves over 94% cosine similarity and 92% success rate in merely 3 integration steps -- without distillation or multi-stage training -- substantially narrowing the gap between high-fidelity generative policies and the low-latency demands of Embodied AI.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **RSBM（Rectified Schrödinger Bridge Matching，修正薛定谔桥匹配）** 的新框架，旨在解决具身智能（Embodied AI）视觉导航中生成式策略推理延迟高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于扩散模型（Diffusion）和薛定谔桥（SB）的生成式导航策略虽然能很好地处理多模态动作分布（即面对障碍物时可以左转也可以右转），但由于其随机传输过程方差较高，通常需要数十步迭代推理（ODE/SDE 积分）才能生成平滑轨迹。
*   **研究背景**：在实时机器人控制中，高延迟是致命的。现有的加速采样技术（如蒸馏或一致性模型）往往需要复杂的多阶段训练，且可能损害轨迹的结构完整性。
*   **整体含义**：论文试图在保持生成模型多模态表达能力的同时，通过理论创新将推理步数压缩至极低水平（如 3 步），以满足实时导航的需求。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将标准薛定谔桥（最大熵传输）与确定性最优传输（流匹配）统一在一个由熵正则化参数 $\varepsilon$ 控制的光谱中。通过调节 $\varepsilon$，在路径的“随机多样性”与“几何直线性”之间取得平衡。
*   **关键技术细节**：
    *   **速度场结构不变性（Theorem 1）**：证明了条件速度场的函数形式在整个 $\varepsilon$ 谱系中是保持不变的。这意味着一个单一的网络参数化方案可以同时适用于不同的正则化强度。
    *   **方差缩减（Proposition 1）**：证明了减小 $\varepsilon$ 可以线性降低条件速度的方差，从而使 ODE 积分轨迹更平直，减少离散化误差。
    *   **条件先验锚定**：引入一个学习到的变分先验网络 $g_\psi$，生成粗略的初始轨迹 $a_T$，缩短了传输距离，使模型只需学习从粗略到精细的残差修正。
    *   **训练目标**：采用基于 $v$-prediction 的模拟免训练流匹配损失函数，直接参数化 ODE 速度场。

### 3. 实验设计
*   **数据集/场景**：
    *   **5 个真实世界数据集**：HuRoN, Recon, SACSoN, SCAND, GoStanford（共约 6 万条轨迹）。
    *   **2 个仿真环境**：基于 Gazebo 的自定义室内环境（Custom Indoor）和城市模拟环境（CitySim）。
*   **对比方法（Benchmark）**：
    *   **确定性方法**：ViNT（单次推理）。
    *   **生成式方法**：NoMaD（扩散模型）、DDPM、条件流匹配（CFM）、NaviBridger（标准薛定谔桥）。
*   **评估指标**：动作均方误差（MSE）、余弦相似度（CosSim）、终点误差（FDE）、碰撞率和导航成功率。

### 4. 资源与算力
*   **训练算力**：使用单张 **NVIDIA RTX 4090** GPU 进行训练。
*   **部署算力**：在 **NVIDIA Jetson Orin**（嵌入式平台）上进行四足机器人实机部署。
*   **训练细节**：训练 30 个 epoch，AdamW 优化器，学习率 $1 \times 10^{-4}$，Batch Size 为 256。
*   **推理效率**：在 Orin 上推理延迟约为 50ms/周期，满足 4Hz 的实时控制要求。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从离线数据集评估到闭环仿真，再到实机部署的全流程。
*   **消融实验**：非常充分。包括 $\varepsilon$ 参数的影响、预测目标（$v$ vs $x_0$ vs $\epsilon$）的对比、先验初始化与桥修正的贡献分解、以及不同 ODE 求解器（Euler vs Heun）的对比。
*   **客观性**：所有实验均重复 3 次随机种子取平均值，并提供了标准差，展示了结果的统计稳定性。

### 6. 主要结论与发现
*   **极少步数下的高性能**：RSBM 在仅需 **3 步积分（5 次函数调用）** 的情况下，成功率达到 92%，余弦相似度超过 94%。
*   **超越基准**：在 3 步推理下，RSBM 的 MSE 比 NaviBridger 低 6.3 倍；仅用 3 步即可达到 NaviBridger 10 步的精度，计算量减少 3.8 倍。
*   **无需蒸馏**：与一致性模型（Consistency Models）不同，RSBM 在单阶段训练后即可在任意步数下运行，无需额外的教师模型蒸馏。
*   **$\varepsilon$ 的作用**：发现 $\varepsilon=0.5$ 是平衡路径平滑度与多模态覆盖的最佳点。

### 7. 优点
*   **理论优雅**：通过数学证明揭示了薛定谔桥与流匹配的内在联系，并利用不变性简化了网络设计。
*   **部署友好**：显著降低了生成式策略的推理开销，使高保真模型能在算力受限的移动机器人上运行。
*   **单阶段训练**：相比于 Rectified Flow 等需要多次迭代重流（Reflow）的方法，RSBM 更加简洁高效。

### 8. 不足与局限
*   **实机场景有限**：虽然进行了实机部署，但测试场景仅限于少数室内环境，缺乏大规模、复杂动态障碍物的标准化实机基准测试。
*   **先验依赖性**：模型性能部分依赖于学习到的条件先验，如果先验网络在未见过的领域泛化性差，可能会限制整体系统的零样本迁移能力。
*   **动态避障**：虽然在 SACSoN 数据集上表现良好，但对于高速动态物体的实时规避能力尚需进一步验证。

（完）
