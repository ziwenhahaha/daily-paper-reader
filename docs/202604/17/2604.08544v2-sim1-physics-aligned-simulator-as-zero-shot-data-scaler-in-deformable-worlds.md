---
title: "SIM1: Physics-Aligned Simulator as Zero-Shot Data Scaler in Deformable Worlds"
title_zh: SIM1：可变形世界中作为零样本数据扩展器的物理对齐模拟器
authors: "Yunsong Zhou, Hangxu Liu, Xuekun Jiang, Xing Shen, Yuanzhen Zhou, Hui Wang, Baole Fang, Yang Tian, Mulin Yu, Qiaojun Yu, Li Ma, Hengjie Li, Hanqing Wang, Jia Zeng, Jiangmiao Pang"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08544v2"
tags: ["query:wam-vla"]
score: 9.0
evidence: 具身学习中针对变形物体的机器人操作
tldr: 15的合成与真实数据等效比，支持零样本迁移，显著提升了机器人在处理布料等变形物体时的泛化能力和操作成功率。
selection_source: fresh_fetch
motivation: 现有的机器人仿真系统多基于刚体抽象，难以准确模拟变形物体的复杂动力学和几何变化，导致严重的虚实差异。
method: 提出SIM1引擎，通过场景数字化、弹性动力学校准及基于扩散模型的轨迹生成与过滤，将少量演示转化为大规模高质量合成数据。
result: 1。
conclusion: 物理对齐的仿真系统可作为变形物体操作的有效监督来源，是实现高效、可扩展策略学习的实用路径。
---

## 摘要
可变形物体的机器人操作是具身学习中一个数据密集型的领域，其中形状、接触和拓扑结构的共同演化方式远超刚体的多变性。尽管模拟技术有望缓解现实世界数据获取的成本压力，但主流的“模拟到现实”（sim-to-real）流程仍根植于刚体抽象，导致几何形状不匹配、软体动力学脆弱，以及不适用于布料交互的运动原语。我们认为，模拟之所以失败，并非因为它是合成的，而是因为它是未接地的。为了解决这一问题，我们推出了 SIM1，这是一个物理对齐的“现实-模拟-现实”（real-to-sim-to-real）数据引擎，旨在将模拟接地于物理世界。在给定有限演示的情况下，该系统将场景数字化为度量一致的孪生体，通过弹性建模校准可变形动力学，并通过带有质量过滤的基于扩散的轨迹生成来扩展行为。该流程将稀疏的观测转化为具有接近演示保真度的大规模合成监督信号。实验表明，在 1:15 的等效比例下，纯合成数据训练的策略达到了与真实数据基准相当的水平，同时在现实世界部署中实现了 90% 的零样本成功率和 50% 的泛化增益。这些结果验证了物理对齐模拟作为可变形操作的可扩展监督手段的有效性，并为高效数据策略学习提供了一条切实可行的路径。

## Abstract
Robotic manipulation with deformable objects represents a data-intensive regime in embodied learning, where shape, contact, and topology co-evolve in ways that far exceed the variability of rigids. Although simulation promises relief from the cost of real-world data acquisition, prevailing sim-to-real pipelines remain rooted in rigid-body abstractions, producing mismatched geometry, fragile soft dynamics, and motion primitives poorly suited for cloth interaction. We posit that simulation fails not for being synthetic, but for being ungrounded. To address this, we introduce SIM1, a physics-aligned real-to-sim-to-real data engine that grounds simulation in the physical world. Given limited demonstrations, the system digitizes scenes into metric-consistent twins, calibrates deformable dynamics through elastic modeling, and expands behaviors via diffusion-based trajectory generation with quality filtering. This pipeline transforms sparse observations into scaled synthetic supervision with near-demonstration fidelity. Experiments show that policies trained on purely synthetic data achieve parity with real-data baselines at a 1:15 equivalence ratio, while delivering 90% zero-shot success and 50% generalization gains in real-world deployment. These results validate physics-aligned simulation as scalable supervision for deformable manipulation and a practical pathway for data-efficient policy learning.

---

## 论文详细总结（自动生成）

### SIM1: 物理对齐模拟器作为变形世界中的零样本数据扩展器

#### 1. 核心问题与整体含义
论文针对机器人操作**变形物体**（如衣物、布料）时面临的“数据饥渴”问题展开研究。变形物体具有无限的自由度和复杂的接触动力学，现实世界的数据采集成本极高。现有的“模拟到现实”（Sim-to-Real）方法主要基于刚体抽象，在处理软体时存在几何不匹配、动力学不稳定以及运动原语不适用等缺陷。论文的核心思想是：模拟失败并非因为它是合成的，而是因为它是“未接地”（ungrounded）的。为此，作者提出了 **SIM1** 引擎，通过物理对齐的“现实-模拟-现实”（R2S2R）范式，将模拟器锚定在物理世界中，从而实现高质量合成数据的规模化扩展。

#### 2. 方法论
SIM1 框架由三个关键阶段组成，旨在实现几何、动力学和运动的全面对齐：
*   **SIM1-Scene（场景数字化）**：利用高精度 3D 扫描（EinScan Rigil Pro）获取衣物的度量一致性网格和纹理，结合机器人 URDF 导入，构建与现实环境 1:1 对应的数字孪生场景。
*   **SIM1-Sim（变形稳定物理仿真）**：
    *   **核心技术**：开发了基于**增强顶点块下降（AVBD）**的求解器。
    *   **关键公式**：引入显式应变约束 $C(e) = \|e_i - e_j\| - (1 + \xi)l_0 \le 0$，当边缘拉伸超过阈值 $\xi$ 时，激活虚拟弹性约束力，加速收敛并抑制软体在交互中的过度拉伸或穿透。
    *   **校准基础设施**：通过双向同步流，将现实机器人的关节状态流式传输至模拟器，由专家根据视觉反馈迭代调整物理参数（杨氏模量、摩擦力等）。
*   **SIM1-DataGen（结构化操作合成）**：
    *   **轨迹分解**：将专家演示分解为“交互段”（抓取/释放）和“移动段”。
    *   **扩散模型生成**：利用条件扩散强迫（Diffusion Forcing）模型合成平滑的过渡运动。
    *   **过滤与随机化**：通过基于状态的统计规则和视频判别器（Transformer-based）过滤无效轨迹，并利用 Blender 进行光照、纹理和视角的视觉随机化渲染。

#### 3. 实验设计
*   **任务场景**：核心基准任务为**长时程 T 恤折叠**（涉及抓取、提起、拉伸、翻转等复杂动作），此外还扩展到了毛巾翻转、短裤折叠和长袖衣物折叠。
*   **Benchmark 与对比方法**：
    *   **Real Data**：直接使用现实世界采集的 1,000 条演示数据。
    *   **Sim Teleoperated Data**：在模拟器中通过遥操作采集的数据。
    *   **Sim Generated Data**：SIM1 自动生成的规模化合成数据。
*   **评估模型**：使用了 $\pi_0$（VLA 模型）和 $\pi_{0.5}$（开源 VLA 模型）作为策略骨干。
*   **评估维度**：包括域内（In-domain）测试和四种泛化性测试（空间位移、纹理变化、光照变化、视角偏移）。

#### 4. 资源与算力
*   **硬件环境**：
    *   **仿真与数据采集**：单台 RTX 4090 工作站。
    *   **大规模生成**：配备 **8× NVIDIA RTX 4090 GPU** 的服务器。
*   **效率数据**：
    *   模拟器运行速度约为 15 FPS（基于 NVIDIA Warp GPU 加速）。
    *   合成数据生成的单位成本约为 $0.10/条，相比现实采集（$2.71/条）降低了约 27 倍成本，吞吐量提升了 6.8 倍。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   真实机器人测试：每个配置进行 30 次试验，总计进行了数百次真实部署测试。
    *   数据规模：从 200 条种子演示扩展到 10,000 条合成轨迹。
*   **充分性与客观性**：
    *   **消融实验**：详细对比了求解器稳定性、轨迹分解策略、扩散生成模型对最终成功率的影响。
    *   **从头训练实验**：通过在无预训练权重的模型上对比真实数据与合成数据，证明了性能提升源于数据质量而非先验知识。
    *   **泛化性验证**：引入了训练集中未出现的衣物（如 Polo 衫）进行零样本迁移测试，实验设计较为严谨且具有说服力。

#### 6. 主要结论与发现
*   **等效比**：在 T 恤折叠任务中，**15 条 SIM1 合成数据约等于 1 条真实演示数据**的训练价值。
*   **零样本迁移**：纯合成数据训练的模型在真实机器人上实现了 **90% 的成功率**。
*   **泛化增益**：相比真实数据基准，SIM1 生成的数据在面对空间、纹理、光照等分布偏移时，提供了高达 **50% 的性能增益**。
*   **扩展性**：合成数据在低数据量时表现稍弱，但随着规模增加，其性能提升曲线比真实数据更陡峭且上限更高。

#### 7. 优点
*   **物理对齐的深度**：不仅是视觉上的相似，更通过 AVBD 求解器解决了软体仿真中常见的“过度拉伸”和“计算发散”问题。
*   **R2S2R 闭环**：通过场景数字化和参数校准，确保了模拟器生成的轨迹在物理上是可执行的。
*   **高效的数据引擎**：将扩散模型与物理过滤相结合，解决了变形物体操作中难以定义抓取原语的痛点。

#### 8. 不足与局限
*   **自动化程度受限**：物理参数（如布料的弹性、摩擦力）的校准仍需专家引导进行手动微调，尚未实现完全自动化的系统辨识。
*   **材料覆盖范围**：虽然展示了多种衣物，但对于极薄、极厚或具有特殊物理特性的材料（如丝绸或皮革），其仿真精度和泛化能力尚待验证。
*   **任务复杂度**：目前主要集中在折叠类任务，对于涉及裁剪、缝纫或更剧烈拓扑变化的变形操作尚未涉及。

（完）
