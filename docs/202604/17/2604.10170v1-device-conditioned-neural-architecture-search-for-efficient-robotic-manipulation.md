---
title: Device-Conditioned Neural Architecture Search for Efficient Robotic Manipulation
title_zh: 面向高效机器人操控的设备感知神经架构搜索
authors: "Yiming Wu, Huan Wang, Zhenghao Chen, Ge Yuan, Dong Xu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10170v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 高效机器人操纵与视觉运动策略
tldr: 针对机器人视觉运动策略在异构硬件上部署效率低、通用性差的问题，本文提出DC-QFA框架。该框架通过构建包含多种架构和混合精度位宽的超网络，结合硬件感知的正则化进行训练。利用一次性训练的超网络，可针对不同设备快速搜索最优子网络，无需重复优化。此外，引入多步在线蒸馏提升低精度下的稳定性。实验证明，该方法在多种模型和硬件上实现了2-3倍加速且性能几乎无损。
selection_source: fresh_fetch
motivation: 现有的机器人策略优化方法通常针对特定设备且耗时，难以适应多样化的硬件约束。
method: 提出DC-QFA框架，通过训练一个包含架构和量化位宽搜索空间的超网络，并结合多步在线蒸馏来减少低精度下的误差累积。
result: 在多种主流策略模型上实现了2-3倍的推理加速，且在边缘设备和云端平台上均保持了极高的任务成功率。
conclusion: DC-QFA提供了一种高效且通用的机器人策略部署方案，在保证操作稳定性的同时显著降低了跨硬件平台的适配成本。
---

## 摘要
视觉运动策略日益增长的复杂性给在异构机器人硬件约束下的部署带来了重大挑战。然而，大多数现有的机器人操控模型效率优化方法都是针对特定设备和模型的，缺乏泛化性，且在适配过程中需要耗时的逐设备优化。在这项工作中，我们提出了一个名为设备感知全量化（Device-Conditioned Quantization-For-All, DC-QFA）的统一框架，通过设备感知的量化感知训练和硬件约束的架构搜索来分摊部署成本。具体而言，我们引入了一个单一的超网络（supernet），其设计空间涵盖了丰富的网络架构和混合精度位宽。该超网络在逐设备查找表的指导下，利用延迟和内存感知的正则化进行优化。借助该超网络，对于每个目标平台，我们可以执行一次性的轻量级搜索来选择最优子网络，而无需任何逐设备重新优化，从而实现了在异构硬件上更具泛化性的部署，并显著缩短了部署时间。为了提高低精度下的长程稳定性，我们进一步引入了多步同策略蒸馏（multi-step on-policy distillation），以减轻闭环执行过程中的误差累积。在 DiffusionPolicy-T、MDT-V 和 OpenVLA-OFT 等三个代表性策略骨干网络上的广泛实验表明，我们的 DC-QFA 在边缘设备、消费级 GPU 和云平台上实现了 2-3 倍的加速，且任务成功率的性能下降可以忽略不计。在配备力/力矩传感器的 Inovo 机器人上进行的真实世界评估进一步验证了，即使在严重的量化情况下，我们的低比特 DC-QFA 策略仍能保持稳定且具有丰富接触的操控。

## Abstract
The growing complexity of visuomotor policies poses significant challenges for deployment with heterogeneous robotic hardware constraints. However, most existing model-efficient approaches for robotic manipulation are device- and model-specific, lack generalizability, and require time-consuming per-device optimization during the adaptation process. In this work, we propose a unified framework named \textbf{D}evice-\textbf{C}onditioned \textbf{Q}uantization-\textbf{F}or-\textbf{A}ll (DC-QFA) which amortizes deployment effort with the device-conditioned quantization-aware training and hardware-constrained architecture search. Specifically, we introduce a single supernet that spans a rich design space over network architectures and mixed-precision bit-widths. It is optimized with latency- and memory-aware regularization, guided by per-device lookup tables. With this supernet, for each target platform, we can perform a once-for-all lightweight search to select an optimal subnet without any per-device re-optimization, which enables more generalizable deployment across heterogeneous hardware, and substantially reduces deployment time. To improve long-horizon stability under low precision, we further introduce multi-step on-policy distillation to mitigate error accumulation during closed-loop execution. Extensive experiments on three representative policy backbones, such as DiffusionPolicy-T, MDT-V, and OpenVLA-OFT, demonstrate that our DC-QFA achieves $2\text{-}3\times$ acceleration on edge devices, consumer-grade GPUs, and cloud platforms, with negligible performance drop in task success. Real-world evaluations on an Inovo robot equipped with a force/torque sensor further validates that our low-bit DC-QFA policies maintain stable, contact-rich manipulation even under severe quantization.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DC-QFA**（Device-Conditioned Quantization-For-All）的框架，旨在解决大型视觉运动策略（Visuomotor Policies）在不同机器人硬件平台上部署效率低、适配成本高的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：基于 Transformer 的视觉运动策略（如 Diffusion Policy, VLA 模型）虽然性能强大，但计算和内存开销巨大，难以在资源受限的边缘设备（如嵌入式 GPU/NPU）上实时运行。
*   **研究动机**：现有的模型压缩方法（如剪枝、量化）通常是“设备特定”的，即针对每一个新硬件都需要重新进行耗时的训练或微调。此外，低比特量化在闭环控制中容易产生误差累积，导致长程（Long-horizon）任务失败。
*   **整体含义**：本文旨在通过“一次训练，到处部署”的超网络（Supernet）思路，实现跨异构硬件的高效策略部署，同时保证操控的稳定性。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：构建一个包含多种架构配置（深度、宽度）和多种量化位宽（INT4, INT8, FP8 等）的单一超网络。通过一次性训练，利用搜索算法为不同硬件快速匹配最优子网络。
*   **关键技术细节**：
    *   **设备感知超网络训练**：引入拉格朗日松弛法，将硬件约束（延迟、内存）作为正则项加入训练目标。利用预先测量的硬件查找表（LUT）指导优化。
    *   **多步同策略蒸馏（Multi-step On-policy Distillation, OPD）**：为了解决量化导致的误差累积，让量化后的学生模型进行多步 rollout，并由全精度教师模型在每个状态下提供监督，逐步扩大训练时界（Horizon）。
    *   **硬件感知子网搜索**：在部署阶段，使用 **NSGA-II** 多目标进化算法，在不重新训练的情况下，根据目标设备的资源预算搜索帕累托最优（Pareto-optimal）的子网配置。

### 3. 实验设计
*   **数据集与场景**：
    *   **Push-T**：平面推箱子任务，评估基础操控成功率。
    *   **CALVIN**：长程语言条件操控任务，评估连续执行多个指令的能力。
    *   **LIBERO**：包含 130 个任务的多任务基准，评估模型的泛化性。
    *   **真实世界实验**：使用 Inovo 6自由度机械臂，执行扫豆子、抓鸡蛋、插辣椒等接触密集型任务。
*   **对比方法**：
    *   **PTQ（后量化）**：SmoothQuant, AWQ。
    *   **QAT（量化感知训练）**：SQIL。
    *   **QFA（全量化搜索）**：BatchQuant。
*   **模型骨干**：DiffusionPolicy-T, MDT-V, OpenVLA-OFT。

### 4. 资源与算力
*   **训练时长**：
    *   训练 **DiffusionPolicy-T** 的超网络约需 **85 GPU 小时**。
    *   训练 **MDT-V** 的超网络约需 **156 GPU 小时**。
*   **硬件平台**：实验涵盖了边缘设备（Jetson Orin NX）、消费级 GPU 和云端平台。
*   **算力说明**：论文未明确提及具体的 GPU 型号（如 A100 或 H100），但给出了总体的训练小时数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 38MB 的小模型到 15GB 的大模型（OpenVLA），跨越了仿真和实物。
*   **消融实验**：对多步同策略蒸馏（OPD）进行了专门的对比实验，验证了其在长程任务（CALVIN）中的必要性。
*   **客观性与公平性**：在相同的位宽（W8A8, W4A4）下与多种 SOTA 方法进行了对比，并提供了帕累托前沿曲线（Pareto Frontier）来展示精度与效率的权衡，实验设计较为严谨。

### 6. 主要结论与发现
*   **加速效果显著**：在 Jetson Orin NX 等边缘设备上实现了 **2-3倍的推理加速**，内存占用减少了 **2-4倍**。
*   **性能保持良好**：在 W4A4（4比特）的极端量化下，DC-QFA 的任务成功率仍能接近全精度（FP16）模型，显著优于传统的 PTQ 方法。
*   **长程稳定性**：引入 OPD 后，量化模型在长序列任务中的误差累积得到有效抑制，平均完成指令数大幅提升。
*   **实物鲁棒性**：在真实世界中，即使在 4-bit 精度下，机器人仍能处理抓取鸡蛋等对力控和精度要求较高的任务。

### 7. 优点（亮点）
*   **通用性强**：不局限于单一模型或单一硬件，提供了一套通用的机器人策略压缩与部署流程。
*   **部署效率高**：通过超网络搜索，将原本需要数天的设备适配时间缩短至分钟级的轻量级搜索。
*   **解决痛点**：针对机器人领域特有的“误差累积”问题提出了 OPD 方案，比通用的量化方法更贴合具身智能需求。

### 8. 不足与局限
*   **超网络训练开销**：虽然部署快，但超网络的初始训练成本（85-156 GPU 小时）较高，且随着搜索空间扩大，训练难度会增加。
*   **硬件查找表依赖**：该方法依赖于预先构建的硬件 LUT，如果目标硬件的底层算子优化发生变化（如编译器更新），可能需要重新测量 LUT。
*   **应用限制**：目前主要针对 Transformer 架构，对于其他非 Transformer 结构的策略（如简单的 MLP 或 CNN）未做深入探讨。

（完）
