---
title: "World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems"
title_zh: 世界-价值-动作模型：视觉-语言-动作系统的隐式规划
authors: "Runze Li, Hongyin Zhang, Junxi Jin, Qixin Zeng, Zifeng Zhuang, Yiqi Tang, Shangke Lyu, Donglin Wang"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.14732v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 视觉-语言-动作 (VLA) 系统的隐式规划
tldr: 针对现有视觉-语言-动作（VLA）模型在长程任务中缺乏推理和评估能力的问题，本文提出了WAV模型。该模型通过学习结构化潜空间表示，结合世界模型预测未来状态和价值函数评估长远收益，将动作生成转化为潜空间内的隐式规划。理论证明该方法能有效解决动作空间搜索效率低的问题，在模拟和现实实验中显著提升了长程复杂任务的成功率和泛化性。
selection_source: fresh_fetch
motivation: 现有的VLA模型多采用直接动作预测，难以在复杂决策任务中进行长程轨迹推理和后果评估。
method: 提出WAV模型，利用世界模型预测未来状态并结合价值函数，在结构化潜空间中通过隐式规划生成高价值且动态可行的动作。
result: 实验表明WAV在长程和组合任务中表现优异，显著提升了任务成功率、泛化能力和鲁棒性。
conclusion: 潜空间推理能有效重塑搜索分布，是提升具身智能体长程决策能力的有效途径。
---

## 摘要
视觉-语言-动作（VLA）模型已成为构建具身智能体的一种极具前景的范式，能够将感知和语言落地为动作。然而，现有的大多数方法依赖于直接的动作预测，缺乏对长时程轨迹进行推理并评估其后果的能力，这限制了它们在复杂决策任务中的表现。在这项工作中，我们提出了世界-价值-动作（WAV）模型，这是一个在 VLA 系统中实现隐式规划的统一框架。WAV 模型并非进行显式的轨迹优化，而是学习一种以视觉观测和语言指令为条件的未来轨迹结构化潜表示。一个学习到的世界模型负责预测未来状态，而轨迹价值函数则评估其长时程效用。动作生成随后被表述为在该潜空间中的推理过程，模型逐渐将概率质量集中在高价值且动力学可行的轨迹上。我们提供了一个理论视角，表明直接在动作空间中进行规划会面临可行轨迹概率随时间跨度增加而呈指数级衰减的问题。相比之下，潜空间推理将搜索分布重塑向可行区域，从而实现了高效的长时程决策。大量的仿真和真实世界实验表明，WAV 模型始终优于现有最先进的方法，在任务成功率、泛化能力和鲁棒性方面取得了显著提升，特别是在长时程和组合场景中。

## Abstract
Vision-Language-Action (VLA) models have emerged as a promising paradigm for building embodied agents that ground perception and language into action. However, most existing approaches rely on direct action prediction, lacking the ability to reason over long-horizon trajectories and evaluate their consequences, which limits performance in complex decision-making tasks. In this work, we introduce World-Value-Action (WAV) model, a unified framework that enables implicit planning in VLA systems. Rather than performing explicit trajectory optimization, WAV model learn a structured latent representation of future trajectories conditioned on visual observations and language instructions. A learned world model predicts future states, while a trajectory value function evaluates their long-horizon utility. Action generation is then formulated as inference in this latent space, where the model progressively concentrates probability mass on high-value and dynamically feasible trajectories. We provide a theoretical perspective showing that planning directly in action space suffers from an exponential decay in the probability of feasible trajectories as the horizon increases. In contrast, latent-space inference reshapes the search distribution toward feasible regions, enabling efficient long-horizon decision making. Extensive simulations and real-world experiments demonstrate that the WAV model consistently outperforms state-of-the-art methods, achieving significant improvements in task success rate, generalization ability, and robustness, especially in long-horizon and compositional scenarios.

---

## 论文详细总结（自动生成）

这是一份关于论文《World-Value-Action Model: Implicit Planning for Vision-Language-Action Systems》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型（如 RT-2, OpenVLA）大多采用“直接动作预测”范式，将决策视为独立的步骤，缺乏对长程（Long-horizon）轨迹的推理能力和对未来后果的评估机制。这导致模型在处理复杂、多步骤任务时容易产生累积误差。
论文提出了 **WAV（World-Value-Action）模型**，旨在将“世界模型预测”与“价值评估”集成到 VLA 系统中，通过**潜空间（Latent Space）的隐式规划**代替显式的动作空间搜索，从而提升智能体在复杂环境中的决策鲁棒性和长程任务成功率。

### 2. 方法论：核心思想与技术细节
WAV 模型由三个紧密耦合的模块组成，采用**流匹配（Flow Matching）**技术进行训练：
*   **核心思想**：将规划视为在结构化潜空间中的推理过程。模型不直接在动作空间搜索，而是通过迭代优化未来轨迹的潜表示，使其向“动力学可行”且“高价值”的区域集中。
*   **关键模块**：
    1.  **语言条件视频生成模块（世界模型）**：基于 Diffusion Transformer (DiT)，根据指令和当前观测预测未来视觉轨迹。
    2.  **轨迹价值模块**：评估预测视频轨迹的长程效用（累积奖励）。
    3.  **动作解码模块**：融合视频特征和价值特征，解码出具体的机器人控制动作。
*   **迭代推理算法**：受模型预测路径积分（MPPI）启发，WAV 在推理时通过多次迭代更新视频和价值模块的噪声分布。它采样多个候选未来，计算其信噪比（SNR）作为评分，筛选出“精英”样本来修正潜空间的分布，最终生成最优动作。
*   **理论支撑**：论文证明了在动作空间直接规划会随时间步长增加面临“可行轨迹概率指数级衰减”的问题，而潜空间推理能有效重塑搜索分布。

### 3. 实验设计
*   **仿真实验**：在 **LIBERO** 基准测试集上进行，涵盖四个子集：Spatial（空间推理）、Object（物体泛化）、Goal（目标条件）和 Long（长程组合任务）。
*   **真实世界实验**：使用 **Piper 双臂机器人**，执行包括整理碗筷（Bowl organization）、铺平毛巾（Towel flattening）和复杂的抽屉任务（打开抽屉-放入物体-关闭抽屉）。
*   **对比方法**：对比了包括 Diffusion Policy, Octo, OpenVLA, $\pi_0$, 以及带有世界模型的 WorldVLA, DreamVLA, GE-ACT 等多种 SOTA 模型。

### 4. 资源与算力
论文明确说明了算力消耗：
*   **硬件**：使用了一台配备 **8 张 NVIDIA A100-SXM4-80GB GPU** 的服务器。
*   **训练时长**：
    *   LIBERO 任务：视频模块训练 4万步，价值/动作模块训练 3万步，总计约 **5 天**。
    *   真实世界任务：每个任务单独训练，约需 **3 天**。
*   **推理开销**：论文在消融实验中讨论了推理时间与迭代次数 $K$ 的关系，指出 $K=3$ 是性能与效率的平衡点。

### 5. 实验数量与充分性
实验设计非常充分且具有说服力：
*   **多维度评估**：不仅有标准的成功率对比，还包含了针对迭代次数 $K$、采样数 $M/N$、平滑参数 $\alpha/\beta$ 的详细消融实验。
*   **泛化性测试**：通过 LIBERO 的四个不同维度测试了模型的空间、物体和长程泛化能力。
*   **真实感验证**：在真实机器人上进行了 15 次重复实验并取平均值，确保了结果的客观性。
*   **理论与实践结合**：通过可视化预测视频和价值曲线，验证了模型内部推理的逻辑正确性。

### 6. 主要结论与发现
*   **性能领先**：WAV 在 LIBERO 基准上取得了 **98.1%** 的平均成功率，刷新了 SOTA 纪录。
*   **长程任务优势明显**：在最难的 LIBERO-Long 任务中，隐式规划使性能提升显著（从 91.8% 提升至 94.4%）。
*   **真实世界表现强劲**：在 Piper 机器人实验中，WAV 的平均成功率（75.6%）远超基准模型 GE-ACT（35.6%），尤其在多步骤协调任务中表现出极强的鲁棒性。
*   **规划的必要性**：实验证明，单纯的潜空间采样是不够的，**迭代推理**对于集中概率质量到高价值区域至关重要。

### 7. 优点（亮点）
*   **理论深度**：为“为什么要在潜空间规划”提供了严谨的数学证明（引理 4.1 和命题 4.2）。
*   **架构统一**：将预测、评估和执行整合在一个基于流匹配的 DiT 框架内，模块化程度高且易于扩展。
*   **隐式规划**：避免了显式轨迹优化的计算负担，同时保留了前瞻性推理的能力。

### 8. 不足与局限
*   **计算延迟**：由于涉及视频生成和多次迭代推理，实时性（Inference Time）相比纯反应式模型仍有差距（如图 6 所示，随 $K$ 增加耗时显著增长）。
*   **存储开销**：模型参数量较大（约 2.2B），且推理时需维护多个采样分支，对显存要求较高。
*   **闭环频率限制**：目前主要在仿真和特定真实任务中验证，尚未完全实现在极高频率环境下的实时闭环部署。

（完）
