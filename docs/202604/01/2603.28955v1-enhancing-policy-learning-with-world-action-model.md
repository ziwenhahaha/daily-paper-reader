---
title: Enhancing Policy Learning with World-Action Model
title_zh: 利用世界-动作模型增强策略学习
authors: "Yuci Han, Alper Yilmaz"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28955v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 利用世界动作模型（WAM）增强机器人操纵任务的策略学习
tldr: 本研究提出世界-动作模型（WAM），通过在DreamerV2中引入逆动力学目标，使模型能同时推理未来视觉观测和驱动状态转移的动作。该方法增强了潜在表征对动作相关结构的捕捉能力。在CALVIN操纵任务中，WAM显著提升了行为克隆和PPO微调的成功率，并大幅减少了训练步数，证明了动作正则化在提升策略学习效率方面的关键作用。
selection_source: fresh_fetch
motivation: 传统世界模型仅依赖图像预测，难以有效捕捉对下游控制至关重要的动作相关特征。
method: 在DreamerV2框架中加入逆动力学目标，通过从潜在状态转移中预测动作来正则化世界模型。
result: "在CALVIN任务中，WAM将策略成功率提升至92.8%，且相比基准模型实现了8.7倍的训练加速。"
conclusion: 动作正则化的世界模型能显著增强表征质量，从而大幅提升复杂操纵任务中的策略学习性能和效率。
---

## 摘要
本文提出了世界-动作模型（World-Action Model, WAM），这是一种动作正则化的世界模型，能够对未来的视觉观测以及驱动状态转移的动作进行联合推理。与仅通过图像预测进行训练的传统世界模型不同，WAM 在 DreamerV2 中引入了逆动力学目标，通过潜在状态转移来预测动作，从而鼓励学习到的表示捕捉对下游控制至关重要的动作相关结构。我们在 CALVIN 基准测试的八个操纵任务上评估了 WAM 在增强策略学习方面的表现。我们首先在世界模型潜在空间上通过行为克隆预训练了一个扩散策略，然后在冻结的世界模型内部使用基于模型的 PPO 对其进行微调。在不修改策略架构或训练过程的情况下，WAM 将平均行为克隆成功率从 DreamerV2 和 DiWA 基准的 59.4% 提升至 71.2%。经过 PPO 微调后，WAM 实现了 92.8% 的平均成功率，而基准模型为 79.8%，其中两个任务达到了 100% 的成功率，且训练步数减少了 8.7 倍。

## Abstract
This paper presents the World-Action Model (WAM), an action-regularized world model that jointly reasons over future visual observations and the actions that drive state transitions. Unlike conventional world models trained solely via image prediction, WAM incorporates an inverse dynamics objective into DreamerV2 that predicts actions from latent state transitions, encouraging the learned representations to capture action-relevant structure critical for downstream control. We evaluate WAM on enhancing policy learning across eight manipulation tasks from the CALVIN benchmark. We first pretrain a diffusion policy via behavioral cloning on world model latents, then refine it with model-based PPO inside the frozen world model. Without modifying the policy architecture or training procedure, WAM improves average behavioral cloning success from 59.4% to 71.2% over DreamerV2 and DiWA baselines. After PPO fine-tuning, WAM achieves 92.8% average success versus 79.8% for the baseline, with two tasks reaching 100%, using 8.7x fewer training steps.

---

## 论文详细总结（自动生成）

以下是对论文《Enhancing Policy Learning with World-Action Model》（利用世界-动作模型增强策略学习）的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的世界模型（如 Dreamer 系列）主要通过预测未来观测（像素）来学习环境动力学。然而，这种“仅观测”的训练目标存在不对称性：模型虽然以动作为条件，但从未显式地建模动作本身。这导致学习到的潜在表征（Latent Representations）可能无法有效捕捉对下游控制至关重要的“动作相关结构”。
*   **核心目标**：提出**世界-动作模型（World-Action Model, WAM）**，通过引入逆动力学目标，使模型在学习预测未来的同时，也能理解是哪些动作驱动了状态转移，从而提升表征质量和策略学习效率。

### 2. 方法论
*   **核心思想**：在 DreamerV2 的 RSSM（循环状态空间模型）架构基础上，增加一个轻量级的**逆动力学头（Inverse Dynamics Head）**。
*   **关键技术细节**：
    *   **逆动力学预测**：使用一个三层 MLP，输入连续两个时刻的编码器嵌入 $e_t$ 和 $e_{t+1}$，预测执行的动作 $\hat{a}_t$。
    *   **级联效应（Cascading Effect）**：这是 WAM 的设计精髓。通过正则化编码器 $e_t$，动作感知信息会流向后验分布 $z_t$，再通过 KL 散度损失传播到先验分布 $\hat{z}_t$。最终，下游的扩散策略（Diffusion Policy）在基于先验分布的“想象”轨迹中学习时，能获得更具控制相关性的特征。
    *   **训练目标**：联合优化 KL 散度损失、图像重建损失和动作预测损失（$L_{action}$ 使用 L1 回归）。
    *   **策略学习流程**：
        1.  **行为克隆（BC）**：在冻结的 WAM 潜在特征上预训练 DiffusionMLP 策略。
        2.  **模型内微调**：在冻结的 WAM（作为模拟器）中使用 DPPO（扩散策略优化）进行离线强化学习微调。

### 3. 实验设计
*   **数据集与场景**：使用 **CALVIN 基准测试**，这是一个桌面机器人操纵环境，包含 7 自由度 Franka 手臂和 6 小时的演示数据。
*   **任务设置**：涵盖 8 个操纵任务（如开关抽屉、移动滑块、开关灯泡/LED）。
*   **对比基准（Benchmarks）**：
    *   **世界模型质量**：对比 DreamerV2（评估 PSNR, SSIM, LPIPS, FVD 等指标）。
    *   **策略学习性能**：对比 **DiWA**（一种基于扩散策略的世界模型适配方法）。
*   **评估方式**：在 29 个未见过的初始配置上测试成功率。

### 4. 资源与算力
*   **训练效率**：WAM 仅需 **230K 个梯度步**即可达到优异性能，而基准模型 DreamerV2 通常需要 2M 步，训练步数减少了约 **8.7 倍**。
*   **算力细节**：文中未明确给出具体的 GPU 型号和数量，但强调了其“轻量级扩展”的特性，且在更短的时间内实现了更高的性能。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同的操纵任务上进行了详尽测试。
*   **充分性**：
    *   提供了视频预测质量的定量对比（表 I）。
    *   展示了行为克隆阶段的成功率对比（表 III）。
    *   展示了 PPO 微调后的最终性能及收敛曲线（表 IV 和图 4）。
*   **客观性**：实验保持了策略架构（DiffusionMLP）和训练流程与基准模型完全一致，仅改变了世界模型提供的特征，确保了对比的公平性。

### 6. 主要结论与发现
*   **表征质量提升**：动作正则化显著改善了世界模型的生成质量，生成的未来图像在形状保持和色彩准确度上优于 DreamerV2。
*   **策略性能飞跃**：
    *   **行为克隆**：平均成功率从 45.8% 提升至 61.7%。
    *   **PPO 微调**：最终平均成功率达到 **92.8%**（基准为 79.8%），其中两个任务达到 100%。
*   **任务敏感性**：在涉及复杂关节物体（如抽屉、滑块）的任务中提升最为显著，证明了动作感知特征对精细空间控制的帮助。

### 7. 优点
*   **简单高效**：无需重新设计复杂的大模型架构，仅通过增加一个简单的逆动力学目标就实现了显著的性能提升。
*   **高采样效率**：在更少的训练步数下实现了更强的泛化能力和更高的成功率。
*   **即插即用**：该方法可以作为现有基于 RSSM 的世界模型的通用增强模块。

### 8. 不足与局限
*   **场景局限性**：实验仅限于 CALVIN 桌面操纵环境，尚未在更复杂的开放世界或真实物理硬件上进行大规模验证。
*   **超参数敏感性**：动作损失权重 $\lambda_{act}$ 设为 1000.0，这表明不同任务可能需要仔细调优损失平衡。
*   **依赖奖励模型**：策略微调仍依赖于在潜在空间训练的奖励分类器，如果分类器本身存在偏差，可能会影响最终策略的上限。

（完）
