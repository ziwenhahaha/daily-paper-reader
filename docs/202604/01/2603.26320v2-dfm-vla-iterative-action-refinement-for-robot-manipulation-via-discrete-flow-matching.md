---
title: "DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching"
authors: "Jiayi Chen, Wenxuan Song, Shuai Chen, Jingbo Wang, Zhijun Li, Haoang Li"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26320v2"
tags: ["query:wam-vla"]
score: 10.0
evidence: 提出了一种用于机器人操作中迭代动作细化的离散流匹配VLA模型。
tldr: 针对现有视觉-语言-动作（VLA）模型在离散动作生成中无法修正早期错误的问题，本文提出DFM-VLA。该模型利用离散流匹配技术，通过建模动作序列的概率速度场实现迭代优化。结合两阶段解码策略，DFM-VLA在CALVIN和LIBERO等基准测试中显著超越了自回归和扩散模型基准，在保持高推理效率的同时提升了机器人操作的准确性与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的VLA模型在生成离散动作令牌后无法进行后续修正，导致早期生成的错误会持续累积并影响最终操作效果。
method: 提出一种基于离散流匹配的VLA框架，通过概率速度场动态更新动作序列，并采用迭代细化与确定性验证的两阶段解码策略。
result: "在CALVIN和LIBERO基准测试中，DFM-VLA分别实现了4.44的平均成功长度和95.7%的平均成功率，性能显著优于自回归和扩散模型。"
conclusion: 离散流匹配为机器人操作中的动作细化提供了一种高效且高性能的解决方案，有效解决了离散动作生成的不可逆性问题。
---

## Abstract
Vision--Language--Action (VLA) models that encode actions using a discrete tokenization scheme are increasingly adopted for robotic manipulation, but existing decoding paradigms remain fundamentally limited. Whether actions are decoded sequentially by autoregressive VLAs or in parallel by discrete diffusion VLAs, once a token is generated, it is typically fixed and cannot be revised in subsequent iterations, so early token errors cannot be effectively corrected later. We propose DFM-VLA, a discrete flow matching VLA for iterative refinement of action tokens. DFM-VLA~models a token-level probability velocity field that dynamically updates the full action sequence across refinement iterations. We investigate two ways to construct the velocity field: an auxiliary velocity-head formulation and an action-embedding-guided formulation. Our framework further adopts a two-stage decoding strategy with an iterative refinement stage followed by deterministic validation for stable convergence. Extensive experiments on CALVIN, LIBERO, and real-world manipulation tasks show that DFM-VLA consistently outperforms strong autoregressive, discrete diffusion, and continuous diffusion baselines in manipulation performance while retaining high inference efficiency. In particular, DFM-VLA achieves an average success length of 4.44 on CALVIN and an average success rate of 95.7\% on LIBERO, highlighting the value of action refinement via discrete flow matching for robotic manipulation. Our project is available https://chris1220313648.github.io/DFM-VLA/

---

## 论文详细总结（自动生成）

这是一份关于论文《DFM-VLA: Iterative Action Refinement for Robot Manipulation via Discrete Flow Matching》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的核心问题是现有视觉-语言-动作（VLA）模型在离散动作生成中的**“不可逆承诺”（Irreversible Commitment）**缺陷。
*   **背景**：目前的离散 VLA 主要分为自回归（AR）和离散扩散（DD）两类。AR 模型按顺序生成 Token，一旦生成便无法更改；DD 模型虽然并行生成，但在推理后期通常只更新被掩码的部分，早期生成的错误 Token 难以被有效修正。
*   **动机**：在机器人操作中，早期的动作预测错误会随时间累积，导致任务失败。作者希望引入**离散流匹配（Discrete Flow Matching, DFM）**技术，通过建模“概率速度场”来实现对全序列动作 Token 的迭代细化和纠错。

### 2. 方法论：核心思想与关键技术
DFM-VLA 的核心思想是将动作生成视为在离散空间中的连续流演化过程。
*   **概率速度场（Velocity Field）**：模型学习一个条件变化率函数，描述当前 Token 状态 $x_t$ 如何随时间 $t$ 向目标状态 $x_1$ 演化。
*   **两种速度场构建方案**：
    1.  **辅助速度头（Auxiliary Velocity Head）**：通过额外的预测头从模型隐藏状态直接预测 Token 替换的速度。
    2.  **动作嵌入引导（Action-Embedding-Guided）**：利用动作 Token 在嵌入空间中的语义距离定义概率路径，推导出动力学最优速度。实验证明此方案收敛更快、性能更强。
*   **两阶段解码策略**：
    1.  **迭代细化阶段**：利用 Euler 离散化对连续时间马尔可夫链进行采样，允许 Token 在全序列范围内多次跳转和修正。
    2.  **确定性验证阶段**：在最后几步切换为贪婪解码（Greedy Decoding），以确保轨迹的稳定收敛。
*   **自适应 KV 缓存**：针对迭代解码的冗余计算，通过监控动作 Token 特征的变化，动态更新 KV 缓存，实现了 2.4 倍的推理加速。

### 3. 实验设计
作者在模拟环境和真实世界中进行了广泛评估：
*   **数据集/场景**：
    *   **CALVIN**：评估长程（Long-horizon）操作能力，采用 ABCD→D 设置，考察连续完成 5 个子任务的平均长度。
    *   **LIBERO**：评估跨任务泛化能力，包含 Spatial、Object、Goal、Long 四个子库。
    *   **真实世界**：在 AgileX 双臂平台上执行协作抬锅、放蔬菜、放方块等任务。
*   **对比方法**：
    *   **自回归基准**：RT-1, OpenVLA, UniVLA, π0-FAST 等。
    *   **扩散模型基准**：Dream-VLA, Discrete Diffusion VLA, RDT（连续扩散）等。
    *   **推理增强方法**：ThinkAct, CoT-VLA 等。

### 4. 资源与算力
*   **硬件**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练细节**：
    *   仿真实验训练步数为 20k 至 32k 步。
    *   真实世界任务每个任务训练约 5k 步。
    *   学习率设为 1e-4，Batch Size 为 8（梯度累积后有效 Batch Size 更大）。
*   **初始化**：模型初始化自预训练的机器人视频模型权重。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了数千次 Rollout 测试（CALVIN 1000 次，LIBERO 每个任务 50 次，真实世界每个任务 40 次）。
*   **消融实验**：非常充分，涵盖了速度场构建方式对比、超参数（c 和 α）敏感度分析、两阶段解码步数分配、数据规模（10%、50%、100%）对性能的影响。
*   **客观性**：通过与多种不同范式（AR、DD、CD）的 SOTA 模型在统一基准下对比，实验结果具有较高的说服力和公平性。

### 6. 主要结论与发现
*   **性能领先**：DFM-VLA 在 CALVIN 上达到 4.44 的平均成功长度，在 LIBERO 上达到 95.7% 的成功率，均刷新了纪录。
*   **纠错能力**：实验证实 DFM 机制能有效修正早期生成的错误 Token，显著提升了长程任务的成功率。
*   **数据效率**：在仅有 10% 训练数据的情况下，DFM-VLA 的表现远超 AR 和 DD 模型，显示出极强的学习效率。
*   **推理效率**：结合自适应缓存技术，DFM-VLA 在保持高性能的同时，推理速度优于传统的自回归模型。

### 7. 优点（亮点）
*   **范式创新**：首次将离散流匹配引入 VLA 领域，解决了离散动作生成中长期存在的“不可逆”痛点。
*   **设计优雅**：提出的嵌入引导速度场不需要复杂的启发式规则，能自动利用动作空间的语义结构。
*   **实用性强**：两阶段解码平衡了探索（纠错）与利用（稳定），且自适应缓存解决了扩散类模型推理慢的通病。

### 8. 不足与局限
*   **计算开销**：尽管有加速技术，但 16 步迭代的计算量仍高于单步生成的优化版 AR 模型，在极低延迟要求的场景下可能受限。
*   **任务复杂度**：真实世界实验虽然涵盖了双臂协作，但场景相对受控，对于更复杂、非结构化环境的鲁棒性仍需进一步验证。
*   **Token 依赖**：模型性能高度依赖于动作 Token 化（Tokenizer）的质量，若 Token 空间本身缺乏语义结构，嵌入引导的效果可能会打折扣。

（完）
