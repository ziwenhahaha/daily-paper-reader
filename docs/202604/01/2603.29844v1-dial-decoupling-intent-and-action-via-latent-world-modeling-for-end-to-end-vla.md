---
title: "DIAL: Decoupling Intent and Action via Latent World Modeling for End-to-End VLA"
title_zh: DIAL：通过潜空间世界建模实现端到端 VLA 的意图与动作解耦
authors: "Yi Chen, Yuying Ge, Hui Zhou, Mingyu Ding, Yixiao Ge, Xihui Liu"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29844v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 具有潜世界建模的端到端 VLA
tldr: DIAL 提出了一种解耦意图与动作的端到端视觉-语言-动作（VLA）模型框架。它通过 VLM 构建的 System-2 进行潜空间世界建模，生成代表意图的视觉预见，再由轻量化的 System-1 解码为具体动作。该方法通过两阶段训练保持了预训练知识的稳定性，在减少 10 倍训练数据的情况下达到了 SOTA 性能，并展现出强大的零抽样泛化能力。
selection_source: fresh_fetch
motivation: 现有 VLA 模型将 VLM 仅视为多模态编码器，忽略了其高层决策潜力，且直接映射到低层动作会导致训练不稳定和语义表征退化。
method: 引入潜意图瓶颈，利用 System-2 在 VLM 特征空间合成视觉预见，并由 System-1 通过潜逆动力学将其解码为机器人动作。
result: 在 RoboCasa GR1 基准测试中刷新了 SOTA 记录，仅需十分之一的演示数据，并在人形机器人实机部署中实现了对未知物体的零抽样泛化。
conclusion: DIAL 通过解耦意图与执行，有效结合了 VLM 的高层推理能力与高效的底层控制，为构建稳健且数据高效的通用机器人策略提供了新路径。
---

## 摘要
预训练视觉-语言模型（VLM）显著加速了视觉-语言-动作（VLA）模型的发展。然而，大多数现有的端到端 VLA 主要将 VLM 视为多模态编码器，直接将视觉-语言特征映射到低级动作。这种范式未能充分利用 VLM 在高级决策方面的潜力，并引入了训练不稳定性，经常导致其丰富的语义表示退化。为了解决这些局限性，我们提出了 DIAL，这是一个通过可微潜意图瓶颈连接高级决策与低级电机执行的框架。具体而言，基于 VLM 的 System-2 通过在 VLM 的原生特征空间内合成潜视觉预见来进行潜世界建模；这种预见显式地编码了意图，并作为结构化瓶颈。随后，轻量级的 System-1 策略通过潜逆动力学将该预测意图与当前观测共同解码为精确的机器人动作。为确保优化稳定性，我们采用了一种两阶段训练范式：首先是解耦预热阶段，System-2 学习预测潜未来，而 System-1 在统一特征空间内根据真实未来的引导学习电机控制；随后进行无缝的端到端联合优化。这使得动作感知梯度能够以受控的方式微调 VLM 主干网络，从而保留预训练知识。在 RoboCasa GR1 Tabletop 基准测试上的广泛实验表明，DIAL 达到了新的 SOTA 水平，在演示数据量比以往方法少 10 倍的情况下实现了更优越的性能。此外，通过利用异构人类演示，DIAL 学习了符合物理规律的操作先验，并在人形机器人的真实部署中，对未见物体和新颖配置表现出鲁棒的零样本泛化能力。

## Abstract
The development of Vision-Language-Action (VLA) models has been significantly accelerated by pre-trained Vision-Language Models (VLMs). However, most existing end-to-end VLAs treat the VLM primarily as a multimodal encoder, directly mapping vision-language features to low-level actions. This paradigm underutilizes the VLM's potential in high-level decision making and introduces training instability, frequently degrading its rich semantic representations. To address these limitations, we introduce DIAL, a framework bridging high-level decision making and low-level motor execution through a differentiable latent intent bottleneck. Specifically, a VLM-based System-2 performs latent world modeling by synthesizing latent visual foresight within the VLM's native feature space; this foresight explicitly encodes intent and serves as the structural bottleneck. A lightweight System-1 policy then decodes this predicted intent together with the current observation into precise robot actions via latent inverse dynamics. To ensure optimization stability, we employ a two-stage training paradigm: a decoupled warmup phase where System-2 learns to predict latent futures while System-1 learns motor control under ground-truth future guidance within a unified feature space, followed by seamless end-to-end joint optimization. This enables action-aware gradients to refine the VLM backbone in a controlled manner, preserving pre-trained knowledge. Extensive experiments on the RoboCasa GR1 Tabletop benchmark show that DIAL establishes a new state-of-the-art, achieving superior performance with 10x fewer demonstrations than prior methods. Furthermore, by leveraging heterogeneous human demonstrations, DIAL learns physically grounded manipulation priors and exhibits robust zero-shot generalization to unseen objects and novel configurations during real-world deployment on a humanoid robot.

---

## 论文详细总结（自动生成）

### DIAL: 通过潜空间世界建模实现端到端 VLA 的意图与动作解耦 —— 论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在将高层语义意图转化为底层精确电机控制时面临挑战。
*   **研究背景**：
    *   **分层规划器**（如 LLM 生成代码/文本）存在不可微的“断层”，导致高延迟且底层反馈无法优化高层模型。
    *   **端到端 VLA** 通常将 VLM 仅视为特征编码器，直接映射到动作。这种方式容易导致 VLM 的语义表征在动作监督下发生“坍缩”（Overfitting），且缺乏对物理世界的显式预见。
*   **研究目标**：设计一种既能保留 VLM 认知泛化能力，又能实现高精度动作执行的端到端可微框架。

#### 2. 方法论：DIAL 框架
DIAL 借鉴生物学的双系统理论，通过**潜空间意图瓶颈**（Latent Intent Bottleneck）连接推理与执行：
*   **System-2（大脑/VLM）**：基于预训练 VLM（如 Qwen2.5-VL），执行**潜世界建模（LWM）**。它不直接输出动作，而是在 VLM 原生的 ViT 特征空间内合成“视觉预见”（Visual Foresight），即预测 $H$ 个时间步后的环境潜状态。
*   **System-1（小脑/策略）**：一个轻量级的流匹配（Flow-matching）扩散变压器（DiT）。它充当**潜逆动力学模型**，输入当前的视觉特征和 System-2 预测的潜意图，通过对比两者的差异来推导精确的机器人动作。
*   **关键技术细节**：
    *   **统一特征空间**：System-1 和 System-2 共享同一个冻结的 ViT 编码器，确保意图传递无语义损失。
    *   **两阶段训练**：
        1.  **解耦预热**：System-2 学习预测未来状态，System-1 在真实未来状态引导下学习控制。
        2.  **端到端协同**：打通梯度流，动作误差产生的梯度可以回传至 VLM，使其生成的意图具备“动作感知”能力。

#### 3. 实验设计
*   **仿真环境**：使用 **RoboCasa GR1 Tabletop** 基准测试，包含 24 个任务（18 个拾取放置任务，6 个涉及橱柜、微波炉等关节物体的交互任务）。
*   **真实世界**：在 **IRON-R01-1.11 人形机器人**上进行部署，测试包括拨动瓶子、倒水等任务。
*   **数据集**：
    *   机器人演示数据（全量 24,000 条，少样本 2,400 条）。
    *   异构人类数据：引入 **EgoDex** 数据集（约 2.7 万条人类视频），验证跨具身学习能力。
*   **对比方法**：包括 Diffusion Policy、UWM、FLARE、GR00T-N1.6 以及基于 Qwen3-VL 的多种最新 VLA 变体（如 $\pi$-Qwen3, FAST-Qwen3）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号、数量或总训练时长。
*   **训练规模**：提到了训练步数，例如全量数据模式下为 160,000 步，少样本模式下为 40,000 步。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖 24 个任务，每个任务评估 50 个回合，数据量充足。
    *   **消融实验**非常详尽：分别验证了世界建模目标、接口设计（对比了 SEER 和 FLARE 的连接方式）、特征空间一致性以及预热策略的重要性。
    *   **泛化性测试**：设计了三种 OOD（分布外）场景：未见过的外观、未见过的物体组合、未见过的物体类别。
*   **评价**：实验设计客观且公平，通过与相同 VLM 主干（Qwen2.5/Qwen3）的变体对比，排除了模型规模带来的增益，证明了架构设计的有效性。

#### 6. 主要结论与发现
*   **性能领先**：DIAL 在 RoboCasa 基准上刷新了 SOTA，平均成功率达 70.2%。
*   **高数据效率**：DIAL 仅需 **1/10** 的机器人演示数据（少样本模式），性能即可超越使用全量数据的现有最强方法（FLARE）。
*   **跨具身泛化**：通过学习人类视频，DIAL 显著提升了在未见物体上的零样本泛化能力。
*   **稳定性**：两阶段训练（先解耦再统一）是防止 VLM 表征坍缩、确保端到端训练成功的关键。

#### 7. 优点与亮点
*   **结构化瓶颈设计**：将视觉预见作为硬性瓶颈，强制 System-1 必须理解 System-2 的意图才能执行，有效避免了 VLA 模型常见的“捷径学习”（Shortcut Learning）。
*   **潜空间操作**：在潜空间而非像素空间进行世界建模，极大地降低了计算开销，同时保留了 VLM 的语义深度。
*   **可视化可解释性**：通过 PCA 可视化证明了 System-2 确实预测了任务相关的物理变化（如物体移动后的位置），而非简单的图像重建。

#### 8. 不足与局限
*   **System-1 规模限制**：目前 System-1 的 DiT 骨干相对较小，可能限制了处理极高频、极复杂多模态反馈的能力。
*   **视觉编码器冻结**：为了稳定训练，目前冻结了原生 ViT。虽然论文提到未来计划通过 EMA 等策略解冻，但当前模型对视觉特征的微调能力受限。
*   **推理延迟**：尽管比视频生成类模型快，但 System-2（3B 规模 VLM）的实时推理对于某些超高频控制场景（如 >100Hz）仍可能存在压力。

（完）
