---
title: "StarVLA-$α$: Reducing Complexity in Vision-Language-Action Systems"
title_zh: StarVLA-α：降低视觉-语言-动作系统的复杂性
authors: "Jinhui Ye, Ning Gao, Senqiao Yang, Jinliang Zheng, Zixuan Wang, Yuxin Chen, Pengguang Chen, Yilun Chen, Shu Liu, Jiaya Jia"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11757v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 视觉-语言-动作系统的 StarVLA 基准
tldr: "StarVLA-α 针对当前视觉-语言-动作（VLA）模型架构复杂且碎片化的问题，提出了一个极简且强大的基准模型。通过在受控条件下重新评估动作建模、预训练和接口工程等关键设计，该研究证明了仅依靠强大的 VLM 主干和最简设计即可在 LIBERO、SimplerEnv 等多个基准测试中取得优异性能。在真实世界 RoboChallenge 测试中，其表现优于 π0.5 达 20%，为 VLA 研究提供了简洁高效的新起点。"
selection_source: fresh_fetch
motivation: 现有的 VLA 模型架构和训练流程过于复杂且碎片化，导致难以系统分析不同设计选择对性能的实际影响。
method: 提出 StarVLA-α 基准，通过最小化架构复杂度和消除工程技巧，在统一的多基准数据集上重新评估动作建模与预训练策略。
result: "该模型在 LIBERO、SimplerEnv 等多个仿真环境中表现强劲，并在真实世界 RoboChallenge 基准测试中比 π0.5 性能提升了 20%。"
conclusion: 强大的 VLM 主干结合极简设计足以实现高性能 VLA，无需依赖复杂的架构或额外的工程技巧。
---

## 摘要
视觉-语言-动作 (VLA) 模型近期已成为构建通用机器人智能体的一种极具前景的范式。然而，VLA 领域仍处于高度碎片化且复杂的态势：现有方法在架构、训练数据、具身配置以及特定基准测试的工程设计方面存在显著差异。在这项工作中，我们推出了 StarVLA-α，这是一个简单而强大的基准模型，旨在受控条件下研究 VLA 的设计选择。StarVLA-α 刻意最小化了架构和流水线的复杂性，以减少实验混杂因素并实现系统性分析。具体而言，我们重新评估了几个关键的设计维度，包括动作建模策略、机器人特定预训练和接口工程。通过在 LIBERO、SimplerEnv、RoboTwin 和 RoboCasa 上进行的统一多基准训练，这一简单的基准模型表现出极强的竞争力，这表明强大的 VLM 骨干网络结合极简设计，已足以在不依赖额外架构复杂性或工程技巧的情况下实现卓越性能。值得注意的是，我们的单个通用模型在公开的真实世界 RoboChallenge 基准测试中比 π_{0.5} 高出 20%。我们期望 StarVLA-α 能为 VLA 领域的未来研究提供一个坚实的起点。代码将发布于 https://github.com/starVLA/starVLA。

## Abstract
Vision-Language-Action (VLA) models have recently emerged as a promising paradigm for building general-purpose robotic agents. However, the VLA landscape remains highly fragmented and complex: as existing approaches vary substantially in architectures, training data, embodiment configurations, and benchmark-specific engineering. In this work, we introduce StarVLA-$α$, a simple yet strong baseline designed to study VLA design choices under controlled conditions. StarVLA-$α$ deliberately minimizes architectural and pipeline complexity to reduce experimental confounders and enable systematic analysis. Specifically, we re-evaluate several key design axes, including action modeling strategies, robot-specific pretraining, and interface engineering. Across unified multi-benchmark training on LIBERO, SimplerEnv, RoboTwin, and RoboCasa, the same simple baseline remains highly competitive, indicating that a strong VLM backbone combined with minimal design is already sufficient to achieve strong performance without relying on additional architectural complexity or engineering tricks. Notably, our single generalist model outperforms $π_{0.5}$ by 20\% on the public real-world RoboChallenge benchmark. We expect StarVLA-$α$ to serve as a solid starting point for future research in the VLA regime. Code will be released at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

这篇论文由香港科技大学、西安交通大学、香港中文大学、清华大学、阿里巴巴通义实验室及思谋科技的研究团队共同完成。以下是对该论文的深度结构化总结：

### 1. 研究动机与核心问题
*   **研究背景**：视觉-语言-动作（VLA）模型是构建通用机器人智能体的核心。然而，当前的 VLA 领域高度碎片化，不同系统在模型架构（如扩散模型 vs. 自回归）、预训练数据、具身配置以及特定基准测试的工程技巧上差异巨大。
*   **核心问题**：这种复杂性导致难以判断性能提升究竟源于模型创新，还是源于数据集选择或繁琐的工程调优。
*   **研究目标**：提出一个极简且强大的基准模型 **StarVLA-α**，通过消除实验混杂因素，系统性地重新评估哪些设计选择（如动作头、预训练、数据工程）对性能真正起决定性作用。

### 2. 方法论：StarVLA-α 框架
*   **核心思想（最小充分性假设）**：一个强大的视觉-语言模型（VLM）骨干网络配合轻量级的动作头，足以捕捉复杂设计所带来的大部分收益。
*   **关键技术细节**：
    *   **极简架构**：采用 **Qwen3-VL** 作为统一的骨干网络，原生处理视觉和语言输入。
    *   **轻量动作头**：在 VLM 之上仅添加一个简单的 **MLP（多层感知机）** 动作头，读取特定动作 Token 的隐藏状态并回归连续动作块。
    *   **最小化数据处理**：不使用复杂的历史帧堆叠或机器人状态输入，仅对原始 RGB 图像和语言指令进行处理，动作值仅进行简单的均值/方差归一化。
    *   **统一接口**：通过简单的 **填充（Padding）策略** 将不同机器人的动作空间统一到 32 维，无需为不同具身设计专门的动作头。

### 3. 实验设计
*   **仿真基准测试**：
    *   **LIBERO**：评估多任务学习和知识迁移。
    *   **SimplerEnv**：在仿真中评估真实世界策略（Google Robot 和 WidowX 场景）。
    *   **RoboTwin 2.0**：评估双臂协作能力。
    *   **RoboCasa-GR1**：评估人形机器人在家庭场景中的操作。
*   **真实世界基准**：**RoboChallenge**（包含 11 项标准任务，如插花、折抹布、开关灯等）。
*   **对比方法**：包括 OpenVLA-OFT、$\pi_0$、$\pi_{0.5}$、GR00T-N1.6、FAST 等当前最先进的 VLA 模型。

### 4. 资源与算力
*   **算力消耗**：论文在附录 C 中明确列出了不同任务所需的算力资源：
    *   **LIBERO**：8 × A100 GPU。
    *   **SimplerEnv / RoboCasa / RoboTwin**：各需 16 × A100 GPU。
    *   **全基准联合训练（Generalist）**：使用了 **64 × A100 GPU**。
*   **训练细节**：最大训练步数为 100k，采用余弦学习率调度。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了极其详尽的实验，涵盖了 4 个仿真基准（含数十个子任务）和 1 个真实世界基准。
*   **消融实验**：针对三个关键轴进行了系统性消融：
    1.  **动作头设计**：对比了 MLP、FAST（离散 Token）、Diffusion/Flow-matching（扩散/流匹配）等。
    2.  **预训练影响**：对比了无预训练、OXE 预训练、InternData 预训练等。
    3.  **数据工程**：对比了本体感知输入、历史帧、增量动作（Delta）等。
*   **客观性与公平性**：通过在受控的骨干网络和训练协议下进行对比，有效隔离了变量，实验设计非常客观且具有说服力。

### 6. 主要结论与发现
*   **架构不重要，骨干很重要**：在拥有强大 VLM（如 Qwen3）的前提下，动作头的设计（MLP vs. 扩散模型）对性能影响有限，简单的 MLP 表现极佳。
*   **预训练是双刃剑**：加入异构的机器人预训练数据（如 OXE）有时反而会损害特定领域的泛化能力，除非预训练数据与目标任务高度对齐。
*   **工程技巧边际效应递减**：本体感知、历史帧等工程手段在小数据量下有微弱提升，但在数据充足时几乎没有额外收益。
*   **通用模型可行性**：通过简单的动作填充策略，单个通用模型（Generalist）在多个基准上均能达到或超过专用模型（Specialist）的水平。
*   **关键因素**：**更大的 Batch Size** 和 **更强的 VLM 初始化** 是提升通用 VLA 性能的最关键因素。

### 7. 优点与亮点
*   **化繁为简**：挑战了当前 VLA 领域追求复杂架构的趋势，证明了“大道至简”的可能性。
*   **系统性分析**：通过严谨的受控实验，为社区理清了 VLA 设计中的有效因素和无效因素。
*   **真实世界表现强劲**：在 RoboChallenge 真实机器人测试中，StarVLA-α 的成功率比 $\pi_{0.5}$ 高出 20%，证明了其极简设计的实用性。
*   **开源贡献**：提供了 Lego 式的模块化代码库，方便后续研究者快速复现和扩展。

### 8. 不足与局限
*   **对 VLM 的依赖**：模型性能高度依赖于 Qwen3-VL 这种顶尖 VLM 的预训练质量，对于无法获取此类强大骨干的研究者门槛较高。
*   **高频动态任务覆盖不足**：实验主要集中在桌面操纵任务，对于需要极高频率反馈或复杂动态平衡的任务（如高速接球或复杂地形行走）尚未充分验证。
*   **数据效率**：虽然证明了数据工程在数据充足时无效，但在极低数据（Few-shot）场景下，如何更高效地利用数据仍是挑战。

（完）
