---
title: "AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement"
title_zh: AnySlot：用于零样本插槽级放置的目标条件化视觉-语言-动作策略
authors: "Zhaofeng Hu, Sifan Zhou, Qinbo Zhang, Rongtao Xu, Qi Su, Ci-Jyun Liang"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10432v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于机器人操作的目标条件视觉-语言-动作策略
tldr: AnySlot是一个针对零样本槽位级放置任务的VLA框架，旨在解决现有模型在复杂指令下精度不足的问题。它通过引入显式空间视觉目标（场景标记）作为中间表示，将任务解耦为高层语义定位与底层精准执行，并配套推出了SlotBench基准测试。实验表明，AnySlot在零样本场景下的放置精度和鲁棒性显著优于现有基准模型。
selection_source: fresh_fetch
motivation: 现有的VLA策略在处理需要亚厘米级精度和复杂语言理解的槽位放置任务时，难以同时保证语义定位的准确性和动作执行的精确度。
method: AnySlot通过将语言指令转化为显式的视觉场景标记作为中间目标，并利用目标条件化的VLA策略实现高层选择与底层控制的有效解耦。
result: 在包含九类任务的SlotBench基准测试中，AnySlot在零样本槽位级放置任务上的表现大幅领先于传统的扁平化VLA模型和模块化定位方法。
conclusion: 将复杂的组合指令解耦为显式视觉目标引导的层次化架构，是提升机器人策略在精密操作任务中泛化能力和准确性的有效途径。
---

## 摘要
视觉-语言-动作（VLA）策略已成为通用机器人操控的一种多功能范式。然而，在组合式语言指令下进行精确的物体放置，对于现代单体式 VLA 策略来说仍然是一个重大挑战。插槽级任务既需要可靠的插槽定位（grounding），也需要亚厘米级的执行精度。为此，我们提出了 AnySlot，这是一个通过引入显式空间视觉目标作为语言定位与控制之间的中间表示，从而降低组合复杂性的框架。AnySlot 通过生成场景标记（scene marker）将语言转化为显式视觉目标，随后利用目标条件化 VLA 策略执行该目标。这种分层设计有效地将高层插槽选择与底层执行解耦，确保了语义准确性和空间鲁棒性。此外，考虑到目前缺乏针对此类高精度任务的基准测试，我们推出了 SlotBench，这是一个包含九个任务类别的综合仿真基准，专门用于评估插槽级放置中的结构化空间推理。大量实验表明，AnySlot 在零样本插槽级放置方面显著优于扁平化（flat）VLA 基线和之前的模块化定位方法。

## Abstract
Vision-Language-Action (VLA) policies have emerged as a versatile paradigm for generalist robotic manipulation. However, precise object placement under compositional language instructions remains a major challenge for modern monolithic VLA policies. Slot-level tasks require both reliable slot grounding and sub-centimeter execution accuracy. To this end, we propose AnySlot, a framework that reduces compositional complexity by introducing an explicit spatial visual goal as an intermediate representation between language grounding and control. AnySlot turns language into an explicit visual goal by generating a scene marker, then executes this goal with a goal-conditioned VLA policy. This hierarchical design effectively decouples high-level slot selection from low-level execution, ensuring both semantic accuracy and spatial robustness. Furthermore, recognizing the lack of existing benchmarks for such precision-demanding tasks, we introduce SlotBench, a comprehensive simulation benchmark featuring nine task categories tailored to evaluate structured spatial reasoning in slot-level placement. Extensive experiments show that AnySlot significantly outperforms flat VLA baselines and previous modular grounding methods in zero-shot slot-level placement.

---

## 论文详细总结（自动生成）

这是一份关于论文《AnySlot: Goal-Conditioned Vision-Language-Action Policies for Zero-Shot Slot-Level Placement》的结构化深度总结：

### 1. 核心问题与整体含义
该论文关注机器人操作中一个极具挑战性的任务：**插槽级精准放置（Slot-Level Placement）**。
*   **研究动机**：现有的视觉-语言-动作（VLA）策略在处理复杂、组合式的语言指令（如“将方块放入左数第二个、最稳定的插槽”）时，往往难以兼顾高层语义推理与底层亚厘米级的执行精度。
*   **核心矛盾**：扁平化（Flat）端到端模型缺乏空间推理能力；而传统的模块化模型（VLM预测坐标+控制器）由于坐标回归的粗糙性，难以满足精密对准的要求。
*   **整体含义**：AnySlot 提出通过“显式视觉目标”作为桥梁，将复杂的语言理解转化为图像中的视觉标记，从而实现推理与控制的解耦。

### 2. 方法论
AnySlot 采用两阶段的分层架构：
*   **核心思想**：不让模型直接输出动作或坐标，而是先在图像上“画”出目标位置，再让底层策略去“追”这个视觉标记。
*   **阶段一：高层目标构建（High-level Goal Construction）**
    *   利用现成的图像生成模型（如 Nano-Banana）根据语言指令在主视图图像中生成一个**蓝色球体标记**。
    *   通过深度图和相机内参将该标记反投影到 3D 空间，获得世界坐标系下的锚点。
    *   将该 3D 锚点重新投影到所有相机视图（如手眼相机、侧视相机），生成视图一致的视觉目标覆盖层（Overlay）。
*   **阶段二：底层目标条件化策略（Low-level Policy）**
    *   使用基于 PaliGemma-3B 主干和流匹配（Flow-matching）动作专家的 **$\pi_{0.5}$ 策略**。
    *   策略输入包含带有蓝色球体标记的多视图图像，指令被简化为固定的“移动物体到蓝色球体引导的插槽”。
    *   这种设计让底层策略专注于视觉跟踪和精密控制，而非复杂的语义解析。

### 3. 实验设计
*   **基准测试（SlotBench）**：论文推出了专门的仿真基准，包含 9 类空间推理任务：序数推理、尺寸比较、高度比较、距离推理、组合关系、逻辑否定、模糊语言、适宜性推理（Affordance）和常识推理。
*   **数据集**：
    *   **Dsyn（合成数据集）**：包含 20,000 个带有视觉目标标记的插槽放置示例，用于训练底层策略。
    *   **Dstd（标准数据集）**：仅含语言指令，用于对比实验。
*   **对比方法**：
    *   **扁平化 VLA**：OpenVLA, $\pi_0$, $\pi_{0.5}$, Diffusion Policy。
    *   **模块化方法**：RoboPoint, HAMSTER, AnyPlace。
    *   **Oracle**：由人类专家提供完美的高层目标。

### 4. 资源与算力
*   **硬件**：使用了单台 **NVIDIA H200 GPU**。
*   **训练细节**：对 $\pi_{0.5}$ 进行了全参数微调，训练步数为 20,000 步，Batch Size 为 64。
*   **推理延迟**：图像生成模型（Nano-Banana 2）的推理延迟约为 11 秒，这在实时性要求极高的场景下是一个显著的开销。

### 5. 实验数量与充分性
*   **实验规模**：在 SlotBench 的 9 个类别中，每个任务至少包含 5 种随机场景布局和 5 种指令变体，每种方法在每个任务上进行 50 次测试。
*   **消融实验**：非常充分。作者对比了不同的高层视觉模型（GPT-5.3, Gemini-3.1, Flux 等）、不同的底层策略架构（PEFT vs 全微调）、以及有无合成数据集的对比。
*   **客观性**：通过引入 Oracle（人类专家）基线，明确了系统的性能瓶颈在于高层推理还是底层执行，实验设计逻辑严密。

### 6. 主要结论与发现
*   **性能飞跃**：AnySlot 在 SlotBench 上的平均成功率接近 **90%**，而所有扁平化端到端 VLA 模型在绝大多数任务上几乎全军覆没（成功率接近 0%）。
*   **解耦的必要性**：实验证明，将空间意图显式化为视觉目标，比直接让 VLM 输出坐标（IA 精度低）或让 VLA 直接理解复杂指令要有效得多。
*   **泛化能力**：AnySlot 展现了极强的零样本（Zero-shot）泛化能力，能够处理从未见过的插槽布局和复杂的逻辑指令。

### 7. 优点
*   **架构优雅**：通过“视觉标记”这一中间表示，巧妙地解决了 VLM 空间定位不准和 VLA 语义理解不足的痛点。
*   **视图一致性**：通过 3D 锚点重投影技术，确保了多摄像头系统在执行任务时对目标位置的认知是统一的。
*   **基准贡献**：SlotBench 填补了机器人领域缺乏精密插槽级操作评测工具的空白。

### 8. 不足与局限
*   **推理延迟**：依赖外部图像生成模型导致单次决策耗时较长（>10秒），目前难以应用于动态或高速操作场景。
*   **深度依赖**：第一阶段的目标构建高度依赖高质量的深度图和精确的相机标定，在光照不佳或透明物体场景下可能失效。
*   **误差累积**：如果图像生成模型在第一步标记错了位置，底层策略会“精准地犯错”，系统缺乏自我纠错机制。

（完）
