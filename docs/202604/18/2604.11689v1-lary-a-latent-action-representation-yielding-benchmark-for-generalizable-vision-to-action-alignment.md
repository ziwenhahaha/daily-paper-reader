---
title: "LARY: A Latent Action Representation Yielding Benchmark for Generalizable Vision-to-Action Alignment"
title_zh: LARY：一个用于可泛化视觉到动作对齐的潜在动作表示生成基准
authors: "Dujun Nie, Fengjiao Chen, Qi Lv, Jun Kuang, Xiaoyu Li, Xuezhi Cao, Xunliang Cai"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11689v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 评估VLA模型的潜动作表示
tldr: 针对视觉-语言-动作（VLA）模型中显式动作数据短缺的问题，本文提出了LARY基准，用于评估从视频中提取的潜动作表示在语义理解和机器人控制方面的能力。该基准包含百万级视频及多模态轨迹数据。研究发现，未经动作监督的通用视觉基础模型在动作对齐上优于专门的具身模型，且潜空间比像素空间更适合物理动作对齐，强调了语义抽象在视觉到动作转化中的重要性。
selection_source: fresh_fetch
motivation: 旨在解决如何将大规模无标签人类视频转化为可用于机器人控制的鲁棒潜动作表示，并缺乏统一评估标准的问题。
method: 提出了LARY基准框架，通过涵盖151个类别、百万级视频及多具身轨迹的大规模数据集，评估潜动作在语义和控制层面的表现。
result: 实验表明通用视觉基础模型在动作表征上优于专用具身模型，且潜空间在物理动作对齐方面显著优于像素空间。
conclusion: 通用视觉表示天然蕴含动作相关知识，且语义层面的抽象比像素级重建更有效地促进了从视觉到动作的对齐。
---

## 摘要
虽然显式动作数据的短缺限制了视觉-语言-动作（VLA）模型，但人类动作视频提供了一个可扩展但未标注的数据源。利用大规模人类视频数据集的一个关键挑战在于将视觉信号转化为独立于本体的表示，即潜在动作。然而，潜在动作表示从视觉观测中推导出鲁棒控制的能力尚未得到严格评估。我们提出了潜在动作表示生成（LARY）基准，这是一个统一的框架，用于在高级语义动作（做什么）和低级机器人控制（怎么做）两个层面上评估潜在动作表示。该精心策划的数据集包含超过 100 万个视频（1,000 小时），涵盖 151 个动作类别，以及跨越不同具身形式和环境的 62 万个图像对和 59.5 万条运动轨迹。我们的实验揭示了两个关键见解：（i）在没有任何动作监督的情况下训练的通用视觉基础模型，其表现始终优于专门的具身潜在动作模型。（ii）基于潜在空间的视觉空间在本质上比基于像素的空间更能与物理动作空间对齐。这些结果表明，通用视觉表示本质上编码了用于物理控制的动作相关知识，并且语义级抽象是比像素级重建更有效的视觉到动作路径。

## Abstract
While the shortage of explicit action data limits Vision-Language-Action (VLA) models, human action videos offer a scalable yet unlabeled data source. A critical challenge in utilizing large-scale human video datasets lies in transforming visual signals into ontology-independent representations, known as latent actions. However, the capacity of latent action representation to derive robust control from visual observations has yet to be rigorously evaluated. We introduce the Latent Action Representation Yielding (LARY) Benchmark, a unified framework for evaluating latent action representations on both high-level semantic actions (what to do) and low-level robotic control (how to do). The comprehensively curated dataset encompasses over one million videos (1,000 hours) spanning 151 action categories, alongside 620K image pairs and 595K motion trajectories across diverse embodiments and environments. Our experiments reveal two crucial insights: (i) General visual foundation models, trained without any action supervision, consistently outperform specialized embodied latent action models. (ii) Latent-based visual space is fundamentally better aligned to physical action space than pixel-based space. These results suggest that general visual representations inherently encode action-relevant knowledge for physical control, and that semantic-level abstraction serves as a fundamentally more effective pathway from vision to action than pixel-level reconstruction.

---

## 论文详细总结（自动生成）

这是一份关于论文《LARY: A Latent Action Representation Yielding Benchmark for Generalizable Vision-to-Action Alignment》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
具身智能领域面临严重的“数据孤岛”问题：带动作标注的机器人数据稀缺，而海量的人类动作视频虽易获取却缺乏显式动作标签。为了利用这些视频，研究者提出了**潜在动作（Latent Actions）**的概念，即将视觉变化转化为独立于具体本体的表征。
然而，现有的潜在动作模型（LAM）评估通常依赖于下游任务的成功率或定性的聚类可视化，这导致**表征质量与策略学习过程耦合**，难以客观衡量表征本身的泛化性和物理对齐能力。本论文提出了 **LARY 基准**，旨在通过解耦评估，系统性地衡量潜在动作在语义理解（做什么）和物理控制（怎么做）两个维度的表现。

### 2. 论文提出的方法论
论文构建了一个统一的评估框架，核心思想是将潜在动作表征的质量转化为两个具体的数学任务：
*   **分层语义探测协议（Hierarchical Semantic Probing）**：
    *   **运动级原子原语（Kinematic-Level Atomic Primitives）**：评估表征是否能区分微小的末端执行器动作（如“向上移动”、“关闭夹爪”）。
    *   **任务级复合行为（Task-Level Composite Behaviors）**：评估表征在不同具身（人/机器人）和场景下抽象复杂行为（如“拿起”、“放置”、“搅拌”）的能力。
*   **物理执行映射评估（Physical Execution Mapping Assessment）**：
    *   通过**动作回归任务**，测试潜在空间是否保留了足够的物理细节来重建末端执行器的连续运动轨迹。
*   **关键技术细节**：
    *   **自动化数据引擎**：利用大模型（如豆包-1.5-pro-vision）对异构数据集进行自动重切分和重标注，确保时间边界和语义标签的一致性。
    *   **通用 LAM 训练**：提出将冻结的通用视觉编码器（如 DINOv3, SigLIP 2）作为骨干网络，通过 VQ-VAE 架构训练潜在动作模型，以利用大规模视觉预训练的先验知识。

### 3. 实验设计
*   **数据集与场景**：
    *   涵盖 11 种机器人平台及人类数据。
    *   包含 120 万+ 视频剪辑（1000+ 小时）、62 万个图像对、59.5 万条运动轨迹。
    *   涉及 151 个动作类别，覆盖家庭、工厂、厨房等多样化环境。
*   **对比方法（4 类共 11 个模型）**：
    *   **具身 LAM**：LAPA, UniVLA, villa-X。
    *   **通用语义编码器**：DINOv3, V-JEPA 2。
    *   **生成式像素编码器**：Wan2.2 VAE, FLUX.2-dev VAE。
    *   **通用 LAM（本文提出）**：基于 DINOv2/v3、SigLIP2、MAGVIT2 改造的 LAPA 变体。

### 4. 资源与算力
*   **算力设备**：使用了 **8 台 NVIDIA H800 (141GB) GPU**。
*   **训练细节**：通用 LAM 变体在包含 200 万帧的内部数据集上训练了 **100,000 步**。
*   **推理/评估**：分类任务使用 4 层注意力探测器（Attentive Probe），回归任务使用简单的 MLP 动作专家模型。

### 5. 实验数量与充分性
*   **实验规模**：论文在 11 个模型上进行了跨任务（分类与回归）、跨具身（人与机器人）、跨视角（第一与第三视角）的全面评估。
*   **消融实验**：针对 Codebook 大小、序列长度、潜在维度以及采样步长（Stride）进行了详细的消融研究。
*   **充分性评价**：实验设计非常充分且具有说服力。通过解耦表征与策略，并引入大规模通用视觉模型作为基准，揭示了现有具身模型在表征学习上的局限性。实验过程考虑了长尾分布、时空接地（Grounding）等多个维度，评估较为客观公平。

### 6. 论文的主要结论与发现
1.  **通用视觉模型胜过专用模型**：未经任何动作监督训练的通用视觉基础模型（如 V-JEPA 2, DINOv3）在动作分类和回归任务上均显著优于专门为机器人设计的具身 LAM。
2.  **潜在空间优于像素空间**：基于特征（Latent-based）的视觉空间比基于像素（Pixel-based）的空间更能自然地与物理动作空间对齐。
3.  **表征坍缩风险**：现有的具身 LAM 往往因为数据规模有限或过早引入低级控制约束，导致表征质量受限甚至坍缩。
4.  **语义抽象是关键**：从视觉到动作的有效路径在于语义层面的抽象，而非简单的像素级重建。

### 7. 优点
*   **解耦评估**：首次提出了不依赖下游策略性能的潜在动作表征独立评估标准。
*   **规模宏大**：构建了目前已知规模最大、覆盖面最广的潜在动作评估基准数据集。
*   **见解深刻**：挑战了“必须在机器人数据上训练动作空间”的传统观念，证明了通用视觉预训练的强大潜力。
*   **工具链完整**：提供了自动化的数据处理引擎，便于社区持续扩展。

### 8. 不足与局限
*   **机器人动作性能鸿沟**：虽然通用 LAM 在人类动作上表现优异，但在特定机器人动作上的表现仍有提升空间，存在一定的“人-机”表现差异。
*   **计算成本**：自动化数据引擎依赖于高性能 VLM API，对于大规模实时处理可能存在成本和速度限制。
*   **应用限制**：目前主要关注末端执行器的宏观位移，对于极其精细的手指操作（如捏取微小物体）的表征能力尚未深入探讨。

（完）
