---
title: Generalizable Audio-Visual Navigation via Binaural Difference Attention and Action Transition Prediction
title_zh: 通过双耳差异注意力与动作迁移预测实现可泛化的视听导航
authors: "Jia Li, Yinfeng Yu"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05007v1"
tags: ["query:profile-2"]
score: 8.0
evidence: 3D环境中的视听导航与策略优化
tldr: "针对视听导航（AVN）中模型易过拟合语义特征和特定环境的问题，本文提出BDATP框架。该框架通过双耳差异注意力（BDA）模块建模双耳间差异以增强空间定向能力，并引入动作转换预测（ATP）辅助任务作为正则化项。实验表明，BDATP在Replica和Matterport3D数据集上显著提升了泛化性能，在未见声音场景下成功率最高提升21.6%，达到了SOTA水平。"
selection_source: fresh_fetch
motivation: 现有视听导航方法过度依赖声音语义特征且易在特定训练环境中产生过拟合，导致在未知场景下泛化能力较差。
method: 提出BDATP框架，结合双耳差异注意力模块增强空间感知，并利用动作转换预测辅助任务来缓解环境过拟合。
result: "在Replica和Matterport3D数据集上取得SOTA性能，尤其在Replica未听过声音设置下成功率实现了21.6%的显著提升。"
conclusion: BDATP框架能有效提升视听导航的泛化能力和鲁棒性，且可无缝集成到多种主流基准模型中。
---

## 摘要
在视听导航（AVN）中，智能体必须利用视觉和听觉线索在未见的 3D 环境中定位声源。然而，现有方法往往在未见场景中面临泛化难题，因为它们倾向于过拟合语义声音特征和特定的训练环境。为了应对这些挑战，我们提出了基于双耳差异注意力与动作迁移预测（BDATP）的框架，该框架共同优化了感知与策略。具体而言，双耳差异注意力（BDA）模块显式地建模双耳间差异以增强空间定向能力，从而减少对语义类别的依赖。同时，动作迁移预测（ATP）任务引入了一个辅助动作预测目标作为正则化项，以减轻针对特定环境的过拟合。在 Replica 和 Matterport3D 数据集上的广泛实验表明，BDATP 可以无缝集成到各种主流基准模型中，并带来持续且显著的性能提升。值得注意的是，我们的框架在大多数设置下都达到了最先进的成功率，在 Replica 数据集的未闻声音设置中，绝对提升幅度高达 21.6 个百分点。这些结果凸显了 BDATP 卓越的泛化能力及其在不同导航架构中的鲁棒性。

## Abstract
In Audio-Visual Navigation (AVN), agents must locate sound sources in unseen 3D environments using visual and auditory cues. However, existing methods often struggle with generalization in unseen scenarios, as they tend to overfit to semantic sound features and specific training environments. To address these challenges, we propose the \textbf{Binaural Difference Attention with Action Transition Prediction (BDATP)} framework, which jointly optimizes perception and policy. Specifically, the \textbf{Binaural Difference Attention (BDA)} module explicitly models interaural differences to enhance spatial orientation, reducing reliance on semantic categories. Simultaneously, the \textbf{Action Transition Prediction (ATP)} task introduces an auxiliary action prediction objective as a regularization term, mitigating environment-specific overfitting. Extensive experiments on the Replica and Matterport3D datasets demonstrate that BDATP can be seamlessly integrated into various mainstream baselines, yielding consistent and significant performance gains. Notably, our framework achieves state-of-the-art Success Rates across most settings, with a remarkable absolute improvement of up to 21.6 percentage points in Replica dataset for unheard sounds. These results underscore BDATP's superior generalization capability and its robustness across diverse navigation architectures.

---

## 论文详细总结（自动生成）

这是一份关于论文《Generalizable Audio-Visual Navigation via Binaural Difference Attention and Action Transition Prediction》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
视听导航（Audio-Visual Navigation, AVN）要求智能体在未见的 3D 环境中，仅依靠视觉（深度图）和听觉（双耳音频）线索找到声源。
*   **核心挑战**：现有方法在面对“未听过的声音类别”或“未见的建筑布局”时泛化能力极差。
*   **研究动机**：作者发现两个关键瓶颈：
    1.  **感知过拟合**：模型倾向于学习声音的语义特征（如“电话声”或“流水声”），而非通用的空间定位线索。
    2.  **策略过拟合**：强化学习（RL）策略容易死记硬背训练环境的几何结构，导致在陌生场景中出现震荡或回溯行为。

### 2. 方法论：BDATP 框架
论文提出了 **BDATP** 框架，旨在让智能体“听得更准（Hear Sharper）”且“行动更聪明（Act Smarter）”。

*   **双耳差异注意力（Binaural Difference Attention, BDA）**：
    *   **核心思想**：显式建模左右耳音频特征的差异，以提取跨类别的空间定向线索。
    *   **技术细节**：将双耳频谱图分别通过共享权重的 CNN 编码，得到左右声道特征图。计算两者的元素级绝对差异（Difference），并利用 1x1 卷积和 Sigmoid 函数生成通道注意力权重。最后，用该权重对原始特征进行调制并融合，强制模型关注双耳间的强度差和时间差。
*   **动作迁移预测（Action Transition Prediction, ATP）**：
    *   **核心思想**：引入一个辅助任务作为正则化项，预测智能体在下一时刻的动作。
    *   **技术细节**：在训练过程中，利用当前状态特征和前一动作，通过一个辅助网络（AuxNet）预测下一时刻的动作。该任务使用交叉熵损失进行监督。其逻辑在于：虽然环境在变，但导航的统计规律（如遇到障碍物转向、顺着声音梯度移动）是跨环境通用的。
*   **总损失函数**：将 PPO 强化学习损失与 ATP 辅助损失（权重 $\lambda=0.1$）相结合进行联合优化。

### 3. 实验设计
*   **数据集与场景**：
    *   **Replica**：较小的室内场景（平均 47.24 $m^2$）。
    *   **Matterport3D**：大型且复杂的室内场景（平均 517.34 $m^2$）。
*   **Benchmark 与对比方法**：
    *   **基准模型**：AV-NaV（基础 RL 模型）和 AV-WaN（分层路标预测模型）。
    *   **对比 SOTA**：SAVi（语义感知）、Dav-NaV（空间融合）、SA2GVAN（语义无关）、ORAN（全向感知）等。
*   **评估设置**：分为 **Heard**（训练过的声音，未见过的场景）和 **Unheard**（未见过的声音，未见过的场景）两种设置。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的硬件配置（如 GPU 型号、数量）以及具体的训练时长。这在 Embodied AI 论文中较为常见，通常依赖于 SoundSpaces 模拟器进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流数据集上进行了详尽测试。
    *   针对两种不同的导航架构（AV-NaV 和 AV-WaN）验证了 BDATP 的“即插即用”能力。
    *   **消融实验**：分别验证了 BDA 模块和 ATP 任务的贡献，并对辅助损失权重 $\lambda$ 进行了敏感度分析。
    *   **可视化分析**：提供了双耳特征分布图、动作迁移矩阵以及实际导航轨迹的对比图。
*   **充分性评价**：实验设计较为充分，涵盖了从定量指标（SR, SPL, SNA）到定性分析的多个维度，客观地展示了模型在极端泛化场景下的优势。

### 6. 主要结论与发现
*   **性能飞跃**：BDATP 在所有设置下均提升了基准模型的表现。在 Replica 数据集的 Unheard 设置下，将 AV-NaV 的成功率（SR）从 47.3% 提升至 68.6%（绝对提升 **21.3%**）。
*   **效率提升**：SNA（动作效率）指标的显著增长表明，智能体不仅能到达终点，而且路径更短、冗余动作更少。
*   **泛化机制**：BDA 成功解耦了声音的语义和空间信息；ATP 则通过时间一致性约束，使策略对环境几何变化的鲁棒性更强。

### 7. 优点
*   **即插即用**：作为一个通用的增强框架，它可以轻松集成到现有的各种 AVN 架构中。
*   **感知与策略协同**：不同于只关注特征融合的研究，该方法同时从感知（听觉差异）和策略（动作正则化）两个维度解决泛化问题。
*   **显著的零样本能力**：在完全陌生的声音类别上表现出极强的鲁棒性。

### 8. 不足与局限
*   **静态环境限制**：实验主要在静态声源和静态场景中进行，未考虑现实世界中常见的动态声源（如移动的人）或环境噪声干扰。
*   **算力信息缺失**：未提供训练成本参考，难以评估其在大规模部署时的效率。
*   **单智能体局限**：目前仅限于单智能体导航，未探讨在多智能体协作或竞争环境下的表现。

（完）
