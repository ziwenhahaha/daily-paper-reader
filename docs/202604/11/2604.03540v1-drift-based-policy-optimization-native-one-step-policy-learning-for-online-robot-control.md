---
title: "Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control"
title_zh: 基于漂移的策略优化：面向在线机器人控制的原生单步策略学习
authors: "Yuxuan Gao, Yedong Shen, Shiqi Zhang, Wenhao Yu, Yifan Duan, Jia pan, Jiajia Wu, Jiajun Deng, Yanyong Zhang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03540v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 机器人操作的单步策略学习
tldr: 针对多步生成式策略（如扩散模型）在机器人控制中推理速度慢、难以实现高频闭环控制的问题，本文提出了一种原生单步生成式策略框架。该框架包含漂移基策略（DBP）和漂移基策略优化（DBPO），通过将迭代优化过程内化到模型参数中，实现了在保持多峰动作建模能力的同时，仅需单步推理即可完成动作生成。实验证明该方法在离线模仿学习和在线强化学习中均表现优异，推理速度提升达100倍，并成功应用于105.2 Hz的实机双臂机器人控制。
selection_source: fresh_fetch
motivation: 现有的多步生成式策略因推理时需要多次迭代去噪，导致计算开销巨大，难以满足高频闭环控制和在线强化学习的需求。
method: 提出DBP利用定点漂移目标将迭代细化过程内化为单步生成模型，并结合DBPO实现在线强化学习的稳定更新。
result: 在多个基准测试中，该方法在保持甚至超越扩散策略性能的同时，推理速度提升了100倍，并在实机上实现了105.2 Hz的高频控制。
conclusion: 本研究证明了通过设计原生的单步生成式策略，可以在不牺牲多峰建模能力的前提下，显著提升机器人在线控制的实时性和学习效率。
---

## 摘要
尽管多步生成式策略通过建模多模态动作分布在机器人操控中取得了卓越性能，但它们在推理时需要多步迭代去噪。因此，生成每个动作都需要数十到数百次网络函数评估（NFEs），这使得它们在高频闭环控制和在线强化学习（RL）中成本高昂。为了解决这一局限性，我们提出了一个原生单步生成式策略的两阶段框架，将细化过程从推理阶段转移到训练阶段。首先，我们引入了基于漂移的策略（Drift-Based Policy, DBP），它利用不动点漂移目标将迭代细化内化到模型参数中，在设计上实现了单步生成式骨干网络，同时保留了多模态动作建模能力。其次，我们开发了基于漂移的策略优化（Drift-Based Policy Optimization, DBPO），这是一个在线强化学习框架，为预训练骨干网络配备了兼容的随机接口，在不牺牲单步部署特性的情况下实现了稳定的同策略（on-policy）更新。广泛的实验证明了该框架在离线模仿学习、在线微调和真实世界控制场景中的有效性。DBP 在推理速度提升高达 100 倍的同时，性能达到或超过了多步扩散策略。在挑战性的操控基准测试中，它也始终优于现有的单步基准模型。此外，DBPO 在在线设置中实现了有效且稳定的策略改进。在真实双臂机器人上的实验证明了其在 105.2 Hz 下的可靠高频控制。

## Abstract
Although multi-step generative policies achieve strong performance in robotic manipulation by modeling multimodal action distributions, they require multi-step iterative denoising at inference time. Each action therefore needs tens to hundreds of network function evaluations (NFEs), making them costly for high-frequency closed-loop control and online reinforcement learning (RL). To address this limitation, we propose a two-stage framework for native one-step generative policies that shifts refinement from inference to training. First, we introduce the Drift-Based Policy (DBP), which leverages fixed-point drifting objectives to internalize iterative refinement into the model parameters, yielding a one-step generative backbone by design while preserving multimodal action modeling capacity. Second, we develop Drift-Based Policy Optimization (DBPO), an online RL framework that equips the pretrained backbone with a compatible stochastic interface, enabling stable on-policy updates without sacrificing the one-step deployment property. Extensive experiments demonstrate the effectiveness of the proposed framework across offline imitation learning, online fine-tuning, and real-world control scenarios. DBP matches or exceeds the performance of multi-step diffusion policies while achieving up to $100\times$ faster inference. It also consistently outperforms existing one-step baselines on challenging manipulation benchmarks. Moreover, DBPO enables effective and stable policy improvement in online settings. Experiments on a real-world dual-arm robot demonstrate reliable high-frequency control at 105.2 Hz.

---

## 论文详细总结（自动生成）

以下是对论文《Drift-Based Policy Optimization: Native One-Step Policy Learning for Online Robot Control》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：当前机器人操控领域的主流是多步生成式策略（如扩散策略 Diffusion Policy），它们通过迭代去噪来建模复杂的动作分布。然而，这种机制在推理时需要数十次甚至数百次网络评估（NFE），导致高延迟，难以满足高频闭环控制（如 100Hz 以上）和在线强化学习（RL）的效率需求。
*   **核心问题**：如何在不牺牲多模态建模能力的前提下，实现“原生单步”（Native One-Step）的生成式策略，即在训练阶段完成细化，而在推理阶段仅需一次前向传播。

### 2. 方法论
论文提出了一个两阶段框架：
*   **第一阶段：基于漂移的策略（DBP）**
    *   **核心思想**：借鉴“漂移模型”（Drifting Models），将迭代细化过程内化到模型参数中。
    *   **关键技术**：在训练时构建一个“漂移场”，通过吸引力（向专家演示靠近）和排斥力（生成的多个假设之间相互排斥）来优化模型。
    *   **目标函数**：利用定点回归目标（Fixed-point regression），使模型在单次推理中就能直接生成高质量的动作块（Action Chunk）。
*   **第二阶段：基于漂移的策略优化（DBPO）**
    *   **核心思想**：为预训练的 DBP 骨干网络添加一个随机适配器，使其支持在线强化学习。
    *   **关键技术**：引入一个极简的随机接口，在训练时计算精确的动作似然（Likelihood）以支持 PPO 算法更新，而在部署时移除噪声，恢复确定性的单步推理。
    *   **锚点正则化（Anchor Regularization）**：通过惩罚更新后的策略与预训练策略之间的偏差，稳定在线微调过程。

### 3. 实验设计
*   **数据集与场景**：
    *   **离线模仿学习**：Diffusion Policy 测试集（Push-T, BlockPush, RoboMimic, Kitchen 等 12 个任务）；点云操控基准（Adroit 和 Meta-World 共 37 个任务）。
    *   **在线强化学习**：RoboMimic 操控任务和 D4RL 运动任务。
    *   **真实世界**：双臂 UR5 机器人，执行 Lift（提升）、Can（罐子移动）和 Bimanual Transport（双臂协作运输）。
*   **对比方法**：
    *   多步策略：Diffusion Policy, DP3。
    *   单步/加速策略：MP1, OMP, FlowPolicy, AdaFlow, ReinFlow, DMPO 等。

### 4. 资源与算力
*   **硬件配置**：实验使用了 **8 张 NVIDIA RTX 3090 GPU**。
*   **训练时长**：在 Adroit 任务上训练 3000 个 epoch，在 Meta-World 任务上训练 1000 个 epoch。
*   **推理效率**：推理延迟仅为 **9.5 ms**，在实机上实现了 **105.2 Hz** 的控制频率。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低维状态到图像、再到 3D 点云的多种输入模态，总计超过 50 个仿真任务及 3 个真实世界任务。
*   **消融实验**：针对在线 RL 中的“锚点正则化”进行了专门的消融研究，验证了其对稳定性的贡献。
*   **客观性**：通过在统一的架构（如 U-Net）和协议下对比现有最强基准（如 OMP, MP1），证明了 DBP 在原生单步生成上的优越性，实验设计较为公平、充分。

### 6. 主要结论与发现
*   **性能与速度双赢**：DBP 在推理速度提升 **100 倍**（从 100 NFE 降至 1 NFE）的同时，在多个任务上的成功率甚至超过了多步扩散策略。
*   **单步基准新高度**：在 37 个点云操控任务中，DBP 达到了 **88.4%** 的平均成功率，显著优于之前的单步领先模型 OMP（82.3%）。
*   **在线提升显著**：DBPO 能够有效利用在线交互进一步提升离线预训练策略的性能，且在不同领域（操控与运动）均表现出稳健性。

### 7. 优点
*   **原生单步设计**：不同于事后蒸馏或压缩，DBP 从训练目标入手实现单步生成，保证了推理的确定性低延迟。
*   **高频控制能力**：实机 105.2 Hz 的表现证明了其在实时闭环控制中的巨大潜力。
*   **多模态兼容性**：原生支持点云、图像等异构传感器输入。

### 8. 不足与局限
*   **任务复杂度限制**：目前的评估主要集中在桌面操控和结构化环境中的运动，对于长程（Long-horizon）组合任务或高度非结构化环境的泛化能力仍需进一步验证。
*   **样本效率**：虽然在线 RL 稳定，但在“从零开始学习”（Learning from scratch）场景下的样本效率尚未与大规模预训练模型进行深度对比。
*   **实机失败模式**：在双臂协作中仍存在动作冲突导致的失败，说明在极高精度的多臂协同上仍有优化空间。

（完）
