---
title: "Hi-LOAM: Hierarchical Implicit Neural Fields for LiDAR Odometry and Mapping"
title_zh: Hi-LOAM：用于激光雷达里程计与建图的分层隐式神经场
authors: "Zhiliu Yang, Jianyuan Zhang, Lianhui Zhao, Jinyu Dai, Zhu Yang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01720v1"
tags: ["query:profile-2"]
score: 8.0
evidence: 用于具身智能应用的LiDAR里程计与建图
tldr: Hi-LOAM 是一种针对自动驾驶和机器人导航提出的分层隐式神经 LiDAR 里程计与建图框架。针对现有方法依赖监督或大场景重建精度不足的问题，该框架利用基于八叉树的多级哈希表存储分层潜在特征，并通过浅层 MLP 解码为符号距离值。它采用无对应关系的扫描到隐式匹配进行位姿估计，且全过程自监督，无需预训练，在多个数据集上展现了卓越的重建精度和泛化能力。
selection_source: fresh_fetch
motivation: 现有 LOAM 框架在大规模复杂场景中存在依赖监督信号或重建细节保真度不足的问题。
method: 采用基于八叉树的多级哈希表存储分层特征，通过浅层 MLP 解码 SDF，并利用扫描到隐式匹配实现位姿估计。
result: 实验证明 Hi-LOAM 在多个真实和合成数据集上的性能及泛化性均优于现有的最先进方法。
conclusion: 该框架通过自监督的分层隐式神经表示，有效提升了大规模场景下 LiDAR 定位与建图的精度。
---

## 摘要
激光雷达里程计与建图（LOAM）是自动驾驶和机器人导航等具身智能（embodied-AI）应用中的关键技术。大多数现有的 LOAM 框架要么依赖于监督信号，要么缺乏重建保真度，在描绘大规模复杂场景的细节方面存在不足。为了克服这些局限性，我们提出了一种使用激光雷达传感器的多尺度隐式神经定位与建图框架，称为 Hi-LOAM。Hi-LOAM 接收激光雷达点云作为输入数据模态，在基于八叉树结构的多级哈希表中学习并存储分层潜在特征，随后在建图过程中，通过浅层多层感知机（MLP）将这些多尺度潜在特征解码为符号距离值。在位姿估计过程中，我们依靠一种无对应关系的“扫描到隐式”（scan-to-implicit）匹配范式来估计最优位姿，并将当前扫描注册到子图中。整个训练过程以自监督方式进行，无需模型预训练，并在应用于不同环境时展现出良好的泛化能力。在多个真实世界和合成数据集上的广泛实验表明，与现有的最先进方法相比，我们的 Hi-LOAM 在有效性和泛化能力方面具有优越的性能。

## Abstract
LiDAR Odometry and Mapping (LOAM) is a pivotal technique for embodied-AI applications such as autonomous driving and robot navigation. Most existing LOAM frameworks are either contingent on the supervision signal, or lack of the reconstruction fidelity, which are deficient in depicting details of large-scale complex scenes. To overcome these limitations, we propose a multi-scale implicit neural localization and mapping framework using LiDAR sensor, called Hi-LOAM. Hi-LOAM receives LiDAR point cloud as the input data modality, learns and stores hierarchical latent features in multiple levels of hash tables based on an octree structure, then these multi-scale latent features are decoded into signed distance value through shallow Multilayer Perceptrons (MLPs) in the mapping procedure. For pose estimation procedure, we rely on a correspondence-free, scan-to-implicit matching paradigm to estimate optimal pose and register current scan into the submap. The entire training process is conducted in a self-supervised manner, which waives the model pre-training and manifests its generalizability when applied to diverse environments. Extensive experiments on multiple real-world and synthetic datasets demonstrate the superior performance, in terms of the effectiveness and generalization capabilities, of our Hi-LOAM compared to existing state-of-the-art methods.

---

## 论文详细总结（自动生成）

以下是对论文《Hi-LOAM: Hierarchical Implicit Neural Fields for LiDAR Odometry and Mapping》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的激光雷达里程计与建图（LOAM）框架在处理大规模复杂场景时，往往面临两难境地：要么依赖昂贵的监督信号（如真值位姿进行预训练），要么重建精度不足，难以捕捉环境的精细几何细节。
*   **研究动机**：
    *   传统方法（如ICP及其变体）在点云稀疏或非均匀时容易失效。
    *   现有的隐式神经表示方法（如NeRF-LOAM）多采用单尺度特征，难以兼顾计算效率与环境细节的表达。
    *   需要一种无需预训练、能够自监督学习、且具备高保真重建能力的通用定位与建图框架。

### 2. 方法论
Hi-LOAM 提出了一种基于分层隐式神经场的增量式框架，核心流程如下：
*   **分层特征嵌入（Hierarchical Feature Embedding）**：
    *   利用**八叉树（Octree）**结构构建多尺度潜在特征，并存储在多级哈希表中。
    *   通过三线性插值提取最后三层（L-1, L-2, L-3）的特征进行拼接，形成多尺度特征向量。
*   **隐式神经建图**：
    *   **射线采样**：在物体表面附近和自由空间进行非均匀采样。
    *   **解码器**：使用浅层多层感知机（MLP）将拼接后的特征解码为**符号距离值（SDF）**。
    *   **损失函数**：采用自监督训练，结合了二元交叉熵损失（SDF监督）、Eikonal正则化（保证梯度物理意义）和法线方向对齐损失。
*   **神经隐式里程计**：
    *   **扫描到隐式地图匹配（Scan-to-Implicit Matching）**：直接将当前帧点云与隐式子图（Submap）进行匹配，无需显式寻找点对点或点对面关联。
    *   **优化策略**：使用**Levenberg-Marquardt (LM)** 算法进行二阶优化，通过最小化预测SDF值的平方和来估计最优的6-DoF位姿。

### 3. 实验设计
*   **数据集**：涵盖了7个以上真实与合成数据集，包括：
    *   **大规模室外**：KITTI（00-10序列）、MulRAN（长距离城市漫游）。
    *   **高动态环境**：SemanticPOSS。
    *   **手持/非平稳运动**：Newer College、Hilti-21、Hilti-23。
    *   **合成数据**：Mai City。
*   **Benchmark（基准指标）**：绝对轨迹误差（ATE RMSE）、相对平移漂移误差（%）、重建精度（Accuracy/Completion/F-score）。
*   **对比方法**：
    *   **传统/显式方法**：LeGO-LOAM, F-LOAM, KISS-ICP, SuMa++, IMLS-SLAM, SLAMesh等。
    *   **学习型/隐式方法**：LO-Net, PWCLO, NeRF-LOAM, PIN-SLAM等。

### 4. 资源与算力
*   **硬件平台**：实验主要在单张 **NVIDIA GeForce RTX 4090** GPU 上运行。
*   **运行效率**：
    *   **位姿估计**：平均每帧耗时约 **0.6秒**（约1.7 Hz）。
    *   **地图优化**：每帧耗时约 **2.8至5.5秒**（取决于数据集复杂度）。
    *   **模型规模**：MLP包含2个隐藏层，每层32个神经元，特征向量长度为12。

### 5. 实验数量与充分性
*   **实验规模**：论文在超过20个不同的场景序列上进行了测试，涵盖了从自动驾驶到手持设备、从室内到室外的多种工况。
*   **消融实验**：详细分析了**特征层级（LOF）**对精度的影响，证明了多尺度特征优于单尺度。
*   **敏感性分析**：对叶节点尺寸（Leaf Node Size）、八叉树深度（Tree Levels）和子图大小（Submap Size）进行了详尽的参数调优实验。
*   **评价**：实验设计非常充分且客观，通过跨数据集的对比验证了算法的泛化能力，尤其是在高动态和高重复性场景下的鲁棒性。

### 6. 主要结论与发现
*   **定位精度**：Hi-LOAM 在 KITTI 上的平均平移误差达到 0.48%，优于所有对比的学习型方法，并与最先进的传统 ICP 方法持平。
*   **建图质量**：在 Mai City 和 Newer College 数据集上，Hi-LOAM 的重建 F-score 显著高于 NeRF-LOAM 和 PIN-SLAM，证明了分层特征对细节恢复的有效性。
*   **泛化性**：自监督机制使其无需针对特定传感器或环境进行预训练，在手持设备和合成数据上均表现稳定。

### 7. 优点
*   **高保真度**：分层隐式表示能够自适应地拟合复杂几何形状，生成的网格（Mesh）更完整、伪影更少。
*   **无需监督**：完全自监督的学习方式解决了真值位姿难以获取的问题。
*   **鲁棒性强**：通过子图匹配和多尺度特征，有效缓解了动态物体干扰和环境重复性带来的定位漂移。
*   **开源贡献**：提供了公开的源代码，便于社区复现和改进。

### 8. 不足与局限
*   **实时性挑战**：虽然比 NeRF-LOAM 快，但 1.7 Hz 的位姿估计速度仍未达到严格的实时要求（通常需 10 Hz 以上），且地图优化耗时较长。
*   **闭环检测缺失**：目前框架主要关注里程计和增量建图，尚未集成全局闭环检测（Loop Closure），在超长距离场景下仍存在累计误差风险。
*   **内存开销**：相比单尺度隐式方法，存储多级哈希表特征会消耗更多的显存（虽然仍远低于原始点云）。

（完）
