---
title: "D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay for Stable Reinforcement Learning in Robotic Manipulation"
title_zh: D-SPEAR：用于机器人操作中稳定强化学习的双流优先经验自适应回放
authors: "Yu Zhang, Karl Mason"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27346v2"
tags: ["query:wam-vla"]
score: 9.0
evidence: 机器人操纵的强化学习与策略稳定性
tldr: 针对机器人操作中强化学习训练不稳定的问题，本文提出D-SPEAR框架。该框架通过解耦Actor和Critic的采样策略，让Critic利用优先经验回放提高学习效率，而Actor利用低误差样本稳定策略优化。结合自适应锚点机制和Huber损失函数，D-SPEAR在robosuite基准测试中显著提升了SAC、TD3等算法的性能与稳定性。
selection_source: fresh_fetch
motivation: 现有的离策强化学习算法在处理复杂机器人任务时，因忽视了Actor和Critic对数据需求的不同而导致训练不稳定和性能崩溃。
method: 提出双流经验回放框架，Critic采用优先采样，Actor采用低误差样本采样，并引入自适应锚点机制平衡采样策略。
result: 在robosuite的抬块和开门等挑战性任务中，D-SPEAR在最终性能和训练稳定性上均优于SAC、TD3和DDPG等基准算法。
conclusion: 解耦Actor与Critic的采样需求并结合自适应调整机制，是提升机器人操作任务中强化学习鲁棒性的有效途径。
---

## 摘要
由于丰富的接触动力学、长时程以及训练不稳定性，机器人操作对于强化学习而言仍具挑战性。尽管 SAC 和 TD3 等离策演员-评论家（actor-critic）算法在仿真中表现良好，但在现实设置中往往会遭遇策略振荡和性能崩溃，部分原因在于经验回放策略忽略了演员和评论家对数据的不同需求。我们提出了 D-SPEAR：双流优先经验自适应回放，这是一种在维持共享经验回放池的同时，解耦演员和评论家采样的回放框架。评论家利用优先回放进行高效的价值学习，而演员则使用低误差样本进行更新，以稳定策略优化。一种自适应锚定机制根据 TD 误差的变异系数来平衡均匀采样和优先采样，而基于 Huber 损失的评论家目标函数进一步提高了在异构奖励尺度下的鲁棒性。我们在 robosuite 基准测试中具有挑战性的机器人操作任务（包括抬块和开门）上评估了 D-SPEAR。结果表明，D-SPEAR 在最终性能和训练稳定性方面均一致优于包括 SAC、TD3 和 DDPG 在内的强离策基准算法，消融实验证实了演员端和评论家端回放流的互补作用。

## Abstract
Robotic manipulation remains challenging for reinforcement learning due to contact-rich dynamics, long horizons, and training instability. Although off-policy actor-critic algorithms such as SAC and TD3 perform well in simulation, they often suffer from policy oscillations and performance collapse in realistic settings, partly due to experience replay strategies that ignore the differing data requirements of the actor and the critic. We propose D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay, a replay framework that decouples actor and critic sampling while maintaining a shared replay buffer. The critic leverages prioritized replay for efficient value learning, whereas the actor is updated using low-error transitions to stabilize policy optimization. An adaptive anchor mechanism balances uniform and prioritized sampling based on the coefficient of variation of TD errors, and a Huber-based critic objective further improves robustness under heterogeneous reward scales. We evaluate D-SPEAR on challenging robotic manipulation tasks from the robosuite benchmark, including Block-Lifting and Door-Opening. Results demonstrate that D-SPEAR consistently outperforms strong off-policy baselines, including SAC, TD3, and DDPG, in both final performance and training stability, with ablation studies confirming the complementary roles of the actorside and critic-side replay streams.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **D-SPEAR** 的新型强化学习经验回放框架，旨在解决机器人操作任务中训练不稳定和性能崩溃的问题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在处理具有丰富接触动力学（contact-rich）和长时程（long-horizon）的机器人操作任务时，传统的离策（off-policy）演员-评论家（Actor-Critic）算法（如 SAC、TD3）容易出现策略振荡和后期性能崩溃。
*   **研究动机**：作者发现 Actor 和 Critic 对数据的需求存在“不匹配”。Critic 需要高 TD 误差的样本来快速修正价值估计；而 Actor 如果使用这些高误差（即不准确）的样本进行梯度更新，会导致策略不稳定。现有的经验回放（如 PER）统一对待两者，忽视了这种需求差异。

### 2. 方法论：D-SPEAR 框架
D-SPEAR 的核心思想是**在共享经验池的基础上，为 Actor 和 Critic 解耦采样策略**。
*   **双流采样结构**：
    *   **锚点集（Anchor Set）**：从回放池中均匀采样，保证数据的分布覆盖，防止过度偏向某些样本。
    *   **评论家流（Critic Stream）**：采用高 TD 误差优先采样（类似标准 PER），强调信息量大的样本以加速价值函数收敛。
    *   **演员流（Actor Stream）**：采用**逆优先采样**，即优先选择低 TD 误差的样本。这确保了 Actor 仅在 Critic 已经“学得较好”的稳定区域进行策略改进，避免被不可靠的梯度误导。
*   **自适应锚点机制**：引入 TD 误差的**变异系数（CV）**。当 TD 误差分布差异大时，降低均匀采样比例，增加优先采样；当误差分布趋于均匀时，增加均匀采样以维持稳定性。
*   **鲁棒目标函数**：Critic 采用 **Huber 损失**代替均方误差（MSE），以降低对异常值和异构奖励尺度的敏感性。

### 3. 实验设计
*   **实验场景**：基于 `robosuite` 仿真基准测试。
    *   **Lift（抬块）**：抓取并提升物体，涉及接触和长时程操作。
    *   **Door（开门）**：与铰链门交互，具有复杂的关节动力学。
*   **机器人平台**：7 自由度 Franka Panda 机械臂。
*   **对比基准（Baselines）**：
    *   **SAC**（Soft Actor-Critic）
    *   **TD3**（Twin Delayed DDPG）
    *   **DDPG**（Deep Deterministic Policy Gradient）

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或具体的训练时长。
*   **训练参数**：每个任务训练 250,000 个环境步（500 个回合），控制频率为 20Hz。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个代表性任务（Lift, Door）上进行了对比实验。
    *   所有实验均使用了 **5 个不同的随机种子**，并给出了均值和标准差（阴影区域）。
    *   **消融实验**：分别验证了“无双流结构”、“无低误差 Actor 流”和“无高误差 Critic 流”的情况。
*   **充分性评价**：实验设计较为标准，消融实验清晰地展示了各组件的贡献。虽然任务数量（2个）不算极多，但涵盖了机器人操作中核心的接触和动力学挑战，具有一定的说服力。

### 6. 主要结论与发现
*   **性能提升**：D-SPEAR 在最终回报（Return）上显著优于 SAC、TD3 和 DDPG。在 Lift 任务中，D-SPEAR 的得分几乎是 SAC 的两倍。
*   **稳定性增强**：相比于基准算法在后期出现的性能剧烈波动或崩溃，D-SPEAR 的学习曲线更加平滑且单调上升。
*   **互补性**：消融实验证明，Actor 端和 Critic 端的差异化采样策略是互补的，缺一不可。

### 7. 优点与亮点
*   **洞察深刻**：敏锐地捕捉到了 Actor-Critic 架构中采样需求的内在矛盾，并给出了直观的物理解释。
*   **即插即用**：该框架不改变底层 RL 算法的结构，可以轻松集成到现有的离策算法中。
*   **自适应性**：通过 CV 动态调整采样比例，减少了人工调参的负担。

### 8. 不足与局限
*   **任务覆盖面**：实验仅限于 `robosuite` 的两个任务，尚未在更复杂的长链任务（如多阶段装配）或视觉输入（Pixel-based）任务上进行验证（本实验使用低维状态输入）。
*   **计算开销**：虽然解耦采样提升了性能，但维护两个采样流和计算 CV 会带来额外的计算开销，文中未详细量化这一成本。
*   **现实迁移**：目前仅在仿真环境中验证，缺乏真机实验（Sim-to-Real）的支撑。

（完）
