Title: SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation

URL Source: https://arxiv.org/pdf/2603.26837v1

Published Time: Tue, 31 Mar 2026 00:06:36 GMT

Number of Pages: 10

Markdown Content:
# SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

Jiwen Zhang 1 2 * Xiangyu Shi 2 Siyuan Wang 3 Zerui Li 2 Zhongyu Wei 1 4 Qi Wu 2

Homepage: https://imnearth.github.io/Spatial-X/ 

Abstract 

Vision-and-Language Navigation (VLN) has re-cently benefited from Multimodal Large Lan-guage Models (MLLMs), enabling zero-shot nav-igation. While recent exploration-based zero-shot methods (Zhang et al., 2026) have shown promis-ing results by leveraging global scene priors, they rely on high-quality human-crafted scene recon-structions, which are impractical for real-world robot deployment. When encountering an un-seen environment, a robot should build its own priors through pre-exploration. However, these self-built reconstructions are inevitably incom-plete and noisy, which severely degrade meth-ods that depend on high-quality scene reconstruc-tions. To address these issues, we propose Spa-tialAnt , a zero-shot navigation framework de-signed to bridge the gap between imperfect self-reconstructions and robust execution. SpatialAnt introduces a physical grounding strategy to re-cover the absolute metric scale for monocular-based reconstructions. Furthermore, rather than treating the noisy self-reconstructed scenes as ab-solute spatial references, we propose a novel vi-sual anticipation mechanism. This mechanism leverages the noisy point clouds to render future observations, enabling the agent to perform coun-terfactual reasoning and prune paths that contra-dict human instructions. Extensive experiments in both simulated and real-world environments demonstrate that SpatialAnt significantly outper-forms existing zero-shot methods. We achieve a 66% Success Rate (SR) on R2R-CE and 50.8% SR on RxR-CE benchmarks. Physical deploy-ment on a Hello Robot further confirms the effi-ciency and efficacy of our framework, achieving a 52% SR in challenging real-world settings. 

> *

Work done during the visiting at Adelaide University. 1Fudan University 2Adelaide University 3University of Southern California 

> 4

Shanghai Innovation Institute. Correspondence to: Zhongyu Wei <zywei@fudan.edu.cn>. 

Preprint. March 31, 2026. Hole Clutter      

> Missing Structure
> Occlusion
> Human-crafted Scene Real Robot Reconstructed Scene
> (clean, idealized) (noisy)
> Global Scene Priors Previous Zero-shot Agents
> Real Robot Pre-exploration
> Idealized Pre-exploration
> Ours Framework (SpatialAnt)
> Real Robot Pre-exploration
> Idealized Pre-exploration
> Action Space: Waypoints Action Space: Sub-Paths
> front rear right
> Anticipated Views
> Fragile to noise
> Robust to noise

✓  

> ❌
> Robot Current Position Candidate Waypoint History Trajectory

Figure 1. The reality gap for pre-exploration based zero-shot VLN agents. Previous works adopt the ideal scene reconstructions provided by existing datasets (Chang et al., 2017), treating these global priors as accurate maps, and are sensitive to noise. Instead, we use the real robot reconstructed noisy scenes and propose a visual anticipation approach to transform the noisy scene into supportable sub-paths. 

1. INTRODUCTION 

Vision-and-Language Navigation (VLN) (Anderson et al., 2018) is a fundamental challenge in embodied AI that requires agents to follow human instructions while nav-igating real environments (Gu et al., 2022). With the rapid development of Multimodal Large Language Models (MLLMs) (Singh et al., 2025; Comanici et al., 2025), which possess strong cross-modal reasoning ability and open-world knowledge, MLLM-based zero-shot VLN agents have emerged (Qiao et al., 2025; Shi et al., 2025; He et al., 2025). Recent advances in zero-shot VLN methods (Bhatt et al., 2025; Zhang et al., 2026) have demonstrated the importance of pre-exploring the environment and obtain-ing global scene priors for navigation. These approaches, 1

> arXiv:2603.26837v1 [cs.RO] 27 Mar 2026 SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation

especially (Zhang et al., 2026), make the idealized assump-tion that high-quality scene reconstructions, such as the perfect point cloud of the environment in Matterport3D dataset (Chang et al., 2017), are readily available after pre-exploration. This introduces two significant reality gaps when a real robot is deployed in an unseen environment to actively explore the scene. Firstly, the acquisition of physically-grounded scene recon-structions is difficult under real robot deployment. Many robots are equipped only with monocular RGB cameras to maintain lightweight and low-cost. Even when depth sen-sors are present, they are often constrained by considerable noise and range limitations, compromising traditional depth-dependent methods such as TSDF (Curless & Levoy, 1996). While recent neural SLAM approaches (Wang et al., 2024; Liu et al., 2025) have attempted 3D reconstruction solely from RGB sequences, the absence of absolute depth leads to a fundamental scale ambiguity of the reconstructed scene. Without a reliable metric scale, the self-reconstructed scene fails to serve as the navigation reference. Secondly, reconstructed scenes from the egocentric robotic observations are inevitably subjected to noise, as shown in Figure 1. Unlike human-captured exocentric scene recon-structions in standard datasets (Chang et al., 2017), egocen-tric perception of robots is constrained by their physical embodiment, such as lower camera mounting heights and re-stricted field of view, leading to incomplete spatial coverage and structural occlusions in the reconstructed point clouds. This discrepancy causes previous exploration-based zero-shot agents to degrade significantly when the noisy scene priors are used, posing a dilemma: if a self-reconstructed scene is destined to be noisy, can it still reliably guide a zero-shot agent? 

To address these challenges, we present SpatialAnt , an au-tonomous zero-shot navigation framework that bridges the gap between imperfect reconstructions and robust execu-tion. Specifically, SpatialAnt adopts a practical setting to actively explore the environment using a single RGB cam-era. To resolve the scale ambiguity of monocular vision, we incorporate a physical grounding strategy that anchors the self-reconstructed point cloud to the metric scale in real-world units (meters), through a comparison between the rendered depth from the point cloud and the predicted metric depth from Depth-Anything-2 (Yang et al., 2024a). Since the reconstructed scenes are inherently noisy, in con-trast to previous works (Zhang et al., 2026), we propose not to treat them simply as an absolute position reference, but rather as a foundation for visual alignment. Analogous to biological ants utilizing their antennae to anticipate the path ahead, we propose a novel visual anticipation mech-anism , that leverages the noisy point clouds to render the future view image for each action candidate. As shown in Figure 1, the rendered expectations are combined with the agent’s current observations to reformulate the action space as sub-paths, which constitute the embodied basis for counterfactual reasoning, allowing the agent to prune paths that visually contradict the instructions. Through extensive experiments in both simulated and real-world environments, we demonstrate that the self-constructed noisy scenes can still serve as a reliable naviga-tion prior knowledge under the visual anticipation mecha-nism, where SpatialAnt establishes a new zero-shot state-of-the-art, achieving 66% Success Rate (SR) in R2R-CE and 50.8% SR in RxR-CE benchmarks, improving the previous best zero-shot baseline by +2% and +18.4% SR, respectively. Moreover, SpatialAnt reaches 52% SR when deployed on the Hello Robot (Kemp et al., 2022), demonstrating strong sim-to-real transfer ability for challenging real-world tasks. Our contributions are summarized below: • We propose SpatialAnt , a zero-shot VLN frame-work that seamlessly integrates active exploration, physically-grounded scene reconstruction and anticipation-guided navigation, achieving state-of-the-art performance in both simulated and real environments. • We introduce a physical grounding strategy that re-covers the physical metric for monocular RGB-based reconstructions, ensuring metric-consistent scene rep-resentations as the reliable navigation foundation. • We develop a novel visual anticipation mechanism that treats noisy scene reconstructions as verifiable visual cues, enabling zero-shot agents to perform counterfac-tual reasoning in anticipated physical space. 

2. RELATED WORK 

Vision-and-Language Navigation (VLN) requires an em-bodied agent to navigate by following natural language in-structions (Anderson et al., 2018; Qi et al., 2020). Early studies mainly focused on discrete environments (Ander-son et al., 2018; Tan et al., 2019) where the action space is abstracted as a predefined topological graph. Recent trends have shifted towards continuous environments (VLN-CE) (Krantz et al., 2020; Hong et al., 2022) to better align with real-world scenarios. Existing methods primarily adopt end-to-end learning paradigms on massive annotated datasets (Hong et al., 2021; Zhang et al., 2021; Qiao et al., 2022), and these approaches are further combined with MLLMs (Zhou et al., 2024; Zhang et al., 2024; 2025a). However, these learning-based methods are computationally expensive and generalize poorly to unseen environments. 

Zero-shot VLN To address these limitations, zero-shot VLN agents are proposed (Qiao et al., 2025; Shi et al., 2025; 2SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

He et al., 2025), leveraging the reasoning capabilities of MLLMs to facilitate generalizable navigation. The fun-damental challenge lies in providing sufficient embodied context for MLLMs to ground high-level semantic reason-ing into actionable, spatially-aware decisions (Shi et al., 2026; Li et al., 2026). To this end, Spatial-VLN (Yue et al., 2026) equips MLLM-based agents with explicit spatial at-tributes such as doors. However, the lack of persistent global structure awareness makes it struggle in long-horizon tasks. SpatialNav (Zhang et al., 2026) pushes this direction further by enabling pre-exploration and building a spatial scene graph to provide reusable global spatial priors. While effec-tive, SpatialNav is primarily based on idealized point clouds provided by annotated datasets (Chang et al., 2017). There is a substantial discrepancy between the noisy reconstructed scenes from the egocentric perception of real robots and these human-crafted surrogates. Motivated by this, we pro-pose a novel visual anticipation mechanism to bridge the reality gap. 

Scene Representations in VLN Current scene represen-tations in VLN can be broadly categorized into on-the-fly and pre-explored structures. The prior typically construct topological maps (Wang et al., 2021; Chen et al., 2022) or topometric memories (Wang et al., 2023; Zhang et al., 2025b) to expand the agent searchable action space. Re-cently, GTA (Li et al., 2026) uses an TSDF-reconstructed metric world built incrementally to decouple spatial mod-eling from semantic reasoning. However, these on-the-fly approaches suffer from the agent local line-of-sight, lacking necessary global context needed to resolve spatial ambigu-ities. In contrast, pre-exploration methods have emerged to establish a persistent global scene understanding before task execution. While VLN-Zero (Bhatt et al., 2025) caches sparse local views and SpatialNav (Zhang et al., 2026) lever-ages spatial maps to underscore the importance of global geometry, a critical gap remains: none of these works have attempted full scene reconstruction after pre-exploration. This is largely due to the practical challenges of recoding precise pose and depth of the agent during exploration. To eliminate these constraints, we propose to leverage neu-ral SLAM-based method to reconstruct dense 3D scenes from egocentric RGB streams. By incorporating a physi-cal grounding strategy, to the best of our knowledge, we are the first to establish the closed-loop from active agent exploration to physically-grounded scene reconstruction. 

3. PROBLEM FORMULATION 

In this work, we investigate the task of vision-and-language navigation in continuous environments (VLN-CE), where an embodied agent is required to navigate in a continuous 3D environment W ∈ R3, following human instructions I to reach a target destination (xd, y d, z d). Following real-world applications such as sweeping robots, we adopt a practical setting where an agent is allowed to pre-explore the envi-ronment before task execution, yielding a reconstructed 3D scene point cloud fW. Equipped with this reconstruction, the agent will receive additional signals to serve as global priors for the navigation. Specifically, at time-step t the agent state S is defined as a quadruple st = ( xt, y t, z t, θ t),where θ represents the heading of the agent. For every step, the agent perceives the environment through panoramic ob-servations by evenly rotating right at a 30 degree interval, yielding 12 RGB images O = {o1, ..., o 12 }, with each im-age o ∈ RH×W ×3. The action space A is defined as a set of continuous local waypoints predicted by a waypoint predic-tor (Hong et al., 2022), together with an explicit backtrack action as suggested by (Shi et al., 2025). Each action ak

denotes a movement (∆ x, ∆y) relative to the agent current position. Combining these local perception with additional global signals from the reconstructed scene fW, the zero-shot agents then apply MLLM as the policy model f to predict the next action. 

4. METHOD 

We propose SpatialAnt , an autonomous framework that bridges the gap between noisy reconstructions and robust navigation in real scenarios (Figure 2). Specifically, our sys-tem has three progressive stages: (1) Active scene discovery 

that gathers diverse egocentric observations through pre-exploration; (2) Physically-grounded scene reconstruction 

that consolidates these observations into a coherent 3D point cloud with real metrics; (3) Anticipation-guided navigation 

that leverages the reconstructed model for rendering future observations to conduct counterfactual reasoning. 

4.1. Active Scene Discovery 

To autonomously explore the environment and collect high-quality visual data for subsequent scene reconstruction, we introduce an active scene discovery algorithm, as summa-rized in Algorithm 1, which is decoupled into two steps: active scene exploration and visibility-aware path planning. 

Active Scene Exploration The primary objective of this step is to discover the navigable boundaries of the unseen environment. We initiate the agent from an arbitrary origin 

p0. The agent then performs frontier-based exploration us-ing a depth-first search strategy to incrementally reveal the scene structures. To keep consistent with the navigation, we utilize the waypoint predictor to generate candidate loca-tions for the agent. To manage the continuous 3D space, we discretize all positions into a grid-based navigation graph 

G = ( V, E) and apply a non-uniform voxelization to map a position p = ( x, y, z ) into a unique grid node id: the horizontal X − Y grid size ∆xy is set to 0.2m for precise obstacle representation, while the vertical Z grid size ∆z

3SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation Hello-Robot 

Stage 1: Active Scene Discovery Monocular RGB Camera Onboard IMU 

Active Exploration 

> （Frontier-based DFS ）

Visibility-Aware Path Planning    

> （2-Opt Local Search ）
> Planned Paths !!Videos "!

…

Stage 2: Physically-grounded Scene Reconstruction 

Stage 3: Anticipation-Guided Navigation     

> RGB Video "!Point Cloud #!
> $"#$%"
> SLAM3R

Regional Reconstruction Cross-modal Metric Alignment  

> Render RGB Image
> Frame_08 Frame_06
> SSIM=0.27
> Render Depth
> …
> SSIM=0.41
> Depth Anything
> Scale Factor !!
> Matched Frame
> compare

Global Pose Integration 

> Eq(2)
> Scale factor Frame Pose
> …
> Predicted Depth

Input Context 

User Instruction !

Compressed History Streams "!"

> …
> Turn right
> Go Fwd
> Go Fwd
> Omitted

Candidate Sub-Paths #!

Visual Anticipation Mechanism 

Spatial Map $!

MLLM Prompt Manager            

> (A) Turn left
> Go forward
> Move back
> (B) (C)
> … … Thought :
> -Progress Estimation :My history confirms that the bay-window corridor has already been followed …
> -Backtrack Check :Currently my path is consistent with the instruction.
> -Action Plan Update :From the current position near the staircase, take action A to move closer to the staircase.
> Action : A
> Cand Waypoints
> !!= (Δ%, Δ')
> Current position
> (% ", ' ", ) !)
> Pinhole Camera Projection
> Camera Planes Points & Colors
> 3×3 Gaussian Kernel Splatting
> Anticipated View Images

Figure 2. Overview of our proposed SpatialAnt framework. We firstly let the agent to pre-explore the environment, gathering the coordinates of navigable places. Then, we use these information to plan visibility-aware tours to collect geometrically coherent RGB videos and IMU traces for subsequent regional point cloud reconstruction. The reconstructed point clouds are then aligned with physical scale and merged into a global point cloud. Afterall, this global point cloud provides extra global context by a novel visual anticipation mechanism for MLLM to conduct sub-path based high-level reasoning. 

is set as 0.5m. For each node v ∈ V , we assign a binary attribute to distinguish between regions the agent has vis-ited and those that are merely observed as candidates. We only add edges between two physically traversable nodes. The exploration process terminates when the set of frontier nodes is exhausted. This ensures that the agent obtains a comprehensive navigation map while minimizing redundant movements. Moreover, by monitoring consecutive vertical movements and applying graph community detection (i.e. Louvain algorithm (Que et al., 2015)), we identify stairs and roughly partition the navigation graph into distinct region-level subgraphs {G 1, ..., Gn}.

Visibility-Aware Path Planning Unlike the stochastic tra-jectories obtained from active exploration, at this step we aim to collect view-continuous RGB videos with pose traces as the basis for 3D reconstruction, by planning and execut-ing visibility-aware scanning paths that ensure sufficient spatial coverage and view overlap. For each subgraph Gi,we formulate the path planning task as a weighted visibility-aware traveling salesman problem (Applegate et al., 2011). We solve the problem by first downsampling all reachable grid cells by voxel sparsification with grid size set as 1m. We then select a subset of nodes as candidate hubs and em-ploy a greedy set cover algorithm to identify a subset of hubs H that satisfies a volumetric coverage threshold τ :

| S

h∈H Vis (h) ∩ G i||G i| ≥ τ (1) where Vis (h) is the estimated visible cell set for hub h

with an estimated 3m radius. We set τ = 0 .9 to guar-antee sufficient geometric information for high-fidelity re-construction. Then we plan a path by using a 2-opt lo-cal search (Applegate et al., 2011) for an optimal Hamil-tonian cycle over H with a visibility-aware cost function 

C(u → v) = L(u, v ) + λoverlap (IoU (Vis (u) → Vis (v))) ,where L(u, v ) is the path distance between two nodes and 

λoverlap is a penalty term for insufficient visual overlap between adjacent hubs. To ensure safety in real-world de-ployment and account for the robot’s physical body size, the planned paths are refined to maintain a sufficient clearance from obstacles. The agent then traverses each path Ti with a pre-defined start heading θTi

0 using a monocular RGB cam-era to record a video Di together with the agent state traces 

Si from IMU. At each node on the path, the agent executes a “look-around” action by rotating ±60 ◦ horizontally and 

±45 ◦ vertically, resulting in geometrically consistent RGB videos for the next scene reconstruction. 

4.2. Physically-grounded Scene Reconstruction 

To transform the egocentric RGB videos and IMU traces col-lected during previous active scene discovery into a metric-consistent 3D representation fW of the real environment 4SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

Algorithm 1 Active Scene Discovery Require: Origin p0, Voxel sizes (∆ xy , ∆z ) for Navigation graph G, Coverage threshold τ , Overlap penalty λoverlap  

> 1:

Initialize: G ← ({pvisited =1 0 }, ∅), Frontiers ← { p0} 

> 2:

// Step 1: Active Scene Exploration  

> 3:

while Frontiers ̸ = ∅ do  

> 4:

pt ← DFS (Frontiers ) // Move to next place  

> 5:

pcand ← WaypointPredictor (pt) 

> 6:

G ← G ∪ ({pvisited =1  

> t

, pvisited =0  

> cand

}, {pt−1 ↔ pt}) 

> 7:

Frontiers ← Frontiers ∪ { v ∈ G , v visited =0 } 

> 8:

end while  

> 9:

Identify stairs and partition subgraphs {G 1, . . . , Gn} 

> 10:

// Step 2: Visibility-Aware Path Planning  

> 11:

for each subgraph Gi do  

> 12:

// Hub Selection via Weighted Set Cover  

> 13:

Vhub ← VoxelDownsample (Gi, grid = 1m ) 

> 14:

H ← GreedySetCover (Vhub ) using Eq.(1)  

> 15:

// Path Planning  

> 16:

Ti ← 2-opt (H) using visibility-aware cost function 

C 

> 17:

Ti ← RefineWithSafety (Ti, clearance ) 

> 18:

end for  

> 19:

return Planned paths {T 1, . . . , Tn}W, we design a physically-grounded scene reconstruction pipeline, featuring regional reconstruction, cross-modal met-ric alignment, and global pose integration. 

Regional Reconstruction To ensure robustness against lighting variations and potential pose estimation drifts dur-ing pre-exploration, we recover the local 3D point cloud 

fWlocal  

> i

∈ RK×3 for each RGB video Di by employing SLAM3R (Liu et al., 2025), a state-of-the-art neural SLAM model. For each regional point cloud, the origin is defined at the beginning robot position of the corresponding video. Although SLAM3R bypasses the requirements for precise depth maps, the downside of this advantage is that the esti-mated metric of the reconstructed point cloud is not aligned with the real physical metric. This scale ambiguity, coupled with the lack of a unified global coordinate system, prevents the point clouds from being used directly for navigation. 

Cross-modal Metric Alignment To bridge this gap, we propose a cross-modal alignment approach that anchors the relative depth of fWlocal  

> i

to an absolute metric scale. Specif-ically, for each regional point cloud fWlocal  

> i

, we render a forward-facing RGB image oif ∈ RH×W ×3 together with a depth map dif ∈ RH×W at the local origin. We iden-tify the best-matching video frame oit ∈ RH×W ×3 at the start position of path Ti by computing the average struc-tural similarity score (Wang et al., 2004) across RGB chan-nels. Then, we employ Depth-Anything-2 (Yang et al., 2024a) to predict the metric depth ˆdt ∈ RH×W of that frame. The point cloud scale factor δi is then determined by computing the average ratio of the metric depth ˆdt and ren-dered depth dif ∈ RH×W over the set of valid pixels Ω, i.e. 

δi = P 

> (j,k )∈Ω

ˆdt(j, k )/d if (j, k ) where Ω excludes pixels from reconstruction holes or depth-missing areas. By apply-ing this scale factor, we obtain the metric-aligned regional point cloud as fWmetric  

> i

= δi · fWlocal  

> i

.

Global Pose Integration To construct a unified scene, we further transform the local point clouds into a global coordinate system by utilizing the start position pTi 

> 0

and absolute heading θTi 

> 0

of path Ti, together with the relative heading θt corresponding to the matched frame. 

fWglobal  

> i

=



Rz



θTi 

> 0

+ θt

  fWmetric 

> i

⊤

+ pTi 

> 0

1⊤

⊤

(2) where Rz (·) ∈ R3×3 is the rotation matrix relative to the Z-axis, and 1 ∈ R3 is an all-ones vector. Finally, we have the reconstructed point cloud for the whole scene by merging all the local point clouds together, fW = SNi=1 fWglobal  

> i

.

4.3. Anticipation-Guided Navigation 

To enable MLLMs to reason with explicit scene knowledge, we propose a novel navigation paradigm that efficiently transforms the point clouds into MLLM-compatible for-mats. Specifically, given the following components: (1) the Instruction I, (2) the agent current position pt = ( xt, y t, z t)

and heading θt in point cloud fW coordinate system, (3) the visual Observations Ot, (4) the Action Space At, (5) the History Ht, which is a succinct record of the agent’s previ-ous actions. We define an anticipation-guided navigation paradigm that endows the MLLM agent with the ability of “foreseeing before arriving” by introducing a visual antici-pation mechanism for counterfactual reasoning via future sub-path selection. 

Visual Anticipation Mechanism Unlike generative world models that imagine future frames through stochastic syn-thesis (Wang et al., 2025), which suffer from physical hallu-cinations, our approach anticipates future perspectives via a deterministic geometric projection from our physically-grounded scene point clouds. Specifically, for each nav-igable waypoint ak = (∆ xk, ∆yk) ∈ A t, we calculate its absolute position in the point cloud coordinate system 

pak = ( xt + ∆ xk, y t + ∆ yk, z k). To calculate the expected visual observation ˆok ∈ R256 ×256 ×3 at this navigable loca-tion with anticipated heading ϕk = atan2 (∆ yk, ∆xk), we adopt a gaussian splatting algorithm (Yang et al., 2024b). For each point p ∈ fW, we firstly project it onto the tar-get image plane using the pinhole camera model. Then to 5SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

mitigate the possible noise and structural holes, we apply a 3 × 3 gaussian kernel to distribute the color and depth of each point to its neighboring pixels. To handle occlusions, only the points whose depth falls within a small tolerance ϵ

of the minimum recorded depth at a given pixel contribute to the final color through a distance-weighted accumulation. 

Counterfactual Reasoning via Sub-Path Selection In-stead of most previous VLN agents that take the navigation as the next waypoint select problem (Qiao et al., 2025; Shi et al., 2025; Li et al., 2026), we formulate the navigation as a sub-path selection problem. Specifically, at each step we provide the MLLM with: • Compressed History Streams ( HVt ): To maintain the path coherence, we represent the historical trajectory as a temporal stream of visual observations, connected by the actions taken. As the raw history sequences have much redundancy, particularly related to long-range forward movements, we therefore introduce a simple compression strategy by merging the intermediate steps for consecutive forward actions. We only retain the key observations that mark the beginning and end of the forward sequence. The remaining history streams are further resized to 256 × 256 pixel size. This strategy allows the MLLM to perceive the history as a series of coherent events without overwhelming the context. • Anticipated Navigable Sub-Paths ( Ak): We augment each candidate action ak with a projected future view 

ˆok by using our visual anticipation mechanism. By presenting these anticipated views alongside the cur-rent observation, we transform the action decision into a counterfactual visual alignment task by asking the MLLM to select the sub-path (together with the his-tory) that best aligns with the instruction. Specifically, each candidate action has the form: “[observed view 

ok] [action ak] → if I took this action, I would partially imagine the next view: [anticipated view ˆok]”. This form allows the MLLM to perform semantic cross-verification on the physical reality of potential future observation, hence reducing the error backtracking. • Spatial Map ( Mt): Following (Zhang et al., 2026), we additionally provide a top-down topological represen-tation to ground the agent location in a global spatial context. The map is generated by projecting the recon-structed 3D point cloud fW onto a 2D occupancy grid. To provide the MLLM with a coherent understanding of the navigation progress, we also visualize the histor-ical trajectory and the candidate waypoints together on the map. Combining these components with the user instruction I, the MLLM (specifically, GPT-5.1-2025-11-13) acts as a high-level spatial-temporal policy that evaluates the semantic consistency of each sub-path candidate Ak:

A∗ = f  I, H Vt , Ok, {Ak|ak ∈ A t}, M t

 (3) After selecting the best A∗, the agent executes the corre-sponding action ak and moves to the next place. 

5. EXPERIMENTS 

5.1. Experiment Setup Simulation Benchmarks We evaluate our method in the Habitat simulator (Puig et al., 2023) using the R2R-CE (Krantz et al., 2020) and RxR-CE datasets (Ku et al., 2020). These datasets provide complex and diverse instruc-tions in the continuous environment spanning 11 distinct scans. To align with established evaluation protocols (Qiao et al., 2025; Shi et al., 2025; Zhang et al., 2026), we ran-domly sample 100 and 195 episodes from the validation unseen split of each dataset respectively for computationally intensive zero-shot MLLM agents. To keep consistent with previous works, we adopt the simulator-rendered metric depth for waypoint predictor (Shi et al., 2025). 

Real-World Deployment For real-world validation, we deploy our model on a Hello Robot platform (Kemp et al., 2022). The robot is equipped with an Intel RealSense D435if RGB camera for visual perception and a 9-DoF IMU. We adopt Depth-Anything-2(Yang et al., 2024a) to produce the metric depth used by waypoint predictor. All onboard inference is executed on a single NVIDIA RTX A6000 GPU. For fair comparison, we follow the experimen-tal protocols defined in (Shi et al., 2025; 2026) to construct a real-world dataset consisting of 25 cross-room paths fea-turing open-vocabulary targets, such as a “red ladder”. As introduced in Section 4.1 and Section 4.2, we leverage the robot’s onboard sensors to pre-explore and reconstruct the environment before navigation. This setup evaluates the transition of our approach from high-fidelity simulation to unstructured real-world scenarios. 

Evaluation Metrics Following the standard protocols for VLN-CE (Hong et al., 2022), we evaluate our model using the following metrics: Success Rate (SR), Success weighted by Path Length (SPL), Navigation Error (NE), Normalized Dynamic Time Warping (nDTW), and Trajectory Length (TL). Following previous approaches in (Qiao et al., 2025; Shi et al., 2025; He et al., 2025), an episode is considered successful if the robot stops within a distance threshold 

dth from the goal, where dth = 3 .0m in simulation and 

dth = 2 .0m for real-world experiments to reflect the higher precision required in physical environments. 

Baselines We evaluate SpatialAnt against two categories of competitive methods: (1) Supervised Learning ap-proaches, including current state-of-the-art NavFoM (Zhang 6SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

Table 1. Comparison in continuous environments on R2R-CE and RxR-CE Val-Unseen splits. The best supervised results are highlighted in bold , while the best zero-shot results are underlined . “Pre-Exp” denotes whether the zero-shot agent adopts the pre-exploration based navigation settings.                                                                                                     

> #Methods Pre-Exp R2R-CE RxR-CE NE (↓)OSR (↑)SR (↑)SPL (↑)nDTW (↑)NE (↓)SR (↑)SPL (↑)nDTW (↑)
> Supervised Learning:
> 1NavFoM (Zhang et al., 2025a) –4.61 72.1 61.7 55.3 –4.74 64.4 56.2 65.8 2Efficient-VLN (Zheng et al., 2025) –4.18 73.7 64.2 55.9 –3.88 67.0 54.3 68.4
> Zero-Shot:
> 3Open-Nav (Qiao et al., 2025) ×6.70 23.0 19.0 16.1 45.8 ––––4Smartway (Shi et al., 2025) ×7.01 51.0 29.0 22.5 –––––5STRIDER (He et al., 2025) ×6.91 39.0 35.0 30.3 51.8 11.19 21.2 9.6 30.1 6VLN-Zero (Bhatt et al., 2025) ✓5.97 51.6 42.4 26.3 –9.13 30.8 19.0 –7SpatialNav (Zhang et al., 2026) ✓5.15 66.0 64.0 51.1 65.4 7.64 32.4 24.6 55.0 8SpatialAnt (Ours) ✓4.42 76.0 66.0 54.4 69.5 5.28 50.8 35.6 65.4

Table 2. Comparison of backtrack times on the successful tasks. 

We report the results by agent-reconstructed scene point clouds on sampled val-unseen splits of R2R-CE.             

> Methods #Backtrack #Steps( ↓)TL( ↓)SPL( ↑)
> SpatialNav (Zhang et al., 2026) 8 / 51 (15.7%) 8.60 10.77 77.9
> SpatialAnt (Ours) 8 / 66 ( 12.1% )8.26 10.48 84.9

et al., 2025a) and Efficient-VLN (Zheng et al., 2025), and (2) 

Zero-Shot methods, spanning exploration-free navigators (including Open-Nav (Qiao et al., 2025), Smartway (Shi et al., 2025) and STRIDER (He et al., 2025)) and pre-exploration based approaches (including VLN-Zero (Bhatt et al., 2025) and SpatialNav (Zhang et al., 2026)). Our Spa-tialAnt is directly comparable with the latter two methods under a shared pre-exploration setting. 

5.2. Main Results 

Table 1 provides a quantitative comparison of our proposed SpatialAnt with state-of-the-art (SOTA) methods in R2R-CE and RxR-CE datasets. The results demonstrate a clear trend that pre-exploration based agents significantly outper-form normal on-the-fly zero-shot agents, highlighting the necessity of explicitly modeling the global spatial knowl-edge. On R2R-CE, SpatialAnt reaches the supervised-level perfor-mance with a 66% SR, slightly exceeding the strongest su-pervised baselines Efficient-VLN (Zheng et al., 2025). This indicates that the presence of global scene priors reduces the dependence on in-domain training to achieve SOTA perfor-mance. Moreover, SpatialAnt improves significantly over the previous SOTA zero-shot baseline, SpatialNav (Zhang et al., 2026), by raising the OSR from 66% to 76% while increasing the SPL and nDTW to 54.4% and 69.5% respec-tively. This efficiency gain is further supported by Table 2, SpatialAnt executes 3.6% less backtracking than Spatial-Nav to succeed, indicating fewer erroneous moves and more faithful instruction following. On the more challenging RxR-CE, where previous methods 

Table 3. Comparison between the human-curated (H) and agent-reconstructed (A) scene point clouds. Methods Scene R2R-CE RxR-CE OSR( ↑) SR( ↑) SPL( ↑) OSR( ↑) SR( ↑) SPL( ↑)                                      

> SpatialNav (Zhang et al., 2026) H66.0 64.0 51.1 47.5 32.4 24.1 A61.0 51.0 39.8 40.3 26.1 19.3
> Performance gap -5.0 -13.0 -11.3 -7.2 -6.3 -4.8
> SpatialAnt H80.0 69.0 52.7 69.2 53.9 37.6 A76.0 66.0 54.4 63.1 50.8 35.6
> Performance gap -4.0 -3.0 -1.7 -6.1 -2.1 -2.0

suffer severe performance degradation, SpatialAnt main-tains excellent generalization with 50.8% SR and 35.6% SPL, significantly surpassing prior zero-shot methods by +18.4% SR and +11.0% SPL. Such improvement indicates that our anticipation-based sub-path selection provides a robust reasoning basis for MLLM against the complex cross-room instructions, thereby enabling high path efficiency. 

5.3. Ablation Study 

We conduct comprehensive ablation studies to validate the robustness and effectiveness of our framework. 

Robustness to Sim-to-Real Transfer To investigate how reconstruction noise affects exploration-based zero-shot agents, we compare SpatialNav (Zhang et al., 2026) and our SpatialAnt using: • Human-curated (H) high-quality point clouds provided by Matterport3D dataset (Chang et al., 2017), which is an idealized pre-exploration assumption taken by SpatialNav that perfect scene point clouds are directly available, • Agent-constructed (A) point clouds using our methods. As presented in Table 3, the performance of SpatialNav drops significantly (-13.0% SR on R2R-CE and -6.3%SR on RxR-CE) when switching to noisy reconstructed point clouds, indicating that using spatial-map only as an abso-lute reference yields poor robustness to real scenarios. In contrast, SpatialAnt exhibits much smaller degradation with 7SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

Table 4. Ablation study on visual anticipation mechanism (VA) and its horizon field of view (HFOV). We report the results on sampled val-unseen splits of R2R-CE.                             

> Methods HFOV NE( ↓)OSR( ↑)SR( ↑)SPL( ↑)nDTW( ↑)
> SpatialAnt (w/o VA) -5.19 77.0 55.0 41.4 70.0 SpatialAnt (w/ VA) 60 ◦4.33 72.0 65.0 55.2 69.2 90 ◦4.42 76.0 66.0 54.4 69.5
> 120 ◦4.85 74.0 59.0 48.7 67.7 Qwen3.5-Plus
> Gemini-2.5-Pro
> GPT-5.1
> 50
> 60
> 70
> 80
> OSR
> Qwen3.5-Plus
> Gemini-2.5-Pro
> GPT-5.1
> 40
> 50
> 60
> SR
> Qwen3.5-Plus
> Gemini-2.5-Pro
> GPT-5.1
> 30
> 40
> 50
> SPL
> SpatialAnt (w/o VA) SpatialAnt (w/ VA)

Figure 3. Comparison across different MLLM backbones. We report the results on sampled subset of R2R-CE. The default HFOV of visual anticipation (VA) is set as 90 ◦.

a -3.0 % SR on R2R-CE and a -2.1% on RxR-CE), because our SpatialAnt treats the reconstructed geometry as a source of counterfactual visual evidence by constructing verifiable sub-paths, thereby effectively narrowing the sim-to-real gap. 

Effectiveness of Visual Anticipation Mechanism To ver-ify the necessity and impact of visual anticipation (VA), we compare the performance of our agent with and without this mechanism. Table 4 demonstrates that incorporating visual anticipation consistently improves most evaluation metrics, confirming that VA provides crucial guidance for action disambiguation. We further ablate the anticipation horizon field of view (HFOV). Results illustrate that HFOV set as 90 ◦ achieves the best overall trade-off, offering richer look-ahead evidence that 60 ◦ HFOV, while avoiding the increased uncertainty introduced by a wider 120 ◦ HFOV. 

Scalability with MLLM Reasoning Abilities We vali-date the model-agnostic property of our framework by in-stantiating SpatialAnt with multiple MLLM backbones. As shown in Figure 3, we can have two key insights. Firstly, the effectiveness of our visual anticipation mechanism is model-agnostic. Secondly, the improvements on SpatialAnt become more pronounced as the reasoning capability of the backbone increases, showing that stronger MLLMs are better at leveraging the counterfactual evidence provided by anticipated observations to disambiguate actions and avoid instruction-inconsistent paths. These results suggest that SpatialAnt scales favorably with future MLLM advances, translating improved reasoning capacity into navigation gains. 

5.4. Real-world Deployment 

To validate the practical effectiveness of our framework, we deploy SpatialAnt in the real environment with Hello-

Table 5. Real-world navigation performance. We compare Spa-tialAnt against representative supervised-learning (VLN-BERT) and zero-shot (SmartWay) baselines.                  

> Method LP Time (s) GP Time (s) SR (↑, %) NE (↓, m)
> Supervised Learning:
> VLN-BERT (Hong et al., 2021) 22.40 -20.0 3.92
> Zero-Shot:
> SmartWay (Shi et al., 2025) 22.40 -32.0 3.01
> SpatialAnt (Ours) 22.40 0.57 52.0 2.49

Robot and conduct pre-exploration. The video collection following the planned path finished within 10 minutes per region, resulting in around 1000 frames. The scene recon-struction then took about 200 seconds on a single NVIDIA RTX A6000 GPU. Besides the navigation metrics, we also report the per-step local perception (LP) time that used to collect real visual observations, and the per-step global perception (GP) time that used to project spatial map and gather visual anticipations from the point cloud. As shown in Table 5, our method achieves significantly higher success rate along with the reduced navigation error, indicating that our pre-explored, physically-grounded scene priors provide actionable guidance in real scenarios. Importantly, with the reconstructed global point cloud (which is a one-time cost), the global perception is very efficient, demonstrating that the additional computation introduced by our frame-work is lightweight and well within practical deployment constraints. 

5.5. Case Study 

We present two qualitative examples in Figure 4 to demon-strate how SpatialAnt benefits from the global priors ob-tained by pre-exploration. As shown in Figure 4a, at the be-ginning, the candidate waypoints all seem plausible for the “turn around” instruction. In this case, SpatialAnt queried the pre-explored point cloud to render anticipated obser-vations, resulting in semantically different sub-paths, and selected the one that best supports the subsequent instruction “go forward towards the door”. The example in Figure 4b further shows SpatialAnt’s ability to recover from an early mistake: after the agent passed the dual-arm robot (because the waypoint predictor fails to propose a proper candidate), our rendered sub-paths provide counterfactual evidence that highlights the dual-arm robot and triggers corrective action to re-align with the instruction. Taken together, these cases illustrate that the practical value of our framework extends beyond mere increased performance, but to equip the agent with complex reasoning skills in anticipated physical space. 

6. CONCLUSION 

In this work, we present SpatialAnt , a zero-shot VLN framework that closed-loop the scene exploration to scene reconstruction, finalizing the real robotic navigation. By actively exploring the environment with a single RGB 8SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation Turn around and exit the corner. Go forward towards the door. Stop next to a black robot man (with silver T-shirt) lying on a small chair.   

> Spatial Map
> Candidate Sub-Paths
> Turn Right 135 °
> Turn Right 150 °
> Turn Left 170 °
> Anticipated Right View
> Anticipated Right View
> Anticipated Left View
> Panoramic Observations (clockwise)

# ✓ 

> Panoramic Observations (clockwise)
> Front View (t=0) Front View (t=1)

(a) Example: Disambiguate at the beginning. Front View (t=4) 

> Panoramic Observations (clockwise)
> Spatial Map
> Candidate Sub-Paths
> Front View (t=5)
> Turn Left
> Go Forward
> Turn Right
> Anticipated Left View
> Anticipated Front View
> Anticipated Right View

# ✓             

> Panoramic Observations (clockwise)
> Go forward, walk across the chairs, turn right and stop at dual arm robot .

(b) Example: Amend at midway. 

Figure 4. Representative cases in real environments. We show frames of the agent egocentric front view, panoramic observations (from right to left), and candidate actions on the spatial map to-gether with our anticipated views. 

camera, SpatialAnt introduces a physical grounding strat-egy that resolves monocular scale ambiguity to produce metric-consistent point clouds. Rather than treating the self-constructed point clouds only as reference maps, we propose a visual anticipation mechanism that renders future observations from this noisy geometry and supports coun-terfactual reasoning to validate candidate sub-paths against instructions. Extensive results on R2R-CE and RxR-CE, together with real-world deployment on the Hello Robot, demonstrate superior zero-shot performance under imper-fect scene reconstructions, highlighting that reliable naviga-tion depends on how the world is grounded and verified. 

References 

Anderson, P., Wu, Q., Teney, D., Bruce, J., Johnson, M., Sünderhauf, N., Reid, I., Gould, S., and Van Den Hengel, A. Vision-and-language navigation: Interpreting visually-grounded navigation instructions in real environments. In 

CVPR , pp. 3674–3683, 2018. Applegate, D. L., Bixby, R. E., Chvátal, V., and Cook, W. J. The traveling salesman problem: a computational study. 2011. Bhatt, N. P., Yang, Y., et al. Vln-zero: Rapid exploration and cache-enabled neurosymbolic vision-language planning for zero-shot transfer in robot navigation. arXiv preprint arXiv:2509.18592 , 2025. Chang, A., Dai, A., Funkhouser, T., Halber, M., Niebner, M., Savva, M., Song, S., Zeng, A., and Zhang, Y. Matter-port3d: Learning from rgb-d data in indoor environments. In 3DV , pp. 667–676. IEEE, 2017. Chen, S., Guhur, P.-L., Tapaswi, M., Schmid, C., and Laptev, I. Think global, act local: Dual-scale graph transformer for vision-and-language navigation. In CVPR , pp. 16537– 16547, 2022. Comanici, G., Bieber, E., Schaekermann, M., Pasupat, I., Sachdeva, N., Dhillon, I., Blistein, M., et al. Gemini 2.5: Pushing the frontier with advanced reasoning, multi-modality, long context, and next generation agentic capa-bilities. arXiv preprint arXiv:2507.06261 , 2025. Curless, B. and Levoy, M. A volumetric method for building complex models from range images. In SIGGRAPH , pp. 303–312, 1996. Gu, J., Stefani, E., Wu, Q., Thomason, J., and Wang, X. Vision-and-language navigation: A survey of tasks, meth-ods, and future directions. In ACL , pp. 7606–7623, 2022. He, D., Gao, X., Li, H., et al. Strider: Navigation via instruction-aligned structural decision space optimization. 

NeurIPS , 2025. Hong, Y., Wu, Q., Qi, Y., et al. Vln-bert: A recurrent vision-and-language bert for navigation. In CVPR , pp. 1643–1653, 2021. Hong, Y., Wang, Z., Wu, Q., and Gould, S. Bridging the gap between learning in discrete and continuous environments for vision-and-language navigation. In CVPR , pp. 15439– 15449, 2022. Kemp, C. C., Edsinger, A., Clever, H. M., and Matulevich, B. The design of stretch: A compact, lightweight mobile manipulator for indoor human environments. In ICRA ,pp. 3150–3157. IEEE, 2022. Krantz, J., Wijmans, E., Majumdar, A., Batra, D., and Lee, S. Beyond the nav-graph: Vision-and-language navigation in continuous environments. In ECCV , pp. 104–120. Springer, 2020. Ku, A., Anderson, P., Patel, R., Ie, E., and Baldridge, J. Room-across-room: Multilingual vision-and-language navigation with dense spatiotemporal grounding. In 

EMNLP , pp. 4392–4412, 2020. 9SpatialAnt: Autonomous Zero-Shot Robot Navigation via Active Scene Reconstruction and Visual Anticipation 

Li, Z., Zheng, H., Zhao, F., Chan, A., Zhou, J., et al. One agent to guide them all: Empowering mllms for vision-and-language navigation via explicit world representation. 

arXiv preprint arXiv:2602.15400 , 2026. Liu, Y., Dong, S., Wang, S., Yin, Y., Yang, Y., Fan, Q., and Chen, B. Slam3r: Real-time dense scene reconstruction from monocular rgb videos. In CVPR , pp. 16651–16662, 2025. Puig, X., Undersander, E., Szot, A., Cote, M. D., Yang, T.-Y., Partsey, R., Desai, R., et al. Habitat 3.0: A co-habitat for humans, avatars and robots. arXiv preprint arXiv:2310.13724 , 2023. Qi, Y., Wu, Q., Anderson, P., Wang, X., Wang, W. Y., Shen, C., and Hengel, A. v. d. Reverie: Remote embodied visual referring expression in real indoor environments. In CVPR , pp. 9982–9991, 2020. Qiao, Y., Qi, Y., Hong, Y., Yu, Z., Wang, P., and Wu, Q. Hop: History-and-order aware pre-training for vision-and-language navigation. In CVPR , pp. 15418–15427, 2022. Qiao, Y., Lyu, W., Wang, H., et al. Open-nav: Exploring zero-shot vision-and-language navigation in continuous environment with open-source llms. In ICRA , pp. 6710– 6717. IEEE, 2025. Que, X., Checconi, F., Petrini, F., et al. Scalable community detection with the louvain algorithm. In IPDPS , pp. 28– 37. IEEE, 2015. Shi, X., Li, Z., et al. Smartway: Enhanced waypoint predic-tion and backtracking for zero-shot vision-and-language navigation. In IROS , pp. 16923–16930. IEEE, 2025. Shi, X., Li, Z., Qiao, Y., and Wu, Q. Fast-smartway: Panoramic-free end-to-end zero-shot vision-and-language navigation. ICRA , 2026. Singh, A. K., Fry, A., et al. Openai gpt-5 system card, 2025. URL https://arxiv.org/abs/2601.03267 .Tan, H., Yu, L., and Bansal, M. Learning to navigate un-seen environments: Back translation with environmental dropout. In NAACL-HLT , pp. 2610–2621, 2019. Wang, H., Wang, W., Liang, W., et al. Structured scene memory for vision-language navigation. In CVPR , pp. 8455–8464, 2021. Wang, S., Leroy, V., Cabon, Y., Chidlovskii, B., and Revaud, J. Dust3r: Geometric 3d vision made easy. In CVPR , pp. 20697–20709, 2024. Wang, T., Zhao, X., Cai, W., and Sun, C. Imagine-nav++: Prompting vision-language models as embod-ied navigator through scene imagination. arXiv preprint arXiv:2512.17435 , 2025. Wang, Z., Bovik, A. C., Sheikh, H. R., et al. Image quality assessment: from error visibility to structural similarity. 

TIP , 2004. Wang, Z., Li, X., Yang, J., et al. Gridmm: Grid memory map for vision-and-language navigation. In CVPR , pp. 15625–15636, 2023. Yang, L., Kang, B., Huang, Z., Zhao, Z., Xu, X., Feng, J., and Zhao, H. Depth anything v2. NeurIPS , 37:21875– 21911, 2024a. Yang, Z., Gao, X., Sun, Y.-T., Huang, Y.-H., Lyu, X., Zhou, W., Jiao, S., Qi, X., and Jin, X. Spec-gaussian: anisotropic view-dependent appearance for 3d gaussian splatting. In 

NeurIPS , pp. 61192–61216, 2024b. Yue, L., Fan, Y., Lian, S., Zhao, Y., et al. Spatial-vln: Zero-shot vision-and-language navigation with ex-plicit spatial perception and exploration. arXiv preprint arXiv:2601.12766 , 2026. Zhang, J., Fan, J., Peng, J., and Wei, Z. Curriculum learning for vision-and-language navigation. NeurIPS , 34:13328– 13339, 2021. Zhang, J., Wang, K., Xu, R., Zhou, G., et al. Navid: Video-based vlm plans the next step for vision-and-language navigation. RSS , 2024. Zhang, J., Li, A., Qi, Y., Li, M., Liu, J., Wang, S., et al. Embodied navigation foundation model. arXiv preprint arXiv:2509.12129 , 2025a. Zhang, J., Li, Z., Wang, S., Shi, X., Wei, Z., and Wu, Q. Spatialnav: Leveraging spatial scene graphs for zero-shot vision-and-language navigation. arXiv preprint arXiv:2601.06806 , 2026. Zhang, L., Hao, X., Xu, Q., Zhang, Q., et al. Mapnav: A novel memory representation via annotated semantic maps for vlm-based vision-and-language navigation. In 

ACL , pp. 13032–13056, 2025b. Zheng, D., Huang, S., et al. Efficient-vln: A training-efficient vision-language navigation model. arXiv preprint arXiv:2512.10310 , 2025. Zhou, G., Hong, Y., Wang, Z., Wang, X. E., and Wu, Q. Navgpt-2: Unleashing navigational reasoning capability for large vision-language models. In ECCV , pp. 260–278. Springer, 2024. 10