---
title: Can Vision Foundation Models Navigate? Zero-Shot Real-World Evaluation and Lessons Learned
title_zh: 视觉基础模型能导航吗？零样本真实世界评估与经验教训
authors: "Maeva Guerrier, Karthik Soma, Jana Pavlasek, Giovanni Beltrame"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25937v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 视觉导航基础模型的真实世界评估
tldr: 本研究对五种先进视觉导航模型（VNM）在真实世界中的零样本性能进行了系统评估。通过在多种机器人平台和室内外环境下的测试，研究不仅关注任务成功率，还引入了路径质量、目标识别及鲁棒性等多维度指标。结果揭示了现有模型在几何理解、感知去混淆及应对环境变化方面的系统性缺陷，并提供了开源评估工具以促进该领域的基准测试。
selection_source: fresh_fetch
motivation: 现有的视觉导航模型评估过于依赖单一的成功率指标，无法全面反映模型在轨迹质量、碰撞规避及环境适应性方面的真实表现。
method: 在两个机器人平台和五个室内外场景中，对GNM、ViNT等五种主流模型进行实地测试，并结合图像扰动分析其鲁棒性。
result: 研究发现现有模型普遍存在几何理解不足导致的频繁碰撞、感知相似场景下的目标识别错误以及在分布偏移下的性能大幅下降。
conclusion: 视觉导航模型在处理复杂几何和语义环境时仍面临巨大挑战，未来研究需超越简单成功率，关注更深层的环境理解与鲁棒性。
---

## 摘要
视觉导航模型 (VNM) 通过从大规模视觉演示中学习，有望实现可泛化的机器人导航。尽管在真实世界中的部署日益增多，但现有的评估几乎完全依赖于成功率（即机器人是否到达目标点），这掩盖了轨迹质量、碰撞行为以及对环境变化的鲁棒性。我们在两个机器人平台和涵盖室内外设置的五个环境中，对五种最先进的 VNM（GNM、ViNT、NoMaD、NaviBridger 和 CrossFormer）进行了真实世界评估。除了成功率之外，我们还将基于路径的指标与基于视觉的目标识别评分相结合，并通过受控的图像扰动（运动模糊、太阳耀斑）来评估鲁棒性。我们的分析揭示了三个系统性局限性：(a) 即使是架构复杂的基于扩散模型和 Transformer 的模型也会频繁发生碰撞，表明其几何理解能力有限；(b) 模型无法区分感知上相似的不同位置，尽管存在一些语义差异，这导致在重复性环境中出现目标预测错误；(c) 性能在分布偏移下会下降。我们将公开发布我们的评估代码库和数据集，以促进 VNM 的可重复基准测试。

## Abstract
Visual Navigation Models (VNMs) promise generalizable, robot navigation by learning from large-scale visual demonstrations. Despite growing real-world deployment, existing evaluations rely almost exclusively on success rate, whether the robot reaches its goal, which conceals trajectory quality, collision behavior, and robustness to environmental change. We present a real-world evaluation of five state-of-the-art VNMs (GNM, ViNT, NoMaD, NaviBridger, and CrossFormer) across two robot platforms and five environments spanning indoor and outdoor settings. Beyond success rate, we combine path-based metrics with vision-based goal-recognition scores and assess robustness through controlled image perturbations (motion blur, sunflare). Our analysis uncovers three systematic limitations: (a) even architecturally sophisticated diffusion and transformer-based models exhibit frequent collisions, indicating limited geometric understanding; (b) models fail to discriminate between different locations that are perceptually similar, however some semantics differences are present, causing goal prediction errors in repetitive environments; and (c) performance degrades under distribution shift. We will publicly release our evaluation codebase and dataset to facilitate reproducible benchmarking of VNMs.

---

## 论文详细总结（自动生成）

## 视觉基础模型能导航吗？零样本真实世界评估与经验教训

### 1. 核心问题与整体含义
*   **研究动机**：视觉导航模型（VNMs）旨在通过大规模机器人数据集的学习，实现无需预建地图、仅凭视觉图像的通用导航。然而，目前的评估指标过于单一，通常仅依赖“成功率”（Success Rate），这掩盖了模型在轨迹质量、碰撞规避、几何理解以及对环境变化鲁棒性方面的真实表现。
*   **核心问题**：在真实世界的复杂场景中，这些所谓的“视觉导航基础模型”是否真的具备可靠的导航能力？它们的失败模式是什么？架构的复杂性（如 Transformer 或扩散模型）是否转化为了更好的性能？

### 2. 方法论
*   **核心思想**：通过在多种机器人平台和异构环境中部署五种最先进的 VNM，利用路径质量、视觉感知和鲁棒性等多维度指标进行系统性审计。
*   **关键技术细节**：
    *   **模型输入输出**：模型 $f_\theta$ 接收历史观测图像序列 $O_t$ 和目标图像 $o_g$，输出动作 $\hat{A}_t$（单个航点或轨迹）及到目标的估计时间距离 $d$。
    *   **架构组成**：通常包含图像编码器（提取特征）、距离编码器（计算目标关联）、主干网络（如 Transformer 或扩散模型）和动作解码器。
    *   **拓扑映射导航**：在长程任务中，利用预采集的图像节点构建拓扑图，通过图搜索确定子目标序列，引导机器人逐步到达最终目标。
    *   **视觉扰动测试**：在拓扑图中引入运动模糊（Motion Blur）和太阳耀斑（Sunflare）等人工扰动，测试模型在非理想视觉条件下的零样本泛化能力。

### 3. 实验设计
*   **实验场景**：涵盖5个真实环境，包括室内（走廊、楼梯、办公室环路、带多个相似门洞的竞技场）和室外（雪地停车场，属于分布外 OOD 场景）。
*   **机器人平台**：Spot（四足机器人）和 Bunker（履带式移动机器人）。
*   **对比方法**：评估了五种主流模型：**GNM**（基于全连接层）、**ViNT**（基于 Transformer）、**NoMaD**（基于扩散模型）、**NaviBridger**（基于扩散桥）和 **CrossFormer**（跨具身 Transformer）。
*   **Benchmark**：以人工驾驶生成的参考轨迹作为基准，对比模型自主生成的路径。

### 4. 资源与算力
*   **部署算力**：模型部署在机器人车载计算单元 **Nvidia AGX Xavier** 和 **Orin** 上。
*   **优化手段**：使用了 **ONNX** 导出和优化技术，以实现高效的实时推理。
*   **训练数据规模**：文中提到 GNM 训练了约 70 小时数据，ViNT 约 80 小时，但未详细列出训练这些模型所消耗的具体 GPU 集群算力和训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：在5个环境下对5种模型进行了多次部署试验。例如，在办公室环路中进行了10次试验，在走廊和雪地等场景中进行了3-5次重复试验。
*   **充分性与客观性**：
    *   实验设计较为全面，涵盖了从简单直线到复杂环路、从室内语义环境到室外极端天气（雪地）的跨度。
    *   引入了 **LPIPS、DSSIM、PSNR** 等视觉度量指标，以及**拓扑节点误差**和**碰撞率**，比单纯的成功率更客观地揭示了模型的局限性。
    *   **局限性**：受限于真实世界测试的成本，单项实验的重复次数（3-10次）相比仿真环境较少，统计显著性略受限。

### 6. 主要结论与发现
*   **几何理解匮乏**：即使是架构最复杂的扩散模型和 Transformer 模型，在走廊和办公室中也频繁发生碰撞，表明纯视觉学习难以获得稳健的空间几何推理能力。
*   **语义去混淆困难**：在具有重复特征的环境（如竞技场中多个相似的门）中，模型容易产生“感知混淆”，导致过早预测到达目标或偏离航线。
*   **架构复杂性不等于鲁棒性**：简单的 GNM（MobileNetV2 架构）在某些指标上（如视觉相似度、雪地泛化）甚至优于更复杂的模型，暗示训练数据的质量和多样性比架构更关键。
*   **分布偏移敏感**：所有模型在面对雪地或视觉扰动（模糊、强光）时，性能均出现不同程度的下降，扩散模型在 OOD 场景下表现尤为脆弱。

### 7. 优点
*   **多维度评估体系**：提出了一套超越“成功率”的评估框架，为视觉导航领域提供了更细致的性能度量标准。
*   **真实世界导向**：完全基于真实机器人和真实场景，揭示了仿真环境难以发现的“碰撞”和“感知混淆”问题。
*   **开源贡献**：承诺发布评估代码库和数据集，有助于社区进行可重复的基准测试。

### 8. 不足与局限
*   **样本量限制**：真实世界试验次数有限，可能存在偶发性因素影响。
*   **缺乏动态交互**：实验主要在静态环境中进行，未评估模型在处理行人、车辆等动态障碍物时的“社交导航”能力。
*   **避障机制干扰**：在 Spot 机器人上的测试依赖了其自带的底层避障，这在一定程度上掩盖了 VNM 模型本身的原始碰撞倾向。
*   **数据瓶颈**：研究指出模型性能受限于训练数据的规模和多样性，而非仅仅是算法架构。

（完）
