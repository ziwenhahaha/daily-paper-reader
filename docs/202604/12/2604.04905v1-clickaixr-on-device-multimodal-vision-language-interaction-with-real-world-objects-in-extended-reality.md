---
title: "ClickAIXR: On-Device Multimodal Vision-Language Interaction with Real-World Objects in Extended Reality"
title_zh: ClickAIXR：扩展现实中与现实世界物体的设备端多模态视觉-语言交互
authors: "Dawar Khan, Alexandre Kouyoumdjian, Xinyu Liu, Omar Mena, Dominik Engel, Ivan Viola"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04905v1"
tags: ["query:q13"]
score: 8.0
evidence: 与真实世界物体的端侧多模态视觉语言交互
tldr: 本研究提出ClickAIXR，一个在扩展现实（XR）中实现端侧多模态视觉语言交互的框架。它结合了基于控制器的物体选择范式与本地视觉语言模型（VLM），允许用户通过点击现实物体进行提问并获得语音或文本回复。该系统解决了云端AI的隐私与延迟问题，并通过精确选择减少了交互歧义，显著提升了XR交互的透明度与信任感。
selection_source: carryover_cache
motivation: 旨在解决现有XR交互中云端AI带来的隐私风险、高延迟以及注视点选择方式存在的歧义性问题。
method: 开发了基于Magic Leap SDK的端侧框架，利用ONNX进行本地VLM推理，并采用控制器点击方式实现精确的物体选择与多模态问答。
result: 实验显示该系统在延迟适中的情况下提供了良好的可用性与用户满意度，且在隐私保护和交互透明度上表现出色。
conclusion: ClickAIXR证明了结合点击式物体选择与端侧AI是实现可信、隐私保护且高效XR交互的有效途径。
---

## 摘要
我们提出了 ClickAIXR，这是一个用于在扩展现实（XR）中与物体进行多模态视觉-语言交互的新型设备端框架。与以往依赖云端 AI（如 ChatGPT）或基于注视的选择（如 GazePointAR）的系统不同，ClickAIXR 将设备端视觉-语言模型（VLM）与基于控制器的物体选择范式相结合，使用户能够在 XR 中精确点击现实世界中的物体。一旦选中，物体图像将由 VLM 在本地处理，通过文本和语音回答自然语言问题。这种以物体为中心的交互减少了仅靠注视或语音界面固有的歧义，并通过在设备端执行所有推理提高了透明度，解决了隐私和延迟方面的担忧。我们在 Magic Leap SDK (C API) 中实现了 ClickAIXR，并采用了基于 ONNX 的本地 VLM 推理。我们进行了一项用户研究，将 ClickAIXR 与 Gemini 2.5 Flash 和 ChatGPT 5 进行了对比，评估了可用性、信任度和用户满意度。结果显示，延迟处于中等水平，用户体验是可以接受的。我们的研究结果证明了基于点击的物体选择与设备端 AI 相结合在推进可信、隐私保护的 XR 交互方面的潜力。源代码和补充材料可在 nanovis.org/ClickAIXR.html 获取。

## Abstract
We present ClickAIXR, a novel on-device framework for multimodal vision-language interaction with objects in extended reality (XR). Unlike prior systems that rely on cloud-based AI (e.g., ChatGPT) or gaze-based selection (e.g., GazePointAR), ClickAIXR integrates an on-device vision-language model (VLM) with a controller-based object selection paradigm, enabling users to precisely click on real-world objects in XR. Once selected, the object image is processed locally by the VLM to answer natural language questions through both text and speech. This object-centered interaction reduces ambiguity inherent in gaze- or voice-only interfaces and improves transparency by performing all inference on-device, addressing concerns around privacy and latency. We implemented ClickAIXR in the Magic Leap SDK (C API) with ONNX-based local VLM inference. We conducted a user study comparing ClickAIXR with Gemini 2.5 Flash and ChatGPT 5, evaluating usability, trust, and user satisfaction. Results show that latency is moderate and user experience is acceptable. Our findings demonstrate the potential of click-based object selection combined with on-device AI to advance trustworthy, privacy-preserving XR interactions. The source code and supplementary materials are available at: nanovis.org/ClickAIXR.html

---

## 论文详细总结（自动生成）

这是一份关于论文《ClickAIXR: On-Device Multimodal Vision-Language Interaction with Real-World Objects in Extended Reality》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决在扩展现实（XR）环境中使用视觉语言模型（VLM）时面临的三大挑战：
*   **隐私与安全**：现有的XR助手（如GazePointAR）多依赖云端AI（ChatGPT/Gemini），需要将用户环境的敏感视觉数据上传至云端。
*   **交互歧义性**：仅靠语音或注视点（Gaze）往往难以精确指定目标物体（例如在拥挤场景中问“这是什么？”），导致代词歧义。
*   **系统不稳定性**：云端推理受网络延迟、订阅费用和连接稳定性的限制。
**研究动机**：开发一个完全在设备端（On-Device）运行、支持精确物体选择且保护隐私的多模态XR交互框架。

### 2. 论文提出的方法论
核心思想是结合**本地VLM推理**与**基于控制器的精确裁剪窗口**。
*   **关键技术细节**：
    *   **设备端架构**：基于Magic Leap 2 (ML2) 硬件，使用C API和ONNX Runtime实现本地推理。
    *   **注视锁定裁剪窗口 (GCW)**：引入一种创新的选择机制。用户通过注视点确定窗口中心，利用控制器上的滑块实时调整窗口的宽度、高度和深度（距离），从而精确框选目标物体（ROI）。
    *   **多模态流水线**：
        1.  **图像处理**：捕获GCW选中的局部图像并缩放至224x224。
        2.  **语音识别 (ASR)**：使用本地Vosk模型将用户提问转为文本。
        3.  **VLM推理**：采用ViT-GPT2架构（ViT-Base编码器 + GPT-2解码器），在本地执行编码和生成。
        4.  **输出**：通过本地文本转语音 (TTS) 和屏幕显示返回答案。
*   **算法流程**：系统通过坐标映射公式将HUD（抬头显示）上的注视坐标转换为图像像素坐标，并在触发快门时锁定ROI，确保VLM接收到的图像与用户看到的完全一致。

### 3. 实验设计
*   **数据集/场景**：
    *   **延迟测试**：使用了 **COCO数据集**（100张室内场景图）和 **Book Covers数据集**（100张书籍封面图）。
    *   **用户研究**：邀请12名参与者在真实室内环境（包含多个摆放密集的物体）中进行实地测试。
*   **Benchmark与对比方法**：
    *   对比了当前顶尖的云端多模态模型：**OpenAI ChatGPT 5** 和 **Google Gemini 2.5 Flash**（均在智能手机上运行）。
    *   评估指标包括：系统可用性量表 (SUS)、推理延迟、令牌生成速度、感知可靠性和错误率。

### 4. 资源与算力
*   **硬件平台**：Magic Leap 2 头显。
*   **算力细节**：
    *   推理完全在ML2的**CPU**上运行（未使用GPU加速，作为未来改进方向）。
    *   **模型加载时长**：平均约3.51秒。
    *   **训练时长**：文中未提及模型训练，因为使用的是预训练的ViT-GPT2检查点并导出为ONNX格式。
*   **网络环境**：对比组（云端模型）使用了92/160 Mbps的高速Wi-Fi。

### 5. 实验数量与充分性
*   **实验规模**：
    *   定量测试：200张图像的推理性能分析。
    *   定性测试：12名用户的内受试者实验（Within-subjects），每人对每种方法进行约15次物体查询。
*   **充分性评价**：实验设计较为客观，涵盖了性能指标和用户主观体验。通过与成熟的商业云端产品对比，展示了端侧系统的差距与潜力。但样本量（12人）相对较小，且对比基准（手机端云AI vs 头显端本地AI）在硬件形态上存在固有差异。

### 6. 主要结论与发现
*   **性能可行性**：端侧VLM推理时间约为 **5.36 - 5.48秒**，令牌生成速度为3.36 tokens/s。虽然慢于云端，但在XR交互的可接受范围内（低于10秒的注意力丧失阈值）。
*   **可用性**：ClickAIXR的SUS得分为 **60.0**，属于“及格/边缘”水平，与同类XR研究（如GazePointAR的62.1）持平，但低于Gemini (81.9) 和 ChatGPT (76.7)。
*   **消除歧义**：GCW裁剪机制能有效解决代词歧义，用户可以精确选择物体的特定部分（如人脸上的鼻子）。

### 7. 优点
*   **隐私保护**：数据不离机，解决了XR设备全天候开启摄像头带来的隐私顾虑。
*   **交互透明度**：用户明确知道AI看到了什么（即裁剪框内的内容），增强了对AI回复的信任感。
*   **零网络依赖**：在无网络或弱网环境下（如远程工业现场、机密实验室）依然可用。
*   **模型无关性**：框架支持任何可导出为ONNX格式的VLM，具有良好的扩展性。

### 8. 不足与局限
*   **模型能力限制**：目前使用的ViT-GPT2主要用于图像描述（Captioning），在复杂的指令遵循（Instruction Following）方面不如大型云端模型。
*   **硬件瓶颈**：在CPU上运行导致延迟较高，且长时间运行可能影响头显电池寿命。
*   **用户疲劳**：相比于自动分割，手动使用滑块调整裁剪窗口虽然精确，但增加了用户的操作负担。
*   **可靠性感知**：用户研究显示，其感知可靠性低于商业成熟产品，这可能受限于模型规模和端侧算力。

（完）
