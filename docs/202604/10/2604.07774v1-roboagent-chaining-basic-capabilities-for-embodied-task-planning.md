---
title: "RoboAgent: Chaining Basic Capabilities for Embodied Task Planning"
title_zh: RoboAgent：用于具身任务规划的基础能力链式调用
authors: "Peiran Xu, Jiaqi Zheng, Yadong Mu"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.07774v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 使用视觉语言模型的能力驱动流水线进行具身任务规划
tldr: 针对具身任务规划中视觉语言模型（VLM）在长程推理和多轮交互方面的局限，本文提出 RoboAgent 框架。该框架通过调度器调用多种子能力，将复杂规划分解为一系列基础视觉语言问题，实现了透明且可控的推理过程。通过行为克隆、DAgger 和强化学习的多阶段训练，RoboAgent 在多个具身规划基准测试中表现优异，显著提升了模型在复杂环境下的任务完成能力。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在处理具身规划中的长程推理、多轮交互和长上下文分析时表现有限。
method: 提出一种能力驱动的规划流水线，利用单一 VLM 实现调度器和多种子能力，并通过多阶段训练（BC、DAgger、RL）和数据增强来优化模型。
result: 在广泛使用的具身任务规划基准测试上，该方法验证了其有效性，展现了比现有模型更强的规划能力和鲁棒性。
conclusion: RoboAgent 通过将复杂任务分解为基础能力链，为提升 VLM 在具身智能领域的规划性能提供了一种透明且高效的解决方案。
---

## 摘要
本文关注具身任务规划，即智能体从环境中获取视觉观测并执行原子动作以完成给定任务。尽管近期的视觉语言模型（VLMs）在多模态理解和推理方面取得了显著成果，但在应用于涉及多轮交互、长程推理和长上下文分析的具身规划时，其性能仍然受限。为了弥补这一差距，我们提出了 RoboAgent，这是一个能力驱动的规划流水线，模型在其中主动调用不同的子能力。每种能力维护其自身的上下文，并根据调度器给出的查询产生中间推理结果或与环境进行交互。该框架将复杂的规划分解为一系列 VLMs 能够更好处理的基础视觉语言问题，从而实现更透明、更可控的推理过程。调度器和所有能力均由单个 VLM 实现，不依赖外部工具。为了训练该 VLM，我们采用了一个多阶段范式，包括：（1）使用专家规划进行行为克隆，（2）使用模型收集的轨迹进行 DAgger 训练，以及（3）由专家策略引导的强化学习。在这些阶段中，我们利用环境模拟器的内部信息为每种能力构建高质量的监督信号，并进一步引入增强数据和合成数据，以提升模型在更多样化场景下的性能。在广泛使用的具身任务规划基准测试上的大量实验验证了所提方法的有效性。我们的代码将在 https://github.com/woyut/RoboAgent_CVPR26 上发布。

## Abstract
This paper focuses on embodied task planning, where an agent acquires visual observations from the environment and executes atomic actions to accomplish a given task. Although recent Vision-Language Models (VLMs) have achieved impressive results in multimodal understanding and reasoning, their performance remains limited when applied to embodied planning that involves multi-turn interaction, long-horizon reasoning, and extended context analysis. To bridge this gap, we propose RoboAgent, a capability-driven planning pipeline in which the model actively invokes different sub-capabilities. Each capability maintains its own context, and produces intermediate reasoning results or interacts with the environment according to the query given by a scheduler. This framework decomposes complex planning into a sequence of basic vision-language problems that VLMs can better address, enabling a more transparent and controllable reasoning process. The scheduler and all capabilities are implemented with a single VLM, without relying on external tools. To train this VLM, we adopt a multi-stage paradigm that consists of: (1) behavior cloning with expert plans, (2) DAgger training using trajectories collected by the model, and (3) reinforcement learning guided by an expert policy. Across these stages, we exploit the internal information of the environment simulator to construct high-quality supervision for each capability, and we further introduce augmented and synthetic data to enhance the model's performance in more diverse scenarios. Extensive experiments on widely used embodied task planning benchmarks validate the effectiveness of the proposed approach. Our codes will be available at https://github.com/woyut/RoboAgent_CVPR26.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **RoboAgent** 的具身任务规划框架，旨在通过将复杂的规划任务分解为一系列基础的视觉语言能力，提升视觉语言模型（VLM）在具身智能场景中的表现。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：具身任务规划（Embodied Task Planning, ETP）要求智能体根据视觉观测和指令生成原子动作序列。虽然视觉语言模型（VLM）在多模态理解上表现出色，但在处理**长程推理（Long-horizon reasoning）**、**多轮交互**和**复杂上下文管理**时仍然吃力。
*   **核心问题**：现有的方法（如自由格式的思维链 CoT）生成的推理过程往往缺乏规范化和直接监督，导致决策不可控且难以诊断。
*   **研究动机**：通过将复杂的规划过程解耦为可定义的“基础能力”，利用 VLM 内部已有的知识来简化规划，并提供细粒度的监督。

### 2. 方法论
*   **核心思想**：提出一种**能力驱动（Capability-driven）**的规划流水线。由一个中央调度器（Scheduler）根据当前上下文调用特定的子能力，每个能力负责处理特定的视觉语言子问题。
*   **关键技术细节**：
    *   **五大基础能力**：
        1.  **探索引导（EG）**：预测目标物体可能出现的方向。
        2.  **物体定位（OG）**：在当前视野中识别并定位目标。
        3.  **场景描述（SD）**：描述目标物体的状态和空间关系。
        4.  **动作解码（AD）**：将指令转化为可执行的原子动作序列。
        5.  **经验总结（ES）**：分析动作执行的结果及失败原因。
    *   **单一模型实现**：调度器和所有能力均由同一个 VLM（Qwen2.5-VL-3B）实现，无需外部工具。
    *   **三阶段训练范式**：
        1.  **阶段 1（专家 SFT）**：利用模拟器内部信息（特权信息）为每个能力构建高质量监督数据。
        2.  **阶段 2（DAgger SFT）**：在模型生成的轨迹上进行纠错训练，增强鲁棒性。
        3.  **阶段 3（专家诱导策略优化 EIPO）**：提出一种新的强化学习算法，利用专家策略的优势函数（Advantage Function）作为优化目标，比传统的 PPO/GRPO 更稳定。

### 3. 实验设计
*   **数据集与场景**：主要基于 **ALFRED** 训练集进行数据构建。
*   **Benchmark（基准测试）**：
    *   **ALFWorld**：侧重于视觉观测下的探索和简单操作。
    *   **EB-ALFRED**：侧重于复杂指令理解和长程任务。
    *   **EB-Habitat & LoTa-WAH**：用于测试跨模拟器和跨模态（纯文本）的泛化能力（OOD 评估）。
*   **对比方法**：包括闭源模型（GPT-4o, Claude-3.7, Gemini-1.5）和开源 SOTA 模型（RoboBrain2.0, Vlaser, REBP 等）。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 台 NVIDIA H800 (80GB) GPU**。
*   **训练细节**：
    *   基础模型：Qwen2.5-VL-3B。
    *   阶段 1：2 个 Epoch，学习率 1e-5。
    *   阶段 2：1 个 Epoch。
    *   阶段 3：120 次迭代，Batch Size 为 512。
*   **推理效率**：文中提到虽然 VLM 调用次数增加，但由于单次调用的 Token 数较少，总运行时间与直接生成动作的 Planner 相当。

### 5. 实验数量与充分性
*   **实验规模**：论文在 4 个不同的基准测试上进行了评估，涵盖了视觉和文本两种模态。
*   **消融实验**：详细分析了不同训练阶段（SFT, DAgger, RFT）、不同数据源（专家数据、合成数据）以及不同 RL 算法（EIPO vs GRPO）的影响。
*   **充分性评价**：实验设计非常全面，不仅验证了性能提升，还通过失败案例分析（Error Analysis）和真实世界演示（Real-world Demo）展示了模型的实用性和局限性，评估过程客观且公平。

### 6. 主要结论与发现
*   **性能领先**：RoboAgent 在 ALFWorld 和 EB-ALFRED 上均达到了领先水平，甚至在某些指标上超越了强大的闭源模型。
*   **透明性**：能力驱动的架构使得推理过程高度透明，可以清晰地定位是“定位失败”还是“动作解码失败”。
*   **泛化性**：模型展现了良好的跨模态泛化能力，在未经针对性训练的情况下，能直接处理纯文本环境的任务。
*   **算法优势**：提出的 EIPO 算法在具身任务中比 GRPO 收敛更快、更稳定。

### 7. 优点
*   **架构优雅**：将复杂的具身智能问题分解为结构化的子任务，符合人类解决问题的逻辑。
*   **数据利用率高**：巧妙地利用模拟器的内部状态（如语义地图、掩码）来生成自动化的细粒度监督信号。
*   **算法创新**：EIPO 解决了具身强化学习中奖励稀疏和方差大的痛点。
*   **诊断友好**：模块化的能力调用让开发者能够轻松进行错误溯源。

### 8. 不足与局限
*   **视觉瓶颈**：错误分析显示，约 50% 的失败源于视觉识别（Object Recognition），说明底层感知能力仍受限于基础 VLM 的视觉分辨率和识别精度。
*   **跨域差距**：虽然在 OOD 测试中表现尚可，但与闭源模型相比，在完全陌生的模拟器环境（如 EB-Habitat）中仍有明显性能差距。
*   **模拟器依赖**：训练过程高度依赖模拟器提供的特权信息，这在纯真实世界数据采集时可能难以完全复现。

（完）
