---
title: "HiRO-Nav: Hybrid ReasOning Enables Efficient Embodied Navigation"
title_zh: HiRO-Nav：混合推理实现高效具身导航
authors: "He Zhao, Yijun Yang, Zichuan Lin, Deheng Ye, Chunyan Miao"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08232v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 使用大推理模型的具身导航
tldr: HiRO-Nav 针对具身导航中大推理模型（LRM）计算开销大的问题，提出了一种自适应混合推理策略。该方法通过监测动作熵，仅在面临复杂场景或关键决策的高熵时刻激活推理，而在简单场景下保持反射性动作。通过结合混合监督微调与在线强化学习，HiRO-Nav 在提升导航成功率的同时显著降低了 Token 消耗，实现了性能与效率的平衡。
selection_source: fresh_fetch
motivation: 旨在解决具身智能体在长程导航任务中每一步都进行推理导致的计算资源浪费和效率低下问题。
method: 提出基于动作熵的自适应推理机制，仅对高熵动作触发推理，并采用混合监督微调与在线强化学习进行训练。
result: 在 CHORES-S ObjectNav 基准测试中，HiRO-Nav 在成功率和 Token 效率之间取得了比全推理或无推理基线更好的权衡。
conclusion: 通过按需激活推理能力，HiRO-Nav 证明了混合推理策略是实现高效且智能的具身导航的有效途径。
---

## 摘要
基于大语言推理模型（LRMs）构建的具身导航智能体能够处理复杂的、多模态的环境输入，并进行每一步的落地推理，以改进长程任务中的序列决策。然而，一个关键问题仍然存在：如何智能且高效地利用 LRMs 的推理能力来完成长程导航任务？在简单场景中，智能体应当表现出反射式的行动，而在复杂场景中，它们应在行动前进行深思熟虑的推理。为了实现这一目标，我们引入了混合推理导航（HiRO-Nav）智能体，这是首个能够根据自身动作熵自适应地决定是否在每一步执行思考的智能体。具体而言，通过研究智能体动作熵在导航轨迹中的演变，我们观察到只有一小部分动作表现出高熵，而这些动作通常会将智能体引导至新颖场景或关键物体。此外，研究动作熵与任务完成度（即 Q 值）之间的关系表明，改进高熵动作对任务成功的贡献更为显著。因此，我们提出了一套定制的训练流程，包括作为冷启动的混合监督微调，以及随后采用所提混合推理策略的在线强化学习，从而仅针对高熵动作显式激活推理，在显著降低计算开销的同时提高决策质量。在 CHORES-S ObjectNav 基准测试上的广泛实验表明，与密集思考和不思考的基准模型相比，HiRO-Nav 在成功率和 Token 效率之间实现了更好的权衡。

## Abstract
Embodied navigation agents built upon large reasoning models (LRMs) can handle complex, multimodal environmental input and perform grounded reasoning per step to improve sequential decision-making for long-horizon tasks. However, a critical question remains: \textit{how can the reasoning capabilities of LRMs be harnessed intelligently and efficiently for long-horizon navigation tasks?} In simple scenes, agents are expected to act reflexively, while in complex ones they should engage in deliberate reasoning before acting.To achieve this, we introduce \textbf{H}ybr\textbf{i}d \textbf{R}eas\textbf{O}ning \textbf{Nav}igation (\textbf{HiRO-Nav}) agent, the first kind of agent capable of adaptively determining whether to perform thinking at every step based on its own action entropy. Specifically, by examining how the agent's action entropy evolves over the navigation trajectories, we observed that only a small fraction of actions exhibit high entropy, and these actions often steer the agent toward novel scenes or critical objects. Furthermore, studying the relationship between action entropy and task completion (i.e., Q-value) reveals that improving high-entropy actions contributes more positively to task success.Hence, we propose a tailored training pipeline comprising hybrid supervised fine-tuning as a cold start, followed by online reinforcement learning with the proposed hybrid reasoning strategy to explicitly activate reasoning only for high-entropy actions, significantly reducing computational overhead while improving decision quality. Extensive experiments on the \textsc{CHORES}-$\mathbb{S}$ ObjectNav benchmark showcases that HiRO-Nav achieves a better trade-off between success rates and token efficiency than both dense-thinking and no-thinking baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **HiRO-Nav** 的具身导航智能体，旨在解决大推理模型（LRM）在长程导航任务中因“过度思考”导致的效率低下和性能受损问题。

### 1. 核心问题与研究动机
*   **研究背景**：大推理模型（如 DeepSeek-R1, o3）通过思维链（CoT）显著提升了具身智能体的决策能力。
*   **核心问题**：现有的推理导航智能体通常采用“每步必思考”（Dense-Thinking）模式。这带来了两大问题：
    1.  **计算开销巨大**：在简单的移动步骤中进行长文本推理会显著增加延迟和 Token 消耗。
    2.  **过度思考（Overthinking）**：在简单场景下，过度的推理反而可能引入幻觉，降低任务成功率。
*   **研究动机**：智能体应像人类一样，在简单环境下形成“肌肉记忆”（反射式动作），仅在遇到复杂路口或关键决策点时才进行“深思熟虑”。

### 2. 方法论
HiRO-Nav 的核心思想是**基于动作熵（Action Entropy）的自适应混合推理**。
*   **动作熵分析**：研究发现，导航轨迹中仅约 30% 的动作具有高熵（不确定性高），这些动作通常发生在探索新区域或接近目标的关键点。实验证明，仅对高熵动作进行推理能最大化提升 Q 值（任务成功概率）。
*   **混合推理策略**：
    *   智能体首先预测动作。如果首个 Token 的动作熵 $H$ 超过预设阈值 $\tau$，则触发推理模式，重新生成包含 `<think>` 标签的 CoT 过程及最终动作。
    *   引入**无推理窗口（NTW）**：防止智能体陷入连续高熵状态导致的重复推理。
*   **训练流程**：
    1.  **混合监督微调（H-SFT）**：利用 Gemini-2.0-Flash 对专家轨迹中的高熵样本生成推理轨迹，构建包含“动作”和“推理+动作”的混合数据集进行冷启动。
    2.  **两阶段在线强化学习（RL）**：
        *   **第一阶段**：仅训练“无推理”能力，提升基础导航水平。
        *   **第二阶段**：引入混合推理策略，专门优化推理能力，并通过 KL 散度约束防止第一阶段能力的退化（模型崩溃）。

### 3. 实验设计
*   **数据集与场景**：使用 **CHORES-S ObjectNav** 基准测试，包含 200 个场景和任务。环境基于 AI2-THOR 模拟器，使用 Stretch RE-1 机器人。
*   **对比方法**：
    *   **闭源模型**：GPT-4o（无推理）、o3（密集推理）、Gemini 2.5-Pro（密集推理）。
    *   **开源基线**：NaVILA、Qwen2.5-VL-3B（分别测试无推理、密集推理、每 K 步推理等策略）。
*   **评估指标**：成功率（SR）、路径长度加权成功率（SEL）、每回合平均 Token 消耗（#Token/E）。

### 4. 资源与算力
*   **模型规模**：基于 **Qwen2.5-VL-3B**。
*   **算力细节**：论文**未明确说明**具体的 GPU 型号和数量。
*   **训练参数**：SFT 阶段 Batch Size 为 256，学习率 2e-5；RL 阶段使用 PPO 算法，Batch Size 为 384。使用了 Verl-Agent 训练框架。

### 5. 实验数量与充分性
*   **实验规模**：在 200 个测试任务上进行了评估，涵盖了简单、中等、困难三种难度。
*   **消融实验**：非常充分。作者针对动作熵阈值 $\tau$、无推理窗口大小、两阶段 RL 的必要性、KL 散度的影响以及长短期记忆（ASM）的作用均做了详细对比。
*   **鲁棒性测试**：额外测试了在非真值（由 Mask R-CNN 和 Depth-Anything 生成的噪声地图）下的表现，证明了方法的实用性。
*   **客观性**：通过 Pass@k 评估探索了性能上限，并与 SOTA 方法 Poliformer 进行了对比，实验设计较为严谨。

### 6. 主要结论与发现
*   **性能与效率的双赢**：HiRO-Nav 在成功率上达到 81.0%（显著高于无推理的 70.5%），同时 Token 消耗仅为密集推理模式的约 1/10。
*   **克服过度思考**：实验证明，在低熵步骤强制推理（阈值过低）反而会导致成功率下降。
*   **训练策略至关重要**：直接进行混合推理的 RL 会导致模型崩溃，必须采用先训练反射能力、再训练推理能力的两阶段法。

### 7. 优点
*   **自适应性强**：首个根据动作熵动态决定“是否思考”的导航智能体，符合认知直觉。
*   **训练管线优雅**：通过两阶段 RL 解决了推理与非推理模式之间的梯度不平衡问题。
*   **落地潜力**：显著降低了推理成本，使基于 LRM 的具身智能体在实时任务中更具可行性。

### 8. 不足与局限
*   **阈值依赖**：熵阈值 $\tau$ 目前是预设的固定值，不同模型或环境可能需要重新调优。
*   **反应式延迟**：目前的逻辑是“先预测动作 -> 发现熵高 -> 重新推理”，这在发现高熵时实际上执行了两次预测，存在一定的计算冗余。
*   **环境局限**：实验主要在模拟器（AI2-THOR）中完成，尚未在复杂的真实物理世界中进行大规模验证。

（完）
