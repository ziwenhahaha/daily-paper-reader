---
title: A Physical Agentic Loop for Language-Guided Grasping with Execution-State Monitoring
title_zh: 一种带有执行状态监测的语言引导抓取物理智能体循环
authors: "Wenze Wang, Mehdi Hosseinzadeh, Feras Dayoub"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07395v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 带有执行状态监测的语言引导抓取
tldr: 针对机器人语言引导抓取中常见的单次执行且缺乏反馈的问题，本文提出了一种受数字智能体启发的物理智能体循环框架。该框架通过引入名为Watchdog的执行监控层，将夹爪遥测数据转化为离散的状态标签，并结合确定性策略实现对抓取失败（如滑落、空抓等）的自动重试或用户求助。实验证明，该方法在不改变底层抓取模型的情况下，显著提升了机器人在复杂环境下的鲁棒性和可解释性。
selection_source: fresh_fetch
motivation: 现有的语言引导抓取系统多为开环执行，无法有效识别并处理抓取过程中的物理失败或语义错误。
method: 提出一种物理智能体循环，利用Watchdog监控层融合触觉与遥测数据，并配合确定性策略进行状态反馈与任务恢复。
result: 在包含视觉歧义和诱导失败的多种场景下，该闭环系统比开环执行表现出更强的鲁棒性，且架构开销极小。
conclusion: 通过显式的执行状态监控和有界恢复策略，可以显著提升移动操作机器人在处理不确定性任务时的可靠性。
---

## 摘要
遵循语言指令的机器人操纵系统通常以单次尝试（single-shot）的方式执行抓取原语：模型提出动作，机器人执行，而诸如空抓、打滑、卡死、超时或语义错误抓取等失败情况并未以结构化的方式反馈给决策层。受数字工具使用智能体中智能体循环（agentic loops）的启发，我们将语言引导的抓取重新表述为一个在具身执行状态上运行的有界具身智能体，其中物理动作会暴露显式的工具状态流。我们引入了一种物理智能体循环，它通过 (i) 基于事件的接口和 (ii) 执行监测层 Watchdog，封装了一个未经修改的学习型操纵原语（抓取并提升）。Watchdog 利用接触感知融合和时间稳定技术，将嘈杂的夹爪遥测数据转换为离散的结果标签。这些结果事件（可选结合抓取后的语义验证）由一个确定性的有界策略处理，该策略决定完成、重试或上报给用户进行澄清，从而保证有限次终止。我们在配备手眼 D405 相机的移动协作机器人上验证了该循环，保持底层抓取模型不变，并评估了涉及视觉歧义、干扰物和诱导执行失败的代表性场景。结果表明，显式的执行状态监测和有界恢复比开环执行实现了更鲁棒且更具可解释性的行为，同时增加了极小的架构开销。源代码和演示请参考我们的项目页面：https://wenzewwz123.github.io/Agentic-Loop/

## Abstract
Robotic manipulation systems that follow language instructions often execute grasp primitives in a largely single-shot manner: a model proposes an action, the robot executes it, and failures such as empty grasps, slips, stalls, timeouts, or semantically wrong grasps are not surfaced to the decision layer in a structured way. Inspired by agentic loops in digital tool-using agents, we reformulate language-guided grasping as a bounded embodied agent operating over grounded execution states, where physical actions expose an explicit tool-state stream. We introduce a physical agentic loop that wraps an unmodified learned manipulation primitive (grasp-and-lift) with (i) an event-based interface and (ii) an execution monitoring layer, Watchdog, which converts noisy gripper telemetry into discrete outcome labels using contact-aware fusion and temporal stabilization. These outcome events, optionally combined with post-grasp semantic verification, are consumed by a deterministic bounded policy that finalizes, retries, or escalates to the user for clarification, guaranteeing finite termination. We validate the resulting loop on a mobile manipulator with an eye-in-hand D405 camera, keeping the underlying grasp model unchanged and evaluating representative scenarios involving visual ambiguity, distractors, and induced execution failures. Results show that explicit execution-state monitoring and bounded recovery enable more robust and interpretable behavior than open-loop execution, while adding minimal architectural overhead. For the source code and demo refer to our project page: https://wenzewwz123.github.io/Agentic-Loop/

---

## 论文详细总结（自动生成）

### 论文总结：一种带有执行状态监测的语言引导抓取物理智能体循环

#### 1. 核心问题与整体含义
*   **研究动机**：当前的语言引导机器人抓取系统大多采用“单次执行”模式（Open-loop），即根据指令生成动作并执行，但缺乏对执行结果的结构化反馈。当发生空抓、物体滑落、机械臂卡死或抓错目标（语义错误）时，系统无法感知失败并进行自我修正。
*   **核心问题**：如何借鉴数字智能体（如 LLM Agent）的“工具调用-状态反馈”循环机制，为物理抓取任务构建一个能够监测执行状态、具备容错能力且能保证任务终止的闭环系统？

#### 2. 方法论
该论文提出了一种**物理智能体循环（Physical Agentic Loop）**框架，其核心思想是将物理动作视为具有显式状态流的“工具调用”。
*   **核心流程（Observe-Act-Evaluate-Decide）**：
    1.  **Observe（观察）**：接收语言指令并解析为结构化目标，结合 RGB-D 图像进行感知调节。
    2.  **Act（行动）**：调用预训练的操纵原语（如 ForceSight），执行抓取动作。
    3.  **Evaluate（评估）**：通过 **Watchdog** 监测层，将夹爪的电流、位置等遥测数据转化为离散的状态标签（如 SUCCESS, EMPTY, SLIP, WEAK, STALL, TIMEOUT）。
    4.  **Decide（决策）**：根据状态标签执行**有界恢复策略**（如完成任务、重试一次、或请求用户澄清）。
*   **关键技术细节**：
    *   **Watchdog 监测层**：利用接触感知融合和时间稳定技术，处理嘈杂的传感器信号，输出高置信度的执行结果。
    *   **有界策略（Bounded Policy）**：为了防止系统陷入无限循环，设定了严格的重试预算（如仅重试一次）和明确的升级机制（向用户求助）。
    *   **语义验证（可选）**：在物理抓取成功后，通过视觉模型验证抓取物是否符合指令描述。

#### 3. 实验设计
*   **实验平台**：使用 Hello Robot Stretch 3 移动协作机器人，配备 D405 手眼相机。
*   **底层模型**：采用未经修改的 **ForceSight** 作为抓取原语。
*   **实验场景**：
    *   **视觉歧义**：颜色歧义（多个不同颜色的杯子）、空间歧义（左边/右边的物体）。
    *   **干扰物鲁棒性**：目标物旁存在显著干扰。
    *   **诱导失败**：人为移除目标物以触发“空抓”状态，测试重试逻辑。
    *   **目标缺失**：场景中无目标物，测试系统是否能安全终止并求助。
*   **Benchmark**：以传统的**开环执行（Open-loop）**作为基准对比方法。

#### 4. 资源与算力
*   **模型使用**：使用了轻量级的本地大语言模型 **TinyLlama-1.1B** 用于指令解析。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。由于该方法的核心是系统架构的封装而非重新训练大型视觉-语言-动作模型（VLA），其运行开销主要集中在推理端，论文提到其增加的延迟极小（约 1 秒左右）。

#### 5. 实验数量与充分性
*   **实验规模**：在系统开发和评估过程中共进行了超过 **290 次**真实机器人实验，其中 **180 次**是针对最终系统的正式测试。
*   **消融实验**：对比了“无 Watchdog”、“无视觉调节”、“无重试逻辑”等多种变体，验证了各组件对成功率的贡献。
*   **充分性评价**：实验覆盖了物理失败和语义歧义两大类常见问题，每组场景进行 10 次重复实验。虽然样本量在统计学上不算巨大，但在真实机器人领域属于较为充分且客观的验证。

#### 6. 主要结论与发现
*   **性能提升**：在存在歧义和干扰的场景下，该闭环系统的成功率远高于开环系统（例如在干扰物场景下从 0% 提升至 100%）。
*   **鲁棒性**：显式的执行状态监测能够有效识别“空抓”，并通过重试机制挽救任务。
*   **低开销**：引入智能体循环带来的运行时间开销（约 15.94s vs 基准 14.78s）在可接受范围内。
*   **可解释性**：系统能够明确报告失败原因（如输出“EMPTY”标签），增强了人机交互的透明度。

#### 7. 优点
*   **非侵入式封装**：无需重新训练或修改底层的抓取模型，具有很强的通用性和可移植性。
*   **确定性与安全性**：通过有界策略保证了任务总会终止，避免了自主系统常见的死循环问题。
*   **多模态融合**：巧妙地将底层的物理遥测信号（电流/力矩）与高层的语义指令结合。

#### 8. 不足与局限
*   **语义验证依赖性**：如果上游的视觉感知模型（如实例分割）出错，系统仍可能抓错物体。
*   **恢复策略单一**：目前的恢复动作主要是简单的“重试”，缺乏针对特定失败模式（如针对 SLIP 进行调整抓取力度）的精细化策略。
*   **场景复杂度**：实验主要集中在桌面抓取，对于更复杂的长程任务（Long-horizon tasks）或动态环境的适应性尚待验证。

（完）
