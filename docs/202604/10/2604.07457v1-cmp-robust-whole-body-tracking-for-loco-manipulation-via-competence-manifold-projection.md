---
title: "CMP: Robust Whole-Body Tracking for Loco-Manipulation via Competence Manifold Projection"
title_zh: CMP：通过能力流形投影实现移动操作的鲁棒全身跟踪
authors: "Ziyang Cheng, Haoyu Wei, Hang Yin, Xiuwei Xu, Bingyao Yu, Jie Zhou, Jiwen Lu"
date: 2026-04-08
pdf: "https://arxiv.org/pdf/2604.07457v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 移动操作的全身跟踪与控制
tldr: 针对腿式移动操作机器人全身控制在面对分布外（OOD）输入（如传感器噪声或不可行指令）时表现脆弱的问题，本文提出了能力流形投影（CMP）框架。该方法通过逐帧安全方案、安全估计器及同构潜空间，将复杂安全约束转化为高效的流形投影，显著提升了机器人在极端环境下的鲁棒性，实现了在保证任务连续性的同时，对不可行目标的“尽力而为”式追踪。
selection_source: fresh_fetch
motivation: 现有的全身控制策略在面对传感器噪声或超出训练分布的指令时容易崩溃，缺乏处理分布外输入的鲁棒性。
method: 提出能力流形投影（CMP），利用安全估计器构建同构潜空间，将不安全或不可行的指令实时投影回机器人的能力范围内。
result: "在典型分布外场景下，CMP 将机器人的生存率提升了 10 倍，且追踪性能损失低于 10%，并展现出向目标逼近的“尽力而为”行为。"
conclusion: CMP 有效解决了腿式操作机器人在复杂动态环境下的鲁棒性难题，为实现安全、连续的全身运动控制提供了新思路。
---

## 摘要
虽然足式移动操作机器人的解耦控制方案已表现出鲁棒性，但用于跟踪全局末端执行器位姿的整体全身控制策略在面对由传感器噪声或不可行用户指令引起的分布外（OOD）输入时仍然十分脆弱。为了在不牺牲任务性能和连续性的情况下提高对这些扰动的鲁棒性，我们提出了能力流形投影（CMP）。具体而言，我们利用一种逐帧安全方案，将无限时界安全约束转换为计算高效的单步流形包含问题。为了实例化该能力流形，我们采用了一个下界安全估计器，用以区分训练分布之外的未掌握意图。随后，我们引入了一个同构潜空间（ILS），将流形几何与安全概率对齐，从而实现对任意 OOD 意图的高效 O(1) 无缝防御。实验表明，在基准方法遭受灾难性失败的典型 OOD 场景中，CMP 实现了高达 10 倍的生存率提升，而跟踪性能下降不足 10%。值得注意的是，该系统表现出涌现的“尽力而为”泛化行为，通过遵循能力边界逐步完成 OOD 目标。结果视频见：https://shepherd1226.github.io/CMP。

## Abstract
While decoupled control schemes for legged mobile manipulators have shown robustness, learning holistic whole-body control policies for tracking global end-effector poses remains fragile against Out-of-Distribution (OOD) inputs induced by sensor noise or infeasible user commands. To improve robustness against these perturbations without sacrificing task performance and continuity, we propose Competence Manifold Projection (CMP). Specifically, we utilize a Frame-Wise Safety Scheme that transforms the infinite-horizon safety constraint into a computationally efficient single-step manifold inclusion. To instantiate this competence manifold, we employ a Lower-Bounded Safety Estimator that distinguishes unmastered intentions from the training distribution. We then introduce an Isomorphic Latent Space (ILS) that aligns manifold geometry with safety probability, enabling efficient O(1) seamless defense against arbitrary OOD intents. Experiments demonstrate that CMP achieves up to a 10-fold survival rate improvement in typical OOD scenarios where baselines suffer catastrophic failure, incurring under 10% tracking degradation. Notably, the system exhibits emergent ``best-effort'' generalization behaviors to progressively accomplish OOD goals by adhering to the competence boundaries. Result videos are available at: https://shepherd1226.github.io/CMP.

---

## 论文详细总结（自动生成）

以下是对论文《CMP: Robust Whole-Body Tracking for Loco-Manipulation via Competence Manifold Projection》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
足式移动操作机器人（如四足机器人加机械臂）在执行全身控制（WBC）任务时，通常需要跟踪全局末端执行器位姿。然而，现有的端到端学习策略在面对**分布外（OOD）输入**时表现得非常脆弱。这些输入可能源于视觉惯性里程计（VIO）的漂移、传感器噪声或用户给出的物理不可行指令（如要求机器人向后方极远处抓取）。
*   **挑战**：传统的安全机制要么计算量太大（如基于优化的方法），无法满足高频控制需求；要么过于生硬（如紧急停止），会导致任务中断。
*   **目标**：开发一种既能保证物理安全，又能实现“尽力而为”式任务跟踪的鲁棒控制框架。

### 2. 方法论
论文提出了**能力流形投影（CMP）**框架，核心思想是将复杂的安全约束转化为潜空间中的几何投影：
*   **逐帧安全方案（Frame-Wise Safety Scheme）**：通过数学推导，将难以处理的无限时界（Infinite-Horizon）安全问题简化为单步的潜空间包含问题。定义了“永久安全最大概率”指标 $W^\pi$。
*   **下界安全估计器（Lower-Bounded Safety Estimator）**：利用时序差分（TD）学习训练一个估计器，预测当前状态和意图下的生存概率。该估计器被证明是真实安全值的下界，从而保证了保守性和安全性。
*   **同构潜空间（Isomorphic Latent Space, ILS）**：这是本文的关键创新。通过动态 KL 散度正则化，强制潜空间的几何结构与安全概率对齐——**安全的意图分布在原点附近，不安全的意图分布在远离原点的球壳上**。
*   **$O(1)$ 实时投影**：在推理时，只需检查潜向量的范数。若超出预设的安全半径 $R_{safe}$，则直接进行向量截断（投影）。这种操作计算复杂度极低，且能找到语义上最接近原始指令的安全指令。

### 3. 实验设计
*   **实验环境**：Isaac Gym 仿真环境及真机（Unitree Go2 四足机器人 + Saber 机械臂）。
*   **数据集/场景**：
    *   **In-Distribution (ID)**：机器人前方 60° 范围内的常规操作任务。
    *   **OOD-Geometry**：要求机器人操作后方（120°-240°）的目标，测试几何泛化。
    *   **OOD-Sensor**：在 ID 轨迹中注入高强度传感器噪声，模拟 VIO 失效。
*   **Benchmark 与对比方法**：
    *   **UMI-on-Legs**：目前的 SOTA 全身控制基准。
    *   **Latent Shielding**：基于潜空间的屏蔽方法。
    *   **Neural CBF**：基于神经控制屏障函数的方法。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX A6000** GPU。
*   **训练时长**：总训练时间约为 **2.5 小时**（进行 2000 次迭代）。
*   **推理延迟**：CMP 的投影算法延迟仅为 **2.99 ms**，远低于 Neural CBF（5.36 ms），能够支持高频全身控制。

### 5. 实验数量与充分性
*   **仿真实验**：每个场景测试了 **7,000 条** 轨迹（共 21,000 条），每条持续 13 秒，样本量巨大。
*   **消融实验**：对比了 CVAE、带安全过滤的 SCVAE 以及完整的 CMP，验证了 ILS 几何对齐的必要性。
*   **真机实验**：涵盖 15 个任务类别（ID、中度 OOD、极端 OOD），每个任务重复 3 次，共 45 次真机测试。
*   **评价**：实验设计全面，涵盖了从感知失效到指令不可行的多种极端情况，对比基准权威，数据量足以支撑其结论。

### 6. 主要结论与发现
*   **生存率飞跃**：在 OOD 场景下，CMP 将机器人的生存率提升了 **10 倍**（从 4.7% 提升至 46.9% 以上）。
*   **性能保持**：在 ID 场景下，CMP 的跟踪误差增加不到 10%，证明了安全机制不会显著损害正常性能。
*   **“尽力而为”行为**：当面对不可行指令时，CMP 不会死机或崩溃，而是沿着能力边界移动（例如通过转身来尝试接近后方的目标），展现了极强的任务连续性。
*   **抑制正反馈失效**：CMP 能有效切断“传感器误差 -> 激进动作 -> 更大传感器误差 -> 崩溃”的恶性循环。

### 7. 优点
*   **极高的计算效率**：$O(1)$ 的投影操作非常适合对实时性要求极高的腿式机器人控制。
*   **优雅的理论还原**：将复杂的时序安全约束简化为潜空间的几何范数约束，理论推导严密。
*   **平滑的任务降级**：相比于直接停机，其“投影”机制保留了指令的语义，实现了性能的平滑衰减。

### 8. 不足与局限
*   **估计器误差风险**：如果安全估计器训练不充分，可能会出现过高估计安全性的情况，导致机器人产生振荡。
*   **统计学保证而非硬约束**：ILS 的球形边界是基于统计分布的，对于某些极端的边缘情况，投影后的指令仍可能导致失败。
*   **动态环境适应性**：目前主要处理指令和感知的 OOD，尚未深入探讨负载变化（如搬运重物）或外部强力干扰下的动态 OOD 鲁棒性。

（完）
