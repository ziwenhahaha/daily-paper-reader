---
title: "Mask World Model: Predicting What Matters for Robust Robot Policy Learning"
title_zh: 掩码世界模型：为鲁棒的机器人策略学习预测关键信息
authors: "Yunfan Lou, Xiaowei Chi, Xiaojie Zhang, Zezhong Qian, Chengxuan Li, Rongyu Zhang, Yaoxu Lyu, Guoyu Song, Chuyao Fu, Haoxuan Xu, Pengwei Wang, Shanghang Zhang"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19683v2"
tags: ["query:wam-vla"]
score: 9.0
evidence: 用于鲁棒机器人策略学习的世界模型
tldr: 针对机器人策略学习中世界模型易受背景和光照等无关视觉因素干扰的问题，本文提出掩码世界模型（MWM）。该模型利用视频扩散架构预测语义掩码而非像素，通过几何信息瓶颈过滤视觉噪声并捕捉核心物理动力学，同时结合扩散策略头实现端到端控制。实验证明，MWM在LIBERO和RLBench等基准测试及真实场景中表现优异，具有极强的泛化能力和对纹理信息丢失的鲁棒性。
selection_source: fresh_fetch
motivation: 传统的基于RGB视频预测的世界模型容易过度拟合背景和光照等无关因素，导致生成的控制策略在复杂环境下不够稳健。
method: 提出一种预测语义掩码演化的视频扩散架构，通过几何信息瓶颈强制模型学习物理接触关系，并集成扩散策略头进行控制。
result: 在LIBERO和RLBench仿真基准以及真实世界实验中，MWM显著优于现有的基于RGB的世界模型，展现出更强的泛化性和抗干扰能力。
conclusion: 预测语义掩码而非像素能有效提取关键物理特征并过滤视觉噪声，是构建鲁棒且通用的机器人世界模型的一种高效范式。
---

## 摘要
源自大规模视频生成预训练的世界模型已成为通用机器人策略学习的一种极具前景的范式。然而，标准方法通常侧重于高保真 RGB 视频预测，这可能导致对动态背景和光照变化等无关因素的过拟合。这些干扰降低了模型的泛化能力，最终导致控制策略不可靠且脆弱。为了解决这一问题，我们引入了掩码世界模型（Mask World Model, MWM），它利用视频扩散架构来预测语义掩码而非像素的演变。这种转变引入了一个几何信息瓶颈，迫使模型在过滤视觉噪声的同时，捕捉核心的物理动力学和接触关系。我们将这种掩码动力学骨干网络与基于扩散的策略头无缝集成，以实现鲁棒的端到端控制。广泛的评估证明了 MWM 在 LIBERO 和 RLBench 仿真基准测试中的优越性，显著优于目前最先进的基于 RGB 的世界模型。此外，真实世界实验和鲁棒性评估（通过随机令牌剪枝）表明，MWM 展现出卓越的泛化能力，以及对纹理信息丢失的强韧性。

## Abstract
World models derived from large-scale video generative pre-training have emerged as a promising paradigm for generalist robot policy learning. However, standard approaches often focus on high-fidelity RGB video prediction, this can result in overfitting to irrelevant factors, such as dynamic backgrounds and illumination changes. These distractions reduce the model's ability to generalize, ultimately leading to unreliable and fragile control policies. To address this, we introduce the Mask World Model (MWM), which leverages video diffusion architectures to predict the evolution of semantic masks instead of pixels. This shift imposes a geometric information bottleneck, forcing the model to capture essential physical dynamics and contact relations while filtering out visual noise. We seamlessly integrate this mask dynamics backbone with a diffusion-based policy head to enable robust end-to-end control. Extensive evaluations demonstrate the superiority of MWM on the LIBERO and RLBench simulation benchmarks, significantly outperforming the state-of-the-art RGB-based world models. Furthermore, real-world experiments and robustness evaluation (via random token pruning) reveal that MWM exhibits superior generalization capabilities and robust resilience to texture information loss.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Mask World Model (MWM)** 的新型机器人学习框架，旨在通过预测语义掩码而非原始像素来提升机器人控制策略的鲁棒性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：当前基于大规模视频预训练的世界模型（World Models）主要通过预测未来的 RGB 像素来学习环境动力学。
*   **核心痛点**：RGB 预测目标与控制任务之间存在“失配”。RGB 图像包含大量与决策无关的干扰因素（如光照变化、纹理、动态背景），这会导致模型过拟合视觉噪声，在面对环境分布偏移（OOD）时表现脆弱。
*   **研究动机**：通过引入“几何信息瓶颈”，迫使模型专注于物体的空间布局、身份和接触动力学，从而学习到更具泛化性的特征。

### 2. 方法论
*   **核心思想**：将世界模型的预测目标从“像素”转向“语义掩码（Semantic Masks）”。在训练阶段利用语义监督，但在推理阶段仅需原始 RGB 输入。
*   **关键技术细节**：
    *   **两阶段训练协议**：
        1.  **阶段一（掩码动力学预训练）**：使用条件流匹配（Conditional Flow Matching）目标，训练一个 Diffusion Transformer (DiT) 骨干网络来预测未来的语义掩码潜变量。
        2.  **阶段二（策略学习）**：冻结或微调骨干网络，训练一个扩散策略头（Diffusion Policy Head），该头通过交叉注意力机制从骨干网络的“预测特征库”中提取控制相关的特征。
    *   **统一编码方案**：使用预训练的视频 VAE。为了保持一致性，将离散的语义掩码渲染为彩色图像后再送入 VAE 编码器，使其与 RGB 潜空间对齐。
    *   **架构优化**：采用 3D 旋转位置编码（3D RoPE）以适应 VAE 的时空压缩；使用 AdaIN 风格的时间步调节来增强扩散过程的稳定性。
    *   **推理机制**：采用后退地平线控制（Receding Horizon Control, RHC），通过 10 步 Euler 离散扩散采样生成动作序列。

### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 130 个语言条件下的操作任务，分为 Spatial、Object、Goal 和 Libero-10 四个子集。
    *   **RLBench**：选取了 6 个具有挑战性的桌面操作任务（如扫灰尘、插伞等）。
    *   **真实世界**：使用 Franka Emika Panda 机器人，执行包括“放面包入篮”、“开抽屉放笔”、“倒水”、“放书上架”在内的 4 类任务。
*   **对比方法**：包括 OpenVLA、CogACT、$\pi_0$、GE-ACT 以及基于 Cosmos 的不同变体。
*   **评估维度**：成功率（SR）、视觉泛化性（背景/光照/颜色偏移）、鲁棒性（随机 Token 剪枝）。

### 4. 资源与算力
*   **硬件设施**：使用了由 8 张 **NVIDIA A100 (80GB)** GPU 组成的集群。
*   **训练时长**：
    *   阶段一（动力学）：约 **3.5 天**（30k steps）。
    *   阶段二（策略）：每个任务套件约 **1.5 天**（18k steps）。
*   **软件框架**：使用了 DeepSpeed ZeRO-2 优化器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   LIBERO 评估了超过 500 个 episode。
    *   RLBench 每个任务进行 20 次随机初始化评估。
    *   真实世界实验每个任务进行 20 次尝试，并涵盖了 3 种不同的视觉偏移条件。
*   **充分性与公平性**：论文设计了多组消融实验（如 MWM-C1, C2 变体），对比了显式掩码解码与潜特征预测的差异。实验涵盖了仿真与真机、长程与短程任务，对比基准均为当前最先进（SOTA）的模型，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：MWM 在 LIBERO (98.3% SR) 和 RLBench (68.3% SR) 上显著优于基于 RGB 的世界模型。
*   **泛化性强**：在真实世界视觉偏移测试中，MWM 的性能保持率（Retain）最高，尤其在背景剧烈变化时，RGB 模型几乎失效，而 MWM 仍能保持较高成功率。
*   **抗干扰能力**：通过随机剪掉 50%-60% 的视觉 Token，MWM 的性能下降速度慢于基准模型，证明其特征更集中于关键几何结构。
*   **部署优势**：尽管训练需要掩码，但推理时完全摆脱了对外部分割模型的依赖。

### 7. 优点
*   **信息瓶颈设计巧妙**：通过掩码预测有效地过滤了视频中的高频噪声和无关细节。
*   **端到端集成**：将世界模型的预测特征直接作为策略输入，而非仅仅作为辅助任务，增强了特征的控制效用。
*   **实战价值高**：在真机实验中表现出的视觉鲁棒性（抗光照、抗背景干扰）对实际工业应用具有重要意义。

### 8. 不足与局限
*   **依赖离线标注**：训练阶段仍需高质量的语义掩码。虽然文中使用了 RoboEngine 自动标注，但在处理全新、未定义类别的物体时可能存在瓶颈。
*   **任务复杂度限制**：实验主要集中在桌面级操纵任务，尚未在全身控制或更广阔的户外动态场景中验证。
*   **计算开销**：作为基于扩散模型的策略，其推理时的多步采样可能在对实时性要求极高的场景下受限。

（完）
