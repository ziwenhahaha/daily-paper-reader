---
title: "EmbodiedMidtrain: Bridging the Gap between Vision-Language Models and Vision-Language-Action Models via Mid-training"
title_zh: EmbodiedMidtrain：通过中段训练弥合视觉语言模型与视觉语言动作模型之间的差距
authors: "Yiyang Du, Zhanqiu Guo, Xin Ye, Liu Ren, Chenyan Xiong"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.20012v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 通过中期训练衔接 VLM 与 VLA
tldr: 针对视觉语言模型（VLM）与具身智能动作模型（VLA）之间的分布差异，本文提出 EmbodiedMidtrain 框架。该方法通过轻量级邻近估计器从大规模 VLM 数据中筛选出与具身任务高度对齐的数据进行“中场训练”，为后续 VLA 微调提供更强的初始化。实验证明该方法在多个机器人操控基准上显著提升了性能，甚至优于更大规模或更高训练预算的专家模型。
selection_source: fresh_fetch
motivation: 现有的 VLA 模型多直接使用未针对具身领域优化的通用 VLM，导致两者之间存在显著的数据分布鸿沟，限制了下游任务表现。
method: 构建了一个中场训练数据引擎，利用可学习的邻近估计器从 VLM 数据池中筛选出与 VLA 任务最相关的样本，在正式微调前对 VLM 进行预热训练。
result: 在三个机器人操控基准上的实验显示，该方法在不同骨干网络上均取得一致提升，性能可媲美更大规模或更高训练预算的专家模型。
conclusion: 中场训练通过捕捉空间推理等关键对齐信号，为 VLA 提供了更优的初始化权重，是弥合通用视觉语言能力与具身动作能力差距的有效途径。
---

## 摘要
视觉语言动作模型 (VLA) 从视觉语言模型 (VLM) 中继承了视觉和语言能力，但大多数 VLA 是基于未经具身领域适配的现成 VLM 构建的，这限制了它们的下游性能。在这项工作中，我们提出了 EmbodiedMidtrain，旨在弥合 VLM 与 VLA 之间的差距。我们首先刻画了两者之间的数据分布差异，结果表明 VLA 数据占据了与更广泛的 VLM 分布基本分离的紧凑区域，而对齐程度在不同 VLM 数据源之间以及同一数据源内部都存在显著差异。随后，我们构建了一个中段训练数据引擎，利用轻量级的可学习邻近度估计器从庞大的 VLM 池中筛选出与 VLA 最对齐的候选样本，并在下游 VLA 微调之前，使用这些精选的混合数据对 VLM 进行中段训练。在三个机器人操控基准上的实验表明，中段训练在不同的 VLM 骨干网络上均能持续提升性能，其结果可与专家级 VLA 以及具有更大模型规模和训练预算的现成 VLM 相媲美。进一步的分析表明，中段训练为 VLA 微调提供了更强大的初始化，其增益从训练初期就开始显现，并在整个训练过程中不断扩大。此外，该数据引擎能够捕捉数据集级和样本级的对齐信号，在保留 VLM 数据多样性的同时，更倾向于空间推理任务而非以文本为中心的任务。我们将发布所有代码、数据和模型以供后续研究。

## Abstract
Vision-Language-Action Models (VLAs) inherit their visual and linguistic capabilities from Vision-Language Models (VLMs), yet most VLAs are built from off-the-shelf VLMs that are not adapted to the embodied domain, limiting their downstream performance. In this work, we propose EmbodiedMidtrain to bridge the gap between VLMs and VLAs. We first characterize the data distribution gap between them, showing that VLA data occupy compact regions that are largely separated from the broader VLM distribution, while the degree of alignment varies substantially both across and within VLM data sources. Then, we build a mid-training data engine that leverages a lightweight learnable proximity estimator to select the most VLA-aligned candidates from a large VLM pool, and mid-trains the VLM on this curated mixture before downstream VLA fine-tuning. Experiments on three robot manipulation benchmarks show that mid-training consistently improves performance across different VLM backbones, achieving results competitive with expert VLAs and off-the-shelf VLMs trained with larger model scale and training budgets. Further analysis reveals that mid-training provides a stronger initialization for VLA fine-tuning, with gains emerging from the earliest steps and widening throughout training. Moreover, the data engine captures both dataset-level and sample-level alignment signals, favoring spatial reasoning over text-centric tasks while preserving the diversity of the VLM data. We will release all code, data and models for future research.

---

## 论文详细总结（自动生成）

这篇论文提出了 **EmbodiedMidtrain**，一个旨在弥合通用视觉语言模型（VLM）与具身智能动作模型（VLA）之间分布鸿沟的中段训练（Mid-training）框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 VLA 模型通常直接采用现成的（Off-the-shelf）通用 VLM 作为骨干网络。然而，通用 VLM 的预训练数据（如网页图文对、问答）与机器人操控所需的具身数据（如动作轨迹、物理交互）之间存在巨大的**分布差异（Distribution Gap）**。
*   **研究动机**：直接在具身数据上微调通用 VLM 往往难以充分发挥其潜力。作者发现 VLA 数据在特征空间中呈紧凑分布，且与广阔的 VLM 数据分布高度分离。因此，需要一个中间阶段（Mid-training）来调整 VLM 的分布，使其在进入正式的动作生成训练前，先具备更强的具身感知和推理能力。

### 2. 方法论（核心思想与技术细节）
*   **核心思想**：构建一个“数据引擎”，从海量的通用 VLM 数据中筛选出与具身领域最相关的样本，在 VLA 微调之前对 VLM 进行一次“预热”训练。
*   **关键技术：邻近度估计器（Proximity Estimator）**：
    *   **分类器构建**：在冻结的 VLM 特征层之上添加一个轻量级的线性层（二分类器）。
    *   **训练目标**：将 VLA 数据视为正样本，VLM 数据视为负样本，通过交叉熵损失训练该分类器。
    *   **数据筛选**：利用训练好的分类器对 VLM 候选池进行打分，得分越高表示该样本在分布上越接近具身领域。
*   **算法流程**：
    1.  **特征提取**：使用 VLM 提取 VLA 和 VLM 数据的隐藏层特征。
    2.  **估计器训练**：训练轻量级分类器学习两者的分布边界。
    3.  **Top-K 筛选**：从 VLM 池中选出得分最高的子集（如 120 万条样本）。
    4.  **中段训练**：在筛选出的数据混合物上对 VLM 进行全参数微调。
    5.  **VLA 微调**：在目标机器人数据集上进行最终的动作生成训练。

### 3. 实验设计
*   **数据集与场景**：
    *   **Calvin ABC-D**：测试模型在不同场景配置下的多任务泛化能力。
    *   **SimplerEnv Bridge**：一个真实到模拟（Real-to-Sim）的基准，评估桌面操控任务。
    *   **LIBERO-10**：包含 10 个长程（Long-horizon）任务，极具挑战性。
*   **对比方法**：
    *   **专家 VLA 模型**：OpenVLA (7.7B), $\pi_0$ (3.1B)。
    *   **现成 VLM 基准**：Qwen2.5VL, Qwen3VL (2B 到 30B 规模), Paligemma 1/2, KosMos-2。
*   **骨干网络**：主要使用 InternVL3.5-1B 和 Qwen3VL-2B 进行验证。

### 4. 资源与算力
*   **算力平台**：论文提到使用了 CMU 的 FLAME 中心资源，以及美国国家超级计算应用中心（NCSA）的 Delta 和 DeltaAI 集群（ACCESS 项目支持）。
*   **训练细节**：
    *   **中段训练**：全局 Batch Size 为 256，训练 5,000 步。
    *   **VLA 微调**：全局 Batch Size 为 256，训练 16,000 步。
    *   **精度**：使用 `bfloat16`。
*   **GPU 型号**：文中未明确标注具体的 GPU 型号（如 A100 或 H100），但从集群名称和任务规模推断为高性能计算集群。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流机器人操控基准，对比了超过 10 种不同规模和架构的模型。
*   **消融实验**：
    *   对比了**随机筛选**与**邻近度筛选**的效果。
    *   对比了不同的邻近度度量指标（如特征空间距离、困惑度 PPL 等）。
    *   验证了筛选出的数据在不同架构（InternVL 到 Qwen）之间的**可迁移性**。
*   **充分性评价**：实验设计非常充分且客观。作者不仅关注最终成功率，还分析了训练动力学（Loss 曲线与性能演变）以及数据多样性，证明了提升并非来自简单的过拟合。

### 6. 主要结论与发现
*   **性能飞跃**：经过中段训练的 1.1B 模型在 Calvin 基准上超过了 7.7B 的 OpenVLA，并在多个指标上媲美 30B 规模的模型。
*   **初始化至关重要**：中段训练提供了一个更优的初始化权重，使得 VLA 微调从第一步起就具有更高起点，且领先优势随训练持续扩大。
*   **数据偏好**：邻近度估计器自动倾向于选择包含**空间推理、坐标定位**的样本（如 RefSpatial 数据集），而排除了纯文本识别（如书名识别）等无关任务。
*   **跨模型通用性**：使用 InternVL 筛选的数据同样能显著提升 Qwen 模型的性能，说明具身对齐信号具有通用性。

### 7. 优点
*   **数据驱动的优雅方案**：不改变模型架构，通过优化训练数据分布解决领域鸿沟问题，具有极强的普适性。
*   **高效轻量**：邻近度估计器非常轻量，训练和推理成本极低，易于在大规模数据集上扩展。
*   **深刻的洞察**：通过可视化和分布分析，定量地揭示了 VLM 与 VLA 之间的“鸿沟”本质。

### 8. 不足与局限
*   **评估环境局限于模拟器**：虽然使用了 SimplerEnv 这种 Real-to-Sim 基准，但缺乏在真实物理机器人上的直接验证。
*   **目标数据依赖**：邻近度估计器的训练需要一定量的目标 VLA 数据作为参考，对于完全没有标注数据的全新具身领域，其效果可能受限。
*   **任务覆盖面**：实验主要集中在机械臂操控（Manipulation），尚未探讨该方法在足式机器人导航或复杂全身协调任务中的表现。

（完）
