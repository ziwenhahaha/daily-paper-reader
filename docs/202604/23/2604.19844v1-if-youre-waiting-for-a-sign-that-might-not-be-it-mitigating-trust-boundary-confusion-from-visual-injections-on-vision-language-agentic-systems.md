---
title: "If you're waiting for a sign... that might not be it! Mitigating Trust Boundary Confusion from Visual Injections on Vision-Language Agentic Systems"
title_zh: 如果你在等待一个信号……那可能不是它！缓解视觉-语言智能体系统中视觉注入导致的信任边界混淆
authors: "Jiamin Chang, Minhui Xue, Ruoxi Sun, Shuchao Pang, Salil S. Kanhere, Hammond Pearce"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19844v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 视觉-语言智能体系统（VLAS）的安全与信任
tldr: 本研究探讨了视觉语言智能体（VLAS）在处理环境信号时面临的“信任边界混淆”问题，即难以区分合法的环境指令（如红绿灯）与恶意的视觉注入攻击。作者构建了双重意图数据集和评估框架，发现现有模型在平衡响应合法信号与抵御误导信号方面表现不佳。为此，提出了一种将感知与决策分离的多智能体防御框架，在保持正确响应的同时显著提升了系统的安全性与鲁棒性。
selection_source: fresh_fetch
motivation: 视觉语言智能体在区分合法的环境信号与恶意的视觉注入攻击时存在信任边界混淆，导致安全风险。
method: 提出了一个双重意图评估框架，并设计了一种将感知与决策分离的多智能体防御机制来动态评估视觉输入的可靠性。
result: 实验表明现有LVLM智能体极易受视觉注入影响，而所提防御框架能有效减少误导行为并保持对合法信号的正确响应。
conclusion: 通过分离感知与决策过程，可以有效缓解视觉语言智能体中的信任边界混淆问题，增强其在复杂环境下的鲁棒性。
---

## 摘要
由大型视觉-语言模型（LVLM）驱动的具身视觉-语言智能体系统（VLAS）的最新进展，使 AI 系统能够感知现实场景并进行推理。在此背景下，交通信号灯等环境信号是关键的带内信号，能够且应当影响智能体的行为。然而，类似的信号也可能被精心设计为误导性的视觉注入，从而覆盖用户意图并带来安全风险。这种双重性带来了一个根本性的挑战：智能体必须对合法的环境线索做出反应，同时对误导性线索保持鲁棒。我们将这种矛盾称为“信任边界混淆”。为了研究这一行为，我们设计了一个双重意图数据集和评估框架，结果表明当前的基于 LVLM 的智能体无法可靠地平衡这种权衡，表现为要么忽略有用信号，要么遵循有害信号。我们在多种具身设置下，针对基于结构和基于噪声的视觉注入，系统地评估了 7 个 LVLM 智能体。为了应对这些漏洞，我们提出了一个多智能体防御框架，通过将感知与决策分离来动态评估视觉输入的可靠性。我们的方法在保留正确响应的同时显著减少了误导性行为，并提供了对抗扰动下的鲁棒性保证。评估框架的代码和产物已在 https://anonymous.4open.science/r/Visual-Prompt-Inject 公开。

## Abstract
Recent advances in embodied Vision-Language Agentic Systems (VLAS), powered by large vision-language models (LVLMs), enable AI systems to perceive and reason over real-world scenes. Within this context, environmental signals such as traffic lights are essential in-band signals that can and should influence agent behavior. However, similar signals could also be crafted to operate as misleading visual injections, overriding user intent and posing security risks. This duality creates a fundamental challenge: agents must respond to legitimate environmental cues while remaining robust to misleading ones. We refer to this tension as trust boundary confusion. To study this behavior, we design a dual-intent dataset and evaluation framework, through which we show that current LVLM-based agents fail to reliably balance this trade-off, either ignoring useful signals or following harmful ones. We systematically evaluate 7 LVLM agents across multiple embodied settings under both structure-based and noise-based visual injections. To address these vulnerabilities, we propose a multi-agent defense framework that separates perception from decision-making to dynamically assess the reliability of visual inputs. Our approach significantly reduces misleading behaviors while preserving correct responses and provides robustness guarantees under adversarial perturbations. The code of the evaluation framework and artifacts are made available at https://anonymous.4open.science/r/Visual-Prompt-Inject.

---

## 论文详细总结（自动生成）

这篇论文对视觉-语言智能体系统（VLAS）中的安全风险进行了深入探讨，以下是对该论文的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着大型视觉-语言模型（LVLM）的发展，具身智能体（如自动驾驶、无人机、机器人）需要同时处理用户指令和环境信号（如交通标志）。
*   **核心问题**：论文识别并定义了**“信任边界混淆”（Trust Boundary Confusion）**现象。即智能体难以区分视觉输入中哪些是合法的安全约束（如“前方施工，请停车”），哪些是恶意的视觉注入攻击（如伪造的“右转”标志以诱导坠崖）。
*   **研究目标**：评估现有 LVLM 智能体在面对冲突意图时的决策偏好，并提出缓解这种混淆的防御机制。

### 2. 方法论
*   **核心思想**：将**感知（Perception）**与**决策（Decision-making）**分离，通过多智能体协作建立动态信任边界。
*   **关键技术细节（多智能体防御框架）**：
    1.  **观察智能体（Observation Agent）**：充当视觉语义解析器，提取图像中所有潜在的指令、文本或符号（$I_v$），克服模型的“模态懒惰”。
    2.  **判断智能体（Judgment Agent）**：核心认知层。接收提取的 $I_v$ 和用户指令 $I_u$，利用上下文学习（ICL）和领域安全准则，判定 $I_v$ 是“有益的约束”还是“误导性的载荷”。
    3.  **执行智能体（Execution Agent）**：根据判断结果生成行动计划。若 $I_v$ 有益，则遵循环境信号；若误导，则忽略 $I_v$ 严格执行 $I_u$。
*   **攻击向量**：
    *   **结构化注入**：利用语义增强（如添加“忽略之前指令”前缀、警告图标等）来提升视觉指令的优先级。
    *   **噪声化注入**：使用投影梯度下降（PGD）优化像素级扰动，使视觉特征在潜空间中强制对齐恶意目标。

### 3. 实验设计
*   **数据集与场景**：
    *   **图像编辑（数字域）**：基于 InstructPix2Pix 构建，包含 2500 个测试用例，涉及版权警告（有益）和恶意编辑（误导）。
    *   **具身操控（物理域）**：基于 MSS 基准，涵盖自动驾驶、无人机紧急着陆和机器人抓取场景。
*   **对比方法**：评估了 7 个主流 LVLM（GPT-4o, GPT-5, Gemini-1.5-Pro, Claude-3.5-Sonnet, Qwen2.5-VL-7B, InternVL3-8B, DeepSeek-VL-7B）。
*   **基准测试**：对比了原始模型、传统 OCR 增强、简单过滤机制以及论文提出的多智能体框架。

### 4. 资源与算力
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量。
*   **实现细节**：提到对于开源模型（如 Qwen, InternVL），由于计算限制，实验集中在 8B 参数以下的规模。噪声注入实验中使用了 2000 步的 PGD 优化。大部分闭源模型通过 API 进行评估。

### 5. 实验数量与充分性
*   **实验规模**：
    *   系统评估了 7 种不同架构的模型。
    *   涵盖了从数字图像处理到物理机器人操控的多种跨维度场景。
    *   进行了消融实验（如仅使用观察智能体 vs. 完整框架）。
*   **充分性评价**：实验设计较为充分且客观。通过“双重意图”设计（同时测试有益和误导信号），避免了防御机制走向“一刀切”的极端（即盲目拒绝所有视觉信号），体现了公平性。

### 6. 主要结论与发现
*   **模态懒惰（Modality Laziness）**：许多模型虽然能识别视觉文本，但在决策时倾向于忽略它，盲目遵循文本指令。
*   **能力与风险并存**：感知能力越强的模型（如 GPT-4o），越容易受到视觉注入的影响，表现出更高的“信任边界混淆”。
*   **防御效果**：提出的多智能体框架将误导性攻击的成功率降低至 **3%** 以下，同时保留了超过 **95%** 的有益环境信号响应。
*   **白盒 vs. 黑盒**：噪声注入在白盒条件下极具威胁，但跨模型的迁移性较差，进一步证实了模型的模态偏好。

### 7. 优点
*   **理论创新**：首次形式化定义了 VLAS 中的“信任边界混淆”问题。
*   **数据集价值**：构建了首个区分“有益”与“误导”意图的视觉注入数据集。
*   **防御实用性**：多智能体框架不依赖于重新训练模型，具有良好的模型无关性（Model-agnostic）和可解释性。
*   **现实迁移**：通过 Sim-to-Real 框架在实体机器人上验证了漏洞的真实存在。

### 8. 不足与局限
*   **推理延迟**：多智能体框架需要三次连续的模型调用，这在自动驾驶等实时性要求极高的场景中可能存在延迟瓶颈。
*   **策略定制化**：判断智能体依赖于预设的安全准则，对于医疗机器人等极度专业的领域，可能需要更精细的策略微调。
*   **自适应攻击风险**：攻击者未来可能会针对“判断智能体”的逻辑进行专门的对抗性设计。

（完）
