---
title: A Hierarchical Spatiotemporal Action Tokenizer for In-Context Imitation Learning in Robotics
title_zh: 一种用于机器人上下文内模仿学习的分层时空动作分词器
authors: "Fawad Javed Fateh, Ali Shah Ali, Murad Popattia, Usman Nizamani, Andrey Konin, M. Zeeshan Zia, Quoc-Huy Tran"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15215v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 用于机器人上下文模仿学习的动作分词器
tldr: 本文提出了一种用于机器人上下文模仿学习的分层时空动作分词器（HiST-AT）。该方法通过两级矢量量化将动作映射为细粒度子簇和高层簇，并结合空间重构与时间戳预测来捕捉动作的时空特征。在多个仿真和真实机器人操作基准测试中，HiST-AT 显著优于非分层方法，达到了新的 SOTA 性能，证明了分层时空建模在复杂任务中的有效性。
selection_source: fresh_fetch
motivation: 旨在解决现有动作分词方法在上下文模仿学习中对复杂时空信息建模能力不足的问题。
method: 提出一种双层矢量量化架构，通过多级聚类同时恢复动作数据及其关联的时间戳信息。
result: 在多项仿真和真实机器人操作实验中，该方法均取得了当前最佳的性能表现。
conclusion: 分层时空动作分词是提升机器人上下文模仿学习效率和准确性的关键技术路径。
---

## 摘要
我们提出了一种用于上下文内模仿学习的新型分层时空动作分词器。我们首先提出了一种分层方法，该方法由两个连续层级的向量量化组成。具体而言，底层将输入动作分配给细粒度的子簇，而高层则进一步将细粒度子簇映射到簇。我们的分层方法优于非分层方法，同时主要通过重构输入动作来利用空间信息。此外，我们通过利用空间和时间线索扩展了我们的方法，形成了一个分层时空动作分词器，即 HiST-AT。具体来说，我们的分层时空方法在进行多级聚类的同时，恢复输入动作及其相关的时间戳。最后，在多个仿真和真实机器人操作基准上的广泛评估表明，我们的方法在上下文内模仿学习中达到了新的最先进性能。

## Abstract
We present a novel hierarchical spatiotemporal action tokenizer for in-context imitation learning. We first propose a hierarchical approach, which consists of two successive levels of vector quantization. In particular, the lower level assigns input actions to fine-grained subclusters, while the higher level further maps fine-grained subclusters to clusters. Our hierarchical approach outperforms the non-hierarchical counterpart, while mainly exploiting spatial information by reconstructing input actions. Furthermore, we extend our approach by utilizing both spatial and temporal cues, forming a hierarchical spatiotemporal action tokenizer, namely HiST-AT. Specifically, our hierarchical spatiotemporal approach conducts multi-level clustering, while simultaneously recovering input actions and their associated timestamps. Finally, extensive evaluations on multiple simulation and real robotic manipulation benchmarks show that our approach establishes a new state-of-the-art performance in in-context imitation learning.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **HiST-AT**（Hierarchical Spatiotemporal Action Tokenizer）的新型动作分词器，旨在提升机器人**上下文内模仿学习（In-Context Imitation Learning, ICIL）**的性能。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在上下文模仿学习中，机器人需要通过推理时提供的少量演示（Prompt）来执行新任务。现有的动作分词器（Action Tokenizer）通常采用“扁平化”的聚类方式，且主要关注空间信息的重构，忽略了动作内部的分层结构以及时间上的连续性和相关性。
*   **研究背景**：虽然大语言模型（LLM）的上下文学习能力很强，但将其迁移到机器人领域时，如何有效地将连续的机器人动作离散化为包含丰富语义和时空信息的“Token”仍是一个巨大挑战。

### 2. 方法论
HiST-AT 的核心思想是结合**分层矢量量化（Hierarchical VQ）**与**时空联合重构**。
*   **分层聚类（Hierarchical Clustering）**：
    *   **底层（Subcluster）**：将输入动作映射到细粒度的子簇原型（Codebook $Z$），捕捉短期的动作基元。
    *   **高层（Cluster）**：将底层的输出进一步映射到高层簇原型（Codebook $A$），形成长期的连贯动作表示。
*   **时空重构（Spatiotemporal Reconstruction）**：
    *   **空间维度**：通过空间解码器重构输入的机器人动作（位置、角度等）。
    *   **时间维度**：通过时间解码器预测动作对应的时间戳（Timestamp），强制模型学习动作随时间演变的动态特征。
*   **平滑性约束**：引入 Lipschitz 正则化网络，确保生成的动作 Token 在时间上是平滑的，减少机器人执行时的抖动。
*   **损失函数**：综合了量化损失（Commitment & Codebook Loss）、空间重构 MSE、时间重构 MSE 以及 Lipschitz 正则化损失。

### 3. 实验设计
*   **数据集/场景**：
    *   **RoboCasa**：包含 7 类厨房操作任务（如取放、开门、转动旋钮等）。
    *   **ManiSkill**：包含 3 类基础操作任务（推方块、堆叠方块等）。
    *   **真实世界**：在实际机器人平台上进行了验证（详细结果见补充材料）。
*   **Benchmark 与对比方法**：
    *   **基准框架**：以 ICRT 为基础框架。
    *   **对比分词器**：包括 MLP、离散分箱（Binning）、FAST、VQ-VAE、LFQ-VAE 以及当前的 SOTA 方法 LipVQ-VAE。
    *   **对比策略**：BC-Transformer、ACT、MCR 等主流模仿学习算法。

### 4. 资源与算力
*   **硬件**：使用了单张 **NVIDIA A100 GPU**。
*   **训练时长**：
    *   在 RoboCasa 模拟器上训练了 **500K 次迭代**。
    *   在 ManiSkill 上训练了 **30K 次迭代**。
*   **模型规模**：使用了 ResNet-18 作为视觉骨干网络，Transformer 作为核心策略模型。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括：
    *   **标准性能测试**：在两个大型仿真环境的多个任务上进行成功率对比。
    *   **消融实验**：分别验证了“分层结构”和“时空重构”各自的贡献。
    *   **跨数据集泛化（Cross-Dataset）**：从 MimicGen 数据集迁移到 Human 数据集，验证鲁棒性。
    *   **零样本泛化（Zero-Shot）**：在未见过的任务组合上测试。
*   **客观性**：实验涵盖了从基础聚类到最新 SOTA 的全面对比，且通过跨数据集和零样本实验证明了方法的泛化能力，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能提升**：HiST-AT 在 RoboCasa 上的平均成功率达到 **59%**，显著高于之前最佳方法 LipVQ-VAE 的 53%。
*   **分层优势**：分层聚类比扁平聚类更能捕捉复杂的动作结构，尤其在需要精细操作的任务中表现突出。
*   **时空协同**：同时重构动作和时间戳能显著增强 Token 的代表性，比单纯的空间重构更有效。
*   **泛化能力**：在零样本任务中，HiST-AT 的表现（11.4%）远超 LipVQ-VAE（5.2%），证明了其学习到的动作表示具有更强的通用性。

### 7. 优点
*   **结构创新**：首次将分层时空向量量化引入机器人动作分词领域。
*   **时空融合**：通过预测时间戳这一简单而有效的方式，解决了动作 Token 缺乏时间上下文的问题。
*   **强泛化性**：实验证明该方法在处理未见过的数据分布和任务时具有明显优势。

### 8. 不足与局限
*   **超参数敏感性**：实验显示时间重构权重 $\lambda_{temp}$ 对结果影响较大，需要针对不同环境进行精细调优。
*   **计算复杂度**：虽然使用了单卡训练，但双层 VQ 和双解码器结构相比简单分词器增加了计算开销。
*   **长程任务挑战**：尽管在短期操作上表现优异，但在极长序列或需要极高逻辑推理的复杂长程任务中的表现仍有待进一步验证。

（完）
