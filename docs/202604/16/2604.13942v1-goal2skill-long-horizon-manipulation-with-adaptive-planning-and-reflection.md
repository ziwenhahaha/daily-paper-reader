---
title: "Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection"
title_zh: Goal2Skill：具有自适应规划与反思能力的长程操纵
authors: "Zhen Liu, Xinyu Ning, Zhe Hu, Xinxin Xie, Weize Li, Zhipeng Tang, Chongyu Wang, Zejun Yang, Hanlin Wang, Yitong Liu, Zhongzhu Pu"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13942v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于长程具身操纵的VLA系统
tldr: 针对现有VLA系统在长程、依赖记忆的任务中表现脆弱的问题，本文提出Goal2Skill框架。该框架采用双系统架构，将高层语义推理（VLM规划器）与底层电机执行（VLA控制器）分离。通过引入结构化任务记忆、目标分解及闭环错误恢复机制，显著提升了机器人在复杂环境下的操作鲁棒性，在RMBench基准测试中表现远超现有基准。
selection_source: fresh_fetch
motivation: 现有的端到端VLA策略在处理具有部分可观测性和多阶段依赖的长程任务时，缺乏持久记忆和自适应纠错能力。
method: 提出一种结合VLM高层规划（负责分解与验证）与VLA底层执行（基于扩散模型的动作生成）的闭环双系统框架。
result: "在RMBench任务中，该框架将平均成功率从最强基准的9.8%大幅提升至32.4%，展现了极强的长程操作能力。"
conclusion: 实验证明，结构化记忆与闭环恢复机制是实现稳健长程具身操纵的关键。
---

## 摘要
最近的视觉-语言-动作（VLA）系统在具身操纵中展现了强大的能力。然而，大多数现有的 VLA 策略依赖于有限的观测窗口和端到端动作预测，这使得它们在具有部分可观测性、遮挡和多阶段依赖的长程、依赖记忆的任务中表现脆弱。此类任务不仅需要精确的视觉运动控制，还需要持久记忆、自适应任务分解以及从执行失败中显式恢复的能力。为了解决这些局限性，我们提出了一种用于长程具身操纵的双系统框架。该框架将高层语义推理与底层运动执行显式分离。高层规划器实现为基于视觉语言模型（VLM）的智能体模块，负责维护结构化任务记忆，并执行目标分解、结果验证和错误驱动的纠正。底层执行器实例化为基于 VLA 的视觉运动控制器，通过以保持几何特征的过滤观测为条件的扩散动作生成来执行每个子任务。这两个系统共同构成了规划与执行之间的闭环，实现了记忆感知推理、自适应重新规划和鲁棒的在线恢复。在代表性 RMBench 任务上的实验表明，所提出的框架显著优于代表性基线模型，平均成功率达到 32.4%，而最强基线仅为 9.8%。消融研究进一步证实了结构化记忆和闭环恢复对于长程操纵的重要性。

## Abstract
Recent vision-language-action (VLA) systems have demonstrated strong capabilities in embodied manipulation. However, most existing VLA policies rely on limited observation windows and end-to-end action prediction, which makes them brittle in long-horizon, memory-dependent tasks with partial observability, occlusions, and multi-stage dependencies. Such tasks require not only precise visuomotor control, but also persistent memory, adaptive task decomposition, and explicit recovery from execution failures. To address these limitations, we propose a dual-system framework for long-horizon embodied manipulation.   Our framework explicitly separates high-level semantic reasoning from low-level motor execution. A high-level planner, implemented as a VLM-based agentic module, maintains structured task memory and performs goal decomposition, outcome verification, and error-driven correction. A low-level executor, instantiated as a VLA-based visuomotor controller, carries out each sub-task through diffusion-based action generation conditioned on geometry-preserving filtered observations. Together, the two systems form a closed loop between planning and execution, enabling memory-aware reasoning, adaptive replanning, and robust online recovery. Experiments on representative RMBench tasks show that the proposed framework substantially outperforms representative baselines, achieving a 32.4% average success rate compared with 9.8% for the strongest baseline. Ablation studies further confirm the importance of structured memory and closed-loop recovery for long-horizon manipulation.

---

## 论文详细总结（自动生成）

以下是对论文《Goal2Skill: Long-Horizon Manipulation with Adaptive Planning and Reflection》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
现有的视觉-语言-动作（VLA）模型虽然在短程指令遵循和多任务控制方面取得了进展，但在**长程（Long-Horizon）具身操纵**任务中表现脆弱。
*   **核心挑战**：长程任务通常具有部分可观测性、物体遮挡和复杂的多阶段依赖关系。
*   **现有局限**：大多数 VLA 策略采用端到端的反应式（Reactive）预测，缺乏持久的任务记忆、自适应的任务分解以及从执行失败中恢复的显式机制。
*   **研究目标**：构建一个能够像人类大脑（大脑皮层负责规划，小脑负责执行）一样工作的双系统框架，实现具备记忆感知推理和闭环纠错能力的长程操纵。

### 2. 方法论：核心思想与关键技术
论文提出了 **Goal2Skill** 框架，将决策过程分解为高层规划器（High-Level Planner）和底层执行器（Low-Level Executor）。

*   **高层规划器（VLM-based）**：
    *   **任务分解**：将全局目标 $G$ 分解为一系列结构化子任务 $\tau_k$，每个子任务包含自然语言指令、预设条件、后置条件、空间约束 $B_k$ 和技能索引。
    *   **结构化记忆管理**：维护一个记忆状态 $M_t = \{H_t, W_t, E_t\}$，其中 $H_t$ 是情节历史，$W_t$ 是对世界状态的自然语言总结（工作记忆），$E_t$ 是记录失败原因的错误寄存器。
    *   **验证与反思**：在子任务结束后，通过 VLM 验证后置条件。若失败，反思引擎（Reflection Engine）会诊断原因并决定是重试、调整参数还是重新规划。
*   **底层执行器（VLA-based）**：
    *   **几何导向感知**：利用高层提供的空间约束 $B_k$ 生成掩码，过滤掉环境中的干扰物（Distractors），生成保持几何特征的观测图像 $\hat{I}_t$。
    *   **扩散技能库**：基于扩散模型（Diffusion-based）的视觉运动控制器，根据过滤后的图像和指令生成连续的动作序列。

### 3. 实验设计
*   **基准测试**：使用 **RMBench**（一个专门针对依赖记忆的机器人操纵基准）。
*   **实验场景**：选取了 5 个代表性任务，分为 M(1)（低记忆需求）和 M(n)（高记忆需求）两类，包括：观察并抓取、重排积木、电池安装尝试、积木排序尝试、按按钮。
*   **对比方法**：
    *   非预训练策略：**DP** (Diffusion Policy), **ACT** (Action Chunking Transformer)。
    *   预训练 VLA 策略：**Pi0.5**, **X-VLA**。
*   **评估指标**：任务成功率（Success Rate）。

### 4. 资源与算力
*   **训练数据**：每个任务使用 50 个专家演示。
*   **训练细节**：模型训练了 30k 个优化步数（Optimization Steps）。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。

### 5. 实验数量与充分性
*   **实验规模**：每个任务进行了 100 次 Rollout 测试，以确保成功率统计的客观性。
*   **消融实验**：
    *   **记忆消融**：分别验证了情节历史（$H_t$）和工作记忆（$W_t$）对长程任务的影响。
    *   **恢复机制消融**：验证了验证模块（$\Phi_{verify}$）和反思模块（$\Phi_{reflect}$）在应对执行失败时的作用。
*   **充分性评价**：实验设计较为全面，涵盖了从基础性能对比到核心模块贡献的深度分析，对比基线均为当前领域内的代表性模型，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **性能飞跃**：Goal2Skill 的平均成功率达到 **32.4%**，远高于最强基线（X-VLA 和 ACT 的 9.8%）。
*   **记忆的重要性**：在 M(n) 类高难度任务中，Goal2Skill 的优势更明显（38.7% vs 9.0%），证明了结构化记忆是处理长程依赖的关键。
*   **闭环恢复的价值**：消融实验显示，引入验证和反思机制后，任务成功率从 8.0% 提升至 28.0%，证明了显式纠错比单纯提升动作预测精度更有效。

### 7. 优点与亮点
*   **解耦设计**：成功将高层语义推理与底层精细控制分离，降低了端到端模型在长程任务中的学习难度。
*   **主动记忆机制**：记忆不再是简单的存储，而是作为验证、诊断和恢复的主动基质。
*   **感知过滤**：通过高层引导的掩码过滤干扰物，显著增强了底层执行器在复杂、杂乱环境中的鲁棒性。

### 8. 不足与局限
*   **绝对成功率仍有提升空间**：虽然远超基线，但 32.4% 的平均成功率说明在复杂具身任务中仍存在巨大挑战。
*   **VLM 依赖风险**：高层规划和验证高度依赖 VLM 的推理能力，若 VLM 产生幻觉（Hallucination）或误判，可能导致错误的恢复策略。
*   **任务覆盖范围**：实验主要集中在 RMBench 的子集上，尚未在更广泛的真实世界非结构化场景或更多样化的机器人形态上进行大规模验证。

（完）
