---
title: "UMI-3D: Extending Universal Manipulation Interface from Vision-Limited to 3D Spatial Perception"
title_zh: UMI-3D：将通用操作接口从视觉受限扩展到 3D 空间感知
authors: Ziming Wang
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14089v1"
tags: ["query:profile-2"]
score: 9.0
evidence: 使用激光雷达和视觉进行具身操作数据采集的多模态扩展
tldr: UMI-3D是对通用操作接口（UMI）的增强，旨在解决原系统在遮挡和动态场景下视觉SLAM不稳定的问题。通过在手腕接口集成轻量级LiDAR传感器，实现了高精度的3D空间感知和位姿估计。该系统结合了硬件同步的多模态感知流水线和时空校准框架，显著提升了数据采集的质量和可靠性。实验证明，UMI-3D在处理大变形物体和关节物体等复杂任务上表现优异，且保持了原系统的便携性并已开源。
selection_source: fresh_fetch
motivation: 原始UMI依赖单目视觉SLAM，在遮挡、动态场景或跟踪失败时表现脆弱，限制了其在复杂现实环境中的应用。
method: 在手腕端集成轻量级LiDAR，引入以LiDAR为中心的SLAM算法和多模态时空校准框架，实现精确的度量尺度位姿估计。
result: 实验显示UMI-3D显著提高了数据采集的可靠性，并成功实现了大变形物体和关节物体操作等视觉方案难以完成的任务。
conclusion: UMI-3D通过引入3D空间感知，在保持便携性的同时大幅增强了具身智能数据采集的鲁棒性，为大规模复杂任务学习提供了有力支持。
---

## 摘要
我们提出了 UMI-3D，这是通用操作接口（Universal Manipulation Interface, UMI）的多模态扩展，旨在为具身操作提供鲁棒且可扩展的数据采集。虽然 UMI 实现了便携式、腕戴式的数据获取，但其对单目视觉 SLAM 的依赖使其容易受到遮挡、动态场景和跟踪失败的影响，限制了其在真实环境中的适用性。UMI-3D 通过引入一个紧密集成在腕戴式接口中的轻量级、低成本 LiDAR 传感器解决了这些局限性，从而在挑战性条件下实现以 LiDAR 为中心的 SLAM，并提供精确的米级尺度位姿估计。我们进一步开发了硬件同步的多模态传感流水线和统一的时空标定框架，将视觉观测与 LiDAR 点云对齐，生成一致的演示 3D 表示。尽管保留了原始的 2D 视觉运动策略公式，UMI-3D 显著提高了采集数据的质量和可靠性，这直接转化为增强的策略性能。广泛的真实世界实验表明，UMI-3D 不仅在标准操作任务上取得了很高的成功率，而且能够学习对于原始仅视觉 UMI 设置而言具有挑战性或不可行的任务，包括大型变形物体操作和关节物体操作。该系统支持数据采集、对齐、训练和部署的端到端流程，同时保留了原始 UMI 的便携性和易用性。所有硬件和软件组件均已开源，以促进大规模数据采集并加速具身智能研究：https://umi-3d.github.io。

## Abstract
We present UMI-3D, a multimodal extension of the Universal Manipulation Interface (UMI) for robust and scalable data collection in embodied manipulation. While UMI enables portable, wrist-mounted data acquisition, its reliance on monocular visual SLAM makes it vulnerable to occlusions, dynamic scenes, and tracking failures, limiting its applicability in real-world environments. UMI-3D addresses these limitations by introducing a lightweight and low-cost LiDAR sensor tightly integrated into the wrist-mounted interface, enabling LiDAR-centric SLAM with accurate metric-scale pose estimation under challenging conditions. We further develop a hardware-synchronized multimodal sensing pipeline and a unified spatiotemporal calibration framework that aligns visual observations with LiDAR point clouds, producing consistent 3D representations of demonstrations. Despite maintaining the original 2D visuomotor policy formulation, UMI-3D significantly improves the quality and reliability of collected data, which directly translates into enhanced policy performance. Extensive real-world experiments demonstrate that UMI-3D not only achieves high success rates on standard manipulation tasks, but also enables learning of tasks that are challenging or infeasible for the original vision-only UMI setup, including large deformable object manipulation and articulated object operation. The system supports an end-to-end pipeline for data acquisition, alignment, training, and deployment, while preserving the portability and accessibility of the original UMI. All hardware and software components are open-sourced to facilitate large-scale data collection and accelerate research in embodied intelligence: \href{https://umi-3d.github.io}{https://umi-3d.github.io}.

---

## 论文详细总结（自动生成）

这是一份关于论文《UMI-3D: Extending Universal Manipulation Interface from Vision-Limited to 3D Spatial Perception》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：原始的通用操作接口（UMI）虽然通过腕戴式相机实现了便携的数据采集，但其核心依赖于**单目视觉 SLAM**。这种方案在面对无纹理区域（如白墙）、强光变化、动态遮挡（如开门、拉窗帘时物体遮挡相机）以及缺乏视觉特征的场景时极易发生跟踪失败，限制了可采集任务的范围和数据的可靠性。
*   **核心问题**：如何突破视觉 SLAM 的局限，构建一个更鲁棒、具备度量尺度感知能力且能处理复杂动态交互的大规模具身数据采集系统？
*   **整体含义**：UMI-3D 通过引入轻量级 LiDAR，将 SLAM 从“视觉中心”转向“几何中心”，从而显著提升了位姿估计的鲁棒性，扩展了机器人学习复杂、长程及变形物体操作任务的能力。

### 2. 核心方法论
*   **核心思想**：在手腕处集成多模态传感器（LiDAR + 相机 + IMU），利用 LiDAR 的主动深度感知能力实现高精度的空间定位与 3D 重建，同时保持与原 UMI 兼容的视觉策略接口。
*   **关键技术细节**：
    *   **硬件同步（SP1）**：使用 STM32 微控制器作为中心时钟，通过 PPS 信号同步 LiDAR，并以 20Hz 硬件触发工业相机，确保多模态数据在时间上严格对齐。
    *   **时空标定（SP2）**：开发了 `livox2cam` 标定模块，通过特制的标定板实现鱼眼相机与固态 LiDAR 之间的精确外参对齐。
    *   **以 LiDAR 为中心的 SLAM（SP3）**：采用基于流形上的迭代误差状态卡尔曼滤波（ESIKF）的 LiDAR-惯性里程计（LIO）。利用体素化概率平面特征进行扫描匹配，在挑战性环境下提供厘米级的位姿估计。
    *   **数据处理流水线（SP4）**：将 SLAM 轨迹、视频流和夹持器状态（通过 ArUco 标记跟踪）整合进统一的 Zarr 格式回放缓存，供扩散策略（Diffusion Policy）训练。

### 3. 实验设计
*   **实验场景与任务**：
    1.  **杯子摆放（Cup Arrangement）**：评估基础操作与泛化能力（涉及 8x8 种杯子与托盘组合）。
    2.  **拉窗帘（Curtain Pulling）**：评估大范围变形物体操作，此类任务中视觉 SLAM 常因光照和动态遮挡失效。
    3.  **开门并取杯（Door Opening & Cup Placement）**：评估长程、涉及关节类物体的复杂任务。
    4.  **鼠标摆放（Mouse Placement）**：评估跨具身兼容性（直接运行原 UMI 训练的策略）。
*   **Benchmark 与对比**：
    *   主要对比对象是原始的**仅视觉 UMI**。
    *   评估指标采用多阶段评分制（如抓取、放置各占分值），计算累积得分和归一化得分。

### 4. 资源与算力
*   **硬件成本**：BOM 成本约 700 美元（LiDAR $550 + 相机 $100 + 打印件等）。
*   **算力说明**：文中**未明确说明**具体的训练 GPU 型号、数量及具体训练时长。但提到使用了 CLIP ViT-L/14 和 DINOv2 等大模型作为视觉编码器，并采用了扩散策略框架，这通常需要高性能 GPU（如 A100 或 3090/4090 级别）进行数小时至数天的训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **数据量**：杯子任务采集了 3500 条演示，窗帘任务 769 条，开门任务 340 条。
    *   **测试次数**：杯子任务进行了 640 次真实世界测试，窗帘任务 120 次，开门任务 40 次。
*   **充分性与客观性**：实验覆盖了从简单到复杂、从刚体到变形体的多种任务。通过 8x8 的物体组合测试了泛化性，并使用 Sankey 图分析了长程任务的失败传播。实验设计较为客观，涵盖了分布内和分布外的场景。

### 6. 主要结论与发现
*   **鲁棒性提升**：LiDAR SLAM 在视觉特征缺失或剧烈运动下依然稳定，解决了数据采集中的“跟踪失败”瓶颈。
*   **任务扩展**：成功实现了视觉方案难以完成的拉窗帘（强背光/大变形）和开门（严重遮挡）任务。
*   **泛化与兼容**：策略在未见过的物体组合上表现出优雅的性能下降（归一化得分从 0.86 降至 0.73）；同时证明了 UMI-3D 硬件可以无缝运行原 UMI 的策略。

### 7. 优点（亮点）
*   **硬件级同步**：相比于其他依赖软件同步或闭源 SLAM（如 iPhone ARKit）的方案，UMI-3D 的硬件同步更精确，数据一致性更高。
*   **开源精神**：提供了完整的硬件设计、标定工具和软件流水线，极具实用价值。
*   **几何感知**：将 SLAM 视为对齐感知与动作的底层机制，而非仅仅是定位工具，提升了数据采集的上限。

### 8. 不足与局限
*   **硬件负担**：集成 LiDAR 后夹持器重量达到 1120g，长时间手持采集会导致用户疲劳。
*   **3D 信息利用不足**：虽然采集了 3D 点云，但目前的**推理策略仍主要基于 2D 视觉**，LiDAR 主要用于辅助 SLAM 生成高质量标签，未能在推理阶段充分发挥几何信息的潜力。
*   **单臂限制**：目前仅支持单臂操作，无法处理需要双臂协作的复杂任务。
*   **长程任务失败率**：在“开门-取杯-放置”任务中，后期阶段成功率较低（5%），显示出长程任务中的误差累积和运动学约束问题仍待解决。

（完）
