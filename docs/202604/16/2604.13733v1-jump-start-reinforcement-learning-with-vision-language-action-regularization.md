---
title: Jump-Start Reinforcement Learning with Vision-Language-Action Regularization
title_zh: 利用视觉-语言-动作正则化引导强化学习
authors: "Angelo Moroncelli, Roberto Zanetti, Marco Maccarini, Loris Roveda"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.13733v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于机器人操作的视觉-语言-动作 (VLA) 模型
tldr: "针对强化学习在长程任务中探索效率低及VLA模型难以直接用于高频控制的问题，本文提出VLAJS方法。该方法将VLA作为临时的动作建议源，通过方向性动作一致性正则化引导PPO算法的早期探索。VLA引导采用稀疏触发并随时间退火，使智能体能超越引导策略。实验表明，VLAJS在多项任务中减少了50%以上的交互需求，并支持零样本实机迁移，显著提升了复杂机器人操作的学习效率。"
selection_source: fresh_fetch
motivation: 强化学习在处理长程任务和稀疏奖励时效率低下，而视觉语言动作（VLA）模型具备通用推理能力但难以直接用于高频精确控制。
method: 提出VLAJS方法，通过在PPO中引入方向性动作一致性正则化，利用VLA提供的稀疏引导来辅助早期探索，并随训练过程逐渐退火。
result: "在多项仿真任务中将环境交互需求降低了50%以上，并在真实机器人实验中实现了零样本迁移和对干扰的鲁棒性。"
conclusion: VLAJS成功结合了VLA的通用推理与RL的高频控制能力，显著提升了复杂操作任务的学习效率和泛化性能。
---

## 摘要
强化学习 (RL) 能够实现机器人操作的高频闭环控制，但由于探索效率低和信用分配困难，将其扩展到具有稀疏或不完善奖励的长程任务仍然具有挑战性。视觉-语言-动作 (VLA) 模型利用大规模多模态预训练来提供通用的任务级推理，但目前的局限性阻碍了它们在快速且精确的操作中的直接应用。在本文中，我们提出了视觉-语言-动作引导 (VLAJS)，这是一种将稀疏的 VLA 指导与同策略 (on-policy) RL 相结合的方法，旨在提高探索和学习效率。VLAJS 将 VLA 视为高层动作建议的瞬时来源，用于引导早期探索并改善信用分配，同时保留了 RL 基于状态的高频控制。我们的方法通过方向性动作一致性正则化增强了近端策略优化 (PPO)，在训练早期使 RL 智能体的动作与 VLA 指导软对齐，而无需强制执行严格模仿、不需要演示数据，也不依赖于持续的教师查询。VLA 指导以稀疏方式应用并随时间退火，使智能体能够在线适应并最终超越指导策略。我们在六个具有挑战性的仿真操作任务（提升、取放、销钉重定向、销钉插入、拨动和推动物体）中评估了 VLAJS，并在真实的 Franka Panda 机器人上验证了其中一部分任务。VLAJS 在样本效率方面始终优于 PPO 和蒸馏式基准方法，在多个任务中将所需的环境交互减少了 50% 以上。现实世界实验证明了零样本的从仿真到现实 (sim-to-real) 迁移，以及在杂乱环境、物体变化和外部扰动下的鲁棒执行。

## Abstract
Reinforcement learning (RL) enables high-frequency, closed-loop control for robotic manipulation, but scaling to long-horizon tasks with sparse or imperfect rewards remains difficult due to inefficient exploration and poor credit assignment. Vision-Language-Action (VLA) models leverage large-scale multimodal pretraining to provide generalist, task-level reasoning, but current limitations hinder their direct use in fast and precise manipulation. In this paper, we propose Vision-Language-Action Jump-Starting (VLAJS), a method that bridges sparse VLA guidance with on-policy RL to improve exploration and learning efficiency. VLAJS treats VLAs as transient sources of high-level action suggestions that bias early exploration and improve credit assignment, while preserving the high-frequency, state-based control of RL. Our approach augments Proximal Policy Optimization (PPO) with a directional action-consistency regularization that softly aligns the RL agent's actions with VLA guidance during early training, without enforcing strict imitation, requiring demonstrations, or relying on continuous teacher queries. VLA guidance is applied sparsely and annealed over time, allowing the agent to adapt online and ultimately surpass the guiding policy. We evaluate VLAJS on six challenging manipulation tasks: lifting, pick-and-place, peg reorientation, peg insertion, poking, and pushing in simulation, and validate a subset on a real Franka Panda robot. VLAJS consistently outperforms PPO and distillation-style baselines in sample efficiency, reducing required environment interactions by over 50% in several tasks. Real-world experiments demonstrate zero-shot sim-to-real transfer and robust execution under clutter, object variation, and external perturbations.

---

## 论文详细总结（自动生成）

这篇论文提出了 **VLAJS (Vision-Language-Action Jump-Starting)**，旨在通过结合大模型（VLA）的语义推理能力和强化学习（RL）的高频控制能力，解决机器人操作任务中的学习效率问题。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的强化学习（RL）在处理**长程任务（Long-horizon）**和**稀疏/不完善奖励**时，面临严重的探索效率低下和信用分配（Credit Assignment）困难。
*   **背景**：
    *   **RL 的优缺点**：支持高频闭环控制，精度高，但从零开始学习极慢。
    *   **VLA 模型的优缺点**：如 OpenVLA、Octo 等模型具备强大的语义理解和通用推理能力，但推理延迟高、控制频率低，且难以处理精确的闭环反馈。
*   **研究目标**：利用 VLA 模型作为“引导者”，在训练早期为 RL 智能体提供方向性建议，实现“快速启动（Jump-Start）”，随后让 RL 智能体超越 VLA 的表现。

### 2. 提出方法论：VLAJS
VLAJS 的核心思想是将 VLA 模型视为一种**瞬时的、稀疏的辅助引导源**。
*   **稀疏 VLA 查询与时间离散化**：为了降低计算开销，VLA 仅在每个 Rollout 中被查询少数几次（最大 20% 的步数）。查询得到的动作增量通过线性插值或球面线性插值（SLERP）分解到多个控制步中。
*   **方向性动作一致性损失（Directional Loss）**：这是本文的关键创新。不同于传统的策略蒸馏（使用 MSE 损失强制匹配动作数值），VLAJS 使用**余弦失配损失（Cosine Misalignment Loss）**。它只要求 RL 智能体的动作方向与 VLA 建议一致，而允许智能体自主决定动作的大小（Magnitude），这降低了对不完美 VLA 教师的依赖。
*   **基于奖励趋势的跳启动机制（Adaptive Annealing）**：
    *   **自适应查询率**：根据奖励增长情况动态减少 VLA 查询频率。
    *   **永久停用**：一旦 RL 智能体的平均奖励达到预设阈值（证明已进入稳定学习阶段），则完全关闭 VLA 引导，节省算力并避免受限于教师策略。

### 3. 实验设计
*   **仿真环境**：使用 **ManiSkill** 机器人操作基准，包含六类任务：抬起（Lifting）、取放（Pick-and-place）、销钉重定向、销钉插入、拨动（Poking）和推动（Pushing）。
*   **实验场景**：
    *   **用例 1（长程任务）**：将任务步数延长 10 倍，测试信用分配能力。
    *   **用例 2（不完善奖励）**：简化奖励函数，使其更稀疏，测试探索能力。
*   **对比基准（Benchmarks）**：
    *   **PPO**：标准的同策略强化学习。
    *   **Sparse RPD**：基于 MSE 损失的稀疏策略蒸馏方法。
    *   **VLAJS (RPD)**：消融实验，使用 VLAJS 的框架但将损失函数换回 MSE。
*   **真实世界验证**：在 Franka Panda 机器人上进行零样本（Zero-shot）迁移测试。

### 4. 资源与算力
*   **算力平台**：论文提到使用了瑞士国家超算中心（CSCS）的 **Alps 系统**。
*   **具体细节**：文中未明确给出具体的 GPU 型号数量或总训练时长，但强调了 VLAJS 通过稀疏查询显著降低了“墙钟时间（Wall-clock time）”开销，使得在长程任务中使用 VLA 引导变得可行。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个仿真任务上进行了测试，每个实验运行了 6 个随机种子（n=6），并提供了 95% 的置信区间。
*   **消融与鲁棒性**：
    *   对比了不同的 VLA 教师（OpenVLA, Octo）。
    *   测试了不同质量的教师（成功率 10% vs 40%）对结果的影响。
    *   测试了相机视角变化（OOD 视角）下的鲁棒性。
*   **评价**：实验设计较为充分，涵盖了从算法性能、消融分析到真实机器人部署的完整链路，对比实验设置公平。

### 6. 主要结论与发现
*   **效率提升**：VLAJS 在多个任务中将环境交互需求降低了 **50% 以上**。
*   **超越教师**：即使 VLA 教师本身的成功率仅为 40%，通过 VLAJS 训练的 RL 智能体最终能达到接近 100% 的成功率。
*   **方向性损失的优越性**：相比于 MSE 损失，方向性损失在引导早期探索时更灵活，不会因为教师动作的数值偏差而误导智能体。
*   **零样本迁移**：训练后的基于状态的 RL 策略配合 YOLO 目标检测，可以直接部署在真实机器人上，且对环境遮挡和人为干扰具有很强的鲁棒性。

### 7. 优点
*   **解耦了推理与控制**：巧妙地解决了 VLA 推理慢与机器人控制频率高之间的矛盾。
*   **非侵入式引导**：通过正则化项而非直接接管控制权来引导，保证了 RL 的自由探索空间。
*   **实用性强**：支持零样本实机迁移，且对 VLA 教师的质量要求不高，降低了微调大模型的成本。

### 8. 不足与局限
*   **对 VLA 的依赖**：虽然不要求 VLA 完美，但仍需要 VLA 能提供“起码正确”的方向性建议。如果 VLA 在某些极端任务中完全无法给出合理动作，该方法可能失效。
*   **系统复杂性**：在训练过程中需要同时运行大型 VLA 模型和 RL 环境，对显存压力和工程实现有一定要求。
*   **状态估计依赖**：实机部署依赖于预训练的 YOLO 检测器来获取物体状态，若视觉检测失败，策略将失效。
*   **退火策略**：目前的引导停用机制基于简单的奖励阈值启发式规则，在高度随机的环境中可能不够稳健。

（完）
