---
title: Adaptive Action Chunking at Inference-time for Vision-Language-Action Models
title_zh: 视觉-语言-动作模型在推理阶段的自适应动作分块
authors: "Yuanchang Liang, Xiaobo Wang, Kai Wang, Shuo Wang, Xiaojiang Peng, Haoyu Chen, David Kim Huat Chua, Prahlad Vadakkepat"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04161v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: VLA模型的自适应动作分块
tldr: 视觉-语言-动作（VLA）模型中，固定长度的动作分块难以平衡响应速度与动作连贯性。本文提出了一种自适应动作分块（AAC）策略，在推理阶段利用动作熵作为指标，动态调整分块大小。该方法有效解决了固定分块导致的反应迟钝或动作抖动问题，在多种仿真和真实机器人任务中显著提升了操作性能，为提升机器人操作的灵活性提供了新思路。
selection_source: fresh_fetch
motivation: 现有的VLA模型多采用固定的动作分块长度，无法兼顾模型对环境变化的实时响应能力与动作执行的平滑连贯性。
method: 提出一种自适应动作分块（AAC）策略，利用动作熵作为线索，在推理过程中根据当前预测动态确定最优的分块大小。
result: 在多种仿真和真实世界机器人操作任务中的实验表明，该方法在性能上显著优于目前最先进的固定分块方案。
conclusion: AAC策略通过动态平衡反应性与一致性，有效增强了VLA模型在多样化操作任务中的优越性和可扩展性。
---

## 摘要
在视觉-语言-动作（VLA）模型中，动作分块（即在没有中间重新规划的情况下执行一系列动作）是提高机器人操作能力的关键技术。然而，较大的分块尺寸会降低模型对新信息的响应能力，而较小的分块尺寸则会增加模态跳变（mode-jumping）的可能性，即由于分块之间的不连续性导致的动作抖动。因此，选择最优的分块尺寸是平衡模型反应性与一致性的迫切需求。遗憾的是，当前 VLA 模型的主流趋势是在推理阶段采用经验性的固定分块长度，这阻碍了它们在多样化操作任务中的优越性和可扩展性。为了解决这一问题，我们提出了一种新颖的自适应动作分块（AAC）策略，该策略利用动作熵作为线索，根据当前预测自适应地确定分块尺寸。在广泛的模拟和真实世界机器人操作任务上的大量实验表明，我们的方法相比于现有的最先进方案显著提高了性能。视频和源代码可在 https://lance-lot.github.io/adaptive-chunking.github.io/ 公开获取。

## Abstract
In Vision-Language-Action (VLA) models, action chunking (i.e., executing a sequence of actions without intermediate replanning) is a key technique to improve robotic manipulation abilities. However, a large chunk size reduces the model's responsiveness to new information, while a small one increases the likelihood of mode-jumping, jerky behavior resulting from discontinuities between chunks. Therefore, selecting the optimal chunk size is an urgent demand to balance the model's reactivity and consistency. Unfortunately, a dominant trend in current VLA models is an empirical fixed chunk length at inference-time, hindering their superiority and scalability across diverse manipulation tasks. To address this issue, we propose a novel Adaptive Action Chunking (AAC) strategy, which exploits action entropy as the cue to adaptively determine the chunk size based on current predictions. Extensive experiments on a wide range of simulated and real-world robotic manipulation tasks have demonstrated that our approach substantially improves performance over the state-of-the-art alternatives. The videos and source code are publicly available at https://lance-lot.github.io/adaptive-chunking.github.io/.

---

## 论文详细总结（自动生成）

### 视觉-语言-动作模型在推理阶段的自适应动作分块（AAC）论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
在视觉-语言-动作（VLA）模型中，“动作分块”（Action Chunking）是标准范式，即模型一次性预测并执行一系列动作序列。
*   **研究动机**：当前主流模型（如 GR00T, $\pi_0$）在推理时通常使用**固定长度**的分块。
    *   **分块过大**：模型对环境变化的响应速度变慢（反应性差）。
    *   **分块过小**：动作之间容易出现不连续，导致机器人动作抖动（一致性差）。
*   **核心问题**：如何动态地平衡机器人的“反应性”与“一致性”？论文指出，不同任务甚至同一任务的不同阶段对分块大小的需求是不同的。

#### 2. 方法论：核心思想与关键技术
论文提出了 **自适应动作分块（AAC）** 策略，其核心思想是利用**动作熵（Action Entropy）**作为不确定性的度量，动态决定当前步执行多少个动作。
*   **核心思想**：当模型预测的动作熵较低（确定性高）时，执行较长的分块以保持连贯；当熵较高（不确定性高）时，执行较短的分块并频繁重新规划。
*   **关键技术细节**：
    1.  **动作熵计算**：
        *   **连续动作**（平移、旋转）：使用高斯微分熵公式计算。
        *   **离散动作**（夹爪开关）：使用标准香农熵公式计算。
    2.  **并行采样**：在推理时并行采样 $N$ 个候选动作块，用以估计概率分布和协方差矩阵。
    3.  **最优分块选择**：计算不同分块长度下的平均熵，寻找熵增量最大的转折点作为最优长度 $h^*$。
    4.  **最小动作幅度约束**：引入阈值 $\xi$，确保分块长度至少能产生足够的物理位移，避免无效的微小动作。
*   **算法流程**：采样 -> 计算各步熵值 -> 计算平均熵 -> 寻找最大差异点 -> 结合最小幅度约束输出 $h^*$。

#### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：RoboCasa Kitchen（24个厨房操作任务，涵盖重定位、容器、旋转、按钮四类）和 LIBERO（4个任务集，共40个任务）。
    *   **真实世界**：使用 Realman 单臂机器人完成香蕉放置、紧急按钮按下、抽屉收纳（长程任务）三个场景。
*   **Benchmark 与对比方法**：
    *   **基准模型**：以 GR00T N1.5 为主要 Baseline，同时也验证了在 $\pi_{0.5}$ 上的效果。
    *   **对比方案**：固定分块长度（$h=2, 4, 8, 12, 16$）的 vanilla 模型。

#### 4. 资源与算力
*   **训练算力**：使用了 **8 台 NVIDIA A800 GPU** 进行混合精度微调。
*   **训练时长**：
    *   仿真实验：在 RoboCasa 和 LIBERO 上分别微调了 90 个 epoch。
    *   真实世界：针对每个任务微调了 300 个 epoch。
*   **推理开销**：由于采用并行采样，AAC 引入的延迟较小。在 A800 上，采样 20 个样本仅增加约 **20ms** 的推理延迟。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真：RoboCasa 每项任务 100 次测试，LIBERO 每项任务 50 次测试。
    *   真实世界：每个任务 20 次试验。
*   **充分性评价**：实验设计较为充分。不仅对比了不同固定长度的基准，还进行了消融实验（如采样数量 $N$ 的影响）、跨模型骨干（GR00T vs $\pi_{0.5}$）的泛化性实验，以及分布外（OOD）扰动测试。实验结果在统计上具有说服力。

#### 6. 主要结论与发现
*   **性能提升**：AAC 在 RoboCasa 上平均成功率提升 2.3%，在 LIBERO-Long（长程任务）上提升达 4%，真实世界任务平均提升 15%。
*   **符合人类直觉**：定性分析显示，AAC 在“运输阶段”（远离物体）倾向于选择大分块以提高效率，在“精细操作阶段”（接近物体）倾向于选择小分块以提高精度。
*   **安全性增强**：在真实实验中，AAC 能有效减少夹爪与桌面的碰撞，证明了其在处理高熵（高风险）预测时的优越性。

#### 7. 优点（亮点）
*   **即插即用**：无需修改模型架构，无需额外训练（仅在推理阶段实施），具有极强的通用性。
*   **动态平衡**：巧妙地利用动作熵解决了机器人领域长期存在的反应性与一致性的矛盾。
*   **效率与精度兼顾**：通过并行采样和自适应调整，在不显著增加计算负担的前提下提升了成功率。

#### 8. 不足与局限
*   **超参数依赖**：算法仍依赖于最小动作幅度阈值 $\alpha$ 等超参数，可能需要针对不同硬件进行微调。
*   **采样依赖**：熵的估计质量高度依赖于采样数量 $N$，在算力受限的嵌入式设备上可能存在性能折中。
*   **极端场景**：虽然提升了鲁棒性，但在极端扰动（如 LIBERO-Pro 高强度扰动）下，所有方法的成功率依然会大幅下降，AAC 无法完全弥补模型本身的认知局限。

（完）
