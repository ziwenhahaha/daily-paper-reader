---
title: Human-Robot Copilot for Data-Efficient Imitation Learning
title_zh: 面向高效数据模仿学习的人机协作驾驶系统
authors: "Rui Yan, Zaitian Gongye, Lars Paulsen, Xuxin Cheng, Xiaolong Wang"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03613v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 人在回路的模仿学习与选择性人类干预
tldr: 针对机器人模仿学习中因演示数据有限导致的分布外（OOD）状态及现有交互式方法灵巧性不足的问题，本文提出了一种“人机协作（Human-Robot Copilot）”框架。该框架通过引入缩放因子增强了遥操作的精细度，同时保持了对多种机械臂的通用性。研究表明，该方法在提升任务成功率的同时，显著降低了人类干预的时间成本，实现了更高效的数据采集与策略学习。
selection_source: fresh_fetch
motivation: 解决模仿学习中有限数据导致的分布外状态问题，并克服现有交互式方法在精细控制与通用性之间的矛盾。
method: 提出一种人机协作框架，利用缩放因子实现灵巧遥操作，并兼容多种工业和研究用机械臂。
result: 在相同演示轨迹数量下，该框架获得了更高的性能表现，且数据采集过程更加高效省时。
conclusion: 该框架通过优化人机交互机制，有效提升了机器人模仿学习的数据效率和任务执行的鲁棒性。
---

## 摘要
通过远程操作收集人类演示是教授机器人特定任务技能的一种常用方法。然而，当可用的演示数量有限时，由于复合误差或环境随机性，策略容易进入分布外（OOD）状态。现有的交互式模仿学习或人机在环方法试图通过遵循人类门控 DAgger (HG-DAgger) 范式来解决这一问题，该方法通过在策略执行期间进行选择性的人类干预来增强演示。尽管如此，这些方法难以平衡灵巧性和通用性：它们要么提供细粒度的修正但受限于特定的运动学结构，要么以牺牲精确控制为代价来实现通用性。为了克服这一局限性，我们提出了人机协作驾驶（Human-Robot Copilot）框架，该框架可以利用缩放因子进行灵巧的远程操作，同时保持与各种工业和研究机械臂的兼容性。实验结果表明，在相同数量的演示轨迹下，我们的框架实现了更高的性能。此外，由于仅需间歇性地进行纠正性干预，整个数据收集过程更加高效且耗时更短。

## Abstract
Collecting human demonstrations via teleoperation is a common approach for teaching robots task-specific skills. However, when only a limited number of demonstrations are available, policies are prone to entering out-of-distribution (OOD) states due to compounding errors or environmental stochasticity. Existing interactive imitation learning or human-in-the-loop methods try to address this issue by following the Human-Gated DAgger (HG-DAgger) paradigm, an approach that augments demonstrations through selective human intervention during policy execution. Nevertheless, these approaches struggle to balance dexterity and generality: they either provide fine-grained corrections but are limited to specific kinematic structures, or achieve generality at the cost of precise control. To overcome this limitation, we propose the Human-Robot Copilot framework that can leverage a scaling factor for dexterous teleoperation while maintaining compatibility with a wide range of industrial and research manipulators. Experimental results demonstrate that our framework achieves higher performance with the same number of demonstration trajectories. Moreover, since corrective interventions are required only intermittently, the overall data collection process is more efficient and less time-consuming.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Human-Robot Copilot** 的人机协作驾驶框架，旨在通过高效的人类干预和异构遥操作技术，解决机器人模仿学习中的数据效率和分布外（OOD）泛化问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的模仿学习（IL）在有限数据下容易因复合误差或环境随机性进入**分布外（OOD）状态**，导致任务失败。
*   **研究背景**：现有的“人在回路”（Human-in-the-loop）方法存在矛盾：
    *   **通用性不足**：如 Robo-Copilot 需要相同运动学结构的机器人对。
    *   **灵巧性不足**：如 SpaceMouse 或 VR 控制器采用增量控制，缺乏绝对空间参考，难以进行毫米级的精细修正。
*   **研究动机**：开发一个既能兼容不同型号机械臂（异构性），又能提供高精度控制（灵巧性），且数据采集效率高的系统。

### 2. 方法论
*   **核心思想**：构建一个低成本的异构遥操作硬件，通过**双向控制架构**和**可调缩放因子**，实现人类对机器人策略执行过程中的实时、精细干预。
*   **关键技术细节**：
    *   **异构遥操作**：使用低成本（约$1000）的 3D 打印主端设备（Leader），通过逆运动学（IK）解算将位姿映射到从端（Follower）机械臂，不要求两者结构一致。
    *   **双向同步逻辑**：即使在机器人自主运行策略时，主端设备也会实时跟随从端的位姿。这确保了人类在干预瞬间，主端与从端处于同步状态，消除了切换时的突跳。
    *   **控制缩放因子 ($\alpha$)**：引入空间映射缩放。大缩放（如 $\alpha=2.0$）用于快速大范围移动；小缩放（如 $\alpha=0.5$）用于高精度对准（如插入任务），显著降低操作者的控制难度。
    *   **数据剪辑（Data Clips）微调**：人类仅在机器人出错时介入并录制“修正片段”，而非重新录制完整轨迹。将这些片段与原始数据混合，对轻量化策略（ACT + ResNet-18）进行快速微调。

### 3. 实验设计
*   **实验场景**：
    *   **仿真环境**：Robomimic 基准测试中的 PickPlace（抓取放置）和 NutAssembly（螺母组装）。
    *   **现实世界**：高度随机化的“方块分类”任务和高精度（2mm 容差）的“汉诺塔盘插入”任务。
*   **对比基准（Baselines）**：
    *   **Base Policy**：仅使用初始演示训练的策略。
    *   **Keyboard/VR Policy**：使用键盘（速度控制）或 VR 控制器作为干预手段进行微调的策略。
*   **评估指标**：分阶段成功率（抓取阶段 vs. 完成阶段）、总数据采集时间、末端轨迹的均方根误差（RMS）。

### 4. 资源与算力
*   **训练时长**：论文提到使用较小的网络规模以实现快速迭代。从头训练基础策略约需 **40 分钟**，而加入干预数据后的微调过程仅需 **10 分钟**。
*   **硬件成本**：主端设备成本约为 **$1,000**。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量，但强调了“快速重部署”和“本地高频运行（LCM 通信）”，暗示了该方法对算力的要求相对亲民。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真和现实任务各 2 个，每个任务评估 50 次实验（Rollouts）。
    *   对比了三种不同的人机交互接口（键盘、VR、本框架）。
*   **充分性评价**：实验设计较为全面，涵盖了“高随机性”和“高精度”两种典型的机器人挑战。通过对比不同干预工具的采集时间，客观证明了该框架在提升性能的同时降低了人类的工作量。

### 6. 主要结论与发现
*   **性能提升**：在相同轨迹数量下，使用 Copilot 框架微调后的策略成功率显著高于基础策略和其他干预手段。
*   **数据效率**：采集“修正片段”的时间远低于采集完整轨迹。在汉诺塔任务中，Copilot 的采集效率比 VR 高出约 30%。
*   **精度优势**：实验证明，较小的缩放因子（$\alpha=0.5$）能显著减少人类操作时的超调和震荡，使生成的演示数据更平滑、更易于策略学习。

### 7. 优点
*   **低成本与开源**：硬件成本低且全开源，有利于社区复现。
*   **双向同步机制**：解决了人机切换时的不连续性问题，提升了干预的直观性。
*   **缩放因子设计**：简单有效地解决了“大范围移动”与“微米级操作”之间的矛盾。

### 8. 不足与局限
*   **多模态处理能力**：由于使用了 ACT 算法，该框架难以处理人类干预产生的多模态（多种解法）数据，修正动作必须与原策略风格一致。
*   **固定缩放因子**：目前缩放因子需要手动预设，未来若能根据任务阶段自动调整（自适应缩放）会更智能。
*   **干预策略依赖**：系统性能仍高度依赖于人类操作者的判断时机和操作水平。

（完）
