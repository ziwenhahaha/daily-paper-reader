---
title: "Action Images: End-to-End Policy Learning via Multiview Video Generation"
title_zh: 动作图像：通过多视图视频生成的端到端策略学习
authors: "Haoyu Zhen, Zixian Gao, Qiao Sun, Yilin Zhao, Yuncong Yang, Yilun Du, Tsun-Hsuan Wang, Yi-Ling Qiao, Chuang Gan"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.06168v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于机器人策略学习的世界动作模型 (WAM)
tldr: 本研究提出Action Images，一种将机器人策略学习转化为多视角视频生成的统一世界动作模型。通过将7自由度动作转换为像素级、可解释的多视角动作图像，该方法使预训练视频模型能直接作为零样本策略，无需额外动作模块。在RLBench和真实场景中，该模型在零样本成功率和视频动作联合生成质量上均表现优异，为机器人控制提供了一种新的像素级表征路径。
selection_source: fresh_fetch
motivation: 现有世界动作模型依赖独立的动作模块或非像素级表征，难以充分利用预训练视频模型的知识并限制了跨环境迁移。
method: 将机器人动作转化为可解释的多视角动作图像（Action Images），并利用统一的视频生成架构实现端到端的策略学习与视频预测。
result: 在RLBench和真实世界评估中，该模型实现了极强的零样本成功率，并显著提升了视频与动作联合生成的质量。
conclusion: 将动作表征为像素级的动作图像是实现高效、可迁移的机器人策略学习和世界建模的一种极具潜力的方法。
---

## 摘要
世界动作模型 (WAMs) 已成为机器人策略学习的一个极具前景的方向，因为它们可以利用强大的视频骨干网络来建模未来状态。然而，现有方法通常依赖于独立的动作模块，或者使用非像素对齐（pixel-grounded）的动作表示，这使得充分利用视频模型的预训练知识变得困难，并限制了在不同视角和环境之间的迁移。在这项工作中，我们提出了 Action Images，这是一种统一的世界动作模型，将策略学习公式化为多视图视频生成。我们没有将控制编码为低维 token，而是将 7 自由度 (7-DoF) 机器人动作转化为可解释的动作图像：即在 2D 像素中对齐并显式跟踪机械臂运动的多视图动作视频。这种像素对齐的动作表示允许视频骨干网络本身充当零样本策略，而无需单独的策略头或动作模块。除了控制之外，同一个统一模型还支持在共享表示下的视频-动作联合生成、动作条件视频生成以及动作标注。在 RLBench 和真实世界评估中，我们的模型实现了最强的零样本成功率，并比之前的视频空间世界模型提高了视频-动作联合生成的质量，这表明可解释的动作图像是策略学习的一条极具前景的路径。

## Abstract
World action models (WAMs) have emerged as a promising direction for robot policy learning, as they can leverage powerful video backbones to model the future states. However, existing approaches often rely on separate action modules, or use action representations that are not pixel-grounded, making it difficult to fully exploit the pretrained knowledge of video models and limiting transfer across viewpoints and environments. In this work, we present Action Images, a unified world action model that formulates policy learning as multiview video generation. Instead of encoding control as low-dimensional tokens, we translate 7-DoF robot actions into interpretable action images: multi-view action videos that are grounded in 2D pixels and explicitly track robot-arm motion. This pixel-grounded action representation allows the video backbone itself to act as a zero-shot policy, without a separate policy head or action module. Beyond control, the same unified model supports video-action joint generation, action-conditioned video generation, and action labeling under a shared representation. On RLBench and real-world evaluations, our model achieves the strongest zero-shot success rates and improves video-action joint generation quality over prior video-space world models, suggesting that interpretable action images are a promising route to policy learning.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Action Images** 的新型机器人策略学习框架，旨在通过将机器人动作转化为“像素对齐”的视频信号，消除视频生成模型与机器人控制策略之间的表征鸿沟。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的世界动作模型（WAMs）虽然在预测未来视频方面表现出色，但在将其转化为可泛化的机器人策略时存在瓶颈。
*   **研究动机**：
    *   **表征脱节**：现有方法通常将动作编码为低维向量或离散 Token，并依赖额外的策略头（Policy Head）。这导致视频模型的预训练知识（如物理规律、空间关系）无法直接迁移到控制任务中。
    *   **泛化差距**：视频生成的泛化能力强，但控制策略在未见过的环境或视角下容易失效。
    *   **目标**：将 7 自由度（7-DoF）的机器人动作转化为与视频模态完全一致的“动作图像”，使视频骨干网络本身就能直接输出可执行的控制指令。

### 2. 方法论
*   **核心思想**：将机器人动作（位置、姿态、夹爪状态）渲染为多视角的 2D 高斯热图视频，使其成为视频生成任务的一部分。
*   **关键技术细节**：
    1.  **动作到 3D 语义点**：将 7-DoF 动作转化为 3 个关键 3D 点：位置点（Position）、法线点（Normal）和向上点（Up），以完整表征末端执行器的位姿。
    2.  **多视角动作图像渲染**：利用相机参数将 3D 点投影到 2D 图像平面，并渲染为 RGB 高斯热图。其中，红色通道代表位置，绿色代表法线，蓝色通道同时编码向上点和夹爪的开合状态（通过低响应背景区域编码）。
    3.  **统一模型训练**：基于预训练的 **Wan 2.2** 视频生成骨干网络，将视频 Latent 和动作图像 Latent 在时间轴上拼接。采用多掩码（Multi-mask）策略，支持：
        *   视频-动作联合生成（Joint Generation）。
        *   动作条件下的视频预测（Action-conditioned Video Gen）。
        *   视频到动作的标注（Video-to-action Labeling）。
    4.  **动作解码**：通过多视角几何约束（射线投射 Ray Casting 与侧视图匹配），将生成的 2D 热图还原为 3D 空间中的 7-DoF 动作。

### 3. 实验设计
*   **数据集**：
    *   **RLBench**（仿真）：提供精确的动作和相机标注，用于训练和评估。
    *   **DROID**（真实）：大规模真实机器人操作数据。
    *   **BridgeV2**（真实）：用于增强视频生成的质量。
*   **Benchmark 与对比方法**：
    *   **对比方法**：包括扩散策略（MV-Policy）、视觉语言动作模型（$\pi_{0.5}$、MolmoAct）、以及基于视频的世界模型（TesserAct、Cosmos-Policy）。
    *   **评估场景**：RLBench 域内任务、RLBench 零样本（Zero-shot）任务、以及真实世界 xArm 机器人的零样本迁移实验。

### 4. 资源与算力
*   **算力设备**：使用了 **32 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   使用 DeepSpeed ZeRO 优化器。
    *   训练步数为 **100,000 步**。
    *   混合精度训练（bfloat16）。
    *   推理阶段通过并行化（CFG Parallelism）和缓存技术，在 8 张 H100 上可将推理延迟显著降低。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了仿真（RLBench）和真实世界（xArm）的对比。
    *   进行了零样本泛化实验，测试了未见过的物体、环境和任务。
    *   包含了消融实验和多种任务模式（联合生成、条件生成、动作标注）的定量评估。
*   **充分性评价**：实验设计较为全面，不仅对比了成功率，还通过 PSNR、SSIM、FVD 等指标评估了视频生成质量，并验证了动作解码的精度。对比基线涵盖了当前最前沿的 VLA 和 WAM 模型，具有较强的说服力。

### 6. 主要结论与发现
*   **像素对齐的优势**：将动作表征为图像显著提升了策略的零样本泛化能力，尤其是在跨视角和跨环境任务中。
*   **统一表征的效能**：同一个模型在不改变架构的情况下，能够同时胜任预测世界（视频生成）和在世界中行动（策略执行）。
*   **性能领先**：在 RLBench 和真实世界评估中，Action Images 在零样本成功率上均优于现有的 SOTA 模型（如 Cosmos-Policy 和 TesserAct）。

### 7. 优点
*   **创新性表征**：提出了一种直观且可解释的动作图像表征，解决了低维动作与高维视觉特征难以对齐的问题。
*   **无需专用模块**：去除了复杂的策略头，完全复用视频生成模型的能力，降低了系统复杂度。
*   **多任务统一**：实现了控制、预测和标注的闭环，展示了世界动作模型的强大潜力。

### 8. 不足与局限
*   **开环控制限制**：目前主要展示的是开环（Open-loop）预测结果，虽然效果好，但在处理动态干扰时，闭环（Closed-loop）控制的实时性仍是挑战。
*   **推理开销**：尽管有优化，但基于大型视频扩散模型的推理速度（约 2.3 秒/序列）对于高频实时控制仍显不足。
*   **相机依赖**：解码过程依赖准确的相机内参和外参，在相机参数未知的野外环境下，性能受限于相机估计模型的精度。

（完）
