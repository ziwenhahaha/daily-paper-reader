---
title: "StarVLA: A Lego-like Codebase for Vision-Language-Action Model Developing"
title_zh: StarVLA：一个用于视觉-语言-动作模型开发的类乐高代码库
authors: StarVLA Community
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05014v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于视觉-语言-动作（VLA）模型研究和开发的开源代码库
tldr: StarVLA是一个开源的视觉-语言-动作（VLA）模型开发框架，旨在解决当前具身智能研究中架构碎片化和评估不统一的挑战。它采用模块化的“主干-动作头”设计，支持多种VLM和世界模型，并集成了跨具身学习策略与统一的评估接口。该框架通过简化的训练方案在多个基准测试中达到或超过了现有水平，显著降低了VLA模型的开发与复现门槛。
selection_source: fresh_fetch
motivation: 解决VLA研究中因架构不兼容、代码库碎片化和评估协议不一导致的复现与对比难题。
method: 构建模块化架构以实现主干网络与动作头的灵活替换，并提供统一的训练策略和多基准评估接口。
result: 在LIBERO、SimplerEnv等多个主流基准测试中，StarVLA的基准方案表现均达到或优于现有先进方法。
conclusion: StarVLA为具身智能领域提供了一个全面、可扩展且易于使用的开源框架，促进了VLA技术的标准化与创新。
---

## 摘要
构建通用具身智能体需要整合感知、语言理解和动作，这些是基于多模态基础模型（包括视觉-语言模型和世界模型的最新进展）的视觉-语言-动作（VLA）方法所解决的核心能力。尽管取得了快速进展，但 VLA 方法在不兼容的架构、代码库和评估协议中仍然处于碎片化状态，阻碍了有原则的比较和可复现性。我们提出了 StarVLA，一个用于 VLA 研究的开源代码库。StarVLA 从三个方面应对这些挑战。首先，它提供了一种模块化的“主干网络-动作头”架构，支持 VLM 主干（如 Qwen-VL）和世界模型主干（如 Cosmos），以及代表性的动作解码范式，所有这些都在一个共享的抽象层下，其中主干和动作头可以各自独立更换。其次，它提供了可重用的训练策略，包括跨具身学习和多模态协同训练，这些策略在支持的范式中一致适用。第三，它通过统一的评估接口集成了主要的基准测试，包括 LIBERO、SimplerEnv、RoboTwin 2.0、RoboCasa-GR1 和 BEHAVIOR-1K，支持仿真和真实机器人部署。StarVLA 还提供了简单、完全可复现的单基准训练方案，尽管数据工程极少，但在使用 VLM 和世界模型主干的多个基准测试上，已经达到或超过了之前的方法。据我们所知，StarVLA 是目前最全面的开源 VLA 框架之一，我们期望它能降低复现现有方法和开发新方法原型的门槛。StarVLA 正在积极维护和扩展；我们将随着项目的发展更新本报告。代码和文档可在 https://github.com/starVLA/starVLA 获取。

## Abstract
Building generalist embodied agents requires integrating perception, language understanding, and action, which are core capabilities addressed by Vision-Language-Action (VLA) approaches based on multimodal foundation models, including recent advances in vision-language models and world models. Despite rapid progress, VLA methods remain fragmented across incompatible architectures, codebases, and evaluation protocols, hindering principled comparison and reproducibility. We present StarVLA, an open-source codebase for VLA research. StarVLA addresses these challenges in three aspects. First, it provides a modular backbone--action-head architecture that supports both VLM backbones (e.g., Qwen-VL) and world-model backbones (e.g., Cosmos) alongside representative action-decoding paradigms, all under a shared abstraction in which backbone and action head can each be swapped independently. Second, it provides reusable training strategies, including cross-embodiment learning and multimodal co-training, that apply consistently across supported paradigms. Third, it integrates major benchmarks, including LIBERO, SimplerEnv, RoboTwin~2.0, RoboCasa-GR1, and BEHAVIOR-1K, through a unified evaluation interface that supports both simulation and real-robot deployment. StarVLA also ships simple, fully reproducible single-benchmark training recipes that, despite minimal data engineering, already match or surpass prior methods on multiple benchmarks with both VLM and world-model backbones. To our best knowledge, StarVLA is one of the most comprehensive open-source VLA frameworks available, and we expect it to lower the barrier for reproducing existing methods and prototyping new ones. StarVLA is being actively maintained and expanded; we will update this report as the project evolves. The code and documentation are available at https://github.com/starVLA/starVLA.

---

## 论文详细总结（自动生成）

### StarVLA：类乐高视觉-语言-动作（VLA）模型开发框架总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的具身智能（Embodied AI）研究中，视觉-语言-动作（VLA）模型面临严重的**碎片化**问题。不同方法在模型架构（如基于VLM或基于世界模型）、代码实现、数据处理和评估协议上互不兼容，导致研究者难以进行公平的横向对比，且复现成本极高。
*   **整体含义**：StarVLA 旨在构建一个“类乐高”的开源代码库，通过统一的抽象层将感知（主干网络）与执行（动作头）解耦。它不仅是一个工具包，更提供了一套标准化的 VLA 开发范式，旨在降低具身智能的研究门槛，促进通用具身智能体的开发。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：采用**“主干网络-动作头”（Backbone-Action-Head）**的解耦架构。主干网络负责多模态感知与推理，动作头负责将表征映射为具体的机器人指令。
*   **关键技术细节**：
    *   **统一 I/O 接口**：训练和推理均直接消耗原始环境观测（如原始图像、文本指令），避免了复杂的预处理导致的训练/测试分布偏移。
    *   **四种动作解码范式**：
        1.  **StarVLA-FAST**：离散动作标记化，利用 LLM 词表进行自回归生成。
        2.  **StarVLA-OFT**：并行回归，通过 MLP 直接预测连续动作。
        3.  **StarVLA-π**：集成流匹配（Flow-matching）去噪器，通过迭代去噪生成动作。
        4.  **StarVLA-GR00T**：双系统设计，VLM 作为慢速推理系统，DiT 模块作为快速动作生成系统。
    *   **模块化组合**：支持 VLM 主干（如 Qwen2.5-VL）和世界模型主干（如 Cosmos-Predict2）的无缝切换。
    *   **训练策略**：支持标准行为克隆（SFT）、多模态协同训练（Co-training，防止 VLM 能力退化）以及跨具身（Cross-embodiment）混合训练。

#### 3. 实验设计
*   **数据集与场景**：
    *   **LIBERO**：包含 130 个任务，涵盖空间、对象、目标和长程操作。
    *   **SimplerEnv**：模拟真实机器人（WidowX, Google Robot）的仿真环境。
    *   **RoboTwin 2.0**：大规模双臂协同操作基准。
    *   **RoboCasa-GR1**：类人机器人家庭场景操作。
    *   **BEHAVIOR-1K**：人类生活场景中的复杂交互任务。
*   **对比方法**：与 OpenVLA, $\pi_0$, GR00T-N1.5/1.6, RT-1-X, CogACT, Magma 等当前最先进的（SOTA）开源或闭源模型进行了对比。

#### 4. 资源与算力
*   **GPU 型号与数量**：
    *   **单基准训练**：通常使用 8 到 16 块 **NVIDIA A100 (80GB)**。
    *   **大规模训练**：在 RoboTwin 2.0 上使用了 **48 块 A100**；在计算效率测试中，扩展到了 **256 块 A100**。
*   **训练时长/步数**：
    *   LIBERO：30K 步（约 10 个 epoch）。
    *   SimplerEnv/RoboCasa：100K 步。
    *   RoboTwin：150K 步。
*   **效率分析**：论文详细记录了不同 Batch Size 下的吞吐量（Samples/s）和多节点扩展效率（在 256 卡规模下保持约 79% 的并行效率）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大量的实验，包括 5 个主流基准的单任务专家模型训练、多模态协同训练的消融研究，以及跨基准/跨具身的通用模型（Generalist）训练。
*   **充分性与公平性**：实验设计非常充分。作者遵循各基准的官方评估协议，并采用 Server-Client 架构确保推理环境与仿真环境隔离，避免了依赖冲突。对比实验涵盖了从 VLM 到世界模型的多种架构，且在相同训练条件下进行，体现了高度的客观性和公平性。

#### 6. 主要结论与发现
*   **模块化优势**：证明了通过更换动作头或主干网络，可以在不改变基础设施的情况下显著提升性能。
*   **数据效率**：StarVLA 的训练方案极其高效，例如在 LIBERO 上仅需 30K 步即可达到或超过其他模型训练 175K 步的效果。
*   **协同训练的重要性**：实验发现，仅进行动作微调会导致 VLM 的感知能力（如空间接地）迅速退化，而协同训练能有效保持模型的多模态推理能力。
*   **通用模型可行性**：单一模型通过跨具身训练，可以在多个异构基准上同时保持竞争力，甚至在某些任务（如 RoboCasa）上超过了专门训练的专家模型。

#### 7. 优点
*   **类乐高设计**：极高的灵活性，支持研究者快速原型化新的动作解码算法。
*   **统一接口**：解决了具身智能领域长期存在的环境配置冲突和评估不一致问题。
*   **高性能基准**：提供的 Baseline 性能强劲，且完全开源可复现，具有很高的实用价值。
*   **工程质量高**：提供了详尽的算力消耗和扩展性分析，对工业界部署具有指导意义。

#### 8. 不足与局限
*   **强化学习（RL）支持**：目前主要侧重于监督学习（BC）和协同训练，RL 微调功能尚在集成中。
*   **主干网络覆盖**：虽然支持多种主干，但实验主要集中在 Qwen 和 Cosmos 系列，对更多新兴架构（如纯 Transformer 动作预测）的适配有待进一步验证。
*   **动作空间对齐**：在跨具身训练中，目前采用的简单填充（Padding）策略可能不是处理异构动作空间的最优解。

（完）
