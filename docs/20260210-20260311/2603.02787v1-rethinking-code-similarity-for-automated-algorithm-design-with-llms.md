---
title: Rethinking Code Similarity for Automated Algorithm Design with LLMs
title_zh: 重新审视大语言模型自动化算法设计中的代码相似性
authors: "Rui Zhang, Zhichao Lu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02787v1"
tags: ["query:ahd"]
score: 9.0
evidence: 提出了一种评估基于LLM的自动化算法设计中算法相似性的方法。
tldr: 本研究针对大语言模型自动算法设计（LLM-AAD）中传统代码相似度指标无法捕捉算法逻辑差异的问题，提出了BehaveSim方法。该方法通过分析算法执行过程中的中间解序列（即问题解决轨迹PSTrajs），利用动态时间规整（DTW）量化行为对齐度。实验证明，BehaveSim能有效提升LLM-AAD框架的性能并辅助算法聚类分析，为AI生成算法的评估提供了新视角。
selection_source: fresh_fetch
motivation: 现有的代码相似度指标侧重于语法或输出等效性，难以区分具有不同底层逻辑的算法创新。
method: 提出BehaveSim方法，通过动态时间规整（DTW）量化算法执行过程中产生的中间解序列（PSTrajs）之间的对齐程度。
result: 将BehaveSim集成到FunSearch和EoH等框架中显著提升了算法设计的性能，并实现了对AI生成算法的有效聚类分析。
conclusion: 通过关注问题解决行为而非表面代码，BehaveSim为评估和优化自动生成的算法提供了一个更本质且有效的维度。
---

## 摘要
基于大语言模型的自动化算法设计（LLM-AAD）的兴起，通过自主生成专家级算法的代码实现，彻底改变了算法开发模式。与传统的专家驱动型算法开发不同，在 LLM-AAD 范式中，算法背后的核心设计原则通常隐含在生成的代码中。因此，直接从代码评估算法相似性，从而将真正的算法创新与单纯的语法变化区分开来，变得至关重要。虽然目前存在多种代码相似性指标，但它们往往关注表层语法或输出等价性，而非底层的算法逻辑，因此无法有效捕捉算法相似性。我们提出了 BehaveSim，这是一种衡量算法相似性的新方法，它从问题解决行为的角度出发，将执行过程中产生的一系列中间解定义为问题解决轨迹（PSTrajs）。通过使用动态时间规整（DTW）量化 PSTrajs 之间的对齐程度，BehaveSim 能够区分出那些虽然语法或输出相似但逻辑迥异的算法。我们在两个关键应用中展示了该方法的效用：(i) 增强 LLM-AAD：将 BehaveSim 集成到现有的 LLM-AAD 框架（如 FunSearch、EoH）中可以促进行为多样性，从而显著提高在三个 AAD 任务上的性能；(ii) 算法分析：BehaveSim 根据行为对生成的算法进行聚类，实现了对问题解决策略的系统分析，这对于日益增长的 AI 生成算法生态系统而言是一个至关重要的工具。本工作的代码和数据已在 https://github.com/RayZhhh/behavesim 开源。

## Abstract
The rise of Large Language Model-based Automated Algorithm Design (LLM-AAD) has transformed algorithm development by autonomously generating code implementations of expert-level algorithms. Unlike traditional expert-driven algorithm development, in the LLM-AAD paradigm, the main design principle behind an algorithm is often implicitly embedded in the generated code. Therefore, assessing algorithmic similarity directly from code, distinguishing genuine algorithmic innovation from mere syntactic variation, becomes essential. While various code similarity metrics exist, they fail to capture algorithmic similarity, as they focus on surface-level syntax or output equivalence rather than the underlying algorithmic logic.   We propose BehaveSim, a novel method to measure algorithmic similarity through the lens of problem-solving behavior as a sequence of intermediate solutions produced during execution, dubbed as problem-solving trajectories (PSTrajs). By quantifying the alignment between PSTrajs using dynamic time warping (DTW), BehaveSim distinguishes algorithms with divergent logic despite syntactic or output-level similarities. We demonstrate its utility in two key applications: (i) Enhancing LLM-AAD: Integrating BehaveSim into existing LLM-AAD frameworks (e.g., FunSearch, EoH) promotes behavioral diversity, significantly improving performance on three AAD tasks. (ii) Algorithm analysis: BehaveSim clusters generated algorithms by behavior, enabling systematic analysis of problem-solving strategies--a crucial tool for the growing ecosystem of AI-generated algorithms. Data and code of this work are open-sourced at https://github.com/RayZhhh/behavesim.

---

## 论文详细总结（自动生成）

这是一份关于论文《Rethinking Code Similarity for Automated Algorithm Design with LLMs》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：随着大语言模型（LLM）在自动化算法设计（LLM-AAD）领域的应用，AI 能够自主生成专家级的算法代码。
*   **核心问题**：传统的代码相似性度量指标（如基于 Token、抽象语法树 AST 或 Embedding 的方法）主要关注代码的**语法表面**或**最终输出**，而忽略了**底层算法逻辑**。
*   **研究动机**：在 LLM-AAD 过程中，区分“真正的算法创新”与“单纯的代码重写”至关重要。例如，两个代码片段可能语法迥异但执行逻辑相同（如递归 vs 迭代的 DFS），或者语法极度相似但逻辑完全不同（如仅将 `argmin` 改为 `argmax`）。现有的指标在这些场景下表现极差。

### 2. 核心方法论：BehaveSim
*   **核心思想**：通过算法在解决问题过程中的“行为”来定义相似性。这种行为被量化为**问题解决轨迹（Problem-Solving Trajectories, PSTrajs）**，即算法执行过程中产生的一系列中间解或局部解。
*   **关键技术细节**：
    *   **轨迹定义**：将算法视为一个迭代过程 $x_{t+1} = f(x_t)$，收集序列 $T = (x_0, x_1, \dots, x_T)$。
    *   **点对距离计算**：根据解的类型（分类、排列、连续值等）采用编辑距离（Edit Distance）或欧氏距离（Euclidean Distance），并进行归一化。
    *   **轨迹对齐（DTW）**：使用**动态时间规整（Dynamic Time Warping）**来计算两个不同长度或存在时间偏移的轨迹之间的距离。DTW 能够捕捉局部行为的相似性。
    *   **相似度量化**：$Sim_{PSTraj} = 1 - \frac{DTW(X, Y)}{\min(|X|, |Y|)}$。最终的 `BehaveSim` 是在多个问题实例和起始点上的期望相似度。
*   **算法流程**：执行算法 -> 记录中间状态序列 -> 计算归一化点对距离 -> 应用 DTW 对齐 -> 得到行为相似度分数。

### 3. 实验设计
*   **数据集与场景**：
    1.  **算法相似性基准集**：人工构建了 4 类典型案例（Type 1-4），涵盖了“代码相似但行为不同”、“代码不同但行为相似”等极端情况。
    2.  **AAD 任务**：容许集问题（ASP）、旅行商问题（TSP）、圆堆积问题（CPP）。
*   **Benchmark 与对比方法**：
    *   **相似性指标对比**：ROUGE, BLEU, CrystalBLEU, AST, CodeBLEU, CodeBERTScore, Jina-Code-Embedding, Qwen3-Embedding, 以及基于执行结果和执行追踪（Execution Trace）的方法。
    *   **AAD 框架对比**：将 BehaveSim 集成到 **FunSearch** 和 **EoH**（Evolution of Heuristics）中，与原始版本进行对比。

### 4. 资源与算力
*   **LLM 模型**：实验使用了 **GPT-5-Nano**（通过 API 调用）。
*   **算力说明**：文中**未明确说明**具体的本地硬件配置（如 GPU 型号或数量）。由于主要任务是算法搜索和 Python 代码评估，算力消耗主要集中在 LLM API 的调用以及本地沙盒环境中运行生成的 Python 代码。
*   **评估规模**：TSP 和 CPP 任务设置了 2,000 次评估预算，ASP 任务设置了 10,000 次评估预算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 4 类相似性类型进行了详尽的对比测试。
    *   在 3 个主流 AAD 任务上进行了独立运行（3 次重复实验并取均值/标准差）。
    *   进行了消融实验，探讨了岛屿间选择概率（$p_{s1}$）和聚类操作对性能的影响。
*   **充分性评价**：实验设计较为充分且客观。通过人工构建的极端案例证明了现有指标的失效，随后在实际的自动化设计任务中验证了新指标的增益。消融实验进一步证明了“行为多样性”是提升搜索效率的关键。

### 6. 主要结论与发现
*   **现有指标失效**：静态代码指标和简单的输出对比无法捕捉算法的动态逻辑。
*   **BehaveSim 的优越性**：在 Type-3（代码不同但行为相同）案例中，BehaveSim 准确给出了 1.0 的相似度，而其他指标均表现不佳。
*   **提升 AAD 性能**：将 BehaveSim 作为多样性控制手段集成到进化算法中，显著提升了 FunSearch 和 EoH 在 ASP、TSP 和 CPP 任务上的表现，找到了更优的算法。
*   **算法分析工具**：BehaveSim 能够对 AI 生成的大量算法进行有效聚类，帮助研究人员识别出不同的解题策略（如“贪心策略” vs “前瞻策略”）。

### 7. 优点（亮点）
*   **视角创新**：跳出了“代码即文本”或“代码即结构”的传统框架，提出了“代码即行为”的评估维度。
*   **鲁棒性强**：利用 DTW 处理变长轨迹，能够容忍算法实现中的时间偏移或步长差异。
*   **即插即用**：作为一种度量工具，可以轻松集成到现有的各种进化搜索或强化学习框架中，用于维持种群多样性。

### 8. 不足与局限
*   **适用范围限制**：目前主要针对**迭代式/增量式**改进解的算法（如启发式搜索、优化算法），对于“一步到位”生成结果的算法（One-shot）适用性较弱。
*   **计算开销**：计算 BehaveSim 需要实际执行代码并记录轨迹，相比于静态分析，其计算成本更高，且依赖于测试用例的质量。
*   **维度单一**：目前仅关注行为相似性，未考虑算法的时间复杂度和空间复杂度等其他关键维度。
*   **随机性处理**：对于内部具有高度随机性的算法，如何稳定地获取代表性轨迹仍是一个挑战（文中虽有讨论，但仍需更多实证）。

（完）
