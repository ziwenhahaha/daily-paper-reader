---
title: "RankEvolve: Automating the Discovery of Retrieval Algorithms via LLM-Driven Evolution"
title_zh: RankEvolve：通过大语言模型驱动的演化实现检索算法发现的自动化
authors: "Jinming Nian, Fangchen Li, Dae Hoon Park, Yi Fang"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16932v1"
tags: ["query:ahd"]
score: 10.0
evidence: 通过 LLM 驱动的进化自动发现检索算法
tldr: 本研究针对传统词法检索算法长期依赖人工直觉和调优的局限，提出了RankEvolve框架。该框架利用大语言模型驱动程序进化，将检索算法表示为可执行代码，通过在多个数据集上进行迭代变异、重组和筛选，自动探索更优的排序逻辑。实验表明，RankEvolve成功发现了多种新颖且高效的算法，在BEIR、BRIGHT及TREC DL等基准测试中展现出卓越的性能和跨领域泛化能力，为检索算法的自动化设计开辟了新路径。
selection_source: fresh_fetch
motivation: 传统检索算法如BM25主要依赖人工直觉和参数微调，缺乏自动化的创新发现机制。
method: 提出RankEvolve框架，利用LLM对以代码形式表示的检索算法进行迭代变异、重组和基于性能的进化搜索。
result: 进化出的新算法在BEIR、BRIGHT和TREC DL等多个基准测试中均优于初始种子算法，并表现出良好的迁移性。
conclusion: 由评估器引导的LLM程序进化是自动发现新型高效排序算法的一条切实可行的路径。
---

## 摘要
BM25 和带有狄利克雷平滑（Dirichlet smoothing）的查询似然（query likelihood）等检索算法仍然是强大且高效的第一阶段排序器，但其改进主要依赖于参数调优和人类直觉。我们研究了在评估器和演化搜索的指导下，大语言模型是否能够自动发现改进的词法检索算法。我们介绍了 RankEvolve，这是一种基于 AlphaEvolve 的程序演化设置，其中候选排序算法被表示为可执行代码，并根据在 BEIR 和 BRIGHT 的 12 个信息检索（IR）数据集上的检索性能进行迭代变异、重组和筛选。RankEvolve 从两个种子程序开始：BM25 和带有狄利克雷平滑的查询似然。演化出的算法具有新颖性、有效性，并在完整的 BEIR 和 BRIGHT 基准测试以及 TREC DL 19 和 20 上表现出良好的迁移能力。我们的结果表明，评估器指导的大语言模型程序演化是实现自动发现新颖排序算法的一条切实可行的路径。

## Abstract
Retrieval algorithms like BM25 and query likelihood with Dirichlet smoothing remain strong and efficient first-stage rankers, yet improvements have mostly relied on parameter tuning and human intuition. We investigate whether a large language model, guided by an evaluator and evolutionary search, can automatically discover improved lexical retrieval algorithms. We introduce RankEvolve, a program evolution setup based on AlphaEvolve, in which candidate ranking algorithms are represented as executable code and iteratively mutated, recombined, and selected based on retrieval performance across 12 IR datasets from BEIR and BRIGHT. RankEvolve starts from two seed programs: BM25 and query likelihood with Dirichlet smoothing. The evolved algorithms are novel, effective, and show promising transfer to the full BEIR and BRIGHT benchmarks as well as TREC DL 19 and 20. Our results suggest that evaluator-guided LLM program evolution is a practical path towards automatic discovery of novel ranking algorithms.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **RankEvolve**，这是一个利用大语言模型（LLM）驱动程序演化，从而自动发现新型词法检索算法的框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的词法检索算法（如 BM25、查询似然 QL）虽然在第一阶段检索中表现强劲，但其改进长期依赖于人工直觉和繁琐的参数微调。
*   **研究动机**：作者探讨是否可以利用 LLM 作为优化器，在没有人工干预的情况下，通过演化搜索自动发现比现有标准算法更优的排序逻辑。这标志着从“人工设计算法”向“自动发现算法”的范式转变。

### 2. 方法论
RankEvolve 将检索算法表示为可执行的 Python 代码，并通过以下四个核心组件进行迭代优化：
*   **搜索空间定义**：以 BM25 和 QL-Dir 为种子程序（Seed Programs），将其分解为可进化的抽象组件（如文档表示、查询表示、评分函数）。
*   **种群管理**：结合了**孤岛模型（Island-based evolution）**和 **MAP-Elites 算法**。种群被划分为多个独立进化的孤岛，并根据代码复杂度和多样性（编辑距离）将程序映射到网格中，以避免陷入局部最优。
*   **LLM 驱动的变异**：使用 LLM（如 GPT-5.2）作为变异算子。系统向 LLM 提供当前表现最佳的程序、详细的评估指标以及演化历史，要求其以 `SEARCH/REPLACE` 的 diff 格式提出改进建议。
*   **评估器（Evaluator）**：在 12 个 IR 数据集上运行候选程序，计算适应度得分（Fitness Score）。公式定义为：$0.8 \times \text{Avg Recall@100} + 0.2 \times \text{Avg nDCG@10}$，侧重于召回率以符合第一阶段检索的场景。

### 3. 实验设计
*   **数据集**：共涉及 28 个数据集。
    *   **演化阶段**：使用来自 BEIR 和 BRIGHT 的 12 个子集进行训练/演化。
    *   **测试阶段（泛化性验证）**：在 16 个完全未见的数据集上测试，包括 BEIR 剩余子集、BRIGHT 剩余子集以及 TREC DL 2019/2020。
*   **对比基准（Benchmarks）**：
    *   **BM25 系列**：标准 BM25、BM25+、BM25-adpt。
    *   **查询似然系列**：QL-Dirichlet、QL-Jelinek-Mercer。
*   **实验流程**：从种子程序开始，BM25 演化 300 步，QL-Dir 演化 200 步。

### 4. 资源与算力
*   **LLM 算力**：实验使用了 **GPT-5.2**（通过 API 调用），设置温度系数为 0.85。
*   **本地算力**：论文未明确说明运行检索评估（索引、分词、检索）的具体 GPU 或 CPU 型号及具体耗时。但提到演化步数分别为 300 和 200 步，每一步都需要在 12 个数据集上进行完整的索引和检索评估，计算开销较大。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 28 个数据集，这在信息检索研究中是非常广泛且具有代表性的。
*   **消融实验**：作者进行了关于“种子程序结构自由度”的消融实验，对比了受限型（仅调参）、组合型（模块化重写）和自由型（全代码演化）的效果。
*   **客观性**：通过在 16 个未见数据集上进行测试，有效地证明了算法的泛化能力，而非仅仅是对 12 个演化数据集的过拟合。

### 6. 主要结论与发现
*   **性能超越**：演化出的算法（BM25★ 和 QL-Dir★）在多个基准测试中显著优于原始种子算法和已知的人工变体。
*   **独立发现 IR 原理**：LLM 在演化过程中自动“重新发现”了许多经典的 IR 概念，如**软停用词过滤、前缀匹配（类似词干提取）、自适应词频饱和以及非线性文档长度归一化**。
*   **结构自由度的重要性**：实验证明，给予 LLM 越多的代码修改自由度，最终发现的算法性能越强。
*   **收敛性**：尽管起始点不同，两种演化路径最终都趋向于相似的高层策略（如协调机制和精细的长度惩罚）。

### 7. 优点与亮点
*   **首创性**：这是首次尝试通过 LLM 引导的程序演化来发现完整的检索算法。
*   **可解释性**：与黑盒神经网络不同，RankEvolve 输出的是 Python 代码，人类研究员可以阅读并理解其背后的数学逻辑。
*   **泛化能力强**：演化出的算法在推理密集型任务（BRIGHT）和标准 Web 检索（TREC DL）中均表现良好。

### 8. 不足与局限
*   **延迟开销**：演化出的算法结构复杂，导致检索延迟显著增加（BM25★ 比原始 BM25 慢约 11 倍）。
*   **效率未优化**：演化目标仅关注准确率指标，未将计算效率（Latency）纳入优化目标。
*   **LLM 偏见风险**：LLM 可能在训练数据中见过 BM25 的变体，其“发现”可能部分源于对已有文献的记忆而非纯粹的逻辑推演。
*   **成本问题**：依赖昂贵的商业 LLM API 进行数百步迭代，对于资源有限的研究者门槛较高。

（完）
