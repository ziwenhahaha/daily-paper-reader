---
title: Large Language Models for Designing Participatory Budgeting Rules
title_zh: 用于设计参与式预算规则的大语言模型
authors: "Nguyen Thach, Xingchen Sha, Hau Chan"
date: 2026-02-10
pdf: "https://arxiv.org/pdf/2602.09349v2"
tags: ["query:ahd"]
score: 9.0
evidence: 大语言模型用于预算规则的自动化算法设计
tldr: 参与式预算（PB）是分配公共资金的民主模式，但设计兼顾效用与公平的规则极具挑战。本文提出LLMRule框架，利用大语言模型（LLM）结合进化搜索算法，自动化设计PB规则。通过对美、加、波、荷等国600多个真实案例的测试，证明LLM生成的规则在保持公平性的同时，效用优于传统人工设计的规则，为自动化算法设计提供了新思路。
selection_source: fresh_fetch
motivation: 传统参与式预算规则设计依赖大量领域知识，且难以在效用与公平性之间取得理想平衡。
method: 提出LLMRule框架，将大语言模型集成到进化搜索过程中，实现参与式预算规则的自动化设计。
result: 在多个国家的600多个真实PB实例上，LLM生成的规则在总效用上超越了现有手工规则，且公平性表现相当。
conclusion: 研究证明了利用LLM自动化设计复杂社会选择规则的可行性，为优化民主决策机制提供了高效的新工具。
---

## 摘要
参与式预算（PB）是一种根据居民偏好决定公共项目资金分配的民主范式，已在全球众多城市中得到采用。PB 的核心在于设计规则，即在满足预算约束的情况下，为一组项目返回可行预算分配方案的函数。由于需要广泛的领域知识，且效用与公平性这两个概念之间已被证明存在权衡（trade-off），基于智能体偏好设计同时优化效用和公平性目标的 PB 规则一直具有挑战性。近年来，大语言模型（LLMs）越来越多地被用于自动化算法设计。鉴于 PB 规则与经典背包问题算法的相似性，本文提出了一个名为 LLMRule 的新颖框架，通过将 LLM 融入进化搜索过程来自动化设计 PB 规则，从而解决了现有工作的局限性。我们在来自美国、加拿大、波兰和荷兰的 600 多个真实 PB 实例（具有不同的智能体偏好表示）上进行了评估，实验结果表明，LLM 生成的规则在总效用方面通常优于现有的手工设计规则，同时仍保持了相似的公平程度。

## Abstract
Participatory budgeting (PB) is a democratic paradigm for deciding the funding of public projects given the residents' preferences, which has been adopted in numerous cities across the world. The main focus of PB is designing rules, functions that return feasible budget allocations for a set of projects subject to some budget constraint. Designing PB rules that optimize both utility and fairness objectives based on agent preferences had been challenging due to the extensive domain knowledge required and the proven trade-off between the two notions. Recently, large language models (LLMs) have been increasingly employed for automated algorithmic design. Given the resemblance of PB rules to algorithms for classical knapsack problems, in this paper, we introduce a novel framework, named LLMRule, that addresses the limitations of existing works by incorporating LLMs into an evolutionary search procedure for automating the design of PB rules. Our experimental results, evaluated on more than 600 real-world PB instances obtained from the U.S., Canada, Poland, and the Netherlands with different representations of agent preferences, demonstrate that the LLM-generated rules generally outperform existing handcrafted rules in terms of overall utility while still maintaining a similar degree of fairness.

---

## 论文详细总结（自动生成）

这是一份关于论文《Large Language Models for Designing Participatory Budgeting Rules》的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究背景**：参与式预算（Participatory Budgeting, PB）是现代民主决策的重要形式，旨在根据居民偏好分配公共资金。
*   **核心挑战**：设计 PB 规则（即分配算法）极具挑战性，因为需要在**效用最大化**（Utilitarian Social Welfare）与**公平性**（如比例代表性）之间取得平衡。现有的规则大多由专家手工设计，且理论上已证明两者之间存在难以调和的权衡（Trade-off）。
*   **研究动机**：利用大语言模型（LLM）在自动化算法设计方面的潜力，探索是否能通过 LLM 自动发现比手工设计更优、且能兼顾效用与公平的 PB 规则。

### 2. 论文提出的方法论：LLMRule 框架
*   **核心思想**：将 LLM 集成到进化搜索（Evolutionary Search）过程中。LLM 负责生成、交叉和变异用 Python 编写的启发式规则代码，通过迭代优化规则的性能。
*   **关键技术细节**：
    *   **Strong-EJR 近似指标**：针对公平性难以量化的问题，论文基于“强扩展正当代表性”（Strong-EJR）定义了一个连续的公平性目标函数 $\phi$，用于衡量分配方案对凝聚力群体的保护程度。
    *   **高效验证算法**：证明了 Strong-EJR 的验证可以简化为仅检查“极大凝聚力群体”，利用频繁项集挖掘算法（如 Apriori）将验证复杂度从指数级降低，使其在训练循环中可行。
    *   **适应度函数（Fitness Function）**：定义为 $q(f) = \text{相对效用} - \text{公平性惩罚项}$。如果规则生成的方案公平性低于设定阈值，则会受到严厉惩罚，从而引导 LLM 设计出“本质公平”的规则。
    *   **提示词细化（Prompt Refinement）**：引入了一个元模块，当搜索停滞时，让 LLM 自动生成新的提示词策略（探索型或修改型），以维持种群多样性。

### 3. 实验设计
*   **数据集**：使用了来自 **Pabulib** 数据库的 **617 个真实 PB 实例**，涵盖美国、加拿大、波兰和荷兰。
*   **场景设置**：
    *   **赞成票（Approval Ballots）**：包括成本满意度（sat_cost）和数量满意度（sat_card）两种设置。
    *   **基数票（Cardinal Ballots）**：选民为项目打分。
*   **Benchmark 与对比方法**：
    *   **效用类**：MaxUtil（理论最优效用）、GreedUtil（贪心效用）。
    *   **公平类**：MES（等额分享法）、SeqPhrag（顺序 Phragmén 规则）、MaximinSupp（最大化支持规则）及其多种补全变体（Add1, Add1U 等）。
*   **测试方式**：在小规模实例上训练，在更大规模、不同分布的实例上进行分布外（OOD）测试。

### 4. 资源与算力
*   **硬件环境**：一台运行 Ubuntu 20.04 的虚拟机，配置为 **NVIDIA GeForce RTX 3050 Ti GPU** 和 **12th Gen Intel Core i7-12700H CPU**。
*   **模型使用**：选用 **GPT-4o mini** 作为底层 LLM。
*   **训练时长**：每个实验设置通常运行 3 次，单次进化过程（20 个种群迭代）大约耗时 **30 分钟**。

### 5. 实验数量与充分性
*   **实验规模**：使用了超过 600 个真实案例，这在计算社会选择领域属于大规模评估。
*   **充分性**：
    *   涵盖了多种选票类型（赞成票 vs 基数票）和多种满意度定义。
    *   进行了 **ID（分布内）** 和 **OOD（分布外）** 测试，验证了规则的泛化能力。
    *   附录中还补充了基于二维欧几里得模型的**合成数据实验**。
*   **客观性**：通过与多种最先进的手工规则（如 MES 的各种变体）进行 Pareto 前沿对比，客观展示了 LLM 规则在效用-公平权衡中的优越位置。

### 6. 论文的主要结论与发现
*   **性能超越**：LLM 生成的规则在总效用上显著优于现有的比例代表性规则（如 MES），同时保持了与之相当甚至更高的公平性水平。
*   **泛化性强**：在小规模数据上发现的规则，直接应用到选民人数多达数千人的大规模实例上依然表现稳健。
*   **规则创新**：LLM 能够发现一些非直观的数学组合（如结合项目成本比例与赞成率的几何平均数），这些规则比传统的简单贪心规则更精细。
*   **作为补全工具**：LLM 生成的规则不仅可以独立运行，作为 MES 等非完备规则的补全方法（Completion Method）时，效果也优于传统的补全策略。

### 7. 优点与亮点
*   **首创性**：这是首个将 LLM 用于自动化设计参与式预算规则的研究。
*   **理论与实践结合**：不仅是简单的 Prompt Engineering，还通过数学证明优化了公平性验证的计算效率，解决了 LLM 评估过程中的计算瓶颈。
*   **低门槛**：该框架减少了对领域专家深厚背景知识的依赖，能够自动适应不同的效用目标。

### 8. 不足与局限
*   **项目数量限制**：由于公平性验证仍涉及频繁项集挖掘，当项目数量 $m$ 极大时（如超过 50-100 个），计算开销可能会显著增加。
*   **规则类型局限**：目前主要集中在设计“贪心优先级函数”，对于更复杂的非贪心类规则结构探索较少。
*   **黑盒风险**：虽然生成的 Python 代码是可读的，但 LLM 为何选择特定的数学形式（如特定的对数或平方根组合）仍缺乏深层的理论解释。

（完）
