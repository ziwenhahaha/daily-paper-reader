---
title: "elasticAI.explorer: Towards a Unified End-to-End Framework for Hardware-Aware Neural Architecture Search"
title_zh: elasticAI.explorer：迈向硬件感知神经架构搜索的统一端到端框架
authors: "Natalie Maman, Florian Hettstedt, Andreas Erbslöh, Gregor Schiele"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30019v1"
tags: ["query:dgnn-nas"]
score: 8.0
evidence: 硬件感知的神经架构搜索框架
tldr: 针对现有神经架构搜索（NAS）框架耦合度高、难以扩展至新硬件平台的问题，本文提出了一个基于Python和Optuna的硬件感知NAS框架elasticAI.explorer。该框架通过YAML动态定义搜索空间，并集成了硬件代码生成、Docker交叉编译和自动化设备基准测试，旨在降低嵌入式AI部署的工程开销，加速异构加速器平台的NAS研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30019v1/fig-001.webp\", \"caption\": \"Fig. 1. Overview of the end-to-end architecture within the elasticAI.explorer from defining the search space over candidate sampling to the deployment. Interpretation of the highlighted boxes: yellow = user inputs, blue = search space sampling and model building, purple = model performance evaluation / estimation, red = hardware deployment part\", \"page\": 2, \"index\": 1, \"width\": 783, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2605.30019v1/fig-002.webp\", \"caption\": \"Fig. 2. Different types of NAS search space paradigms: (a) layer-wise - (b) cell-based - (c) hierarchical. Red components are considered during NAS. Grey components are decided manually. Image adapted from [18].\", \"page\": 3, \"index\": 2, \"width\": 521, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2605.30019v1/fig-003.webp\", \"caption\": \"TABLE I DIFFERENT REPEAT OPTIONS FOR BLOCK OPERATOR SAMPLING.\", \"page\": 5, \"index\": 3, \"width\": 1076, \"height\": 208}]"
motivation: 现有的NAS框架将搜索空间、模型实现和部署管道紧密耦合，导致向新硬件平台和自定义算子的扩展变得非常困难。
method: 构建了一个可扩展的框架，利用YAML动态定义搜索空间生成模型，并集成硬件代码生成、Docker交叉编译工具链及自动化设备基准测试以实现硬件在环工作流。
result: 该框架成功支持了层级、单元级和分层搜索空间，并提供了包含FLOPs、参数量和延迟评估在内的统一优化与部署接口。
conclusion: elasticAI.explorer有效降低了嵌入式AI部署的工程开销，为异构加速器平台上的硬件感知NAS研究提供了统一的端到端解决方案。
---

## 摘要
神经架构搜索（NAS）已成为在特定任务和特定硬件约束下自动设计神经网络的重要方法。然而，许多现有的NAS框架将搜索空间定义、模型实现和部署流程紧密耦合，使得向新硬件平台和自定义算子的扩展变得困难。在本文中，我们提出了elasticAI.explorer，这是一个基于Optuna构建的、用于硬件感知NAS的可扩展Python框架。该框架引入了基于YAML的搜索空间规范，该规范在采样过程中动态转换为可执行的神经网络模型。该方法支持逐层、基于单元和分层搜索空间，同时为优化和部署维护了统一的接口。除了架构生成之外，该框架还集成了特定硬件的代码生成、基于Docker的交叉编译工具链，以及设备端基准测试二进制文件的自动创建，从而实现了硬件在环的NAS工作流。该系统进一步为FLOPs、参数量和延迟估计提供了可扩展的评估器。elasticAI.explorer旨在降低嵌入式AI部署的工程开销，并加速针对异构加速器平台的硬件感知NAS研究。

## Abstract
Neural Architecture Search (NAS) has become an important approach for automatically designing neural networks under task-specific and hardware-specific constraints. However, many existing NAS frameworks tightly couple search space definitions, model implementations, and deployment pipelines, making extension to new hardware platforms and custom operators difficult. In this paper, we present the elasticAI.explorer, an extensible Python framework for hardware-aware NAS built on top of Optuna. The framework introduces a YAML-based search space specification that dynamically translates into executable neural network models during sampling. The approach supports layer-wise, cell-based, and hierarchical search spaces while maintaining a unified interface for optimization and deployment. Beyond architecture generation, the framework integrates hardware-specific code generation, Docker-based cross-compilation toolchains, and automated creation of on-device benchmarking binaries, enabling hardware-in-the-loop NAS workflows. The system further provides extensible evaluators for FLOPs, parameter count, and latency estimation. The elasticAI.explorer aims to reduce the engineering overhead of embedded AI deployment and accelerate research on hardware-aware NAS for heterogeneous accelerator platforms

---

## 论文详细总结（自动生成）

以下是对论文《elasticAI.explorer: Towards a Unified End-to-End Framework for Hardware-Aware Neural Architecture Search》的详细结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的神经架构搜索（NAS）框架通常将搜索空间定义、模型实现代码和硬件部署流程紧密耦合。这导致研究人员在向新的嵌入式硬件平台扩展，或者引入自定义算子时，需要耗费大量的手动工程精力。
*   **整体含义与背景**：在嵌入式深度学习场景中，神经网络不仅需要满足任务精度要求，还必须严格服从硬件的延迟、内存占用和资源使用等约束。虽然硬件感知NAS（HW-NAS）能有效平衡精度与硬件效率，但缺乏一个统一、解耦且能实现“搜索到部署”端到端自动化的工具。本文旨在填补这一空白，提出一个高度模块化、可扩展的框架，以降低嵌入式AI部署的工程门槛。

### 2. 论文提出的方法论：核心思想、关键技术细节
*   **核心思想**：基于 Optuna 构建了一个名为 `elasticAI.explorer` 的端到端 Python 框架。其核心在于**解耦**——将搜索空间的声明、模型的动态构建、性能评估以及硬件部署完全模块化。
*   **关键技术细节**：
    *   **基于 YAML 的搜索空间规范**：提出了一种领域特定语言（DSL），用户通过 YAML 文件声明式地定义搜索空间。该 DSL 支持“块”的组合与重复机制，能够统一表达三种主流的 NAS 搜索空间范式：逐层、基于单元和分层搜索空间。
    *   **动态实例化与适配器**：在 Optuna 采样出试验参数后，框架的 `ModelBuilder` 会动态将 YAML 配置转化为可执行的 PyTorch 模型。对于不兼容的层，框架会自动插入适配器模块处理张量形状变换，无需手动编写连接逻辑。
    *   **预处理搜索空间**：除了模型架构，框架还支持将传感器信号的预处理（如滤波、降采样）纳入 NAS 搜索过程，实现端到端的信号处理流水线优化。
    *   **硬件感知部署管道**：包含生成器系统，可将模型转换为特定硬件的工件。支持基于 Docker 的交叉编译工具链以确保环境可复现。
    *   **硬件在环**：框架不仅支持传统的分析式成本估计（如 FLOPs、参数量），还能在 NAS 搜索过程中，将候选模型自动编译并部署到真实物理设备上运行，以获取最真实的延迟等硬件指标并反馈给优化循环。

### 3. 实验设计：数据集、场景与对比方法
*   **说明**：本文是一篇**框架/系统型论文**，其重点在于软件架构的设计与功能展示，**并未包含传统的定量比较实验**。
*   **场景展示**：文中通过代码清单展示了如何使用 YAML 定义一个一维卷积分类器的搜索空间，并提及了框架在信号处理/时间序列传感器数据场景下的适用性。
*   **对比方法**：仅在“相关工作”章节中与现有的 NAS 框架（如 NNI, NASLib, Archai, AW-NAS, HANNAH）进行了定性的特性对比，指出 `elasticAI.explorer` 的独特优势在于声明式的搜索空间定义和深度的硬件在环集成。

### 4. 资源与算力
*   **未明确说明**：由于本文主要介绍软件框架的架构设计、API 和工作流，文中**未提及**具体的算力消耗、GPU 型号、数量或搜索时长的数据。

### 5. 实验数量与充分性
*   **实验数量**：无传统的基准数据集精度或搜索效率的定量实验。论文的验证主要体现在系统原型的实现（已开源在 GitHub）以及框架各模块（DSL 解析、模型构建、硬件部署）的连贯性设计上。
*   **充分性与客观性**：作为一篇工具/框架发表周期的论文，其系统设计的完备性（支持多种搜索空间、多种硬件后端）是充分的。但是，由于缺乏在标准 NAS 基准（如 NAS-Bench）上的性能对比，无法客观评估该框架在实际运行中的搜索效率或系统开销。

### 6. 论文的主要结论与发现
*   `elasticAI.explorer` 成功提供了一个统一的、解耦的端到端 HW-NAS 框架。
*   通过引入声明式的 YAML 搜索空间定义，极大降低了定义复杂（层级、单元级）架构空间的复杂度。
*   集成的硬件在环和 Docker 编译工具链，使得针对嵌入式设备（如树莓派、MCU、FPGA）的自动化基准测试和模型部署成为可能，有效减少了嵌入式 AI 的工程阻力。

### 7. 优点：方法或实验设计上的亮点
*   **高度解耦与可扩展**：通过注册器机制，研究人员可以极低的成本引入自定义层或硬件特定的原语，无需修改 NAS 核心引擎。
*   **统一的搜索空间表达**：用一套 YAML 语法统一表达了逐层、Cell 和分层三种复杂的 NAS 搜索空间，设计非常优雅。
*   **真实硬件反馈**：支持“硬件在环”是本文最大的工程亮点，相比依赖理论计算的代理模型，直接在真实设备上测延迟能获得最准确的部署约束。
*   **全栈优化**：将数据预处理参数与模型架构参数放在同一个 NAS 工作流中联合优化，对物联网传感器场景非常实用。

### 8. 不足与局限
*   **缺乏定量评估**：论文没有提供使用该框架进行实际搜索的案例研究，缺乏搜索时间、内存消耗、找到的模型精度与基线方法的定量对比。
*   **硬件后端的实际表现未知**：虽然宣称支持 FPGA（elasticAI.creator）和微控制器，但未展示具体的端到端运行示例和性能数据。
*   **未来工作的局限**：目前框架尚未集成高级的 NAS 加速技术（如 Supernet 或 One-shot NAS），主要依赖 Optuna 的采样算法，在超大规模搜索空间中可能面临效率瓶颈。

（完）
