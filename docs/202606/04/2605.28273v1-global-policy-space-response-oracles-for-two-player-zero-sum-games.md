---
title: Global Policy-Space Response Oracles for Two-Player Zero-Sum Games
authors: "Junyu Zhang, Feihong Yang, Jian Wang, Chao Wang, Xudong Zhang"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28273v1"
tags: ["query:game-ai"]
score: 9.0
evidence: 深度强化学习游戏AI用于零和博弈均衡计算
tldr: PSRO结合深度强化学习引导种群扩展以计算两人零和博弈均衡。
source: arxiv
selection_source: fresh_fetch
motivation: 深度强化学习游戏AI用于零和博弈均衡计算。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
The Policy-Space Response Oracles (PSRO) framework scales equilibrium computation to large zero-sum games by iteratively expanding a restricted strategy set using deep reinforcement learning (DRL). A central challenge is to construct, under limited computational budgets, a small strategy population whose induced game well approximates the full game. Existing PSRO variants typically expand the population using best responses to meta-strategies computed from restricted-game payoffs, which can lead to inefficient expansions that provide limited global improvement. We propose to guide population expansion by directly evaluating the post-expansion population quality. Specifically, we adopt Population Exploitability (PE) to measure how well a restricted strategy set represents the full game, and introduce a two-phase exploration--selection framework that explicitly minimizes PE during expansion. We instantiate this framework as Global PSRO, a practical DRL-based algorithm that efficiently generates candidate responses and estimates PE via parameter-sharing conditional neural networks. Experiments across multiple two-player zero-sum games show that Global PSRO achieves lower exploitability and approximates Nash equilibria with significantly fewer policy iterations than prior PSRO methods.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：在大规模两人零和博弈中计算均衡（如纳什均衡）是极具挑战性的问题，策略空间庞大且 exhaustive 分析不可行。  
- **PSRO 框架**：Policy-Space Response Oracles (PSRO) 通过迭代扩展受限策略集合，并利用深度强化学习（DRL）学习最佳响应（Best Response, BR），已在多智能体学习中取得成功。  
- **核心难题**：如何在有限的计算预算下，构建一个规模小但能忠实近似完整博弈的策略种群（population）。现有 PSRO 变种通常仅基于受限博弈（restricted game）的收益来计算元策略（meta‑strategy），导致种群扩展效率低下，甚至在某些对抗构造的博弈中需要加入几乎全部策略才能收敛。  
- **研究动机**：引入**全局信息**（即完整博弈的收益结构）来指导种群扩展，使每次扩展都能直接评估其对整体均衡近似的改进程度，从而提升迭代效率。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想
- **Population Exploitability (PE)**：定义 `PE(Πr; G) = min_{σ∈Δ(Πr)} ε(σ)`，衡量受限策略集合 Πr 对完整博弈 G 的近似质量。  
- **目标**：在每次迭代中，选择能够最小化扩展后 PE 的候选策略，实现种群扩展的全局优化。  
- **两阶段探索–选择框架**：
  1. **探索阶段（Exploration）**：生成多样化的候选响应集合。  
  2. **选择阶段（Selection）**：对每个候选计算扩展后的 PE，选取得分最低的候选并将其对应的评估 BR 也加入种群。

### 2.2 关键技术细节
- **候选生成**：维护一个混合策略池 `St = {σk}`，其中包含基础 RGB‑MSS（如受限博弈 NE）产生的混合策略，并辅以从 Dirichlet(1) 均匀采样的混合策略。使用 **参数共享的条件策略网络** `πθ(a|s, σ)` 同时为所有 σk 生成最佳响应，得到候选集 `Π+_k = Πr ∪ {πθ(·|σk)}`。  
- **PE 估计**：在选择阶段，对每个候选 Π+_k 运行 **RM–BR（Regret‑Minimization Best Response）** 过程估计 PE。为避免为每个候选训练单独的 BR 网络，采用共享的条件评估器 `βψ(a|s, σ)`，对每个候选切片得到 β_k。  
- **正则化评分**：由于 PE 估计存在误差，提出正则化评分  
  `cPE_reg_k = (1 - \hat{p}_k) [cPE(Πr; G) - U(β_k, ρ_r)] + cPE_k`，其中 \hat{p}_k 是候选策略在新混合中的权重，防止候选策略未获得足够权重时产生误导性低 PE。  
- **算法流程**（见论文 Algorithm 1）：
  1. 形成混合策略池 St（含 base MSS）。  
  2. 用 DRL 更新 Explorer `πθ` 对所有 σk 同时训练。  
  3. 生成候选响应集合并构建候选种群。  
  4. 对每个候选运行 RM–BR 估计 PE。  
  5. 按正则化 PE 选择最优候选 k⋆，并将 π_{k⋆} 与 β_{k⋆} 同时加入种群。  
  6. 更新受限收益矩阵，重复直至收敛。

## 3. 实验设计：数据集、基准、对比方法

### 3.1 数据集与场景
- **Kuhn Poker**（两人扑克，规模小）  
- **Liar’s Dice**（1 颗骰子、3 面）  
- **Leduc Poker**（两人扑克）  
- **Goofspiel**（5 张牌和 13 张牌两种规模）  
- 以上均为 OpenSpiel 库实现的两人零和扩展形式博弈。

### 3.2 基准（Benchmark）
- 使用 **Population Exploitability (PE)** 作为主要评估指标，同时报告 **Exploitability**（衡量个体混合策略的可利用性）。

### 3.3 对比方法
| 类别 | 方法 |
|------|------|
| PSRO + 各类元策略求解器（MSS） | Restricted‑Game NE (Nash)、AlphaRank、Projected Replicator Dynamics (PRD)、Uniform、Anytime PSRO |
| 多样性驱动 PSRO | PSD‑PSRO（使用 KL‑散度正则化） |
| 条件种群 PSRO | NeuPL（Neural Population Learning） |
| 本文方法 | Global PSRO（ Ours） |

- 所有方法在 **相同的環境交互步数（environment interaction steps）** 预算下进行比较，确保公平。

## 4. 资源与算力

- **硬件平台**（论文 C.3 节）：
  - CPU：Intel Xeon Gold 6348 @ 2.60 GHz  
  - GPU：8 × NVIDIA RTX A6000  
  - 内存：512 GB  
  - 操作系统：Ubuntu 18.04  
- **训练预算**：以“环境交互步数”为统一计量单位，论文未明确给出实际 wall‑time 时长。不同游戏的超参数（如每轮 BR 训练步数）见 Table 2‑6，例如 Leduc Poker 每轮 BR 训练 8×10⁵ 步，Payoff 估计每策略 4×10⁴ 步。

## 5. 实验数量与充分性

- **实验规模**：
  - 5 种博弈 × 多种基线方法的 PE 曲线（Figure 4、5、6）以及大尺度 Goofspiel（13 张牌）的表格对比（Table 1）。  
  - **消融实验**（5 种变体）：仅利用（Exploitation only）、随机选择、无 PE 正则化、邻居搜索（Neighbor‑search）探索、Exact PE 评价（不计预算）。  
  - **额外结果**：附录中包含对抗构造的最坏情况博弈（Figure 8）、BR 分布可视化（Figure 9）、更多多样性驱动 PSRO 对比（Figure 10）、NeuPL 额外对比（Figure 11）以及 Exploitability 指标（Figure 12）。  
- **客观性与公平性**：
  - 所有学习类方法均使用相同的策略网络架构（MLP，隐藏层 128），并采用相同的 PPO 超参数。  
  - 报告 **4 次独立运行** 的均值与方差，保证结果可复现且噪声可控。  
  - 在相同的交互预算下比较，避免因算力差异导致的优势。  
- **充分性**：实验覆盖了从小规模（Kuhn Poker）到中等规模（Liar’s Dice、Leduc）再到大规模（Goofspiel 13 张牌）的多种博弈，并同时进行基线、消融和额外验证，实验设计较为完整。

## 6. 论文的主要结论与发现

1. **Global PSRO 在所有测试博弈中均实现更低的 PE**，尤其在大尺度 Goofspiel（13 张牌）上，PE 从 0.579（PSRO‑Nash）降至约 0.046（在 153.6 M 步时）。  
2. **迭代效率显著提升**：在相同交互预算下，Global PSRO 收敛所需的策略迭代次数明显少于所有基线方法。  
3. **消融验证**：移除任何关键组件（仅利用、随机选择、PE 正则化等）均导致性能下降，证明 **全局 PE 选择是性能提升的主要来源**。  
4. **理论与实践互补**：理论分析表明基于受限博弈的 MSS 在最坏情况下需遍历全部策略，而 Global PSRO 的两阶段框架能够在理论上保持收敛性并在实际中绕过最坏情况。  
5. **参数共享与条件网络**使得多候选生成和 PE 估计在计算上可行，兼顾了可扩展性与有效性。

## 7. 优点

- **全局优化目标**：直接以最小化扩展后 PE 为目标， 与 PSRO 的根本目标（构建高质量策略种群）高度对齐。  
- **两阶段框架**：探索阶段产生多样化候选，选择阶段依据真实全局改进进行筛选，兼顾探索与 exploitation。  
- **理论与实验相结合**：提供了针对受限博弈 MSS 局限性的形式化证明（Theorem 3.2、3.3）以及框架收敛保证（Theorem 4.2、4.3），不只是经验性改进。  
- **可扩展实现**：通过参数共享条件网络在一次训练中生成 K 个候选响应，并用共享的 PE 评估器，避免了为每个候选单独训练的高成本。  
- **实验严谨**：在多种博弈、多种基线和消融设定下进行公平比较，并报告方差，表明结果稳健。  

## 8. 不足与局限

- **仅限两人零和博弈**：论文聚焦于两人零和设置，PE 在一般和博弈中不再具备鞍点结构，难以直接迁移；作者亦指出将框架推广到多人一般和博弈是未来工作。  
- **PE 估计依赖近似**：实际使用 RM–BR 与正则化评分进行 PE 近似，误差不可避免，虽然正则化提升了鲁棒性，但在极高精度要求的场景可能仍显不足。  
- **候选池大小 K 的选择**：K 影响探索多样性与计算开销，论文未系统探讨不同 K 对收敛速度的影响。  
- **实验规模相对有限**：虽然覆盖了多种博弈，但最大只到 13 张牌的 Goofspiel，未在大规模实际游戏（如完整棋牌或实时策略游戏）上进行验证。  
- **对博弈结构假设**：理论分析基于有限策略集合，对连续或混合动作空间的适用性未讨论。  
- **正则化参数手工设定**：正则化权重等超参数在实验中固定，未进行自动化调优或敏感性分析。

（完）
