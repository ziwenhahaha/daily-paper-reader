---
title: "SIGMA: A Versatile Streaming Graph Partitioner for Vertex- and Edge-Balanced Distributed GNN Training"
title_zh: SIGMA：一种面向顶点与边缘平衡的分布式GNN训练的通用流式图分割器
authors: "Barbara Hoffmann, Shai Dorian Peretz, Adil Chhabra, Ahmet Kadir Yalcinkaya, Ruben Mayer, Christian Schulz"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03519v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 支持分布式GNN顶点与边平衡的流式图分割器
tldr: 分布式图神经网络（GNN）训练高度依赖图划分质量，但现有方法通常只关注单一的划分模式（点或边）及单一优化目标。为此，本文提出了SIGMA，一种支持顶点和边划分的统一流式图划分器。它通过多目标、多约束框架同时优化通信与负载均衡，并引入基于聚类的预处理以提升划分质量。实验表明，SIGMA在多种系统下均表现出色，有效解决了分布式GNN训练中的通信、计算和内存挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03519v1/fig-001.webp\", \"caption\": \"Figure 7: Memory consumption of GNN training under vertex partitioning (DistDGL): peak RAM usage acrossWorkers and peak GPU usage across workers. Blue bars represent streaming approaches, while orange bars represent in-memory approaches.\", \"page\": 11, \"index\": 1, \"width\": 1056, \"height\": 267}, {\"url\": \"assets/figures/arxiv/2606.03519v1/fig-002.webp\", \"caption\": \"Figure 1: Comparison of GNN partitioning paradigms for distributed training.\", \"page\": 3, \"index\": 2, \"width\": 506, \"height\": 457}, {\"url\": \"assets/figures/arxiv/2606.03519v1/fig-003.webp\", \"caption\": \"Figure 2: Quality Metrics for edge partitioners by dataset and partitioner. Blue bars represent streaming approaches, while orange bars represent in-memory approaches. The dashed horizontal line indicates the optimal value, if there is one.\", \"page\": 9, \"index\": 3, \"width\": 1056, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2606.03519v1/fig-004.webp\", \"caption\": \"Figure 3: Quality metrics for vertex partitioners by dataset and partitioner. Blue bars represent streaming approaches, while orange bars represent in-memory approaches. The dashed horizontal line indicates the optimal value, if there is one.\", \"page\": 9, \"index\": 4, \"width\": 1056, \"height\": 479}]"
motivation: 现有的图划分方法通常局限于单一的划分模式（顶点或边划分）和单一的优化目标，无法满足复杂分布式GNN训练中多方面的需求。
method: 提出SIGMA流式图划分器，在统一的多目标、多约束框架下同时支持顶点和边划分，并结合基于聚类的预处理阶段以提升划分质量。
result: 在多个基准图和两种不同的分布式GNN系统上的实验表明，SIGMA能很好地平衡划分质量、训练效率和内存消耗，性能优于流式基线且媲美内存划分器。
conclusion: 统一的流式图划分器能够有效应对不同系统架构下分布式GNN训练在通信、计算和内存方面的挑战。
---

## 摘要
分布式图神经网络（GNN）训练在很大程度上取决于底层图在计算资源间的划分方式。现有的图分割器要么专注于顶点分割，要么专注于边分割，并且通常在单一平衡约束（顶点平衡或边平衡）下仅优化单一通信目标（边割或顶点割）。我们提出了SIGMA（具有多目标感知的流式集成图分割算法），这是一种通用的流式图分割器，在统一的多目标、多约束框架内同时支持顶点分割和边分割。根据目标分布式GNN系统的不同，SIGMA可以配置为面向边割的顶点分割或面向顶点割的边分割，同时兼顾顶点平衡与边平衡。基于聚类的预处理阶段融入了全局图结构，以提高分割质量，同时保留了流式分割在效率与可扩展性方面的优势。我们在跨越不同领域和规模的六个基准图上，使用两个分布式GNN训练系统：Dist-GNN（边分割）和DistDGL（顶点分割），对SIGMA进行了评估。在这两种设置下，SIGMA均表现出持续优异的性能，展现了其在分割质量、训练效率和内存消耗之间处理复杂权衡的能力，其表现经常优于流式基线方法，同时与METIS、KaHIP和HEP等高质量的内存分割器保持了竞争力。这些结果表明，统一的流式分割器能够有效应对跨越根本不同系统架构的分布式GNN训练在通信、计算和内存方面的挑战。

## Abstract
Distributed Graph Neural Network (GNN) training depends critically on how the underlying graph is partitioned across compute resources. Existing graph partitioners focus either on vertex partitioning or edge partitioning and typically optimize only a single communication objective (edge cut or vertex cut) under a single balance constraint (vertex balance or edge balance). We present SIGMA (Streaming Integrated Graph Partitioning with Multi-objective Awareness), a versatile streaming graph partitioner that supports both vertex and edge partitioning within a unified multi-objective, multi-constraint framework. Depending on the target distributed GNN system, SIGMA can be configured for edgecut-oriented vertex partitioning or vertex-cut-oriented edge partitioning while simultaneously accounting for both vertex and edge balancing. A clustering-based preprocessing stage incorporates global graph structure to improve partition quality while preserving the efficiency and scalability advantages of streaming partitioning. We evaluate SIGMA on six benchmark graphs spanning diverse domains and scales using two distributed GNN training systems: Dist-GNN (edge-partitioned) and DistDGL (vertex-partitioned). Across both settings, SIGMA consistently achieves strong performance, showing its ability to navigate complex trade-offs between partition quality, training efficiency, and memory consumption, frequently outperforming streaming baselines while remaining competitive with high-quality in-memory partitioners such as METIS, KaHIP and HEP. These results demonstrate that a unified streaming partitioner can effectively address the communication, compute, and memory challenges of distributed GNN training across fundamentally different system architectures.

---

## 论文详细总结（自动生成）

以下是对论文《SIGMA: A Versatile Streaming Graph Partitioner for Vertex- and Edge-Balanced Distributed GNN Training》的详细结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：分布式图神经网络（GNN）训练的效率高度依赖于底层图在计算资源间的划分质量。图划分直接影响计算负载均衡和通信开销。
*   **现有痛点**：
    *   **模式割裂**：现有的图划分方法通常只关注单一的划分模式（顶点划分或边划分），导致研究精力分散。
    *   **目标单一**：传统方法通常在单一平衡约束（仅顶点平衡或仅边平衡）下优化单一通信目标（边割或顶点割）。然而，GNN训练同时涉及顶点计算（聚合）和边计算（消息传递），单一约束会导致内存溢出或计算木桶效应。
*   **整体含义**：本文提出需要一种**统一的、多目标多约束**的流式图划分框架，以同时满足分布式GNN训练在通信开销、计算负载和内存消耗上的复杂权衡需求。

### 2. 提出的方法论：SIGMA
*   **核心思想**：提出 SIGMA（具有多目标感知的流式集成图划分算法），这是一个统一的流式框架，支持顶点流式划分和边流式划分，并在多约束条件下联合优化多个目标。
*   **关键技术细节与流程**：
    *   **统一评分框架**：当流元素（顶点或边）到达时，将其分配给使评分 $S(x, p) = \text{affinity}(x, p) - \text{penalty}(x, p)$ 最大化的可行块 $p$。其中 affinity 鼓励局部性（相邻元素同块），penalty 惩罚负载不平衡。
    *   **多约束与动态容量**：为每个块维护多维负载向量（如顶点数、边体积、副本数）。引入动态容量缩放因子 $\sigma(t)$，在流处理早期严格限制容量，防止过早填满某一维度而导致后续分配困难。
    *   **顶点划分模式**：优化边割。扩展了经典的 Fennel 算法，不仅考虑顶点平衡和边体积平衡，还引入了**复制感知惩罚项**（$R(v, p)$），以减少跨分区的镜像顶点数量。
    *   **边划分模式**：优化顶点复制因子。扩展了 HDRF 算法，在保证边平衡的同时，在评分函数中加入了**副本负载平衡项**（$b^{rep}_p$），防止某些分区积累过多顶点副本导致内存倾斜。
    *   **基于聚类的预处理**：为了弥补流式算法缺乏全局视野的缺陷，引入 CluStRE 聚类算法进行预处理。将聚类映射到分区（转化为 Makespan 调度问题），对符合局部一致性和平衡约束的顶点/边进行预分配，其余再交由流式规则处理。

### 3. 实验设计
*   **数据集**：使用了 6 个不同领域和规模的基准图数据集：Amazon Computers, Flickr, Reddit, Twitch, ogbn-arxiv, ogbn-products（最大包含 240 万顶点和 6190 万条边）。
*   **评估场景与系统**：
    *   **边划分场景**：使用 Dist-GNN 系统（基于全图训练）。
    *   **顶点划分场景**：使用 DistDGL 系统（基于 Mini-batch 采样训练）。
*   **Benchmark（对比方法）**：
    *   **边划分基线**：Random, DBH (无状态流式); HDRF, HeiStreamE, 2PS (有状态/缓冲流式); FSM, HEP (内存划分)。
    *   **顶点划分基线**：Random, LDG, Fennel (流式); Cuttana, HeiStream, BuffCut (缓冲流式); METIS, KaHIP (内存划分)。

### 4. 资源与算力
*   **硬件配置**：实验在一个本地计算集群上进行，包含两台物理服务器。
    *   **CPU**：每台配备 AMD EPYC 9454P 处理器（48核）。
    *   **内存**：768 GB DDR5 ECC。
    *   **GPU**：每台配备两块 NVIDIA L40S GPU（48 GB GDDR6 显存）。
    *   **存储**：SATA 数据中心 SSD。
*   **训练配置**：训练两层 GraphSAGE 模型，运行 100 个 Epoch。DistDGL 的 batch size 为 1024。

### 5. 实验数量与充分性
*   **实验数量**：实验规模庞大且全面。针对 6 个数据集，在两种不同的分布式 GNN 系统下，与多达 8 种顶点划分和 7 种边划分的基线方法进行了对比。测试了不同分区数 $k$（如 2, 4, 8, 16, 32）下的表现。
*   **充分性与客观性**：
    *   **非常充分且客观**：评估指标不仅包含传统的图划分指标（边割率、复制因子），还深入测量了分布式系统的真实开销（每 epoch 训练时间、CPU峰值内存、GPU峰值内存）。
    *   公平地指出了 SIGMA 在某些单一指标（如纯粹的边割率）上不如专业的内存划分器（如 METIS），但强调了其在多目标综合表现上的优势。

### 6. 论文的主要结论与发现
*   **划分质量**：SIGMA 在所有流式方法中取得了最低的顶点复制因子（边划分）和极佳的顶点/边双重平衡（顶点划分）。
*   **训练效率与内存**：高质量的划分直接转化为系统收益。在 Dist-GNN 和 DistDGL 中，SIGMA 的每 epoch 训练时间显著优于所有流式基线，甚至经常超越昂贵的内存划分器，同时保持了极低的内存消耗。
*   **核心发现**：顶点划分和边划分不必作为完全独立的问题处理。统一的流式框架能够有效应对不同系统架构下分布式 GNN 训练在通信、计算和内存方面的挑战。

### 7. 优点
*   **理念创新**：打破了顶点划分与边划分的壁垒，首次在流式计算中引入了多目标（通信开销）、多约束（顶点与边双重平衡）的联合优化。
*   **工程价值极高**：算法保持了流式划分的轻量级特性（时间复杂度与 Fennel/HDRF 相当），同时通过预处理机制巧妙提升了全局划分质量。
*   **评估极其严谨**：没有停留在“划分算法的数学指标”上，而是真正在主流分布式 GNN 框架中落地，测量了通信时间、内存墙等系统级瓶颈。

### 8. 不足与局限
*   **超大规模下的局限性**：在超大图（如 ogbn-products，且分区数 $k=2$）的测试中，由于其他基线方法在该特定设置下恰好也能达到较好的双重平衡，SIGMA 的性能表现落后于部分流式和内存划分器。这说明在分区数极小且图极度庞大时，其优势难以完全发挥。
*   **参数敏感性**：算法引入了多个控制参数（如 $\gamma, \tau, \lambda$ 等），虽然论文给出了默认推荐值，但在不同图结构下是否需要大量调优以获得最优权衡，文中未做详尽的消融实验探讨。
*   **应用场景限制**：SIGMA 主要针对静态图的划分，未涉及动态图（边/顶点动态增加）的在线重划分场景。

（完）
