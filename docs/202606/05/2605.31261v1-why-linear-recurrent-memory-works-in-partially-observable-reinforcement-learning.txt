Title: Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning

URL Source: https://arxiv.org/pdf/2605.31261v1

Published Time: Mon, 01 Jun 2026 01:30:34 GMT

Number of Pages: 36

Markdown Content:
# Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Yike Zhao 1 Onno Eberhard 2 Malek Khammassi 1 Ali H. Sayed 1 Michael Muehlebach 2

## Abstract 

The family of linear recurrent neural networks has shown strong performance as recurrent mem-ory units in partially observable reinforcement learning. We provide a theoretical justification for their empirical effectiveness by constructing and studying two linear filters: (i) the first ex-actly reproduces the pre–softmax logits of the belief vector in a hidden Markov model (HMM) under a deterministic transition matrix, thereby serving as a sufficient statistic for optimal pol-icy learning, (ii) the second achieves vanishing state-decoding error under a nearly deterministic transition matrix, thus reducing state ambiguity to near zero. The results extend to action-controlled HMMs, where the corresponding linear filters be-come time-varying with action-dependent dynam-ics. We illustrate our main results through nu-merical experiments and further show that the constructed linear filter serves as a strong feature extractor in a small reinforcement learning game. 

## 1. Introduction 

Many environments in reinforcement learning (RL) are par-tially observable. In this setting, formalized as a partially observable Markov decision process (POMDP), the true state of the environment is unknown and the agent only receives stochastic observations. Hence, a single observa-tion does not fully determine the state and the agent must therefore leverage its interaction history to make effective decisions. A common approach is to learn a recurrent mem-ory that recursively compresses this history into a latent state (Ni et al., 2022; Lu et al., 2023). A particularly effective choice are linear recurrent neural networks (linear RNNs), where the hidden state transition 

> 1

EPFL, Lausanne, Switzerland 2Max Planck Institute for In-telligent Systems, T ¨ubingen, Germany. Correspondence to: Yike Zhao <yike.zhao@epfl.ch >.

Proceedings of the 43 rd International Conference on Machine Learning , Seoul, South Korea. PMLR 306, 2026. Copyright 2026 by the author(s). 

is linear and governed by a state transition matrix. Exist-ing linear RNNs can be classified as either time-invariant 

(Gu et al., 2021; Smith et al., 2022; Orvieto et al., 2023) or 

time-varying (Gu & Dao, 2024; Yang et al., 2024a;b), de-pending on whether this matrix is fixed or input-dependent. Compared with nonlinear RNNs, linear RNNs achieve sub-stantially lower training time by parallelizing computation across time steps (Gu et al., 2021; Gu & Dao, 2024), and have demonstrated strong performance on partially observ-able benchmarks (Lu et al., 2023; 2024). In contrast, nonlin-ear RNNs often suffer from optimization issues that lead to suboptimal results (Pascanu et al., 2013; Kanai et al., 2017). A sufficient statistic for optimal decision-making in POMDP is the Bayesian posterior distribution over the environment’s state given the history of interactions (e.g., Krishnamurthy, 2016). The posterior, or belief vector, is updated recur-sively following Bayes’ rule, resulting in a nonlinear opti-mal filter. Therefore, a linear recurrent memory cannot in general reproduce these nonlinear dynamics. This raises questions about the information captured by linear dynamics and, more broadly, the explanation of the strong empirical success of linear RNNs. We address these questions by explicitly constructing linear filters related to the (nonlinear) log-belief dynamics in hidden Markov models (HMMs) and their action-controlled counterpart (the dynamics of general POMDPs). We establish the following two key results: 1. A time-invariant linear recurrent memory exactly re-produces the log-belief dynamics in HMMs with de-terministic state transitions; the argument extends to POMDPs, yielding a time-varying linear memory. 2. We construct a linear recurrent memory called the time-invariant Adaptive Logit Filter (ALF) for HMMs with a nearly deterministic transition matrix, i.e., T =

D + εQ . This means that states evolve deterministi-cally following D and are perturbed by an arbitrary small stochastic element εQ . We show that this filter recovers the true state with an asymptotic error rate of ε log(1 /ε ), which vanishes as ε → 0. This rate matches the optimal asymptotic rate achieved by non-linear filtering. A corresponding time-varying ALF satisfies the same guarantee in POMDPs. 1

> arXiv:2605.31261v1 [cs.LG] 29 May 2026 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning

Taken together, these results provide theoretical support that linear recurrent memories can represent or approximate the log-belief vector in partially observable RL. We underline these findings with experiments: We first illustrate the the-oretical properties of ALF, using its time-invariant form, in a numerical simulation. We then construct a small RL environment and show that the time-varying ALF yields a strong learned policy and serves as an effective target for linear memories to learn. The constructed filters offer new insights for the design of linear RNNs in partially observable RL: 1. Our two ALFs highlight a link between the eigenvalues of the latent dynamics matrix and environmental deter-minism: less stochastic environments require eigenval-ues closer to the unit circle. 2. All constructed filters use a hidden dimension equal to the state space size, highlighting the representational efficiency achievable by linear memories. 3. The proposed two ALFs balance past memory and new information via the coefficients (1 − δ) and δ, a struc-ture widely observed in well-performing linear RNNs (e.g., Orvieto et al., 2023). Our analysis provides a principled interpretation of this mechanism in the rein-forcement learning context. 

## 2. Related Work 

Our work focuses on providing a theoretical justification for linear RNNs in POMDPs. This section reviews common memory mechanisms in partially observable RL and prior works closely related to our theoretical developments. The most widely used classes of memory are lin-ear/nonlinear RNNs and Transformers. Although Trans-formers (Vaswani et al., 2017) revolutionized supervised sequence modeling, their impact in RL has been more mod-est (Lu et al., 2024) due to their nonrecurrent structure and the quadratic complexity of the attention mechanism. Ex-isting theoretical studies in the context of RL have there-fore focused on recurrent memory and investigated specific 

memory mechanisms, such as fixed-length windows (Efroni et al., 2022; Cayci et al., 2024) or exponential moving av-erages (Eberhard et al., 2025). We build on these works by providing a theoretical justification for the strong empir-ical success of linear RNNs in general, highlighting their validity as memory units. We focus on the capability of linear RNNs for state tracking. Our analysis is inspired by the literature on Adaptive Social Learning (ASL; Bordignon et al., 2021; Khammassi et al., 2025), a multi-agent state tracking algorithm. In particular, Khammassi et al. (2025) analyze its performance under slow-varying HMMs. In the single-agent setting, we identify a direct connection between ASL and linear RNNs: after removing normalization and applying a log transform, ASL falls into this class (Hu et al., 2023, (10) and (13)). Building on this insight, we construct linear filters that provably track the fast-changing states in nearly-deterministic HMMs and action-controlled HMMs. 

## 3. Preliminaries 

3.1. Notation 

Boldface letters denote random variables. The floor and ceil-ing operators are denoted by ⌊·⌋ and ⌈·⌉ , respectively. The operator diag {v} transforms a state-related vector v ∈ RN

into a diagonal matrix, where the elements are interchange-ably referred to as vi or vxi . Throughout the paper, {ui}

denotes the canonical basis vectors, where the i-th com-ponent of ui equals 1 and all other components equal 0. 

3.2. Linear Recurrent Memory 

Linear recurrent memory can be broadly classified into two categories: time-invariant and time-varying . Time-invariant linear memory is widely used in RL practice, where the core dynamics take the form 1:

zk = Azk−1 + ϕ(bk), (1) where zk ∈ RH is the hidden state and A ∈ RH×H is the transition matrix. The input bk is a feature vector from recent interactions, such as the current observation, an obser-vation–action pair, or a fixed window of past observations. The encoder ϕ(·) is a nonlinear mapping (e.g., a multi-layer perceptron (MLP)). Time-varying linear memory has recently received increas-ing attention, whose dynamics become: 

zk = A(bk)zk−1 + ϕ(bk), (2) where A(·) is a function that maps bk to a matrix in RH×H .This formulation retains parallelizable computation across time (Smith et al., 2022) while providing additional flexibil-ity through input-dependent state transitions. 

3.3. POMDPs 

We consider a finite POMDP with state space X =

{x1, . . . , x N }, action space A = {a1, . . . , a L}, and ob-servation space Y = {u1, . . . , u S }, where each ui is a canonical basis vector in RS (i.e., one-hot encoded). The initial state distribution is π0 ∈ RN . Starting at time step 

k = 0 , the true state x⋆k evolves according to the controlled transition matrices {T (a)}a∈A , where 

tij (a) ≜ P(x⋆k+1 = xi | x⋆k = xj , ak = a). (3)         

> 1Linear time-invariant RNNs are often written as zk=
> Azk−1+Bbk, but since inputs are typically passed through a nonlinear encoder, the effect of Bcan be absorbed into ϕ. The same argument also applies to the time-varying case.

2Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Given the state–action pair (x⋆k, ak), the agent receives a reward r(x⋆k, ak). The next observation yk+1 is generated according to the emission matrix E ∈ RS×N with entries 

eij ≜ P(yk+1 = ui | x⋆k+1 = xj ). (4) Thus, the state–observation dynamics form an action-controlled HMM (Krishnamurthy, 2016). 

3.4. Belief Vector and Logit–Space Filtering 

The belief vector is the posterior distribution over the states given the entire action–observation history. It is a sufficient statistic for optimal decision-making (Krishnamurthy, 2016, Theorems 7.2.1, 7.6.1), and therefore provides a compact representation of the history. We denote the belief vector by αk ∈ RN . In an action-controlled HMM, with α0 = π0,for k ≥ 1, αk has components: 

αk(xi) ≜ P(x⋆k = xi | y1: k, a0: k−1) . (5) The belief vector can be updated recursively via Bayesian filtering, also known as the optimal filter (Shue et al., 1998, (3)): 

αk = diag {E⊤yk}T (ak−1)αk−1

1⊤diag {E⊤yk}T (ak−1)αk−1

. (6) Removing the normalization and taking elementwise loga-rithms on both sides yields: 

wok = log( T (ak−1) exp( wok−1)) + log( E⊤yk), (7) where wok is related to the belief vector αk via softmax: 

αk = softmax (wok) ≜ 1

1⊤ exp( wok) exp( wok). (8) We therefore call wok the optimal logit and (7) the logit optimal filter (LOF) .From (7) , we see that LOF looks similar to a linear recurrent memory, except for the nonlinear first term. In general, 

log( T (ak−1) exp( wok−1)) ̸ = T (ak−1)wok−1, (9) so LOF is not a linear recurrent memory. Equality holds only in special cases, e.g., when T (ak−1) is the identity or a permutation matrix. Motivated by this similarity, we focus on designing surro-gate logit filters in the next two sections: recursions that evolve a surrogate logit wk via a linear rule and provide the necessary information for learning a performant policy. The logit wk induces a proxy belief via softmax, 

bαk ≜ softmax( wk). (10) 

Remark 3.1 . For analytical convenience, we will mainly focus on the HMM setting with a fixed transition matrix T .The concepts introduced above remain unchanged, except that (3) , (5) , (6) , and (7) become action-independent. This structure underlies partially observable RL benchmarks (e.g., card games in POPGym (Morad et al., 2023)). Note that our key results extend naturally to the more general class of action-controlled HMMs. We will explicitly indi-cate whether the discussion pertains to the fixed-transition or action-controlled setting when necessary. 

## 4. Deterministic Transitions 

In this section, we construct linear recurrent memories that reproduce the optimal logits (i.e., wk = wok), which serve as sufficient statistics for optimal policy learning. We first consider a time-invariant linear filter in the HMM setting with deterministic transitions. We then extend the construc-tion to the action-controlled HMM setting, resulting in a time-varying linear filter. 

4.1. Linear Filters for HMMs 

We consider a transition matrix T of an HMM written in column form as: 

T =  T1 · · · TN

 , (11) where Ti denotes the i-th column of T . The definition of a deterministic matrix is: 

Definition 4.1 (Deterministic Matrix ). A matrix T ∈

RN ×N is called deterministic if each column Ti is one of the canonical basis vectors in RN :

Ti ∈ { u1, . . . , u N }, ∀ i ∈ { 1, . . . , N }. (12) The definition of a deterministic matrix naturally includes permutation matrices, and it may also include matrices with repeated columns, which create transient states (i.e., states that, once exited, cannot be reentered). 

Lemma 4.2. Let r denote the number of transient states for a deterministic transition matrix T ∈ RN ×N . The matrix T

can be represented as follows: 

T =

 T11 0

T21 P



, (13) 

where: 

• T11 ∈ Rr×r is a strictly lower-triangular matrix with all-zero diagonal entries. The nilpotent index is de-noted by ℓ (i.e., T ℓ 

> 11

= 0 ), where ℓ ≤ r. This matrix represents transitions between transient states. 

• T21 ∈ RN −r×r represents transitions from transient states to recurrent states. If r̸ = 0 , it is non-zero. 

• P ∈ RN −r×N −r describes the transitions among the recurrent states, and is a permutation matrix. Proof. See Appendix C.1. 3Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Remark 4.3 . If T is a permutation matrix, then the number of transient states is r = 0 , and the nilpotent index is ℓ = 1 .Now we reproduce the optimal logit wok using a concrete realization of the time-invariant linear recurrent memory structure (1). 

Theorem 4.4. Under a deterministic transition matrix T

as defined in Definition 4.1, the optimal logits wok can be reproduced by the following dynamics: 

wk = T ′wk−1 + ψ(k, yk, . . . , yk−ℓ+1 ), (14) 

where 

T ′ ≜

 Ir 00 P



, (15) 

P is the same matrix as in (13) , and ψ(·) is a nonlinear mapping that takes as input the most recent ℓ observations together with the time step k. The recursion is initialized at an arbitrary vector w0 ∈ RN . The input to ψ(·) is padded with zeros for k − ℓ < 0.Proof. Here we provide the proof when T is a permutation matrix for intuition. The full proof for general deterministic matrices is deferred to Appendix C.2. Let T = P , then we have r = 0 and ℓ = 1 . For k ≥ 1, the optimal logit wok satisfies: 

wok

> (a)

= P wok−1 + log( E⊤yk), (16) where (a) follows from the fact that P exp( wok−1) permutes the entries of exp( wok−1).However, as the arbitrary initialization w0 in our dynamics might differ from wo 

> 0

= log( π0), we design ψ(k, y ) to accommodate the potentially wrong initialization: 

ψ(k, y ) = log  E⊤y + I{k = 1 }P  log( π0) − w0

, (17) where I{·} is the indicator function. Here, the effect of wrong initialization is canceled out when k = 1 , and later on, only the first term remains valid. Note that the structure of ψ is, however, uncommon in deep learning. Understanding how well standard architectures, such as MLPs, can approximate this mapping is an interest-ing future direction, particularly given the common use of MLP encoders. 

4.2. Linear Filters for Action-Controlled HMMs 

We consider an action-controlled HMM and suppose that the transition matrices are permutation matrices, i.e., T (a) = 

P (a), ∀a ∈ A . We then construct a realization of the time-varying linear recurrent structure (2) that reproduces the optimal logits. 

Corollary 4.5. If the transition matrices satisfy T (a) = 

P (a), ∀a ∈ A , the optimal logits wok can be reproduced by the following dynamics: 

wk = P (ak−1)wk−1 + ψ(k, yk, ak−1), (18) 

where ψ(·) is a nonlinear mapping that takes as input the most recent action-observation pair together with the time step k. The initialization w0 ∈ RN is arbitrary. Proof. The result follows directly from Theorem 4.4 by replacing the fixed permutation matrix P in (16) – (17) by 

P (a) and ψ(k, y ) by ψ(k, y, a ).Here, only the latest observation and action are required, since there are no transient states (i.e., r = 0 ) for each P (a).It should be noted that both the constructed linear filters (14) and (18) have a hidden dimension N , the state space dimension. The choice of initialization w0 is not critical, as is typical in linear RNNs, which often use zero initialization across tasks (Gu et al., 2021). Additionally, all eigenvalues of T ′ and P (ak−1) lie on the unit circle, consistent with the requirements of linear RNNs for modeling long-range dependencies (Orvieto et al., 2023; Grazzi et al., 2024). 

## 5. Nearly-Deterministic Transitions 

In this section, we show that vanishing state-decoding error can be achieved by concrete realizations of linear recurrent memory. We first present a time-invariant filter for HMMs with nearly deterministic transitions, and then extend the construction to the action-controlled setting, yielding a time-varying linear filter. The ability to reproduce the true states with vanishing error is important for effective policy learning. The benefits are further illustrated through experiments in Section 6.2. We define the instantaneous state-decoding error at time step k as: 

pk ≜ P



arg max  

> x∈X

bαk(x)̸ = x⋆k



> (a)

= P



arg max  

> x∈X

wk(x)̸ = x⋆k



. (19) where (a) follows from the fact that softmax preserves the ordering of elements. As a reference, the maximum a poste-riori rule based on the exact belief vector αk in (6) achieves the minimum state-decoding error. We refer to this rule as the Bayes-optimal decoder , and compare the performance of our filters against it. 

5.1. Linear Filters for HMMs Time-Invariant ALF. In the following, we construct a time-invariant linear filter under the nearly-deterministic 4Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

transition model. A nearly-deterministic transition matrix in an HMM is defined as follows: 

Definition 5.1 (Nearly-Deterministic Matrix ). Let D be a deterministic transition matrix as in Definition 4.1. Amatrix T is called nearly-deterministic if it admits the decomposition 

T = D + εQ (20) for some small drift parameter 0 < ε < 1.The matrix Q satisfies for each j = 1 , . . . , N :

qij ≥ 0 ( i̸ = nj ), qnj j = − X

> i̸=nj

qij , (21) where nj is the row index of the unique 1 in column j of D

(i.e., dnj j = 1 , dij = 0 , for all i̸ = nj ). The condition (21) guarantees that the matrix T is a valid transition matrix (necessary and sufficient). It can be easily shown that T is column-stochastic with nonnegative entries. This type of matrix is called nearly-deterministic because the small parameter ε allows the deterministic component 

D to dominate. As a result, the states x⋆k follow the deter-ministic dynamics induced by D for most time steps, with only occasional irregular transitions due to the term εQ .

Remark 5.2 . When D = I, T in Definition 5.1 reduces to the transition matrix of the slowly varying Markov chains studied by Khasminskii & Zeitouni (1996), Yin & Krishna-murthy (2005), and Khammassi et al. (2025). According to Lemma 4.2, the deterministic transition matrix 

D can be expressed, after proper state labeling, as 

D =

P D12 

0 D22 



, (22) where P ∈ RN −r×N −r is a permutation matrix, D12 ∈

RN −r×r , and D22 ∈ Rr×r is nilpotent with index ℓ. In contrast to Lemma 4.2, we label the recurrent states of D as the first N − r states: 

eX ≜ {x1, . . . , x N −r }, (23) and the transient states as the last r states, i.e., X \ eX . In what follows, we assume D has this form. Motivated by the ASL algorithm in Bordignon et al. (2021); Khammassi et al. (2025), we propose the following realiza-tion of the time-invariant linear recurrent memory (1) in the logit space, referred to as the adaptive logit filter (ALF) 

for nearly-deterministic transition matrices: 

wk =

 P 00 Ir



(1 − δ)wk−1 + δ log( E⊤yk). (24) ALF recursively updates a surrogate logit vector by com-bining a permuted, discounted past surrogate logit with the instantaneous log-likelihood of new observations. When 

δ = 0 , ALF depends entirely on the past surrogate logit and thus cannot adapt to changes in the underlying obser-vation distribution that arise from irregular state transitions. In contrast, when δ = 1 , ALF relies solely on the instan-taneous observation, allowing ALF to adapt quickly but making it more sensitive to observation noise. Hence, δ

governs the speed of adaptation, trading off stability against responsiveness. The main difference in relation to the model used by Bor-dignon et al. (2021) and Khammassi et al. (2025) is the presence of the matrix before (1 − δ)wk−1 in (24) . This matrix enables wk to capture the fast state transition that dominates most of the time; without it, the state decod-ing error increases substantially. For wk, we denote the components corresponding to the recurrent states as: 

ewk ≜



wk(x1)

...

wk(xN −r )

 ∈ RN −r . (25) We impose the following assumption on the initial condition of ALF. 

Assumption 5.3. The initial w0 for ALF is: 

w0 =

 ew0

−∞ 1r



, (26) where ew0 is finite (i.e., ∥ ew0∥∞ < ∞). We adopt the convention −∞ · 0 = 0 . Assumption 5.3 ensures that the decoded states over time are restricted to the recurrent states, never transient ones. 

Observability Assumption. Our goal is to analyze the state-decoding property of ALF, which will require addi-tional definitions and assumptions on the emission matrix. These will be introduced next. For the permutation P , we define its order as M :

Definition 5.4. The order of a permutation matrix P is the smallest positive integer M such that P M = I.

Lemma 5.5. (Humphreys & Prest, 2004, Theorem 4.2.6) Every permutation matrix has a finite order M , which is given by the least common multiple of the lengths of the cycles in the underlying permutation. 

For any x ∈ eX , let σ : eX → eX denote the permutation operation corresponding to P . Applying the permutation matrix P repeatedly i times corresponds to σi(x). Note that 

σM (x) = x according to Lemma 5.5. 5Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

The emission matrix E ∈ RS×N has the following form: 

E =  E1: N −r EN −r+1 . . . EN

 (27) where E1, . . . , E N are column vectors. We denote 

eE ≜ E1: N −r (28) and define a corresponding stacked matrix eEs as: 

eEs ≜

eEP 0

...

eEP M −1

 ∈ RSM ×N −r , (29) where M is the order of P defined in Definition 5.4. To build intuition, we consider the first column: 

eEsu1 =

eEP 0u1

...

eEP M −1u1

 (a)

=

eEu x1

...

eEu σM −1(x1)

 , (30) where (a) follows since P iu1 applies the permutation σ to 

x1 exactly i times, yielding uσi(x1). The first S elements correspond to the observation distribution when the true state is x1, the next S elements correspond to the state 

σ(x1), and so on. 

Assumption 5.6. The emission matrix E satisfies: (a) All elements of eE are strictly positive. (b) No two columns of the stacked matrix eEs are identical. In Assumption 5.6, no restrictions are placed on the ob-servation models P(· | x) for transient states x ∈ X \ eX .For recurrent states x ∈ eX , Assumption 5.6a requires that the distributions P(· | x) share identical support, ensuring finite Kullback–Leibler divergence across states, while As-sumption 5.6b enforces that observation distributions along different M -step trajectories are distinct. Similar multi-step observability assumptions are commonly imposed in POMDPs (e.g., Liu et al., 2022; Golowich et al., 2023) to ensure stable filtering and learnability. Assumption 5.6 is weaker than the one used in Khammassi et al. (2025); we defer the discussion to Appendix A.1. 

Main Results. Our goal is to recover the true state x⋆k.The following theorem provides a sufficient condition on the parameters ε and δ under which the dynamics in (24) achieve vanishing error probability in identifying x⋆k. This result extends Theorems 1 and 2 of Khammassi et al. (2025) to the model (24) , allowing a more general class of transition matrices and relying on a weaker observability assumption. Further intuition on the sufficient condition in the special case of D = I can be found in Khammassi et al. (2025). 

Theorem 5.7. Consider a nearly-deterministic transition matrix T under Assumptions 5.3 and 5.6. Let δ = δε be the adaptation parameter used for a given drift parameter ε. If the following conditions are satisfied: 

lim  

> ε→0

δε = 0 , (31) 

lim 

> ε→0

εδε

= 0 , (32) 

then the ALF consistently learns the true states as ε goes to zero, which is formally expressed as 

lim  

> ε→0

lim sup 

> k→∞

pk = 0 . (33) 

Moreover, following conditions (31) and (32) , consider 

δε = λ

log(1 /ε ) , with λ ∈ (0 , ξ ), (34) 

where the constant ξ > 0 depends only on the permutation backbone P and the likelihood model E; its detailed defini-tion and further discussion are provided in Appendices D.1 and D.2, respectively. Then it holds that 

lim sup 

> k→∞

pk ≤ κ ε log 1

ε + o(ε log 1

ε ), (35) 

with the constant κ > 0 defined in Appendix D.1. The resulting decay rate matches that of the Bayes-optimal de-coder (Khasminskii & Zeitouni, 1996), in the sense that the long-term error is also dominated by an ε log(1 /ε ) term. Proof. Let ε > 0 and k ∈ N. For any Tε > 0, we can write the instantaneous error probability as follows: 

pk = P



arg max  

> x∈X

wk(x)̸ = x⋆k, no IrJ in [k − Tε, k ]



+ P



arg max  

> x∈X

wk(x)̸ = x⋆k, IrJ in [k − Tε, k ]



≤ P



arg max  

> x∈X

wk(x)̸ = x⋆k, no IrJ in [k − Tε, k ]



+ P [IrJ in [k − Tε, k ]] , (36) where “IrJ” stands for irregular jumps . An irregular jump is observed at instant k if x⋆k−1 to x⋆k does not follow matrix 

D. We would like to properly choose Tε such that both terms in (36) are small. For each δε, choose 

Tε ≜ αδε

, (37) where α > 0. As ε goes to 0, δε goes to 0 as well according to (31) . For δε small enough, it is possible to find a hε > 0

such that 

hε ≜

 Tε

M



, (38) 6Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

which means Tε = αδε ≥ hεM , where M is the order of the permutation matrix P according to Lemma 5.5. We denote 

Ik ≜ [k − hεM + 1 , k ] (39) Since {no IrJ in [k − Tε, k ]} ⊆ { no IrJ in Ik}, we further upper bound (36) by: 

X

> x′∈X

P



arg max  

> x∈X

wk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x′



P no IrJ in Ik x⋆k−hεM = x′ P x⋆k−hεM = x′

+ P [IrJ in [k − Tε, k ]] 

> (a)

≤ X 

> x′∈eX

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x′



+ X 

> x′∈X \ eX

P x⋆k−hεM = x′+P [IrJ in [k − Tε, k ]] , (40) where (a) follows since probabilities are smaller or equal to 1 and arg max x∈X wk(x) = arg max x∈ eX ewk(x) due to the −∞ initialization of w0 for the transient states, as specified in Assumption 5.3. From Lemma D.1, since hεM does not change as k → ∞ ,we have: 

lim  

> ε→0

lim sup 

> k→∞

X 

> x′∈X \ eX

P x⋆k−hεM = x′ = 0 , (41) 

lim  

> ε→0

lim sup 

> k→∞

P [IrJ in [k − Tε, k ]] = 0 . (42) By Lemma D.2, under the specified evolution pattern of the true states, we have: 

lim  

> ε→0

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik,

x⋆k−hεM = x′ = 0 , ∀x′ ∈ eX (43) Now we return to (40) and complete the proof: 

lim  

> ε→0

lim sup 

> k→∞

pk

> (a)

= 0 (44) where (a) follows from (41) , (42) , and (43) . The derivations of Lemmas D.1 and D.2, as well as the error decaying rate under the δε rule (34) can be found in Appendix D.1. Theorem 5.7 shows that, by appropriately controlling the adaptation parameter, linear recurrent memory can achieve vanishing state-decoding error, with a decay rate that matches that of the Bayes-optimal decoder. This result provides a quantitative and strong performance bound on ALF’s state-decoding capability, demonstrating that the gap between ALF and the nonlinear optimal filter is small. 

5.2. Linear Filters for Action-Controlled HMMs 

In action-controlled HMMs, we assume each transition ma-trix is nearly a permutation: 

T (a) = P (a) + εQ (a), a ∈ A , (45) with each T (a) being a subclass of nearly-deterministic matrices. We propose the following time-varying ALF: 

wk = P (ak−1)(1 − δ) wk−1 + δ log( E⊤yk), (46) Moreover, we assume the following: 

Assumption 5.8. The initial and emission matrix E satisfy: 1. The initialization w0 is finite, i.e., ∥w0∥ < ∞.2. All entries of E are strictly positive, and no two columns of E are identical. 

Theorem 5.9. Consider transition matrices satisfying (45) .Under Assumption 5.8, the results in Theorem 5.7 hold for time-varying ALF. Proof. The result follows analogously to the proof of Theo-rem 5.7. Our results show the ability of time-varying ALF in recover-ing the true states. Experiments in the next section underline that time-varying ALF enables learning performant policies. Moreover, both the time-invariant and time-varying ALF resemble practical linear RNNs, with eigenvalues near the unit circle that support long-range dependencies. 

## 6. Experiments 

In this section, we first illustrate the theoretical properties of ALF with a numerical example in the HMM setting. We then introduce a partially observable game, the RingWorld ,to show that time-varying ALF is a suitable learning target for linear RNNs. 

Remark 6.1 . In this section, we use “ALF” for both its time-invariant and time-varying variants for brevity. 

6.1. Illustrative Example 

We consider a HMM with state space X = {x1, x 2} and observation space Y = {u1, u 2}. Following Definition 5.1, the nearly-deterministic transition matrix T and the emis-sion matrix E are: 

T =

 ε 1 − ε

1 − ε ε



, E =

0.9 0.10.1 0.9



. (47) Since D is a two-state permutation matrix, we have r = 0 ,and ALF (24) simplifies accordingly. Similar to Khammassi et al. (2025), we select the following δε values that satisfy conditions (31) and (32). 7Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning      

> Figure 1. Long-run error probability versus 1/ε for ALF and LOF. Solid/dashed lines correspond to valid/invalid δεselections, respectively.

δε = ε1/2, δε = 0.7log(1 /ε ) . (48) The latter choice also satisfies (34) ; the detailed deriva-tions are provided in Appendix D.2. In addition to valid δε

choices, we also consider three invalid ones: 

δε = ε2, δε = 0 , δε = 1 , (49) where δε = ε2 and δε = 0 violate (32) , and δε = 1 violates both (31) and (32) . We choose 23 values of 1/ε uniformly from [30 , 250] and run 20 ,000 Monte Carlo iterations for each ε to estimate the instantaneous error probabilities. As the error stabilizes after 1000 steps (see Appendix E.1), we adopt p1000 as an empirical proxy for lim sup k→∞ pk and study its variation as 1/ε increases, as shown in Figure 1. Valid choices yield vanishing long-run error probabilities as ε → 0, with only a small performance gap. Consistently, 

δε = 0 .7/ log(1 /ε ) performs better than δε = ε1/2. For invalid choices, p1000 does not vanish; for example, δε = ε2

is too small to track the state transitions. Together, these observations provide evidence that Theorem 5.7 accurately describes the state-decoding in practice. These observa-tions are also consistent with those reported for D = I in Khammassi et al. (2025). 

6.2. RingWorld Game 

In this experiment, we study a general POMDP with action-dependent state transitions. Motivated by a classical aliased cyclic-state benchmark from the predictive modeling litera-ture (e.g., Tanner & Sutton, 2005; Schlegel et al., 2023), we design a RingWorld game with stochastic observations and more complex action-dependent dynamics. The state space is X = {1, . . . , 12 } (positions on a ring) and the action set is A = {CW 1, CW 2, CCW 1, CCW 2}, where CW 1/CCW 1 move one step clockwise/counterclockwise. The agent may overshoot or slip, each with probability 

ε = 0 .05 , making the transitions nearly permutation. Four beacons are placed around the ring. At each step, the agent observes the index of the strongest beacon, encoded as 

ui ∈ Y = {u1, . . . , u 4}. The closer the agent is to a beacon, States       

> Trap States
> Goal States
> Beacons
> Figure 2. RingWorld environment. Birepresents the i-th beacon.
> Figure 3. Episodic return versus update. The shaded area is the standard deviation across 8 random seeds. ALF/LOF are con-structed from environment knowledge, whereas S5 and Deep ALF are trained from scratch.

the more likely it will be observed. The states 6, 11 , and 12 

are “trap” states, and the states 2 and 3 are goal states. Enter-ing a trap state incurs a penalty, while entering a goal state yields a reward. An episode terminates after a fixed number of steps and the total episodic return lies in [−1, 1] . The objective is to avoid or quickly escape the trap states while remaining in the goal states under partial observability. The setting is illustrated in Figure 2. We train Proximal Policy Optimization agents (PPO; Schul-man et al., 2017) to act in this environment under four types of memory: LOF , ALF , an S5-based memory which is a single S5 layer (Smith et al., 2022) with a two–layer MLP encoder ϕ(·), and an ALF-inspired deep learning variant called Deep ALF . Specifically, Deep ALF is obtained by parameterizing the ALF recursion in complex space and treating the parameters as learnable, similar to standard ap-proaches for designing linear RNNs. For LOF and ALF ,the memory output is either fed directly to the actor and critic, or passed through a softmax to form a (proxy) be-lief vector. Both the actor and critic are implemented as two–layer MLPs. For LOF and ALF , the memory is fixed and obtained from environment knowledge ( ALF follows (46) , while LOF uses (7) ), and only the actor and critic are trained. For both S5-based memory and Deep ALF ,the memory parameters are randomly initialized and jointly trained with the actor and critic. 8Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

As shown in Figure 3, ALF with direct outputs converges quickly to a strong return, whereas LOF trains slowly with high variance across seeds. The instability stems from the logits in LOF drifting increasingly negative over time, while 

ALF remains bounded (Lemma D.11). Applying a softmax to LOF recovers a belief vector, stabilizes training, and yields the best returns in this experiment. For the S5-based memory , increasing the hidden size from 12 (the memory size of LOF , ALF , and Deep ALF ) to 256 allows it to match the performance of Deep ALF , but it still falls short of ALF and softmax-processed LOF . Notably, Deep ALF is more parameter-efficient than the S5-based memory , as its encoder has a number of parameters comparable to matrix 

E, rather than a two-layer MLP. Overall, these results show that ALF provides an efficient and reliable linear memory, and Deep ALF achieves better performance than the S5-based memory using fewer parameters. Moreover, when environment knowledge is partially or fully available, Deep ALF can leverage this information to ob-tain improved initialization and achieve higher returns (see Figure 10). In contrast, it remains unclear how such prior knowledge can be incorporated into existing linear recurrent networks. More details can be found in Appendix E.2. 

## 7. Discussion and Limitations 

Our analysis focuses on environments with (near) determin-istic transitions, which provide a sufficient regime for linear memory to perform well. While this regime may appear restrictive, such environments are not uncommon in existing benchmarks (e.g., the Passive T-Maze task in Lu et al. (2024) and the RepeatPrevious task in Lu et al. (2023)). Moreover, although our theoretical results are developed for finite state, observation, and action spaces, many continuous-control problems can be approximated by finite, nearly determin-istic POMDPs through discretization (e.g., robotic control tasks in MuJoCo (Todorov et al., 2012)). Extending the eval-uation to these more complex environments is an important direction for future work. A natural question is how linear memory performs outside the (near) deterministic regime. We note that, in highly stochastic environments, sequence models may offer lim-ited gains over memoryless models for state decoding. For example, in the fully stochastic version of the two-state ex-ample from Section 6.1 ( ε = 0 .5; see (47) ), the first term in the LOF update (7) becomes proportional to the all-one vector, and the optimal filter reduces to a myopic estimator based solely on log( E⊤y). Consequently, past observations no longer provide useful information for state estimation. This suggests that, when the environment dynamics are suffi-ciently stochastic, leveraging long-term history may provide little benefit, regardless of whether one uses linear RNNs or other sequence models. 

## 8. Conclusions 

We justify the use of linear recurrent neural networks (RNNs) in partially observable reinforcement learning by constructing specific instances: First, we show that linear recurrent memory can exactly reproduce the log-belief dy-namics under deterministic state transitions. Second, we introduce the Adaptive Logit Filter (ALF), which asymp-totically recovers the true states under nearly deterministic state transitions. Theoretical properties of ALF are illus-trated through a numerical example, and we further demon-strate that ALF provides a strong learning target in a small POMDP environment, the RingWorld .

## Impact Statement 

This paper presents work whose goal is to advance the field of machine learning. There are many potential societal consequences of our work, none which we feel must be specifically highlighted here. 

## References 

Bordignon, V., Matta, V., and Sayed, A. H. Adaptive social learning. IEEE Transactions on Information Theory , 67 (9):6053–6081, 2021. Cayci, S., He, N., and Srikant, R. Finite-time analysis of natural actor-critic for pomdps. SIAM Journal on Mathematics of Data Science , 6(4):869–896, 2024. Eberhard, O., Muehlebach, M., and Vernade, C. Partially ob-servable reinforcement learning with memory traces. In-ternational Conference on Machine Learning , 267:14934– 14949, 2025. Efroni, Y., Jin, C., Krishnamurthy, A., and Miryoosefi, S. Provable reinforcement learning with a short-term mem-ory. International Conference on Machine Learning , 162: 5832–5850, 2022. Golowich, N., Moitra, A., and Rohatgi, D. Planning and learning in partially observable systems via filter stability. 

ACM Symposium on Theory of Computing , pp. 349–362, 2023. Gray, R. M. Toeplitz and circulant matrices: A review. Foun-dations and Trends® in Communications and Information Theory , 2(3):155–239, 2006. Grazzi, R., Siems, J., Zela, A., Franke, J. K., Hutter, F., and Pontil, M. Unlocking state-tracking in linear RNNs through negative eigenvalues. arXiv:2411.12537 , 2024. Gu, A. and Dao, T. Mamba: Linear-time sequence modeling with selective state spaces. Conference on Language Modeling , 2024. 9Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Gu, A., Goel, K., and R ´e, C. Efficiently modeling long se-quences with structured state spaces. arXiv:2111.00396 ,2021. Hu, P., Bordignon, V., Vlaski, S., and Sayed, A. H. Optimal aggregation strategies for social learning over graphs. 

IEEE Transactions on Information Theory , 69(9):6048– 6070, 2023. Humphreys, J. F. and Prest, M. Y. Numbers, Groups and Codes . Cambridge University Press, 2004. Kanai, S., Fujiwara, Y., and Iwamura, S. Preventing gradient explosions in gated recurrent units. Advances in Neural Information Processing Systems , 30:435–444, 2017. Khammassi, M., Bordignon, V., Matta, V., and Sayed, A. H. Adaptive social learning for slow Markov chains. IEEE Transactions on Signal Processing , 73:3671–3687, 2025. Khasminskii, R. and Zeitouni, O. Asymptotic filtering for finite state Markov chains. Stochastic Processes and Their Applications , 63(1):1–10, 1996. Krishnamurthy, V. Partially Observed Markov Decision Processes . Cambridge University Press, 2016. Liu, Q., Chung, A., Szepesv ´ari, C., and Jin, C. When is partially observable reinforcement learning not scary? 

Conference on Learning Theory , pp. 5175–5220, 2022. Lu, C., Schroecker, Y., Gu, A., Parisotto, E., Foerster, J., Singh, S., and Behbahani, F. Structured state space models for in-context reinforcement learning. Advances in Neural Information Processing Systems , 36:47016– 47031, 2023. Lu, C., Shi, R., Liu, Y., Hu, K., Du, S. S., and Xu, H. Re-thinking transformers in solving POMDPs. International Conference on Machine Learning , 235:33089–33112, 2024. Matta, V., Braca, P., Marano, S., and Sayed, A. H. Diffusion-based adaptive distributed detection: Steady-state perfor-mance in the slow adaptation regime. IEEE Transactions on Information Theory , 62(8):4710–4732, 2016. Meyer, C. D. Matrix Analysis and Applied Linear Algebra .SIAM, 2023. Morad, S., Kortvelesy, R., Bettini, M., Liwicki, S., and Prorok, A. POPGym: Benchmarking partially observable reinforcement learning. arXiv:2303.01859 , 2023. Ni, T., Eysenbach, B., and Salakhutdinov, R. Recur-rent model-free RL can be a strong baseline for many POMDPs. International Conference on Machine Learn-ing , 162:16691–16723, 2022. Orvieto, A., Smith, S. L., Gu, A., Fernando, A., Gulcehre, C., Pascanu, R., and De, S. Resurrecting recurrent neural networks for long sequences. International Conference on Machine Learning , 202:26670–26698, 2023. Pascanu, R., Mikolov, T., and Bengio, Y. On the difficulty of training recurrent neural networks. International Con-ference on Machine Learning , 28:1310–1318, 2013. Puterman, M. L. Markov decision processes. Handbooks in operations research and management science , 2:331–434, 1990. Schlegel, M. K., Tkachuk, V., White, A. M., and White, M. Investigating action encodings in recurrent neural networks in reinforcement learning. Transactions on Machine Learning Research , 2023. Schulman, J., Wolski, F., Dhariwal, P., Radford, A., and Klimov, O. Proximal policy optimization algorithms. 

arXiv:1707.06347 , 2017. Shue, L., Anderson, D., and Dey, S. Exponential stabil-ity of filters and smoothers for hidden Markov models. 

IEEE Transactions on Signal Processing , 46(8):2180– 2194, 1998. Smith, J. T., Warrington, A., and Linderman, S. W. Simplified state space layers for sequence modeling. 

arXiv:2208.04933 , 2022. Tanner, B. and Sutton, R. S. Temporal-difference networks with history. International Joint Conference on Artificial Intelligence , pp. 865–870, 2005. Todorov, E., Erez, T., and Tassa, Y. Mujoco: A physics engine for model-based control. IEEE/RSJ International Conference on Intelligent Robots and Systems , pp. 5026– 5033, 2012. Vaswani, A., Shazeer, N., Parmar, N., Uszkoreit, J., Jones, L., Gomez, A. N., Kaiser, Ł., and Polosukhin, I. Atten-tion is all you need. Advances in Neural Information Processing Systems , 30:5998–6008, 2017. Yang, S., Wang, B., Shen, Y., Panda, R., and Kim, Y. Gated linear attention transformers with hardware-efficient train-ing. International Conference on Machine Learning , 235: 56501–56523, 2024a. Yang, S., Wang, B., Zhang, Y., Shen, Y., and Kim, Y. Par-allelizing linear transformers with the delta rule over se-quence length. Advances in Neural Information Process-ing Systems , 37:115491–115522, 2024b. Yin, G. G. and Krishnamurthy, V. LMS algorithms for tracking slow Markov chains with applications to hid-den Markov estimation and adaptive multiuser detection. 

IEEE Transactions on Information Theory , 51(7):2475– 2490, 2005. 10 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

## A. Additional Discussion 

A.1. Observability Assumption 

When r = 0 and P = I, we have eE = E and eEs reduces to E since M = 1 . In this case, Assumptions 5.6a and 5.6b coincide with those of Khammassi et al. (2025, Assumptions 1 and 3). For r ≥ 1 or M > 1, however, they are weaker: if 

r ≥ 1, the columns EN −r+1 , . . . , E N may be duplicates of others or contain zero components; if M > 1, eE may contain identical columns. For example: 

Example 1. Let 

P =



0 1 0 01 0 0 00 0 0 10 0 1 0

, eE =

 0.5 0.8 0.5 0.20.5 0.2 0.5 0.8



. (50) Then, 

eEs =



0.5 0.8 0.5 0.20.5 0.2 0.5 0.80.8 0.5 0.2 0.50.2 0.5 0.8 0.5

 , (51) where eE assigns identical observation distributions to x1 and x3, yet no two columns of eEs are identical. 

A.2. Beyond Permutation-Based POMDPs 

For general nearly-deterministic transition matrices (instead of nearly-permutation), there’s no easy extension of ALF. Our approach relies on setting the logit entries corresponding to transient states to −∞ . Consider a simple deterministic experiment: 

T (a1) = 

 1 10 0



, T (a2) = 

 0 01 1



, (52) In this example, action a1 makes x2 transient, while action a2 makes x1 transient. If one action is applied consistently, the model reduces to an HMM, where setting the corresponding transient state to −∞ is valid. However, if actions alternate, no true transient states exist (since every state can be visited with non-negligible probability), and there is no natural way to extend ALF to this case. Even when transient states coincide across transition matrices, generalization is still difficult. Consider Figure 4: the transient states are always x1 and x2, but under alternating actions a1, a 2, a 1, . . . , a nonzero initial probability for x1 implies that the probability for x1 and x2 never vanishes.  

> Figure 4. Illustration of a difficult case for generalization.

A possible generalization to nearly-deterministic POMDPs is to assume backbone deterministic matrices 

D(a) = 

 P (a) D12 (a)0 D22 (a)



, (53) with strictly upper-triangular D22 (a) for all a ∈ A , so that transient-state probabilities go to zero after finitely many steps. 11 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning              

> Figure 5. Sample evolution of the logit difference wk(x2)−wk(x1)for LOF and ALF under ε= 0 .005 .

## B. Visualization of ALF’s State Tracking 

In this section, we provide additional visualizations to further illustrate how ALF recovers the underlying states. 

B.1. Time-Invariant ALF 

In this section, we consider a two-state HMM: 

T =

1 − ε εε 1 − ε



, E =

0.8 0.20.2 0.8



, (54) with ε = 0 .005 . In this setting, the sign of the difference wk(x2) − wk(x1) fully determines the decoded state (positive implies x2, and vice versa). Consider δ1 = 0 .07 and δ2 = 0 .19 .Two observations can be drawn from Figure 5. First, although the overall trends are similar, there is a clear discrepancy between the dynamics of ALF and LOF . Nevertheless, ALF still achieves good state decoding. Second, since δ1 < δ 2,the dynamics under δ1 appear smoother than those under δ2, as the smaller value reduces sensitivity to instantaneous (and noisy) observations. However, this smoother behavior comes at the cost of slower adaptation to irregular state transitions, as reflected by the lag after switching points (e.g., k = 115 , 270 , 406 , 593 ). This reflects the intrinsic tradeoff between stability and responsiveness in ALF . Similar plots can be generated for the model in Section 6.1; however, due to the frequent state transitions, these are less interpretable. 

B.2. Time-Varying ALF 

We next consider the RingWorld environment in Section 6.2. Figure 6 shows a sample trajectory of true states, estimated states, actions, and observations. ALF is constructed using environment knowledge with δ = 0 .1, and the corresponding trained actor is used to select actions. In the legend, Irregular Transition refers to state transitions that deviate from the permutation backbone; Off-peak Observation indicates observations that do not match the most likely ones given the current state; and Decoding Error denotes mismatches between the decoded and true states. Two observations follow. First, ALF is more sensitive to noisy observations than LOF . For example, at timesteps 61 and 69, observations B3 and B4 occur at state 3, which are unlikely; these lead to decoding errors for ALF but not for LOF .Second, the learned policy is effective: the agent takes CW1 at state 2 and CCW1 at state 3 for most of the time, which leads to high return. 12 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning States 

> Trap States
> Goal States
> Beacons
> Timestep
> True States
> Observations
> ALF
> LOF
> ALF Actions
> Decoding Erorr
> Irregular Transition
> Off-peak Observation

Figure 6. Sample trajectory of true states, estimated states, actions, and observations in the RingWorld environment. 

## C. Proofs for Section 4 

C.1. Proof of Lemma 4.2 

If T is a permutation matrix, there are no transient states (i.e., r = 0 ), and Lemma 4.2 holds trivially as T11 and T21 are empty matrices. Otherwise, T must have at least two identical columns, which implies the existence of at least one transient state: there must be a basis vector ui ∈ { u1, . . . , u N } that does not appear in T . Consequently, no state can reach xi, so 

r > 0, and the matrix T is reducible. According to the canonical form of a reducible matrix, T can be expressed as (Meyer, 2023, (8.4.6); Puterman, 1990, (A.1)): 

T =

 T11 0

T21 T22 



, (55) where the first r states are transient, and the remaining N − r are recurrent states. This matrix T11 ∈ Rr×r is a lower-triangular block matrix whose diagonal blocks are either irreducible or [0] 1×1. Additionally, T21 ∈ R(N −r)×r is non-zero, and T22 ∈ R(N −r)×(N −r) is a block diagonal matrix where each diagonal block is irreducible. This is a classical result for absorbing Markov chains. We now focus on its properties in the case of deterministic transitions. We now prove, by contradiction, that all diagonal blocks in T11 must be [0] 1×1. If a diagonal block were irreducible, it would form an irreducible Markov chain isolated from other states, meaning no external state could enter it. Therefore, such states would not be transient, and they should appear in the bottom-right corner of T , which contradicts their placement in 

T11 .Thus, T can be written as: 



t11 · · · 0

... . . . ...

tr1 · · · trr 

0

T21 

P11 · · · 0

... . . . ...

0 · · · Pmm 



, (56) where T21 ̸ = 0 , and the states {x1, . . . , x r } are transient. Moreover, all diagonal entries t11 , . . . , t rr in T11 are zero. The 13 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

blocks P11 , . . . , P mm are irreducible, and since T is deterministic, each Pii must be a permutation matrix. Consequently, the entire block P is itself a permutation matrix. 

C.2. Proof of Theorem 4.4 

We now consider the more general case where the number of transient states r does not equal to 0. After removing the normalization in the action-independent version of (6) (we focus on a fixed-transition HMM), we obtain the unnormalized optimal filter :

βk = diag {E⊤yk}T βk−1

= diag {E⊤yk}T · · · diag {E⊤y1}T π 0. (57) We first show that 

βk =



0r

βk(r + 1) 

...

βk(N )

 ≜

 0r

eβk



, (58) for k ≥ ℓ, we use 0r to denote zero vector with dimension r to avoid confusion. We prove this by induction, if βk admits the form (58), then 

βk+1 = diag {E⊤yk+1 }T βk

= diag {E⊤yk+1 }

 0r

P eβk



=

 0r

eβk+1 



. (59) Therefore, what is left is to prove the base case where βℓ admits the form (58), which is 

 0r

eβℓ



= diag {E⊤yℓ}T · · · diag {E⊤y1}T

| {z } 

> U1: ℓ

π0. (60) Note that T ℓ has: 

T ℓ =

 T ℓ 

> 11

0

· P ℓ

 (a)

=

 0 0

· P ℓ



, (61) where (a) is due to Lemma 4.2 and the property of the nilpotent matrix T ℓ 

> 11

= 0 . Next, we show that U 1: ℓ maintains the zeros in T ℓ. The matrix U 1: ℓ can be expressed as: 

U 1: ℓ = diag {E⊤yℓ}T · · · diag {E⊤y1}T. (62) Then we apply Lemma C.1 iteratively to prove that U 1: ℓ maintains the zeros in T ℓ. This finishes the proof of (58). It should be noted that the optimal logit can be obtained from βk by: 

wok = log βk. (63) Therefore, from (59), we have for the optimal logits wok+1 with k ≥ ℓ:

wok+1 = log( βk+1 ) = 

 −∞ 1r

log( eβk+1 )



=

 Ir 00 P

  −∞ 1r

log( eβk)



+ log( E⊤yk+1 )=

 Ir 00 P



wok + log( E⊤yk+1 ), (64) 14 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

where we assume −∞ · 0 = 0 . Thus, to reproduce the optimal logits, we should have ψ(k, yk, · · · , yk−ℓ+1 ) = log( E⊤yk)

when k > ℓ . Also, since the evolution of the optimal logits wok for k > ℓ relies on obtaining the exact log( βℓ) (i.e. woℓ ), we need to reproduce woℓ as well as wok, 1 ≤ k < ℓ precisely. Therefore, we need to design our ψ(k, yk, · · · , yk−ℓ+1 ) for 

k ≤ ℓ. This is due to the fact that the evolution of wok, 1 ≤ k ≤ ℓ generally does not satisfy (64) , and we need to design a ψ

to account for the errors induced by using the matrix T ′.The detailed design of ψ relies on ℓ. Instead of describing ψ explicitly for all ℓ, we only give a sample implementation of 

ψ(k, y 2, y 1) for ℓ = 2 :

ψ(k, y 2, y 1) = 



log( T π 0) − T ′w0 + log( E⊤y2), k = 1 log( T diag {E⊤y1}T π 0) − T ′ log( diag {E⊤y1}T π 0) + log( E⊤y2), k = 2 log( E⊤y2), k ≥ 3

. (65) We can check that when k > ℓ = 2 , ψ(k, y 2, y 1) = log( E⊤y2) which is the same as what we found in (64) . Using this 

ψ(k, y 2, y 1), our dynamics produces: 

w1 = T ′w0 + ψ(1 , y1, 0) = T ′w0 + log( T π 0) − T ′w0 + log( E⊤y1)= log  diag {E⊤y1} T π 0



= wo

> 1

,

w2 = T ′w1 + ψ(2 , y2, y1)= T ′ log  diag {E⊤y1} T π 0

 + log  T diag {E⊤y1} T π 0

 − T ′ log  diag {E⊤y1} T π 0

 + log( E⊤y2)= log  diag {E⊤y2} T diag {E⊤y1} T π 0



= wo

> 2

,

w3 = T ′w2 + log( E⊤y3)= T ′wo 

> 2

+ log( E⊤y3)= wo

> 3

,

... (66) Note that ψ for other values of ℓ can be constructed in similar ways. 

C.3. Supporting Lemma Lemma C.1. Assume all columns of matrix B are standard basis vectors: 

B =  un1 · · · unN

 , (67) 

we have: 

diag {c1, · · · , c N }B = B diag {cn1 , · · · , c nN }. (68) 

Proof. The result follows from 

diag {c1, · · · , c N }B

=  diag {c1, · · · , c N }un1 · · · diag {c1, · · · , c N }unN



=  cn1 un1 · · · cnN unN



=  un1 · · · unN

 diag {cn1 , · · · , c nN }. (69) 15 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

## D. Proof for Section 5 

D.1. Main Lemmas Lemma D.1. Under a nearly-deterministic transition matrix as defined in Definition 5.1, let Tε be defined as in (37) . Then, under conditions (31) –(32) , we have: 

lim  

> ε→0

lim sup 

> k→∞

X 

> x′∈X \ eX

P [x⋆k = x′] = 0 ,

lim  

> ε→0

lim sup 

> k→∞

P [IrJ in [k − Tε, k ]] = 0 . (70) 

Proof. From Lemma D.4, we have: 

lim sup 

> k→∞

X 

> x′∈X \ eX

P [x⋆k = x′] = O(ε), (71) which implies 

lim  

> ε→0

lim sup 

> k→∞

X 

> x′∈X \ eX

P [x⋆k = x′] = 0 . (72) Similarly, from Lemma D.5, we have: 

lim sup 

> k→∞

P [IrJ in [k − Tε, k ]] = O(εT ε) (73) As we can verify that our Tε in (37) satisfies the following two conditions according to (31) and (32): 

lim  

> ε→0

εT ε = 0 , (74) 

lim  

> ε→0

Tε = + ∞, (75) we have: 

lim  

> ε→0

lim sup 

> k→∞

P [IrJ in [k − Tε, k ]] = 0 . (76) 

Lemma D.2. Under a nearly-deterministic transition matrix as defined in Definition 5.1, let Tε be defined as in (37) . Then, under conditions (31) –(32) , we have: 

lim  

> ε→0

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x′



= 0 , ∀x′ ∈ eX (77) 

Proof. We let x′ = x1 and prove 

lim  

> ε→0

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x1



= 0 , (78) the argument also holds for all x′ ∈ eX . Now we relabel the timestep k − hεM as 0, then (77) can be rewritten as: 

P



arg max  

> x∈eX

ewhεM (x)̸ = x⋆hεM x⋆ 

> 0

= x1, · · · , x⋆M −1 = σM −1(x1), x⋆M = x1, · · · , x⋆hεM = x1



. (79) From the ALF dynamics (24), we have: 

ewi = P (1 − δε) ewi−1 + δε log( eE⊤yi). (80) 16 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Now we define for 0 ≤ i ≤ hεM ,

bwi ≜ (P ⊤)i ewi = ( P ⊤)i mod M ewi. (81) Therefore, 

P ⊤ ew1

| {z } 

> bw1

= (1 − δε) ew0

|{z} 

> bw0

+δεP ⊤ log( eE⊤y1),

(P ⊤)2 ew2

| {z }

> bw2

= (1 − δε) P ⊤ ew1

| {z } 

> bw1

+δε(P ⊤)2 log( eE⊤y2), (82) so on. Then, we have: 

bwi = (1 − δε) bwi−1 + δε(P ⊤)i mod M log( eE⊤yi). (83) Now we define oi ∈ RN −r−1:

oi ≜

bwi(x1) − bwi(x2)

bwi(x1) − bwi(x3)

...

bwi(x1) − bwi(xN −r )

 =



oi(x2)

oi(x3)

...

oi(xN −r )

 . (84) Here, bwi(x1) = ewi(σi mod M (x1)) tracks the ewi(x⋆i ) that corresponds to the true state x⋆i (see (79) ). This can be verified by the following: 

bwi(x1) = u⊤ 

> 1

bwi = u⊤ 

> 1

(P ⊤)i mod M ewi

> (a)

= u⊤    

> σimod M(x1)

ewi = ewi(σi mod M (x1)) , (85) where (a) is due to u⊤ 

> 1

(P ⊤)i mod M (P )i mod M u1 = 1 and (P )i mod M u1 = uσi mod M (x1). We can show that (79) becomes: 

P



arg max  

> x∈eX

ewhεM (x)̸ = x⋆hεM x⋆ 

> 0

= x1, x⋆ 

> 1

= σ(x1), · · · , x⋆hεM = x1



> (a)

≤ P

h

∃x ∈ eX \ { x1}, s.t. ohεM (x) ≤ 0 x⋆ 

> 0

= x1, x⋆ 

> 1

= σ(x1), · · · , x⋆hεM = x1

i

≤ X 

> x∈eX \{ x1}

P ohεM (x) ≤ 0 x⋆ 

> 0

= x1, x⋆ 

> 1

= σ(x1), · · · , x⋆hεM = x1

 , (86) where (a) is due to the fact that for any x ∈ eX \ { x1},

oi(x) ≤ 0 ⇔ bwi(x1) ≤ bwi(x) ⇔(P i mod M )⊤ ewi(x)

> x1

≤(P i mod M )⊤ ewi(x) 

> x

(87) and by substituting i = hεM , we have P i mod M = I, which implies that 

ohεM (x) ≤ 0 ⇔ ewhεM (x1) ≤ ewhεM (x). (88) Now, for any x ∈ eX \ { x1}, we have: 

oi(x) = (1 − δε)oi−1(x)+ δε

h

(P ⊤)i mod M log( eE⊤yi)

i

> x1

−

h

(P ⊤)i mod M log( eE⊤yi)

i

> x



. (89) By recalling the definition of E in (4), we can verify that: 

h

(P ⊤)i mod M log( eE⊤yi)

i

> x

= u⊤ 

> x

(P ⊤)i mod M log( eE⊤yi) = log( P(yi | σi mod M (x))) . (90) 17 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Therefore, (89) becomes: 

oi(x) = (1 − δε)oi−1(x)+ δε log 

P(yi | σi mod M (x1)) 

P(yi | σi mod M (x)) 



. (91) Here, we define: 

gi(x) ≜ log 

 P(yi | σi mod M (x1)) 

P(yi | σi mod M (x)) 



, (92) then we have: 

ohεM (x) = δεhεM −1X

> m=0

(1 − δε)mghεM −m(x)

| {z } 

> o′
> hεM (x)

+(1 − δε)hεM o0(x). (93) Now we continue from (86) . In the following, since all probabilities and expectations are conditioned on the same event as in (86), we omit the conditional notation for clarity. We set 

γε ≜ 1 − (1 − δε)M , (94) and conclude for x ∈ eX \ { x1}:

P [ohεM (x) ≤ 0] = P o′ 

> hεM

(x) ≤ − (1 − δε)hεM o0(x)

> (a)

= P



− 1

γε

o′ 

> hεM

(x) ≥ 1

γε

(1 − δε)hεM o0(x)



> (b)

≤ P



− 1

γε

o′ 

> hεM

(x) ≥ − 1

γε

(1 − δε)hεM 2W



> (c)

≤

E

h

exp 

n

− 1 

> γε

o′ 

> hεM

(x)

oi 

exp 

n

− 1 

> γε

(1 − δε)hεM 2W

o

= exp 

 1

γε



γεΛ



− 1

γε

; x



+ 2(1 − δε)hεM W

 

, (95) where (a) is due to multiplying both sides by − 1 

> γε

< 0. For (b), we took advantage of the following: 

o0(x) = bw0(x1) − bw0(x)= ew0(x1) − ew0(x). (96) Since ∥ ewk∥∞ ≤ W, ∀k according to Lemma D.11 (recall that we labeled k − hεM by 0 in (79) , so our ew0(x1) is actually 

ewk−hεM (x1)), we have 

o0(x) ≥ − 2W, (97) which implies (b). For (c), we apply the Chernoff’s bound. Here, we introduced the logarithmic moment generating function 18 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

of o′ 

> hεM

(x), which can be defined for t ∈ R as: 

Λ ( t; x) ≜ log E

h

eto′ 

> hεM (x)

i

= log E

"

exp 

(hεM −1X

> m=0

δε(1 − δε)mtghεM −m(x)

)# 

= log E

"

exp 

(hε−1X 

> h=0
> M−1

X

> n=0

δε(1 − δε)hM +ntghεM −hM −n(x)

)# 

> (a)

=

> hε−1

X 

> h=0
> M−1

X

> n=0

log E exp {δε(1 − δε)n(1 − δε)hM tghεM −hM −n(x)}

> (b)

= 

> M−1

X

> n=0
> hε−1

X

> h=0

Λn

 δε(1 − δε)n(1 − δε)hM t; x , (98) where (a) follows from the fact that, under the condition that the states of the Markov chain x⋆i , 0 ≤ i ≤ hεM are fixed (see (86) ), the observations y1, · · · , yhεM are independent and so are the gi(x), since from (92) , it can be seen that gi(x) are functions of yi; (b) introduced the following notation: 

Λn(t; x) ≜ log E

h

etghεM −n(x)i

. (99) Since yhεM −n are discrete random variables, so are the ghεM −n(x), therefore (99) and (98) are well defined for t ∈ R. We also utilize the fact that every M timesteps, x⋆i are the same, as are the distributions of yi and, correspondingly, gi(x) (i.e., the distribution of ghεM −n(x), ghεM −M −n(x), · · · , gM −n(x) are identical). We define: 

Γ( x) ≜ n ∈ { 0, · · · , M − 1} E  uσ(hεM −n) mod M (x) − uσ(hεM −n) mod M (x1)

̸ = 0 

> (a)

= n ∈ { 0, · · · , M − 1} E  uσ(−n) mod M (x) − uσ(−n) mod M (x1)

̸ = 0 

> (b)

=

n

n ∈ { 0, · · · , M − 1} ∃y ∈ Y , s.t. P



y σ(−n) mod M (x)

̸

= P



y σ(−n) mod M (x1)

o 

, (100) where (a) is due to the cyclic property of the permutation σ : eX → eX ; (b) is due to the definition of the emission matrix 

E. It can be proved that for all x ∈ eX \ x1, we have |Γ( x)| ≥ 1 according to Lemma D.6. Furthermore, for those 

n ∈ { 0, · · · , M − 1} \ Γ( x), we have the following: 

∀y ∈ Y , P



y σ(−n) mod M (x)



= P



y σ(−n) mod M (x1)



⇒∀ y ∈ Y , log 

 P(y | σ(−n) mod M (x1)) 

P(y | σ(−n) mod M (x)) 



= 0 

> (a)

⇒ghεM −n(x) = 0 a.s. 

⇒Λn(t; x) = 0 , ∀t ∈ R, (101) where (a) follows from the definition of ghεM −n in (92) and the cyclic property of permutation. Therefore, Λ( t; x) can be further simplified as follows: 

Λ( t; x) = X

> n∈Γ( x)
> hε−1

X

> h=0

Λn

 δε(1 − δε)n(1 − δε)hM t; x (102) For n ∈ Γ( x), Λn(t; x) has several interesting properties which are summarized in Lemma D.8, now we apply Lemma D.9 19 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

to prove an upper bound for the inner sum of (102). By setting c = δε(1 − δε)n and μ = 1 − (1 − δε)M = γε, we have: 

> hε−1

X

> h=0

Λn

 δε(1 − δε)n(1 − δε)hM t; x

≤ 1

γε

"Z δε(1 −δε)ntδε(1 −δε)n(1 −δε)hεM t

Λn(τ ; x)

τ dτ + γε

2 − γε

h1(δε(1 − δε)nt)

#

. (103) By setting t = − 1 

> γε

and denoting 

ηε ≜ δε

γε

, (104) we get: 

> hε−1

X

> h=0

Λn

 −ηε(1 − δε)n(1 − δε)hM ; x

≤ 1

γε

"Z −ηε(1 −δε)n

> −ηε(1 −δε)n(1 −δε)hεM

Λn(τ ; x)

τ dτ + γε

2 − γε

h1(−ηε(1 − δε)n)

#

= 1

γε

"Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ −

Z −ηε(1 −δε)n(1 −δε)hεM 

> 0

Λn(τ ; x)

τ dτ + γε

2 − γε

h1(−ηε(1 − δε)n)

#

= 1

γε

"Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ +

Z 0

> −ηε(1 −δε)n(1 −δε)hεM

Λn(τ ; x)

τ dτ + γε

2 − γε

h1(−ηε(1 − δε)n)

#

> (a)

≤ 1

γε

"Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + ηε(1 − δε)n(1 − δε)hεM dn(x) + γε

2 − γε

h1(−ηε(1 − δε)n)

#

, (105) where (a) follows from the fact that Λn(τ ; x) ≥ dn(x)τ for all t ∈ R in (2) of Lemma D.8. Substituting the obtained results back into (102) and (95), we have: 

P [ohεM (x) ≤ 0] 

≤ exp 

 1

γε



γεΛ



− 1

γε

; x



+ 2(1 − δε)hεM W

 

> (a)

≤ exp 



1

γε

 X

> n∈Γ( x)

γεhε−1X

> h=0

Λn

 −ηε(1 − δε)n(1 − δε)hM ; x + 2(1 − δε)hεM W



> (b)

≤ exp 



1

γε

 X

> n∈Γ( x)

"Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + ηε(1 − δε)n(1 − δε)hεM dn(x)+ γε

2 − γε

h1(−ηε(1 − δε)n)



+ 2(1 − δε)hεM W

 

= exp 



1

γε

 X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n)

 , (106) 20 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

where (a) follows from (102); (b) follows from (105). Now, from (86) and the original notation in (78), we have: 

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no irregular jumps in [k − hεM + 1 , k ], x⋆k = x1



≤ X 

> x∈eX \{ x1}

exp 



1

γε

 X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n)

 . (107) For x ∈ eX \ { x1}, by setting 

Aε(x) ≜ X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n), (108) 

Bε(x) ≜ exp 

 1

γε

Aε(x)



, (109) we have: 

lim  

> ε→0

Aε(x)= lim 

> ε→0

X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + lim 

> ε→0

(1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ lim 

> ε→0

γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n)

> (a)

= X

> n∈Γ( x)

Z − 1

> M
> 0

Λn(τ ; x)

τ dτ + exp( −α)

 X

> n∈Γ( x)

1

M dn(x) + 2 W

 , (110) where (a) follows from the facts that 

lim  

> ε→0

ηε(1 − δε)n = 1

M (111) 

lim 

> ε→0

(1 − δε)hεM = exp( −α) (112) 

lim 

> ε→0

γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n) = 0 . (113) We can prove (111) by noticing that 

lim  

> ε→0

ηε(1 − δε)n = lim  

> δε→0

ηε(1 − δε)n = lim 

> δε→0

δε

γε

> (a)

= 1

M , (114) 21 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

where (a) follows from Lemma D.10. We can prove (112) by: 

lim 

> ε→0

(1 − δε)hεM = lim 

> δε→0

(1 − δε)hεM

> (a)

= lim 

> δε→0

(1 − δε)⌊ Tε M ⌋M

> (b)

= lim 

> δε→0

(1 − δε)Tε

= lim 

> δε→0

(1 − δε) αδε 

= exp( −α), (115) where (a) follows from the definition of hε in (38); For (b), we have 

( Tε

M − 1) M ≤ ⌊ Tε

M ⌋M ≤ Tε

⇒(1 − δε)Tε ≤ (1 − δε)⌊ Tε M ⌋M ≤ (1 − δε)Tε−M , (116) and since M does not depend on δε, the limit on the right hand side can be established as: 

lim 

> δε→0

(1 − δε)Tε−M = lim 

> δε→0

(1 − δε)Tε lim 

> δε→0

(1 − δε)−M = lim 

> δε→0

(1 − δε)Tε . (117) Equation (113) can be proved by: 

lim 

> ε→0

γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n) = lim 

> ε→0

γε

2 − γε

X

> n∈Γ( x)

lim  

> ε→0

h1(−ηε(1 − δε)n)

> (a)

= 0 · h1(− 1

M )= 0 , (118) where (a) follows from Lemma D.10 and (111). Now, we consider the following function for t ∈ R:

r(t) = 

Z t

> 0

X

> n∈Γ( x)

Λn(τ ; x)

τ dτ. (119) This r(t) is strictly convex since r′′ (t) > 0, ∀t ∈ R according to (5) of Lemma D.8. We have r(0) = 0 and 

r′(0) = X

> n∈Γ( x)

Λn(τ ; x)

τ τ =0 (a)

= X

> n∈Γ( x)

dn(x) > 0, (120) where (a) follows from (2) of Lemma D.8. Also, from (2) of Lemma D.8, r(t) reaches its minimum r(−1) < 0 at t = −1:

r′(−1) = X

> n∈Γ( x)

Λn(−1; x)

−1 = 0 . (121) Therefore, r(t) increases monotonically from r(−1) < 0 to r(0) = 0 for t ∈ [−1, 0] , and it must have r(− 1 

> M

) < 0. Hence, the first term in (110) is strictly smaller than 0. Now, if we set 

α ≥ α1(x) = log( X

> n∈Γ( x)

1

M dn(x) + 2 W ) − log( − 12

Z − 1

> M
> 0

X

> n∈Γ( x)

Λn(τ ; x)

τ dτ ), (122) and substitute it back into (110), we obtain: 

lim  

> ε→0

Aε(x) ≤ 12

Z − 1

> M
> 0

X

> n∈Γ( x)

Λn(τ ; x)

τ dτ < 0. (123) 22 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Now letting α ≥ max x∈X \{ x1} α1(x), we conclude the proof: 

lim  

> ε→0

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no irregular jumps in [k − hεM + 1 , k ], x⋆k = x1



> (a)

≤ X 

> x∈eX \{ x1}

lim  

> ε→0

Bε(x)= X 

> x∈eX \{ x1}

exp {lim 

> ε→0

1

γε

lim  

> ε→0

Aε(x)}

= X 

> x∈eX \{ x1}

exp {−∞} 

= 0 , (124) where (a) follows from (107). 

Lemma D.3. Consider a nearly-deterministic transition matrix T under Assumptions 5.3 and 5.6. If we choose 

δε = λ

log(1 /ε ) , with λ ∈ (0 , ξ ), (125) 

where ξ > 0 is a constant defined in (145) to ensure δε small enough, then it holds that 

lim sup 

> k→∞

pk ≤ κ ε log 1

ε + o(ε log 1

ε ). (126) 

The resulting decay rate matches that of Bayes-optimal decoder (Khasminskii & Zeitouni, 1996), in the sense that the long-term error is also dominated by an ε log(1 /ε ) term. The constant factor κ > 0 is specified in (148) .Proof. From (40), we obtain that 

lim sup 

> k→∞

pk ≤ X 

> x′∈eX

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x′



+ lim sup 

> k→∞

X 

> x′∈X \ eX

P x⋆k−hεM = x′ + lim sup 

> k→∞

P [IrJ in [k − Tε, k ]] . (127) By Lemma D.4, we have: 

lim sup 

> k→∞

X 

> x′∈X \ eX

P x⋆k−hεM = x′ = O(ε) = o(ε log( 1

ε )) . (128) Moreover, from Lemma D.5, we have: 

lim sup 

> k→∞

P [IrJ in [k − Tε, k ]] ≤ (Tε + 1) ε(−qmin ) = −qmin Tεε + ( −qmin )ε

> (a)

= −qmin αλ ε log( 1

ε ) + o(ε log( 1

ε )) , (129) where (a) follows from the definitions of Tε and δε in (37) and (125), which imply 

Tε = αδε

= αλ log( 1

ε ). (130) 23 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Here, qmin is related to matrix Q and defined in (159) . Now, we only need to focus on the first term. In the following, we let 

x′ = x1 and prove that 

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x1



≤ o(ε log( 1

ε )) , (131) and the same arguments apply for all x′ ∈ eX without loss of generality. Recall from (107), we have 

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no irregular jumps in [k − hεM + 1 , k ], x⋆k = x1



≤ X 

> x∈eX \{ x1}

exp 



1

γε

 X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n)

 . (132) We define the function 

f (ε; x) ≜ exp 



1

γε

 X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n)

 , (133) and consider the ratio 

f (ε; x)

ε log( 1 

> ε

) = 1log( 1 

> ε

) exp 



1

γε

 X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n)

 + log( 1

ε )

 . (134) Based on δε = λ/ log(1 /ε ) in (125), we have 

log( 1

ε ) = λ 1

δε

= λM 1

M δ ε

> (a)

≤ λM 11 − (1 − δε)M

> (b)

= λM 1

γε

, (135) where (a) follows from the inequality (1 + x)M ≥ 1 + M x, ∀x ≥ − 1 and by setting x = −δε; (b) follows from the definition of γε in (94). Taking (135) into (134), we have: 

f (ε; x)

ε log( 1 

> ε

) ≤ 1log( 1 

> ε

) exp 



1

γε

 X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n) + λM 

 . (136) We define 

Cε(x) = X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + (1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n) + λM. (137) 24 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Then we have: 

lim  

> ε→0

Cε(x) = lim 

> ε→0

X

> n∈Γ( x)

Z −ηε(1 −δε)n

> 0

Λn(τ ; x)

τ dτ + lim 

> ε→0

(1 − δε)hεM

 X

> n∈Γ( x)

ηε(1 − δε)ndn(x) + 2 W



+ lim 

> ε→0

γε

2 − γε

X

> n∈Γ( x)

h1(−ηε(1 − δε)n) + λM 

> (a)

= X

> n∈Γ( x)

Z − 1

> M
> 0

Λn(τ ; x)

τ dτ + λM + exp( −α)

 X

> n∈Γ( x)

1

M dn(x) + 2 W

 , (138) where (a) is due to (111) , (112) , and (113) proved previously. Given that the first term is smaller than 0 (see (119) -(121) ), if 

λ satisfies 

λ < − 1

M

X

> n∈Γ( x)

Z − 1

> M
> 0

Λn(τ ; x)

τ dτ, (139) then there always exists a large enough α such that 

lim  

> ε→0

Cε(x) < 0. (140) Now we have: 

lim 

> ε→0

f (ε; x)

ε log( 1 

> ε

) ≤ lim 

> ε→0

1log( 1 

> ε

) exp 



lim 

> ε→0

1

γε

lim  

> ε→0

Cε(x)



= 0 · exp( −∞ ) = 0 , (141) and we can conclude that 

lim 

> ε→0

f (ε; x)

ε log( 1 

> ε

) = 0 . (142) From (132), we have: 

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no irregular jumps in [k − hεM + 1 , k ], x⋆k = x1



≤ X 

> x∈eX \{ x1}

f (ε; x)

> (a)

= o(ε log( 1

ε )) . (143) where (a) follows from the choice of 

λ < ξ (x1) ≜ min  

> x∈eX \{ x1}

− 1

M

X

> n∈Γ( x)

Z − 1

> M
> 0

Λn(τ ; x)

τ dτ. (144) Now, going back to (127) , the above derivation considers the case x′ = x1 without loss of generality. Similar steps can be carried out for all x′ ∈ eX , yielding an overall choice of 

λ < ξ ≜ min  

> x′∈eX

{ξ(x′)} (145) that ensures 

X 

> x′∈eX

lim sup 

> k→∞

P



arg max  

> x∈eX

ewk(x)̸ = x⋆k no IrJ in Ik, x⋆k−hεM = x′



≤ o



ε log 1

ε



. (146) 25 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Combining (127), (128), (129), and (143), we obtain: 

lim sup 

> k→∞

pk ≤ −qmin αλ ε log( 1

ε ) + o(ε log( 1

ε )) 

> (a)

= κε log( 1

ε ) + o(ε log( 1

ε )) , (147) where in (a) we define 

κ ≜ −qmin αλ . (148) Now we complete the proof. 

D.2. Discussion on ξ

The constant ξ defined in (145) plays an important role in this state-decoding problem. Here, we provide a more detailed discussion of this constant. As shown in (144) –(145) , ξ depends on the permutation backbone P and the emission model E through the quantities M ,the order of the permutation matrix P , the set Γ( x), and Λn, the moment generating function of the log-likelihood ratios. This Λn characterizes the intrinsic difficulty of the inference task. In particular, the emission matrix E directly influences 

Λn: when the likelihoods under different states (i.e., the columns of E) are more distinguishable, ξ becomes larger, allowing for a larger choice of λ in (34) . When the emission model is known, ξ can be computed analytically from (145) . When the emission model is unknown or too complex, ξ can instead be estimated empirically from data. The parameter ξ is related to the notion of the error exponent in the social learning literature; see, e.g., Khammassi et al. (2025). Now we provide an example of ξ computation corresponding to the model in Section 6.1: We consider a two-state HMM without transient states ( r = 0 ), thus we have X = eX = {x1, x 2}. We have the permutation order M = 2 . Due to the symmetry, we have 

ξ = ξ(x1)= min  

> x∈eX \{ x1}

− 12

X

> n∈Γ( x)

Z − 12

> 0

Λn(τ ; x)

τ dτ 

> (a)

=

> 1

X

> n=0

− 12

Z − 12

> 0

Λn(τ ; x2)

τ dτ 

> (b)

= −

Z − 12

> 0

Λ0(τ ; x2)

τ dτ (149) where (a) follows from the fact that eX \ { x1} = {x2}, Γ( x2) = {0, 1} since the two columns in E are not identical; (b) is due to the symmetry of the likelihood model determined by E. Then, following the definition of Λn, we have 

ξ = −

Z − 12

> 0

log  0.9 et log(0 .9/0.1) + 0 .1 et log(0 .1/0.9) 

t dt ≈ 0.7206 . (150) 

D.3. Supporting Lemmas Lemma D.4. Consider a nearly-deterministic transition matrix T with D in (22) , let ℓ be the nilpotent index of D22 . Then, we have for k ≥ ℓ:

P [x⋆k = x] = O(ε), ∀x ∈ X \ eX . (151) 

Proof. In the following, we assume D has the from in (22) . The nearly-deterministic transition matrix T can be formed as: 

T =

 P + εQ 11 D12 + εQ 12 

εQ 21 D22 + εQ 22 



, (152) 26 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

where the blocks Q11 ∈ Rr×r, Q 12 ∈ Rr×N −r , Q 21 ∈ RN −r×r , and Q22 ∈ RN −r×N −r form the matrix Q. Then, we have: 

T 2 =

 · ·

(D22 + εQ 22 )εQ 21 + εQ 21 (P + εQ 11 ) (D22 + εQ 22 )2 + εQ 21 (D12 + εQ 12 )



=

 · ·

O(ε) D222 + O(ε)



. (153) Similarly, for T ℓ we can get: 

T ℓ =

 · ·

O(ε) Dℓ 

> 22

+ O(ε)



> (a)

=

 · ·

O(ε) O(ε)



. (154) where (a) follows from the fact that D22 is a nilpotent matrix with index ℓ. For k ≥ ℓ, we always have: 

T k =

 · ·

O(ε) O(ε)



. (155) Therefore, in conclusion, for k ≥ ℓ and ∀x ∈ X \ eX , we have: 

P [x⋆k = x] = πk(x) = (T )kπ0

 

> x

= O(ε). (156) 

Lemma D.5. Under a nearly-deterministic transition matrix as defined in Definition 5.1, we have 

P [one or more irregular jumps in [k − Tε, k ]] = O(εT ε). (157) 

Proof. Since 

P [one or more irregular jumps in [k − Tε, k ]] = 1 − P [no irregular jumps in [k − Tε, k ]] . (158) The probability of not having an irregular jump at a single step k, given x⋆k−1 = xj , is 1 + ε q nj j . Since qnj j ≤ 0, define 

qmax ≜ max  

> j

qnj j , qmin ≜ min  

> j

qnj j . (159) The probability of not having irregular jumps at any step lies between 1 + εq min and 1 + εq max . Therefore, 

(1 + εq min )⌈Tε⌉ ≤ P[no irregular jumps in [k − ⌈ Tε⌉, k ]] 

≤ P[no irregular jumps in [k − Tε, k ]] 

≤ P[no irregular jumps in [k − ⌊ Tε⌋, k ]] 

≤ (1 + εq max )⌊Tε⌋. (160) This implies that 

P [one or more irregular jumps in [k − Tε, k ]] ≤ 1 − (1 + εq min )⌈Tε⌉. (161) We use Bernoulli’s inequality, which states that for any real number x ≥ − 1 and any integer n ≥ 0:

(1 + x)n ≥ 1 + nx. (162) 27 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Now let x = εq min , n = ⌈Tε⌉, if ε is small enough such that 

ε ≤ − 1

qmin 

, (163) we have: 

(1 + εq min )⌊Tε⌋ ≥ 1 + ⌈Tε⌉εq min . (164) Now we have the upper bound: 

P [one or more irregular jumps in [k − Tε, k ]] 

≤ 1 − (1 + εq min )⌈Tε⌉

≤ ⌈ Tε⌉ε(−qmin )

≤ (Tε + 1) ε(−qmin ). (165) Note that when the matrix Q = 0 in Definition 5.1, all the steps in Lemma D.5 still hold, while ε no longer matters since in (163) the right hand side is ∞, and the conclusion becomes 

P [one or more irregular jumps in [k − Tε, k ]] = 0 (166) which is also O(Tεε).

Lemma D.6. Under Assumption 5.6, the set Γ( x) defined in (100) has |Γ( x)| ≥ 1, ∀x ∈ X \ { x1}.Proof. We prove this by contradiction. Suppose there exists an x ∈ eX \ { x1} such that |Γ( x)| = 0 . According to the definition of Γ( x) in (100), this implies that: 

E  uσ(−n) mod M (x) − uσ(−n) mod M (x1)

 = 0 , ∀n ∈ { 0, · · · , M − 1}. (167) Expanding this, we get: 

E (ux − ux1 ) = 0 

E  uσM −1(x) − uσM −1(x1)

 = 0 

E  uσM −2(x) − uσM −2(x1)

 = 0 

...

E  uσ(x) − uσ(x1)

 = 0 . (168) This leads to: 



Eu x − Eu x1

Eu σ(x) − Eu σ(x1)

...

Eu σM −1(x) − Eu σM −1(x1)

 = 0 . (169) This implies: 

eEs(ux − ux1 ) = 0 (170) which means there are two columns in eEs that are identical. Thus, this contradicts Assumption 5.6b. 

Lemma D.7. Under Assumption 5.6, for each n ∈ Γ( x), we have : 

dn(x) ≜ E ghεM −n(x) > 0. (171) 28 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Proof. We have that 

dn(x)= E ghεM −n(x)

= E

"

log P(yhεM −n | σ(hεM −n) mod M (x1)) 

P(yhεM −n | σ(hεM −n) mod M (x)) 

!# 

= X 

> yhεM −n∈Y

P(yhεM −n | σ(hεM −n) mod M (x1)) log P(yhεM −n | σ(hεM −n) mod M (x1)) 

P(yhεM −n | σ(hεM −n) mod M (x)) 

!

> (a)

> 0, (172) where (a) follows from the definition of Γ( x) in (100) , the two likelihood P(yhεM −n | σ(hεM −n) mod M (x1)) and 

P(yhεM −n | σ(hεM −n) mod M (x)) have the same support, and since the two distributions are not the same, we always have 

dn(x) > 0 based on the definition of Kullback–Leibler divergence. 

Lemma D.8. Under Assumption 5.6, let Λn(t; x) be as defined in (99) . It has the following properties for each n ∈ Γ( x):(1) Λn(t; x) < ∞ for all t ∈ R and is infinitely differentiable in R.(2) Λn(0; x) = 0 , Λn(−1; x) = 0 , and Λ′

> n

(0; x) = dn(x) > 0.(3) Λn(t; x) is strictly convex in R:

Λ′′ 

> n

(t; x) > 0, ∀t ∈ R. (173) 

(4) Λn(τ ; x) ≥ dn(x)τ for all t ∈ R.(5) Λn(t;x) 

> t

is continuously differentiable in R. In addition, we have: 

ddt 

Λn(t; x)

t > 0, ∀t ∈ R. (174) 

Proof. We have: (1) The statement follows from the definition of Λn(t; x) < ∞ and the fact that ghεM −n(x) is a discrete random variable. (2) The statement follows from simple calculations and Lemma D.7. (3) The statement can be found in Matta et al. (2016, (115)). (4) The statement can be proved by fist noticing that Λn(τ ; x) is convex from (3) and the first-order convexity characteriza-tion: 

Λn(τ ; x) − Λn(0; x) ≥ Λ′

> n

(0; x)( τ − 0) (a)

⇒ Λn(τ ; x) ≥ dn(x)τ, (175) where (a) follows from (2). (5) The statement can be found in Matta et al. (2016, (119)). 

Lemma D.9. Let f (t) be twice continuously differentiable on R, and define 

g(t) ≜ f (t)

t (176) 29 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

which is continuously differentiable on R. Suppose further that f (0) = 0 . Let τh ≜ c(1 − μ)ht where 0 < μ < 1 and c > 0.Then, for t < 0 it holds that 

> H−1

X

> h=0

f (τh) ≤ 1

μ

"Z ct c(1 −μ)H t

g(τ )dτ + μ

2 − μ h1(ct )

#

, (177) 

where 

h1(t) ≜ max   

> τ∈[t, 0]

|g′(τ )| t2

2 . (178) 

Proof. Now we have t < 0. For h ∈ { 0, · · · , H }, we have 

τh = c(1 − μ)ht ∈ [ct, c (1 − μ)H t]. (179) Now let 

Gh(t) ≜

Z tτh

g(τ )dτ. (180) We can obtain: 

G′

> h

(t) = g(t) (181) 

G′′ 

> h

(t) = g′(t). (182) A second-order Taylor expansion gives: 

Gh(τh+1 ) (a)

= Gh(τh) + G′

> h

(τh)( τh+1 − τh) + 12 G′′ 

> h

(¯ th)( τh+1 − τh)2(b)

= g(τh)( τh+1 − τh) + 12 g′(¯ th)( τh+1 − τh)2(c)

= −g(τh)μτ h + g′(¯ th)2 μ2τ 2

> h
> (d)

= −f (τh)μ + g′(¯ th)2 μ2τ 2 

> h

, (183) where (a) follows from the fact that f (t) is twice continuously differentiable and Taylor’s theorem with Lagrange remainder, here ¯th ∈ [τh, τ h+1 ]; (b) follows from the differentiation of Gh(t) in (181) and (182) ; (c) follows from τh’s definition; (d)

follows from g(t)’s definition. Then we have: 

Z c(1 −μ)H tct 

g(τ )dτ =

> H−1

X

> h=0

Gh(τh+1 )= −

> H−1

X

> h=0

f (τh)μ +

> H−1

X

> h=0

g′(¯ th)2 μ2τ 2 

> h

. (184) 30 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

Therefore, 

> H−1

X

> h=0

f (τh) = 1

μ

"Z ct c(1 −μ)H t

g(τ )dτ +

> H−1

X

> h=0

g′(¯ th)2 μ2τ 2

> h

#

> (a)

≤ 1

μ

"Z ct c(1 −μ)H t

g(τ )dτ +

> H−1

X

> h=0

g′(¯ th)2 μ2c2(1 − μ)2ht2

#

> (b)

≤ 1

μ

"Z ct c(1 −μ)H t

g(τ )dτ + μ2 max   

> τ∈[ct, 0]

|g′(τ )| (ct )2

2

> H−1

X

> h=0

(1 − μ)2h

#

> (c)

≤ 1

μ

"Z ct c(1 −μ)H t

g(τ )dτ + μ

2 − μ max   

> τ∈[ct, 0]

|g′(τ )| (ct )2

2

#

, (185) where (a) follows from the definition of τh; (b) follows from the fact that ¯th ∈ [τh, τ h+1 ] and τh ∈ [ct, c (1 − μ)H t] ⊂ [ct, 0] ;

(c) follows from the fact that P∞

> h=0

(1 − μ)2h = 12μ−μ2 .

Lemma D.10. If δε satisfies (31) and (32) , and γε is defined as in (94) , we have: 

lim  

> ε→0

γε = 0 (186) 

lim 

> ε→0

δε

γε

= 1

M . (187) 

Proof. For the first one, we can easily prove by: 

lim  

> ε→0

γε = lim  

> ε→0

1 − (1 − δε)M = lim  

> δε→0

1 − (1 − δε)M = 0 . (188) For the second one, we have: 

lim 

> ε→0

δε

γε

= lim 

> ε→0

δε

1 − (1 − δε)M

= lim 

> δε→0

δε

1 − (1 − δε)M

> (a)

= lim 

> δε→0

1

M (1 − δ)M −1

= 1

M , (189) where (a) follows from L’Hˆ opital’s Rule and the fact that (δε)′ = 1 and  1 − (1 − δε)M ′ = M (1 − δε)M −1.

Lemma D.11. Under Assumptions 5.3 and 5.6, we have ∥ ewk∥∞ ≤ W , where 

AE = max  

> y∈Y

∥ log( eE⊤y)∥∞,W = max {∥ ew0∥∞, A E }. (190) 

Proof. From Assumption 5.6, we can see AE < ∞ since eE > 0. From Assumption 5.3, we can get W < ∞. Given the dynamics in (24), we have: 

∥ ewk∥∞ = ∥P (1 − δ) ewk−1 + δ log( eE⊤yk)∥∞

≤ ∥ P (1 − δ) ewk−1∥∞ + ∥δ log( eE⊤yk)∥∞

= (1 − δ)∥P ewk−1∥∞ + δ∥ log( eE⊤yk)∥∞

= (1 − δ)∥ ewk−1∥∞ + δ∥ log( eE⊤yk)∥∞. (191) 31 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning        

> Figure 7. Per-time-step error probability for ALF and LOF under ε= 0 .005 .

Then we prove by induction. For k = 0 , the statement is ∥ ew0∥∞ ≤ max {∥ ew0∥∞, A E }, which is trivially true. Assume the hypothesis is true for k − 1, i.e., ∥ ewk−1∥∞ ≤ max {∥ ew0∥∞, A E }. From (191), we have: 

∥ ewk∥∞ ≤ (1 − δ)∥ ewk−1∥∞ + δA E

≤ (1 − δ) max {∥ ew0∥∞, A E } + δ max {∥ ew0∥∞, A E }

= max {∥ ew0∥∞, A E }. (192) Thus, the hypothesis holds for k. By the principle of mathematical induction, it holds for all k ≥ 0.

## E. Additional Details for Section 6 

E.1. Illustrative Example 

The state distribution is initialized as π0 = [1 , 0] ⊤, and the initial logit vector as w0 = [0 , 0] ⊤ for both ALF and LOF .Let ε = 0 .005 . With 20 ,000 Monte Carlo iterations, the estimated instantaneous error probabilities for LOF and ALF 

are shown in Fig. 7. Since w0 = [0 , 0] ⊤, the first update of ALF is given by w1 = δε log( E⊤y1), which implies that the decoding decision at k = 1 depends entirely on the observation. From the observation model in (47) , the resulting error probability at the first timestep is 0.1. A similar argument holds for the LOF dynamics, which explains why all the curves in Fig. 7 start at approximately 0.1. We find that the estimated pk stablizes after 1000 steps, thus it’s valid to use p1000 to approximate lim sup k→∞ pk.It should be noted that the per-time-step error probabilities in Figure 7 and the long-run error behavior in Figure 1 exhibit similar patterns for more general deterministic matrices D and when the underlying Markov chain involves more than two states. These additional results are carefully checked and omitted here for conciseness. 

E.2. RingWorld Game 

E.2.1. S ETTINGS 

A sample transition matrix corresponding to CW1 is 

T (CW 1) = P (CW 1) + εQ (CW 1) , (193) with entries for all i, j ∈ { 1, . . . , N } defined as 

pij (CW 1) = 

(

1, i = ( j mod N ) + 1 ,

0, otherwise , qij (CW 1) = 

(

−2, i = ( j mod N ) + 1 ,

1, i ∈ { (j − 1 mod N ) + 1 , (j + 1 mod N ) + 1 }.

(194) Let θj and ξi denote the angles of state j and beacon i, respectively. The observation model is 

P(yk = ui | x⋆k = j) ∝ exp   cos( θj − ξi). (195) 32 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning               

> Figure 8. Observation probabilities P(y=ui|x⋆)for i= 1 , . . . , 4versus x⋆∈ { 1, . . . , 12 }, computed from (195).

and is further illustrated in Figure 8. The reward function is defined as 

r(x⋆k) = 1

K



1{x⋆k is a goal state } − 1{x⋆k is a trap state }



, (196) where K is the episode length. The total episodic reward therefore lies in PKk=1 r(x⋆k) ∈ [−1, 1] .Our PPO implementations, including the encoder, S5, actor, and critic, follow Lu et al. (2023). The general hyperparameters used for LOF , ALF , and S5-based memory in PPO training are summarized in Tables 1.  

> Table 1. Hyperparameters for training PPO

Parameter Value 

Adam Learning Rate 5e-5 Number of Environments 64 Unroll Length 1024 Number of Timesteps 30e6 Number of Epochs 30 Number of Minibatches 8Discount γ 0.99 GAE λ 1.0 Clipping Coefficient ε 0.2 Entropy Coefficient 0.0 Value Function Weight 1.0 Maximum Gradient Norm 0.5 Learning Rate Annealing None Activation Function LeakyReLU Action Decoder Layer Sizes [128, 128] Value Decoder Layer Sizes [128, 128]  

> Table 2. Additional hyperparameters for S5-based memory

Parameter Value 

S5 Discretization ZOH S5 ∆min 0.001 S5 ∆max 0.1 For S5-based memory , the encoder layer sizes are [128 , 12] with recurrent hidden size 12 , and [128 , 256] with hidden size 

256 . Additional fixed hyperparameters are shown in Table 2. 33 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning 

E.2.2. ALF-I NSPIRED DEEP LEARNING MEMORY 

In our experiment, all permutation matrices P (CW1 ), P (CW2 ), P (CCW1 ), and P (CCW2 ) are circulant, meaning each row is a cyclic right shift of the previous one. According to Gray (2006, Theorem 3.1), any circulant matrix can be diagonalized by the discrete Fourier transform (DFT) matrix: 

P (CW1 ) = V Λ( CW1 )V −1,P (CW2 ) = V Λ( CW2 )V −1,P (CCW1 ) = V Λ( CCW1 )V −1,P (CCW2 ) = V Λ( CCW2 )V −1, (197) where V ∈ CN ×N is the DFT matrix, and each Λ( ·) ∈ CN ×N is diagonal with eigenvalues lying on the complex unit circle. Specifically, Λ( CW1 ) has N diagonal entries uniformly spaced on the unit circle. Moreover, since 

P (CW2 ) = P (CW1 )2, P (CCW1 ) = P (CW1 )⊤, P (CCW2 ) = ( P (CW1 )⊤)2, (198) we obtain 

Λ( CW2 ) = Λ( CW1 )2,

Λ( CCW1 ) = Λ( CW1 )−1,

Λ( CCW2 ) = Λ( CW1 )−2. (199) Then, ALF in (46) can be rewritten as 

V −1wk = Λ( ak−1)(1 − δ) ( V −1wk−1) + δV −1 log( E⊤yk),

wk

|{z} 

> to actor and critic

= V (V −1wk). (200) Let 

hk ≜ V −1wk, (201) so that 

hk = Λ( ak−1)(1 − δ) hk−1 + δV −1 log( E⊤yk),

wk = V hk. (202) This transformation maps the real-valued state dynamics into the complex diagonal space, which is how linear RNNs are usually implemented. In this complex space, we aim to develop ALF-inspired deep learning memory architectures by treating Λ( ·), δ, E, and V as learnable parameters. We refer to these architectures collectively as Deep ALF .We first make all parameters learnable, initializing Λ( ·), E, and V with their true values, and setting δ = 0 .1 to the empirically good value used in Section 6.2. The results are compared with the original ALF (with δ = 0 .1) and the softmax-processed LOF in Figure 9. From Figure 9, we observe that making all parameters learnable allows Deep ALF to slightly outperform ALF , reaching performance levels close to the softmax-processed LOF .Next, we keep Λ( ·), E, V , and δ learnable, but vary their initialization schemes: • Random E: Λ( ·) and V are initialized with their true values, and δ = 0 .1. For E, we first draw κ ∈ RS×N from a standard normal distribution and then normalize it column-wise: 

E = softmax( κ) (column-wise) . (203) This setup represents the case where the observation model is unknown, but other environment parameters are known. 34 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning  

> (a)
> (b) Figure 9. Episodic return versus update. (a) shows the full results, and (b) shows a zoomed-in view of the last 100 updates. The shaded area indicates the standard deviation across 8 random seeds.

• Random δ: Λ( ·), E, and V are initialized with their true values, while δ is drawn uniformly from (0 , 0.5) . This corresponds to an unknown perturbation pattern εQ , since the optimal δ should depend on the specific εQ .• Random Λ( ·): E and V are initialized with their true values, and δ = 0 .1. Each eigenvalue of Λ( ·) is initialized on the complex unit circle with uniformly random phases, representing an unknown permutation transition pattern. • All random: Λ( ·), E, and δ follow the random initialization schemes above. For V , each element is initialized with independent standard normal real and imaginary parts. This setting models a fully unknown environment where only the numbers of states N and observations S are known. From Figure 10, we observe that random initialization of E or δ still enables effective learning, achieving performance close to that with full informed initialization. Random Λ( ·) causes a slight degradation in performance. In addition, when all parameters Λ( ·), E, V , and δ are randomly initialized, making this setting directly comparable to 

S5-based memory , Figure 10a shows that the parameterization of Deep ALF is more efficient and yields better performance than S5-based memory : With the same hidden size of 12, both Deep ALF and S5 converge quickly, but Deep ALF 

35 Why Linear Recurrent Memory Works in Partially Observable Reinforcement Learning  

> (a)
> (b) Figure 10. Episodic return versus update. (a) shows the full results, and (b) shows a zoomed-in view of the last 100 updates. The shaded area indicates the standard deviation across 8 random seeds.

achieves noticeably better final performance, as shown in Figure 10a. Only by increasing S5 ’s hidden size to 256 (with substantially more parameters) does its performance approach that of Deep ALF . Furthermore, Deep ALF offers a far more efficient encoder parameterization. The S5 agent uses a two-layer MLP encoder with 2048 (4 × 128 + 128 × 12) parameters for hidden size 12 and 33 ,280 (4 × 128 + 128 × 256) parameters for hidden size 256. In contrast, Deep ALF uses the encoder log( E⊤y) (Equation (202) , with E learned from scratch, with only 48 (4 × 12) parameters, due to its well-designed dynamics. These results demonstrate that Deep ALF achieves better performance with significantly fewer parameters. However, note that our transformation from ALF in (46) to Deep ALF in (202) relies on all permutation matrices in this experiment being circulant, such that a common DFT matrix V diagonalizes them. In general, while permutation matrices are diagonalizable, they do not necessarily share the same diagonalization matrix V . Designing ALF-inspired deep learning memory architectures for more general nearly-permutation environments is an interesting direction for future work. 36