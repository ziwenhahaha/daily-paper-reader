---
title: Policy Gradient for Continuous-Time Robust Markov Decision Processes
authors: "Tanya Veeravalli, David M. Bossens, Atsushi Nitanda"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04335v2"
tags: ["query:rl"]
score: 9.0
evidence: 连续时间鲁棒MDP策略梯度算法
tldr: 推导出连续时间鲁棒MDP策略梯度并提供样本高效算法。
source: arxiv
selection_source: fresh_fetch
motivation: 连续时间鲁棒MDP策略梯度算法。
method: We propose double-loop optimisers to obtain linear convergence in the oracle-based setting and an $。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
The framework of robust Markov decision processes (RMDPs) allows the design of reinforcement learning agents that satisfy performance guarantees under worst-case transition dynamics. Traditional RMDPs consider discrete-time dynamics and recently, sample-efficient policy gradient algorithms have been considered in this context. This paper investigates policy gradient algorithms within a continuous-time RMDP framework. Policy gradients and adversarial gradients are derived using pathwise and adjoint-based formulas for stochastic and ordinary differential equations. We propose double-loop optimisers to obtain linear convergence in the oracle-based setting and an $\tilde{\mathcal{O}}(\frac{1}{ε^2})$ sample complexity in the sample-based setting in an analysis which also derives novel tools for the framework of undiscounted total cost MDPs. Additionally, we propose mean-field optimisers as distributional optimisers with an $\tilde{\mathcal{O}}(\frac{1}{K})$ oracle-based convergence rate and an $\tilde{\mathcal{O}}(\frac{N^2}ε)$ sample complexity under $N$-particle approximation. The effectiveness of continuous-time policy gradient algorithms is confirmed for both optimisers on continuous-time RMDPs with neural ordinary differential equation dynamics.

---

## 论文详细总结（自动生成）

# 论文总结：连续时间鲁棒马尔可夫决策过程的策略梯度

## 1. 核心问题与整体含义（研究动机和背景）

- **研究问题**：传统鲁棒马尔可夫决策过程（RMDP）研究的是离散时间动力学，而许多实际应用（如机器人控制、物理化学模拟、扩散模型等）使用连续时间 formalism（如 ODE、SDE、PDE）建模。该论文首次将 RMDP 框架扩展到连续时间设置，并设计相应的策略梯度算法。
- **研究动机**：强化学习智能体在真实环境中部署时，转移模型往往不够准确，导致从模拟到真实的迁移性能严重下降。RMDP 通过在“最坏情况”转移动力学下优化策略来解决这一问题，但现有方法均基于离散时间，无法直接应用于连续时间系统。
- **核心挑战**：如何为连续时间 RMDP 推导出样本高效的策略梯度公式，并在理论上证明其收敛性。

---

## 2. 方法论：核心思想、关键技术细节

### 2.1 连续时间 RMDP 形式化
- **动力学模型**：状态由受控微分方程描述 $\dot{x}_t = f_\xi(t, x_t, u_t)$，其中 $\xi$ 参数化不确定动力学，等价于 $f_\xi(t,x,u) = \bar{f}(t,x,u) + g_\xi(t,x,u)$。
- **目标函数**：最小化最坏情况累积成本 $\min_\theta \sup_{\xi \in \Xi} J(\theta, \xi)$，其中 $J(\theta, \xi) = \mathbb{E}\left[\int_0^T r(\tau, x_\tau, u_\tau)d\tau + R(x_T)\right]$。

### 2.2 策略梯度推导（路径形式与伴随形式）
论文针对 ODE 和 SDE 系统分别推导出策略梯度和对抗梯度：

| 定理 | 内容 | 适用场景 |
|------|------|----------|
| **Theorem 1** | 路径形式策略梯度（确定性策略） | ODE 动力学 |
| **Theorem 2** | 路径形式对抗梯度 | ODE 动力学 |
| **Theorem 3** | 伴随形式确定性策略梯度 | ODE 动力学 |
| **Theorem 4** | 伴随形式确定性对抗梯度 | ODE 动力学 |
| **Theorem 5** | 随机策略的伴随梯度 | ODE 动力学 |
| **Theorem 6** | SDE 动力学的确定性策略梯度 | SDE 动力学 |
| **Theorem 7** | SDE 动力学的对抗梯度 | SDE 动力学 |

关键公式示例（Theorem 3，伴随形式确定性策略梯度）：

$$\nabla_\theta J(\theta, \xi) = \mathbb{E}\left[\int_0^T \left(\nabla_\theta \mu_\theta(t, x_t) + \nabla_x\mu_\theta(t, x_t)z_t\right)^\top \nabla_u H(t, x_t, u_t, p_t) dt\right]$$

其中 $z_t = \nabla_\theta x_t$ 为参数敏感性过程，$p_t$ 为伴随状态（costate），由后向 ODE 给出。

### 2.3 两种优化器

#### 双循环优化器（Algorithm 2）
- **结构**：外层循环更新策略（梯度下降），内层循环求解对抗问题（投影梯度上升）。
- **理论保证**：
  - Oracle 环境下线性收敛速率
  - 样本复杂度 $\tilde{O}(1/\varepsilon^2)$
- **关键工具**：提出了针对间接参数化的矩形性（rectangularity）引理（Lemma 2）和 PL 条件分析。

#### 平均场优化器（Algorithm 3，MFL-SDA）
- **结构**：基于分布的优化，在参数分布空间而非点参数空间进行梯度上升-下降。
- **理论保证**：
  - Oracle 收敛速率 $\tilde{O}(1/K)$
  - 样本复杂度 $\tilde{O}(N^2/\varepsilon)$（$N$ 为粒子数）
- **假设**：需要 Log-Sobolev 不等式（LSI）成立。

### 2.4 离散化误差分析
- 证明了 Euler 离散化的一阶误差界 $O(h)$（Lemma 1）。
- 推论表明策略梯度和对抗梯度的离散化误差同样为 $O(h)$（Corollary 1）。

---

## 3. 实验设计：数据集、基准、对比方法

### 3.1 实验环境
- **领域**：鲁棒线性二次调节器（LQR）+ 神经 ODE 不确定性。
- **名义动力学**：$\bar{f}(t,x_t,u_t) = Ax_t + Bu_t$（$A \in \mathbb{R}^{d_x \times d_x}$，$B \in \mathbb{R}^{d_x \times d_u}$）。
- **不确定性项**：MLP 形式 $g_\xi(t,x,u) = 2(W_2 \tanh(W_1[t,x,u] + B_1) + B_2)$，参数约束 $\|W,b\|_\infty \leq \phi = 1.0$。
- **状态/动作空间**：$[-5.0, 5.0]^2$（二维）。
- **策略网络**：2 层全连接隐层（128 神经元）+ Dropout（p=0.6）+ ReLU，输出层依算法类型不同。

### 3.2 对比算法
| 算法类别 | 具体变体 |
|----------|----------|
| **离散时间基线** | Discrete Non-robust / Robust（REINFORCE） |
| **随机哈密顿** | Stochastic Hamiltonian Non-robust / Robust / ZO Both / ZO Inner |
| **确定性哈密顿** | Deterministic Hamiltonian Non-robust / Robust / Robust (explore) |
| **路径形式** | Pathwise Non-robust / Robust / ZO Both / ZO Inner |

### 3.3 测试场景
- **对抗测试**：对训练得到的策略执行 100 步路径形式梯度上升，评估最坏情况成本。
- **鲁棒性测试**：随机采样 50 个转移核，计算均值和最大成本。
- **时间步长**：$\Delta t = 0.0005, 0.001, 0.005, 0.01, 0.05$。

### 3.4 MFL-SDA 实验
- 粒子数 $N = 25$。
- 对比方法：Discrete Robust、Deterministic Hamiltonian Robust、Pathwise Robust。
- 50 次迭代。

---

## 4. 资源与算力

**论文未明确说明 GPU 型号、数量或具体训练时长。** 仅从实验描述中可推断：
- 每次实验运行 5 次独立随机种子。
- 双循环算法训练 100 次宏迭代，每轮宏迭代包含 4 次策略更新和 20 次对抗更新。
- MFL-SDA 训练 50 次迭代，每次迭代需模拟 $N=25$ 个策略-对抗粒子对。
- 所有实验均在标准计算环境中进行，但具体硬件配置未报告。

---

## 5. 实验数量与充分性

### 5.1 实验数量
- **双循环实验**：5 个随机种子 × 5 种时间步长 × 14 种算法变体 = 约 350 个实验配置。
- **MFL-SDA 实验**：5 个种子 × 5 种时间步长 × 3 种算法 = 75 个实验配置。
- **对抗有效性验证**：Table 1 比较 3 种对抗方法。
- **直方图分析**：图 1 展示策略性能的分布（5 seeds × 25 particles）。

### 5.2 实验充分性评估
**较为充分**，体现在：
- ✅ 多时间步长覆盖（5 种）验证离散化敏感性。
- ✅ 对抗测试与鲁棒性测试双视角验证。
- ✅ 零阶（ZO）方法作为弱基准对比。
- ✅ 双循环与平均场两种优化器均有实验支撑。

**存在局限**：
- ❌ 仅在 LQR + 神经 ODE 环境测试，未涉及高维实际应用（如机器人操纵）。
- ❌ 仅二维状态/动作空间，规模泛化性存疑。
- ❌ 未进行消融实验（如移除某类梯度组件的效果）。

---

## 6. 论文的主要结论与发现

1. **连续时间算法显著优于离散时间算法**：即使在最小的测试时间步长下，连续时间策略梯度也大幅超越离散时间 REINFORCE，表明连续时间模型更好地捕捉了动力学信息。

2. **路径形式鲁棒优化器表现最佳**（Table 2, 3）：在对抗测试和鲁棒性测试中，Pathwise Robust 均取得最低成本或最高归一化分数。

3. **随机哈密顿梯度整体最优**：在非零阶方法中，Stochastic Hamiltonian Robust 表现最佳，可能得益于探索-利用的平衡。

4. **零阶方法不适合鲁棒优化**：ZO Both 变体在对抗测试中表现尚可，但在鲁棒性测试中产生极高成本，说明零阶估计无法有效逼近最坏情况转移核。

5. **时间步长对测试性能影响有限**：训练阶段的时间步长对测试性能的影响不显著，验证了连续时间公式的鲁棒性。

6. **MFL-SDA 具有更好的全局优化性质**：表 3 显示平均场优化器的最佳粒子性能普遍优于双循环算法，但代价是更高的样本复杂度。

---

## 7. 优点

1. **理论贡献完整**：系统性地推导了连续时间 RMDP 的策略梯度和对抗梯度（路径形式 + 伴随形式），覆盖 ODE 和 SDE 系统，填补了该领域空白。

2. **收敛性保证强**：双循环算法提供线性收敛和 $\tilde{O}(1/\varepsilon^2)$ 样本复杂度；平均场算法提供 $\tilde{O}(1/K)$ 收敛率和 $\tilde{O}(N^2/\varepsilon)$ 复杂度，均有严格数学证明。

3. **工具创新**：引入了针对间接参数化的矩形性引理（Lemma 2）、总成本 MDP 框架下的性能差异引理、以及修改的 PL 条件（Modified PL condition），可复用于其他 RMDP 分析。

4. **实验设计合理**：对比了多种梯度形式（路径/伴随）、多种策略类型（确定性/随机）、多种时间步长，基准覆盖全面。

5. **离散化误差分析**：提供了 $O(h)$ 离散化误差界，并讨论了高阶积分器（如 Runge-Kutta）的改进空间。

---

## 8. 不足与局限

1. **环境泛化性不足**：仅在 LQR + 神经 ODE 环境验证，未在真实机器人控制或物理模拟中测试，实用性存疑。

2. **维度受限**：实验仅涉及二维状态/动作空间，高维场景（如自动驾驶、围棋）下的可扩展性未验证。

3. **矩形性假设**：分析依赖于 $(x,u)$-矩形性假设（Definition 1），非矩形不确定性集的收敛性仅通过额外因子讨论（Section 5.1），实际应用受限。

4. **算力信息缺失**：未报告 GPU 型号、数量或训练时长，难以评估计算成本的实际可行性。

5. **缺乏消融实验**：未单独验证每种梯度组件（如 $\nabla_x \mu_\theta$ 项）的贡献，理论分析的可解释性受限。

6. **参数敏感性未探索**：超参数（如学习率、内循环步数）通过网格搜索设定，但未系统分析敏感性。

7. **SDE 实用性有限**：Theorem 6、7 的推导假设可访问二阶伴随 $Q_t = \frac{1}{2}\nabla_x^2 V(t,X_t)\sigma_\xi(t,X_t)$，实际中该量难以估计，实验未验证 SDE 情况。

8. **长期收敛未验证**：理论分析基于有限时域 $[0,T]$，无限时域或周期任务的表现未讨论。

---

（完）
