---
title: Resource-aware Computation-Communication Overlap for multi-GPU ML Workloads
title_zh: 面向多GPU机器学习负载的感知资源的计算-通信重叠
authors: "Minyu Cui, Miquel Pericas"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09200v1"
tags: ["query:dgnn-nas"]
score: 8.0
evidence: 多GPU分布式训练的资源感知计算通信重叠
tldr: 随着大规模机器学习模型不断增大，多GPU分布式训练中的通信开销成为严重瓶颈。本研究提出了一种资源感知的计算与通信重叠方法，通过控制共享内存分配和提升通信流优先级，在不修改底层库的情况下实现了高效的计算与通信并发执行，有效降低了总执行时间。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09200v1/fig-001.webp\", \"caption\": \"Table 1: Summary of platforms, workloads, and tile configurations.\", \"page\": 6, \"index\": 1, \"width\": 550, \"height\": 238}, {\"url\": \"assets/figures/arxiv/2606.09200v1/fig-002.webp\", \"caption\": \"Fig. 2: TimeRatio of baseline overlap for cb-ar.\", \"page\": 7, \"index\": 2, \"width\": 568, \"height\": 283}]"
motivation: 在多GPU机器学习训练中，计算与通信的串行执行导致了严重的通信开销瓶颈。
method: 通过调节计算内核的共享内存分配以腾出片上资源，并赋予通信内核更高的调度优先级，从而实现两者的并发执行。
result: "在NVIDIA和AMD多款GPU上的实验表明，该方法实现了有效的计算通信重叠，最高可将总执行时间降低25.5%。"
conclusion: 该研究提供了一种无需修改厂商库或内核代码即可显著提升多GPU系统训练效率的通用解决方案。
---

## 摘要
大规模机器学习（ML）的快速发展使得跨多GPU的分布式训练成为现代ML系统的基本组成部分。随着模型规模和计算吞吐量的持续增加，通信开销已成为多GPU训练中的主要瓶颈，尤其是在计算与通信顺序执行时。本研究探讨了使用两种可移植的运行时控制来实现计算与集合通信的并发执行：用于计算核的基于共享内存的占用率整形，以及用于通信核的提升调度优先级。我们的方法通过逐块（per-block）的共享内存分配来调节计算核的驻留，为通信核留出充足的片上资源以推进其执行。此外，为通信流分配更高的优先级可确保一旦资源可用，通信即可获得稳步推进。在NVIDIA A40、A100、H100以及AMD MI250X GPU上的实验表明，所提方法无需修改厂商库或核实现，即可实现有效的计算-通信重叠，并将总执行时间最高减少25.5%。

## Abstract
The rapid growth of large-scale machine learning (ML) has made distributed training across multiple GPUs a fundamental component of modern ML systems. As model sizes and computational throughput continue to increase, communication overhead has become a dominant bottleneck in multi-GPU training, particularly when computation and communication are executed sequentially. This work explores concurrent execution of computation and collective communication using two portable runtime controls: shared-memory-driven occupancy shaping for computation kernels and elevated scheduling priority for communication kernels. Our approach regulates computation-kernel residency through per-block shared-memory allocation, leaving sufficient on-chip resources for communication kernels to make progress. In addition, assigning higher priority to communication streams ensures steady communication progress once resources become available. Experiments on NVIDIA A40, A100, H100, and AMD MI250X GPUs demonstrate that the proposed method enables effective computation-communication overlap and reduces total execution time by up to 25.5 percent, without modifying vendor libraries or kernel implementations.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **核心问题**：在多GPU分布式机器学习训练（如大语言模型训练）中，计算（如GEMM矩阵乘）与集合通信（如Allreduce、Alltoall）通常串行执行。随着模型规模急剧扩大，通信开销已成为严重的性能瓶颈。
* **研究动机**：虽然现有的并发执行或算子融合技术试图将计算与通信重叠以隐藏延迟，但它们往往面临硬件资源竞争导致的性能下降，或者需要深度修改底层代码、依赖特定硬件/编译器，缺乏通用性和可移植性。
* **整体含义**：本研究旨在探索一种**轻量级、跨平台且无需修改底层厂商库或内核代码**的资源感知与优先级感知方法，以实现多GPU系统中计算与通信的高效并发重叠，从而提升整体训练吞吐量。

### 2. 论文提出的方法论
* **核心思想**：通过调节计算内核的GPU片上资源占用率来“腾出空间”，同时赋予通信内核更高的流调度优先级来“抢占时间”，从而在运行时无缝实现计算与通信的并发执行。
* **关键技术细节**：
  * **基于共享内存的资源驻留控制**：计算密集型的GEMM内核通常会大量使用共享内存。本研究通过调整GEMM的Tile大小（$TILE_M, TILE_N, TILE_K$），控制每个线程块分配的共享内存大小，从而限制GEMM内核的驻留线程块数量。这为轻量级的通信内核预留了充足的SM（流多处理器）资源，使其能够与计算内核共存于同一硬件上。
  * **多流基线重叠**：将计算和通信分配到不同的GPU流中，通过轻量级事件（如CUDA/HIP Event）强制执行迭代内的依赖关系（计算完成后再通信），而迭代间的计算和通信则可以流水线并发。
  * **优先级感知的调度优化**：利用GPU运行时API（如 `cudaStreamCreateWithPriority`），显式地将通信流的优先级设置为高于计算流。这确保了当资源可用时，硬件调度器会优先推进通信任务，防止其被连续不断的GEMM计算饿死。

### 3. 实验设计
* **数据集/场景**：未使用传统意义上的数据集，而是基于LLaMA模型训练的真实计算特征，构建了4种代表性的合成工作负载场景：
  * **cb-ar** (Compute-bound + Allreduce)
  * **mb-ar** (Memory-bound + Allreduce)
  * **cb-a2a** (Compute-bound + Alltoall)
  * **mb-a2a** (Memory-bound + Alltoall)
* **Benchmark（评价指标）**：
  * **TimeRatio**：重叠执行时间与串行执行时间的比值（越低越好）。
  * **Norm. time**：优化后的执行时间相对于基线重叠方法的归一化时间。
  * **Overlap rate**：通信与计算并发执行的时间比例。
* **对比方法**：
  1. **Sequential**：无重叠的串行执行。
  2. **Baseline overlap**：仅使用多流和资源驻留控制，无优先级调度。
  3. **Optimized overlap**：本文提出的完整方法（资源控制 + 高优先级通信调度）。

### 4. 资源与算力
* **GPU型号及数量**：实验在4种主流高性能GPU平台上进行，涵盖了两大厂商：
  * NVIDIA A40 (4卡)
  * NVIDIA A100 (4卡)
  * NVIDIA H100 (4卡)
  * AMD MI250X (8卡)
* **训练时长**：文中**未明确说明**具体的单次实验运行时长，但提到每次实验的前三分之一作为预热排除，剩余取平均值。

### 5. 实验数量与充分性
* **实验数量**：实验涵盖了4种硬件平台 $\times$ 4种工作负载 $\times$ 2种Tile配置 $\times$ 多种不同线程块数量组合。此外，还进行了针对不同Tile大小（opt1 vs opt2）的消融分析。
* **充分性与客观性**：实验设计**非常充分且客观**。跨硬件架构（NVIDIA Ampere/Hopper, AMD CDNA2）的验证证明了方法的普适性；通过控制变量法逐步引入优先级机制，并客观展示了在AMD MI250X上由于架构限制带来的性能折损，没有刻意隐瞒方法的局限性。

### 6. 论文的主要结论与发现
* **结论**：所提方法无需侵入式修改代码，即可在主流GPU上实现高效的计算通信重叠，最高可将总执行时间降低 **25.5%**（在A40的cb-ar场景下）。
* **关键发现**：
  1. 仅提供并发机制是不够的，资源竞争会导致通信被计算阻塞。
  2. 提升通信流的优先级能显著缓解“尾部效应”，在NVIDIA GPU上最高可实现90%的重叠率。
  3. 增大GEMM的Tile大小（$TILE_K=64$）可以延长计算时间窗口，为优先级调度提供更多隐藏通信延迟的机会，尤其在内存密集型任务中效果显著。
  4. **架构限制**：在AMD MI250X上，由于其每个CU的共享内存和L2缓存较小，高并发度下资源极易饱和，导致该方法在高线程块数量时效果不佳甚至产生负优化。

### 7. 优点
* **极高的可移植性与易用性**：不依赖定制化的编译器、不需要重写底层NCCL/RCCL库或内核代码，仅通过调整运行时参数（共享内存分配、流优先级）即可生效。
* **深刻的系统级洞察**：巧妙利用了计算与通信内核对资源需求的不对称性（计算吃Shared Memory，通信吃优先级），实现了“四两拨千斤”的优化效果。
* **广泛的硬件验证**：同时在NVIDIA和AMD的最新架构上进行了详尽的测试，为分布式ML系统的跨平台调优提供了极具价值的实操指导。

### 8. 不足与局限
* **应用场景限制**：该方法高度依赖于底层GPU硬件调度器的行为。如文中所述，在AMD MI250X等多芯片模块架构且缓存较小的硬件上，强制资源隔离会削弱计算并发度，导致吞吐量下降。
* **缺乏端到端验证**：实验主要基于合成的GEMM+通信微基准测试，虽然具有代表性，但**未在真实的大规模端到端分布式训练框架（如Megatron-LM）中进行验证**。真实框架中的内存分配、其他算子的干扰可能会影响实际收益。
* **自动化程度不足**：Tile大小的调整和线程块数量的配置目前需要手动或启发式设定，未来需要开发自适应的运行时策略来根据不同负载自动寻优。

（完）
