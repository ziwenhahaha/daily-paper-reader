Title: Global Unknown Estimation: A Statistical Framework for Wireless Distributed Learning

URL Source: https://arxiv.org/pdf/2606.02891v1

Published Time: Wed, 03 Jun 2026 00:20:31 GMT

Number of Pages: 6

Markdown Content:
# Global Unknown Estimation: A Statistical Framework for Wireless Distributed Learning 

Yicheng Qu 

University of Toronto 

eason.qu@mail.utoronto.ca 

Ali Bereyhi 

University of Toronto 

ali.bereyhi@utoronto.ca 

Ben Liang 

University of Toronto 

liang@ece.utoronto.ca 

Abstract —Over-the-air computation (AirComp) is widely used for model aggregation in wireless distributed learning. Although it enhances communication efficiency, we believe the AirComp aggregation has limited effectiveness due to the difference between its target problem and that of distributed learning. In this paper, we develop a rigorous formulation for optimal 

model aggregation in wireless distributed learning. Using this formulation, we show that AirComp aggregation generally assumes a mismatched statistical model for local parameters. We then propose a statistical framework for model aggregation, called global unknown estimation (GUE). It captures the statistical relation between the local and global model parameters, allowing to interpret model aggregation as an inference task. We validate the efficiency of GUE through numerical experiments. Our results show that, in the low SNR regime, GUE can reduce the required power for model aggregation by approximately 15 dB compared to AirComp aggregation. Remarkably, this gain is obtained without additional computational overhead. 

I. I NTRODUCTION 

In distributed learning, multiple clients collaboratively train a model, potentially with the help of a central server, without sharing their local datasets. A well-known approach is federated learning [1], where the training is carried out through multiple rounds of communication: in each round, clients train local models on their own datasets and share the models with the server. The server aggregates these models into a global model and broadcasts it to the clients to initiate the next round of training. This paradigm has gained significant importance in modern large-scale and privacy-sensitive applications [2], as it allows computation to be pushed closer to where data are stored. Under perfect communication links, the model transmission and aggregation can be readily performed [3]. In practical wireless networks, however, the clients are connected through noisy communication links with imperfect channel conditions. This introduces a fundamental bottleneck [4]. To address this challenge, various approaches for efficient model aggregation in these systems have been proposed. Among these proposals, over-the-air computation ( AirComp )has been widely accepted as the state-of-the-art [5]. In this approach, instead of recovering transmitted signals separately and then aggregating them afterwards, the server directly esti-mates the desired aggregation by leveraging the superposition of waveforms over the wireless channel [6]. This enables 

> This work was supported in part by the Natural Sciences and Engineering Research Council of Canada.

simultaneous transmissions over the same channel, enhancing the communication efficiency. From the statistical viewpoint, AirComp implicitly assumes that realizing the predefined aggregation policy that is deployed in an error-free network, e.g. , weighted averaging, is the optimal approach to aggregate a global update from the noisy received signals. Although this approach leads to a valid aggregation, its optimality is not guaranteed. In fact, using AirComp , the global update is determined by minimizing the computation error between the aggregated update and a predefined function of local models. How explicitly this computation error affects the learning performance, however, remains unclear. Several studies characterize the efficiency of AirComp aggre-gation by connecting its computation error to learning metrics. The work in [7] shows that the computation error determines an upper bound on the optimality gap of the learning algorithm. The studies in [8] and [9] extend this analysis to settings with imperfect channel state information ( CSI ) and heterogeneous data, respectively, and use this connection to propose adaptive aggregation schemes. The study in [10] considers a joint design in settings with both imperfect CSI and heterogeneous data and develops an alternative aggregation scheme that directly minimizes an upper bound on the optimality gap. This idea is further extended to digital aggregation in [11] by implementing a coded AirComp approach via lattice coding. All existing studies commonly focus on optimality bounds in terms of computation error and assume that minimizing this error 

directly enhances learning performance. In this study, we challenge this assumption and show that there is no strong connection between these two metrics. Intuitively, the goal of model aggregation is not to realize a predefined function, but rather to provide an efficient estimate 

of the global update from the received signals. This describes a distributed inference task, in which a global unknown is to be estimated from a set of distributed observations; see for instance [12], [13]. Despite its intuitive connection, this framework has not been adapted to distributed learning. Our study aims to establish a novel framework that exploits the connection between these two concepts for efficient model aggregation in wireless networks. 

A. Contributions 

This work proposes a new framework, called global unknown estimation ( GUE ), for model aggregation in wireless dis-

> arXiv:2606.02891v1 [eess.SP] 1 Jun 2026

tributed learning systems. GUE deviates from the conventional AirComp approach and interprets the aggregation task as a distributed inference problem. This enables us to define the notion of optimal aggregation and specify it under a postulated prior distribution for local parameters. Our experiments demon-strate that GUE can significantly enhance the power efficiency of model aggregation by shifting its focus from computation error minimization to the optimal estimation of global model parameter .More precisely, the main contributions of this work are as follows:  

> •

We formulate model aggregation as a statistical inference problem and define the notion of optimal aggregation .By fitting AirComp aggregation into this framework, we show that AirComp aggregates the model under a 

mismatched assumption on local model parameters. This finding explains the restricted efficiency of AirComp aggregation.  

> •

To remedy the inadequacy of AirComp , we propose the GUE framework, which models the aggregation target as a deterministic unknown and treats the local updates as its noisy observations. Using this statistical model, we design a novel aggregation scheme that determines a maximum likelihood ( ML ) estimator of the aggregation target from the local parameters. We study the complexity and efficiency of the proposed scheme and compare it against the AirComp method.  

> •

We validate the efficiency of GUE through extensive experiments. Numerical results depict that GUE offers sub-stantial enhancement in learning performance compared to conventional aggregation via AirComp , especially at the low signal-to-noise ratio ( SNR ) regime. Our investigation further demonstrates that minimizing the computation error does not necessarily lead to better learning performance. This supports our conjecture that the implicit assumption in AirComp does not adequately capture the statistical goal of model aggregation in distributed learning. 

B. Notation 

Throughout this paper, vectors and matrices are denoted by bold lower case letters ( e.g. , v) and upper case letters ( e.g. ,

H), respectively. The i-th component of vector v is denoted by v[i]. The integer set is shortened as [n] = {1, 2, . . . , n }.The diagonal matrix with entries {d1, . . . , d K } is denoted by 

diag {d1, . . . , d K }. The imaginary unit is denoted by j, and the real and imaginary parts of c ∈ C are denoted by ℜ(c), ℑ(c)

respectively. Other notations are defined in the text. II. S YSTEM MODEL AND PROBLEM FORMULATION 

A. System Model 1) Distributed Learning Setting: Let Dk denote the local dataset at client k. For a sample ζi ∈ D k and a model vector 

a ∈ RL, client k computes the sample loss of the model as 

f (a, ζ i) for some loss metric f . The local loss of the model at client k is hence given by 

Fk(a) = 1

|D k|

X

> ζi∈D k

f (a, ζ i). (1) The clients aim to train a global model jointly over the global dataset, i.e. , D = ∪kDk. This learning task can be cast as the distributed minimization of the global loss function, which is defined in terms of the local loss functions as 

F (a) = 1

|D| 

X

> k

|D k|Fk(a). (2) For this task, the clients invoke the following mechanism over T rounds . At the beginning of round t ∈ [T ], the clients set their models to the current global model at−1, broadcast by the server through an error-free downlink channel. Client k

then updates the model locally by reducing its local loss Fk(a)

through multiple iterations of a gradient-based optimizaer. Let 

ak,t be the latest model vector at client k, and the normalized local model ¯ak,t is obtained by 

¯ak,t [ℓ] = ak,t [ℓ] − μk,t 

νk,t 

, (3) with the sample mean μk,t and variance ν2 

> k,t

computed as 

μk,t = 1

L

> L

X

> ℓ=1

ak,t [ℓ], (4a) 

ν2 

> k,t

= 1

L

> L

X

> ℓ=1

(ak,t [ℓ] − μk,t )2. (4b) The local parameters in ¯ak,t are then sent to the server to be 

aggregated into a global model at. Note that it is also common to send the model difference ∆ak,t = ak,t − at−1, and our study can be directly applied to such scenario. In the rest of this work, we focus on federated learning ,where the local parameters are aggregated proportionally to their local data size. Our proposed method and analysis can be straightforwardly applied to other distributed learning systems. 

2) Communication Setting: The aggregation is carried out directly by analog transmissions through a wireless multiple access channel ( MAC ) between the K clients and the server. We assume that the clients are single-antenna, while the server is equipped with an N -antenna array. Since the signal in the communication system contains both in-phase and quadrature components, the transmitted model parameters are converted into the complex domain. Hence, the clients transmit their local parameters, i.e. , ¯ak,t , in ⌈ L 

> 2

⌉ resource blocks. We assume synchronized transmission, such that in block i ∈ [⌈ L 

> 2

⌉],with ℓ = 2 i − 1, the clients send their ℓ-th and (ℓ + 1) -th parameters, i.e. , ¯ak,t [ℓ] + j ¯ ak,t [ℓ + 1] , synchronously. Without loss of generality, we focus on a sample block in an arbitrary round and denote the entry sent by client k as uk ∈ C, i.e. ,

uk = ¯ ak,t [ℓ] + j ¯ ak,t [ℓ + 1] for an odd index ℓ at round t. In the sequel, we drop the indices ℓ and t for simplicity. Client k constructs its transmit signal from its parameter as xk = βkuk with the scaling factor βk ∈ C that satisfies |βk|2 ≤ P for some transmit power P > 0. It then sends xk

over its uplink channel. The server receives the signal y ∈ CN ,which is given by 

y =

> K

X

> k=1

hkxk + z, (5) with hk ∈ CN denoting the vector of channel coefficients from client k to the server, and z ∈ CN being zero-mean complex-valued additive white Gaussian noise with variance 

σ2 

> z

, i.e. , z ∼ CN (0, σ 2 

> z

IN ). Defining x = [ x1, . . . , x K ]T and 

u = [ u1, . . . , u K ]T, we can compactly represent the received signal as y = HBu + z, where B = diag {β1, . . . , β K } and 

H = [ h1, . . . , hK ]. In the sequel, we refer to B as the power matrix and H as the channel matrix, which are assumed to be perfectly known at the server. In addition to the data transmission, the pair of sample mean and sample variance (μk, ν k) is sent to the server without any transmission error, e.g. , using orthogonal channels with error correction codes. They are used by the server in recovering a properly scaled and shifted estimate of the global model. 

B. Problem Formulation 

Let θ ∈ C be the ground-truth global parameter that the server needs to estimate after each transmission block, i.e. , θ =

a∗ 

> t

[ℓ] + j a∗ 

> t

[ℓ + 1] . Here, a∗ 

> t

denotes the global model at round t

in the centralized scenario, where all local datasets are available at the server. The server aims to compute θ by aggregating local parameters from the received signal y. In general, this operation is described by an estimator ˆθ (·) : CN 7 → C, which extracts θ from the received signal. To be able to rigorously define an efficient mapping, we need to define the notion of optimal aggregation .

Definition 1 (Optimal aggregation) : The optimal aggregation from y is defined as 

g(y) = argmin   

> ˆθ(·): CN7→C

C(θ, ˆθ(y)) , (6) 

for some cost function C.

According to Definition 1, the conventional aggregation problem can then be interpreted as an inference problem. This interpretation indicates that the design of the optimal aggregation depends on the statistical model of the ground-truth global parameter θ.In the sequel, we show that the conventional aggregation scheme based on AirComp can fit into this formulation by considering a basic statistical representation for the ground truth. Despite its popularity, we see that AirComp does not capture the most effective strategy for distributed learning. We hence propose an alternative model for the ground truth, through which we can develop a new over-the-air aggregation scheme. III. A IR COMP AGGREGATION AND ITS INADEQUACY 

A. AirComp Aggregation 

With perfect links, the global model parameter is com-puted as a weighted average of local model parameters. Let 

w specifies the client weights, e.g. , by data size ratio as 

w[k] = |D k|/|D| . To recover the normalization process, the server aggregates the local models as 

f (u) = w′Hu + wHμ, (7) where w′[k] = w[k]νk and μ[k] = μk. Conventionally, this aggregation scheme is directly extended to noisy transmission, where the server aims to realize an estimate of global model parameter from its received signal y, i.e. , setting θ = f (u).The aggregation in this case is interpreted as over-the-air computation of the linear function f (u), i.e. , analog function computation [6]. This paradigm is often referred to as over-the-air aggregation [5]. The use of AirComp for aggregation implicitly imposes the following (mismatched) model on the local parameters: they are modeled as independent and identically distributed (i.i.d. ) Gaussian vectors u ∼ CN (0, Ik), with mean zero and unit variance [14]. 1 Since f (u) is linear, this implies the global model parameter θ is a Gaussian random variable. The conventional solution to the computation task in this setting is given by the minimum mean squared error ( MMSE ) estimator, defined as 

gMMSE (y) = argmin 

> ˆθ

Ep(θ|y)

h

|θ − ˆθ|2i

= Ep(θ|y) [θ] . (8) This describes a Gaussian MMSE estimation, whose solution is given by [15] 

gMMSE (y) = ( HBw ′)H(HBB HHH +σ2 

> z

IN )−1y+wHμ. (9) In (9), the aggregation is computed directly from y in terms of the channel and customized power matrix B. To design 

B, we evaluate the mean squared error ( MSE ) between the ground-truth and the estimated parameter as 

E

h

|θ − ˆθ|2i

= E |f (u) − gMMSE (y)|2

= w′Hw′ − (HBw ′)H(HBB HHH + σ2 

> z

IN )−1HBw ′, (10) and set the power matrix B by solving 

max  

> B

(HBw ′)H(HBB HHH + σ2 

> z

IN )−1HBw ′ (11) s.t. |βk|2 ≤ P ∀k ∈ [K],

which minimizes the expected aggregation error. 

B. AirComp as Optimal Aggregation 

The AirComp aggregation approach is readily fitted to our formulation of optimal aggregation. For this scheme, the cost function C determines the squared error between θ and its estimate, i.e. , C(θ, ˆθ) = Ep(θ|y)

h

|θ − ˆθ|2

i

.This interpretation of AirComp aggregation as an optimal aggregation scheme reveals its inadequacy for distributed learn-ing. In fact, AirComp aggregation relies on the assumption that the local parameters are drawn from statistically independent processes and that the ground-truth is explicitly computed 

> 1The zero mean and unit variance follow from the normalization of local parameters.

as θ = f (u). Nevertheless, from a statistical learning point of view, this assumption is not valid. In federated learning, the local model parameters are computed by backpropagation over the same model architecture with the same initial point 

at−1. Assuming statistically independent parameters is hence unrealistic in this case. Following this observation, we design a new optimal aggregation scheme under a more realistic statistical model for the local parameters. IV. G LOBAL UNKNOWN ESTIMATION 

To properly capture the goal of distributed learning in our formulation, we propose a new scheme, GUE , which sets the entries of the centralized global model as the estimation target, 

i.e. , θ = a∗ 

> t

[ℓ] + j a∗ 

> t

[ℓ + 1] . To this end, the ground-truth global parameter θ is characterized as a deterministic unknown .Since the local model training essentially aims to approximate such unknown, the local parameters are modeled as sample estimators of θ, which compute an erroneous estimate of θ due to their limited local dataset Dk. With this model, the local parameters are described by a joint distribution parameterized with θ, which we denote by pθ (u). As a result, the distribution of the received signal y is also parameterized with θ and obtained by 

pθ (y) = 

Z

pθ (u)p(y|u)du, (12) where p(y|u) describes the channel. Motivated by the results of [16], we assume pθ (u) to be a Gaussian distribution. The mean and covariance of this distribution are obtained from the postulated distribution defined later. Building on the distribution, we derive the optimal aggregation in GUE and analyze its efficiency and complexity. 

A. Postulated Distribution 

In GUE framework, the Kullback-Leibler ( KL ) divergence is employed as the cost function, i.e. ,

C(θ, ˆθ) = 

Z

pθ (y) log pθ (y)

pˆθ (y) dy. (13) With this cost function, the optimal aggregation describes a standard ML estimator which estimates the ground-truth θ from 

y by maximizing the likelihood pθ (y).Consistent with AirComp aggregation, we assume that the weighted average function f (u) describe the optimal aggregation under perfect communication links , i.e. , when the server has access to local parameters u. Considering the cost function in GUE , this means that f (u) is an ML estimator of 

θ from the samples collected in u, i.e. ,

f (u) = ˆθML (u), (14) and we use this assumption to specify the distribution pθ (u).

Definition 2: The postulated distribution of local parameter u

is a parameterized family pθ (u), for which f (u) serves as an ML estimator of θ.

The above definition enables us to analytically specify pθ (u).To this end, let pθ (u) belong to a Gaussian family, i.e. , u ∼CN (( θ − wHμ)v, G) for some v ∈ CK and G ∈ CK×K . The postulated distribution is then specified by v[k] = 1 /(Kw′[k]) 

and G = diag 1/(Kw′[1] 2), . . . , 1/(Kw′[K]2) . To show this, we note that 

∇θ log pθ (u) = w′Hu − θ + wHμ, (15) where the complex partial derivative operator is defined as 

∇θ = 12

 ∂∂ℜ(θ) + j ∂∂ℑ(θ)



. (16) Setting this derivative to 0, we have 

ˆθML (u) = w′Hu + wHμ = f (u), (17) which satisfies the condition in (14). Note that, similar to the assumed prior in AirComp, pθ (u)

contains no correlation term. However, the independence observed in GUE is for a fixed θ. Furthermore, it is not an assumption; it is induced by treating f (u) as an ML estimator under the Gaussian model. This is in contrast to AirComp, which enforces independence by design. 

B. Aggregation via GUE 

Considering the postulated distribution, the received signal is described in terms of the unknown θ by the Gaussian distribution y ∼ CN (( θ − wHμ)r, Σ), where r = HBv and 

Σ = HBGB HHH + σ2 

> z

IN . The optimal aggregation via GUE is hence given by the ML estimator of θ from y. Determining the derivative of log-likelihood as 

∇θ log pθ (y) = rHΣ−1(y − (θ − wHμ)r). (18) We set it to zero to find the solution as 

θ = rHΣ−1yrHΣ−1r + wHμ. (19) Hence, the optimal aggregation in the GUE framework is 

gML (y) = (HBv )H(HBGB HHH + σ2 

> z

IN )−1y

(HBv )H(HBGB HHH + σ2 

> z

IN )−1HBv + wHμ.

(20) It is worth noting that while this estimator is computed linearly from y, the linear beamforming vector is different from that of the MMSE estimator. This follows from the fact that this estimator takes a different statistical model from the one considered in AirComp. 

C. Efficiency of GUE Aggregation 

It is straightforward to see that GUE determines an unbiased 

estimator of the ground-truth θ with minimum variance given by the Cramér–Rao lower bound ( CRLB ). To show this, we first note that since the GUE estimator is a linear function of 

y as shown in (19), it is also Gaussian, with mean θtHr and variance tHΣt , where t = Σ−1rrHΣ−1r . Simplifying, we can write 

gML (y) ∼ CN (θ, 1

rHΣ−1r ), (21) which concludes that gML (y) is an unbiased estimator. The estimation error is thus characterized by its variance. We next show that gML (y) achieves the CRLB . Under the regularity conditions, the CRLB states that the variance of any 

unbiased estimator ˆθ(y) is lower bounded by the reciprocal of its Fisher information, i.e. ,

Epθ (y)

h

|ˆθ(y) − θ|2i

≥ 1

Iy(θ) , (22) where the Fisher information is defined as 

Iy(θ) = Epθ (y)

|∇ θ log pθ (y)|2 . (23) For the GUE estimator, we have 

Iy(θ) = Epθ (y)[|rHΣ−1(y − (θ − wHμ)r)|2],

= rHΣ−1r, (24) whose reciprocal coincides with the variance of gML (y). Since the Fisher information exists, and integration and differentiation are interchangeable, the regularity conditions are satisfied and 

gML (y) achieves the minimum variance. Considering the unbiasedness of the GUE estimator, we can design the power matrix B to minimize the variance of the estimator, or equivalently, maximize the Fisher information. This leads to the following power allocation policy: 

max  

> B

(HBv )H(HBGB HHH + σ2 

> z

IN )−1HBv (25) s.t. |βk|2 ≤ P ∀k ∈ [K].

Note that this power allocation policy constitutes the same type of optimization as in AirComp aggregation, i.e. , (11). 

D. Complexity of GUE Aggregation 

The overall complexity of either AirComp or GUE com-prises two components: (i) model aggregation, and (ii) power allocation policy. The subsequent discussion compares these components for these two methods. Considering the complexity of model aggregation, both methods compute their estimator of the global model via linear receive beamforming, i.e. , as bTy + wHμ. The beamforming vector b is computed differently in these approaches; see (9) and (20). The two methods share the same order of computation complexity for determining b from the channel and power matrix: both approaches require a matrix inversion of complexity O(N 3).With respect to power allocation, we note that the design problems in (11) and (25) impose the same computational complexity on the system. In fact, both problems are non-convex, and their exact solutions are intractable. We thus approximate the solution iteratively via sequential least squares quadratic programming ( SLSQP ) [17]. In each iteration, SLSQP builds a quadratic approximation of the objective and a linear approximation of the constraints around the current optimization variable. These approximations define a quadratic program (QP) with a least-squares penalty, which is solved by a standard QP solver; see [18, Chapter 25]. From the above discussions, we can conclude that AirComp and GUE impose the same computational overhead on the system. The performance gains, reported in our numerical experiments, are hence obtained at no extra computation cost. 0 20 40 60 80          

> 40
> 60
> Communication round
> Test Accuracy (%)
> IdealAgg
> GUE, BSLSQP
> GUE, Bmax
> AirComp, BSLSQP
> AirComp, Bmax
> Fig. 1: Test accuracy vs. round at SNR = 5 dB with N= 4 antennas at server.

V. N UMERICAL RESULTS 

We validate our analysis through numerical experiments. To this end, we consider CIFAR-10 classification task with a four-layer convolutional neural network ( CNN ) with ReLU activation at hidden layers as in [10]. The output is computed by a fully-connected layer followed by softmax. The model has roughly 10 5 training parameters. The learning rate η starts at 0.002 and decreases following a cosine annealing scheduler. The local model of each client is trained on mini-batches of size B = 128 over S = 30 local iterations. The training loop contains T = 100 communication rounds. The training dataset is uniformly distributed among K = 32 clients. The channel matrix H is full-rank and follows Rayleigh fading with channel gain ∼ exp(0 .5) . We assume the channel coherence time is longer than the transmission duration, hence H is fixed within each communication round. The SNR is defined as SNR = P/σ 2 

> z

. For power allocation according to (11) and (25), respectively, for AirComp and GUE, we consider 

Bmax = diag 

n√P , . . . , √P

o

for uplink transmission with maximum power, and BSLSQP when the underlying optimization is solved via SLSQP . In SLSQP , we initialized B with Bmax ;thus, BSLSQP always achieves a better objective than Bmax . 2

Fig. 1 plots the test accuracy against communication rounds for Bmax and BSLSQP at SNR = 5 dB. For comparison, the results for ideal aggregation (FedAvg [3]), i.e. , federated averaging with perfect parameter exchange, are further shown. The results depict that at this SNR regime, GUE can closely track ideal aggregation, whereas using AirComp , the training performance is significantly degraded. We also note that BSLSQP 

provides considerably better performance than Bmax in both AirComp and GUE . Therefore, in the following experiments, both methods utilize the power matrix BSLSQP .Fig. 2 sketches the test accuracy and aggregation MSE for both AirComp and GUE against SNR at different numbers of antennas N . Fig. 2(a) shows the maximum test accuracy, and Fig. 2(b) shows the average MSE between the true aggregation and estimated parameters, both determined over T = 100 

rounds. From Fig. 2(a), we note that the two methods exhibit compa-rable accuracy at high SNR s. This is intuitive, as both methods      

> 2The code and further experimental details are available at https://github. com/YQ-Comm/GUE-A-statistical-framework-for-wireless-DL −30 −20 −10 010 20
> 20
> 40
> 60

SNR [dB]              

> Test Accuracy (%)
> GUE, N=32
> GUE, N=4
> AirComp, N=32
> AirComp, N=4 −30 −20 −10 010 20
> 10 −6
> 10 −5
> 10 −4
> 10 −3
> 10 −2
> 10 −1

SNR [dB]                

> MSE
> GUE, N=32
> GUE, N=4
> AirComp, N=32
> AirComp, N=4 10 20 30 40 50 60 70 80
> 10 −6
> 10 −5
> 10 −4
> 10 −3
> 10 −2
> 10 −1

Test Accuracy ( %)        

> MSE
> GUE, N=32
> GUE, N=4
> AirComp, N=32
> AirComp, N=4

(a) (b) (c)     

> Fig. 2: Performance of GUE and AirComp aggregation with different number of server antennas N: (a) and (b) show test accuracy and average MSE against SNR, respectively. (c) is showing the change of average MSE against test accuracy.

become weighted averaging, which is the optimal aggregation scheme under perfect communication links. At lower SNR s, however, GUE significantly outperforms AirComp . In both limited-antenna ( N = 4 ) and full-antenna ( N = 32 ) scenarios, GUE achieves an approximate 15 dB gain over AirComp. From Fig. 2(b), we note that, despite its degraded learning performance, AirComp aggregation interestingly achieves lower MSE values than GUE almost at all SNR s. This observation aligns with our initial conjecture that the computation error of an aggregation scheme is not an adequate metric for evaluating learning performance. In other words, an aggregation method with a lower MSE does not necessarily lead to a higher test accuracy. This observation is depicted more explicitly in Fig. 2(c), where different schemes can achieve the same accuracy with distinct MSE values. To validate the generality of our observation, we present the experimental results for some other datasets in Table I. In the low SNR regime, the test accuracy achieved by AirComp aggregation remains degraded for all datasets, whereas GUE closely tracks ideal aggregation.       

> TABLE I: Accuracy ( %) after T= 100 rounds of training with N= 4 antennas.

Dataset SNR in [dB] IdealAgg GUE AirComp MNIST -5 98.78 98.39 93.51 

MNIST -15 98.78 93.21 87.12 

Fashion MNIST 0 91.57 90.34 84.76 

Fashion MNIST -10 91.57 79.36 70.84 

CIFAR-10 5 78.17 77.12 63.36 

CIFAR-10 -5 78.17 71.29 32.1 

VI. C ONCLUSIONS 

We developed a new framework for model aggregation in wireless distributed learning. By fitting AirComp to this framework, we showed that conventional over-the-air aggrega-tion based on AirComp implicitly considers a mismatched assumption on the local model parameters. Based on this finding, we proposed an alternative aggregation scheme, called GUE , which directly estimates the global model from its noisy observation. Our experimental results depict significant gains, showing close-to-ideal learning performance with GUE at low SNR s. The results of this study suggest that over-the-air aggregation can be effectively addressed at more realistic operating points, e.g. , at significantly lower SNR s, than what earlier studies have reported. REFERENCES [1] H. B. McMahan, E. Moore, D. Ramage, S. Hampson, and B. A. y Arcas, “Communication-efficient learning of deep networks from decentralized data,” in Proc. 20th Int. Conf. Artif. Intell. Statist. (AISTATS) , 2017, pp. 1273–1282. [2] M. Arbaoui et al. , “Federated learning survey: A multi-level taxonomy of FL research areas,” ACM Trans. Intell. Syst. Technol. , pp. 1–41, 2024. [3] X. Li, K. Huang, W. Yang, S. Wang, and Z. Zhang, “On the convergence of FedAvg on Non-IID data,” in Proc. Int. Conf. Learn. Represent. (ICLR) , 2020. [4] T. Gafni, N. Shlezinger, K. Cohen, Y. C. Eldar, and H. V. Poor, “Federated learning: A signal processing perspective,” IEEE Signal Process. Mag. ,vol. 39, no. 3, pp. 14–41, 2022. [5] K. Yang, T. Jiang, Y. Shi, and Z. Ding, “Federated learning via over-the-air computation,” IEEE Trans. Wireless Commun. , vol. 19, no. 3, pp. 2022–2035, 2020. [6] B. Nazer and M. Gastpar, “Computation over multiple-access channels,” 

IEEE Trans. Inf. Theory , vol. 53, no. 10, pp. 3498–3516, 2007. [7] X. Cao, G. Zhu, J. Xu, and S. Cui, “Transmission power control for over-the-air federated averaging at network edge,” IEEE J. Sel. Areas Commun. , vol. 40, no. 5, pp. 1571–1586, 2022. [8] J. Mao, H. Yang, P. Qiu, J. Liu, and A. Yener, “CHARLES: Channel-quality-adaptive over-the-air federated learning over wireless networks,” in Proc. IEEE 23rd Int. Workshop Signal Process. Adv. Wireless Commun. (SPAWC) , 2022, pp. 1–5. [9] T. Sery, N. Shlezinger, K. Cohen, and Y. C. Eldar, “Over-the-air federated learning from heterogeneous data,” IEEE Trans. Signal Process. , vol. 69, pp. 3796–3811, 2021. [10] S. M. Azimi-Abarghouyi and L. Tassiulas, “Over-the-air federated learning via weighted aggregation,” IEEE Trans. Wireless Commun. ,vol. 23, no. 12, pp. 18 240–18 253, 2024. [11] S. M. Azimi-Abarghouyi and L. R. Varshney, “Compute-update federated learning: A lattice coding approach,” IEEE Trans. Signal Process. , vol. 72, pp. 5213–5227, 2024. [12] C.-Z. Lee, L. P. Barnes, and A. Özgür, “Lower bounds for over-the-air statistical estimation,” in Proc. IEEE Int. Symp. Inf. Theory (ISIT) , 2021, pp. 2358–2363. [13] C.-Z. Lee, L. P. Barnes, and A. Özgür, “Over-the-air statistical estimation,” 

IEEE J. Sel. Areas Commun. , vol. 40, no. 2, pp. 548–561, 2022. [14] C. Park, S. Lee, and N. Lee, “Bayesian AirComp with sign-alignment precoding for wireless federated learning,” in Proc. IEEE Global Commun. Conf. (GLOBECOM) , 2021, pp. 1–6. [15] D. Tse and P. Viswanath, Fundamentals of Wireless Communication .Cambridge, U.K.: Cambridge University Press, 2005. [16] S. Mandt, M. D. Hoffman, and D. M. Blei, “Stochastic gradient descent as approximate Bayesian inference,” J. Mach. Learn. Res. , vol. 18, no. 134, pp. 1–35, 2017. [17] P. Virtanen et al. , “Scipy 1.0: Fundamental algorithms for scientific computing in python,” Nat. Methods , vol. 17, no. 3, pp. 261–272, 2020. [18] C. L. Lawson and R. J. Hanson, Solving Least Squares Problems .Philadelphia, PA: SIAM, 1995.