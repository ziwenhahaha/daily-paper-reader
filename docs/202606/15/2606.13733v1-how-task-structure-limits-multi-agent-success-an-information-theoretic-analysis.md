---
title: "How Task Structure Limits Multi-Agent Success: An Information-Theoretic Analysis"
authors: "Shi Pan, Ming Luo"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13733v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体任务约束与成功概率的信息论分析
tldr: 多智能体系统通过协作提升性能，但受限于信息处理能力和任务结构。核心方法提出基于任务约束图的信息论分析，定义最小割成本Cmin量化信息瓶颈。证明成功概率随Cmin指数衰减，该界适用于开放和封闭系统。合成实验和SWE-bench验证表明，当Cmin高时，重组任务优于扩展智能体或通信。本研究为多智能体系统设计提供理论指导。
source: arxiv
selection_source: fresh_fetch
motivation: 多智能体系统虽能协作，但任务约束结构和通信限制导致性能瓶颈，需理论解释。
method: 基于信息论，通过任务约束图的最小割成本Cmin分析信息瓶颈，建立成功概率衰减界。
result: 在合成数据和SWE-bench上验证，成功概率随Cmin指数下降，任务重组更有效。
conclusion: 设计多智能体系统时应结合任务约束，当Cmin高时优先重组任务而非扩展资源。
---

## Abstract
Multi-agent systems (MAS) were expected to overcome the limitation of single-agent systems (SAS) through collaboration. However, under typicality conditions on the task's constraint graph and bounded inter-agent communication, we prove that the success probability of a MAS is closely tied to the connectivity of task constraints, where each agent has limited information-processing capacity. Specifically, the success probability decays exponentially with an information bottleneck that emerges from partitioning the task's constraint graph among agents. We define this quantity as the \emph{minimum cut cost} $C_{\min}$ of the potential constraint graph of each task. This information-theoretic bound applies to both open systems with external feedback and closed systems without. We validate our theory on both synthetic experiments and real-world empirical data from SWE-bench submissions. From our framework, effective MAS design should incorporate task-inherent constraints alongside engineering optimization, and when $\Cmin$ is high, practitioners should restructure tasks rather than simply scaling agents or communication.

---

## 论文详细总结（自动生成）

# 论文总结：任务结构如何限制多智能体系统的成功——基于信息论的分析

## 1. 核心问题与整体含义

### 研究动机
- 多智能体系统（MAS）被认为可通过协作克服单智能体系统（SAS）的局限，但实践中MAS常常在复杂任务上表现不佳，甚至不如单智能体基线。
- 现有研究多从协调开销、错误传播、角色冗余等角度解释现象，缺乏统一的理论框架来说明MAS何时、为何必然失败。

### 核心问题
- **任务结构**（而非系统配置）决定了MAS成功的根本上限。
- 当任务约束跨越多个组件、需要跨边界信息流时，即使引入更强的集中式规划器也无法消除这些依赖。

## 2. 论文提出的方法论

### 核心思想
- 将任务表示为**约束图** $G_C = (V, E)$，其中节点为变量，边为约束关系，每条边携带潜在函数 $\phi_e$ 及其熵 $H(\phi_e)$。
- 将约束图划分给不同智能体时，跨分区的边形成**割边集** $E_{cut}$，造成信息损失。
- 定义**最小割成本** $C_{min} = \min_P \sum_{e \in E_{cut}(P)} H(\phi_e)$ 量化信息瓶颈。

### 关键技术细节

#### 信息间隙守恒引理（Lemma 4.2）
$$H(\tau^* | Internal) \geq c_1 \cdot C_{min}$$
- 证明使用数据处理不等式和割集界方法。
- 表明信息间隙由任务结构决定，与系统设计无关。

#### 规模界（Theorem 4.3）
$$P(success) = O(2^{-\Omega(C_{min} - H_{sol})})$$
- 在典型性条件下，成功概率随最小割成本指数衰减。
- $H_{sol}$ 为解空间熵，多解情况可降低难度。

#### 直径界（Theorem 4.5）
$$P(success) \leq O(2^{-\Omega(D \cdot (1-\eta) \cdot \bar{H}_e)})$$
- 基于强数据处理不等式（SDPI）描述信息沿路径的指数衰减。
- 适用于稀疏图、大直径场景（如路径图）。

#### 信息预算权衡（Theorem 4.7）
- **封闭系统**：$P(success) \leq O(2^{-c \cdot C_{min} + H_{sol}})$，与通信量 $M$ 无关。
- **开放系统**：$P(success) \leq O(2^{-\max(0, c \cdot C_{min} - F - R - H_{sol})})$。
- 发现**相变阈值**：$F + R = c \cdot C_{min} - H_{sol}$。

### 典型性条件
- **T1/T1'**：割边约束携带非冗余信息，无隐藏代数依赖。
- **T2**：排除异常集中（无"后门"配置）。
- **T3'**：熵集中在期望附近。

## 3. 实验设计

### 合成实验
- **数据集/场景**：图着色问题，测试路径、环、星形、网格、完全图、二分图等结构。
- **参数**：节点数 $n \in \{6,8,10,12,16\}$，颜色数 $k \in \{3,4,5\}$，智能体数 $m \in \{2,3,4\}$。
- **验证目标**：
  - 规模界：信息间隙 $\Delta H$ 与 $C_{min}$ 的线性关系（理论 $\Delta H = 0.585 \cdot C_{min}$）。
  - 通信守恒：封闭系统中通信量对成功率无影响。
  - 相变：开放系统中成功率在临界反馈量处突变。

### 实证实验（SWE-bench）
- **数据集**：SWE-bench Lite，300个软件工程任务。
- **系统规模**：46个智能体系统（18个MAS，18个单智能体，10个非智能体基线），共14,891条结果记录。
- **验证目标**：检验4个定性预测：
  - (P1) $C_{min}$ 与成功率负相关（$r = -0.33$）。
  - (P2) 高 $C_{min}$ 下MAS无优势（$-2.3\%$）。
  - (P3) 指数衰减（$R^2 = 0.11$）。
  - (P4) 预算阈值效应（$\rho = -0.70$）。

## 4. 资源与算力

- **论文未明确说明GPU型号、数量或训练时长**。
- 合成实验使用MockAgent（满足假设的模拟智能体），计算开销主要来自图结构验证和蒙特卡洛采样（每配置200-1000次试验）。
- SWE-bench实验为后验评估，利用公开排行榜数据，无需额外训练。

## 5. 实验数量与充分性

### 合成实验
- **信息间隙验证**：3种 $C_{min}$ 配置 × 40次试验。
- **图结构验证**：6种图类型 × 多种参数组合，全部公式精确匹配。
- **常数稳定性**：13种配置，$\kappa = 0.98 \pm 0.03$。
- **开放/封闭系统**：各4种通信/反馈配置 × 200次试验。

### 实证实验
- **SWE-bench**：46个系统 × 300个任务，统计显著性检验（$p < 0.05$）。
- **鲁棒性检查**：多粒度（行、语句、表达式、Token）验证。

### 充分性与客观性
- **优点**：合成实验在理想假设下精确验证理论公式；SWE-bench覆盖真实软件工程任务。
- **局限**：SWE-bench效应量为中等（$R^2 = 0.11$），可能因典型性条件未完全满足（如代码存在"后门"模式）；实验主要在二划分（$m=2$）下进行。

## 6. 主要结论与发现

### 理论贡献
1. **信息间隙守恒**：任务划分产生的信息瓶颈由 $C_{min}$ 决定，与系统配置无关。
2. **成功概率指数衰减**：$P(success) = O(2^{-\Omega(C_{min})})$，在封闭系统中无论增加多少通信都无法弥补。
3. **相变现象**：开放系统中，外部反馈超过阈值 $F + R \geq c \cdot C_{min} - H_{sol}$ 时，成功率急剧上升。
4. **通信无效性**：封闭系统中内部通信 $M$ 不出现在成功概率界中，仅重新分配信息而不创造新信息。

### 实践指导
- 当 $C_{min}$ 高时，应**重组任务结构**而非简单扩展智能体数量或通信带宽。
- 引入外部反馈机制（如执行-测试循环）可有效突破信息瓶颈。

## 7. 优点

1. **理论严谨性**：基于信息论建立了完整的界体系，涵盖封闭/开放系统，公式可追溯至数据处理不等式。
2. **验证扎实**：合成实验达到 $R^2 = 1.0$ 的精确匹配，实证实验在真实数据上验证方向性预测。
3. **实践导向**：提供具体的设计建议（任务重组 vs. 资源扩展），可直接指导工程实践。
4. **条件明确**：典型性条件（T1/T1'/T2/T3'）清晰标注适用范围，便于后续研究检验。
5. **互补性界**：三个定理针对不同图结构提供最优约束（稠密图用Theorem 1，稀疏大直径图用Theorem 2，开放系统用Theorem 3）。

## 8. 不足与局限

1. **典型性条件验证不完整**：
   - T2（无异常集中）未在代码任务上验证，可能存在绕过熵预测的"后门"配置。
   - T1/T1' 的验证率在不同图结构上差异大（Cycle 30%，Star 0%）。
2. **划分限制**：实证实验仅考虑二划分（$m=2$），$m > 2$ 时的NP难最小割问题未经验证。
3. **效应量中等**：SWE-bench上 $R^2 = 0.11$ 表明 $C_{min}$ 仅解释成功率变化的11%，其他因素（代码风格、智能体架构）可能同等重要。
4. **算力透明度**：未公开GPU使用、训练时长等资源信息，难以评估复现难度。
5. **条件紧性依赖未证猜想**：紧性证明依赖随机k-SAT相变猜想，该猜想尚未严格证明。
6. **智能体行为假设**：MockAgent严格满足假设，真实LLM智能体可能通过共享训练数据产生隐式协调，违反T1条件。

（完）
