---
title: "Safe Reinforcement Learning of Autonomous Highway Driving: A Unified Framework for Safety and Efficiency"
authors: "Chufei Yan, Zhihao Cui, Yiyan Lv, Taojie Chen, Ning Bian, Yulei Wang"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14609v1"
tags: ["query:rl"]
score: 9.0
evidence: 自主高速公路驾驶的安全强化学习框架
tldr: MoE-RM-SRL统一安全距离、奖励机与MoE实现自动驾驶安全深度强化学习。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14609v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 314, \"height\": 472}, {\"url\": \"assets/figures/arxiv/2606.14609v1/fig-002.webp\", \"caption\": \"\", \"page\": 13, \"index\": 2, \"width\": 425, \"height\": 434}]"
motivation: 自主高速公路驾驶的安全强化学习框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Deep reinforcement learning (DRL) offers a compelling route to decision-making for advanced autonomous vehicles (AVs), yet its trial-and-error nature makes it difficult to guarantee safety during training and to achieve both safety and efficiency at deployment. We propose a unified safe reinforcement learning (SRL) framework that integrates safe distance (SD), reward machines (RM), and mixture-of-experts (MoE), termed MoE-RM-SRL. For deployment, SD and RM jointly shape a rule-aware reward that encodes highway traffic regulations and stage-wise objectives, enabling safe and reliable behavior without sacrificing efficiency. For training, we introduce a sparsely gated MoE layer comprising up to 11 deep Q-networks (DQNs); an SD-based gating rule activates a minimal set of experts for lane-keeping and lane-changing, mitigating the instability, discontinuities, and impulsive transients commonly induced by switching between heterogeneous controllers (e.g., MPC/rule-based modules and learned policies). We implement the proposed architecture in CARLA and integrate it with a 6-DoF driver-in-the-loop virtual-reality (DiL-VR) platform. Experiments in stochastic two-lane traffic show that MoE-RM-SRL substantially improves safety and efficiency over state-of-the-art baselines, and the framework naturally extends to multi-lane driving as well as on-ramp merging and exiting scenarios.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在高速公路自动驾驶（AV）场景中，如何在保证安全的前提下，利用深度强化学习（DRL）实现高效决策？
- **研究背景**：
  - 传统规则/基于模型的方法依赖手工特征与启发式规则，在复杂动态交通中泛化能力差。
  - DRL 具有自学习能力，但其“试错”特性在安全关键系统中容易导致训练阶段和部署阶段的安全冲突。
  - 现有安全强化学习（SRL）方法多基于软约束（惩罚项）或硬约束（切换策略），仍存在约束违规、控制器切换不稳定、奖励设计困难等问题。
- **研究动机**：将 **安全距离（Safe Distance, SD）**、**奖励机（Reward Machine, RM）** 与 **稀疏门控混合专家（Mixture-of-Experts, MoE）** 三者统一，提出 **MoE‑RM‑SRL**，实现训练过程安全约束严格、部署阶段兼顾安全与效率的端到端决策框架。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

### 2.1 核心思想
- **安全距离（SD）**：基于两车制动模型（考虑人类反应时间 δt、最大减速度等）计算前后车的最小安全间隙，形成 **RSV_i** 指标，用于判断当前车辆是否满足安全距离约束。
- **奖励机（RM）**：利用有限状态机对高速公路任务（如车道保持、换道）进行**阶段分解**，并在状态转移时输出**规则感知的奖励**，实现任务结构的显式表达与信用分配。
- **稀疏门控 MoE**：在每一决策步，将 **11 个 Deep Q‑Network（DQN）专家** 按层级组织（1+4+6），通过 **SD‑based gating** 逐层筛选出安全且 Q 值最大的动作，避免多控制器切换带来的不连续与冲击。
- **映射函数（ID/Rule/Action/Control Mapping）**：将两车道 RM 扩展到多车道、匝道汇入/驶出等场景，保持算法统一性与可复用性。

### 2.2 关键技术细节（文字概述）

| 模块 | 关键内容 |
|------|----------|
| **状态定义** | 采用 Frenet 坐标系；车辆状态向量含 EV 与 4 个周边车辆（前后左右），共 25 维；加入车道标识 `CID`、中心线阈值 `m_L`、目标车道 `TID`、期望速度 `v*_EV,x`，构成 29 维 DQN 输入 |
| **安全距离公式** | 前后车间距 `dEV,SV_i`、反应位移 `ΔxEV,δt`、残余速度 `vSV_i,Res`、最小停车距离 `dEV,SD`、安全间隙 `dSV_i,SD3`（详见原文式(13)–(29)）；RSV_i ∈{0,1} 判别安全状态 |
| **奖励机状态** | `u1`（车道保持且安全）、`u2`（车道保持且不安全）、`u3`（换道且安全）、`u4`（换道且不安全），奖励 `rRM` 按 `vEV,x / v*_EV,x` 计分 |
| **映射函数** | **ID Mapping**：多车道拆为两车道子问题；匝道按纵向弧长 `x` 分区（汇入/驶出/稳定区）并重新映射车道编号。<br>**Rule Mapping**：引入 `m*_L`（车辆四角点最近距离）判别中心线邻近，`R*_SV_i` 对安全指标进行组合修正。<br>**Action Mapping**：动作集简化为 `{Faster, Idle, Slower, LaneChange}`。<br>**Control Mapping**：基于 `m*_L` 与 `R*_SV_i` 动态决定目标车道 `T*_ID`（式(47)–(48)），直接输出给 CARLA 运动控制。 |
| **稀疏门控 MoE** | 11 位专家分为三层：<br>第1层（1专家）输出全动作集 `A*`；<br>第2层（4专家）分别剔除 `{Faster}`、`{Idle}`、`{Slower}`、`{LaneChange}`；<br>第3层（6专家）剔除两两动作的组合。<br>依据 **Table 1（安全掩码）** 逐层检查动作安全性，首个满足安全的专家被激活。 |
| **训练过程** | 采用 DQN + Experience Replay + Target Network；每次动作被记录到对应专家的回放缓冲区；采用 ε‑greedy 探索（ε 在训练期间线性衰减）；每 `nt` 步同步一次目标网络参数。 |

### 2.3 伪代码概述（文字版）

1. **初始化**：创建 11 个 DQN（各自独立权重），初始化回放缓冲区与环境状态 `EA`（含 `S*`、`R*_SV`、`u*`）。
2. **每时间步**：
   - 根据 `R*_SV` 查询 Table 1 获取安全动作集合 `ASafe`。
   - 所有专家独立计算 Q 值并选出候选动作 `a*_i,j`。
   - 若随机数 `< ε`，调用 **ExG**（随机探索）按层级寻找安全动作；否则调用 **PoG**（贪心）按 Q 值顺序寻找安全动作。
   - 执行选中的动作 `a*`，通过 CARLA 低层控制器更新车辆运动。
   - 根据 RM 状态计算奖励 `r`，将经验 `(EA, a*, E'A, r)` 存入对应专家的回放缓冲区。
   - 从回放缓冲区采样，更新对应专家的 TD 目标 `y`，若达到目标网络更新阈值则同步目标网络。
3. **终止条件**：达到最大 episode 步数或碰撞标记。

## 3. 实验设计：场景、数据集与对比方法

### 3.1 实验平台
- **仿真器**：CARLA（Town04 两车道、RoadRunner 构建的多车道/匝道场景）。
- **实车模拟**：6‑DoF 驾驶员‑在环虚拟现实（DiL‑VR）平台，配备 VR 头显、力反馈方向盘、油门踏板。

### 3.2 场景与流量设置
| 场景 | 说明 |
|------|------|
| **两车道高速** | 主线两车道，车流量分 A–F 六档（8–44 vph），每辆车随机选择 **Aggressive / Normal / Cautious** 三种驾驶风格。 |
| **多车道高速** | 三车道，左/中、右/中分别建模为两车道子问题；流量 Level C（24 vph），驾驶风格同上。 |
| **匝道汇入** | 入口匝道 → 合并区 → 稳定区；成功进入稳定区记为任务完成。 |
| **匝道驶出** | 主线 → 分离区 → 出口匝道；成功进入匝道记为任务完成。 |
| **对抗环境** | 相邻车道车辆在距离触发点 `<|dtrig−dSV_i|` 时以概率 ABP（19%）强制换道，制造罕见长尾冲突。 |

### 3.3 对比基线（7 项）
1. **Manual**：10 名受试驾驶员在 DiL‑VR 上操作，取平均性能。
2. **RL**：标准 DQN，仅靠速度/碰撞惩罚奖励。
3. **SR‑RL**：安全动作检查（基于 SD）在动作空间中顺序检验安全性。
4. **RM‑RL**：奖励机 + SD，未使用 MoE。
5. **MP‑RL**：SR‑RL 加上本文提出的 ID/Rule/Action/Control Mapping（用于多车道/匝道）。
6. **LM‑RL**：Lagrangian‑约束 DQN（广泛使用的安全‑约束 RL 基线）。
7. **ET‑RL**：专家接管（IDM）用于不安全时强制切换。

### 3.4 评估指标
- **部署阶段**：碰撞‑自由率 `Cr`、平均车速、部署奖励 `Rr`。
- **训练阶段**：训练‑碰撞‑自由率 `Tc`、训练奖励峰值 `Tr`、奖励上升时间。
- **任务成功率**（匝道）：`Rs`。

## 4. 资源与算力

- **硬件**：Intel Core U9‑285K CPU + NVIDIA GeForce RTX 4090 GPU（单卡）。
- **训练时长**：总计 5 × 10⁵ 步（两车道、匝道等均使用相同预算）。
- **批大小**：256，折扣因子 γ = 0.99，学习率 1 × 10⁻⁴。
- **说明**：文中未明确说明训练所需 Wall‑clock 时间或额外的计算节点，算力信息相对简洁。

## 5. 实验数量与充分性

- **实验组数**：
  1. **两车道密度实验**：6 种流量等级（A–F）× 100 episodes/方法 → 600 episodes/方法。
  2. **多车道、匝道汇入/驶出**：每场景 10 次独立运行，每次 100 episodes → 1 000 episodes/场景。
  3. **对抗实验**：两车道、汇入、驶出分别对应 100 episodes/方法（密度 Level C）。
  4. **消融实验**：4 项变体（Full、w/o MAP、w/o MoE、w/o RM）在对抗两车道下各 100 episodes。
- **实验总量**：约 5 000 episodes（不含手动实验）。
- **充分性评估**：
  - **广度**：覆盖两车道、多车道、匝道汇入/驶出、对抗四大类场景；流量等级与驾驶风格多样化。
  - **深度**：包含完整训练‑部署对比、基线横向对比、针对性消融。
  - **公平性**：所有 RL 方法使用相同网络结构、相同超参数，仅在安全机制或奖励设计上有差异；Manual 基线使用受试者平均，保证对比客观。
  - **不足**：对抗场景仅使用固定 ABP = 19% 与固定触发距离，未进行更大范围的随机性或极端天气验证。

## 6. 论文的主要结论与发现

1. **安全与效率双优**：MoE‑RM‑SRL 在两车道、多车道以及匝道汇入/驶出任务中均实现 **100% 训练‑碰撞‑自由率**，部署阶段碰撞率在对抗环境下仅约 13.4%（普通环境 0%），平均车速保持在 6–7 m/s，显著高于大多数基线。
2. **训练收敛更快**：相比纯 RL 与 LM‑RL，奖励上升时间（rise time）显著缩短，说明 RM 结构化奖励与 MoE 稀疏门控共同加速了信用分配与策略更新。
3. **对抗鲁棒性**：在强制换道对抗场景下，MoE‑RM‑SRL 仍保持 92.9%（两车道）–95.4%（汇入）–92.7%（驶出）的部署碰撞‑自由率，优于所有基线（最高仅 87.5%）。
4. **映射模块的价值**：消融实验显示，移除 Mapping 模块后训练安全率跌至 0%（完全失效），说明 ID/Rule/Action/Control 映射是实现多车道与匝道任务的关键桥梁。
5. **MoE 与 RM 的互补**：单独移除 MoE 仍能实现 38.8% 部署碰撞‑自由率，但效率大幅下降；单独移除 RM 则训练安全率下降至 98.3%（仍有部分不安全），证实两者分别负责安全过滤与高效学习。

## 7. 优点

- **统一框架**：将安全距离、奖励机与稀疏门控 MoE 融合，形成从奖励设计、状态抽象到动作筛选的端到端流水线，无需手工规则或额外安全层。
- **稀疏门控的安全过滤**：通过层级专家与安全掩码 Table 1，实现“**先安全后高效**”的决策机制，避免了多控制器切换导致的冲击与不连续。
- **映射可扩展**：ID/Rule/Action/Control 四大映射函数把两车道模型自然迁移至多车道、匝道场景，仅需少量状态分区与阈值设定。
- **实验全面**：覆盖 6 种流量等级、3 种驾驶风格、4 类场景（两车道/多车道/汇入/驶出）以及对抗扰动，基线 7 项，实验规模与对比维度均较充分。
- **实车模拟平台**：DiL‑VR 与 CARLA 的闭环集成提供了从仿真到人‑在环评估的桥梁，提升了结果的可信度与可迁移性。

## 8. 不足与局限

- **感知层缺失**：实验直接使用 CARLA 地面真值获取车辆位置，未使用雷达/激光雷达或视觉感知系统，真实道路上感知误差、遮挡与传感器噪声未被验证。
- **算力信息不全**：仅给出 GPU 型号（RTX 4090）和训练步数，未披露实际 Wall‑clock 训练时间、内存占用、分布式训练情况，难以评估计算成本。
- **对抗场景单一**：对抗行为仅限固定概率的强制换道，未覆盖逆向行驶、异常加减速、紧急刹车等更极端的异常模式。
- **可扩展性局限**：映射函数依赖人工分区（如匝道纵向坐标 `x`），对非结构化道路或城市交叉口可能需要重新设计，缺乏通用的跨域迁移能力说明。
- **奖励函数依赖**：RM 中的奖励计算仍然基于手动设定的 `v*_EV,x` 与 `dACC` 阈值，若阈值选取不当仍可能影响效率上限。
- **长期安全保证缺失**：虽然训练阶段安全率接近 100%，但未提供形式化安全验证（如 Hamilton‑Jacobi 可达性分析），对极罕见的长尾事件缺乏理论保障。

（完）
