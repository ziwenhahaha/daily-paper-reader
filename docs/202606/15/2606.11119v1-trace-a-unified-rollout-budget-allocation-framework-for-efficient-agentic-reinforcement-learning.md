---
title: "TRACE: A Unified Rollout Budget Allocation Framework for Efficient Agentic Reinforcement Learning"
authors: "Heming Zou, Qi Wang, Yun Qu, Yuhang Jiang, Lizhou Cai, Yixiu Mao, Ru Peng, Xin Xu, Weijie Liu, Kai Yang, Saiyong Yang, Xiangyang Ji"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11119v1"
tags: ["query:rl"]
score: 9.0
evidence: 智能体强化学习框架，包含 rollout 预算分配
tldr: TRACE通过建模前缀级信息量优化多轮智能体RL的rollout分配。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11119v1/fig-001.webp\", \"caption\": \"\", \"page\": 1, \"index\": 1, \"width\": 2362, \"height\": 685}, {\"url\": \"assets/figures/arxiv/2606.11119v1/fig-002.webp\", \"caption\": \"\", \"page\": 6, \"index\": 2, \"width\": 2096, \"height\": 1196}]"
motivation: 智能体强化学习框架，包含 rollout 预算分配。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Reinforcement learning with verifiable rewards (RLVR) is a promising approach for enhancing reasoning and agentic behavior in large language models. However, rollout-intensive policy optimization is often limited by insufficient reward contrast, arising when overly simple or complex prompts generate low-variance feedback and when outcome-only rewards assign the same terminal assessment to every decision in a multi-turn rollout. Past efforts have focused on allocating available rollout resources to promising prompts, yet they only leverage sample informativeness at the prompt level and neglect variation in prefix-level informativeness across turns within the same rollout. This work targets multi-turn agentic RL by modeling each ReAct-style thought-action-observation turn as a semantically distinct node, allowing budget allocation to extend from prompt roots to turn-level prefixes with further continuations, which naturally forms tree-structured rollouts. We introduce Tree Rollout Allocation for Contrastive Exploration (TRACE), a unified rollout allocation framework that enhances reward contrast within a fixed sampling budget. Technically, TRACE allocates rollout budget to both prompt roots and intermediate prefixes that are most likely to yield mixed terminal rewards. A shared generalizable predictor estimates conditional success probability at these anchors from prefix histories to guide this allocation. The resulting adaptive tree structure enriches outcome-only feedback and amplifies the policy-update signal. Empirically, TRACE achieves competitive performance and efficiency gains on typical agentic benchmarks, e.g., improving Qwen3-14B Multi-Hop QA average accuracy by 2.8 points over competitive baselines at equal sampling cost.

---

## 论文详细总结（自动生成）

# TRACE 论文详细总结

## 1. 论文的核心问题与整体含义

**研究动机和背景：**
- **核心问题**：强化学习与可验证奖励（RLVR）在增强大语言模型（LLM）的推理和智能体行为方面前景广阔，但 rollout 密集的策略优化常受限于**奖励对比度不足**。
- **问题来源**：
  - **提示层面**：过于简单或复杂的提示产生低方差的终态奖励反馈
  - **轨迹层面**：只使用终态奖励会导致同一 rollout 中每个决策收到相同的评估信号，造成信用分配稀疏
- **已有方法的局限**：过去的工作（如提示选择和 rollout 分配）只在**提示根层面**分配资源，忽视了同一 rollout 内不同轮次间的前缀信息量差异。

## 2. 论文提出的方法论

**核心思想：**
- 将每个 ReAct 风格的 thought-action-observation 轮次建模为语义不同的节点，使预算分配从提示根延伸到轮级前缀，形成**树结构的 rollout**。
- 核心原则是**混合奖励对比度构建**：将 rollout 预算分配给最可能产生成功和失败混合终态的锚点。

**关键技术细节：**

| 组件 | 说明 |
|------|------|
| **全局根分配** | 在候选 prompt 池上分配根 rollout 预算，决定跳过哪些提示以及每个活跃提示获得多少根 rollout |
| **局部前缀扩展** | 在已访问的前缀上分配继续 rollout 预算，分配给最可能产生反事实继续的锚点 |
| **共享预测器** | 使用轻量级 Qwen3-0.6B 模型估计条件成功概率 `Ṽψ(Ht)`，通过树回溯目标在线更新 |

**核心公式：**

- **根节点价值函数**（式12）：
  - `Vroot(xi, m) = 1 - vi^m - (1 - vi)^m`
  - 其中 `vi = Ṽψ(xi)` 是预测的根成功概率，m 是 rollout 数量
  - 该公式计算 m 个根 rollout 中同时包含成功和失败结果的预测概率

- **前缀节点价值函数**（式15）：
  - `Vpref(i,j,t,k) = 1 - [ri,j·Ṽψ(Hi,j,t) + (1-ri,j)·(1-Ṽψ(Hi,j,t))]^k`
  - 其中 `ri,j` 是该前缀下方已观察到的终态奖励
  - 衡量 k 个新继续 rollout 中至少有一个翻转观察结果的预测概率

**算法流程（Algorithm 1）：**
1. **Stage 1（全局根分配）**：对候选 prompt 池中的每个提示估计成功概率，通过动态规划求解预算约束问题（式13）获得根 rollout 计数 `{mi}`
2. **生成 bare rollout**：对每个活跃提示 xi 生成 mi 个完整轨迹并收集终态奖励
3. **构建前缀锚点集合** `Ai`：识别所有非终态前缀 `(j,t)`
4. **Stage 2（局部前缀扩展）**：对每个活跃提示，分配 `mi·N` 个继续 rollout 预算到其前缀锚点，求解局部优化问题（式16）
5. **更新预测器和策略**：计算递归树回溯目标 `b̂V`，更新预测器 MSE 损失（式19），使用树感知策略优化器（如 TreeRPO 或 Tree-GRPO）更新策略

## 3. 实验设计

**数据集与场景：**

| 任务类型 | 训练数据 | 评估基准 |
|----------|----------|----------|
| **数学推理** | DeepScaler（40,315 题） | AIME24, AMC23, MATH500, MinervaMath, OlympiadBench（ID），MMLU-Pro, ARC-c, GPQA-diamond（OOD） |
| **多跳 QA** | HotpotQA 训练集 | HotpotQA, 2WikiMultiHopQA, MuSiQue, Bamboogle |
| **函数调用** | BFCL v4 多轮 split（80% 训练） | BFCL v4 base, long-context, missing-function, missing-parameter |

**模型配置：**
- 主要策略模型：Qwen3-8B、Qwen3-14B
- 额外验证：Llama-3.2-3B-Instruct
- 预测器：Qwen3-0.6B（共享用于根和前缀评分）

**对比基线（4 个）：**

| 方法 | 说明 |
|------|------|
| **ReAct** | 基础模型在相同多轮智能体脚手架下评估 |
| **GRPO** | 在提示层面随机采样的群体相对策略优化 |
| **PCL** | 基于难度的纯 CoT 提示选择基线 |
| **TreePO** | 添加树结构 rollout 和树感知更新，但继续选择随机 |

**评估指标：**
- 数学推理：最终答案准确率
- 多跳 QA：exact match + token-level F1（阈值 ≥ 0.3 给部分信用）
- 函数调用：官方 BFCL 成功率

## 4. 资源与算力

**训练配置：**
- **主实验**：2 个节点，每个节点 8 GPU
- **框架**：rLLM/verl + 异步 vLLM rollout workers
- **优化器**：Adam（学习率 1e-6），FSDP + 参数和优化器卸载
- **序列长度**：
  - 数学推理：prompt 2048，response 14336，最多 5 轮
  - 多跳 QA：prompt 2048，response 6144，最多 5 轮
  - 函数调用：prompt 12800，response 8192，最多 40 轮

**采样温度：**
- 多跳 QA：1.0，top-p = 1.0
- 数学推理：0.6，top-p = 0.95
- 函数调用：0.9，top-p = 1.0

**计算开销分析（Figure 10）：**
- Qwen3-8B：LLM rollout 28.6%，策略更新 66.5%，预测器评分 1.9%，预测器更新 1.3%
- Qwen3-14B：LLM rollout 21.2%，策略更新 75.4%，预测器评分 1.4%，预测器更新 0.9%

## 5. 实验数量与充分性

**主要实验覆盖：**
- 6 个训练曲线图（Figure 4），覆盖 3 个任务 × 2 个模型规模
- 6 个有效比率图（Figure 5），验证 rollout 信息量的提升
- 6 个提示级 Spearman 相关图（Figure 6）和 6 个前缀级相关图（Figure 7）
- 详细基准表（Table 4-5）：每个设置 8 个评估基准的最终性能

**消融实验：**
- **分配阶段消融**（Table 1）：验证 Stage 1（根分配）和 Stage 2（前缀扩展）的独立贡献
- **预算兼容性**（Table 2）：测试不同根预算 M 和扩展因子 N 组合（512/1024 × 2/6）

**跨模型验证：**
- Llama-3.2-3B-Instruct 实验（Table 6, Figure 8-9），验证方法在非 Qwen 模型上的可迁移性

**实验充分性评估：**
- ✅ **任务覆盖充分**：涵盖推理、多跳推理、函数调用三个代表性智能体场景
- ✅ **模型规模多样**：8B、14B、3B 三个量级
- ✅ **对比方法全面**：包含基线、提示选择、树结构优化三类基线
- ✅ **消融层次清晰**：从端到端性能到分配阶段再到预算配置
- ⚠️ **预算匹配说明**：论文明确指出所有方法使用相同的 rollout 预算会计方式，确保增益来自分配而非额外样本

## 6. 论文的主要结论与发现

**核心发现：**

| 指标 | 关键数据 |
|------|----------|
| **测试准确率提升** | Qwen3-14B 多跳 QA 平均提升 **2.8 分**；数学推理 Qwen3-8B 从 70.0 提升到 71.1 |
| **有效比率提升** | 数学推理 Qwen3-8B 从 26.8% 提升到 **60.6%**；Qwen3-14B 从 34.7% 提升到 **59.7%** |
| **TreePO 对比** | 即使共享树 rollout 和树感知更新，TRACE 通过学习的分配仍持续优于随机 TreePO |

**理论贡献：**
- 提出了**混合奖励对比度分配**的统一视图，将提示过滤、rollout 计数分配、前缀分支统一为树锚点上的预算决策
- **Proposition 1** 证明前缀信息严格改进组难度预测
- **Proposition 2** 识别前缀不确定性与 Bernoulli 方差 `Vπ_t(1-Vπ_t)` 的等价性
- **Proposition 3** 证明 TRACE 分配在局部梯度能量上优于均匀分配

**分配行为观察（Figure 11-12）：**
- Stage 1：约 65-70% 的候选提示获得 `mi=0`（被跳过），活跃提示通常获得 5-7 个根 rollout
- Stage 2：预算加权前缀位置位于 bare rollout 中间附近，延续预算在较深深度仍有非平凡分配

## 7. 优点

**方法亮点：**
- **统一框架**：将提示过滤、rollout 数量分配、前缀分支整合为一个原则——分配给"对比丰富的锚点"
- **理论支撑**：三个数学命题建立了分配目标与梯度信号、激活概率的严格联系
- **轻量级预测器**：使用 Qwen3-0.6B 模型，额外开销仅 2-3%，可迁移到不同策略模型
- **自适应树结构**：不预设固定树形，而是根据预测的信息量动态决定分支位置和数量
- **正交性设计**：作为分配层独立于下游树感知策略优化器（可配合 TreeRPO、Tree-GRPO 等）

**实验亮点：**
- **预算严格匹配**：所有方法使用相同的 rollout 预算，排除混淆因素
- **全面对比**：基线涵盖无 RL、提示选择、树 rollout 三类方法
- **可解释诊断**：有效比率指标直接衡量"对比构建"的中间目标
- **跨模型验证**：Llama 实验证明框架可迁移性

## 8. 不足与局限

**应用范围限制：**
- 主要针对**终态可验证奖励**设计，对于无明确终态验证的任务可能需要重新审视混合对比目标
- 在**开放-ended 工具使用场景**中，预测器训练依赖树回溯目标，可能在长序列交互中面临目标噪声

**预测器依赖：**
- 分配质量依赖预测器准确性，更强的预测器可能带来更好的预算放置
- 论文使用 vanilla 预测器实例，更强的替代方案仍有探索空间
- 面临**稳定性-可塑性困境**：随着策略改进，树回溯目标非平稳，需使用持续学习技术

**实验覆盖局限：**
- 评估集中在**三类任务**（数学、多跳 QA、函数调用），更复杂的非平稳智能体场景（如自主探索、持续任务）未涉及
- 主要使用 **Qwen3 系列模型**，其他模型家族的泛化性需进一步验证
- 训练步数有限（最多 80 步），长期训练稳定性和收敛性未充分探索

**实现考量：**
- 动态规划求解虽然高效，但在极大候选池或极深树结构时仍需考虑扩展性
- Stage 2 扩展仅使用**一轮展开**，未测试递归多轮树生长的效果

（完）
