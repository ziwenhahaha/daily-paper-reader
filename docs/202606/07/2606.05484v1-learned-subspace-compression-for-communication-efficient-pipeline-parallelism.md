---
title: Learned Subspace Compression for Communication-Efficient Pipeline Parallelism
title_zh: 面向通信高效流水线并行的学习型子空间压缩
authors: "Paul Janson, Edouard Oyallon, Eugene Belilovsky"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05484v1"
tags: ["query:dgnn-nas"]
score: 7.0
evidence: 面向通信高效训练的学习型子空间压缩
tldr: 在低带宽网络上进行大语言模型的流水线并行训练时，阶段间的激活通信成为主要瓶颈。现有固定正交投影压缩方法会导致性能显著下降且需非标准优化。为此，本文提出MAPL方法，通过在Stiefel流形约束下学习低秩正交投影，结合因子化锚点嵌入和残差向量量化，实现了高效的激活压缩与重建，大幅提升了性能与压缩率的权衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05484v1/fig-001.webp\", \"caption\": \"Figure 1: Pareto frontier for compressed pipeline-parallel training. Validation cross-entropy versus communication cost (bytes per token, with the corresponding compression ratio relative to the 2048-byte uncompressed baseline) for a 150M-parameter model trained with P = 4 pipeline stages on DCLM-10B using the Muon optimizer [21] unless otherwise stated. Lower-left is better. We compare our learned projection (“Ours,” green) against uncompressed training (⋆) and two SSN [42] baselines, SSN (AdamW [23] version) and SSN. For all methods, the projection rank r sets the activation size and hence the compression level (r = 64, 128, 256, 512 → 128–1024 bytes/token). Our approach traces the Pareto frontier across all regimes, staying within ≈0.08 cross-entropy of uncompressed training even at 16× compression, whereas both SSN baselines degrade sharply as compression increases.\", \"page\": 2, \"index\": 1, \"width\": 562, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-002.webp\", \"caption\": \"Figure 4: Empirical validation of learned orthogonal projectors for activation compression in pipeline-parallel training. (a) Pairwise mean principal angles (degrees) between learned Stiefel manifold projectors across pipeline stages i and j, computed via arccos(σk(A ⊤ i Aj)). Large off-diagonal angles (up to 72◦) confirm that projectors across non-adjacent stages converge to geometrically distinct subspaces, while near-zero diagonal values verify self-consistency. (b) Frobenius-norm energy of residual activations preserved under projection, ∥XresAp∥2F /∥Xres∥2F , as a function of training step for learned Stiefel (r = 128, red) versus fixed orthogonal (r = 128, blue) projectors. Learned projectors retain approximately 2.2× more residual energy (∼ 0.80 vs. ∼ 0.36), demonstrating superior task-adaptive compression. (c) Cosine similarity preservation between token pairs: cos(xiAp, xjAp) versus cos(xi, xj) evaluated on the full-dimensional representations. A Pearson correlation of r = 0.992 confirms that the learned projection Ap faithfully preserves the pairwise relational geometry of the activation space, closely tracking the identity line (y = x).\", \"page\": 7, \"index\": 2, \"width\": 796, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of MAPL compression at a pipeline stage boundary, repeated across all P − 1 inter-stage boundaries. At each boundary, the token-dependent offset is subtracted from the boundary activation Xbp ∈ Rb×n×d before transmission (red arrow): the first stage subtracts the original token embeddings, while subsequent stages subtract their per-stage factorized anchor embeddings Esmall\", \"page\": 4, \"index\": 3, \"width\": 822, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-004.webp\", \"caption\": \"Table 8: Method-specific hyperparameters for the runs in Table 1. “LR scale” is ηadam/ηµ, the AdamW-to-Muon learning-rate multiplier on the 1D parameters (embeddings, biases, LayerNorms, LM head). The SSN baselines update the shared subspace Uk via Grassmann manifold steps every 500 optimizer steps with a fixed Grassmann learning rate of 0.01. “Compression” values are reported relative to a 2-byte (bf16) per-channel payload at the corresponding hidden dimension; SSN, MAPL, and MAPL+VQ are all rank-matched at r=256. Identical values across the three model scales are denoted “–”.\", \"page\": 19, \"index\": 4, \"width\": 514, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-005.webp\", \"caption\": \"Figure 5: Dynamics of the stable rank, defined as srank(A) = ∥A∥2 F\", \"page\": 17, \"index\": 5, \"width\": 794, \"height\": 299}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-006.webp\", \"caption\": \"Table 1: MAPL closes the performance gap to uncompressed training across all model scales while delivering 4–16× communication compression. Cross entropy validation loss and compression results for 150M, 500M, and 1B parameter models under pipeline-parallel settings with P=4 and P=8 show that both MAPL and its vector-quantized variant consistently match or approach the uncompressed baseline. In contrast, they substantially outperform the SSN baselines in accuracy. Best uncompressed values are marked with bold text\", \"page\": 9, \"index\": 6, \"width\": 830, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-007.webp\", \"caption\": \"Table 2: Zero-shot downstream benchmark accuracy under pipeline-parallel communication compression. Accuracy on HellaSwag, PIQA, ARC-Easy (ARC-E), and ARC-Challenge (ARC-C) for 150M, 500M, and 1B parameter models trained with P=4 and P=8 pipeline stages. MAPL consistently approaches the uncompressed baseline across all benchmarks and scales— averaging within ∼1 point at 500M and 1B—while SSN baselines suffer larger accuracy drops (up to ∼9 average points at 1B scale). The vector-quantized variant (MAPL + VQ) achieves higher compression ratios but at a notable accuracy cost. Averages (Avg) are reported across all four tasks. The best compressed values marked with bold text.\", \"page\": 9, \"index\": 7, \"width\": 834, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-008.webp\", \"caption\": \"Figure 3: Boundary activations exhibit intrinsic low-rank structure across all pipeline stages, with rank-250 truncation retaining ≥99% of activation energy. (a) Singular value spectra of the centered boundary activations Xbp − E[tids] (reshaped to (B · T )× d) for all P − 1 = 7 inter-stage boundaries of a 150M LLaMA model (d = 1024, P = 8) trained with Muon [21] on DCLM [28]. The x-axis indexes singular values in descending order; the y-axis is on a log scale. Each colored line\", \"page\": 5, \"index\": 8, \"width\": 796, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2606.05484v1/fig-009.webp\", \"caption\": \"Table 4: Loss with trained Uk versus identity ablation (Uk = I). Values above ≈ 10.38 (uniform random baseline) indicate worse-than-chance predictions.\", \"page\": 16, \"index\": 9, \"width\": 744, \"height\": 345}]"
motivation: 现有基于固定正交投影的流水线并行激活压缩方法会导致模型性能显著下降，且需要复杂的非标准优化约束。
method: 提出MAPL方法，在Stiefel流形约束下为每个流水线阶段学习可自适应的最优低秩正交投影，并引入因子化锚点嵌入和残差向量量化以实现激活的高效重建与通信。
result: 在150M至1B参数的LLaMA模型上的实验表明，MAPL易于集成，能在实现高压缩比的同时保持极低的性能损失，且性能与压缩的权衡远优于Subspace Networks。
conclusion: MAPL通过将阶段间激活压缩转化为流形约束下的可学习正交投影问题，成功解决了低带宽网络下流水线并行的通信瓶颈。
---

## 摘要
流水线并行使得训练超出单设备内存容量的大型语言模型成为可能，然而在低带宽网络上进行训练时，阶段间的激活通信成为了主要的瓶颈。该领域的近期研究提出使用固定的正交投影来压缩激活。然而，这种方法仍会导致显著的性能退化，并且需要大量非标准的适配操作来约束优化过程。一种自然的替代方案是为每个流水线阶段学习一个低秩投影，但在训练过程中维持这些投影算子所必需的正交性仍然是一个挑战。我们提出了流形感知投影学习（MAPL），这是一种将阶段间压缩视为在显式 Stiefel 流形（正交矩阵）约束下的可学习正交投影的方法。MAPL 没有采用预设的固定全局子空间，而是让每个流水线阶段通过流形约束的最速下降法，发现并持续调整其各自任务最优的压缩子空间。为了在阶段边界处恢复特定于 token 的信号，我们引入了逐阶段的分解锚点嵌入，从而允许以可忽略不计的通信开销进行全秩激活重构。我们进一步表明，可以在投影后结合残差向量量化，并利用流式码本同步协议来分摊字典通信的开销。在参数量从 1.5 亿到 10 亿的 LLaMA 模型上，我们证明了 MAPL 可以轻松应用于现有的流水线中，能够实现高压缩率且仅带来可忽略的性能下降；与 Subspace Networks 相比，MAPL 在性能与压缩率的权衡上取得了极大的提升。

## Abstract
Pipeline parallelism enables training of large language models that exceed single-device memory, yet inter-stage activation communication becomes the dominant bottleneck when trained on low-bandwidth networks. Recent work in this area has proposed using fixed orthogonal projections to compress activations. However, this still results in a significant performance degradation and requires a number of non-standard adaptations to constrain the optimization. A natural alternative is to learn a low rank projection for each pipeline stage, however maintaining the necessary orthogonality of these projectors during training remains a challenge. We present Manifold Aware Projection Learning (MAPL), a method that treats inter-stage compression as a learnable orthogonal projection under explicit Stiefel manifold (orthogonal matrices) constraints. Rather than prescribing a fixed global subspace, MAPL lets each pipeline stage discover and continuously adapt its own task-optimal compression subspace via manifold-constrained steepest descent. To recover token-specific signals at stage boundaries, we introduce per-stage factorized anchor embeddings that allow for full-rank activation reconstruction with negligible communication overhead. We further show that we can incorporate residual vector quantization after projection with a streaming codebook synchronization protocol that amortizes dictionary communication. Across LLaMA models from 150M to 1B parameters we show that MAPL can be easily applied to the existing pipeline and can achieve high compression with neglibile performance degradation with a drastically improved tradeoffs in performance vs. compression compared to Subspace Networks.