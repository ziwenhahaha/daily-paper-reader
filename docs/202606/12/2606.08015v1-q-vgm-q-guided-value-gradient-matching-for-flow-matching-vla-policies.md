---
title: "Q-VGM: Q-Guided Value-Gradient Matching for Flow-Matching VLA Policies"
authors: "Ziqian Wang, Jiayu Sun, Xingjian Mao, Minqian Wang, Yao Mu"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08015v1"
tags: ["query:rl"]
score: 9.0
evidence: "离线强化学习方法,通过Q函数优化微调视觉-语言-动作策略"
tldr: Flow-matching VLA策略微调面临梯度回传不稳定且缺乏动作似然度的双重困难。Q-VGM通过VGG-Flow将critic的一阶梯度转化为去噪时的value-gradient field，无需端到端回传即可利用Q函数改进策略。结合action-sensitive Cal-QL ensemble和经验回放，实现了few-shot初始化后通过自生成rollout数据持续提升的性能突破。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08015v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 2172, \"height\": 724}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-002.webp\", \"caption\": \"\", \"page\": 5, \"index\": 2, \"width\": 1978, \"height\": 1114}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-003.webp\", \"caption\": \"\", \"page\": 8, \"index\": 3, \"width\": 4096, \"height\": 3072}, {\"url\": \"assets/figures/arxiv/2606.08015v1/fig-004.webp\", \"caption\": \"\", \"page\": 8, \"index\": 4, \"width\": 4096, \"height\": 3072}]"
motivation: Flow-matching VLA策略微调需要利用critic梯度信息改进策略，但直接回传梯度不稳定，且flow模型缺乏动作似然度，限制了现有off-policy方法的应用。
method: Q-VGM利用VGG-Flow将value gradient转化为去噪时的value-gradient field，避免不稳定的端到端回传。Critic采用action-sensitive Cal-QL ensemble over compact RLT features，在固定replay buffer上学习。
result: "在LIBERO上成功率从75.0%升至92.5%，RoboTwin 2.0上从76.4%升至87.2%，real-robot任务从40.0%升至67.5%，均超越同backbone、同critic基线方法。"
conclusion: Q-VGM为flow-matching VLA策略提供了一种实用的few-shot初始化后学习范式，可通过自生成rollout数据持续提升性能，无需额外专家监督。
---

## Abstract
We propose Q-Guided Value-Gradient Matching (Q-VGM), an off-policy reinforcement learning (RL) method that tackles a long-standing challenge in fine-tuning flow-matching vision-language-action (VLA) policies: efficiently improving an expressive flow-matching action expert with respect to a learned Q-function. Effective improvement must exploit the first-order (gradient) information of the critic, but this is difficult for flow policies, because directly back-propagating the value through their multi-step denoising process is numerically unstable at VLA scale, while the tractable action likelihoods required by policy-gradient methods are unavailable under iterative denoising. Existing value-based methods either backpropagate through the full denoising chain, use the critic only at test time without updating the policy, or distill critic-improved actions as terminal labels without supervising the velocity field. Q-VGM sidesteps these issues by leveraging VGG-Flow, a value-gradient view of flow alignment in generative modeling that transforms value gradient into a denoising-time value-gradient field rather than an unstable end-to-end objective. This requires no action likelihoods and no backpropagation through the denoising chain, and operates on a fixed replay buffer. The critic is an action-sensitive Cal-QL ensemble over compact RLT features with per-layer action injection. Q-VGM enables a practical few-shot initialization then learn-from-experience paradigm: starting from a few-shot-SFT pi0.5 VLA, the method leverages self-generated rollout data to substantially improve task performance without additional expert supervision. On LIBERO, Q-VGM raises the average success rate from 75.0% to 92.5%; on RoboTwin 2.0, from 76.4% to 87.2%; and on two real-robot tabletop tasks, from 40.0% to 67.5%, outperforming all same-backbone, same-critic baselines across all three settings.

---

## 论文详细总结（自动生成）

# Q-VGM 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **应用场景**：Vision-Language-Action (VLA) 模型在通用机器人控制中展现出巨大潜力，其中 flow-matching 策略因能有效捕捉多模态动作分布而受到青睐
- **核心挑战**：对预训练 flow-matching VLA 进行 RL 微调时面临双重困难：
  1. 直接通过多步去噪链回传梯度在 VLA 规模上数值不稳定
  2. Flow 策略通过迭代去噪生成动作，无法提供 policy-gradient 方法所需的动作似然度
- **现有方法局限**：
  - **Diffusion-QL**：需要回传完整的去噪链，梯度路径长且不稳定
  - **Test-time Q-selection/guidance**：仅在推理时使用 critic，策略本身未更新
  - **Q-guided action distillation**：将 critic 改进的动作作为终端标签，忽视 velocity field 结构
- **核心动机**：需要一种方法既能利用 critic 的一阶（梯度）信息，又避免端到端回传的不稳定性

## 2. 方法论：核心思想、关键技术细节、算法流程

### 核心思想
基于 VGG-Flow 的最优控制视角，将状态空间的价值梯度 ∇AQ(s, A) 转化为去噪时步的速度修正量，通过残差速度匹配微调 flow-matching 动作专家。

### 关键技术细节

**① Action-Sensitive Cal-QL Critic**
- 状态表示：从 VLA 压缩的 RLT readout vector (z_rl ∈ R^2048) 与 proprioception 拼接
- 动作敏感架构：每个 critic head 在每层 hidden layer 重新注入 action chunk，保持对局部动作扰动的敏感性
- 训练方式：离线 chunk-level Cal-QL，使用数据集的 next action chunk 作为 Bellman target，保持完全 off-policy

**② Look-forward Clean-action Estimation**
- 对每个离线状态 s，采样初始噪声 x₀ ∼ N(0, I)
- 通过 K 步 Euler roll-out 获得去噪轨迹
- 将每个去噪状态 x_k 通过一步 Euler 投影映射到 clean-action 估计：
  ```
  Â_k^base = x_k - t_k · sg[v_base(x_k, t_k, s)]
  ```

**③ 迭代 Q-梯度上升 + Keep-Best 选择**
- 从 base 预测出发，执行 J 步投影 Q-梯度上升：
  ```
  Â_k,j+1 = Π_A[ Â_k,j + α · clip_G(∇_A Q(s, A)|_{A=Â_k,j}) ]
  ```
- Keep-best 机制选择最高价值候选，j=0 作为 fallback 保证 base 动作不被降级
- 得到的速度修正量：
  ```
  h_k^eff = (Â_k^base - Â_k,j*) / t_k
  ```

**④ 残差速度匹配**
- 去噪时步门控：s(t_k) = (1 - t_k)^p，集中 guidance 在 look-forward 估计可靠的区域
- 对齐损失：
  ```
  L_align = Σ_k s(t_k) || (v_θ(x_k, t_k, s) - sg[v_base(x_k, t_k, s)]) - sg[h_k^eff] ||²
  ```
- 仅通过局部预测 v_θ 反传梯度，其余 stop-gradient

## 3. 实验设计：数据集、场景、benchmark、对比方法

### 数据集与场景

| 环境 | 描述 | 动作空间 |
|------|------|----------|
| **LIBERO** | 40 个桌面操作任务，分 Spatial/Object/Goal/Long 四类 | 7-DoF EEF |
| **RoboTwin 2.0** | 10 个双手协调任务（抓取、放置、交接等） | 14-DoF dual-arm qpos |
| **Real-Robot** | 两个桌面任务：Pick Peach 和 Stack Bowls | 7-DoF arm + 2 RGB-D cameras |

### 对比基线（同一 SFT checkpoint、rollout data、RLT features、Cal-QL critic）
- **Test-time Q Selection**：采样后按 value 重排
- **Test-time Q Guidance**：推理时对动作施加 ∇_aQ refinement
- **Q-Improved Action Distillation**：将 critic 改进动作蒸馏为监督标签
- **Diffusion-QL**：通过去噪链回传 critic

## 4. 资源与算力

**论文中未明确说明具体算力资源**，包括 GPU 型号、数量、训练时长等信息均未在正文中披露。

## 5. 实验数量与充分性

### 定量实验
- **LIBERO**：每个任务 50 次独立 rollout，结果取 40 个任务平均
- **RoboTwin 2.0**：每个任务 50 次 rollout
- **Real-Robot**：每个任务 20 次物理试验

### 消融实验
对 LIBERO 进行 6 项消融研究：
- **Critic 侧**：RLT vs ResNet encoder、per-layer action injection、单 critic head vs ensemble
- **Policy 侧**：keep-best 选择、全步 velocity alignment、frozen-base anchor

### 实验充分性与公平性
- ✅ 对比方法共享完全相同的初始化、数据集、critic，仅在使用方式上区分
- ✅ 涵盖 simulation（LIBERO、RoboTwin 2.0）和真实机器人两个层面
- ✅ 包含丰富的消融实验验证各组件贡献
- ⚠️ Real-robot 实验规模较小（仅 2 个任务，各 20 次试验）

## 6. 主要结论与发现

| 环境 | 初始 SFT | Q-VGM | 提升 |
|------|----------|-------|------|
| LIBERO (avg) | 75.0% | 92.5% | +17.5pp |
| LIBERO-Long | 56.0% | 84.0% | +28.0pp |
| RoboTwin 2.0 | 76.4% | 87.2% | +10.8pp |
| Real-Robot avg | 40.0% | 67.5% | +27.5pp |

**关键发现**：
- Q-VGM 在所有三个设置中均超越所有同 backbone、同 critic 基线
- 推理时 Q-selection/Q-guidance 仅提供 moderate 改善（约 80%），说明训练时 critic guidance 至关重要
- Diffusion-QL 在 VLA 规模上性能下降（69.5%），验证了端到端回传的不稳定性
- 最大绝对增益出现在 SFT 性能较低的任务（如 LIBERO-Long），表明价值引导能有效修复困难场景中的失败模式

## 7. 优点

- **绕过梯度回传不稳定**：通过 look-forward + Q-梯度上升构造 velocity correction，无需通过去噪链回传
- **利用一阶信息**：相比 policy-gradient 的标量 advantage，∇_AQ 提供更丰富的作用方向信号
- **完全 Off-policy**：固定 replay buffer，无需额外环境交互
- **保留 flow 结构**：监督 velocity field 而非终端动作标签，保持 flow-matching 的生成机制
- **实用的初始化-学习范式**：few-shot SFT 初始化后，通过自生成 rollout 数据持续提升
- **自适应 guidance 强度**：keep-best 机制隐式实现 per-sample β_eff，通过离散线搜索在 Q 景观上寻找最优修正幅度
- **Critic 设计合理**：per-layer action injection + RLT features 确保 critic 对动作变化敏感且状态表示丰富

## 8. 不足与局限

- **Critic 可靠性依赖**：∇_AQ 的梯度信号在离线 rollout 支持区域外可能误导；依赖梯度裁剪和 keep-best fallback 缓解
- **Scalability 挑战**：随着任务 horizon 和环境多样性增长，Q-learning 难以 scale
- **Real-robot 实验规模有限**：仅 2 个任务、20 次试验/任务，统计意义有限
- **未探索在线学习**：方法完全离线，可能错失与环境交互带来的探索收益
- **Trust region 未显式建模**：未来可考虑将 trust region 内化到采样动态中
- **潜在方向**：结合 world models 缩短 TD horizon，同时保持 value-gradient matching 的梯度信号

（完）
