---
title: Lossy Compression for Sparse Aggregation
title_zh: 稀疏聚合的有损压缩
authors: "Yijun Fan, Fangwei Ye, Raymond W. Yeung"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2606.30425v1"
tags: ["query:dgnn-nas"]
score: 8.0
evidence: 通信高效的稀疏聚合压缩
tldr: 在分布式学习系统中，n个客户端各自拥有k稀疏d维局部模型，需上传至服务器聚合为全局模型，通信开销是关键挑战。本文提出了一种有损压缩方案，通过串联覆盖编码和草图方法实现高效传输；同时，基于f散度推导了通信成本与聚合精度的权衡下界，该下界在频率估计（二进制字母表）情形下是紧的。对于一般字母表，所提方案与理论下界之间仍存在差距，表明完全刻画此权衡需要进一步研究。本工作为稀疏聚合系统的压缩设计提供了理论基准与实用方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.30425v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 744, \"height\": 383, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30425v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 728, \"height\": 413, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30425v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 916, \"height\": 546, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30425v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 929, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30425v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 746, \"height\": 456, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.30425v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 743, \"height\": 448, \"label\": \"Figure\"}]"
motivation: 降低分布式学习中稀疏模型聚合的通信成本，同时保证聚合精度，是系统高效运行的关键。
method: 结合覆盖编码（cover）与草图（sketching）的有损压缩方案，对局部模型进行稀疏化处理。
result: 提出基于f散度的信息论下界，在二进制字母表下可达最优；一般字母表下方案与下界存在差距。
conclusion: 通信-精度权衡尚未完全刻画，需探索更优编码；本文为后续研究奠定了理论基础。
---

## 摘要
我们考虑在分布式学习系统中将稀疏的局部更新传输到服务器的问题。具体来说，系统由 n 个客户端组成，每个客户端拥有一个 k-稀疏的 d 维局部模型，以及一个负责将客户端模型聚合为全局模型的中央服务器。目标是刻画从客户端到服务器传输中的通信成本与聚合全局模型的准确性之间的权衡。我们提出了一种通过连接覆盖方法和草图方法对稀疏局部模型进行压缩的方案。我们还提出了一个基于 f-散度的逆界，该逆界增强了传统的 Fano 型下界。对于频率估计情况（即每个坐标取值于二元字母表），所提出的下界是紧的。对于一般字母表，所提出的可达方案相对于逆界仍然是次优的，这表明通信-准确性权衡的完整表征需要进一步研究。

## Abstract
We consider the problem of transmitting sparse local updates to the server in a distributed learning system. Specifically, the system consists of $n$ clients, each possessing a $k$-sparse $d$-dimensional local model, and a central server responsible for aggregating the clients' models into a global model. The goal is to characterize the tradeoff between the communication cost in the transmission from the clients to the server and the accuracy in aggregating the global model. We propose a compression scheme for sparse local models by concatenating a covering method and a sketching method. We also present a converse based on f-divergence, which strengthens the conventional Fano-type lower bounds. The proposed lower bound is tight for the frequency estimation case, that is, each coordinate takes values in a binary alphabet. For general alphabets, the proposed achievable schemes remain suboptimal relative to the converse bounds, indicating that a complete characterization of the communication-accuracy tradeoff requires further investigation.

---

## 论文详细总结（自动生成）

### 论文总结：Lossy Compression for Sparse Aggregation

#### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：在分布式学习系统（如联邦学习）中，n个客户端各自持有一个k-稀疏的d维局部模型，需要上传到中央服务器进行聚合。通信带宽是主要瓶颈，因此需要设计压缩机制，在降低通信开销的同时保证聚合精度。
- **背景**：现有方法包括直接传输非零索引与值（稀疏梯度压缩）、基于Reed-Solomon码的确定性稀疏恢复、以及基于Count Sketch和Count-Min Sketch的随机化草图方法。但这些方法要么针对精确恢复，要么未显式刻画通信与ℓ₁失真之间的权衡。
- **整体含义**：本文试图从信息论角度给出通信–精度权衡的上下界，提出基于覆盖编码与草图结合的实用方案，并证明在二进制字母表（频率估计）情形下可达最优。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：利用失真D降低有效稀疏度：原稀疏度k可缩减为约k − D/q（q为坐标最大值），从而减少需要准确传输的坐标数。
- **关键技术细节**：
  - **覆盖压缩方案（Covering Scheme）**：对每个局部向量x，通过排序其坐标值，只保留使得ℓ₁失真不超过D的最大幅值坐标，其余置零。输出向量y满足║x−y║₁ ≤ D，且非零项数≤⌈k − D/q⌉。通信成本约为n·⌈k−D/q⌉·log(dq) bits。
  - **草图方法（Count Sketch & Count-Min Sketch）**：利用哈希和随机符号将高维向量压缩为低维计数向量，服务器通过中位数或最小值还原，但通信成本随原始稀疏度k多项式增长（O(k³)或O(k²)）。
  - **组合方案**：先用覆盖压缩降低有效稀疏度至k′ = k − D₁/q，再对k′-稀疏向量使用草图方法处理剩余失真D₂，使总失真D = D₁ + D₂。通过优化D₁和D₂的分割，可显著降低总通信成本（例如当D ≥ mq时，CMS组合方案成本降至O((k−m) log d)）。
- **逆界（Converse）**：基于f-散度（特别是Rényi散度）取代KL散度得到更紧的下界，形式为`C(D, δ) ≥ D_λ(Ber(1−δ)∥Ber(P_max(D)))`，其中P_max(D)是ℓ₁-ball的最大概率质量。在二进制情形下与可达界匹配。

#### 3. 实验设计
- **无传统实验数据集**：本文为纯理论论文，无实际数据集或基准测试。
- **数值示例**：作者通过设定参数（如n=5, k=10, d=2^10, q=4等）绘制了通信成本与失真关系的曲线图（图3，图5），比较了覆盖方案、Reed-Solomon方案、CMS、cCMS以及下界。
- **对比方法**：包括基于Reed-Solomon码的无损/有损方案、纯Count Sketch与Count-Min Sketch、以及组合方案cCS和cCMS。

#### 4. 资源与算力
- **未提及**：论文中未讨论任何GPU型号、数量或训练时长，因为属于信息论分析，不涉及实际训练。

#### 5. 实验数量与充分性
- **实验数量**：仅有参数化数值计算，未进行大规模或消融实验。结果以曲线形式呈现，比较了不同失真水平下的理论通信成本。
- **充分性与公平性**：作为理论工作，其数值比较足以说明理论界与可达方案之间的差距。但缺乏在实际系统或数据集上的验证，实用性能未知。

#### 6. 论文的主要结论与发现
- 失真可以线性地降低有效稀疏度，从而减少通信成本。
- 覆盖方案在二进制字母表（q=1）时与基于f-散度的下界基本匹配，表明`C(D,0) ≈ n(k−D) log d`。
- 对于一般字母表，组合覆盖+草图方案可以过渡到有效稀疏度主导的区域，优于单独使用草图方案，但与下界仍有差距。
- 基于f-散度的逆界严格优于传统的Fano型不等式，且在λ→∞时得到最简单的下界`log((1−δ)|U|/P_max(D))`。
- 当D ≥ kq时，通信成本降为零（无需传输）。

#### 7. 优点
- **理论贡献扎实**：提出了新颖的覆盖编码构造，并给出了精确的基数公式（Theorem 1）和简洁的上界（Corollary 1）。
- **逆向下界改进**：引入f-散度（Rényi散度）得到更紧的下界，且可退化为传统Fano界。
- **组合思想简洁有效**：将确定性与随机性压缩结合，清晰揭示了失真与通信成本之间的分段线性关系。
- **对二进制情形的完备刻画**：首次在频率估计问题上达到可达与逆界匹配。

#### 8. 不足与局限
- **实验验证缺失**：未在真实分布式学习任务（如CIFAR、ImageNet等）中评估方案的实际性能，无法判断理论承诺在实践中的可用性。
- **一般字母表下的间隙**：对于q≥2，可达方案与下界仍有显著差距，未能完全刻画权衡曲线。
- **未考虑隐私与安全性**：虽然指出草图方法有自然隐私优势，但未分析覆盖方案对隐私的影响，也未结合差分隐私等约束。
- **计算复杂度未讨论**：覆盖方案需要排序及阈值计算，草图需要哈希与去偏，实际部署中的运行时开销未评估。
- **仅关注ℓ₁失真**：其他失真度量（如ℓ₂）和更一般的损失函数未作处理。

（完）
