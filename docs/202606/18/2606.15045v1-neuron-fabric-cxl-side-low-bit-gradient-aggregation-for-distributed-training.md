---
title: "NEURON-Fabric: CXL-Side Low-Bit Gradient Aggregation for Distributed Training"
title_zh: NEURON-Fabric：面向分布式训练的CXL端低位梯度聚合
authors: "Ziqiang Wang, Changcheng Huang, Chung-Horng Lung"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15045v1"
tags: ["query:dgnn-nas"]
score: 6.0
evidence: 用于分布式训练的低位梯度聚合
tldr: 针对大模型分布式训练中梯度All-Reduce带来的内存和通信压力，本文提出了NEURON-Fabric架构。该架构在CXL内存控制器端进行低比特梯度聚合，同时保留FP32路径。通过层级感知策略，在维持模型精度的同时大幅减少了梯度通信量，且硬件开销极小。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15045v1/fig-001.webp\", \"caption\": \"Figure 7.Modeled gradient-communication component for one GPT-2 XL payload. NEURON-Fabric occupies a controller-side low-bit point: lower modeled gradient traffic than FP32 collectives when low-bit admission is valid, but not a universal replacement for software optimizers or switch-supported aggregation.\", \"page\": 11, \"index\": 1, \"width\": 946, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2606.15045v1/fig-002.webp\", \"caption\": \"Figure 4. Validated low-bit convergence regimes. Left: full 100-epoch CIFAR-10/ResNet-18 training. Right: complete threeepoch SST-2/DistilBERT fine-tuning. Endpoint labels report final mean ± standard deviation across three seeds.\", \"page\": 8, \"index\": 2, \"width\": 1037, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2606.15045v1/fig-003.webp\", \"caption\": \"Figure 3. Operating-envelope sensitivity sweep. Panel (a) maps exposed overhead across CXL bandwidth and G-Binary compute cycles; panel (b) varies the fixed CXL memory-access latency; panel (c) shows how LLC capacity controls controllervisible accesses; panel (d) bounds policy telemetry delay as a late mode update.\", \"page\": 8, \"index\": 3, \"width\": 977, \"height\": 712}, {\"url\": \"assets/figures/arxiv/2606.15045v1/fig-004.webp\", \"caption\": \"Figure 1. NEURON-Fabric node architecture. The runtime writes ordinary FP32 or admitted packed low-bit payloads into the CXL-resident gradient buffer; on a later read response, the controller selects identity/bypass or the GBinary/G-Ternary aggregate transform without overwriting stored bytes. A discrete CXL switch is only required in switched pooled-memory deployments.\", \"page\": 4, \"index\": 4, \"width\": 498, \"height\": 289}, {\"url\": \"assets/figures/arxiv/2606.15045v1/fig-005.webp\", \"caption\": \"Figure 5. CIFAR-100 boundary. Full-path G-Binary and GTernary remain far below FP32 and the sign-gradient references. Endpoint labels report final mean± standard deviation and Δ versus FP32 across three seeds.\", \"page\": 9, \"index\": 5, \"width\": 487, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2606.15045v1/fig-006.webp\", \"caption\": \"Figure 6. Guarded recovery pilot over 60 epochs. Endpoint callouts report final validation accuracy and average traffic saved relative to FP32 communication.\", \"page\": 9, \"index\": 6, \"width\": 488, \"height\": 368}]"
motivation: 大模型分布式训练中的梯度All-Reduce操作给内存和通信路径带来了日益严重的压力。
method: 提出一种CXL端控制器架构，在近内存处执行低比特梯度聚合，并提供接口以灵活选择低比特或FP32计算路径。
result: "实验表明该设计运行时间开销极低（最多1.67%），层级感知策略能将梯度流量降至FP32基线的3.6%-5.4%并恢复大部分精度。"
conclusion: NEURON-Fabric作为一种近内存数据通路扩展，能够以极小的硬件开销有效平衡分布式训练中的通信效率与模型精度。
---

## 摘要
在大模型分布式训练中，尤其是大语言模型工作负载下，梯度的All-Reduce操作给内存和通信路径带来越来越大的压力。本文探讨计算互连（CXL）内存控制器能否在梯度缓存行通过时聚合低位梯度信号，同时为不应使用低位近似的工作负载、网络层或阶段保留32位浮点（FP32）路径。我们提出了NEURON-Fabric，这是一种CXL端控制器架构，能够在CXL内存附近执行打包的梯度二值（G-Binary）符号计数聚合和梯度三值（G-Ternary）门控聚合，并提供用于选择低位或FP32路径的控制接口。周期级时序实验表明，所测得的五周期低位聚合数据通路在完全末级缓存未命中的情况下，最多仅增加1.67%的暴露运行时开销；在带宽压力下，该计算阶段会被CXL服务时间完全隐藏。功能测试证实了字节精确的一致性回读、G-Binary符号计数聚合以及G-Ternary门控功能。训练检查量化了通信与精度的权衡：低位聚合在CIFAR-10/ResNet-18和SST-2/DistilBERT上保持接近FP32的精度，而全路径低位聚合在CIFAR-100/ResNet-18上则告失败。层感知准入机制识别出分类器头部对精度敏感；在将头部保留在FP32的同时对主干网络应用低位聚合，恢复了大部分精度，并将梯度流量降至FP32基线的3.6%至5.4%。硬件综合和FPGA布局布线评估表明，512位聚合数据通路足够小，完全可以被视为近存数据通路的扩展，而非独立的加速器级模块。

## Abstract
In large-model distributed training, especially large language model workloads, gradient All-Reduce increasingly stresses the memory and communication path. This paper asks whether a Compute Express Link (CXL) memory controller can aggregate low-bit gradient signals as gradient cache lines pass through it, while preserving a 32-bit floating-point (FP32) path for workloads, layers, or phases that should not use low-bit approximation. We present NEURON-Fabric, a CXL-side controller architecture that performs packed gradient-binary (G-Binary) sign-count aggregation and gradient-ternary (G-Ternary) gated aggregation near CXL memory, with a control interface for selecting low-bit or FP32 paths. Cycle-level timing experiments show that the measured five-cycle low-bit aggregation datapath adds at most 1.67 percent exposed runtime overhead in the full last-level-cache miss regime; under bandwidth pressure, the same compute stage is hidden by CXL service time. Functional tests confirm byte-exact identity read-back, G-Binary sign-count aggregation, and G-Ternary gating. Training checks quantify the communication and accuracy tradeoff: low-bit aggregation remains close to FP32 on CIFAR-10/ResNet-18 and SST-2/DistilBERT, while full-path low-bit aggregation fails on CIFAR-100/ResNet-18. Layer-aware admission identifies the classifier head as sensitive; keeping the head on FP32 while applying low-bit aggregation to the backbone recovers most accuracy and reduces gradient traffic to 3.6-5.4 percent of the FP32 baseline. Hardware synthesis and FPGA place-and-route estimates suggest that the 512-bit aggregation datapath is small enough to be treated as a near-memory datapath extension rather than a separate accelerator-scale block.