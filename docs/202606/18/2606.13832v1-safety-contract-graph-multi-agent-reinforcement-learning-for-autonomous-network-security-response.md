---
title: Safety-Contract Graph Multi-Agent Reinforcement Learning for Autonomous Network Security Response
authors: Jose Luis Lima de Jesus Silva
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.13832v1"
tags: ["query:mas"]
score: 9.0
evidence: 用于网络安全的的多智能体强化学习
tldr: "当前SOC面临反应延迟高、仅依赖奖励的多智能体强化学习方法缺乏操作纪律导致无法部署的问题。本文提出安全契约图MARL框架，通过将操作预算控制、约束优化与图编码解耦，实现可解释的安全约束决策。在CAGE Challenge 4评估中，无约束方法在100% episodes中违反停机预算，而C-MAPPO-GAT将违规率降至0.3%，ACD³-GAT在保持13.8%违规率的同时将平均成本降至48.2，展示了安全约束学习方法在真实网络安全场景中的部署可行性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.13832v1/fig-001.webp\", \"caption\": \"\", \"page\": 20, \"index\": 1, \"width\": 2095, \"height\": 1361}, {\"url\": \"assets/figures/arxiv/2606.13832v1/fig-002.webp\", \"caption\": \"\", \"page\": 21, \"index\": 2, \"width\": 963, \"height\": 750}]"
motivation: SOC自动化响应系统需降低延迟，但纯奖励驱动的多智能体强化学习方法缺乏运营纪律，在真实部署中无法满足停机时间、MTTR等安全预算约束。
method: 提出ACD³-GAT框架，采用安全契约图结构分离模拟器观测与可复用操作预算，通过约束优化、图注意力网络编码、反事实动作筛选，结合CVaR尾险估计和对手信念状态进行决策。
result: "在CAGE Challenge 4的200 episode评估中，C-MAPPO-GAT将停机违规率从100%降至0.3%，平均成本从355.4降至15.5；ACD³-GAT进一步平衡安全与性能，平均成本48.2、违规率13.8%。压力测试表明约束策略的适应性降级更小。"
conclusion: 安全契约约束学习能在保持竞争力的同时实现运营纪律，ACD³-GAT位于安全契约前沿而非最保守合规点，为自主网络安全响应的实际部署提供了可行方案。
---

## Abstract
Autonomous network-security response systems promise to reduce Security Operations Centre (SOC) reaction latency, but reward-only multi-agent reinforcement learning (MARL) can improve security reward while remaining non-deployable. We present a safety-contract graph MARL framework and instantiate it as ACD$^3$-GAT (Adaptive Constrained Counterfactual Decisioning with a Graph Attention Network encoder), an architecture that separates simulator observations from reusable operational budgets, constrained optimization, graph state encoding, and counterfactual action screening. We evaluate the method in CAGE Challenge 4, where agents operate under budgets for Mean Time to Recover (MTTR), false-positive response, and firewall change-management disruption. Across the benchmark, every unconstrained method violates the SOC downtime budget in 100% of evaluated episodes, with mean downtime proxy costs of 311-430 against a budget of 50. This complements prior CAGE Challenge 4 findings by showing that reward-only learning lacks operational discipline. Constrained MAPPO-GAT (C-MAPPO-GAT) isolates Lagrangian operational-cost control and budget-aware screening, while ACD$^3$-GAT adds budget context, CVaR tail-risk estimation, opponent-belief state, and Graph Counterfactual Risk Propagation (G-CRP). The replicated comparison includes three 200-episode seeds for IPPO, MAPPO-GAT, C-MAPPO-GAT, and ACD$^3$-GAT. C-MAPPO-GAT reduces downtime violation from 100% to 0.3% and mean downtime cost from 355.4 to 15.5 relative to MAPPO-GAT. ACD$^3$-GAT reduces mean downtime cost to 48.2 with a 13.8% violation rate, placing it on the safety-contract frontier rather than at the most conservative compliance point. Topology-seed and coupled adaptive Red-process stress tests preserve this contrast and show lower worst adaptive degradation for safety-constrained policies than reward-only MAPPO-GAT.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 核心问题与整体含义（研究动机与背景）

- **问题背景**：现代企业的网络安全事件数量和速度已超出人工响应能力，Security Operations Center（SOC）每天需处理数千条告警，手动关联事件、隔离主机、重装系统、调整防火墙策略，同时需保障关键任务的可用性。
- **核心问题**：现有基于纯奖励的多智能体强化学习（MARL）方法虽然能提升安全奖励，但**缺乏运营纪律**，无法满足真实 SOC 场景下的操作约束（停机时间、MTTR、误报率、防火墙变更管理），导致策略在真实部署中不可行。
- **研究目标**：将安全契约（Safety Contract）——即对 MTTR、误报响应和防火墙变更中断的预算约束——显式建模为可审计的决策支持框架，使自主网络安全响应策略能够同时优化安全性与运营可接受性。

### 2. 方法论：核心思想、关键技术、算法流程

#### 2.1 核心思想

将网络安全响应建模为**约束的 Decentralized POMDP（Dec-POMDP）**，引入三个 SOC 运营预算约束：

| 约束类型 | 预算值 B | 含义 |
|----------|---------|------|
| MTTR/停机成本 | B_down = 50 | Restore 动作的计数上限 |
| 误报响应成本 | B_fp = 10 | 无明显恶意证据时执行 Restore 的计数上限 |
| 防火墙变更成本 | B_fw = 20 | BlockTrafficZone/AllowTrafficZone 动作的计数上限 |

安全契约定义为：策略在某一 episode 中满足契约，当且仅当三个约束的episode累积成本均不超过其预算。

#### 2.2 关键技术与公式

**约束目标函数（拉格朗日松弛）**：

```
max_π J_r(π) = E_π[Σ_t γ^t r̄_t]
s.t. J_c^k(π) = E_π[Σ_t c_k^t] ≤ B_k, ∀k ∈ {down, fw, fp}
```

**拉格朗日松弛目标**：

```
L(π, λ) = J_r(π) − Σ_k λ_k (J_c^k(π) − B_k), λ ≥ 0
```

**拉格朗日乘子更新（双梯度上升）**：

```
λ_k ← [λ_k + η_λ (Ĵ_c^k − B_k)]_+, η_λ = 0.01
```

**ACD³-GAT 复合优势函数**：

```
Â_ACD³_t = Â_r_t − Σ_k λ_k Â_c^k_t − 0.1 Â_CVaR_t − 0.05 Â_exploit_t − 0.01 Â_override_t
```

**上下文向量组成**：

```
ctx_t = [g_t ‖ z_t ‖ b_t ‖ u_t] ∈ ℝ^(64+32+16+8)
```

- g_t：图注意力网络（GAT）编码的图嵌入（64维）
- z_t：GRU 编码的对手（Red进程）信念状态（32维）
- b_t：归一化剩余运营预算嵌入（16维）
- u_t：基于动作类型分布熵的不确定性信号（8维）

**G-CRP（图反事实风险传播）**：使用独立级联模型传播节点妥协概率，评估候选动作对未来安全风险的影响，结合 Q_safe 评分进行动作筛选。

**Budget-Aware Shield**：当某预算耗尽时，屏蔽该预算对应的成本代理为正的动作；否则基于 G-CRP Q_safe 评分选择最优候选。

#### 2.3 ACD³-GAT 完整训练流程

1. **感知层**：将 CAGE-4 二进制观测解析为宿主-子网图结构，通过两层 GAT 编码得到图嵌入 g_t
2. **上下文构建**：将 g_t 与对手嵌入 z_t、预算嵌入 b_t、不确定性嵌入 u_t 拼接为完整上下文
3. **动作提议**：因子化策略 π(τ)π(ν|τ) 采样动作类型 τ 和目标 ν
4. **动作筛选**：Budget-Aware Shield 结合 G-CRP 风险评分，输出最终执行动作
5. **执行与成本记录**：在 CybORG 中执行动作，记录奖励 r_t 和成本代理 c_k^t
6. **策略更新**：基于复合优势函数执行 PPO 更新（4 epoch），更新拉格朗日乘子

### 3. 实验设计：数据集、场景、基准、对比方法

#### 3.1 数据集与场景

- **环境**：CAGE Challenge 4（基于 CybORG 仿真器）
- **设定**：5 个 Blue 智能体，500 步 episode，FSM 驱动的 Red 对手进程
- **网络拓扑**：9 个子网（互联网面向区、承包商区、办公区、管理区、受限区、运营区），任务关键资产位于两个运营区

#### 3.2 基准与方法对比

| 方法类别 | 具体方法 | 说明 |
|----------|----------|------|
| 非学习基线 | Sleep、Random、Rule-based | 固定策略与简单规则 |
| Actor-Critic | IA2C | 独立 Advantage Actor-Critic |
| IPPO 系列 | IPPO（MLP编码） | 独立 PPO |
| MAPPO 编码变体 | MAPPO-MLP、MAPPO-GNN、MAPPO-GAT | 集中式评论家 + 不同编码器 |
| 架构变体 | Factorized-IPPO、Opp-IPPO | 因子化动作头、对手嵌入 |
| 安全组件 | CVaR-MAPPO | CVaR 尾风险重加权 |
| **本文方法** | **C-MAPPO-GAT** | 约束 MAPPO-GAT（最强合规配置） |
| **本文方法** | **ACD³-GAT** | 完整安全契约图 MARL 框架 |

### 4. 资源与算力

- **论文中未明确说明** GPU 型号、数量或具体训练时长
- 仅报告了训练轮次相关参数：batch size（M=8 episodes）、PPO 4 epoch 更新、GAE 参数（γ=0.99, λ_GAE=0.95）、Adam 学习率 η=3×10⁻⁴
- 主基准：每方法 200 episode × 3 seeds；更长复现：300 episode × 2 seeds
- 总计评估 episode 数约 600+ episode（主基准）+ 600 episode（更长复现）+ 压力测试 episode

### 5. 实验数量与充分性

#### 5.1 主实验结构

- **核心基准**：3 seeds × 200 episodes，覆盖 IPPO、MAPPO-GAT、C-MAPPO-GAT、ACD³-GAT
- **更长复现**：2 seeds × 300 episodes，覆盖同上三个图基方法
- **消融/组件检查**：100 episodes（MAPPO-MLP/GNN/Factorized-IPPO/Opp-IPPO/CVaR-MAPPO）、30 episodes（ACD³+AskHuman、ACD³+det.G-CRP）
- **压力测试**：拓扑种子泛化（5 unseen seeds × 20 episodes）、自适应 Red 进程耦合（20 更新轮次）

#### 5.2 充分性与客观性评估

- ✅ **Replication 充分**：核心方法使用 3 seeds，结果有置信区间
- ✅ **对比覆盖全面**：从非学习基线到多种 MARL 变体，包括编码器对比（MLP/GNN/GAT）、架构对比（因子化/对手嵌入）、安全组件对比（CVaR/拉格朗日）
- ✅ **压力测试**：拓扑 OOD 和自适应 Red 进程评估鲁棒性
- ⚠️ **局限**：部分组件检查（如 AskHuman vs G-CRP）仅 30 episodes 且结果相同，未进行因子化消融分离各组件贡献

### 6. 论文主要结论与发现

1. **运营纪律是部署前提**：所有无约束方法在 100% episodes 中违反停机预算，平均成本 311-430（预算仅 50），消耗 6-9 倍允许的 MTTR 预算
2. **C-MAPPO-GAT 是最强合规配置**：违规率降至 0.3%，平均停机成本降至 15.5（降低 95-96%），零灾难性事件
3. **ACD³-GAT 处于安全契约前沿**：平均成本 48.2（接近预算），违规率 13.8%，在完整安全契约栈与性能之间取得平衡
4. **图结构改善尾风险**：MAPPO-GAT 在 CVaR-10% 上优于 MLP/GNN 编码器，但 Raw Return 未占主导
5. **CVaR 重加权单独不足**：仅靠 CVaR 尾风险重加权无法达到约束优化的合规效果
6. **鲁棒性验证**：约束策略在拓扑 OOD 和自适应 Red 进程下仍保持低违规率，降级程度小于无约束 MAPPO-GAT

### 7. 优点与亮点

- **问题定义清晰**：将真实 SOC 运营约束（MTTR、误报、防火墙变更）显式建模为可审计的安全契约，而非隐式偏好
- **框架模块化**：将可复用安全契约组件（预算、拉格朗日学习、图编码、反事实筛选）与环境接口（观测解析、动作映射）解耦
- **组件对比丰富**：从独立 IA2C 到集中式 MAPPO、从 MLP 到 GAT 编码器、从无约束到 CVaR 再到拉格朗日约束，系统揭示各组件贡献
- **双重安全机制**：拉格朗日学习跨 batch 塑造策略 + Budget-Aware Shield 在 episode 内防止预算耗尽
- **G-CRP 创新**：图反事实风险传播提供动作的预测性安全评分，而非仅依赖即时成本代理
- **TCGS 扩展**：Temporal Contract Graph Shielding 展示利用轨迹历史预测未来预算违约的可行性

### 8. 不足与局限

- **实验算力信息缺失**：未报告 GPU 型号、数量或训练时长，难以评估资源需求和可复现性成本
- **组件因果归因不足**：ACD³-GAT 各组件（预算上下文、CVaR、对手嵌入、G-CRP）的因果贡献未通过因子化消融分离；30 episodes 的 AskHuman vs G-CRP 结果相同，无法区分
- **拓扑泛化局限**：ACD³-GAT 在拓扑 OOD 上的评估受限，未充分验证其布局稳定性
- **代理成本信号**：误报代理定义为"无活跃告警时执行 Restore"，依赖可见告警证据，可能不完全反映真实误报
- **机会成本未建模**：安全契约仅考虑负面约束，未显式建模延迟响应或低优先级告警遗漏的机会成本
- **红队评估有限**：自适应 Red 进程评估仅一个种子，压力测试深度不足以支撑强鲁棒性声明
- **C-MARL 理论保证缺失**：非凸神经网络设置下未声称强对偶保证或形式化约束满足，仅报告经验违规率

（完）
