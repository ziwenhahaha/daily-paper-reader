---
title: Lyapunov-Based Sample Complexity Analysis for Weakly-Coupled MDPs
authors: "Tianhao Wu, Matthew Zurek, Weina Wang, Qiaomin Xie"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14095v2"
tags: ["query:rl"]
score: 9.0
evidence: 弱耦合MDP的样本复杂度理论
tldr: 为弱耦合MDP中学习近优策略建立首个有限样本PAC保证。
source: arxiv
selection_source: fresh_fetch
motivation: 弱耦合MDP的样本复杂度理论。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
We study the sample complexity of learning in average-reward weakly-coupled Markov decision processes (WCMDPs) and Restless Bandits (RBs) under a generative model. Naive reduction to a tabular MDP leads to high complexity bounds as the state-action space is exponentially large in the number of arms $N$. By exploiting the weakly coupled structure, we show that near-optimal policies can be learned with sample and computational complexities that are polynomial in $N$. Specifically, we analyze the plug-in approach, which applies an efficient planning algorithm to an empirical model estimated from data. For fully heterogeneous WCMDPs, we establish the first finite-sample PAC guarantee with polynomial complexity and an $O(1/\sqrt{N})$ optimality gap. For homogeneous RBs, we further prove that a smaller optimality gap is achievable under mild structural assumptions. A primary technical contribution of our work is a novel Lyapunov-based analysis framework. Unlike classical approaches that rely on the difficult-to-control bias function, our framework uses an explicitly constructed Lyapunov function along with a drift transfer technique between the true and empirical models. A key step of independent interest in our framework is a fine-grained perturbation analysis for the underlying linear programming (LP) relaxation, which provides a general tool for analyzing LP-based policies and weakly-coupled systems.

---

## 论文详细总结（自动生成）

## 论文总结：Lyapunov-Based Sample Complexity Analysis for Weakly-Coupled MDPs

### 1. 核心问题与整体含义（研究动机与背景）

- **研究问题**：在平均奖励（average-reward）弱耦合马尔可夫决策过程（WCMDPs）和 restless bandits（RBs）中，如何在样本量仅为多项式增长于臂数 N 的条件下，学习到接近最优的策略。
- **挑战**：传统的 tabular MDP 方法将 N 个臂的联合状态-动作空间视为一个整体 MDP，其规模随 N 呈指数增长（状态空间为 S^N，动作空间为 A^N），导致朴素方法的样本复杂度在 N 上呈指数级。
- **动机**：大规模决策系统（如在线广告、机器维护、作业调度、监控、医疗资源分配）天然具有弱耦合结构——各臂独立演化，仅通过全局约束耦合。利用这一结构应能降低学习复杂度。
- **目标**：建立首个针对 WCMDPs 和 RBs 的有限样本 PAC（Probably Approximately Correct）保证，证明多项式复杂度学习的可行性。

### 2. 论文提出的方法论

#### 核心思想
- **插件式方法（Plug-in Approach）**：先用生成模型收集每个单臂 MDP 的转移样本，构建经验转移核（empirical transition kernel），再将经验 MDP 代入已有的高效规划算法，得到学习策略。
- **关键创新**：用显式构造的 Lyapunov 函数替代传统分析中难以控制的 bias 函数（相对值函数），配合漂移转移（drift transfer）技术，在真实模型与经验模型之间建立桥梁。

#### 关键技术细节

- **WCMDP 设置**：每个臂 i 有独立的转移核 P_i、奖励 r_i 和 K 类代价函数 c_{k,i}，约束为 Σ_i c_{k,i}(s_i, a_i) ≤ α_k N。
- **LP 松弛**：构造 N 臂 WCMDP 的 LP 松弛（式 2），其最优值 ρ_rel 是真值 ρ* 的上界；定义最优单臂策略 \bar{π}*_i（式 4）。
- **ID 策略（Indegree）**：作为 WCMDP 的参考规划策略，对臂按 ID 顺序尝试执行“理想动作”，通过符合数 N*_t 动态控制约束满足；具有 O(1/√N) 的规划最优性间隙（Zhang et al. 2025）。
- **Lyapunov 分析框架**（Section 5）：
  - **Step 1**：为参考策略构造 Lyapunov 函数 V，满足漂移界（C1）：E[(P_π − I)V](z) ≤ −C_0 F(z) + G_1(N) 和间隙控制（C2）：ρ_rel − r_π(z) ≤ C'_0 F(z) + G_2(N)。
  - **Step 2**：漂移转移——对同一 Lyapunov 函数 V，经验系统的漂移可写为 (bP_π − P_π)V = (P_π − I)V + (bP_π − P_π)V，即真实漂移加上模型误差项。
  - 结合两者得到经验性能界，最终推导出样本复杂度上界。
- **RB 设置**（homogeneous）：所有臂共享相同转移核和奖励，二值动作空间 A={0,1}，约束为 Σ_i a_i = αN。
- **两集合策略（Two-set Policy）**：作为 RB 的参考规划策略，维持两个不相交集合 D_OL^t（运行局部最优控制）和 D_{\bar{π}*}^t（运行无约束最优控制），利用中性状态 \tilde{s} 调整激活数以满足预算；在适当假设下具有指数小的最优性间隙 O(exp(−cN))。
- **LP 扰动分析**（Theorem 3）：针对 RB 的 LP 松弛（式 3），在转移核受到小扰动（||bP − P||_∞→∞ ≤ δ）下，证明经验 LP 解保持支撑结构（support preservation）、中性状态唯一性、局部稳定性（Part 3）、以及 LP 解的扰动界（Part 4）。这是建立经验策略稳定性的关键。

#### 算法流程概述

1. 对每个臂 i，从生成模型收集每个状态-动作对 (s,a) 的 n 个 i.i.d. 样本。
2. 构建经验转移核 \hat{P}_i。
3. 将 \hat{P}_i 代入对应 LP 松弛（WCMDP 用式 2，RB 用式 3），求解得到 \hat{y}* 和经验最优单臂策略 \hat{\bar{π}}*。
4. 用 \hat{y}* 和 \hat{\bar{π}}* 构建参考策略的经验版本（\hat{π}_{ID} 或 \hat{π}_{TS}）。
5. 执行该经验策略并评估其平均奖励。

### 3. 实验设计

- **实验性质**：本文为纯理论工作，**未进行传统意义下的数值实验或真实数据集评估**。
- **理论验证方式**：通过数学证明和引理建立有限样本 PAC 保证，用数值常数（如 C_1, C_2 等）表述界，并通过分析展示界的多项式依赖性。
- **相关工作对比**：文中大量引用和对比 Zhang et al. (2025) 的 ID 策略规划结果、经典 tabular MDP 的下界（Wang et al. 2022, Zurek and Chen 2025b）等，用于说明本文突破了指数复杂度瓶颈。

### 4. 资源与算力

- **算力说明**：本文不涉及计算实验，无 GPU 使用或训练时长等资源消耗。所有“计算”均为算法复杂度的理论分析（ID 策略和两集合策略均为多项式时间可计算）。
- **算法复杂度**：生成模型采样 O(N·S·A·n)；LP 求解 O(N·poly(S,A))；ID 策略执行 O(N) 每时间步；两集合策略执行 O(N) 每时间步。

### 5. 实验数量与充分性

- **实验数量**：0（无实验）。本文是理论论文，全部贡献为定理、引理及数学证明。
- **理论支撑充分性**：从 Theorem 1（WCMDP）和 Theorem 2（RB）的完整证明，到 Lemma 3-4、Lemma 9-10 的分层推导，再到 Theorem 3 的 LP 扰动分析，理论链条完整。附录提供了大量补充引理（如 Markov 链扰动界、漂移不等式、混合时间分析等），总计 71 页，论证详尽。
- **对比方式**：通过引用对比已有方法的复杂度下界（如 tabular MDP 需要 Θ(|S|^N·|A|^N) 样本），证明本文多项式上界的优越性。

### 6. 主要结论与发现

- **WCMDP 结论**（Theorem 1）：对完全异质 WCMDP，学习策略 \hat{π}_{ID} 的最优性间隙上界为 O(√(S + log(SA N/η))/√n) + O(1/√N)，其中 n 为每状态-动作对的样本数。达到任意 ε = Ω(1/√N) 的间隙需要总样本数 O((S + log(SA N/η))·N^2/ε^2)，在 N 上呈多项式增长，而非指数增长。
- **RB 结论**（Theorem 2）：对同质 RB，学习策略 \hat{π}_{TS} 的最优性间隙上界为 O(√(S + log(SA/η))/√n) + O(exp(−C_5 N))。当目标 ε = Θ(1/poly(N)) 时，总样本数 O((S + log(SA/η))·N^2/ε^2) 仍为多项式。
- **关键发现**：利用弱耦合结构可将样本复杂度从指数级降低到多项式级别，核心在于（1）利用单臂 MDP 的混合时间假设而非联合状态空间的混合时间；（2）参考策略本身的近优性（ID 策略 O(1/√N)，两集合策略 O(exp(−cN))）贡献了部分最优性间隙。
- **LP 扰动鲁棒性**：Theorem 3 揭示了 RB 的 LP 松弛解在转移核小扰动下具有结构性稳定性——支撑集保持、中性状态保持、局部稳定性保持，为经验策略的稳定性提供了严格保证。

### 7. 优点

- **理论突破**：首次为弱耦合 MDP 建立了有限样本 PAC 界，突破了状态-动作空间指数爆炸的障碍，是该方向的里程碑结果。
- **方法论贡献**：提出 Lyapunov 漂移转移框架，为分析插件式学习方法提供了通用工具，可推广到其他具有 Lyapunov 可分析参考策略的弱耦合系统。
- **LP 扰动分析**：Theorem 3 提供了细粒度的 LP 敏感性分析，不仅是本文的关键技术工具，还可作为分析其他基于 LP 的索引策略（如 Whittle 索引）的鲁棒性模块。
- **双重近优性保证**：即使参考策略本身不是精确最优（如 ID 策略有 O(1/√N) 间隙），学习策略也能在此基础上以 O(1/√n) 的速率收敛到参考策略的绩效。
- **假设合理性**：WCMDP 仅需单臂 MDP 的均匀混合时间（Assumption 1），RB 仅需单链遍历性和局部稳定性（Assumption 2-3），不依赖难以验证的全局混合条件。
- **阻塞策略拓展**：为固定样本量场景设计了分组阻塞策略，有效平衡有限样本误差与参考策略最优性间隙，适用于离线学习设置。

### 8. 不足与局限

- **无实验验证**：作为纯理论论文，缺乏在真实或仿真环境（如真实调度数据集、推荐系统日志）上的数值验证，理论界的数值稳定性（如常数大小）未经验证。
- **依赖生成模型**：要求能对任意 (s,a) 独立采样，而在实际在线学习中通常只能获得时序交互数据（无生成模型），限制了方法向在线强化学习的直接迁移。
- **假设条件较强**：RB 的 Assumption 2（遍历性、非退化、中性状态唯一、局部稳定性）和 Assumption 3（严格动作值间隙）虽在规划文献中有实例，但验证这些条件对一般 RB 仍非易事。
- **仅考虑无限时域平均奖励**：论文聚焦于无限时域平均奖励模型，不涉及有限时域或折扣模型的结果。
- **参考策略的最优性间隙不可消除**：WCMDP 的 O(1/√N) 间隙是 ID 策略固有的，即使样本量无限大也无法突破；对于指数小间隙的 RB，Assumptions 2-3 需要轻微加强（相比 Hong et al. 2024a）。
- **未考虑约束违反的方差**：分析隐含假设约束在长时间平均下满足，未严格分析短期约束违反的概率或严重程度。

（完）
