---
title: Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks
title_zh: 可证明通信高效且隐私保护的联邦图神经网络
authors: "Zhishuai Guo, Wenhan Wu, Chen Chen, Lei Zhang, Olivera Kotevska, Ravi K Madduri"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26243v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 通信高效的联邦GNN框架，减少嵌入交换频率
tldr: 针对跨机构图数据因隐私限制无法共享，且现有联邦图神经网络（GNN）在处理跨客户端边时面临通信开销大和隐私泄露风险的问题，本文提出了CE-FedGNN框架。该框架通过低频交换聚合节点表征并引入移动平均估计器处理跨客户端依赖，结合度量差分隐私（metric-DP）提供严格的隐私保护。研究证明了其收敛性与极低的通信复杂度，实验表明该方法在保障隐私的同时显著降低了通信成本并保持了优异性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26243v1/fig-001.webp\", \"caption\": \"Table 3: Statistics of the Datasets\", \"page\": 22, \"index\": 1, \"width\": 572, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2605.26243v1/fig-002.webp\", \"caption\": \"Table 1: Average F1 scores on AML datasets across high-illicit (HI) and low-illicit (LI) ratio settings. Best results in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 185}]"
motivation: 现有联邦图神经网络在处理跨客户端边时，要么牺牲模型准确率，要么面临高昂的通信开销与隐私泄露风险。
method: 提出CE-FedGNN框架，通过低频交换聚合表征、引入移动平均估计器处理依赖陈旧性，并采用度量差分隐私提供严格的隐私保障。
result: "理论证明该算法以O(1/√T)速率收敛且通信复杂度为O(T^{3/4})，实验显示其在反洗钱和引用网络数据集上大幅降低通信开销并保持高精度。"
conclusion: CE-FedGNN成功实现了跨机构图数据的高效联邦学习，在保证严格隐私与低通信成本的前提下，有效解决了跨客户端依赖问题。
---

## 摘要
图神经网络（GNNs）在关系数据上取得了优异的性能，但由于隐私和政策限制，现实世界中的图通常分布在无法共享原始数据的各个组织中。现有的联邦GNN方法要么忽略跨客户端链接，导致准确率下降；要么需要频繁交换嵌入，从而产生巨大的通信和隐私成本。我们提出了CE-FedGNN，一个通信高效且隐私保护的联邦GNN框架，用于在此类耦合图上进行学习。我们的方法通过不频繁地交换聚合的节点表示，避免了共享原始数据或每轮的嵌入。为了处理跨客户端的依赖性和陈旧性，我们引入了一个移动平均估计器，该估计器持续跟踪节点表示，并使其能够在各轮之间稳定地复用。为了对发布的表示提供严格的隐私保证，我们采用了度量差分隐私（metric-DP）框架，该框架通过在学习到的嵌入空间中的距离来衡量隐私，而不是基于最坏情况下的输入扰动。这使得在标准差分隐私变得过于保守的噪声水平下，仍能提供有意义的保证。我们证明了其以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度为 $O(T^{3/4})$。此外，我们在公共队列威胁模型下，通过Rényi差分隐私组合推导出了 $(\varepsilon,δ)$-metric-DP 保证。在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN实现了优异的性能，同时显著降低了通信开销，并在隐私保护噪声下保持了鲁棒性。

## Abstract
Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **核心背景**：图神经网络（GNN）在处理关系数据（如金融交易、社交网络）上表现优异，但现实中的图数据往往由于隐私政策（如GDPR）和商业机密分布在不同的机构（客户端）中，无法集中训练。
* **现有痛点**：在联邦学习（FL）环境下训练GNN面临三大挑战：
  1. **跨客户端边的依赖**：忽略跨机构的边会导致全局结构丢失和准确率下降。
  2. **通信开销巨大**：如果每次迭代都交换跨客户端的节点嵌入，会带来难以承受的通信成本。
  3. **隐私泄露风险**：直接交换中间嵌入容易遭受属性推理攻击，而传统的差分隐私（DP）在处理节点嵌入时，由于最坏情况下的敏感度过高，加入满足传统DP的噪声会完全破坏模型效用。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程
* **核心思想**：提出 **CE-FedGNN** 框架，通过**低频交换聚合节点表征**来解耦跨客户端依赖，利用**移动平均估计器**缓解表征陈旧性问题，并引入**度量差分隐私**在适度噪声下提供严格的隐私保护。
* **关键技术细节**：
  * **移动平均估计器**：针对节点表征和随机梯度分别设计了移动平均更新机制。在本地训练时，客户端不再需要实时获取跨客户端邻居的真实嵌入，而是复用其最近一次共享的移动平均嵌入。这不仅将跨客户端交互限制在一跳邻居，还消除了多跳跨客户端采样的必要性。
  * **跨客户端梯度处理**：当采样到跨客户端的边时，两端客户端各自计算涉及本地节点的梯度分量，将跨客户端嵌入视为固定输入。通过适当的采样加权，聚合后的梯度更新可以恢复为无偏估计。
  * **度量差分隐私**：摒弃了基于最坏情况输入扰动的标准DP，采用基于嵌入空间 L2 距离的 metric-DP。通过结合 Rényi 差分隐私（RDP）进行多轮组合，在“公共队列”威胁模型下提供 $(\varepsilon, \delta)$-metric-DP 保证。
* **算法流程**：
  1. 服务器初始化全局模型和全局嵌入缓存。
  2. 客户端并行执行本地更新：接收模型和所需的跨客户端嵌入；在本地小批量训练中，通过移动平均规则更新节点嵌入和梯度估计器，并更新本地模型。
  3. 通信轮次结束时，客户端将更新的模型参数、梯度估计器和边界节点嵌入发送给服务器。
  4. 服务器聚合模型和梯度，并将更新的嵌入路由给相关的邻居客户端。

### 3. 实验设计：数据集、场景与对比方法
* **数据集与场景**：
  * **反洗钱（AML）场景**：由于真实金融数据受限，采用了真实的交易模拟器生成合成银行间金融网络。包含小、中、大规模数据集，并分为高违法比例（HI）和低违法比例（LI）两种极端场景。
  * **引文网络场景**：使用了四个广泛使用的真实基准数据集：Cora, Citeseer, PubMed, MSAcademic。
* **对比方法**：
  * 单客户端本地训练（SC）。
  * 传统联邦学习：FedAvg。
  * 联邦GNN基线：Swift-FedGNN（间歇性全局图训练）、FedGCN（仅在初始化共享嵌入）、FedGNN-ST（朴素共享陈旧嵌入）、FedSage+（本地生成缺失节点）、FedPUB（加权聚合模型更新）。

### 4. 资源与算力
* **明确说明的算力资源**：论文在附录中明确指出，实验是在通过 InfiniBand 连接的集群上运行的，**每台机器使用了一块 NVIDIA A100 GPU**。
* **训练时长**：未明确说明总训练时长，但提供了每轮平均运行时间（例如在 HI-Large 数据集上，CE-FedGNN-PNA 每轮耗时约 164.73 秒）。

### 5. 实验数量与充分性
* **实验数量**：实验涵盖了 2 大类场景（金融反洗钱、引文网络），共计 10 个数据集变体；对比了至少 7 种基线方法；进行了组件消融实验、通信效率测试、隐私噪声容忍度测试（不同 $\sigma_0$）以及抗属性推理攻击（AIA）测试。
* **充分性与客观性**：实验设计非常充分且客观。
  * **公平性**：统一了基线方法的超参数（如本地批次大小、采样数、通信间隔 $K=32$ 等），并在相同硬件下测试运行时间。
  * **全面性**：不仅测试了预测性能（F1 Score），还深入探讨了通信字节数、时间成本、隐私-效用权衡以及理论收敛率的实际表现。

### 6. 论文的主要结论与发现
* **性能优越**：CE-FedGNN 在绝大多数数据集上显著优于所有基线，特别是在极度不平衡的 AML 低违法比例（LI）场景下，其他基线大多崩溃退化为无效预测，而本方法仍保持较高准确率。
* **通信高效**：理论证明了算法以 $O(1/\sqrt{T})$ 速率收敛，通信复杂度仅为 $O(T^{3/4})$。实验证实，即使通信间隔扩大到 $K=1024$，模型性能依然坚挺，大幅降低了通信开销。
* **隐私与效用的平衡**：在注入满足 metric-DP 的噪声后，模型表现出极强的鲁棒性。相比于标准DP，metric-DP 在适度噪声下即可提供有意义的隐私保证（使得节点与其 $k$ 近邻不可区分），且不会导致模型精度大幅下降。

### 7. 优点：方法或实验设计上的亮点
* **理论与工程结合紧密**：巧妙利用移动平均估计器解决了联邦GNN中的“表征陈旧性”难题，同时避免了多跳通信，具备极强的工程落地价值。
* **创新的隐私框架应用**：首次将 metric-DP 引入联邦 GNN 训练，突破了传统 DP 在节点嵌入发布中“加噪即毁模”的瓶颈，为跨机构图学习提供了切实可行的隐私计算范式。
* **贴近现实的评估**：针对金融反洗钱场景中正负样本极度不平衡的痛点进行了专门的大规模实验验证，证明了该框架在复杂工业场景下的潜力。

### 8. 不足与局限
* **数据真实性的局限**：受限于金融监管，反洗钱实验完全依赖合成数据。尽管模拟器很先进，但与真实的动态金融交易网络仍可能存在分布差异。
* **隐私放大效应的缺失**：论文明确指出在公共队列威胁模型下，没有利用子采样放大来增强隐私。如果未来能结合安全聚合或匿名混洗技术，隐私边界还可以进一步收紧。
* **仅限一跳依赖解耦**：方法设计主要针对一跳跨客户端交互，对于更深层次的跨客户端多跳拓扑依赖，其陈旧嵌入的复用效果可能会随着图卷积层数的增加而衰减。

（完）
