---
title: Self-Optimizing Control of Continuous Processes Based on Reinforcement Learning
authors: "Ziqi Zhuo, Junghui Chen, Lei Xie, Hongye Su"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04471v1"
tags: ["query:rl"]
score: 9.0
evidence: 强化学习工业过程控制
tldr: 提出将受控变量结构嵌入Actor网络的强化学习自优化控制方法用于工业过程。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04471v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1346, \"height\": 619}, {\"url\": \"assets/figures/arxiv/2606.04471v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 450, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2606.04471v1/fig-003.webp\", \"caption\": \"\", \"page\": 4, \"index\": 3, \"width\": 750, \"height\": 600}, {\"url\": \"assets/figures/arxiv/2606.04471v1/fig-004.webp\", \"caption\": \"\", \"page\": 5, \"index\": 4, \"width\": 2541, \"height\": 1985}, {\"url\": \"assets/figures/arxiv/2606.04471v1/fig-005.webp\", \"caption\": \"\", \"page\": 5, \"index\": 5, \"width\": 1200, \"height\": 750}, {\"url\": \"assets/figures/arxiv/2606.04471v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 1800, \"height\": 1200}, {\"url\": \"assets/figures/arxiv/2606.04471v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 1800, \"height\": 678}]"
motivation: 强化学习工业过程控制。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
This paper addresses the Self-Optimizing Control (SOC) problem in industrial continuous processes and proposes a Reinforcement-Learning (RL)-based SOC approach to improve dynamic performance under high-frequency disturbances. In the proposed framework, the SOC controlled variable structure is embedded in the Actor network, and reward functions are designed based on economic indicators. Through interaction with the environment, the RL agent optimizes controlled variables while implicitly considering implementability and steady-state uniqueness. Online fine-tuning is further introduced to alleviate model mismatch. Experiments on a continuous stirred-tank reactor with disturbances compare the proposed RL-based SOC method with the Objective-Guided Controlled Variable Learning Approach based on steady-state data. The results show that the RL method achieves improved dynamic performance under real-time disturbances, generates smooth controlled variable outputs without explicit regularization, reduces hyperparameter-tuning complexity, and enhances adaptability through online adjustment. Overall, the proposed RL-based SOC approach provides an effective solution for nonlinear process control and offers a promising reference for future studies involving multiple disturbances, multiple operating conditions, and model-free scenarios.

---

## 论文详细总结（自动生成）

# 论文总结：基于强化学习的连续过程自优化控制

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：工业连续过程中，优化与控制通常分为上层实时优化（RTO）和下层控制器（如PID）两层结构。RTO在稳态下根据测量扰动求解优化问题得到最优设定点，但在高频率扰动下表现不佳，会导致控制偏差或经济损失。
- **核心问题**：传统自优化控制（SOC）通过定义受控变量为原始变量的线性或非线性组合并赋予固定设定点，避免在高频率扰动下频繁优化。然而现有方法存在三大挑战：
  1. 传统SOC仅关注稳态优化，忽略分布式动态过程中的动态性能；
  2. 为保证可实现性和解的唯一性，需要显式正则化项，但权重调参困难；
  3. 离线设计的模型与实际过程存在失配，需在线微调能力。
- **研究动机**：提出一种基于强化学习（RL）的SOC方法，使受控变量结构嵌入Actor网络，通过经济指标设计奖励函数，隐式考虑可实现性和稳态唯一性，并支持在线微调以缓解模型失配。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

### 核心思想

将SOC的受控变量结构 $c = h(\theta, y)$ 嵌入RL的Actor网络，其中 $c = u - f(\theta, y)$，$f(\theta, y)$ 由Actor网络输出。通过与环境的交互，RL智能体利用基于经济指标的累积奖励优化受控变量，同时隐式考虑可实现性和稳态唯一性。

### 关键技术细节

**动态过程建模**：

- 连续过程的动态方程表示为：
  $$\dot{x}_t = m(x_t, u_t, d_t), \quad y_t = g(x_t, u_t, d_t)$$
  其中 $x_t$ 为状态，$u_t$ 为操纵变量，$d_t$ 为时变扰动，$y_t$ 为测量输出。

**受控变量结构**：

- 结构定义为 $c = h(\theta, y) = u - f(\theta, y)$
- $f(\theta, y)$ 用多层感知器（MLP）实现，作为Actor网络的一部分。

**RL算法选择**：

- 采用近端策略优化（PPO）算法，其裁剪目标函数为：
  $$L^{CLIP}_t(\theta) = E_t \left[ \min(r_t(\theta)\hat{A}_t, \text{clip}(r_t(\theta), 1-\epsilon, 1+\epsilon)\hat{A}_t) \right]$$
- 完整PPO损失包含策略裁剪损失、价值函数损失和策略熵正则化项。

**奖励函数设计**：

- 奖励函数基于经济指标：
  $$r = (-w_u u^2 - w_c (C_A - C_{A,ref})^2) \cdot \frac{\exp(\alpha t_T) - 1}{\exp(\alpha) - 1}$$
- 其中 $C_A$ 为反应器中反应物浓度，$C_{A,ref}$ 为参考浓度，$\frac{\exp(\alpha t_T) - 1}{\exp(\alpha) - 1}$ 为时间依赖权重因子，逐步增加后期近稳态阶段的奖励贡献。

### 算法流程

1. **离线SOC设计**：基于机理模型，使用PPO算法在逐步环境中训练RL智能体，优化 $f(\theta, y)$，得到 $u^* = f(\theta, y)$，设定点 $c_s = 0$。
2. **在线部署**：将训练好的 $f(\theta, y)$ 嵌入 $c = u - f(\theta, y)$，PI控制器维持 $c$ 接近设定点 $c_s = 0$，实现对扰动的鲁棒控制。
3. **在线微调**：收集真实过程数据 $\{y_k, r_{k-1}, y_{k-1}, u_{k-1}\}$，用于RL策略优化算法的在线微调，快速适应模型失配场景。

## 3. 实验设计：数据集、场景、 Benchmark 与对比方法

### 研究对象

- 采用带有扰动的连续搅拌釜反应器（CSTR）作为被控过程。
- 扰动设置为进料温度 $T_i(t) \sim N(\mu_{T_i} = 66.0, \sigma^2_{T_i} = 8)$。
- 输出测量为 $y = \{T, T_C\}$（反应器温度和夹套温度），操纵变量 $u$ 为阀门开度（范围0~1）。

### 对比方法

1. **Objective-Guided Controlled Variable Learning Approach（OGCVL）**：基于稳态数据的受控变量学习方法，作为基准方法。
2. **Real-Time Optimization（RTO）**：在特定进料温度下使用稳态模型优化夹套温度设定点。

### 性能评估指标

- 平均奖励和奖励方差（最后10,000步）。
- 受控变量输出的平滑性。
- 在线微调后的适应能力。

## 4. 资源与算力

论文中**未明确说明**训练所使用的硬件资源（GPU型号、数量）、训练时长或计算开销等具体算力信息。

## 5. 实验数量与充分性

### 实验数量

- **稳态性能验证**：比较不同 $T_i$ 下OGCVL训练的 $u$ 与最优 $u_{opt}$ 的收敛性。
- **动态性能对比**：在相同 $T_i$ 序列下，对比RL-based SOC、OGCVL和RTO的动态表现（包含 $C_A$、$T$、$T_C$、$u$ 和单步奖励的时间曲线）。
- **平滑性比较**：对比RL-based SOC与基准方法中 $f(\theta, y)$ 输出的波动幅度。
- **在线微调实验**：在模型参数失配（$V$ 从7.08变为6.90）条件下，对比预训练+微调与直接在线训练的效果。

### 实验充分性

- 实验覆盖了离线设计、在线部署和模型失配场景，**较为充分**。
- 通过CSTR这一典型非线性过程验证了方法的有效性，但仅在单一系统上测试，**普适性有待更多系统验证**。
- 对比实验包含基准方法和RTO，**较为公平**。

## 6. 论文的主要结论与发现

1. **动态性能提升**：RL-based SOC在高频扰动下实现了更优的平均奖励（-0.84 vs OGCVL的-0.90 vs RTO的-3.23）和更低的奖励方差（0.72 vs OGCVL的1.37 vs RTO的4.82）。
2. **输出平滑性**：RL-based SOC中 $f(\theta, y)$ 的波动幅度约为0.005，远小于OGCVVL的0.05，无需显式正则化即可产生平滑的受控变量输出。
3. **超参数调参简化**：RL通过迭代策略更新隐式处理可实现性和唯一性约束，减少了对显式正则化权重的手动调参需求。
4. **在线适应能力**：在模型失配情况下，预训练+在线微调策略优于直接在线训练，能够快速补偿模型偏差，提升经济性能。
5. **整体优势**：RL-based SOC在非线性过程控制中提供了有效解决方案，为多扰动、多工况和无模型场景提供了有前景的参考。

## 7. 优点

- **隐式正则化**：通过RL的迭代策略更新自然实现可实现性和解的唯一性，避免了复杂的正则化权重调参。
- **动态性能优化**：奖励函数结合时间依赖权重因子，兼顾瞬态和稳态阶段，实现动态轨迹的整体优化。
- **在线微调能力**：支持模型失配情况下的实时调整，增强实际应用中的鲁棒性和适应性。
- **框架通用性**：受控变量结构 $c = u - f(\theta, y)$ 可灵活扩展至其他非线性过程和RL算法。

## 8. 不足与局限

- **算力信息缺失**：论文未提供训练所需算力资源信息，难以评估方法的计算成本和可部署性。
- **实验系统单一**：仅在CSTR上验证，方法在更复杂工业过程（如多变量耦合系统）中的有效性尚未验证。
- **无模型场景未解决**：当无机理模型仅有历史数据时，如何实现RL-based SOC仍是未来研究方向。
- **多扰动多工况扩展**：论文提到需扩展至多扰动、多工况场景，但具体实现方案未给出。
- **安全与稳定性保证**：在线微调过程中缺乏安全性约束分析，可能在某些极端扰动下导致性能下降。

（完）
