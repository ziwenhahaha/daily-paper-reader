---
title: Representation Learning Enables Scalable Multitask Deep Reinforcement Learning
authors: "Johan Obando-Ceron, Lu Li, Scott Fujimoto, Pierre-Luc Bacon, Aaron Courville, Pablo Samuel Castro"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05555v1"
tags: ["query:rl"]
score: 9.0
evidence: 用于可扩展多任务设置的表征学习深度强化学习
tldr: 表明表征学习可实现可扩展多任务深度强化学习，无需规划即可提升性能。
source: arxiv
selection_source: fresh_fetch
motivation: 用于可扩展多任务设置的表征学习深度强化学习。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Scaling reinforcement learning (RL) to diverse multitask settings remains a central challenge. While recent advances in model-based RL achieve strong performance, they rely on planning and complex training pipelines, making it unclear which components are essential for scalability. We revisit this question and argue that the primary driver of scalable multitask RL is not model-based control, but \emph{representation learning}. In particular, we show that combining predictive, model-based representations with high-capacity value function approximation is sufficient to achieve strong performance, even without planning. We evaluate a simple model-free algorithm, MR.Q, coupled with auxiliary predictive objectives into a scalable actor-critic architecture. This approach outperforms a recent world-model-based method and a range of deep RL baselines across a diverse suite of multitask continuous control tasks, while significantly reducing computational overhead and improving wall-clock efficiency. We observe consistent improvements with increased model capacity and show through ablations that predictive representation learning is critical for performance.

---

## 论文详细总结（自动生成）

# 论文总结：Representation Learning Enables Scalable Multitask Deep Reinforcement Learning

## 1. 核心问题与整体含义（研究动机和背景）

- **核心挑战**：将深度强化学习扩展到多样化的多任务设置仍然是核心挑战
- **现有方法的局限**：基于模型的RL方法（如Dreamer、TD-MPC2、Newt）虽然取得强性能，但依赖规划和复杂的训练流程，导致计算开销大、内存占用高、实现复杂，且模型误差会随想象轨迹累积
- **核心假设**：论文假设基于模型的RL的许多收益实际上来自其学习的表征，而非规划本身；预测目标本身可能足以实现有竞争力的样本效率
- **研究目标**：探究纯模型自由的方法（仅使用预测性表征学习，不进行规划）能否在多任务设置中匹配甚至超越世界模型方法的性能

## 2. 论文提出的方法论

### 核心思想
- 将**预测性、基于模型的表征**与**高容量值函数近似**相结合，无需规划即可实现强性能
- 关键洞察：预测目标提供的密集监督信号可以塑造表征，使其捕获环境动态和时间结构

### 关键技术细节
- **MR.Q算法**：基于TD3的模型自由智能体，集成预测性辅助目标
- **潜在空间建模**：观察被编码为潜在表示 $z_t = \phi_\xi(s_t, \tau)$，策略和值函数在潜在空间操作
- **辅助预测目标**：训练模型预测 $(z_{t+1}, r_t, d_t)$ 从 $(z_t, a_t)$，包括动力学模型、奖励预测和终止预测
- **不使用规划**：学习到的模型仅用于塑造表征，不执行潜在展开或轨迹优化

### 算法流程（文字说明）
1. 观察 $s_t$ 和任务信息 $\tau$ 被编码为潜在表示 $z_t$
2. 使用TD学习训练双Critic（Q函数）估计状态-动作对的值
3. Actor根据Critic的估计更新策略以最大化期望回报
4. **额外监督**：辅助网络预测下一个潜在状态、奖励和终止信号，这些梯度反向传播通过编码器
5. 预测目标提供密集监督，鼓励表征捕获时间一致性和动态结构

## 3. 实验设计

### 数据集/场景
- **MMBench基准测试**：涵盖10个不同领域的200个多任务连续控制任务
  - MuJoCo：机器人运动控制（6个任务）
  - DMControl和DMControl Extended：物理模拟控制
  - MetaWorld：机器人操作任务（49个）
  - ManiSkill3：复杂机器人控制
  - PyGame：22个街机风格游戏任务
  - Box2D：2D刚体物理任务
  - RoboDesk：桌面操作任务（9个）
  - Atari：经典雅达利游戏
  - OGBench：目标条件RL基准

### Benchmark设置
- **主要评估**：10M环境步骤的低数据 regime（而非常用的100M+步骤）
- **扩展训练**：部分实验评估至50M步骤
- **视觉输入实验**：使用预训练DINOv2编码器处理像素输入

### 对比方法
- **Newt**：基于TD-MPC2的世界模型方法（当前最先进的多任务世界模型）
- **TD3**：无编码器的基线（使用原始状态+语言嵌入）
- **PPO**：标准策略梯度方法
- **其他深度RL基线**

## 4. 资源与算力

- **GPU型号**：NVIDIA A100
- **内存配置**：约24-48 GB
- **训练时长**：取决于基准和模型大小，每个run约需12-60小时
- **分布式训练**：使用Slurm集群，每个任务在单GPU上训练
- **种子数量**：所有结果平均超过5个随机种子
- **注**：论文未详细说明具体使用的GPU数量或总计算预算

## 5. 实验数量与充分性

### 实验类型与数量
- **多领域评估**：10个不同领域的多任务学习曲线
- **单任务缩放实验**：4种网络规模（Small、Medium、Large、X-Large）× 2个环境（HalfCheetah、Humanoid）
- **消融实验**：
  - 移除编码器（TD3 vs MR.Q）
  - 移除预测目标的影响
- **视觉输入实验**：5个领域的像素基实验
- **规模扩展实验**：
  - 200任务大规模训练
  - 数据规模变化（不同数据集大小）
  - 模型规模变化（256、512、1024隐维度）
  - UTD比率变化（0.025到0.075）
- **迁移实验**：28个保留任务的少样本微调
- **计算效率实验**：墙钟时间vs性能比较
- **每任务学习曲线**：覆盖所有任务域的细粒度分析

### 实验充分性与客观性
- ✅ 覆盖广泛的任务域和规模设置
- ✅ 使用95%置信区间报告不确定性
- ✅ 5个随机种子的统计显著性
- ✅ 多维度消融分析（表征几何、训练动态、神经元活跃度）
- ✅ 包含正负对照（Newt作为强基线，TD3作为无编码器基线）
- ✅ 视觉和状态两种输入模态的验证

## 6. 论文的主要结论与发现

### 核心发现
1. **预测性表征学习是关键**：结合预测目标的高容量模型自由方法可以匹配或超越基于世界模型的方法
2. **无需规划即可实现可扩展性**：MR.Q在10M步骤的低数据regime中一致地匹配或超越Newt
3. **表征质量驱动缩放性能**：有预测目标的PPO在不同网络规模下持续改进，而无预测目标的PPO在增大模型时收益甚微

### 性能对比
- **样本效率**：MR.Q在训练早期阶段（2M步骤时相对提升37%）和整个训练过程中始终优于Newt
- **计算效率**：MR.Q达到更高性能的时间显著早于Newt
- **模型和数据缩放**：MR.Q在减少数据和模型大小时更鲁棒
- **迁移能力**：
  - 零样本性能：MR.Q (0.13) vs Newt (0.09)，提升50%
  - 微调后：200k步骤时MR.Q (0.62) vs Newt (0.55)，提升13%
  - 在28个保留任务中的61%上获胜

### 表征分析
- **SRank（表示秩）**：MR.Q保持更高的表示秩，表明表征容量更好
- **有效维度**：MR.Q的95%-d显著更高（DMControl: 89 vs 21；MuJoCo: 66 vs 15）
- **休眠神经元**：MR.Q的休眠神经元比例更低，表明网络容量利用更充分
- **价值损失**：MR.Q的价值学习更稳定

## 7. 优点

### 方法亮点
- **简洁有效**：纯模型自由方法，无需复杂的世界模型和规划模块
- **计算效率高**：避免潜在展开和轨迹优化，显著降低训练时间和内存开销
- **可扩展性强**：随着模型容量和数据增加持续改进，表现可预测
- **归因清晰**：成功将"预测性表征学习"的贡献从"规划"中解耦出来

### 实验设计亮点
- **低数据regime评估**：在10M步骤评估，更贴近实际应用场景
- **多维度分析**：从表征几何、训练动态、神经元活跃度等多角度验证
- **大规模验证**：200任务的大规模设置验证可扩展性
- **综合消融**：编码器移除、预测目标移除等多组对照实验
- **双模态验证**：状态和像素输入两种设置的验证

## 8. 不足与局限

### 实验覆盖局限
- 主要集中在**连续控制领域**，对更广泛域（如长horizon环境、稀疏奖励任务、推理密集型任务）的泛化性未充分验证
- 未探索**竞争性多智能体设置**或**社交交互场景**

### 方法局限
- **理论理解不足**：虽然实验表明预测目标改善了表征质量和缩放行为，但潜在机制尚未完全理解
- **长期依赖任务**：论文未评估需要长期信用分配的任务
- **混合方法未探索**：结合模型自由表征学习与潜在规划的混合方法可能提供互补优势

### 偏差与风险
- **基准偏差**：MMBench可能偏向于特定类型的任务分布
- **评估协议**：主要在在线RL设置评估，对离线或模仿学习设置未探索
- **超参数敏感性**：虽然展示了鲁棒性，但不同任务域的最优超参数可能不同

### 应用限制
- **视觉输入依赖预训练编码器**：在真实世界部署可能需要针对特定领域的视觉编码器微调
- **连续动作空间**：对离散动作空间任务（如Atari）的适用性相对有限
- **任务条件依赖**：需要语言指令或任务标识符，在完全无监督的开放域设置中可能受限

---

（完）
