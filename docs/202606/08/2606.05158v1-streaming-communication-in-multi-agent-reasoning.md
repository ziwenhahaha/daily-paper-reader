---
title: Streaming Communication in Multi-Agent Reasoning
title_zh: 多智能体推理中的流式通信
authors: "Zhen Yang, Xiaogang Xu, Wen Wang, Cong Chen, Xander Xu, Ying-Cong Chen"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05158v1"
tags: ["query:mas"]
score: 9.0
evidence: 流式多智能体推理通信
tldr: StreamMA流水线化多智能体推理步骤以降低延迟提高精度。
source: arxiv
selection_source: fresh_fetch
motivation: 流式多智能体推理通信。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Multi-agent reasoning systems adopt a "generate-then-transfer" paradigm that forces end-to-end latency to scale linearly with pipeline depth. We introduce StreamMA, a multi-agent reasoning system that streams each reasoning step to downstream agents as soon as it is generated, pipelining adjacent agents and thus reducing latency. Surprisingly, this pipelining also improves effectiveness: because multi-step reasoning quality is non-uniform and early steps are more reliable than later ones, working with these reliable early steps instead of the full chain prevents error-prone late steps from misleading downstream agents. We formalize both advantages with the first closed-form joint analysis of stream, serial, and single protocols, deriving the effectiveness ordering, speedup upper bound, and cost ratio. Across eight reasoning benchmarks spanning mathematics, science, and code, two frontier LLMs (Claude Opus 4.6 and GPT-5.4), and three topologies (Chain, Tree, Graph), StreamMA outperforms both baselines (avg. +7.3 pp, max +22.4 pp on HMMT 2026; Claude Opus 4.6-high). Beyond these contributions, we discover a "step-level scaling law": increasing per-agent steps consistently improves both effectiveness and efficiency, a new scaling dimension orthogonal to and composable with agent-count scaling.

---

## 论文详细总结（自动生成）

# 《Streaming Communication in Multi-Agent Reasoning》论文详细总结

## 1. 论文的核心问题与整体含义

**研究背景与动机：**
- 当前多智能体推理系统普遍采用 **"generate-then-transfer"（串行）范式**：上游智能体必须完成整个响应后，才能将完整结果传递给下游智能体
- 这种范式导致两大问题：
  1. **延迟线性增长**：端到端延迟随流水线深度线性增加
  2. **强制接收有害上下文**：下游智能体被迫接收上游的完整输出，包括可靠性下降的后期推理步骤

**核心洞察：**
- 论文挑战"更多信息带来更好决策"的传统直觉，提出在多步推理中，**上下文到达的时机比上下文数量更重要**
- 早期推理步骤通常更可靠，而后期步骤错误率上升；利用可靠的早期步骤而非完整链，可以防止错误传播

---

## 2. 论文提出的方法论

### 核心思想
- **StreamMA**：流式多智能体推理系统，将通信粒度从"完整响应"细化到"推理步骤"
- **Stream协议**：每生成一个推理步骤就立即转发给下游智能体，实现流水线并行

### 关键技术细节

**算法流程（Chain拓扑）：**
1. 根智能体接收查询，产生多步推理
2. 每完成一步，立即将该步骤推入下游智能体的队列
3. 下游智能体收到步骤后立即开始处理，无需等待上游完成
4. 所有智能体并发执行，形成流水线

**理论框架（三个定理）：**

| 定理 | 内容 | 公式/条件 |
|------|------|----------|
| **Theorem 1**（有效性排序） | 根据 p_head、p_tail、p̄ 与 p* 的关系区分六种情况 | p* = ε/(δ+ε)，p_head > p* 且 p_tail < p* 时 Stream 最优 |
| **Theorem 2**（加速比上界） | Stream 相对 Serial 的延迟加速比 | Speedup ≤ A(S+r_po)r_vdp + S / (S+A-1)(1+αr_vdp+βr_vdc) |
| **Theorem 3**（成本比率） | Stream 相对 Serial 的成本 | Cost_ratio = [ρ·r_cpd(α+r_ccpβ)+1] / [r_cpd(1+r_po/S)+1] |

**关键参数定义：**
- p_j：第 j 步的正确概率
- p_head：头部加权平均正确率（早期步骤权重更高）
- p_tail：尾部加权平均正确率（后期步骤权重更高）
- δ：正确步骤带来的下游增益
- ε：错误步骤带来的下游损失
- α、β：非缓存/缓存上下文令牌数与输出令牌数的比率

---

## 3. 实验设计

### 数据集与 Benchmark
| 类别 | 数据集 | 说明 |
|------|--------|------|
| **竞赛数学** | AIME 2025, AIME 2026, HMMT 2026 | 高中/大学数学竞赛题 |
| **研究生科学** | GPQA-Diamond, HLE | 研究生水平科学问答 |
| **代码理解** | LiveCodeBench (LCB-G/E/T) | 代码生成、执行、测试输出预测 |

### 对比方法
| 方法 | 描述 |
|------|------|
| **Single** | 单智能体，仅接收原始查询 |
| **Serial** | 串行协议，上游完成完整响应后才传递 |
| **StreamMA** | 本文方法，流式传递推理步骤 |

### 拓扑结构
- **Chain**：线性链 A→B→C→D
- **Tree**：分支结构 A→{B,C}→D
- **Graph**：带捷径的图 A→B→C→D, A→C

### 模型
- **Claude Opus 4.6** (high)
- **GPT-5.4** (medium)

---

## 4. 资源与算力

**论文中未明确说明本地 GPU 资源或训练算力**：
- 实验使用商业 API（Anthropic Claude API、OpenAI GPT API）
- 未报告 GPU 型号、数量或训练时长
- 通过 API 调用获取模型推理能力，未进行本地训练

---

## 5. 实验数量与充分性

### 实验规模
- 每个 {backbone, topology, method} 配置：3 次独立运行
- AIME 2025/2026 和 HMMT 2026（测试集较小）：8 次运行
- 步级缩放实验：6×6 配置网格（A ∈ {2,4,8,16,32,64}, S ∈ {2,4,8,16,32,64,auto}）
- 成本分析实验：3×3 配置（N ∈ {1,4,16} 链副本 + 投票）

### 实验充分性评估
| 实验类型 | 充分性 | 说明 |
|----------|--------|------|
| 主实验 | ✓ | 8 个基准 × 2 模型 × 3 拓扑，跨领域覆盖（数学/科学/代码） |
| 消融实验 | ✓ | 步级扰动实验验证 Theorem 1 的 head-tail 不对称性 |
| 案例研究 | ✓ | GPQA-Diamond 题目详细分析，验证 Stream 优势 |
| 缩放实验 | ✓ | 步级和智能体数双维度缩放，覆盖 36 种配置 |
| 成本分析 | ✓ | Pareto 前沿分析，KV-cache 影响评估 |

**客观性与公平性**：
- StreamMA 仅添加 END_STEP 边界符和简短 solver 前言，prompt 内容、解码超参数与 Serial 完全相同
- 消融实验使用受控扰动，排除了 prompt 工程的干扰

---

## 6. 论文的主要结论与发现

### 核心结论

| 结论 | 数据支撑 |
|------|----------|
| **有效性提升** | StreamMA 平均提升 +7.3 pp（最高 +22.4 pp on HMMT 2026，Claude Opus 4.6） |
| **效率提升** | A=64, S=64 时达到 26.9× 加速（理论上界 32.3× 的 83%） |
| **成本优势** | ρ=1 时 Stream 节省 7.5% 成本（full KV-cache） |

### 关键发现

**1. 步级缩放定律（Step-Level Scaling Law）**
- 在固定智能体数 A 下，增加每智能体步数 S **同时**提升有效性和效率
- 这是与"智能体数缩放"正交的新维度，且可组合：
  - A=64, S=auto: 68.2%
  - A=64, S=64: 73.5%（+5.3 pp 补充增益）

**2. Stream 优势的机制**
- 早期步骤可靠性高（p_head > p*），后期步骤可靠性下降（p_tail < p*）
- Stream 让下游智能体从可靠前缀开始独立推理，稀释后期错误的影响

**3. 步级扰动实验验证**
- 扰动尾部步骤：Stream 领先 Serial 达 +24.0 pp
- 扰龙头部步骤：Serial 反超 Stream 达 -36.0 pp
- 证实了 Theorem 1 的 head-tail 不对称性预测

---

## 7. 优点

| 优点 | 详细说明 |
|------|----------|
| **理论与实证并重** | 提供三个闭合形式定理（有效性/加速比/成本），并通过大规模实验验证 |
| **反直觉的发现** | 揭示"流式传递反而提升质量"这一反直觉现象，深化对多步推理误差累积的理解 |
| **新的缩放维度** | 发现步级缩放定律，拓展了多智能体系统的设计空间 |
| **通用性设计** | Stream 协议可扩展至任意 DAG 拓扑，不局限于 Chain |
| **基础设施友好** | 利用 KV-cache 复用降低成本，与现代 serving 栈（vLLM、SGLang）兼容 |
| **Pareto 最优** | Stream × 4 ($2.75, 90.9%) 超越 Serial × 16 ($5.46, 89.4%)，同时降低成本 |
| **协议选择器** | Theorem 1 可作为原则性协议选择依据，根据步正确率分布选择最优执行模式 |

---

## 8. 不足与局限

| 局限类型 | 详细说明 |
|----------|----------|
| **任务适用性** | 仅适用于可分解为步骤的推理任务；开放式创意写作、单 token 分类等任务不适用 |
| **非通用最优** | Stream 并非在所有步正确率分布下都最优；Case II（Serial 优势）和 Case III（Single 优势）存在 |
| **KV-cache 依赖** | 成本优势需要高 KV-cache 命中率；无 cache 时 Stream 可能贵 37.5% |
| **API 调用增加** | Stream 产生更多 API 调用（每下游智能体 S 次），增加了调度复杂度 |
| **扰动风险** | 步级扰动实验暴露潜在安全风险：恶意注入中间步骤错误可导致最终输出偏离 |
| **推理粒度依赖** | 需要预定义"推理步骤"的结构化输出，依赖于 CoT prompting 的有效性 |
| **资源透明度** | 未公开本地算力需求，实验可复现性受限于 API 访问 |

---

（完）
