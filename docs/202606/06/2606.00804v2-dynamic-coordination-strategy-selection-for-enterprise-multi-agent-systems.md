---
title: Dynamic Coordination Strategy Selection for Enterprise Multi-Agent Systems
authors: Thanh Luong Tuan
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00804v2"
tags: ["query:mas"]
score: 9.0
evidence: 直接研究企业多智能体协调策略选择
tldr: 企业多智能体系统存在多种协调模式（共识、辩论、综合、单智能体工作流），但实践中缺乏何时切换策略的证据。研究采用冻结矩阵法，在6个行业、5类问题、4种执行条件下测试30个企业任务，对比4种模型在4种协调条件下的表现，发现动态路由策略虽非严格最优，但在所有模型和问题类型中都始终保持与最优策略在0.10分以内，同时结构化任务的单智能体方案优于共识机制。该结论支持将动态路由作为企业协调策略的校准默认方案。
source: arxiv
selection_source: fresh_fetch
motivation: 企业多智能体系统面临多种协调模式选择难题，但缺乏何时适用哪种策略的实证依据。
method: 设计包含30个企业任务、跨越6个行业5类问题的冻结矩阵，测试4种模型在4种协调条件下的1440个输出，以固定Sonnet标准评判质量。
result: 动态路由策略始终接近最优（差距<0.10分），但无法严格选出确定性获胜策略；结构化任务偏好单智能体而非共识机制；越南语与英语任务表现一致。
conclusion: 企业协调策略应采用动态路由作为校准默认，而非追求确定性最优策略选择规则。
---

## Abstract
Enterprise multi-agent systems increasingly expose multiple coordination patterns, but deployments often lack evidence for when to use consensus, debate, synthesis, or a simpler single-agent workflow. This paper evaluates whether coordination strategy should be selected dynamically by problem class rather than fixed globally. We run a frozen matrix of 30 enterprise tasks spanning six industries, five problem classes, four execution conditions, three replications per cell, and four model arms: qwen_local, sonnet, gemma_openrouter, and an auxiliary openai cloud-validation arm. All 1,440 generated outputs are judged by a fixed Sonnet rubric.   The main finding is bounded and operationally useful, but it is not the original strict H1. The pre-registered exact-winner/CI criterion is not supported: exact winner identity is unstable across model arms, and several predicted strategies are close to, but not above, the best observed alternative. A weaker near-best routing claim is strongly supported. In every pre-registered model arm and problem class, and again in the auxiliary OpenAI validation arm, the predicted strategy is within 0.10 quality-score points of the best observed condition. Structured compliance verification is the clearest exception to the original mapping: all arms favor single_agent rather than consensus. A pre-registered Kendall's W test finds no reliable difference between Vietnamese-domain and English-domain tasks in how consistently the four coordination conditions are ranked (mean W of 0.20 in both strata; signed-rank p = .85), so H2 is not supported. We conclude that enterprise coordination policy should use dynamic routing as a calibrated default, not as a deterministic winner-selection law.

---

## 论文详细总结（自动生成）

# 论文总结：企业多智能体系统的动态协调策略选择

## 1. 核心问题与整体含义

**研究背景**：企业多智能体语言模型系统日益普及，包含多种协调原语（共识、辩论、投票、综合等）。然而，实践中缺乏关于何时应选择哪种协调模式的实证依据。使用错误的协调模式可能导致延迟、成本增加、冗长或集成错误，而不一定能提升答案质量。

**核心问题**：协调策略是否应按问题类别动态选择，而非固定为全局策略？

**研究动机**：在受监管领域、多语言环境和自动化工作流中，所需输出可能是清单、风险权衡、创新设计或澄清问题，而非单一事实答案，因此协调策略的精准选择尤为关键。

---

## 2. 方法论

**核心思想**：将协调策略选择视为**策略校准问题**（policy-calibration problem），而非对多智能体系统的全面公投。通过实证测试预注册的问题类别-策略映射是否有效。

**关键技术细节**：

- **冻结矩阵法（Frozen Matrix）**：设计完整实验矩阵，所有输出用固定评判标准评分
- **预注册策略映射**：
  | 问题类别 | 定义 | 预测策略 |
  |---------|------|---------|
  | PC1 | 高不确定性风险决策 | consensus |
  | PC2 | 冲突目标权衡 | debate |
  | PC3 | 创新设计 | synthesis |
  | PC4 | 结构化合规验证 | consensus |
  | PC5 | 模糊需求澄清 | synthesis |

- **容忍带规则（Tolerance Band）**：预测策略在最佳策略的0.10分以内视为"near-best"命中

**统计分析**：
- Friedman置换检验（5,000次重复）检测策略效应
- 配对Bootstrap置信区间（5,000次重采样）
- 精确符号秩检验（含Bonferroni校正）

---

## 3. 实验设计

**数据集/场景**：
- 30个企业任务，覆盖6个行业（包括英语和越南语领域）
- 5类预注册问题类别
- 4种协调条件（single_agent、consensus、debate、synthesis）

**Benchmark**：
- 4种模型臂对比：Qwen local（预注册次级臂）、Sonnet（主臂）、Gemma/OpenRouter（第三臂）、OpenAI（辅助云验证臂）
- 每单元3次重复

**对比方法**：
- 不同协调策略之间的比较
- 不同模型臂之间的交叉验证
- 越南语vs英语语言领域的分层比较（H2假设）

---

## 4. 资源与算力

**算力信息**：论文未明确说明GPU型号、数量或训练时长等硬件资源信息。

---

## 5. 实验数量与充分性

**实验规模**：
- 总计1,440个有效评判输出（4模型 × 5问题类 × 4协调条件 × 3重复 × 6行业）
- 15个预注册（模型，问题类）细胞对
- 62个PC4材料协调危害行编码
- 33个PC2材料协调危害行编码

**充分性评估**：
- 实验设计**较为充分**：多模型臂交叉验证、多语言领域分层测试、多种统计分析方法
- **预注册设计**增强了结果可信度
- 独立GPT-5.1第二评判者敏感性研究验证结果稳健性
- **客观性**：使用固定评判标准，统计分析流程标准化（固定随机种子20260527）

---

## 6. 主要结论与发现

**核心发现**：
1. **严格精确获胜者假设（H1）不成立**：预测策略并非始终是精确最佳条件，Bootstrap区间不支持全局优越性声明
2. **近最优路由假设成立**：在所有预注册模型臂和辅助OpenAI臂中，预测策略都在最佳策略的0.10分以内
3. **PC4为协调危害例外**：结构化合规验证在所有四模型臂中一致偏好single_agent而非consensus，原因是协调机制引入"引用越界"（添加不正确的法规或制裁清单引用）

**策略修订建议**：
| 原类别 | 原预测策略 | 实证修订 |
|-------|-----------|---------|
| PC4：结构化合规验证 | consensus | single_agent |
| PC2：冲突目标权衡 | debate | 需细分亚型 |

**PC2亚型拆分**：
- **PC2-a（对抗性决策）**：选择不兼容选项→推荐debate
- **PC2-b（平衡运营姿态）**：保留多目标→推荐consensus或synthesis

**H2假设未获支持**：越南语与英语领域在策略排名一致性上无显著差异（平均Kendall's W均为0.20）

---

## 7. 优点

1. **系统化实验设计**：采用完整的冻结矩阵方法论，变量控制严谨
2. **预注册分析框架**：预注册假设和统计分析流程增强了结果可重复性和可信度
3. **多模型臂验证**：跨多个模型臂的交叉验证提高结论稳健性
4. **第二评判者敏感性研究**：GPT-5.1独立评判确认主要发现非评判者偏见
5. **机制解释深入**：系统性编码协调危害行的失败模式（如"引用越界"机制）
6. **实用政策建议**：直接面向企业部署提供可操作的分层路由建议

---

## 8. 不足与局限

1. **固定评判者偏见**：所有输出由单一Sonnet评判，可能偏向Sonnet风格偏好
2. **任务语料库有限**：30个任务为精选基准，结论应解释为特定企业任务族的政策证据，而非通用证明
3. **策略实现单一**：共识、辩论、综合的具体实现（提示词、代理数量、仲裁者、聚合策略）可能影响结果泛化性
4. **统计模型较简单**：使用依赖轻量测试的统计框架，尚未采用更丰富的统计模型
5. **领域覆盖有限**：主要覆盖金融、保险、医疗、软件、银行领域，对其他监管领域的适用性待验证
6. **模型版本未说明**：未明确各模型臂的具体版本，可能影响结果可重复性
7. **缺乏实时适应机制**：研究关注静态路由表，未探索随置信度或任务复杂度动态调整容忍带的机制

---

（完）
