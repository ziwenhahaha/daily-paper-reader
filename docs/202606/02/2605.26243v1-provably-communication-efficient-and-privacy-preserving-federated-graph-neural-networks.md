---
title: Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks
title_zh: 可证明通信高效且隐私保护的联邦图神经网络
authors: "Zhishuai Guo, Wenhan Wu, Chen Chen, Lei Zhang, Olivera Kotevska, Ravi K Madduri"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26243v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 通信高效且隐私保护的联邦图神经网络框架
tldr: 针对跨机构图数据联邦学习中存在的跨客户端边处理难、通信开销大和隐私泄露风险高的问题，本文提出了CE-FedGNN框架。该框架通过低频交换聚合节点表征来降低通信成本，并引入移动平均估计器处理跨客户端依赖。同时，结合度量差分隐私（metric-DP）提供严格的隐私保护。实验证明该框架在保障隐私的同时显著降低了通信开销，并保持了优异的模型性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26243v1/fig-001.webp\", \"caption\": \"Table 3: Statistics of the Datasets\", \"page\": 22, \"index\": 1, \"width\": 572, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2605.26243v1/fig-002.webp\", \"caption\": \"Table 1: Average F1 scores on AML datasets across high-illicit (HI) and low-illicit (LI) ratio settings. Best results in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 185}]"
motivation: 现有的联邦图神经网络方法在处理跨机构图数据时，往往面临忽略跨客户端边导致精度下降，或频繁交换嵌入带来巨大通信与隐私成本的困境。
method: 提出CE-FedGNN框架，利用移动平均估计器低频追踪并交换聚合节点表征以解决跨客户端依赖，并采用度量差分隐私（metric-DP）框架提供形式化隐私保证。
result: "理论证明该算法以O(1/√T)的速率收敛且通信复杂度为O(T^{3/4})，在反洗钱和引用网络数据集上的实验表明其显著降低了通信开销并保持了高精度。"
conclusion: CE-FedGNN成功实现了通信高效与隐私保护的兼顾，为跨机构分布式图数据的联邦学习提供了一种高效且鲁棒的解决方案。
---

## 摘要
图神经网络（GNNs）在关系数据上取得了优异的性能，但由于隐私和政策限制，现实世界中的图通常分布在无法共享原始数据的各个组织中。现有的联邦GNN方法要么忽略跨客户端链接，导致准确率下降；要么需要频繁交换嵌入，从而产生巨大的通信和隐私成本。我们提出了CE-FedGNN，一个通信高效且隐私保护的联邦GNN框架，用于在此类耦合图上进行学习。我们的方法通过不频繁地交换聚合的节点表示，避免了共享原始数据或每轮的嵌入。为了处理跨客户端的依赖性和延迟，我们引入了一个移动平均估计器，该估计器持续跟踪节点表示，并使其能够在各轮次中稳定复用。为了对发布的表示提供严格的隐私保证，我们采用了度量差分隐私（metric-DP）框架，该框架通过在学习到的嵌入空间中的距离（而非最坏情况下的输入扰动）来衡量隐私。这使得在标准差分隐私变得过于保守的噪声水平下，仍能提供有意义的保证。我们证明了其以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度为 $O(T^{3/4})$。此外，我们在公共队列威胁模型下，通过Rényi差分隐私组合推导出了 $(\varepsilon,δ)$-metric-DP 保证。在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN实现了优异的性能，同时显著减少了通信量，并在隐私保护噪声下保持了鲁棒性。

## Abstract
Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **研究背景**：图神经网络（GNN）在处理关系数据（如社交网络、金融交易）时表现优异，但现实中的图数据往往分布在不同的机构（如不同银行）中。由于隐私法规（如GDPR）和政策限制，各机构无法将原始图数据集中起来进行训练，联邦学习（FL）成为解决这一问题的自然范式。
* **核心痛点**：图数据具有耦合性，即图中存在跨越不同客户端（机构）的“跨客户端边”。现有的联邦GNN方法面临一个两难困境：
  1. **忽略跨客户端边**：导致图结构信息缺失，模型准确率大幅下降。
  2. **频繁交换节点嵌入**：为了保留全局结构，每次迭代都交换信息，这带来了不可接受的通信开销，并增加了隐私泄露的风险。
* **整体含义**：本文旨在打破联邦图神经网络中“模型精度”、“通信效率”与“隐私保护”三者之间的壁垒，提出一个既能高效处理跨客户端依赖，又能严格保护隐私且通信成本极低的综合框架。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
论文提出了 **CE-FedGNN** 框架，其核心思想是通过**低频交换聚合的节点表征**来解耦跨客户端依赖，并引入**移动平均估计器**和**度量差分隐私**来保证优化过程的稳定性和数据隐私。

* **移动平均估计器**：
  * **节点嵌入估计**：在本地训练时，算法不使用真实的瞬时节点嵌入，而是维护一个节点嵌入的移动平均值。当需要跨客户端节点的信息时，直接复用该节点最近一次上传的、经过平滑处理的嵌入。这有效缓解了低频通信带来的“表征漂移”和延迟误差。
  * **梯度估计**：为了进一步降低小批量训练带来的方差，对随机梯度也采用了移动平均处理。
* **跨客户端梯度处理机制**：
  * 当一条边跨越两个客户端时，两端客户端各自计算包含本地节点嵌入的梯度分量，并将跨客户端节点的嵌入视为固定输入。通过适当的采样加权，聚合后的梯度可以无偏地估计完整梯度。
* **算法流程**：
  1. 服务器初始化全局模型和全局嵌入缓冲区。
  2. 客户端在本地执行 $K$ 次迭代：采样小批量数据，利用移动平均规则更新本地节点嵌入和梯度估计器，随后更新本地模型。
  3. 每隔 $K$ 步（低频），客户端将更新的模型参数、梯度估计器和边界节点嵌入上传至服务器。
  4. 服务器进行聚合，并将最新的边界节点嵌入路由给相关的邻居客户端。
* **度量差分隐私保护**：
  * **机制**：在通信时，对共享的节点嵌入、模型参数和梯度注入高斯噪声。
  * **创新点**：传统差分隐私（DP）针对最坏情况的输入扰动，导致需要加入破坏效用的巨量噪声；本文采用 **Metric-DP**，基于学习到的嵌入空间中的 L2 距离来衡量隐私。这意味着只需保证节点在嵌入空间中与其 $k$ 个最近邻不可区分，从而在适度的噪声水平下提供有意义的 $(\varepsilon, \delta)$-metric-DP 保证。

### 3. 实验设计：数据集、场景与对比方法
* **数据集与场景**：
  * **反洗钱（AML）场景**：由于真实金融数据不公开，采用了逼真的合成银行间交易数据集（基于 Altman 等人的模拟器），包含小、中、大三种规模，以及高违法比例（HI）和低违法比例（LI）两种设置。
  * **引文网络场景**：使用了四个广泛使用的真实基准数据集：Cora, Citeseer, PubMed, MSAcademic。
* **对比方法**：
  * **单客户端（SC）**：本地独立训练。
  * **传统联邦学习**：FedAvg（仅交换模型参数）。
  * **联邦GNN基线**：Swift-FedGNN（间歇性使用全局图）、FedGCN（仅在初始化时共享嵌入）、FedGNN-ST（朴素地共享陈旧嵌入，无移动平均）、FedSage+（本地生成缺失节点）、FedPUB（学习模型更新的加权聚合）。

### 4. 资源与算力
* **算力说明**：论文在附录 E 中明确提到了算力配置。实验是在通过 InfiniBand 连接的集群上进行的，**每台机器使用了一块 NVIDIA A100 GPU**。
* **训练时长**：文中给出了每轮训练的平均运行时间（例如在 HI-Large 数据集上，CE-FedGNN-PNA 每轮约需 164.73 秒），但未明确说明总训练的绝对小时数或集群的具体节点数量。

### 5. 实验数量与充分性
* **实验数量**：实验涵盖了 2 大类场景（金融反洗钱、引文网络），共 10 个数据集变体；对比了至少 7 种基线方法；进行了通信间隔 $K$ 的影响测试、不同噪声水平 $\sigma_0$ 的隐私-效用权衡测试、属性推断攻击测试、运行时间与通信字节的对比测试，以及详细的组件消融实验。
* **充分性与客观性**：实验设计**非常充分且客观**。
  * 针对联邦GNN的三大痛点（精度、通信、隐私）均设计了对应的验证实验。
  * 公平地指出了某些基线（如 FedSage+, FedPUB）因为需要全批次计算或巨大计算开销而无法扩展到大型 AML 图数据，并在小图上进行了对比，体现了客观性。
  * 提供了严谨的理论收敛性证明（$O(1/\sqrt{T})$）和隐私界限证明。

### 6. 论文的主要结论与发现
* **性能提升**：CE-FedGNN 在几乎所有数据集（特别是大规模和类别极度不平衡的低违法比例数据集）上均显著优于其他联邦基线，证明了其捕获全局图结构的有效性。
* **极高的通信效率**：实验表明，即使将通信间隔拉长至 $K=1024$，模型性能下降也极为有限，依然优于基线。这证明了移动平均估计器能极大减少通信轮次。
* **鲁棒的隐私保护**：在注入满足 Metric-DP 标准的噪声后，模型效用的下降是平缓且可控的。在标准 DP 失去意义的噪声水平下，Metric-DP 仍能提供强有力的隐私保证，且对属性推断攻击展现了极强的抵御能力。

### 7. 优点：方法或实验设计上的亮点
* **理论与工程的完美结合**：不仅提出了启发式的通信压缩，还给出了严格的数学证明（非凸优化的收敛速率和度量差分隐私界限）。
* **巧妙化解跨域依赖**：创新性地将移动平均应用于节点表征，将跨客户端交互严格限制在一跳邻居，避免了高开销的多跳跨域采样。
* **Metric-DP 的前沿应用**：首次将 Metric-DP 引入联邦 GNN 训练，利用嵌入空间的度量特性替代传统最坏情况敏感度，成功解决了节点嵌入释放时的隐私瓶颈。

### 8. 不足与局限
* **数据局限**：受限于金融行业的合规要求，核心应用场景（反洗钱）的实验完全依赖合成数据。尽管模拟器很先进，但与真实世界复杂的洗钱网络可能仍存在分布差异。
* **威胁模型的假设**：隐私分析基于“公共队列威胁模型”，即假设每轮更新的节点集合对攻击者是公开可见的。虽然论文因此规避了采样放大效应的滥用，但在高度敏感的场景中，可能还需要结合安全聚合技术来隐藏参与者的更新队列。
* **仅限一跳依赖**：方法设计主要针对一跳跨客户端邻居，对于需要深度多跳跨域交互的复杂 GNN 架构，其扩展性和有效性未作深入探讨。

（完）
