---
title: A Barrier-Modulated Architecture for Safe Affine Formation Control in Second-Order Multi-Agent Systems
authors: "Ashik Abrar Naeem, Mohammad Ariful Haque"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08137v1"
tags: ["query:mas"]
score: 9.0
evidence: 基于ADP的安全多智能体编队控制
tldr: 屏障调节架构将HOCBF与ADP结合，用于多智能体系统安全仿射编队控制。
source: arxiv
selection_source: fresh_fetch
motivation: 基于ADP的安全多智能体编队控制。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Affine formation control offers immense flexibility for coordinating multi-agent maneuvers, but guaranteeing the safety of agents under parametric uncertainties remains an open challenge. This paper proposes a novel safe affine formation control framework for second-order multi-agent systems by integrating Higher-Order Control Barrier Functions (HOCBFs) with Adaptive Dynamic Programming (ADP). We introduce a barrier-modulated control architecture that smoothly attenuates the nominal formation tracking objective when agents approach safety boundaries, preventing conflicting control inputs. Within this architecture, two distinct safety controllers are developed: (1) an analytical barrier-gradient repulsive controller that provides a computationally efficient, rigorous mathematical baseline, and (2) a data-driven optimal safety controller. The data-driven approach utilizes an actor-critic neural network to solve the Hamilton-Jacobi-Bellman (HJB) equation online, enabling optimal collision avoidance even in the presence of unknown system parameters. Using Nagumo's theorem and Lyapunov stability analysis, we formally prove that both controllers guarantee the forward invariance of the safe set ensuring absolute collision avoidance while maintaining Uniformly Ultimately Bounded (UUB) formation tracking errors. Finally, simulations validate the theoretical findings and demonstrate the robustness of the proposed controllers in dynamic obstacle avoidance scenarios.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义

**研究动机与背景：**
- 仿射编队控制（Affine Formation Control）是多智能体协调控制领域的重要分支，能够实现平移、旋转、缩放、剪切等多种复杂几何机动
- 现有仿射编队控制研究主要关注编队的稳定性和机动性，**忽略了智能体之间的安全避障问题**
- 在实际应用中（如无人机群），智能体之间必须保持最小安全距离以避免碰撞
- 当存在参数不确定性（如空气密度、阻力系数等未知参数）时，保证安全更加困难

**核心问题：**
如何在参数不确定的多智能体系统中，同时实现：
1. 对时变仿射目标编队的稳定跟踪
2. 绝对碰撞避免
3. 编队跟踪误差的有界性（UUB）

---

## 2. 论文提出的方法论

### 核心思想

论文提出一种**屏障调节控制架构（Barrier-Modulated Control Architecture）**，将名义编队控制与安全机制解耦，通过光滑的屏障调制函数动态平衡两者的优先级。

### 关键技术细节

**（1）控制架构**
```
ui = ρi · unom,i + usafe,i
```
- ρi ∈ [0,1]：屏障调制函数，当智能体接近安全边界时衰减名义控制器
- unom,i：名义仿射编队跟踪控制器
- usafe,i：安全控制器（排斥力或ADP学习策略）

**（2）高阶控制屏障函数（HOCBF）**
- 零阶安全约束：h₀(xi,xj) = ‖pi-pj‖² - Ds² ≥ 0
- 高阶安全函数：hsafe(xi,xj) = 2(pi-pj)ᵀ(vi-vj) + γ(‖pi-pj‖² - Ds²)
- 屏障调制函数：ρi = 1 - exp(-β·max(0, min(h₀)))

**（3）两种安全控制器**

| 控制器类型 | 公式 | 特点 |
|-----------|------|------|
| 解析屏障梯度排斥 | usafe,i = Σ 2κμ(pᵢ-pⱼ)·max(hsafe,0)/(hsafe²+ε²) | 计算高效，有解析保证 |
| ADP最优安全控制器 | 基于Actor-Critic神经网络 | 数据驱动，可处理未知动力学 |

**（4）ADP控制器核心组件**

- **危险加权状态映射**：zi = Σ wij · [(pi-pj)ᵀ, (vi-vj)ᵀ]ᵀ
  - 权重 wij = Ds⁴/(‖pi-pj‖² + Ds²)²，靠近的智能体权重更大
  
- **Critic网络**：估计最优值函数 V*ᵢ(zi)
  - 更新律：Ŵ̇c,i = -ηc·ωi/(1+ωᵢᵀωi)² · ec,i
  
- **Actor网络**：逼近最优安全策略 u*safe,i
  - 更新律：Ŵ̇a,i = -ηa·σa,i/(1+σᵢᵀσᵢ) · eᵀa,i

- **探索机制**：注入指数衰减的探测噪声 ni(t) = n₀e^(-κt)

### 算法流程

**屏障梯度控制器（Algorithm 1）：**
1. 获取传感器检测集合 Si(t)
2. 计算每个检测到的智能体j的安全函数 h₀, hsafe
3. 累加屏障梯度排斥力
4. 计算调制函数 ρi
5. 输出总控制输入

**ADP控制器（Algorithm 2）：**
1. 状态映射：计算危险加权状态 zi 和权重 wΣ,i
2. 前向传播：Actor网络输出安全策略
3. 策略执行：应用控制 + 探测噪声
4. 网络更新：Critic更新 → 计算Bellman误差 → Actor更新

---

## 3. 实验设计

### 场景与设置

| 参数 | 值 |
|------|-----|
| 环境 | 3D空间 |
| 智能体数量 | 9个（4领导者 + 5追随者） |
| 编队形状 | 3D dart formation（标枪形） |
| 仿真时间 | 160秒 |
| 领导者机动 | 故意聚集到单点（极端不安全场景） |

### 对比方法

| 方法 | 描述 |
|------|------|
| unom | 仅名义编队控制器（无安全机制） |
| unom + usafe (barrier) | 名义控制器 + 屏障梯度安全控制器 |
| unom + usafe (ADP) | 名义控制器 + ADP安全控制器 |

### Benchmark指标

1. **最小智能体间距离**：验证安全约束
2. **编队跟踪误差**：验证控制性能
3. **控制输入范数**：验证控制能量消耗
4. **神经网络权重收敛**：验证学习效果

### 仿真结果

- 图4：三种方法都能维持Ds=0.5m的最小安全距离
- 图5：三种方法编队误差有界且收敛
- 图6：两种安全控制器控制输入相近
- 图7-8：Critic和Actor网络权重收敛

---

## 4. 资源与算力

**论文未明确说明计算资源信息**，包括：
- GPU型号/数量
- 训练时长
- 内存需求
- 软件环境

注：实验采用纯仿真验证（MATLAB或类似环境），未涉及实际硬件部署。

---

## 5. 实验数量与充分性

### 实验数量

| 实验类型 | 数量 | 内容 |
|---------|------|------|
| 轨迹对比 | 2组 | 屏障梯度 vs ADP的3D轨迹 |
| 距离验证 | 1组（3曲线） | 最小距离随时间变化 |
| 跟踪误差 | 1组（3曲线） | 编队误差对比 |
| 控制输入 | 1组（4曲线） | 控制能量对比 |
| 权重收敛 | 2组 | Critic和Actor权重范数 |

### 充分性分析

**相对充分的方面：**
- 极端场景测试（领导者聚集）验证安全机制有效性
- 对比了两种安全控制器
- 包含与无安全机制的baseline对比

**存在的局限：**
- 仅一个场景配置（9智能体3D dart formation）
- 未测试不同规模智能体（5个 vs 20个）
- 未测试不同编队形状（圆形、菱形等）
- 未考虑通信延迟或丢包
- 未进行实际硬件实验验证

---

## 6. 论文的主要结论与发现

1. **安全保证**：两种控制器均可通过Nagumo定理证明安全集的前向不变性，保证绝对碰撞避免

2. **稳定性保证**：Lyapunov分析表明编队跟踪误差和神经网络权估计误差均为UUB（有界稳定）

3. **性能验证**：
   - 在极端场景下两种方法均能维持安全距离
   - ADP方法在线学习最优安全策略，无需精确系统动力学
   - 屏障梯度方法计算更高效，适合实时部署

4. **架构优势**：屏障调制函数实现了安全与跟踪的平滑权衡，避免控制冲突

---

## 7. 优点

| 亮点类别 | 具体描述 |
|---------|----------|
| **理论贡献** | 首次将HOCBF与ADP结合用于仿射编队安全控制 |
| **双重方案** | 提供解析和ADP两种安全控制器，适应不同应用需求 |
| **严格证明** | 利用Nagumo定理和Lyapunov分析提供形式化安全稳定性证明 |
| **解耦设计** | 安全机制与名义控制分离，便于独立设计和调试 |
| **参数不确定性处理** | ADP方法可处理空气阻力等未知参数 |
| **自适应调节** | 屏障调制函数自动调整安全优先级，无需手动切换 |

---

## 8. 不足与局限

| 不足类别 | 具体描述 |
|---------|----------|
| **实验单一性** | 仅测试单一场景（9智能体dart formation），未覆盖规模多样性 |
| **仿真局限** | 纯仿真验证，缺乏实际硬件平台（如真实无人机）的物理验证 |
| **通信假设** | 仅考虑无向图和理想通信，假设领导者轨迹可被所有追随者获取 |
| **计算资源** | ADP方法需要在线神经网络训练，计算资源需求未评估 |
| **PE条件** | ADP方法依赖持续激励条件，实际中可能难以保证 |
| **应用范围** | 仅针对二阶非线性系统，高阶系统需扩展 |
| **动态障碍物** | 未考虑环境中动态障碍物的避障（仅考虑智能体间避碰） |

---

（完）
