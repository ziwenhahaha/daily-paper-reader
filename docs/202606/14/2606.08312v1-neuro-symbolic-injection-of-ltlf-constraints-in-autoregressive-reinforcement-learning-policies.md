---
title: Neuro-Symbolic Injection of LTLf Constraints in Autoregressive Reinforcement Learning Policies
authors: "Ashkan Ansarifard, Matteo Mancanelli, Elena Umili, Fabio Patrizi"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08312v1"
tags: ["query:rl"]
score: 9.0
evidence: 离线强化学习与基于Transformer策略
tldr: 将LTLf时序约束注入Transformer强化学习策略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08312v1/fig-001.webp\", \"caption\": \"\", \"page\": 8, \"index\": 1, \"width\": 1584, \"height\": 1452}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-002.webp\", \"caption\": \"\", \"page\": 9, \"index\": 2, \"width\": 783, \"height\": 369}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-003.webp\", \"caption\": \"\", \"page\": 9, \"index\": 3, \"width\": 741, \"height\": 230}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-004.webp\", \"caption\": \"\", \"page\": 9, \"index\": 4, \"width\": 2640, \"height\": 748}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-005.webp\", \"caption\": \"\", \"page\": 10, \"index\": 5, \"width\": 2200, \"height\": 1364}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-006.webp\", \"caption\": \"\", \"page\": 11, \"index\": 6, \"width\": 2200, \"height\": 1364}, {\"url\": \"assets/figures/arxiv/2606.08312v1/fig-007.webp\", \"caption\": \"\", \"page\": 12, \"index\": 7, \"width\": 2200, \"height\": 1364}]"
motivation: 离线强化学习与基于Transformer策略。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
In this work we study offline reinforcement learning (RL) under temporally extended task constraints expressed in Linear Temporal Logic over finite traces (LTLf). Recently, transformer-based approaches such as Trajectory Transformers and Decision Transformers have been adopted to address RL as a sequence modeling problem. However, these methods optimize purely for reward and do not account for high-level temporal requirements. Here, we introduce a neurosymbolic framework that injects LTLf background knowledge into such transformer-based RL policies. Our approach compiles LTLf formulas into deterministic finite automata (DFAs) and integrates them into the learning process through a differentiable representation and a logic-based loss function. In particular, we derive differentiable satisfaction signals from DFA progression and use them as a regularization term during training. The resulting method is architecture-agnostic across different models. We evaluate the proposed framework on navigation environments with specification suites covering combinations of safety and reachability temporal properties. Experimental results show that incorporating background knowledge not only improves constraint satisfaction, but also maintains competitive return compared to vanilla baselines.

---

## 论文详细总结（自动生成）

# 论文总结：Neuro-Symbolic Injection of LTLf Constraints in Autoregressive Reinforcement Learning Policies

## 1. 核心问题与整体含义

### 研究动机

- **离线强化学习的挑战**：在离线RL中，智能体只能从预先采集的数据集中学习，无法通过在线交互来检测和纠正不安全行为
- **时序扩展约束的重要性**：许多安全要求无法从单个状态-动作对验证，必须在整个轨迹上评估。例如"最终到达目标同时始终避免危险"结合了安全性（liveness）和可达性（safety）两种时序模态
- **现有方法的缺陷**：基于Transformer的序列模型（如Trajectory Transformer和Decision Transformer）虽然能处理长程依赖，但仅优化奖励，忽视了高层时序要求

### 核心问题

如何将**线性时序逻辑有限迹（LTLf）**形式的符号化约束注入到基于Transformer的自回归策略中，使其在学习过程中兼顾奖励最大化和约束满足？

## 2. 论文提出的方法论

### 核心思想

将LTLf公式编译为**确定性有限自动机（DFA）**，并通过以下两个关键组件将其集成到学习过程：

1. **可微分表示**：使用Gumbel-Softmax采样和DeepDFA实现DFA的连续松弛
2. **逻辑损失函数**：作为训练正则项，强制轨迹满足时序约束

### 关键技术细节

#### 2.2.1 LTLf到DFA的编译

- 使用`ltlf2DFA`工具将LTLf公式自动转换为等价的DFA
- DFA扩展以处理特殊的**EOT（End-of-Trace）**符号，确保只有包含终止标记的完整轨迹才被接受

#### 2.2.2 Gumbel-Softmax采样

用于在保持可微性的同时从离散分布中采样：

$$\tilde{x}_t = \text{softmax}\left(\frac{\log(\tilde{y}_t) + G}{temp}\right)$$

- 当温度$temp \to 0$时，输出接近离散one-hot向量
- 当$temp = 1$时，保持原始连续概率分布

#### 2.2.3 DeepDFA模块

- 将DFA表示为矩阵形式，实现概率化的符号评估
- 计算采样轨迹$\tilde{\tau}$满足LTLf规范的概率 $P_{DDFA}(\tilde{\tau} \models \phi)$

#### 2.2.4 逻辑损失函数

$$L_\phi = -\log\left(\hat{P}_\theta^{\models\phi}\right) = -\log\left(\frac{1}{N}\sum_{i=1}^{N} P_{DDFA}(\tilde{\tau}^{(i)} \models \phi)\right)$$

#### 2.2.5 组合训练目标

$$L = \alpha L_\phi + (1-\alpha) L_D$$

其中$\alpha$是控制逻辑正则化强度的超参数。

### 架构无关性

方法不依赖特定模型假设，只需模型能估计给定序列条件下下一个事件的概率，因此可应用于任意自回归模型（TT、DT等）。

## 3. 实验设计

### 3.1 环境

- **ColourBomb**：网格导航环境，包含：
  - 起点(S)、目标单元格(P/Y/U)、炸弹单元格(B)、墙壁(W)
  - 有限时间步，每步有小幅惩罚
  - 到达目标获得正奖励，进入炸弹获得负奖励

### 3.2 使用的LTLf约束

| 约束类型 | 公式 | 说明 |
|---------|------|------|
| **Safety（安全性）** | $G(\neg bomb)$ | 要求在整个轨迹中避免炸弹 |
| **Reach-while-Safe** | $G(\neg bomb) \land F(goal)$ | 要求最终到达目标同时始终避免炸弹 |

### 3.3 对比方法

| 方法类别 | 描述 |
|---------|------|
| **Vanilla DT/TT** | 无逻辑正则化（$\alpha = 0$） |
| **Logic-Regularized DT/TT** | 添加逻辑损失，$\alpha \in \{0.01, 0.05, 0.1, 0.2, 0.4, 0.6, 0.8\}$ |

### 3.4 评估指标

- **Return**：平均回合奖励
- **Satisfaction Rate**：满足LTLf规范的轨迹比例
- **Goal Rate**：成功到达目标的回合比例
- **Bomb Hit Rate**：进入炸弹的轨迹比例

## 4. 资源与算力

**论文中未明确说明具体的计算资源**（如GPU型号、数量、训练时长等）。

- 仅提及"在三个运行上报告均值和标准差"
- 代码已开源：https://github.com/ashkanans/nesy_rl_test

## 5. 实验数量与充分性

### 5.1 实验规模

| 实验类型 | 数量/配置 |
|---------|----------|
| **DT安全约束实验** | 7个$\alpha$值 × 3次运行 = 21组 |
| **DT Reach-while-Safe实验** | 7个$\alpha$值 × 3次运行 = 21组 |
| **TT安全约束实验** | 7个$\alpha$值 × 3次运行 = 21组 |
| **TT Reach-while-Safe实验** | 7个$\alpha$值 × 3次运行 = 21组 |
| **总计** | 至少84组实验 |

### 5.2 实验充分性分析

**优点**：
- 覆盖了两种架构（DT和TT）
- 测试了两种LTLf约束类型（纯安全 vs. 组合约束）
- 对$\alpha$参数进行了系统性sweep

**不足**：
- 仅在一个环境（ColourBomb）中评估
- 未与现有Safe RL方法（如CPO、PPO-Lagrangian等）进行对比
- 未测试更复杂的LTLf公式（如顺序约束、Declare模式等）
- TT的性能提升幅度较小，未深入分析原因

## 6. 论文的主要结论与发现

### 6.1 Decision Transformer结果

| 约束类型 | 设置 | Satisfaction | Goal | Bomb Hit | Return |
|---------|------|-------------|------|----------|--------|
| Safety | vanilla (α=0) | 0.000 | 0.000 | 1.000 | -1.460 |
| Safety | logic (α=0.8) | **1.000** | **1.000** | **0.000** | **-0.780** |
| Reach-while-Safe | vanilla (α=0) | 0.000 | 0.000 | 1.000 | -1.460 |
| Reach-while-Safe | logic (α=0.8) | **1.000** | **1.000** | **0.000** | **-0.780** |

**关键发现**：DT在vanilla时完全失败（100%炸弹命中率），但高逻辑正则化（α=0.8）实现了完美约束满足和目标到达。

### 6.2 Trajectory Transformer结果

| 约束类型 | 设置 | Satisfaction | Goal | Bomb Hit | Return |
|---------|------|-------------|------|----------|--------|
| Safety | vanilla (α=0) | 0.353 | 0.018 | 0.647 | -0.929 |
| Safety | logic (α=0.8) | **0.499** | **0.027** | **0.470** | **-0.792** |
| Reach-while-Safe | vanilla (α=0) | 0.018 | 0.018 | 0.647 | -0.929 |
| Reach-while-Safe | logic (α=0.1) | **0.047** | **0.047** | 0.650 | **-0.903** |

**关键发现**：TT改进幅度较DT小；对于安全约束，最优$\alpha=0.8$；对于Reach-while-Safe约束，最优$\alpha=0.1$。

### 6.3 核心结论

1. **逻辑正则化有效**：将LTLf约束注入训练目标能显著改善约束满意度
2. **性能不退化**：逻辑正则化不损害任务回报，甚至可能有提升
3. **$\alpha$参数敏感**：需要针对不同约束类型调优；过高值可能导致过度保守（不尝试到达目标）
4. **架构差异**：DT对逻辑正则化响应更强烈，TT的改进相对温和

## 7. 优点

### 7.1 方法层面

| 优点 | 说明 |
|------|------|
| **架构无关性** | 可应用于任意自回归序列模型 |
| **通用性** | 支持任意LTLf公式，而非仅限于安全片段 |
| **无需环境模型** | 仅需约束规范，无需环境先验知识 |
| **端到端可微** | 通过DeepDFA实现连续松弛，支持梯度训练 |
| **数据无关** | 不修改轨迹数据或奖励结构，保留原始分布 |

### 7.2 实验层面

| 优点 | 说明 |
|------|------|
| **系统性参数研究** | 对$\alpha$进行了全面的sweep实验 |
| **多指标评估** | 同时评估性能指标和约束满足度 |
| **代码开源** | 便于复现和扩展 |

## 8. 不足与局限

### 8.1 实验覆盖不足

- 仅在**单一环境**（ColourBomb）中评估，缺乏跨领域泛化验证
- 未与**现有Safe RL方法**（如CPO、PPO-Lagrangian、Shielding等）进行对比
- 未探索**更复杂的LTLf公式**（如顺序约束、周期约束等）

### 8.2 性能分析不充分

- TT改进幅度较小的原因**未深入分析**
- 缺乏对**训练稳定性、收敛性**的分析
- 未讨论不同DFA规模对性能的影响

### 8.3 应用限制

| 限制 | 说明 |
|------|------|
| **离线设置** | 专为离线RL设计，不适用于在线探索场景 |
| **EOT依赖** | 轨迹必须有终止标记，否则视为不满足 |
| **$\alpha$调优** | 需要针对不同任务手动调整正则化强度 |
| **计算开销** | DeepDFA评估引入额外计算成本（论文未量化） |

### 8.4 潜在偏差风险

- **数据集偏差**：实验结果可能受ColourBomb特定配置影响
- **评估指标单一**：未考虑安全性与性能的帕累托前沿分析
- **未讨论假阳性/假阴性**：DFA评估的可能错误传播

---

（完）
