---
title: Individual Control Barrier Functions-Guided Diffusion Model for Safe Offline Multi-Agent Reinforcement Learning
authors: "Qingyun Guo, Junyi Shi, Jianuo Huang, Tianyu Shi"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12640v1"
tags: ["query:mas"]
score: 9.0
evidence: 安全的离线多智能体强化学习算法
tldr: 提出使用控制屏障函数引导扩散模型的离线多智能体强化学习算法进行安全轨迹生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12640v1/fig-001.webp\", \"caption\": \"\", \"page\": 2, \"index\": 1, \"width\": 1511, \"height\": 886}]"
motivation: 安全的离线多智能体强化学习算法。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Offline reinforcement learning allows control policies to be learned directly from data without online interaction, making it suitable for safety-critical tasks. Recent studies have applied diffusion models to offline reinforcement learning to leverage their strong capacity for modeling complex data distributions. However, existing approaches primarily focus on single-agent settings, leaving the safety challenges in multi-agent environments largely unexplored. In this work, we propose a safe offline multi-agent reinforcement learning algorithm that embeds neural individual control barrier functions into the diffusion model to enhance safety during trajectory generation, with control policies recovered through inverse dynamics. We evaluate our algorithm across diverse benchmarks, demonstrating substantial safety improvements while maintaining competitive rewards.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义

### 研究动机
- **安全关键应用的挑战**：自动驾驶、机器人控制等安全关键场景需要在严格安全约束下学习控制策略，但传统在线强化学习（RL）需要与环境交互，可能带来高昂成本和风险。
- **离线强化学习的优势**：离线RL可从预采集数据集直接学习策略，无需在线交互，适合安全关键任务。
- **扩散模型的兴起**：扩散模型在建模复杂数据分布方面表现优异，已被应用于离线RL以缓解外推误差。
- **现有方法的局限性**：
  - FISOR等方法依赖学习成本函数，但在离线设置中因外推误差难以准确估计。
  - SafeDiffuser仅在低维环境验证，难以扩展。
  - 上述方法均局限于单智能体设置，未考虑多智能体系统中的安全与协调问题。

### 研究目标
提出一种安全的离线多智能体强化学习（MARL）算法，将神经个体控制屏障函数（CBF）嵌入扩散模型，在轨迹生成过程中增强安全性，最终通过逆动力学恢复控制策略。

---

## 2. 论文提出的方法论

### 核心思想
- 将**神经个体CBF**与**多智能体扩散模型**结合，在扩散模型训练过程中通过反向传播将CBF损失融入轨迹生成。
- 遵循**集中训练分散执行（CTDE）**范式建模智能体间协调。
- 通过逆动力学模块从生成的状态轨迹推断动作策略。

### 关键技术细节

#### 2.1 问题建模
- 使用**分散部分可观测马尔可夫决策过程（Dec-POMDP）**定义离线MARL问题。
- 状态空间 S、智能体集合 N={1,...,n}、联合动作空间 A 等。

#### 2.2 个体控制屏障函数（CBF）
- 对于智能体 i，定义安全集 Si,s = {si | hi(si) ≥ 0}，其中 hi 为CBF函数。
- **离散CBF条件**：存在扩展K类函数 α，使得：
  sup_{ai∈Ai} hi(fi(si(t), ai(t))) ≥ α(hi(si(t)))
- 通过CBF确保状态始终保持在安全集内，满足前向不变性。

#### 2.3 扩散模型架构
- 基于**MADIFF**构建多智能体扩散模型，扩散对象为**状态轨迹**而非动作。
- 使用**特征剪枝的智能体特定全局状态**：将全局状态 s(t) 与智能体 i 的局部观测 oi(t) 结合，剪除冗余信息，保留协作与个体有效性。
- 输入轨迹格式：τi = [ṡi(t), ṡi(t+1), ..., ṡi(t+H-1)]，其中 H 为轨迹长度。
- 条件机制：基于奖励 r 进行分类器无关引导，生成高奖励轨迹。

#### 2.4 CBF引导的损失函数
通过以下损失函数将CBF嵌入扩散模型：

L_Ci(ωi, θ) = Σ_{ṡi∈Ṡi,0} max(0, δ - h^ωi_i(ṡ^θ_i))   // 初始条件
            + Σ_{ṡi∈Ṡi,d} max(0, δ + h^ωi_i(ṡ^θ_i))    // 危险集
            + Σ_{ṡi∈Ṡi,s} max(0, δ - h^ωi_i(ṡ^θ_i(t+1)) + αh^ωi_i(ṡ^θ_i(t)))  // 前向不变

- 其中 δ 为边界参数，ṡ^θ_i 为带梯度的去噪状态。
- **时间松弛机制**：α = m - ηk^i，随着扩散步骤 k→0 逐渐放松约束，避免过度保守导致局部最优。

#### 2.5 策略学习
- 使用**逆动力学模块**从生成的状态轨迹推断动作：
  L_Pi(φi) = E_D[||ai - f_φi(ṡi(t), ṡi(t+1))||²]
- 安全性通过生成的轨迹分布间接进入策略学习。

### 算法流程（Algorithm 1）
1. 从数据集提取轨迹
2. 对于 k = K 到 1（反向扩散步骤）：
   - 更新扩散模型参数 θ（损失函数6）
   - 对每个智能体 i：更新CBF参数 ωi 和 θ（损失函数8）
   - 更新逆动力学模块参数 φi（损失函数10）
3. 生成样本并提取状态对，计算动作 ai(t)

---

## 3. 实验设计

### 数据集与场景
| 环境 | 描述 | 数据集类型 |
|------|------|------------|
| **Simple Spread** | 多智能体覆盖地标、避免碰撞 | Expert、Medium-Replay、Medium、Random（4组） |
| **Safe MAMUJOCO** | 机器人在速度阈值下前进 | 2x3 HalfCheetah、2x4 Ant、4x2 Ant（各3组：Good、Medium、Poor） |

- Simple Spread：基于MATD3策略收集的数据
- Safe MAMUJOCO：来自Off-the-Grid离线数据集

### Benchmark与对比方法
- **对比基线**：MA-TD3BC、Fujimoto的离线RL方法）、OMAR（离线多智能体策略修正）
- **评价指标**：episode reward（↑越高越好）、cost（↓越低越好）
- **实验配置**：每个任务使用5个不同随机种子

### 实验数量
- **总计约21个任务配置**（涵盖多种环境和数据集质量组合）
- 每个配置5个随机种子，共计约105组独立实验结果

---

## 4. 资源与算力

- **论文未明确说明**使用的GPU型号、数量或具体训练时长。
- 仅在致谢中提及"Aalto Science-IT项目提供的计算资源"，但未给出详细硬件配置。

---

## 5. 实验数量与充分性

### 实验充分性分析
- **多环境覆盖**：涵盖低维（Simple Spread）和高维（Safe MAMUJOCO）场景
- **多智能体规模**：测试2、4个智能体的配置
- **数据集质量多样性**：包含Expert/Good到Random/Poor的完整质量谱系
- **对比方法覆盖**：包含行为克隆（MA-TD3BC）和正则化方法（OMAR）等多种范式
- **随机性验证**：每个配置5个随机种子，结果有均值和标准差

### 客观性与公平性
- 使用相同的归一化参数（l, k, rmin, rmax）确保同一任务下各方法可比
- 所有方法使用相同数据集，排除数据偏差影响

---

## 6. 论文的主要结论与发现

### 核心结论
1. **安全性显著提升**：在几乎所有任务中实现最低cost，同时保持竞争力reward。
2. **高维环境优势**：在2ant-good等高维环境+高质量数据场景下达到最高reward，且cost低。
3. **低维环境卓越**：Simple Spread的medium-replay、medium、random数据集上表现最佳，cost接近零。
4. **鲁棒性验证**：在不同质量数据集上均保持稳定性能，优于依赖行为克隆的方法。

### 具体发现
- **MA-TD3BC问题**：数据集质量高时表现好，但质量下降时性能急剧下降（如Simple Spread-random任务仅获得约一半reward）。
- **OMAR问题**：容易陷入局部最优，虽然部分任务cost低，但reward也最低（如4ant-good、2ant-medium）。
- **本方法平衡性**：在高reward和低cost之间取得良好平衡，适应性强。

---

## 7. 优点

1. **创新性整合**：首次将神经个体CBF嵌入多智能体扩散模型，实现安全引导。
2. **无代价函数依赖**：避免离线RL中代价函数学习的外推误差问题。
3. **任务自适应**：通过学习而非手动设计CBF，适应不同环境和安全约束。
4. **全局-局部平衡**：特征剪枝策略同时保留协作信息和个体任务有效性。
5. **时间松弛机制**：避免过度保守约束导致的局部最优问题。
6. **CTDE范式遵循**：符合多智能体系统标准训练范式，便于实际部署。
7. **实验覆盖全面**：多环境、多智能体规模、多数据集质量的综合评估。

---

## 8. 不足与局限

1. **算力信息缺失**：未披露实验所需计算资源，难以复现和资源规划。
2. **缺乏安全离线MARL专用Benchmark**：评估受限于代表性环境，覆盖范围有限。
3. **分布外风险**：reward和CBF引导仍可能使生成轨迹偏离分布，导致分布外问题。
4. **异步交互未处理**：未考虑多智能体系统中异步交互的影响。
5. **模型失配敏感性**：方法对环境模型准确性敏感，未在验证模块下测试。
6. **感知不确定性未覆盖**：论文提及未来将结合感知不确定性验证，但当前未涉及。
7. **封闭环安全性未认证**：缺乏闭环安全性形式化验证，仍为实验性验证。

---

（完）
