---
title: Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation
title_zh: 通过符号蒸馏获得的可解释解析卡拉比-丘度规
authors: D Yang Eng
date: 2026-02-08
pdf: "https://arxiv.org/pdf/2602.07834v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 符号回归用于物理和可解释公式
tldr: 针对弦理论中卡拉比-丘流形度量计算困难的问题，本研究提出利用符号回归将复杂的神经网络近似模型蒸馏为简洁、可解释的解析公式。通过仅含五个项的表达式，在参数量减少3000倍的情况下达到了与神经网络相当的精度（R²=0.9994）。该方法不仅揭示了几何约束下的核心特征，还能准确复现体积积分和汤川耦合等物理观测值，为复杂流形的解析建模提供了新途径。
motivation: 旨在解决卡拉比-丘流形度量计算极其复杂且缺乏解析表达式的问题，将黑盒神经网络模型转化为可解释的数学公式。
method: 采用符号回归技术对神经网络训练出的度量近似值进行蒸馏，提取出由幂和及对称多项式构成的简洁解析函数。
result: 得到的五项解析式在参数量大幅缩减的同时保持了极高精度，并能跨模空间范围稳定运行，成功复现了关键物理量。
conclusion: 符号蒸馏证明了复杂几何量可以被紧凑且可解释的模型所刻画，为物理学和几何学的解析研究提供了强有力的工具。
---

## 摘要
卡拉比-丘流形对弦理论至关重要，但其度规的计算往往难以处理。在本文中，我们展示了符号回归可以将神经网络近似蒸馏为简单、可解释的公式。我们的五项表达式在参数量减少 3000 倍的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。多种子验证确认了几何约束选择了关键特征，特别是幂和与对称多项式，同时允许结构多样性。在所研究的模空间范围（$ψ \in [0, 0.8]$）内，该函数形式可以保持不变，且系数平滑变化；我们将这些趋势解释为在局部训练的教师模型精度范围内（当 $ψ \neq 0$ 时，$σ \approx 8-9\%$）的经验假设。该公式重现了物理可观测物理量——体积积分和汤川耦合——验证了符号蒸馏能够为以前只能通过黑盒网络获取的物理量恢复出紧凑、可解释的模型。

## Abstract
Calabi--Yau manifolds are essential for string theory but require computing intractable metrics. Here we show that symbolic regression can distill neural approximations into simple, interpretable formulas. Our five-term expression matches neural accuracy ($R^2 = 0.9994$) with 3,000-fold fewer parameters. Multi-seed validation confirms that geometric constraints select essential features, specifically power sums and symmetric polynomials, while permitting structural diversity. The functional form can be maintained across the studied moduli range ($ψ\in [0, 0.8]$) with coefficients varying smoothly; we interpret these trends as empirical hypotheses within the accuracy regime of the locally-trained teachers ($σ\approx 8-9\%$ at $ψ\neq 0$). The formula reproduces physical observables -- volume integrals and Yukawa couplings -- validating that symbolic distillation recovers compact, interpretable models for quantities previously accessible only to black-box networks.

---

## 论文详细总结（自动生成）

这篇论文《Interpretable Analytic Calabi-Yau Metrics via Symbolic Distillation》探讨了如何利用机器学习技术解决弦理论中长期存在的数学难题。以下是对该论文的结构化深入总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在弦理论中，卡拉比-丘（Calabi-Yau, CY）流形的 Ricci 平坦度规（Ricci-flat metrics）是计算粒子物理观测值（如汤川耦合）的基础。然而，Monge-Ampère 方程（定义该度规的偏微分方程）在数学上极难获得解析解。
*   **核心问题**：虽然近年来的神经网络（NN）代理模型可以提供高精度的数值近似，但它们是“黑盒”，无法提供物理直觉。论文旨在探索：**度规观测值是否存在紧凑、可解释且能跨模空间（Moduli Space）保持稳定的解析形式？**

### 2. 论文提出的方法论
*   **核心思想（符号蒸馏）**：采用“教师-学生”架构。首先训练一个高精度的神经网络（教师模型）来近似度规，然后利用**符号回归（Symbolic Regression, SR）**从神经网络中提取简洁的数学表达式（学生模型）。
*   **关键技术细节**：
    *   **教师模型**：使用 Donaldson 算法在多项式阶数 $k=10$ 下生成的平衡度规（H-matrix），作为 Ricci 平坦度规的高精度近似。
    *   **特征工程**：选取了规范不变的几何不变量作为输入特征，包括幂和（Power sum）$p_2 = \sum |z_i|^4$ 和第三初等对称多项式 $\sigma_3$。
    *   **符号回归工具**：使用 **PySR** 库，通过进化算法在算子空间中搜索最优函数形式。
    *   **因子化假设**：提出模空间因子化猜想，即度规行列式比值的对数可以表示为：$\sum c_i(\psi) \cdot f_i(p_2, \sigma_3)$，其中函数形式 $f_i$ 固定，而系数 $c_i$ 随模参数 $\psi$ 平滑变化。

### 3. 实验设计
*   **数据集与场景**：研究了 Dwork 家族的五次超曲面（Quintic Hypersurfaces），采样点数为 $10^5$。实验覆盖了模空间范围 $\psi \in [0, 0.8]$。
*   **Benchmark（基准对比）**：
    *   **Donaldson H-matrix**：作为真值/教师模型（875个参数）。
    *   **神经网络代理（Ashmore 2021）**：约 15,000 个参数。
    *   **3阶多项式拟合**：作为传统的参数化拟合对比。
*   **物理验证**：通过计算体积积分（Volume Integrals）和汤川耦合（Yukawa Couplings）来验证符号公式的物理保真度。

### 4. 资源与算力
*   **硬件使用**：论文明确提到符号回归搜索是在 **NVIDIA A100 GPU**（通过 Google Colab Pro）上运行的。
*   **训练细节**：符号回归运行了 200 次迭代，种群大小为 60。神经网络教师模型的训练在每个 $\psi$ 点上进行 15 次 Adam 优化迭代。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 $\psi=0$（Fermat点）进行了 **10 个不同随机种子**的集成验证，以确保发现的特征（$p_2, \sigma_3$）是稳健的而非偶然。
    *   在 $\psi \in \{0.0, 0.2, 0.4, 0.6, 0.8\}$ 多个点上独立运行符号回归，验证了函数结构的稳定性。
    *   进行了**特征消融实验**，证明了引入 $\sigma_3$ 可将误差降低近 50%。
*   **充分性评价**：实验设计非常系统，不仅关注拟合精度（$R^2$），还通过跨阶数（$k=6, 8, 10$）的收敛性分析和物理量验证，确保了结果的数学和物理合理性。

### 6. 论文的主要结论与发现
*   **极度压缩**：发现了一个仅含 **5 个项**的解析公式，在参数量减少 **3000 倍**的情况下，达到了与神经网络相当的精度（$R^2 = 0.9994$）。
*   **结构稳定性**：在研究的模空间范围内，该五项公式的结构保持不变，几何变形完全由系数 $c_i(\psi)$ 的平滑演化（如符号反转、单调增强）来刻画。
*   **物理一致性**：符号公式计算的汤川耦合与理论值完全吻合，体积积分误差在 5.5% 以内（主要受限于采样和教师模型本身的噪声）。
*   **发现奇异项**：符号回归自动发现了 $1/p_2^2$ 等奇异项，这与轨道折叠（Orbifold）渐近几何的预期相符，而普通多项式拟合无法捕捉这一点。

### 7. 优点与亮点
*   **可解释性突破**：将复杂的黑盒度规转化为人类可读的公式，揭示了 Ricci 平坦性约束下的几何特征选择机制。
*   **计算效率极高**：相比于神经网络，符号公式的推理速度提升了 3000 倍，相比于传统的 Donaldson 算法提升了 10,000 倍，极大地便利了模空间的快速扫描。
*   **方法论创新**：展示了“符号蒸馏”作为一种通用的科学 AI 范式，在处理复杂偏微分方程解方面的潜力。

### 8. 不足与局限
*   **教师模型依赖**：符号公式的精度上限受限于教师模型。在 $\psi \neq 0$ 时，教师模型的 Ricci 平坦度误差（$\sigma \approx 8-9\%$）较高，可能掩盖了某些细微的几何特征。
*   **覆盖范围有限**：目前仅验证了 Dwork 家族这一种单参数模空间，尚未证明该特征集（$p_2, \sigma_3$）在其他更复杂的卡拉比-丘流形家族中的通用性。
*   **全局误差累积**：虽然局部拟合极好，但在进行全流形体积积分时，误差（5.5%）显著高于局部指标，说明在处理流形边缘或奇异点附近仍有改进空间。

（完）
