---
title: A few-shot and physically restorable symbolic regression turbulence model based on normalized general effective-viscosity hypothesis
title_zh: 一种基于归一化广义有效粘度假设的少样本且物理可还原的符号回归湍流模型
authors: "Ziqi Ji, Penghao Duan, Gang Du"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08270v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 符号回归在湍流建模中的物理定律提取应用
tldr: 针对RANS湍流模型精度不足及数据驱动模型泛化性差的问题，本文提出一种基于归一化通用有效粘度假设的少样本、物理可恢复符号回归湍流模型。该模型仅利用少量三维周期山流动数据训练，即可在多种复杂流动场景（如机翼、压气机）中表现出优于基准模型的性能，且在特定物理条件下能自动退化为基准模型，兼具高精度与物理一致性。
motivation: 旨在解决传统RANS模型精度低以及现有数据驱动湍流模型在泛化能力和物理可解释性方面的局限。
method: 基于归一化通用有效粘度假设，利用少量DNS数据通过符号回归方法构建具有物理可恢复性的湍流模型。
result: 该模型在周期山、NACA0012机翼及NASA Rotor 37压气机等多种异构流动中均优于基准模型，并展现出良好的物理退化特性。
conclusion: 研究证明了符号回归在构建兼具强泛化能力和物理约束的湍流模型方面的巨大潜力。
---

## 摘要
湍流是自然过程和工程应用中普遍存在的一种复杂、不规则的流动现象。雷诺平均纳维-斯托克斯（RANS）方法由于其计算成本低，已成为快速模拟工程湍流问题的主要方法。然而，经典湍流模型的不准确性构成了 RANS 框架的主要缺点。随着数据驱动方法的快速发展，许多数据驱动的湍流模型被提出，但它们仍面临泛化性和准确性的问题。在这项工作中，我们提出了一种基于归一化广义有效粘度假设的少样本、物理可还原的符号回归湍流模型。“少样本”表示我们的模型仅在涵盖窄范围湍流物理特性的有限流动构型上进行训练，但仍能在显著不同的湍流中优于基准模型。“物理可还原”意味着我们的模型仅利用符号回归训练结果，即可在满足特定物理条件的区域内几乎还原为基准模型。归一化广义有效粘度假设是在我们之前的研究中提出的。具体而言，我们首先形式化了少样本数据驱动湍流模型的概念。其次，我们仅使用三维周期山流动切片的直接数值模拟（DNS）数据来训练我们的符号回归湍流模型。第三，我们在周期山流动、零压力梯度平板流动、NACA0012 翼型流动以及 NASA Rotor 37 跨声速轴流压气机流动上评估了我们的模型。我们的其中一个符号回归湍流模型始终优于基准模型，并且我们进一步证明了该模型在某些流动区域可以几乎还原为基准行为。

## Abstract
Turbulence is a complex, irregular flow phenomenon ubiquitous in natural processes and engineering applications. The Reynolds-averaged Navier-Stokes (RANS) method, owing to its low computational cost, has become the primary approach for rapid simulation of engineering turbulence problems. However, the inaccuracy of classical turbulence models constitutes the main drawback of the RANS framework. With the rapid development of data-driven approaches, many data-driven turbulence models have been proposed, yet they still suffer from issues of generalizability and accuracy. In this work, we propose a few-shot, physically restorable, symbolic regression turbulence model based on the normalized general effective-viscosity hypothesis. Few-shot indicates that our model is trained on limited flow configurations spanning only a narrow subset of turbulent flow physics, yet can still outperform the baseline model in substantially different turbulent flows. Physically restorable means our model can nearly revert to the baseline model in regimes satisfying specific physical conditions, using only the symbolic regression training results. The normalized general effective-viscosity hypothesis was proposed in our previous study. Specifically, we first formalize the concept of few-shot data-driven turbulence models. Second, we train our symbolic regression turbulence models using only direct numerical simulation (DNS) data for three-dimensional periodic hill flow slices. Third, we evaluate our models on periodic hill flows, zero pressure gradient flat plate flow, NACA0012 airfoil flows, and NASA Rotor 37 transonic axial compressor flows. One of our symbolic regression turbulence models consistently outperforms the baseline model, and we further demonstrate that this model can nearly revert to baseline behavior in certain flow regimes.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种结合物理启发与符号回归（Symbolic Regression, SR）的湍流建模新方法。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的雷诺平均纳维-斯托克斯（RANS）湍流模型（如 $k-\omega$ SST）在处理复杂分离流时精度不足，而现有的数据驱动模型往往面临“黑盒”不可解释、泛化性差以及在大规模数据集上训练成本高昂的问题。
*   **研究动机**：
    *   **少样本学习（Few-shot）**：探索能否仅利用极少数流动工况（如简单的周期山流动）训练模型，使其在完全不同的复杂工程流动（如跨声速压气机）中依然有效。
    *   **物理可还原性（Physically Restorable）**：要求模型在经典模型已经很准确的区域（如近壁面粘性底层）能够自动退化回基准模型，以保持物理一致性。
    *   **可解释性**：通过符号回归生成显式的代数方程，而非深层神经网络。

### 2. 方法论
*   **核心思想**：基于**归一化广义有效粘度假设**（Normalized General Effective-Viscosity Hypothesis），利用符号回归寻找雷诺应力各向异性张量 $\mathbf{b}$ 与流场特征之间的显式代数关系。
*   **关键技术细节**：
    *   **归一化张量基底**：改进了 Pope (1975) 的张量基底理论，对基底张量 $\hat{\mathbf{T}}_i$ 进行 Frobenius 范数归一化，使系数 $\hat{g}_i$ 的物理贡献直观可比。
    *   **输入特征**：选取了 7 个张量不变量（涉及应变率、旋转率、压力梯度和动能梯度）以及 5 个额外的物理特征（如壁面距离雷诺数、湍流时间尺度比等）。
    *   **符号回归算法**：使用开源库 **PySR**，通过多群体进化算法在数学算子空间搜索最优方程。
    *   **物理还原机制**：固定线性项系数 $g_1$ 为基准模型的涡粘性形式，仅对非线性项进行符号回归，确保模型在特定物理极限下（如特征值趋于 0 时）能回归到经典的 Boussinesq 假设。

### 3. 实验设计
*   **训练集**：仅使用三维周期山（Periodic Hill）流动的 DNS 数据切片（$\alpha=0.8$ 和 $1.2$ 两种几何形状）。
*   **测试场景（泛化性验证）**：
    1.  **周期山流动**：验证插值（$\alpha=1.0$）和外推（$\alpha=0.5, 1.5$）能力。
    2.  **零压力梯度平板流动**：验证对壁面束缚流动的兼容性及物理还原性。
    3.  **NACA0012 翼型**：高雷诺数下的分离流模拟。
    4.  **NASA Rotor 37 跨声速轴流压气机**：复杂的工业级三维、压缩、激波干扰流动。
*   **Benchmark（基准）**：标准的 $k-\omega$ SST 湍流模型。
*   **对比模型**：SR 3T（仅含 2D 流动独立的 3 个张量）和 SR 5T（含 5 个张量）。

### 4. 资源与算力
*   **算力说明**：文中**未明确列出**具体的 GPU 型号或训练总时长。
*   **算法复杂度迹象**：提到了 PySR 的超参数设置，包括 16 个种群、每个种群 100 个个体、迭代 2000 次，并进行了 5 次独立运行以确保方程收敛。这表明符号回归阶段的计算量主要集中在 CPU 上的启发式搜索，而非大规模 GPU 并行训练。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从低速到跨声速、从二维到三维、从简单几何到复杂旋转机械的 4 大类测试案例。
*   **充分性评价**：实验设计非常充分且具有挑战性。特别是用极简单的周期山数据去预测复杂的 Rotor 37 压气机性能，这种“跨维度”的泛化测试在湍流建模领域具有很强的说服力。
*   **客观性**：通过 MSE（均方误差）定量对比了速度场误差，并对比了升力曲线、总压比、绝热效率等工程关键指标，评价标准客观。

### 6. 主要结论与发现
*   **SR 5T 模型表现最优**：在所有测试案例中，SR 5T 模型均优于基准 SST 模型，尤其在预测分离区大小和压气机效率方面提升显著。
*   **物理还原成功**：在平板流动实验中，模型在粘性底层自动关闭了非线性修正项，成功还原了基准模型的行为。
*   **关键项发现**：研究发现 $\hat{g}_6 \hat{\mathbf{T}}_6$ 项是改善分离流预测的关键，而这一项在纯二维理论中往往被忽略。
*   **少样本可行性**：证明了通过精心设计的归一化特征，少量高质量 DNS 数据足以训练出具有强泛化能力的显式湍流模型。

### 7. 优点
*   **显式表达**：生成的模型是简单的代数方程，易于植入任何 CFD 软件（如 OpenFOAM, TCAE），无需复杂的神经网络推理库。
*   **强泛化性**：打破了数据驱动模型只能预测“见过”的流动的局限。
*   **物理一致性**：通过归一化和特征选择，保证了模型在近壁面等区域的物理合理性。

### 8. 不足与局限
*   **方程选择的经验性**：虽然符号回归是自动的，但在多个候选方程中选择最终模型时，仍依赖于专家经验来平衡复杂度和精度。
*   **局部过拟合风险**：在 NASA Rotor 37 的上游区域，模型出现了一些非物理的激活（可能是由于周期山训练数据的局限性导致的过拟合）。
*   **张量基底限制**：目前仅使用了 5 个张量基底，对于极度复杂的三维流动，可能需要更完整的张量基底集（如 Pope 的完整 10 张量集）。

（完）
