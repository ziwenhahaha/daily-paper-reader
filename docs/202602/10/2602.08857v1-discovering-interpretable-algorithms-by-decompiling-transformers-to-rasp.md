---
title: Discovering Interpretable Algorithms by Decompiling Transformers to RASP
title_zh: 通过将 Transformer 反编译为 RASP 发现可解释算法
authors: "Xinting Huang, Aleksandra Bakalova, Satwik Bhattamishra, William Merrill, Michael Hahn"
date: 2026-02-09
pdf: "https://arxiv.org/pdf/2602.08857v1"
tags: ["keyword:SR", "query:SR"]
score: 6.0
evidence: 从Transformer中提取可解释程序
tldr: 本研究提出了一种从训练好的 Transformer 模型中提取可解释算法的方法。通过将 Transformer 重新参数化为 RASP（一种模拟 Transformer 计算的编程语言）程序，并利用因果干预技术提取最小有效子程序，研究者成功地从具有长度泛化能力的模型中还原了简洁且可解释的算法。这为 Transformer 内部实现简单 RASP 程序的假设提供了直接证据，增强了对模型泛化机制的理解。
motivation: 尽管已知 Transformer 能模拟 RASP 程序，但尚不清楚训练后的模型在实际运行中是否真的实现了这些简单且可解释的算法。
method: 将 Transformer 忠实地重新参数化为 RASP 程序，并应用因果干预来发现并提取其中最小的充分子程序。
result: 在算法和形式语言任务上的实验表明，该方法能从具有长度泛化能力的 Transformer 中成功恢复出简单、可解释的 RASP 程序。
conclusion: 该研究证明了 Transformer 内部确实实现了简单的 RASP 程序，为理解模型的表达能力和泛化机制提供了直接证据。
---

## 摘要
最近的研究表明，Transformer 的计算可以在 RASP 系列编程语言中进行模拟。这些发现有助于深入理解 Transformer 的表达能力和泛化能力。特别是，有研究认为 Transformer 在具有简单 RASP 程序的任务上能够实现精确的长度泛化。然而，训练好的模型是否真的实现了简单的可解释程序仍然是一个开放性问题。在本文中，我们提出了一种从训练好的 Transformer 中提取此类程序的通用方法。该方法的核心思想是将 Transformer 忠实地重新参数化为 RASP 程序，然后应用因果干预来发现一个足够小的充分子程序。在针对算法和形式语言任务训练的小型 Transformer 实验中，我们展示了该方法通常能从具有长度泛化能力的 Transformer 中恢复出简单且可解释的 RASP 程序。我们的结果为 Transformer 内部实现了简单的 RASP 程序提供了迄今为止最直接的证据。

## Abstract
Recent work has shown that the computations of Transformers can be simulated in the RASP family of programming languages. These findings have enabled improved understanding of the expressive capacity and generalization abilities of Transformers. In particular, Transformers have been suggested to length-generalize exactly on problems that have simple RASP programs. However, it remains open whether trained models actually implement simple interpretable programs. In this paper, we present a general method to extract such programs from trained Transformers. The idea is to faithfully re-parameterize a Transformer as a RASP program and then apply causal interventions to discover a small sufficient sub-program. In experiments on small Transformers trained on algorithmic and formal language tasks, we show that our method often recovers simple and interpretable RASP programs from length-generalizing transformers. Our results provide the most direct evidence so far that Transformers internally implement simple RASP programs.