---
title: Resource-Efficient Iterative LLM-Based NAS with Feedback Memory
title_zh: 具有反馈记忆的资源高效迭代式基于 LLM 的神经架构搜索
authors: "Xiaojie Gu, Dmitry Ignatov, Radu Timofte"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.12091v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 基于大语言模型和反馈记忆的神经网络架构自动设计
tldr: 本研究提出一种资源高效的迭代式神经架构搜索（NAS）框架，利用大语言模型（LLM）在单张消费级GPU上实现CNN架构的自动设计。该方法引入基于马尔可夫链的历史反馈记忆机制，通过结构化诊断三元组记录改进过程，并采用双LLM分工模式（代码生成与提示词改进）。实验证明，该框架无需微调LLM即可在有限算力下显著提升模型性能，为低成本、可复现的硬件感知NAS提供了新范式。
selection_source: fresh_fetch
motivation: 针对传统NAS方法计算资源消耗巨大且难以在单张消费级GPU上运行的问题，探索利用LLM实现高效的架构搜索。
method: 采用带有滑动窗口反馈记忆的闭环迭代流程，结合双LLM分工协作和结构化诊断信号来优化CNN架构。
result: "在单张RTX 4090上，DeepSeek-Coder等模型在CIFAR-10上的准确率从初始的低水平显著提升至70%左右。"
conclusion: 该研究证明了在不依赖云基础设施的情况下，利用冻结的开源LLM进行硬件感知的自动化架构搜索是可行且高效的。
---

## 摘要
神经架构搜索 (NAS) 实现了网络设计的自动化，但传统方法需要消耗大量的计算资源。我们提出了一种闭环流水线，利用大语言模型 (LLM) 在单张消费级 GPU 上迭代地生成、评估和改进用于图像分类的卷积神经网络架构，且无需对 LLM 进行微调。我们方法的核心是受马尔可夫链启发的历史反馈记忆：一个包含 $K=5$ 次近期改进尝试的滑动窗口在保持上下文长度恒定的同时，为迭代学习提供了充足的信号。与以往丢

## Abstract
Neural Architecture Search (NAS) automates network design, but conventional methods demand substantial computational resources. We propose a closed-loop pipeline leveraging large language models (LLMs) to iteratively generate, evaluate, and refine convolutional neural network architectures for image classification on a single consumer-grade GPU without LLM fine-tuning. Central to our approach is a historical feedback memory inspired by Markov chains: a sliding window of $K{=}5$ recent improvement attempts keeps context size constant while providing sufficient signal for iterative learning. Unlike prior LLM optimizers that discard failure trajectories, each history entry is a structured diagnostic triple -- recording the identified problem, suggested modification, and resulting outcome -- treating code execution failures as first-class learning signals. A dual-LLM specialization reduces per-call cognitive load: a Code Generator produces executable PyTorch architectures while a Prompt Improver handles diagnostic reasoning. Since both the LLM and architecture training share limited VRAM, the search implicitly favors compact, hardware-efficient models suited to edge deployment. We evaluate three frozen instruction-tuned LLMs (${\leq}7$B parameters) across up to 2000 iterations in an unconstrained open code space, using one-epoch proxy accuracy on CIFAR-10, CIFAR-100, and ImageNette as a fast ranking signal. On CIFAR-10, DeepSeek-Coder-6.7B improves from 28.2% to 69.2%, Qwen2.5-7B from 50.0% to 71.5%, and GLM-5 from 43.2% to 62.0%. A full 2000-iteration search completes in ${\approx}18$ GPU hours on a single RTX~4090, establishing a low-budget, reproducible, and hardware-aware paradigm for LLM-driven NAS without cloud infrastructure.

---

## 论文详细总结（自动生成）

这是一份关于论文《Resource-Efficient Iterative LLM-Based NAS with Feedback Memory》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的神经架构搜索（NAS）方法（如强化学习或进化算法）计算成本极高，动辄消耗数千个 GPU 天。虽然现有研究开始利用大语言模型（LLM）生成代码，但大多局限于“单次生成”或在受限的预定义搜索空间内运行，缺乏像人类工程师那样的“构建-测试-分析-改进”的迭代能力。
*   **核心问题**：如何在单张消费级 GPU 的有限资源下，利用小型冻结 LLM（≤7B）在无约束的开放代码空间中，通过自我迭代和历史反馈实现高效的神经架构发现？

### 2. 方法论
该论文提出了一个闭环迭代流水线，核心思想是利用 LLM 的推理能力来驱动架构的演进。
*   **核心组件**：
    1.  **代码生成器 (Code Generator)**：调用 LLM 生成完整的 PyTorch 模型类。
    2.  **评估器 (Evaluator)**：进行快速验证（Forward Pass）和代理训练（在目标数据集上训练 1 个 epoch），以 Top-1 准确率作为排名信号。
    3.  **提示词改进器 (Prompt Improver)**：分析评估结果，结合历史记忆生成改进建议。
*   **关键技术细节**：
    *   **历史反馈记忆 (Historical Feedback Memory)**：受马尔可夫链启发，采用 $K=5$ 的滑动窗口记录最近的尝试。这保证了上下文长度恒定，避免了长序列导致的显存溢出。
    *   **结构化诊断三元组**：每个历史条目包含“识别的问题”、“建议的修改”和“产生的结果”。特别地，它将**代码执行失败**（如维度不匹配）视为重要的学习信号，而非直接丢弃。
    *   **双 LLM 分工**：将任务分解为代码生成和诊断推理，降低单个 LLM 调用的认知负荷。
    *   **硬件感知搜索**：由于 LLM 和架构训练共享单卡的 24GB 显存，这种资源约束隐式地引导 LLM 发现更紧凑、硬件效率更高的模型。

### 3. 实验设计
*   **数据集**：CIFAR-10、CIFAR-100 和 ImageNette（ImageNet 的 10 类子集）。
*   **Benchmark (基准)**：以 LLM 的 **Single-shot (单次生成)** 结果作为基线，即没有任何反馈时的初始表现。
*   **对比对象**：评估了三个不同来源的冻结指令微调 LLM：
    1.  **DeepSeek-Coder-6.7B-Instruct**（代码专精型）
    2.  **Qwen2.5-7B-Instruct**（通用型）
    3.  **GLM-5**（大规模通用型，仅运行 100 次迭代）

### 4. 资源与算力
*   **硬件平台**：单张 **NVIDIA GeForce RTX 4090 (24GB VRAM)**。
*   **训练时长**：完成一个完整的 2000 次迭代搜索大约需要 **18 小时**。
*   **低预算特性**：无需云端基础设施，无需对 LLM 进行任何微调（Frozen LLMs）。

### 5. 实验数量与充分性
*   **实验规模**：针对 DeepSeek 和 Qwen 进行了长达 **2000 次迭代**的搜索，这在 LLM-NAS 领域属于较大规模的轨迹观察。
*   **消融实验**：对比了“完整功能”、“无反馈记忆”和“无参考代码”的情况，验证了反馈机制的必要性。
*   **充分性评价**：实验覆盖了不同难度的数据集（从 10 类到 100 类）和不同特性的 LLM，结果具有统计学意义（计算了 Spearman 和 Kendall 相关系数）。实验设计客观，通过固定随机种子确保了可复现性。

### 6. 主要结论与发现
*   **性能显著提升**：在 CIFAR-10 上，DeepSeek-Coder 从 28.2% 提升至 69.2%，Qwen2.5 从 50.0% 提升至 71.5%。
*   **迭代有效性**：所有模型在准确率曲线上均表现出明显的上升趋势，证明了历史反馈记忆能有效引导模型避开失败路径。
*   **模型特性差异**：
    *   **DeepSeek-Coder**：生成稳定性最高（成功率达 76%-95%），改进幅度最大。
    *   **Qwen2.5**：虽然成功率较低（约 18.8%），但倾向于尝试复杂架构，最终挖掘出了最高的峰值准确率。
    *   **GLM-5**：即使只跑 100 次，也表现出极高的生成可靠性和稳健的改进趋势。

### 7. 优点
*   **资源极其高效**：将 NAS 的门槛降低到了单张消费级显卡，具有极高的实用价值。
*   **处理失败信号**：不同于以往只保留“精英”样本的方法，该方法通过分析错误代码来学习，更符合人类工程逻辑。
*   **开放搜索空间**：不局限于预定义的“单元（Cell）”，可以生成任意结构的 PyTorch 代码，具备发现新颖架构的潜力。

### 8. 不足与局限
*   **代理指标局限性**：仅使用 1-epoch 的准确率作为反馈信号，虽然快速，但可能无法完全反映模型收敛后的最终性能。
*   **模型失败率波动**：在某些任务（如 ImageNette）上，部分 LLM（如 DeepSeek）的生成成功率极低（0.7%），说明提示词工程对特定模型的鲁棒性仍有待加强。
*   **任务复杂度**：目前仅在小分辨率图像分类任务上验证，尚未在大规模数据集（如完整的 ImageNet）或更复杂的任务（如目标检测）上进行测试。

（完）
