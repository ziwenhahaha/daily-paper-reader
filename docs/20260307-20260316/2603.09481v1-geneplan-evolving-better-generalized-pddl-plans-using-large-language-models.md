---
title: "GenePlan: Evolving Better Generalized PDDL Plans using Large Language Models"
title_zh: GenePlan：利用大语言模型演化更好的通用 PDDL 规划
authors: "Andrew Murray, Danial Dervovic, Alberto Pozanco, Michael Cashmore"
date: 2026-03-10
pdf: "https://arxiv.org/pdf/2603.09481v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 利用大模型演化优化算法
tldr: GenePlan是一个利用大语言模型（LLM）辅助进化算法生成通用规划器的框架。它将通用规划任务建模为优化问题，通过迭代演化可解释的Python代码来最小化PDDL任务的计划长度。在多个基准测试中，GenePlan的表现接近最先进的规划器，显著优于CoT等LLM基线，且具备极高的推理效率和低廉的生成成本。
selection_source: fresh_fetch
motivation: 针对传统规划器在复杂任务中效率受限以及LLM直接生成规划方案准确性不足的挑战，寻求一种更高效、通用的规划方法。
method: 提出一种基于LLM的进化算法框架，通过迭代优化生成能够处理特定领域内多样化实例的Python通用规划器。
result: 在8个测试领域中取得了0.91的平均SAT分数，接近SOTA水平，且每项任务平均仅需0.49秒，成本极低。
conclusion: GenePlan证明了结合LLM与进化算法可以自动生成高性能、可解释且经济的通用规划器，为解决经典规划问题提供了新思路。
---

## 摘要
我们提出了 GenePlan（通用演化规划器），这是一个利用大语言模型（LLM）辅助演化算法的新型框架，旨在为 PDDL 描述的经典规划任务生成领域相关的通用规划器。通过将通用规划建模为一个优化问题，GenePlan 迭代地演化出可解释的 Python 规划器，以最小化不同问题实例中的规划长度。在对六个现有基准领域和两个新领域的实证评估中，GenePlan 实现了 0.91 的平均 SAT 分数，与最先进的规划器性能（SAT 分数 0.93）非常接近，并显著优于其他基于 LLM 的基准方法，如思维链（CoT）提示（平均 SAT 分数 0.64）。生成的规划器能够快速解决新实例（每个任务平均 0.49 秒），且成本较低（使用 GPT-4o 每个领域平均 1.82 美元）。

## Abstract
We present GenePlan (GENeralized Evolutionary Planner), a novel framework that leverages large language model (LLM) assisted evolutionary algorithms to generate domain-dependent generalized planners for classical planning tasks described in PDDL. By casting generalized planning as an optimization problem, GenePlan iteratively evolves interpretable Python planners that minimize plan length across diverse problem instances. In empirical evaluation across six existing benchmark domains and two new domains, GenePlan achieved an average SAT score of 0.91, closely matching the performance of the state-of-the-art planners (SAT score 0.93), and significantly outperforming other LLM-based baselines such as chain-of-thought (CoT) prompting (average SAT score 0.64). The generated planners solve new instances rapidly (average 0.49 seconds per task) and at low cost (average $1.82 per domain using GPT-4o).

---

## 论文详细总结（自动生成）

这篇论文由摩根大通（J.P. Morgan）AI 研究团队发表，提出了一种名为 **GenePlan** 的框架，旨在通过大语言模型（LLM）驱动的进化算法生成高质量的通用 PDDL 规划器。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：尽管 LLM 在代码生成方面表现出色，但在处理复杂的顺序决策（规划）任务时，直接生成的计划往往质量不佳或不符合逻辑约束。
*   **研究动机**：
    *   现有的“通用规划”（Generalized Planning）方法通常只关注找到可行解（Satisficing），而忽略了计划的质量（如路径长度）。
    *   传统的搜索规划器（如 Fast Downward）虽然强大，但在处理大规模新实例时计算成本高且速度慢。
    *   **GenePlan** 的目标是利用 LLM 的领域知识，通过进化算法自动优化并生成可解释的 Python 代码（即通用规划器），从而在保证计划质量的同时，实现极速推理。

### 2. 方法论：核心思想与算法流程
*   **核心思想**：将通用规划建模为一个**优化问题**。目标是找到一个 Python 函数 $\Phi$，使得在特定领域的所有训练任务中，平均计划长度最小。
*   **关键技术细节**：
    *   **进化框架**：采用类似进化策略的迭代过程。种群中的个体是 Python 编写的规划器代码。
    *   **适应度评估（Fitness）**：使用 PDDL 验证器（VAL）评估生成的 Python 代码在训练集上的表现。适应度函数定义为平均计划长度，无法解决任务的规划器会被赋予惩罚分。
    *   **LLM 作为算子**：LLM（如 GPT-4o）同时充当“交叉（Crossover）”和“变异（Mutation）”算子。通过在 Prompt 中提供多个父代代码及其得分/错误反馈，引导 LLM 生成更优的后代。
    *   **选择机制**：使用基于温度的玻尔兹曼分布进行采样，早期鼓励探索（高溢出），后期鼓励收敛（低溢出）。
    *   **代码保护**：引入 AST（抽象语法树）解析器作为护栏，确保生成的 Python 代码安全且符合语法规范。

### 3. 实验设计
*   **实验场景**：涵盖了 8 个 PDDL 领域，包括 6 个经典基准领域（如 Hiking, Gripper, Miconic）和 2 个新创建的领域（Research, Trading），以测试 LLM 的泛化能力而非单纯记忆。
*   **对比基准（Baselines）**：
    *   **传统规划器**：Fast Downward (LAMA 模式，设置 300s 和 1800s 时限) 以及最优规划器 (SEQ-OPT-LMCUT)。
    *   **LLM 基准**：GPT-4 和 GPT-4o 的思维链（CoT）提示词方法。
    *   **消融实验**：包括使用小模型（GPT-4o mini）、消去语义名称（Ablation）、无反馈演化等变体。
*   **评估指标**：SAT 分数（IPC 标准，越接近 1 越好）、运行时间、生成成本（美元）。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的本地 GPU 硬件，因为主要依赖于 OpenAI 的 API（GPT-4o 和 GPT-4o mini）。
*   **时间成本**：生成一个领域的规划器平均需要约 **645 秒**（约 10 分钟）。
*   **经济成本**：使用 GPT-4o 生成一个领域的规划器平均花费仅为 **1.82 美元**。

### 5. 实验数量与充分性
*   **实验规模**：每个领域包含 5-10 个训练实例和 30 个测试实例，总计评估了数百个规划任务。
*   **充分性**：实验设计较为全面，不仅对比了 SOTA 传统工具，还通过消融实验验证了语义信息、反馈机制和模型规模对结果的影响。
*   **客观性**：引入了统计显著性检验（Friedman test 和 Nemenyi post-hoc test），并使用临界差异图（Critical Difference Diagram）展示结果，确保了结论的科学性。

### 6. 主要结论与发现
*   **性能卓越**：GenePlan 的平均 SAT 分数达到 **0.91**，非常接近最强传统规划器（0.93），且远超 CoT 方法（0.64）。
*   **推理极速**：一旦规划器（Python 代码）生成，解决新任务的平均时间仅为 **0.49 秒**，比传统搜索规划器快几个数量级。
*   **语义依赖**：消融实验显示，如果移除 PDDL 中的谓词和动作名称（使其失去语义），性能会大幅下降，证明 LLM 极大地依赖其预训练中获得的常识。
*   **成本效益**：相比于长时间运行的搜索算法，GenePlan 提供了一种低成本、高回报的自动化规划器设计方案。

### 7. 优点
*   **可解释性**：生成的产物是 Python 代码，人类开发者可以阅读、调试和验证其逻辑。
*   **高质量优化**：不同于以往只求“能用”的 LLM 规划研究，GenePlan 成功实现了对“计划长度”的自动优化。
*   **灵活性**：框架不依赖于特定 LLM，可以随模型能力的提升（如使用 DeepSeek-R1 或 GPT-5）而自动增强。

### 8. 不足与局限
*   **领域限制**：对于不存在简单通用策略的领域（如 **Sokoban/推箱子**，涉及大量不可逆状态和深层搜索），GenePlan 表现不佳，无法替代基于搜索的规划器。
*   **幻觉风险**：尽管有 AST 检查，LLM 仍可能生成逻辑上有微妙错误的代码，需要依赖训练集的覆盖度来发现。
*   **冷启动成本**：虽然推理快，但初始的演化过程（10 分钟）在只需要解决单个简单任务时显得冗余。

（完）
