---
title: "VADF: Vision-Adaptive Diffusion Policy Framework for Efficient Robotic Manipulation"
title_zh: VADF：面向高效机器人操作的视觉自适应扩散策略框架
authors: "Xinglei Yu, Zhenyang Liu, Shufeng Nan, Simo Wu, Yanwei Fu"
date: 2026-04-17
pdf: "https://arxiv.org/pdf/2604.15938v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 用于机器人操作的扩散策略
tldr: 针对扩散策略在机器人操作中训练收敛慢和推理效率低的问题，本文提出VADF框架。该框架在训练阶段引入自适应损失网络（ALN），通过难负样本挖掘和加权采样提升收敛速度；在推理阶段利用分层视觉任务分割器（HVTS）将任务分解，并根据动作复杂度动态调整噪声步长和执行序列长度。VADF具有模型无关性，能显著降低计算开销并提升推理成功率。
selection_source: fresh_fetch
motivation: 扩散策略因均匀采样导致的难负样本不平衡和缺乏样本难度感知，造成训练收敛慢及推理超时。
method: 提出由ALN（训练期实时量化样本难度并加权采样）和HVTS（推理期根据视觉反馈动态调整动作序列与噪声步长）组成的双重自适应框架。
result: 该方法显著减少了训练收敛步数，大幅降低了推理计算开销，并提高了早期任务成功率。
conclusion: VADF是一个通用的视觉驱动框架，通过在训练和推理阶段引入自适应机制，有效提升了扩散策略在机器人操作中的效率和性能。
---

## 摘要
扩散策略正成为机器人操作的主流，但由于均匀采样和缺乏对样本难度的感知，面临着难负样本类别不平衡的问题，导致训练收敛缓慢以及推理过程中频繁出现超时失败。我们提出了 VADF（视觉自适应扩散策略框架），这是一种视觉驱动的双重自适应框架，能够显著减少收敛步数并在推理中实现早期成功。其模型无关的设计使其能够无缝集成到任何扩散策略架构中。在训练期间，我们引入了自适应损失网络（ALN），这是一种基于 MLP 的轻量级损失预测器，可实时量化每步样本的难度。在难负样本挖掘的引导下，它执行加权采样以优先处理高损失区域，从而实现自适应权重更新和更快的收敛。在推理阶段，我们设计了分层视觉任务分割器（HVTS），它根据视觉输入将高级任务指令分解为多阶段的低级子指令。它通过为简单动作分配较短的加噪计划和较长的直接执行序列，为复杂动作分配较长的加噪步数和较短的执行序列，从而将动作序列自适应地分割为简单和复杂的子任务，进而大幅降低计算开销并显著提高早期成功率。

## Abstract
Diffusion policies are becoming mainstream in robotic manipulation but suffer from hard negative class imbalance due to uniform sampling and lack of sample difficulty awareness, leading to slow training convergence and frequent inference timeout failures. We propose VADF (Vision-Adaptive Diffusion Policy Framework), a vision-driven dual-adaptive framework that significantly reduces convergence steps and achieves early success in inference, with model-agnostic design enabling seamless integration into any diffusion policy architecture. During training, we introduce Adaptive Loss Network (ALN), a lightweight MLP-based loss predictor that quantifies per-step sample difficulty in real time. Guided by hard negative mining, it performs weighted sampling to prioritize high-loss regions, enabling adaptive weight updates and faster convergence. In inference, we design the Hierarchical Vision Task Segmenter (HVTS), which decomposes high-level task instructions into multi-stage low-level sub-instructions based on visual input. It adaptively segments action sequences into simple and complex subtasks by assigning shorter noise schedules with longer direct execution sequences to simple actions, and longer noise steps with shorter execution sequences to complex ones, thereby dramatically reducing computational overhead and significantly improving the early success rate.

---

## 论文详细总结（自动生成）

### 论文总结：VADF (Vision-Adaptive Diffusion Policy Framework)

#### 1. 核心问题与整体含义（研究动机和背景）
扩散策略（Diffusion Policy）已成为机器人操作的主流方法，但现有框架存在两个主要瓶颈：
*   **训练效率低：** 传统方法采用均匀采样，忽略了样本难度的差异（即“难负样本”问题），导致模型在简单样本上浪费计算资源，而在关键、复杂的动作区域收敛缓慢。
*   **推理开销大且僵化：** 推理时通常采用固定的去噪步数（$N_d$）和动作预测步长（$N_a$）。这种“一刀切”的策略无法适应任务阶段的异质性（例如：接近物体的简单阶段不需要高精度，而抓取/插入的复杂阶段则需要更多计算资源），导致推理延迟高且在复杂阶段易失败。

**VADF** 旨在通过视觉驱动的双重自适应机制，在不改变底层模型架构的前提下，提升训练收敛速度和推理效率。

#### 2. 方法论：核心思想与关键技术
VADF 提出了两个核心模块，分别针对训练和推理阶段：

*   **自适应损失网络 (ALN - 训练阶段)：**
    *   **核心思想：** 引入一个轻量级的 MLP 损失预测器，实时量化每个采样步骤的难度。
    *   **技术细节：** 采用**可学习的时间步采样器**（基于 REINFORCE 算法更新）和**轨迹级重加权机制**。它通过难负样本挖掘（Hard Negative Mining），优先采样高损失（即高难度）的时间步和轨迹，从而加速模型对关键动作特征的掌握。
*   **分层视觉任务分割器 (HVTS - 推理阶段)：**
    *   **核心思想：** 利用视觉语言模型（VLM）的零样本（Zero-shot）推理能力，将长程任务动态分解为不同难度的子阶段。
    *   **技术细节：** 
        1.  **任务分解：** VLM 将任务指令分解为多个语义阶段（如：接近、对齐、推入、完成）。
        2.  **动态调度：** 为简单阶段分配较少的去噪步数（$N_d$）和较长的执行序列（$N_a$）以提升速度；为复杂阶段分配较多的去噪步数和较短的执行序列以提升精度。
        3.  **实时分类：** 在推理过程中，VLM 根据实时视觉反馈识别当前所处阶段，动态调整计算预算。

#### 3. 实验设计
*   **数据集/场景：**
    *   **Robomimic：** 包含 Lift, Can, Square, Transport, Push-T, ToolHang 等任务，涵盖不同的人类操作熟练度（Proficient/Mixed）。
    *   **Kitchen：** 长程多任务序列环境。
    *   **Adroit：** 极具挑战性的 24 自由度灵巧手操作任务（Door, Hammer, Pen）。
    *   **真实世界：** 使用 ARX5 机械臂进行杯子堆叠、苹果放入微波炉等实验。
*   **对比方法（Benchmarks）：**
    *   基础扩散策略（Diffusion Policy-CNN/Transformer）。
    *   IBC (Implicit BC), BCRNN。
    *   DP3（针对 3D 视觉的扩散策略改进版）。
    *   DDIM（用于加速采样的对比）。

#### 4. 资源与算力
*   **GPU 型号：** 训练使用 **NVIDIA RTX A6000** (48GB VRAM)；真实世界推理使用 **NVIDIA RTX 5880 Ada**。
*   **训练时长：** 训练周期在 600 到 3,500 epoch 之间（约 20k 至 120k 个梯度步数）。
*   **软件框架：** 基于 Hugging Face transformers 和 PyTorch，VLM 使用了 Qwen2-VL-7B-Instruct。

#### 5. 实验数量与充分性
*   **实验规模：** 论文在 10 多个仿真任务和 3 个真实世界任务上进行了测试。
*   **充分性：** 
    *   每个任务均使用 3 个随机种子进行平均，并在每个种子下进行 50 次初始条件的评估。
    *   进行了详尽的**消融实验**（验证 ALN 和 HVTS 的独立贡献）和**灵敏度分析**（研究 $N_a, N_d$ 范围及阶段数 $K$ 的影响）。
    *   实验设计涵盖了从 2D 到 3D、从简单抓取到复杂灵巧操作的多种维度，结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **性能提升：** VADF 在所有基准测试中均优于原始扩散策略，尤其在 Adroit 灵巧手任务中，成功率提升高达 10% 以上。
*   **效率优化：** 在推理阶段，VADF 配合 DDIM 采样器可实现高达 **2.46 倍** 的加速，同时保持或提升成功率。
*   **收敛加速：** ALN 模块显著减少了达到峰值成功率所需的训练步数，并降低了训练过程中的方差。
*   **通用性：** 作为一个“即插即用”框架，VADF 能无缝集成到 DP3 等现有先进模型中。

#### 7. 优点
*   **模型无关性（Model-agnostic）：** 不需要修改扩散模型的核心架构，易于推广。
*   **零样本能力：** HVTS 模块利用预训练 VLM 的常识，无需额外的人工标注或针对特定任务的训练即可实现任务分解。
*   **计算资源优化：** 首次在扩散策略中实现了“按需分配计算量”的理念，平衡了实时性与精度。

#### 8. 不足与局限
*   **VLM 依赖性：** HVTS 的效果高度依赖于底层 VLM（如 Qwen2-VL）的感知和推理能力，若 VLM 识别阶段出错，可能导致错误的参数调度。
*   **推理延迟抵消：** 虽然减少了去噪步数，但引入 VLM 进行阶段分类本身会带来额外的计算开销（文中通过降低分类频率来缓解）。
*   **应用限制：** 目前主要验证了其在扩散类生成策略上的效果，对于非生成式策略或其他架构的适用性尚待探索。

（完）
