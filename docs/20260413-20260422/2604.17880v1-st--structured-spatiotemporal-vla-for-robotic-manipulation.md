---
title: "ST-$π$: Structured SpatioTemporal VLA for Robotic Manipulation"
title_zh: ST-π：用于机器人操纵的结构化时空 VLA 模型
authors: "Chuanhao Ma, Hanyu Zhou, Shihan Peng, Yan Li, Tao Gu, Luxin Yan"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17880v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于细粒度机器人操作的结构化时空VLA模型
tldr: 针对现有VLA模型在精细时空操作中推理不明确的问题，本文提出了ST-π框架。该框架通过时空VLM生成包含子任务、空间和时间定位的块级动作提示，并结合时空动作专家进行双生成器引导，实现了从全局行为规划到局部动作控制的结构化建模。此外，研究还提供了一个带有时空标注的真实机器人数据集，实验证明了该方法在复杂操作任务中的优越性。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要依赖隐式的跨模态映射，难以处理具有明确时空边界的复杂序列化机器人操作任务。
method: 提出一种结构化框架，利用VLM生成块级动作提示进行全局规划，并设计时空动作专家通过双生成器引导预测步级动作参数。
result: 在真实世界机器人任务和模拟实验中，ST-π在处理精细时空操作方面表现出显著的性能提升。
conclusion: 结构化的时空建模能有效提升VLA模型在复杂机器人操作中的规划与控制能力。
---

## 摘要
视觉-语言-动作（VLA）模型在通用机器人任务上取得了巨大成功，但在细粒度时空操纵方面仍面临挑战。通常，现有方法主要将时空知识嵌入到视觉和动作表示中，并直接进行跨模态映射以进行步级动作预测。然而，这种时空推理在很大程度上仍然是隐式的，难以处理具有明确时空边界的多个序列行为。在这项工作中，我们提出了 ST-π，一种用于机器人操纵的结构化时空 VLA 模型。我们的模型由两个关键设计引导：1）时空 VLM。我们将 4D 观测和任务指令编码到潜空间中，并将其输入 LLM，以生成一系列具有因果顺序的块级动作提示，这些提示由子任务、空间定位和时间定位组成。2）时空动作专家。在块级动作提示的约束下，我们设计了一种结构化双生成器引导，以联合建模空间依赖性和时间因果关系，从而预测步级动作参数。在这个结构化框架内，VLM 显式地规划全局时空行为，而动作专家则进一步细化局部时空控制。此外，我们提出了一个具有结构化时空标注的真实世界机器人数据集用于微调。广泛的实验证明了我们模型的有效性。代码链接：https://github.com/chuanhaoma/ST-pi。

## Abstract
Vision-language-action (VLA) models have achieved great success on general robotic tasks, but still face challenges in fine-grained spatiotemporal manipulation. Typically, existing methods mainly embed spatiotemporal knowledge into visual and action representations, and directly perform a cross-modal mapping for step-level action prediction. However, such spatiotemporal reasoning remains largely implicit, making it difficult to handle multiple sequential behaviors with explicit spatiotemporal boundaries. In this work, we propose ST-$π$, a structured spatiotemporal VLA model for robotic manipulation. Our model is guided by two key designs: 1) Spatiotemporal VLM. We encode 4D observations and task instructions into latent spaces, and feed them into the LLM to generate a sequence of causally ordered chunk-level action prompts consisting of sub-tasks, spatial grounding and temporal grounding. 2) Spatiotemporal action expert. Conditioned on chunk-level action prompts, we design a structured dual-generator guidance to jointly model spatial dependencies and temporal causality, thus predicting step-level action parameters. Within this structured framework, the VLM explicitly plans global spatiotemporal behavior, and the action expert further refines local spatiotemporal control. In addition, we propose a real-world robotic dataset with structured spatiotemporal annotations for fine-tuning. Extensive experiments have been conducted to demonstrate the effectiveness of our model. Our code link: https://github.com/chuanhaoma/ST-pi.

---

## 论文详细总结（自动生成）

这是一份关于论文《ST-$\pi$: Structured SpatioTemporal VLA for Robotic Manipulation》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型在处理**细粒度时空操纵**和**长程（Long-horizon）任务**时表现不佳。
*   **研究动机**：
    *   传统模型（如 OpenVLA）多基于单帧观测，缺乏显式的时空表示，导致在复杂任务中难以捕捉子任务间的时空边界。
    *   现有的 4D VLA 方法虽然引入了时间维度，但其推理过程往往是隐式的，难以处理具有明确因果依赖和阶段转换的序列化行为。
    *   论文旨在通过**显式且结构化**的时空建模，实现从全局行为规划到局部动作执行的精准控制。

### 2. 方法论
ST-$\pi$ 框架由两个核心组件组成：
*   **时空视觉语言模型 (ST-VLM)**：
    *   **4D 表示构建**：融合多视图视频（视觉特征）、几何线索（VGGT 编码的几何特征）和时间戳（傅里叶位置编码），构建统一的 4D 潜空间表示。
    *   **结构化任务分解**：将高层指令分解为一系列“块级动作提示（Chunk-level Action Prompts）”。每个提示包含：**语义意图**（做什么）、**空间定位**（在哪里做，3D 边界框）和**时间跨度**（执行多久）。
    *   **因果规划**：利用块级因果注意力机制，确保子任务按逻辑顺序生成。
*   **时空动作专家 (ST-AE)**：
    *   **双生成器引导**：设计了**空间运动生成器**（捕捉全局空间结构，确保轨迹平滑）和**时间运动生成器**（捕捉步级时间依赖，确保时序一致性）。
    *   **流匹配（Flow Matching）**：基于流匹配生成动作块，通过时间相关的融合策略，在生成早期侧重空间塑形，后期侧重时间细化。

### 3. 实验设计
*   **数据集与场景**：
    *   **ScanNet 系列**：用于初始的几何特征与视觉表示对齐。
    *   **DROID-ST**：从 DROID 数据集衍生，用于训练结构化任务分解。
    *   **STAR (新提出)**：真实世界数据集，包含 30 个操纵任务（物体识别、序列目标、长程任务），约 30 万个交互步，带有详细的子任务时空标注。
*   **Benchmark**：LIBERO、SIMPLER（仿真）以及 STAR（真实世界）。
*   **对比方法**：OpenVLA, Octo, SpatialVLA, TraceVLA, 4D-VLA, CogACT, $\pi_{0.5}$ 等主流 VLA 模型。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX PRO 6000 GPU**。
*   **训练细节**：采用 AdamW 优化器，分为三个阶段：表示对齐、行为规划学习、机器人任务微调。
*   **模型底座**：VLM 部分基于 PaliGemma (3B)，动作专家部分基于 Gemma-300M。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真环境（LIBERO 的 4 个套件、SIMPLER 的 2 种协议）和真实世界机器人平台（Franka Research 3）。
*   **消融实验**：非常充分，分别验证了：
    1. 结构化框架（ST-VLM vs ST-AE）的有效性。
    2. 高层时空注意力结构（因果 vs 双向）。
    3. 观测模态（2D vs 3D vs 4D）的影响。
    4. 任务分解粒度（子任务数量 $K$）的影响。
*   **客观性**：通过成功率（SR）和完成时间（CT）双重指标评估，对比了从 2D 到 4D、从单系统到双系统的多种基准，实验设计较为公平。

### 6. 主要结论与发现
*   **显式建模的优越性**：在长程任务中，显式的时空分解显著提升了任务成功率，并缩短了完成时间。
*   **4D 表示的重要性**：融合几何与时间信息能显著增强模型对动态场景的理解。
*   **双生成器协同**：空间生成器负责轨迹平滑，时间生成器负责速度稳定，两者结合能产生最自然的机器人动作。
*   **分解粒度**：适度的任务分解（如 $K=3$）能平衡规划的灵活性与稳定性，过度分解可能引入噪声。

### 7. 优点（亮点）
*   **结构化创新**：将复杂的 VLA 任务拆解为“语义+空间+时间”的显式提示词，降低了长程规划的难度。
*   **双专家设计**：ST-AE 巧妙地解决了动作生成中空间平滑与时间一致性之间的权衡。
*   **高质量数据集**：贡献了 STAR 数据集，为社区提供了带有细粒度时空标注的真实世界训练资源。

### 8. 不足与局限
*   **任务假设限制**：模型假设任务可以被分解为**线性序列**链。对于存在并行操作或复杂分支逻辑的任务，目前的因果分解结构可能无法完美处理。
*   **推理开销**：由于涉及 4D 融合、自回归任务分解以及流匹配迭代生成，其推理延迟可能高于简单的端到端模型（虽然文中 CT 指标表现良好，但未详细讨论实时计算负载）。
*   **标注依赖**：结构化训练依赖于高质量的子任务标注，这在扩展到更大规模的无标注数据时可能面临挑战。

（完）
