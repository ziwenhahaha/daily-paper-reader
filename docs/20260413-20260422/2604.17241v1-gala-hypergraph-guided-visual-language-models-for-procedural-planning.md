---
title: "GaLa: Hypergraph-Guided Visual Language Models for Procedural Planning"
title_zh: GaLa：用于过程规划的超图引导视觉语言模型
authors: "Kun Wang, Yiming Li, Mingcheng Qu, Aqiang Zhang, Guang Yang, Tonghua Su"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17241v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 具身智能中多模态过程规划的视觉语言框架
tldr: 针对具身智能中程序化规划任务，现有视觉语言模型难以有效捕捉物体间的隐式空间关系和深层语义结构。本文提出GaLa框架，通过超图表示法将物体建模为节点，并根据属性和功能语义构建区域级超边。结合TriView超图编码器，利用对比学习增强语义一致性，显著提升了模型在ActPlan1K和ALFRED基准上的规划成功率和准确性。
selection_source: fresh_fetch
motivation: 现有模型过度依赖VLM自身的推理能力，忽视了多模态输入中丰富的结构化语义信息，导致难以理解复杂场景中的功能空间关系。
method: 提出一种基于超图的表示方法，通过TriView编码器在节点、区域及关联视图间强制执行语义一致性，从而将结构化语义注入VLM推理过程。
result: 在ActPlan1K和ALFRED基准测试中，GaLa在执行成功率、LCS和规划正确性等关键指标上均显著优于现有先进方法。
conclusion: 通过引入超图引导的结构化语义建模，可以有效增强视觉语言模型在复杂具身场景下的程序化规划能力。
---

## 摘要
编码在物体属性中的隐式空间关系和深层语义结构对于具身智能系统中的过程规划至关重要。然而，现有方法往往过度依赖视觉语言模型（VLM）本身的推理能力，而忽视了可以从多模态输入中挖掘的丰富结构化语义信息。因此，模型难以有效理解复杂场景中的功能性空间关系。为了充分利用多模态数据中的隐式空间关系和深层语义结构，我们提出了 GaLa，一个用于多模态过程规划的视觉语言框架。GaLa 引入了一种基于超图的表示方法，将图像中的物体实例建模为节点，并根据物体的属性和功能语义聚合物体来构建区域级超边。该设计显式地捕捉了物体间的隐式语义关系以及功能区域的层级组织。此外，我们设计了一个三视图超图编码器（TriView HyperGraph Encoder），通过对比学习确保节点视图、区域视图和节点-区域关联视图之间的语义一致性，从而使超图语义能够更有效地注入到下游 VLM 推理中。在 ActPlan1K 和 ALFRED 基准测试上的广泛实验表明，GaLa 在执行成功率、LCS 和规划正确性方面显著优于现有方法。

## Abstract
Implicit spatial relations and deep semantic structures encoded in object attributes are crucial for procedural planning in embodied AI systems. However, existing approaches often over rely on the reasoning capabilities of vision language models (VLMs) themselves, while overlooking the rich structured semantic information that can be mined from multimodal inputs. As a result, models struggle to effectively understand functional spatial relationships in complex scenes. To fully exploit implicit spatial relations and deep semantic structures in multimodal data, we propose GaLa, a vision language framework for multimodal procedural planning. GaLa introduces a hypergraph-based representation, where object instances in the image are modeled as nodes, and region-level hyperedges are constructed by aggregating objects according to their attributes and functional semantics. This design explicitly captures implicit semantic relations among objects as well as the hierarchical organization of functional regions. Furthermore, we design a TriView HyperGraph Encoder that enforces semantic consistency across the node view, area view, and node area association view via contrastive learning, enabling hypergraph semantics to be more effectively injected into downstream VLM reasoning. Extensive experiments on the ActPlan1K and ALFRED benchmarks demonstrate that GaLa significantly outperforms existing methods in terms of execution success rate, LCS, and planning correctness.

---

## 论文详细总结（自动生成）

这是一份关于论文《GaLa: Hypergraph-Guided Visual Language Models for Procedural Planning》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
在具身智能（Embodied AI）中，**过程规划（Procedural Planning）**要求智能体将高层指令分解为一系列精确动作。现有的视觉语言模型（VLM）在处理此类任务时，往往将图像视为无结构的 Token 序列，过度依赖模型自身的隐式推理能力。
*   **研究动机**：现有模型忽视了视觉场景中丰富的**结构化语义**（如“可放置于”、“被支撑”等隐式空间关系）和**层级组织**（如由多个物体构成的“厨房区”、“用餐区”等功能区域）。
*   **后果**：这导致智能体在复杂或含糊的场景中容易出现逻辑死锁、重复动作或忽略物理约束。

### 2. 方法论
GaLa 提出了一种基于**超图（Hypergraph）**的中间语义抽象表示法，核心流程分为三步：
1.  **超图语义构建（HyperGraph Semantic Construction）**：
    *   使用 YOLO-World 检测物体作为**节点**。
    *   利用 DBSCAN 算法对物体坐标进行空间聚类，并调用大模型（InternVL3）为聚类区域生成语义标签（如“烹饪区”），将其定义为**超边**。
    *   对物体属性进行反事实评分，识别异常状态。
2.  **三视图超图编码器（Tri-View HyperGraph Encoder）**：
    *   通过对比学习（InfoNCE Loss）在三个维度增强表示：**节点视图**（物体语义）、**区域视图**（超边语义）和**全视图**（节点与超边的隶属关系）。
    *   公式化目标：$L = L_n + \alpha_g L_g + \alpha_m L_m$，确保结构化知识被有效注入。
3.  **训练与推理**：将提炼的超图知识与原始视觉、文本输入一同喂给 VLM（以 InternVL3-8B 为主干），预测下一步动作。

### 3. 实验设计
*   **数据集**：
    *   **ActPlan-1K**：包含常规活动和反事实（Counterfactual）活动，测试模型在非常规条件下的适应能力。
    *   **ALFRED**：用于测试具身任务中的零样本（Zero-shot）泛化性能。
*   **评估指标**：执行成功率（Exec.）、最长公共子序列（LCS，衡量序列相似度）、任务正确性（Corr.）。
*   **对比方法**：
    *   闭源模型：GPT-4o, Gemini-Pro-1.5。
    *   开源通用模型：LLaVA-OV, Qwen2-VL, InternVL2/3/3.5 等。
    *   专用规划模型：Embodied-GPT, LLaPa。

### 4. 资源与算力
*   **硬件**：使用了 8 台 **NVIDIA HGX H200 (141GB)** GPU，处理器为 Intel Xeon 8558。
*   **训练配置**：训练时长为 30 个 Epoch，Batch Size 为 128，使用 Adam 优化器，初始学习率为 2e-5。
*   **输入处理**：图像分辨率从 600x600 调整为 448x448。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准上进行了详尽的定量对比。
*   **消融实验**：针对超图模块（w/o Hyper）以及三种不同的对比学习损失（HLn, HLg, HLm）分别在两个数据集上做了消融，验证了每个组件的增益。
*   **充分性评价**：实验设计较为全面，涵盖了从通用能力到专用能力的对比，且通过可视化案例（Case Study）直观展示了超图如何解决“关系盲区”导致的逻辑错误，实验结果具有说服力。

### 6. 主要结论与发现
*   **性能领先**：GaLa 在所有指标上均显著优于现有 SOTA 模型。在 ActPlan-1K 的反事实任务中，正确性比基座模型提升了约 9%。
*   **结构化优势**：显式建模物体间的隐式空间关系和区域级语义，能有效引导 VLM 生成更具空间感知力和逻辑一致性的动作序列。
*   **反事实推理**：超图表示有助于模型在环境发生异常变化时（如物体出现在不该出现的地方）依然能做出合理规划。

### 7. 优点
*   **创新性**：首次将超图理论引入 VLM 过程规划，将非结构化视觉转化为结构化语义知识。
*   **三视图设计**：通过对比学习解耦了语义编码与对比优化，增强了特征的鲁棒性。
*   **可解释性**：超图的引入使得模型的推理过程（如对区域的理解）更具可解释性。

### 8. 不足与局限
*   **数据依赖**：超图的构建依赖于输入图像的质量和数量。在 ActPlan-1K 中，由于动作关联的图像帧有限，可能导致构建的超图信息不完整。
*   **技术深度**：目前仅是超图在规划领域的初步尝试，尚未深入探索更高级的图论技术（如动态图卷积或更复杂的超图演化）。
*   **实时性挑战**：引入物体检测、聚类及 LLM 辅助标注等预处理步骤，可能会增加推理阶段的延迟。

（完）
