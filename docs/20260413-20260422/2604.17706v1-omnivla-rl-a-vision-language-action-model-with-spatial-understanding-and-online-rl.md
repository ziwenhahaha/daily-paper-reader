---
title: "OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL"
title_zh: OmniVLA-RL：一种具备空间理解能力与在线强化学习的视觉-语言-动作模型
authors: "Haoxiang Jie, Yaoyuan Yan, Xiangyu Wei, Kailin Wang, Hongjie Yan, Zhiyou Heng, Daocheng Chen"
date: 2026-04-20
pdf: "https://arxiv.org/pdf/2604.17706v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 具有空间理解和在线强化学习的VLA模型
tldr: OmniVLA-RL 针对具身智能中 VLA 模型空间感知不准、多模态融合欠佳及强化学习不稳定的挑战，提出了一种新型架构。该模型采用混合 Transformer (MoT) 设计，协同集成推理、空间和动作专家，并引入 Flow-GSPO 算法，将流匹配重构为 SDE 过程并结合分组分段策略优化。在 LIBERO 等基准测试中，该模型显著提升了动作精度和训练稳定性，表现优于现有 SOTA 方法。
selection_source: fresh_fetch
motivation: 旨在解决现有 VLA 模型在精确空间感知、多模态融合以及强化学习训练稳定性方面的核心缺陷。
method: 采用混合 Transformer (MoT) 架构整合多专家模块，并结合基于 SDE 的流匹配与分组分段策略优化 (GSPO)。
result: 在 LIBERO 和 LIBERO-Plus 基准测试中的实验结果表明，OmniVLA-RL 的性能显著优于当前最先进的方法。
conclusion: 该研究通过创新的架构与优化算法，有效克服了 VLA 模型的基础性限制，为具身智能提供了更精准稳健的解决方案。
---

## 摘要
视觉-语言-动作（VLA）模型代表了具身智能领域的范式转变，然而现有框架往往面临空间感知不精确、多模态融合欠佳以及强化学习不稳定性等挑战。为了弥补这些差距，我们提出了 OmniVLA-RL，这是一种利用 Mix-of-Transformers (MoT) 设计来协同整合推理、空间和动作专家的创新架构。此外，我们引入了 Flow-GSPO，它将流匹配（flow matching）重新表述为随机微分方程（SDE）过程，并将其与分组分段策略优化（GSPO）相结合，以增强动作精度和训练鲁棒性。在 LIBERO 和 LIBERO-Plus 基准测试上的广泛评估表明，OmniVLA-RL 显著优于现有最先进的方法，有效克服了当前 VLA 模型的根本局限性。

## Abstract
Visual-Language-Action (VLA) models represent a paradigm shift in embodied AI, yet existing frameworks often struggle with imprecise spatial perception, suboptimal multimodal fusion, and instability in reinforcement learning. To bridge these gaps, we propose OmniVLA-RL, a novel architecture that leverages a Mix-of-Transformers (MoT) design to synergistically integrate reasoning, spatial, and action experts. Furthermore, we introduce Flow-GSPO, which reformulates flow matching as a Stochastic Differential Equation (SDE) process and integrates it with Group Segmented Policy Optimization (GSPO) to enhance action precision and training robustness. Extensive evaluations on the LIBERO and LIBERO-Plus benchmarks demonstrate that OmniVLA-RL significantly outperforms state-of-the-art methods, effectively overcoming the fundamental limitations of current VLA models.

---

## 论文详细总结（自动生成）

这是一份关于论文《OmniVLA-RL: A Vision-Language-Action Model with Spatial Understanding and Online RL》的结构化深度总结：

### 1. 核心问题与整体含义
**研究动机与背景：**
现有的视觉-语言-动作（VLA）模型在具身智能领域虽有潜力，但存在三大核心缺陷：
*   **空间感知不精：** 传统视觉语言模型（VLM）擅长语义理解，但在输出物体3D位置和尺寸等精确空间信息方面表现较差，影响了抓取和避障的精度。
*   **多模态融合欠佳：** 现有的早期融合或后期融合方案仅在编码器或动作头层面修改，未能让语言、视觉语义和空间特征在模型核心层深度交互。
*   **强化学习（RL）不稳定：** PPO 架构过于复杂且计算量大，而基于 Token 的 GRPO 在处理动作序列时容易出现训练崩溃和偏置累积。

**核心目标：** 建立一个统一的框架，通过多专家协作提升空间智能，并通过改进的在线强化学习算法（Flow-GSPO）实现稳定、高精度的动作微调。

### 2. 方法论
**核心思想：** 采用混合 Transformer（MoT）架构整合不同能力的专家，并结合随机流匹配（Stochastic Flow Matching）与分组策略优化。

**关键技术细节：**
*   **混合 Transformer (MoT) 架构：**
    *   **推理专家 (Reasoning Expert)：** 基于预训练 VLM（如 PaLiGemma），提取高层语义和指令信息。
    *   **空间专家 (Spatial Expert)：** 使用 VGGT 编码器提取 3D 结构特征，并通过辅助空间头（重建点云、相机参数等）进行监督训练。
    *   **动作专家 (Action Expert)：** 负责将融合特征映射为动作轨迹，采用条件流匹配（CFM）建模动作分布。
*   **块级因果注意力 (Block-wise Causal Attention)：**
    *   将推理和空间 Token 设为“全可见前缀”（Omni-visible Prefix），允许双向对齐。
    *   将动作 Token 设为“因果后缀”，防止动作生成时的随机噪声污染感知特征。
*   **Flow-GSPO 算法：**
    *   **随机化改造：** 通过 Fokker-Planck 方程将确定性的 ODE 流匹配过程转化为随机微分方程（SDE），引入随机性以支持 RL 探索。
    *   **分组分段优化：** 借鉴 GSPO，在“动作块”（Action Block）级别而非 Token 级别计算重要性采样比率和优势函数，显著提升了序列生成的稳定性和一致性。

### 3. 实验设计
*   **数据集/场景：**
    *   **LIBERO：** 包含 Spatial（空间）、Object（物体）、Goal（目标）、Long（长程）四个子任务集。
    *   **LIBERO-Plus：** 更具挑战性的扩展集，包含复杂的组合式长程任务（如多步骤操作）。
    *   **预训练数据：** 使用 DROID 大规模机器人数据集进行动作预训练。
*   **对比方法：**
    *   **基础 VLA：** Diffusion Policy, Octo, OpenVLA, SpatialVLA, CoT-VLA, $\pi_0$, $\pi_{0.5}$, F1。
    *   **RL 算法对比：** PPO, GRPO。

### 4. 资源与算力
*   **文中说明：** 论文提到该模型可以在消费级 GPU 上运行（参考了 Smol-vlm 的效率），但在具体的训练配置上，文中**未明确给出**具体的 GPU 型号、数量及总训练时长。
*   **训练细节：** 提到 RL 阶段进行了 200 个更新步，每 10 步刷新一次 Rollout Buffer，Group Size $G=8$。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 LIBERO 的 4 个维度和 LIBERO-Plus 的复杂任务，提供了平均成功率、收敛曲线和消融实验。
*   **消融实验：** 针对 RL 范式（PPO vs GRPO vs Flow-GSPO）和架构组件（有无空间专家）进行了对比。
*   **充分性评价：** 实验设计较为全面，通过与多种 SOTA 模型在标准 Benchmark 上的对比，证明了其在空间精度和长程任务上的优势。对比 RL 算法的收敛曲线直观地展示了 Flow-GSPO 的稳定性。

### 6. 主要结论与发现
*   **性能领先：** OmniVLA-RL 在 LIBERO 上的平均成功率达到 **97.6%**，显著超过 $\pi_{0.5}$ 和 OpenVLA 等强基准。
*   **RL 稳定性：** Flow-GSPO 在收敛速度和最终性能上均优于 PPO 和 GRPO，尤其在 LIBERO-Plus 复杂任务中，成功率比 GRPO 高出约 14.6%。
*   **空间专家的必要性：** 消融实验显示，移除空间专家会导致性能下降 8.3%，证明了显式空间建模对高精度操作至关重要。

### 7. 优点
*   **架构创新：** MoT 架构巧妙地解决了多模态特征深度融合的问题，避免了早期/后期融合的局限。
*   **算法鲁棒：** 将流匹配与 GSPO 结合，解决了生成式动作模型在强化学习中难以稳定训练的痛点。
*   **感知增强：** 引入专门的空间专家和辅助监督任务，显著提升了 VLA 模型对 3D 环境的理解力。

### 8. 不足与局限
*   **仿真与现实差距（Sim-to-Real）：** 实验主要在仿真环境（LIBERO）中完成，尚未在真实物理机器人上进行大规模验证。
*   **缺乏世界模型：** 虽然处理长程任务表现良好，但模型内部缺乏一个显式的“世界模型”来预测环境状态转移，这可能限制其在极度复杂、非结构化环境中的规划能力。
*   **算力细节缺失：** 未详细披露训练所需的具体算力资源，不便于其他研究者评估复现成本。

（完）
