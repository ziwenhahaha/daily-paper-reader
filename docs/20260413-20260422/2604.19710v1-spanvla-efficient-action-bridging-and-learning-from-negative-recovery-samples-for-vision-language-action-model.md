---
title: "SpanVLA: Efficient Action Bridging and Learning from Negative-Recovery Samples for Vision-Language-Action Model"
title_zh: SpanVLA：视觉-语言-动作模型的高效动作桥接与负面-恢复样本学习
authors: "Zewei Zhou, Ruining Yang, Xuewei, Qi, Yiluan Guo, Sherry X. Chen, Tao Feng, Kateryna Pistunova, Yishan Shen, Lili Su, Jiaqi Ma"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19710v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于自动驾驶的视觉-语言-动作模型，具有高效的动作衔接
tldr: 针对自动驾驶中VLA模型推理延迟高和鲁棒性不足的问题，本文提出SpanVLA框架。该框架结合了自回归推理与流匹配动作专家，通过高效桥接机制利用VLM的推理能力指导轨迹规划，显著降低了推理时间。此外，引入基于GRPO的后训练方法，使模型能从负面样本中学习恢复行为。配合新发布的mReasoning数据集，SpanVLA在NAVSIM基准测试中表现出色，提升了复杂场景下的规划性能与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的自动驾驶VLA模型面临自回归生成导致的推理延迟高以及在长尾场景下鲁棒性不足的挑战。
method: 提出SpanVLA框架，通过流匹配策略加速动作生成，并利用GRPO算法在包含负面-恢复样本的数据集上进行后训练。
result: 在NAVSIM v1和v2基准测试中取得了竞争性性能，显著降低了推理时间并增强了复杂场景下的规划稳定性。
conclusion: SpanVLA通过高效的动作桥接和负面样本学习机制，为构建高效且鲁棒的自动驾驶视觉语言动作模型提供了新方案。
---

## 摘要
视觉-语言-动作（VLA）模型为利用世界知识和推理能力提供了一种极具前景的自动驾驶范式，特别是在长尾场景中。然而，现有的 VLA 模型在使用自回归生成框架进行动作生成时，往往面临高延迟的问题，且鲁棒性有限。在本文中，我们提出了 SpanVLA，这是一种新型的端到端自动驾驶框架，集成了自回归推理和流匹配（flow-matching）动作专家。首先，SpanVLA 引入了一种高效的桥接机制，利用视觉语言模型（VLM）的视觉和推理引导，通过以历史轨迹初始化为条件的流匹配策略来高效规划未来轨迹，从而显著缩短了推理时间。其次，为了进一步提高 SpanVLA 模型的性能和鲁棒性，我们提出了一种基于 GRPO 的后训练方法，使 VLA 模型不仅能从正面驾驶样本中学习，还能学习如何避免典型的负面行为并学习恢复行为。我们还进一步引入了 mReasoning，这是一个全新的真实世界驾驶推理数据集，专注于复杂的、对推理有高要求的场景以及负面-恢复样本。在 NAVSIM（v1 和 v2）上的大量实验证明了 SpanVLA 模型具有竞争力的性能。此外，在不同场景下的定性结果突显了我们模型的规划性能和鲁棒性。

## Abstract
Vision-Language-Action (VLA) models offer a promising autonomous driving paradigm for leveraging world knowledge and reasoning capabilities, especially in long-tail scenarios. However, existing VLA models often struggle with the high latency in action generation using an autoregressive generation framework and exhibit limited robustness. In this paper, we propose SpanVLA, a novel end-to-end autonomous driving framework, integrating an autoregressive reasoning and a flow-matching action expert. First, SpanVLA introduces an efficient bridge to leverage the vision and reasoning guidance of VLM to efficiently plan future trajectories using a flow-matching policy conditioned on historical trajectory initialization, which significantly reduces inference time. Second, to further improve the performance and robustness of the SpanVLA model, we propose a GRPO-based post-training method to enable the VLA model not only to learn from positive driving samples but also to learn how to avoid the typical negative behaviors and learn recovery behaviors. We further introduce mReasoning, a new real-world driving reasoning dataset, focusing on complex, reasoning-demanding scenarios and negative-recovery samples. Extensive experiments on the NAVSIM (v1 and v2) demonstrate the competitive performance of the SpanVLA model. Additionally, the qualitative results across diverse scenarios highlight the planning performance and robustness of our model.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **SpanVLA**，一个旨在解决自动驾驶中视觉-语言-动作（VLA）模型推理延迟高和鲁棒性不足问题的端到端框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的自动驾驶 VLA 模型虽然具备强大的推理和泛化能力，但面临两大挑战：
    1.  **高延迟**：传统的自回归（Autoregressive）解码方式在生成长动作序列时速度极慢，难以满足自动驾驶的高频控制需求。
    2.  **鲁棒性有限**：现有模型大多仅通过模仿学习（Imitation Learning）学习专家（正面）样本，缺乏对错误行为的辨别能力以及从危险状态中恢复的能力。
*   **核心目标**：构建一个既能保持 VLM 推理优势，又能实现高效动作生成，并能从“负面-恢复”样本中学习鲁棒策略的系统。

### 2. 方法论
SpanVLA 结合了自回归推理和非自回归动作生成，核心组件包括：
*   **VLM 主干网络**：采用 Qwen2.5VL-3B，负责处理多视角视频和文本指令，生成思维链（CoT）推理。
*   **高效动作桥接（Efficient Action Bridging）**：
    *   **稀疏层特征提取**：不使用 VLM 的全部层，而是从稀疏的中间层提取 KV Cache 特征，减少冗余。
    *   **历史初始化流匹配（Flow-matching with Historical Initialization）**：不同于从纯高斯噪声开始的扩散/流匹配模型，SpanVLA 使用历史轨迹嵌入作为初始状态，学习从过去动作到未来动作的变换，显著提高了生成效率和质量。
*   **强化微调（RFT）与负面-恢复学习**：
    *   **GRPO 算法**：使用群体相对策略优化（Group Relative Policy Optimization），通过群体样本的相对奖励进行稳定训练。
    *   **奖励函数设计**：除了标准的驾驶得分（PDMS），引入了**负面行为惩罚**（针对不理想的真实轨迹）和**恢复行为奖励**（针对专家纠偏轨迹），并加入推理长度惩罚以实现“自适应推理”。

### 3. 实验设计
*   **数据集**：
    *   **nuPlan (Open-Scene)**：10 万个场景。
    *   **mReasoning (新发布)**：包含 3 万个复杂推理场景，以及 6000 个专门的“负面-恢复”样本（3k 负面 + 3k 恢复）。
*   **基准测试（Benchmark）**：NAVSIM v1 (navtest) 和 NAVSIM v2 (navtest & navhard)。
*   **对比方法**：
    *   传统端到端方法：TransFuser, Hydra-MDP, DiffusionDrive 等。
    *   VLA 相关方法：AutoVLA, DriveVLA, ReCogDrive 等。
*   **评估指标**：PDMS/EPDMS（综合驾驶得分，涵盖碰撞、合规性、舒适度、进度等）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU**。
*   **训练细节**：
    *   **SFT 阶段**：使用 FSDP（全分片数据并行）进行全量微调。
    *   **RFT 阶段**：使用 LoRA 适配器进行高效微调。
    *   **参数设置**：VLM 训练 Batch Size 为 1（梯度累积 4），动作专家训练 Batch Size 为 16，GRPO 采样组大小为 64。

### 5. 实验数量与充分性
*   **实验规模**：论文在 NAVSIM v1 和 v2 两个主流榜单上进行了全面测试，并针对最难的 `navhard` 场景进行了闭环模拟评估。
*   **消融实验**：非常充分。涵盖了桥接层选择、历史初始化的影响、RFT 数据配比（正面/负面/恢复样本的比例）、奖励函数权重等多个维度。
*   **客观性**：通过对比自回归生成与动作专家的推理耗时，客观展示了效率提升；通过定性可视化展示了模型在处理施工区、切入等长尾场景下的纠偏能力。

### 6. 主要结论与发现
*   **性能领先**：SpanVLA 在 NAVSIM v1 和 v2 上均达到了 SOTA（最先进）水平。
*   **效率飞跃**：相比于纯自回归 VLA 模型（如 AutoVLA），SpanVLA 的动作生成耗时降低了 **46% 至 74%**，且轨迹点越多，优势越明显。
*   **鲁棒性增强**：引入负面-恢复样本后，模型在复杂场景下的 PDMS 得分显著提升，证明了“从错误中学习”的有效性。
*   **自适应推理**：RFT 成功减少了简单场景下的冗余推理步骤，实现了“快慢思考”的平衡。

### 7. 优点
*   **创新的桥接机制**：利用历史轨迹初始化流匹配，比传统的从噪声生成轨迹更符合物理连续性，收敛更快。
*   **数据价值挖掘**：首次在 VLA 领域系统性地引入真实世界的负面和恢复样本，填补了模仿学习无法处理“反面教材”的空白。
*   **开源贡献**：引入了 mReasoning 数据集，为社区提供了高质量的复杂场景推理标注。

### 8. 不足与局限
*   **实时性仍有差距**：虽然大幅提速，但目前 1.5 Hz 的推理频率（主要受限于 VLM 主干）距离真正的实时部署（通常需 10Hz 以上）仍有距离，需进一步的硬件加速或模型蒸馏。
*   **奖励函数敏感性**：RFT 阶段的奖励权重（如负面惩罚力度）需要精细调优，否则可能导致模型过于保守或产生非预期行为。
*   **闭环测试覆盖**：虽然在 NAVSIM v2 进行了伪闭环测试，但在更复杂的动态交互模拟器中的表现仍待进一步验证。

（完）
