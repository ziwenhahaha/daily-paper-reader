---
title: "Learning Weakly Communicating Average-Reward CMDPs: Strong Duality and Improved Regret"
title_zh: ...
authors: "Kihyun Yu, Beomhan Baek, Dabeen Lee"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11586v1"
tags: ["query:rl"]
score: 9.0
evidence: 平均奖励CMDP理论与原对偶强化学习算法
tldr: 。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11586v1/fig-001.webp\", \"caption\": \"Figure 3: Comparison of random agent, Algorithm 2 in Ghosh et al. [2023], and PD-LSCVI-UCB (Algorithm 1). Each plot displays regret and constraint violation for K = 1000 episodes. The shaded regions indicate 95% confidence intervals.\", \"page\": 42, \"index\": 1, \"width\": 976, \"height\": 281}]"
motivation: 。
method: 。
result: 。
conclusion: 。
---

## 摘要
...

## Abstract
We study infinite-horizon average-reward constrained Markov decision processes (CMDPs) under the weakly communicating assumption. Our contributions are twofold. First, we establish strong duality for weakly communicating average-reward CMDPs over stationary policies with finite state and action spaces. Despite the absence of a linear programming formulation and the resulting nonconvexity under the weakly communicating setting, we show that strong duality still holds by carefully exploiting the geometric structure of the occupation measure set. Second, building on this result, we propose a primal--dual clipped value iteration algorithm for learning weakly communicating average-reward linear CMDPs. Our algorithm achieves regret and constraint violation bounds of $\widetilde{\mathcal{O}}(T^{2/3})$, improving upon the best known bounds, where $T$ denotes the number of interactions. Our approach extends clipped value iteration to the constrained setting and adapts it to a finite-horizon approximation, which stabilizes the dual variable and is crucial for achieving improved regret bounds. To analyze this, we develop a novel approach based on strong duality that enables the decomposition of the composite Lagrangian regret into separate bounds on regret and constraint violation.

---

## 论文详细总结（自动生成）

<think>
这是一篇关于弱通信平均奖励约束马尔可夫决策过程（CMDP）的论文。让我仔细阅读并按照要求总结。

1. 论文核心问题与整体含义：
- 研究无限时域平均奖励CMDP，在弱通信假设下
- 弱通信假设比遍历性和单链假设更一般
- 主要挑战：之前没有人在弱通信设置下建立强对偶性
- 提出算法获得更好的 regret 界限

2. 方法论：
- 核心思想：利用 occupation measure 的几何结构建立强对偶性
- 关键技术：PD-LSCVI-UCB 算法
- 使用有限时域近似，结合值函数裁剪
- 基于强对偶性分解复合 Lagrangian regret

3. 实验设计：
- 使用表格 CMDP，基于 He et al. (2022) 的 MDP 修改
- 状态空间 S = {0, ..., H+1}
- 与随机智能体和 Ghosh et al. (2023) 的算法对比

4. 资源与算力：
- 论文中没有明确提到 GPU 或算力资源

5. 实验数量与充分性：
- 进行 4 次模拟，K = 1000 episodes
- 比较了 3 种方法


- 实验验证了算法的有效性和性能改进

6. 主要发现：
- 提出了首个弱通信平均奖励 CMDP 的强对偶理论框架
- 创新性地采用有限时域近似和值函数裁剪技术
- 实现了 O(T^{2/3}) 的遗憾和约束违反界限
- 在性能上显著优于 Ghosh 等人的 O(T^{3/4}) 方法

7. 创新点：
- 理论层面：深入分析弱通信 MDP 的几何结构
- 算法层面：精准平衡探索与约束
- 计算效率：设计计算复杂度较低的算法

8. 局限性：
- 计算复杂度受状态空间规模限制
- 遗憾界限未达最优，存在进一步优化空间
- 实验验证范围有待进一步扩展
- 对初始状态选择可能存在敏感性问题
</think>

# 论文总结：Learning Weakly Communicating Average-Reward CMDPs: Strong Duality and Improved Regret

## 1. 论文的核心问题与整体含义

### 研究动机与背景
- **问题领域**：安全强化学习（Safe RL），核心是在满足安全约束的同时优化性能
- **具体设置**：无限时域平均奖励约束马尔可夫决策过程（Average-Reward CMDP）
- **关键挑战**：在**弱通信（Weakly Communicating）**假设下，学习算法的理论保证较差
  - 强通信（Ergodic）→ O(√T) regret
  - 单链（Unichain）→ O(√T) regret
  - 弱通信（Weakly Communicating）→ 之前最优为 O(T³/⁴)
- **核心困难**：弱通信设置下，CMDP 不能等价转化为线性规划（LP），导致问题非凸，传统对偶理论不直接适用

### 论文贡献概述
1. 首次在弱通信平均奖励 CMDP 上建立**强对偶性**
2. 基于此提出改进的 primal-dual 算法，达到 **O(T²/³)** regret

---

## 2. 论文提出的方法论

### 核心思想
利用 occupation measure（占领测度）集合的几何结构，即使它是非凸的，仍能恢复 LP 强对偶性的关键性质。

### 关键技术细节

#### (1) 强对偶性证明（Theorem 1）
- **观察**：弱通信设置下，occupation measure 集合 **bP(s₁)** 虽然非凸，但满足：
  - relint(P) ⊆ bP(s₁)
  - ext(P) ⊆ bP(s₁)
  - bP(s₁) ⊆ P
- **关键洞察**：虽然 bP(s₁) ≠ P（多边形的边界点可能缺失），但这种"缺失"恰好使得在 bP(s₁) 和 P 上优化的目标值相同
- **证明路径**：将原问题转化为 LP → 应用 LP 强对偶 → 替换为原问题的对偶形式

#### (2) 算法设计：PD-LSCVI-UCB
将 clipped value iteration 扩展到约束设置，并采用**有限时域近似**：

- **框架**：T 步分为 K = T²/³ 个 episode，每个 episode 长 H = T¹/³ 步
- **核心机制**：
  - **值函数裁剪（Line 7）**：将估计值裁剪到 [min + 2ζ, min + 2ζ + H(1+2/γ)]，利用 span 界 sp(V*_{r,h}) ≤ 2ζ
  - **Dual variable 更新**：λ_{k+1} = [λ_k + η(Hb - Σ_{h=1}^H g(s_{kh}, a_{kh}))]_{0, 2/γ}
  - **Slater 条件保证** λ* ≤ 1/γ，提供 dual variable 的上界

#### (3) 分析方法
- **复合 Lagrangian regret 分解**：Regret(T) + λViolation(T) 可分解为多个可绑定项
- **两步策略**：
  1. 对任意 λ ∈ [0, 2/γ] 给出上界
  2. 利用强对偶性获得 Regret 的下界，从而分离出 Violation 的界

---

## 3. 实验设计

### 场景与数据集
- **环境**：表格 CMDP（基于 He et al. 2022 修改）
  - 状态空间：S = {0, ..., H+1}（实验中 H=6，共8个状态）
  - 动作空间：A = {-1, 1}^{m-1}（实验中 m=2，共2个动作）
  - 奖励函数：r(s,a) = 0.4 × score(a)
  - 约束函数：g(s,a) = score(a)，阈值 b = 0.7
  - 转移：确定性转移至 s+1（概率 0.95 - 0.01·1^T a）或 H+1

### Benchmark 与对比方法
| 方法 | 描述 |
|------|------|
| **Random Agent** | 基线：随机策略 |
| **Ghosh et al. (2023)** | 先前最优算法，O(T³/⁴) regret |
| **PD-LSCVI-UCB (本文)** | 本文算法 |

---

## 4. 资源与算力

**论文中未明确提及 GPU 型号、数量或训练时长等算力信息。**

---

## 5. 实验数量与充分性

### 实验规模
- **模拟次数**：4 次独立模拟（不同随机种子）
- **Episode 数量**：K = 1000

### 充分性分析
| 评估维度 | 评估结果 |
|----------|----------|
| **对比方法数量** | 3 种（Random, Ghosh, 本文），数量适中 |
| **随机种子** | 4 个，支持统计显著性 |
| **消融实验** | 部分消融（自适应裁剪参数 ζ_k），但不够系统 |
| **场景多样性** | 仅一个表格场景，线性 CMDP 场景未进行真实实验 |
| **客观性** | 公平对比，均使用相同环境和随机种子 |

**结论**：实验设计基本合理，但覆盖场景较少，缺少对线性 CMDP 设置的实证验证。

---

## 6. 论文的主要结论与发现

1. **理论贡献**：即使在弱通信（非凸）设置下，CMDP 的强对偶性仍然成立
2. **算法改进**：PD-LSCVI-UCB 达到 **O(T²/³)** regret 和 constraint violation，优于之前的 O(T³/⁴)
3. **关键技术**：有限时域近似 + 值函数裁剪是提升性能的关键
4. **实验验证**：在表格 CMDP 上验证了算法的有效性

---

## 7. 优点

### 方法层面
- **理论突破**：首次在弱通信设置下建立强对偶性，利用几何结构巧妙绕过非凸性障碍
- **算法创新**：将值函数裁剪从无约束设置扩展到约束设置，稳定性好
- **分析框架**：基于强对偶性的 regret 分解方法具有通用性

### 实验层面
- **对比充分**：与随机基线和先前最优方法对比
- **参数透明**：超参数选择与理论分析一致

---

## 8. 不足与局限

### 理论层面
- **遗憾界限次优**：O(T²/³) 仍未达到 O(√T) 的最优速率
- **有限状态/动作空间**：强对偶性证明依赖有限维 LP arguments，难以推广到无限空间

### 实验层面
- **算力未披露**：缺少复现所需的算力信息
- **场景单一**：仅在表格 CMDP 上验证，线性 CMDP 设置缺乏实证
- **消融不完整**：值函数裁剪、dual variable 稳定化的贡献未充分消融

### 应用限制
- **计算复杂度**：O(d²|S||A|T²)，依赖 |S|（值函数裁剪需遍历所有状态）
- **依赖 Slater 条件**：需提前知道 Slater constant γ
- **假设最优策略存在**：弱通信 CMDP 可能不存在最优 stationary 策略（论文中给出反例）

---

（完）
