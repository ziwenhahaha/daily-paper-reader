---
title: "RankEvolve: Automating the Discovery of Retrieval Algorithms via LLM-Driven Evolution"
title_zh: RankEvolve：通过大语言模型驱动的进化自动发现检索算法
authors: "Jinming Nian, Fangchen Li, Dae Hoon Park, Yi Fang"
date: 2026-02-18
pdf: "https://arxiv.org/pdf/2602.16932v1"
tags: ["query:sr2026"]
score: 8.0
evidence: 通过程序演化和LLM自动发现算法
tldr: 本研究提出 RankEvolve，一种利用大语言模型（LLM）驱动进化搜索来自动发现检索算法的框架。针对传统检索算法（如 BM25）长期依赖人工调优的局限，该方法将排名算法表示为可执行代码，通过迭代变异、重组和基于 12 个数据集性能的筛选，成功演化出新型且高效的词法检索算法。实验证明，这些自动生成的算法在 BEIR、BRIGHT 和 TREC DL 等基准测试中表现优异，展现了极强的泛化能力和实用价值。
motivation: 传统的词法检索算法主要依赖人工直觉和参数微调，限制了更高效排名机制的自动化发现。
method: 提出 RankEvolve 框架，利用 LLM 引导的进化搜索对表示为代码的排名算法进行迭代变异、重组和性能筛选。
result: 演化出的新算法在 BEIR、BRIGHT 和 TREC DL 等多个基准测试中均表现出优于传统算法的性能和良好的迁移性。
conclusion: 研究证明了由评估器引导的 LLM 程序进化是自动发现新型高效排名算法的一种切实可行的路径。
---

## 摘要
诸如 BM25 和带有狄利克雷平滑（Dirichlet smoothing）的查询似然（query likelihood）等检索算法仍然是强大且高效的第一阶段排序器，但其改进主要依赖于参数调优和人类直觉。我们研究了在评估器和进化搜索的指导下，大语言模型是否能够自动发现改进的词法检索算法。我们引入了 RankEvolve，这是一种基于 AlphaEvolve 的程序进化设置，其中候选排序算法被表示为可执行代码，并根据在来自 BEIR 和 BRIGHT 的 12 个信息检索（IR）数据集上的检索性能进行迭代变异、重组和筛选。RankEvolve 从两个种子程序开始：BM25 和带有狄利克雷平滑的查询似然。进化出的算法具有新颖性、有效性，并在完整的 BEIR 和 BRIGHT 基准测试以及 TREC DL 19 和 20 上表现出良好的迁移能力。我们的结果表明，评估器指导的大语言模型程序进化是实现自动发现新颖排序算法的一条切实可行的路径。

## Abstract
Retrieval algorithms like BM25 and query likelihood with Dirichlet smoothing remain strong and efficient first-stage rankers, yet improvements have mostly relied on parameter tuning and human intuition. We investigate whether a large language model, guided by an evaluator and evolutionary search, can automatically discover improved lexical retrieval algorithms. We introduce RankEvolve, a program evolution setup based on AlphaEvolve, in which candidate ranking algorithms are represented as executable code and iteratively mutated, recombined, and selected based on retrieval performance across 12 IR datasets from BEIR and BRIGHT. RankEvolve starts from two seed programs: BM25 and query likelihood with Dirichlet smoothing. The evolved algorithms are novel, effective, and show promising transfer to the full BEIR and BRIGHT benchmarks as well as TREC DL 19 and 20. Our results suggest that evaluator-guided LLM program evolution is a practical path towards automatic discovery of novel ranking algorithms.

---

## 论文详细总结（自动生成）

### RankEvolve: 通过大语言模型驱动的进化自动发现检索算法

#### 1. 核心问题与研究动机
*   **核心问题**：传统的词法检索算法（如 BM25、QL）虽然高效且强大，但其改进长期依赖于人类直觉和繁琐的参数微调。论文探讨的核心问题是：**能否利用大语言模型（LLM）作为优化器，通过程序进化自动发现超越人类设计的词法检索算法？**
*   **研究背景**：尽管深度学习和稠密检索（Dense Retrieval）兴起，词法检索仍是工业界第一阶段排序的主流。现有的自动化方法（如遗传编程）往往缺乏语义理解，而 LLM 具备理解代码逻辑和信息检索理论的能力，为自动化算法发现提供了新路径。

#### 2. 方法论：RankEvolve 框架
RankEvolve 将排名算法表示为 Python 代码，通过 LLM 引导的进化搜索进行迭代优化。
*   **核心思想**：利用 LLM 作为变异算子（Mutation Operator），在评估器的反馈下，对算法代码进行有意义的修改（如引入新的评分项、改变归一化方式）。
*   **关键技术细节**：
    *   **搜索空间定义**：提供两个种子程序（BM25 和 QL-Dir），定义可进化的接口（如文档表示、查询表示、评分函数）。
    *   **种群管理**：结合了 **Island-based Evolution**（多岛屿并行进化以保持多样性）和 **MAP-Elites**（根据代码复杂度和多样性将程序映射到网格中，避免陷入局部最优）。
    *   **变异策略**：LLM 根据当前表现最好的程序、历史尝试记录以及详细的评估指标，通过 SEARCH/REPLACE 格式提出代码修改建议。
    *   **优化目标（Fitness Score）**：定义为 `0.8 × Avg Recall@100 + 0.2 × Avg nDCG@10`，侧重于第一阶段检索所需的召回率。

#### 3. 实验设计
*   **数据集**：共涉及 28 个数据集。
    *   **训练/进化阶段**：使用来自 BEIR 和 BRIGHT 的 12 个代表性数据集。
    *   **测试/泛化阶段**：在 16 个完全未见的数据集上评估，包括 BEIR 剩余子集、BRIGHT 剩余子集以及 TREC DL 2019/2020。
*   **Benchmark 与对比方法**：
    *   **基准算法**：BM25, BM25+, BM25-adpt, QL-Dirichlet, QL-Jelinek-Mercer。
    *   **对比维度**：nDCG@10, Recall@100 以及查询延迟（Latency）。

#### 4. 资源与算力
*   **模型使用**：使用了 **GPT-5.2**（注：原文此处可能指代特定版本的 GPT-4 或后续模型，API 调用）。
*   **进化参数**：BM25 种子运行了 300 步，QL-Dir 种子运行了 200 步。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号、数量或总训练时长，但提到进化过程涉及数百次 API 调用和在 12 个数据集上的反复索引与检索测试。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 28 个数据集上进行了广泛测试，涵盖了从通用 Web 搜索到复杂推理（BRIGHT）的多种场景。
*   **消融实验**：专门设计了关于“种子程序结构自由度”的消融实验（Constrained vs. Composable vs. Freeform），验证了结构自由度对算法发现上限的影响。
*   **充分性评价**：实验设计较为充分且客观。通过划分“已见”和“未见”数据集，有效地验证了算法的泛化能力而非简单的过拟合。

#### 6. 主要结论与发现
*   **性能超越**：进化出的算法（BM25★ 和 QL-Dir★）在多个基准测试中显著优于原始种子算法和已知的人工改进变体。
*   **独立发现**：LLM 自动“重新发现”了许多经典的 IR 概念，如软停用词过滤、词频饱和度调整、点互信息（PMI）建模以及非线性文档长度归一化。
*   **趋同进化**：尽管起点不同，两个进化分支最终都演化出了相似的高层策略（如协调机制和长度惩罚），暗示了高效检索算法的某些基本原则。

#### 7. 优点与亮点
*   **端到端自动化**：实现了从代码生成到性能评估再到逻辑迭代的全自动化闭环。
*   **可解释性**：与黑盒神经网络不同，RankEvolve 生成的是可读的 Python 代码，研究人员可以分析其背后的数学逻辑。
*   **泛化性强**：演化出的算法在未见过的、极具挑战性的推理型数据集（BRIGHT）上表现尤为突出。

#### 8. 不足与局限
*   **计算成本/延迟**：进化出的算法复杂度显著增加。例如，性能最好的 BM25★ 变体比原始 BM25 慢了约 11 倍，因为 LLM 在优化时未被显式要求考虑效率。
*   **代码冗余**：进化过程可能导致代码变得臃肿，缺乏人工设计的简洁美感。
*   **依赖 LLM 素质**：算法发现的上限受限于 LLM 的编程能力和其预训练数据中包含的 IR 知识。
*   **偏差风险**：如果评估数据集存在偏差，进化过程可能会强化这些偏差。

（完）
