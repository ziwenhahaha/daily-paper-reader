---
title: "Time-To-Reach Separation and Safety Filtering for Safe, Fair, and Efficient Multi-Agent Coordination"
title_zh: "安全、公平、高效多智能体协调的到达时间分离与安全过滤"
authors: "Matthew Low, Jasmine Jerry Aloor, Victoria Marie Tuck, Pierluigi Nuzzo, Jason J. Choi"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20625v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体协调与安全过滤
tldr: 提出使用最小到达时间（TTR）作为统一度量，结合TTR分离引导和安全过滤（基于HJ可达性价值函数），在高度拥堵空中走廊场景中实现安全、公平、高效的多智能体协调。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20625v1/fig-001.webp\", \"caption\": \"TABLE II EVALUATION METRICS\", \"page\": 6, \"index\": 1, \"width\": 514, \"height\": 255}, {\"url\": \"assets/figures/arxiv/2605.20625v1/fig-002.webp\", \"caption\": \"TABLE III EVALUATION OF PROPOSED TTR PRIORITIZATION CONTROL SCHEME\", \"page\": 7, \"index\": 2, \"width\": 1052, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2605.20625v1/fig-003.webp\", \"caption\": \"Fig. 3. Trajectories for each combination of reference guidance and safety filters on a representative scenario with eight vehicles. Red indicates safety violations. (a) Guidance without safety filtering: greedy TTR minimization leads to conflicts, while the proposed TTR separation reduces conflicts through temporal spacing. (b) Greedy TTR minimization with safety filtering, which still incurs severe safety violations in this scenario. (c) Proposed TTR separation combined with safety filtering: decentralized and centralized filtering achieve zero safety violations while maintaining the assigned arrival order. The animated trajectories of this scenario are available online at: https://www.youtube.com/watch?v=fPNJK-leRx8\", \"page\": 7, \"index\": 3, \"width\": 957, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.20625v1/fig-004.webp\", \"caption\": \"Fig. 1. Computed minimum reach-avoid time-to-reach (TTR) in colormap (red-blue), for vehicles navigating to the air corridor (blue), and the simulated optimal trajectories (green). The corridor consists of the entry region T (black dashed), and the downstream region D (white background). (a) TTR colormap and optimal trajectories with initial heading θ = 0◦ (eastbound). (b, c) TTR colormap and optimal trajectories with initial heading θ = 60◦, 180◦, respectively. (d, e) TTR colormap and optimal trajectories with initial heading θ = 0◦, under constant wind blowing toward north and southeast, respectively.\", \"page\": 4, \"index\": 4, \"width\": 1052, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2605.20625v1/fig-005.webp\", \"caption\": \"TABLE IV PARAMETERS OF AIR TAXI DYNAMICS & AIR CORRIDOR\", \"page\": 9, \"index\": 5, \"width\": 464, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2605.20625v1/fig-006.webp\", \"caption\": \"Fig. 2. Five vehicles initialized at states with TTR(xi) ≈ 40s and with initial θ = 0◦, v = vnominal. The first row shows the trajectories and the second row shows the TTR values over time (dashed line indicates target TTR values.) (a) Vehicles under TTR optimal control, leading to near-collision (red) near the corridor entry. (b, c) Vehicles under the TTR separation guidance with target separation time ∆T = 10, 20s, respectively. The numbers indicate the assigned priority order. Note that in this scenario, the TTR separation guidance keeps the vehicle safe even without safety filtering.\", \"page\": 5, \"index\": 6, \"width\": 827, \"height\": 440}]"
motivation: AAM运营预计将大幅增加城市空域交通，多辆车汇聚进入同一走廊时协调到达时间并维持安全是挑战性问题；战略调度未考虑车辆动力学和局部拥堵，战术反应式避碰本质短视可能引发多米诺效应。
method: TTR作为统一度量用于优先级分配（先到先服务）、时间分离（均匀分配目标TTR诱导空间分离）、安全过滤（基于HJ可达性价值函数构建CBF-QP安全过滤器），分为战略层和战术层。
result: 集中式过滤器安全违规率0.089%，成功率94.38%；即使不使用安全过滤器，TTR分离引导也比所有使用过滤器的贪婪方法更安全；所有TTR_sep方法维持高公平性（τ_K~0.99）。
conclusion: TTR作为统一度量简化系统设计，结合战略层TTR分离和战术层安全过滤的分层架构是实现安全、公平、高效多智能体协调的有效范式，去中心化和优先级协调模式具有良好可扩展性。
---

## Abstract
Advanced Air Mobility (AAM) operations are expected to significantly increase aerial traffic in urban airspace, requiring autonomous traffic management systems to ensure collision-free operations in highly congested environments. In this paper, we propose a multi-agent coordination framework that uses minimum time-to-reach (TTR) as a unifying metric for priority assignment, temporal separation, and safety filtering. We focus on the problem of coordinating multiple aerial vehicles merging into an air corridor while maintaining safe separation between vehicles. Vehicles are assigned arrival-consistent priority based on TTR, and target TTR values are used to enforce temporal spacing that induces spatial separation. A priority-consistent safety filtering layer based on Hamilton-Jacobi reachability value functions ensures collision avoidance while minimally modifying the reference guidance. Simulation results in a highly congested corridor merging scenario show that the proposed method improves safety, fairness, and efficiency compared to time-optimal guidance and priority-agnostic safety filtering.

---

## 论文详细总结（自动生成）

<think>The user wants a detailed Chinese summary of the given paper, following a specific structure with 8 points. I need to generate a comprehensive summary based on the provided paper content (markdown content). I need to follow the style guidance: use Chinese, hierarchical headings, bullet points, keep it concise but information-rich.

Let me go through each required point:

1. Core problem and overall meaning (research motivation and background).
2. Proposed methodology: core ideas, key technical details, formulas or algorithm flow (in text).
3. Experimental design: datasets/scenarios, benchmarks, compared methods.
4. Resources and compute: GPU model, quantity, training time, etc., if mentioned.
5. Experiment quantity and sufficiency: number of experiments, whether sufficient, objective, fair.
6. Main conclusions and findings.
7. Strengths: highlights in method or experimental design.
8. Limitations and shortcomings: coverage, bias risks, application limits, etc.

I need to output the final line "(完)" after the summary.

Let me analyze the paper:

Title: Time-To-Reach Separation and Safety Filtering for Safe, Fair, and Efficient Multi-Agent Coordination

Authors: Matthew Low, Jasmine Jerry Aloor, Victoria Marie Tuck, Pierluigi Nuzzo, Jason J. Choi

Published: Thu, 21 May 2026 (arXiv:2605.20625v1)

Problem: Multi-agent coordination for AAM (Advanced Air Mobility) vehicles merging into an air corridor, maintaining safe separation. Existing approaches (strategic scheduling, tactical TCAS/ACAS-X) have limitations: strategic scheduling doesn't account for vehicle dynamics and local congestion; reactive safety mechanisms are myopic and can cause domino effects in high-density airspace.

Proposed method: Use minimum time-to-reach (TTR) as a unifying metric for:
1. Priority assignment (arrival-consistent priority based on TTR)
2. Temporal separation (target TTR values to enforce temporal spacing, inducing spatial separation)
3. Safety filtering (priority-consistent safety filtering based on HJ reachability value functions)

Key components:
- Offline computation of HJ reachability value functions: BRAT value function W(t,x) for TTR computation, and pairwise safety value function V(x) for safety filtering.
- Strategic layer: TTR separation guidance - assign target TTR values based on priority order to evenly distribute vehicles in TTR domain.
- Tactical layer: CBF-based safety filtering (decentralized, priority-coordinated, centralized).

Technical details:
- Vehicle dynamics: planar model with position, heading, speed, control inputs (turn rate, acceleration).
- TTR computation from BRAT value function.
- Control law for TTR separation: πsep,i = max/min of ∇Tmin·f depending on whether Tmin <= T_target.
- Safety filtering: CBF-QP, with multiple modes (Dec, Prio, Cent).

Experiments:
- 100 randomly sampled N=8 vehicle scenarios.
- Compare guidance strategies: TTR_min (greedy minimization) vs TTR_sep (proposed separation).
- Compare safety filter modes: None, Decentralized (Dec), Priority-Coordinated (Prio), Centralized (Cent).
- Metrics: νunsafe (safety violation rate), τK (Kendall's tau for fairness), T_last (arrival time of last vehicle), Ppass (success rate), P+_pass (success rate with order maintained), QP time.
- Results: TTR separation combined with safety filtering outperforms baselines. Centralized filter achieves lowest safety violation (0.089%) and highest success rate (93.06%), but highest QP time. Even without safety filtering, TTR separation alone achieves better safety than all baseline methods with safety filters.

Resources:
- GPU: Nvidia RTX 5090, reachability computation completed within 30 minutes.
- CPU: AMD 9800x3D for QP solving.
- OSQP solver used.

Experiment sufficiency:
- 100 scenarios, exclude unsolvable ones (28), results based on 72 scenarios.
- Comprehensive comparison of all combinations of guidance and filtering strategies.
- Representative scenario illustrated in Fig. 3.

Conclusions:
- TTR-based guidance achieves efficient trajectories while enabling preemptive deconfliction.
- TTR separation is an extremely effective mechanism for safe coordination.
- Importance of strategic temporal coordination and aligning priority orders with control mechanisms.

Strengths:
- Novel use of TTR as a unifying metric for priority, temporal spacing, and safety.
- Layered architecture combining strategic guidance and tactical safety filtering.
- Comprehensive evaluation across multiple metrics and configurations.
- Robust to numerical errors in TTR computation (gap < 10s).

Limitations:
- 2D planar model, extension to full 3D dynamics left for future work.
- Focus on corridor merging, in-corridor coordination handled by separate LQR controller.
- QP solve time increases with centralized filtering (28.8ms vs 25.1ms for decentralized).
- Scenarios where all methods fail are excluded from evaluation (feasibility assumption).

Now, I'll write the Chinese summary following the 8-point structure.

Note: The user wants a "detailed Chinese summary" with hierarchical headings and bullet points, concise but complete. I need to include all 8 points and end with "(完)".

Let me structure it:

1. 论文核心问题与整体含义（研究动机和背景）
2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
3. 实验设计：数据集/场景、benchmark、对比方法
4. 资源与算力
5. 实验数量与充分性
6. 论文主要结论与发现
7. 优点
8. 不足与局限

I'll use Markdown with headings (##) and bullet points (*).

Note: I need to ensure the summary is in Chinese and includes all the details from the paper.

Let me draft:

## 1. 论文核心问题与整体含义（研究动机和背景）

* 研究背景：Advanced Air Mobility (AAM) 预计将在城市空域引入大量每日航班（如数万班次），超出当前空管系统的容量。专用空中走廊被提出用于组织密集空域的交通流，但多辆车汇聚进入同一走廊时，协调到达时间并维持安全成为挑战性的多智能体控制问题。
* 核心问题：如何在高度拥堵的环境中协调多辆飞行器安全、无碰撞地合并进入空中走廊，同时兼顾效率与公平性。
* 现有方法局限：战略层面的流量管理/调度方法（如预先分配到达时间或顺序）未考虑车辆动力学和局部暂态拥堵，导致分配的调度可能动态不可行或需要大的安全偏差。战术层面的反应式避碰系统（如TCAS、ACAS-X）本质上是短视的：针对一对车辆的避碰机动可能改变下游车辆的几何构型，触发新冲突，形成"多米诺骨牌效应"。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

* 核心思想：提出使用**最小到达时间（Time-To-Reach, TTR）**作为统一度量，同时实现优先级分配、时间分离和安全过滤。具体而言：
    1. **优先级分配**：基于初始TTR值（车辆到达走廊的最短时间）为车辆分配“先到先服务”的公平优先级。
    2. **时间分离**：通过为目标TTR值分配均匀间隔（如每隔ΔT秒），诱导车辆在时间上均匀到达，从而自然产生空间上的分离，作为战略性安全机制。
    3. **安全过滤**：在战术层，基于Hamilton-Jacobi可达性价值函数构建控制屏障函数（CBF）安全过滤器，在最小化修改参考引导的同时确保防碰撞。
* 关键技术细节：
    * **离线可达性计算**：
        - 计算BRAT（后向到达-避让管）价值函数 \(W(x, t)\) 和对应的TTR价值函数 \(T_{min}(x)\)，用于优先级排序和战略引导控制器设计。
        - 计算成对安全价值函数 \(V(x)\)（基于相对动力学），用于安全过滤器维护分离。
    * **战略层——TTR分离引导**：
        - 最高优先级车辆（索引1）的目标TTR设为当前TTR：\(T^{target}_1(t) := T_{min}(x_1(t))\)。
        - 下游车辆的目标TTR按优先级顺序递增分配：\(T^{target}_i(t) = T^{target}_1(t) + (i-1)\Delta T\)，其中 \(\Delta T\) 为期望的时间间隔。
        - 控制律：车辆调节运动以追踪目标TTR值。当 \(T_{min}(x_i(t)) \leq T^{target}_i(t)\) 时，选择最大化 \(\nabla T_{min} \cdot f(x, u)\) 的控制输入以增大TTR；否则选择最小化该值的输入以减小TTR。
    * **战术层——安全过滤**：
        - 使用CBF最小范数安全过滤器（公式7），将不安全输入投影到安全子空间。
        - 提出多种安全过滤器架构（表1）：**Dec**（去中心化成对CBF-QP）、**Prio**（优先级协调的多阶段CBF-QP）、**Cent**（集中式联合CBF-QP）。
        - Prio模式通过多阶段逻辑（优先级指派、对称、宽松对称）将冲突解决负担分配给低优先级车辆，同时传播已解决的控制输入。

## 3. 实验设计：数据集/场景、benchmark、对比方法

* **场景设置**：
    - 高度拥堵的空中走廊合并场景（模拟AAM飞行器汇聚）。
    - 每场景包含 \(N=8\) 辆异构飞行器，随机初始化位置、航向和速度。
    - 共生成100个随机场景，评估中排除了28个所有方法均无法安全协调的不可行场景，最终基于72个场景计算指标。
* **仿真模型**：
    - 采用简化的2D平面模型（状态：位置 \([p_x, p_y]\)、航向 \(\theta\)、速度 \(v\)；控制：角速率 \(\omega\)、加速度 \(a\)）。
    - 包含风速影响（用于验证TTR在不同风向下的非对称性）。
    - 走廊几何参数（宽度、入口区域、顺流区域）参考行业规范（表IV）。
* **对比方法**：
    - **引导策略**：
        1. **TTR_min**：贪婪地最小化每辆车的TTR（时间最优控制，公式9）。
        2. **TTR_sep**：提出基于TTR分离的引导策略（公式13）。
    - **安全过滤器模式**：
        1. **None**：无安全过滤。
        2. **Dec**：去中心化成对CBF-QP。
        3. **Prio**：优先级协调的多阶段安全过滤。
        4. **Cent**：集中式联合CBF-QP。
    - 总计6种组合（2引导 × 3过滤器模式，其中None用于比较）。
* **评估指标**（表II）：
    - **安全性**：\(\nu_{unsafe}\)（安全违规时间占比）、\(P_{pass}\)（无违规成功率）、\(P^+_{pass}\)（无违规且顺序保持的成功率）。
    - **公平性**：\(\tau_K\)（Kendall's tau，衡量到达顺序与优先级顺序的偏差）。
    - **效率**：\(T_{last}\)（最后车辆到达时间）、\(T_{avg\ delay}^+\)（平均非负延迟）、\(std(T_{delay}/T_{target})\)。
    - **计算效率**：\(QP_{time}\)（安全过滤器每车每步的求解时间）。

## 4. 资源与算力

* **离线可达性计算**：
    - GPU：Nvidia RTX 5090（单卡）。
    - 计算时间：约30分钟完成所有价值函数（\(W(x,t)\) 和 \(V(x)\)）的计算。
* **在线仿真实验**：
    - CPU：AMD 9800x3D（用于QP求解）。
    - QP求解器：OSQP（Operator Splitting Solver for Quadratic Programs）。
    - 各过滤器模式的平均QP求解时间：
        - Dec：25.1 ± 2.7 ms/车/步。
        - Prio：24.3 ± 1.2 ms/车/步。
        - Cent：28.8 ± 3.3 ms/车/步。
* 注：论文未明确说明训练过程（方法为离线计算价值函数，无需在线训练），主要算力消耗在离线可达性计算和在线QP求解。

## 5. 实验数量与充分性

* **实验规模**：
    - 生成并评估了100个随机8车场景。
    - 排除了28个不可行场景（基于可行性假设，即所有车辆在无协调下无法安全到达），最终基于72个场景报告结果。
    - 覆盖了所有引导策略（TTR_min vs TTR_sep）与安全过滤器模式（None、Dec、Prio、Cent）的组合，共6种配置。
* **实验充分性**：
    - 每种配置均在72个场景上运行，提供了统计意义上的结果（均值±标准差）。
    - 提供了代表性场景的轨迹可视化（图3），直观展示不同方法的冲突模式和协调效果。
    - 进行了消融分析：分别评估引导策略（有无TTR分离）和安全过滤器模式（Dec、Prio、Cent）的单独贡献。
* **客观性与公平性**：
    - 所有方法使用相同的仿真参数（车辆动力学、走廊几何、风速等）。
    - 评估指标覆盖安全性、公平性、效率三个维度，避免单一指标导致的偏置。
    - 排除了不可行场景，确保比较基于同等难度的可行任务。

## 6. 论文主要结论与发现

* **TTR分离的有效性**：
    - 即使不使用安全过滤器，TTR分离引导（TTR_sep + None）的安全性（\(\nu_{unsafe}=1.255\%\)）也优于所有使用安全过滤器的TTR贪婪引导方法（TTR_min + Dec/Prio/Cent，\(\nu_{unsafe}\) 在2.564%~4.786%之间）。
    - 这表明战略性时间协调是防止冲突的关键，可减少对反应式安全干预的依赖。
* **安全过滤器的作用**：
    - 在TTR分离引导基础上增加安全过滤器（TTR_sep + Dec/Prio/Cent）进一步提升安全性。
    - **集中式过滤器（Cent）**达到最低安全违规率（0.089%）和最高成功率（\(P_{pass}=94.38\%\)，\(P^+_{pass}=83.33\%\)），但代价是QP求解时间最长。
    - **去中心化（Dec）和优先级协调（Prio）**过滤器在安全性（\(\nu_{unsafe}\)~1.0%）和效率之间取得良好平衡，QP求解时间较短（约25ms），且几乎不影响公平性（\(\tau_K\)~0.99）。
* **公平性维持**：
    - 所有TTR_sep方法均维持高公平性（\(\tau_K\)~0.99），且成功率中顺序保持率（\(P^+_{pass}\)）显著高于TTR_min方法（56.94%~83.33% vs 11.11%~30.56%）。
* **效率影响**：
    - TTR分离引导引入轻度延迟（平均延迟约3.5~5.8秒），但最后车辆到达时间（\(T_{last}\)~90秒）与TTR_min方法相当，表明延迟可控。
* **关键洞察**：
    - TTR作为统一度量能够自然实现“先到先服务”的公平优先级，且该优先级与实际车辆动力学一致。
    - 结合战略层TTR分离和战术层安全过滤的分层架构是实现安全、公平、高效多智能体协调的有效范式。

## 7. 优点

* **创新性统一度量**：
    - 首次将TTR直接用作多智能体协调变量，同时服务于优先级分配、时间分离和安全过滤，简化了系统设计。
* **分层协调架构**：
    - 结合战略层（TTR分离引导）和战术层（CBF安全过滤），兼顾前瞻性防冲突和反应式避碰，优于纯反应式或纯规划式方法。
* **优先级一致性**：
    - 安全过滤器的设计明确考虑了TTR优先级（Prio模式），将冲突解决负担分配给低优先级车辆，减少对高优先级车辆的干扰，从而维持公平性和效率。
* **可扩展性设计**：
    - 去中心化和优先级协调模式仅需邻居状态共享，无需全局信息，降低了通信和计算负担，适合大规模部署。
* **鲁棒性验证**：
    - 实验表明方法对TTR计算误差（小于约10秒）具有鲁棒性，且能应对风速变化导致的非对称TTR分布。
* **全面评估**：
    - 通过6种方法组合、72个场景、多个维度的指标，客观比较了安全、公平、效率和计算效率，提供了丰富的实验证据。

## 8. 不足与局限

* **模型简化**：
    - 采用2D平面模型，仅考虑水平面避碰，未扩展到完整3D动力学。论文指出垂直维度的扩展留待未来工作（假设其他高度层保留用于其他操作或最后手段避碰）。
* **走廊内协调**：
    - 重点关注车辆从外部进入走廊的协调，进入走廊后的协调由单独的LQR控制器和集中式安全过滤器处理，未在论文中详细分析走廊内冲突的彻底解决方案。
* **不可行场景排除**：
    - 100个场景中排除了28个不可行场景（28%），虽然这是合理的实验设计选择，但可能低估了方法在实际可行边界案例上的表现。
* **计算成本**：
    - 离线可达性计算需要约30分钟（尽管使用高性能GPU），且集中式安全过滤器的QP求解时间（~29ms）约为去中心化模式（~25ms）的1.2倍，在极低延迟要求的场景中可能成为瓶颈。
* **缺乏真实数据验证**：
    - 实验基于仿真场景，未使用真实AAM交通数据或硬件在环测试验证方法的实际部署可行性。
* **参数敏感性**：
    - TTR分离的时间间隔 \(\Delta T\) 的选择影响安全性和吞吐量。论文提到最小可行值约为7.1秒（基于分离距离和标称速度），但最优值可能随场景和监管要求变化，未系统研究参数敏感性。
* **假设限制**：
    - 假设初始配置可行（即存在兼容轨迹使所有车辆无碰撞到达），未讨论方法在初始不可行情况下的鲁棒性。
    - 假设车辆间通信可靠，未考虑通信延迟或丢包对优先级协调和过滤的影响。

（完）
</think>

## 1. 论文核心问题与整体含义（研究动机和背景）

* **研究背景**：Advanced Air Mobility (AAM) 预计将在城市空域引入大量每日航班（如数万班次），超出当前空管系统的容量。专用空中走廊被提出用于组织密集空域的交通流，但多辆车汇聚进入同一走廊时，协调到达时间并维持安全成为挑战性的多智能体控制问题。
* **核心问题**：如何在高度拥堵的环境中协调多辆飞行器安全、无碰撞地合并进入空中走廊，同时兼顾效率与公平性。
* **现有方法局限**：
    - **战略层面的流量管理/调度方法**（如预先分配到达时间或顺序）未考虑车辆动力学和局部暂态拥堵，导致分配的调度可能动态不可行或需要大的安全偏差。
    - **战术层面的反应式避碰
