---
title: "ERPPO: Entropy Regularization-based Proximal Policy Optimization"
title_zh: "基于熵正则化的近端策略优化"
authors: "Changha Lee, Gyusang Cho"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13131v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体强化学习算法ERPPO
tldr: ERPPO通过分布时空歧义学习器估计非平稳约束下的目标检测不确定性，并动态调整熵正则化项强度，在高歧义观察下鼓励探索性动作，在低歧义观察下稳定策略更新。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13131v1/fig-001.webp\", \"caption\": \"Figure 4: Qualitative results of the ambiguity mitigation by UAVs control in maritime scenarios using AirSim (Shah et al. 2017) with Unreal Engine (Epic Games). Green text indicates ’High object locational probability’ detections, while red text indicates ’Low object locational probability’ detections, accompanied by localization scores.\", \"page\": 6, \"index\": 1, \"width\": 1052, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2605.13131v1/fig-002.webp\", \"caption\": \"Figure 5: Average reward results of the proposed ERPPO and MAPPO in retraining procedure.\", \"page\": 7, \"index\": 2, \"width\": 500, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2605.13131v1/fig-003.webp\", \"caption\": \"Figure 1: False object detection results generated by (a) YoloOW (Xu et al. 2024) and (b) YoloWorld (Cheng et al. 2024). (c) shows the true case and movement by MAPPO (Yu et al. 2022). Even though applying MAPPO, (d) represents the false case.\", \"page\": 3, \"index\": 3, \"width\": 1052, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2605.13131v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of the proposed framework with distributional spatiotemporal ambiguity (DSA) learner and entropy regularization-based proximal policy optimization (ERPPO).\", \"page\": 4, \"index\": 4, \"width\": 1023, \"height\": 284}, {\"url\": \"assets/figures/arxiv/2605.13131v1/fig-005.webp\", \"caption\": \"Figure 3: Performance comparison of ERPPO with MAPPO, MASAC, DDQN, and QMIX in partially observable maritime dynamic environment\", \"page\": 5, \"index\": 5, \"width\": 1028, \"height\": 345}]"
motivation: MAPPO在多维环境中因非平稳智能体观察无法提取最优策略。
method: ERPPO首先定义多维观测环境下的目标检测歧义，训练分布时空歧义（DSA）学习器估计不确定性，然后通过新型熵正则化项增强PPO，在高歧义时使用L1正则化鼓励探索，低歧义时使用L2正则化稳定更新。
result: 在AirSim海上搜索场景基准上，ERPPO相比MAPPO提升了准确率性能，在高梯度更新中表现更优，有效抑制了视觉不确定条件下的错误检测。
conclusion: ERPPO通过动态熵正则化有效解决了MAPPO在非平稳多智能体环境中的策略优化问题，为时间关键操作中的目标定位提供了更可靠的方案。
---

## 摘要
ERPPO通过分布时空歧义学习器估计非平稳约束下的目标检测不确定性，并动态调整熵正则化项强度，在高歧义观察下鼓励探索性动作，在低歧义观察下稳定策略更新。

## Abstract
Multi-Agent Proximal Policy Optimization (MAPPO) is a variant of the Proximal Policy Optimization (PPO) algorithm, specifically tailored for multi-agent reinforcement learning (MARL). MAPPO optimizes cooperative multi-agent settings by employing a centralized critic with decentralized actors. However, in case of multi-dimensional environment, MAPPO can not extract optimal policy due to non-stationary agent observation. To overcome this problem, we introduce a novel approach, Entropy Regularization-based Proximal Policy Optimization (ERPPO). For the policy optimization, we first define the object detection ambiguity under multi-dimensional observation environment. Distributional Spatiotemporal Ambiguity (DSA) learner is trained to estimate object detection uncertainty in non-stationary constraints. Then, we enhance PPO with a novel Entropy Regularization term. This regularization dynamically adjusts the policy update by applying a stronger (L1) regularization in high-ambiguity observation to encourage significant exploratory actions and a weaker (L2) regularization in low-ambiguity observation to stabilize the proximal policy optimization. This approach is designed to enhance the probability of successful object localization in time-critical operations by reducing detection failures and optimizing search policy. Experiments on a testbed with AirSim-based maritime searching scenarios show that the proposed ERPPO improves accuracy performance. Our proposed method improves higher gradient than MAPPO. Qualitative results confirm that ERPPO effectiveness in terms of suppressing false detection in visually uncertain conditions.

---

## 论文详细总结（自动生成）

# ERPPO: Entropy Regularization-based Proximal Policy Optimization 论文详解

---

## 1. 论文的核心问题与整体含义（研究动机和背景）

### 研究动机

- **MAPPO的局限性**：Multi-Agent Proximal Policy Optimization (MAPPO) 在多维环境中因**非平稳观测（non-stationary observations）**问题，难以准确评估联合状态-动作值，导致策略提取次优。
- **目标检测的不确定性**：在动态海洋环境中（天气、波浪、反射等），目标检测模型（如YoloOW、YoloWorld）仍会产生误检，严重影响搜索救援任务的可靠性。
- **应用场景**：无人机（UAV）搜索救援（MSAR）是典型的时间关键任务，对象定位的准确性至关重要。

### 背景知识

- PPO是on-policy策略梯度方法，在性能和实现简洁性之间取得平衡。
- MAPPO采用"集中训练+分散执行"范式，在训练时使用全局信息，但执行时仅依赖本地观测。
- 现有目标检测方法多聚焦于提升检测精度，缺乏对**不确定性建模**的系统性方法。

---

## 2. 论文提出的方法论

### 2.1 核心思想

论文提出两个关键创新：
1. **Distributional Spatiotemporal Ambiguity (DSA) 学习器**：预测动态环境中的目标检测歧义分布。
2. **Entropy Regularization-based PPO (ERPPO)**：基于熵的正则化机制，动态平衡探索与利用。

### 2.2 关键技术细节

#### (1) 歧义量化

- **定位概率**：目标对象 $o_k$ 在位置 $x_u$ 和时间 $t$ 的定位概率定义为：
$$p(o_k, x_u, t) = p(o_k = f_\theta(Y(x_u, t)))$$
  其中 $f_\theta$ 是目标检测推理函数，$Y$ 是相机图像。

- **Shannon熵量化**：
$$H(o, x, t) = -p(o, x, t) \log p(o, x, t)$$
  - 当概率确定时，熵趋近于0；当概率模糊时，熵趋近于1。

- **歧义掩码**：
$$M(x, t) = \begin{cases} 1 \ (\text{歧义}), & \max_{o \in R} H(o, x, t) \geq \sigma \\ 0 \ (\text{非歧义}), & \max_{o \in R} H(o, x, t) < \sigma \end{cases}$$

#### (2) DSA学习器

- **目标**：学习从位置、时间、天气到歧义的映射 $q_\Theta(x, t, W) \to \hat{M}$
- **损失函数**：
$$\mathcal{L}_\Theta = \|M(x, t, W), q_\Theta(x_u, t, W)\|_2 + \frac{\lambda}{2}|\Theta|^2$$
- **训练数据**：UAV在不同位置、时间、天气条件下拍摄图像，使用YoloWorld检测并标注歧义。

#### (3) 搜索信息量

- **信息梯度价值估计**：
$$I(x_u, p(o, x_u, t, \bar{W})) = \sum_{o_k \in O} \sum_{x_u} \|p(o_k = f_\theta(Y(x_u, t, \bar{W}))) \|^2$$

- **目标值**：
$$V_\phi(t) = I(x_u, p(x_u, t+1, \bar{W})) - I(x_u, p(x_u, t, \bar{W}))$$

#### (4) 熵正则化策略优化

- **策略熵期望**：
$$H(\pi_\theta) = \mathbb{E}_{x_u \sim \pi_\theta}[\mathbb{E}_t[H(o, x_u, t)]]$$

- **ERPPO损失函数**（核心公式）：
$$\mathcal{L}(\theta) = \begin{cases} \mathcal{L}_{\text{CLIP}} + |H(\pi_\theta) - H(\pi_{\theta_k})|, & \text{if } \mathbb{E}[\hat{M}] \geq \sigma \\ \mathcal{L}_{\text{CLIP}} + \frac{1}{2}|H(\pi_\theta) - H(\pi_{\theta_k})|^2, & \text{otherwise} \end{cases}$$

**物理含义**：
- **高歧义时**（$\mathbb{E}[\hat{M}] \geq \sigma$）：使用L1正则化，**强制策略进行更激进的探索**。
- **低歧义时**：使用L2正则化，**保持策略稳定，紧跟最优策略**。

### 2.3 算法流程（Algorithm 1: ERPPO）

```
1. 初始化: 价值函数 V_φ0, 策略 π_θ0
2. for each training iteration do
3.     for each UAV j at each time step t do
4.         估计时空歧义图 M̂
5.         计算搜索信息 I(x_u, p(x_u, t, W̄))
6.         将rollouts存储到replay buffer
7.     end for
8.     更新价值函数 V_φ 和策略模型 π_θ
9.     minimize_φ L(φ) = E_t[V_φ(t) - R_t]
10.    minimize_θ L(θ) = [ERPPO损失函数，如上]
11. end for
12. return 优化后的 V*_φ, π*_θ
```

---

## 3. 实验设计

### 3.1 场景与数据集

| 配置项 | 值 |
|--------|-----|
| **环境** | Gymnasium + AirSim (Unreal Engine) |
| **UAV数量** | 5 |
| **搜索目标** | 2人 + 1船 |
| **搜索区域** | 500×500×500米 |
| **最大高度** | 500米 |
| **最大步数** | 200步/episode |
| **UAV最大速度** | 1 m/step |
| **天气范围** | 雾: [0.0, 0.3], 雨: [0.0, 1.0] |
| **波浪参数** | 波长: [521, 6000], 振幅: [4.0, 80.0] |

- **目标检测模型**：YOLOv8-world（开放词汇检测）
- **DSA地图大小**：200×200×200（位置差2.5米）

### 3.2 Benchmark 对比方法

#### 连续动作空间
| 方法 | 描述 |
|------|------|
| **MAPPO** | 多智能体PPO，共享集中式价值函数 |
| **MASAC** | 多智能体软执行者-批评家，最大熵RL |

#### 离散动作空间
| 方法 | 描述 |
|------|------|
| **DADDQN** | 多智能体双深度Q网络（独立Q学习） |
| **QMIX** | 单调混合网络，分解联合Q值 |

### 3.3 性能指标

- **归一化回报（Normalized Return）**：主要指标，越高越好

---

## 4. 资源与算力

| 硬件配置 | 详情 |
|----------|------|
| **CPU** | 双 Intel Xeon Silver 4110 (2.10 GHz)，共16核32线程 |
| **RAM** | 约128 GB |
| **GPU** | 4块：2×RTX 2080 (8GB) + 2×RTX 2080 Ti (11GB) |
| **CUDA版本** | 12.1 |
| **UAV并行模拟器** | 最多8个并行worker |
| **训练时长** | 200百万时间步 |

> **注**：论文明确给出了硬件配置，但未提供具体的Wall-clock训练时间。

---

## 5. 实验数量与充分性

### 5.1 实验数量

1. **连续动作空间学习曲线**：ERPPO vs MAPPO vs MASAC（200M步）
2. **离散动作空间学习曲线**：ERPPO vs DADDQN vs QMIX（200M步）
3. **定性消融实验（Figure 4）**：目标定位概率变化的可视化案例（多张AirSim截图）
4. **定量消融实验（Figure 5）**：MAPPO vs ERPPO重训练过程（4万次迭代）

### 5.2 实验充分性分析

| 维度 | 评价 |
|------|------|
| **对比方法** | ✅ 覆盖了连续/离散两大类MARL方法，较为全面 |
| **场景多样性** | ⚠️ 仅单一场景（海洋搜索），缺乏跨场景泛化验证 |
| **统计可靠性** | ⚠️ 未明确报告多次运行的均值/方差 |
| **消融实验** | ✅ 有定性和定量两种消融，但数量有限 |
| **公平性** | ✅ 所有方法使用相同超参数（除算法特定参数） |

**总体评价**：实验设计基本合理，但存在以下不足：
- 单一场景可能限制结论的泛化性
- 缺少多次随机种子的统计分析
- 消融实验仅对比了MAPPO，未对比DSA学习器的独立贡献

---

## 6. 论文的主要结论与发现

1. **性能优势**：ERPPO在连续和离散动作空间下均显著优于基线方法：
   - 连续空间：稳定达到0.8+归一化回报，MAPPO波动大，MASAC收敛到0.65
   - 离散空间：稳定达到0.75，DDQN约0.7，QMIX最差且不稳定

2. **歧义缓解有效性**：
   - 定性实验显示ERPPO能正确标注高/低定位概率，减少误检
   - 定量实验显示ERPPO在重训练中保持更高的奖励下界

3. **梯度提升**：ERPPO的学习斜率（0.172）是MAPPO（0.055）的**3.13倍**（原文称2.13倍，疑为笔误）

4. **正则化机制的作用**：
   - 高歧义时强制探索，减少漏检
   - 低歧义时保持稳定，避免策略退化

---

## 7. 优点

### 7.1 方法创新

1. **自适应正则化**：首次将熵正则化与歧义估计结合，实现L1/L2动态切换，平衡探索与利用
2. **DSA学习器**：提供环境感知的归纳偏置，使RL智能体从一开始就具备更智能的搜索策略
3. **跨领域融合**：有效整合目标检测（CV）与强化学习（RL），形成闭环优化

### 7.2 实验设计

1. **真实仿真环境**：使用AirSim+Unreal Engine，视觉效果逼真
2. **多动作空间覆盖**：同时验证连续和离散两类方法
3. **定性+定量结合**：既有学习曲线对比，又有可视化案例

### 7.3 应用价值

- 直接针对搜索救援场景设计，具有明确的实用价值
- 可扩展至其他时间关键的多智能体任务（如无人机编队、协同巡逻）

---

## 8. 不足与局限

### 8.1 实验覆盖

1. **单场景验证**：仅在海洋搜索场景中验证，缺少跨场景泛化（如室内、森林、城市）
2. **无真实世界数据**：完全依赖仿真，未在真实无人机平台上测试
3. **统计不完整**：未报告多次运行的均值和标准差，难以评估稳定性

### 8.2 方法局限

1. **DSA学习器的泛化性**：
   - 训练依赖于仿真环境，真实世界可能分布偏移
   - 需要额外的数据采集和标注

2. **超参数敏感性**：
   - 歧义阈值σ的选择缺乏系统性分析
   - L1/L2正则化强度的固定值未充分讨论

3. **计算开销**：
   - DSA学习器需要额外的前向传播，增加推理延迟
   - 熵估计可能引入额外的计算成本

### 8.3 潜在偏差风险

1. **环境假设**：假设海洋动态环境主要由天气、UAV位置、波浪、反射四因素影响，可能忽略其他因素（如光照、遮挡）
2. **目标检测器依赖**：最终性能受限于YoloWorld的检测质量
3. **协同效应未验证**：多UAV间的协同策略未被单独分析

---

## 总结

ERPPO通过引入**DSA学习器**和**自适应熵正则化**，有效解决了MAPPO在非平稳环境中的策略退化问题。实验在AirSim海洋搜索场景中验证了方法的有效性，但实验场景单一、统计不完整等不足限制了其结论的泛化性。未来工作可探索跨场景验证、真实平台部署，以及DSA学习器的在线更新机制。

---

（完）
