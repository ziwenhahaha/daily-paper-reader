Title: Second-Order Actor-Critic Methods for Discounted MDPs via Policy Hessian Decomposition

URL Source: https://arxiv.org/pdf/2605.14982v1

Published Time: Fri, 15 May 2026 01:21:15 GMT

Number of Pages: 19

Markdown Content:
# Second-Order Actor-Critic Methods for Discounted MDPs via Policy Hessian Decomposition 

Sanjeev Manivannan 

Department of Biotechnology Indian Institute of Technology Madras 

Shuban Venkatraman 

Department of Biotechnology Indian Institute of Technology Madras 

# Abstract 

We address the discounted reward setting in reinforcement learning (RL). To mitigate the value approximation challenges in policy gradient methods, actor-critic approaches have been developed and are known to converge to stationary points under suitable assumptions. However, these methods rely on first-order updates. In contrast, second-order optimization provides principled curvature-aware updates that are proven to accelerate convergence, but its application in RL is limited by the computational complexity of Hessian estimation. In this work, we analyze second-order approximations for the actor update that leverage the full curvature information of the objective as much as possible. A stable approximation requires treating the action-value function as locally constant with respect to policy parameters, which does not generally hold in policy gradient methods. We show that this approximation becomes well-justified under a two-timescale actor-critic framework, where the critic evolves on a faster timescale and can be treated as quasi-stationary during actor updates. Building on this insight, we formulate a second-order actor-critic method for the discounted reward setting that leverages Hessian-vector product (HVP) computations, resulting in a computationally efficient and stable second-order update. 

# 1 Introduction 

Markov Decision Processes (MDPs) provide a framework for sequential decision making under uncer-tainty, where an agent interacts with an environment defined by states, actions, transition dynamics, and rewards. When the transition dynamics are known, dynamic programming methods such as value iteration Bellman [1957] and policy iteration Howard [1960] compute optimal policies via recursive Bellman equations over value functions. These formulations have been successfully applied to a wide range of control problems, including traffic signal control Wiering [2000], robotics Kober et al. [2013], autonomous navigation Paden and et al. [2016], and nonlinear dynamical systems Bertsekas and Tsitsiklis [1996]. However, in real world scenarios, the transition dynamics are unknown and therefore one has to resort to RL methods that learn optimal behavior through sampled interactions using approaches such as Monte-Carlo methods Sutton and Barto [2018], (TD( λ)) learning Sutton [1988], and Q-learning Watkins and Dayan [1992]. Even though these methods converge to stationary points under suitable conditions, they rely on tabular representations of state or state-action value functions which scale with the size of the state and action spaces. Consequently, in large-scale settings, these representations become intractable due to the curse of dimensionality Bellman [1957]. This has led to the development of policy parameterization Sutton et al. [2000] and modern policy search methods, where the policy is represented as a parametric function πθ (a | s), often defined over feature representations ϕ(s) or learned function approximators parameterized as θ. There are two major challenges that are being addressed in policy search methods. 

> Preprint.
> arXiv:2605.14982v1 [cs.LG] 14 May 2026

The first challenge in policy search methods is value estimation. Standard policy gradient approaches often rely on Monte Carlo returns Sutton and Barto [2018], which provide high variance estimates of the return Sutton [1988] using complete trajectories and therefore require waiting until the end of an episode before performing updates based on stochastic approximation Borkar [1997]. To address this limitation, actor-critic methods Konda and Tsitsiklis [2000] were introduced where a parametric value function (critic) is learned alongside the policy to provide relatively low variance estimates of the return in every state. In particular, two-timescale actor-critic approaches Bhatnagar et al. [2009], where the critic evolves on a faster timescale than the actor, provide a stable framework in which the value function can be treated as approximately stationary during policy updates. The second challenge lies in incorporating curvature information during optimization. Standard policy gradient methods such as REINFORCE Sutton et al. [2000] rely on first-order updates that are not affine-invariant, i.e., their performance depends on the parameterization of the policy and does not account for the underlying geometry of the objective. As a result, these methods ignore curvature information, which can lead to slow convergence. In contrast, second-order optimization offers principled curvature-aware updates that are invariant to affine transformations of the parameter space and can significantly improve convergence efficiency Kakade [2001], Schulman et al. [2015]. However, its application in reinforcement learning is limited due to the computational complexity (O(d3)) of Hessian estimation as well as the instability induced by the high-variance interaction terms in the policy Hessian decomposition Prashanth and Bhatnagar [2018]. This motivates the need for structured approximations Furmston et al. [2016] of the Hessian that mitigate the effect of the high variance cross terms and the curvature components that impede convergence of the objective, while preserving useful curvature information for efficient optimization. 

Our work: Prior work typically addresses these challenges independently. In this work, we establish a connection between them: 

• Two-timescale insight: Second-order approximations are generally unstable due to high-variance curvature components and can lead to loss of useful curvature information. Stabilizing these ap-proximations requires: (i) low-variance state-action value estimates, which are supported by the rapidly evolving critic in the two-timescale actor-critic framework, and (ii) treating the action-value function as locally constant with respect to the policy parameters during actor updates. Although this assumption does not generally hold in policy gradient methods, it becomes more valid in the two-timescale actor-critic framework Bhatnagar et al. [2009], where the critic evolves on a faster timescale than the actor and can be treated as quasi-stationary Konda and Tsitsiklis [2000]. 

• Efficient Second-order updates: Building on this insight, we analyze second-order approximations Furmston et al. [2016] that efficiently capture the curvature structure of the objective, and show that they can be implemented in the two-timescale actor-critic setting using Hessian-vector products (HVP). This results in computationally efficient and stable second-order updates in discounted MDPs. In this paper, we further demonstrate our claims across a range of benchmark environments, ranging from discrete control tasks such as CartPole and LunarLander Brockman et al. [2016], to continuous control domains from MuJoCo environments including Reacher and Humanoid Todorov et al. [2012]. 

# 2 Mathematical Preliminaries 

In this section, we introduce Markov decision processes and the standard terminology used throughout the paper. We further discuss policy search methods Sutton et al. [2000], policy Hessian decomposi-tion Prashanth and Bhatnagar [2018], and actor-critic methodsBhatnagar et al. [2009]. 

2.1 Markov Decision Processes (MDPs) 

In a Markov decision process an agent (or controller) sequentially interacts with an environment by selecting actions based on the the current state of the environment. The system then transitions to a new state, often in a stochastic manner, and the agent receives a scalar reward that depends on the selected action and the resulting state. In our setting, the objective is to maximize the expected total discounted reward. Formally an MDP is defined using a tuple (S, A, P, R, γ, p i), where S and A

denote the action and state spaces respectively, R(s, a ) represents the reward function, P (s′ | s, a )

defines the transition dynamics, γ ∈ [0 , 1) is the discount factor and pi(so) represents the initial state distribution. 2We describe the evolution of an MDP as follows. At time step t, the agent observes state st ∼ pt,selects an action at ∼ π(· | st), transitions to the next state st+1 ∼ P (· | st, a t), and receives a scalar reward determined by R. We consider a finite horizon discounted setting. We represent the states and actions until the termination at H using a trajectory τ = ( s0, a 0, s 1, a 1, . . . , s H−1, a H−1, s H ) where 

H is the trajectory or episode length. The probability of trajectory τ following policy π is given by: 

p(τ | π) = 

 H−1Y

> t=0

P (st+1 | st, a t)π(at | st)



pi(s0) (1) We denote the total discounted reward of a trajectory τ using D(τ ) = PH−1 

> t=0

γtR(st, a t) where 

γ ∈ [0 , 1) . Our objective is to maximize the expected cumulative discounted reward under a policy π:

J(π) = Eτ ∼p(τ |π)

D(τ ) = Est,a t∼pt

 H−1X

> t=0

γtR(st, a t) (2) The objective J(π) represents the expected return starting from the initial state distribution. To characterize returns at the state and state–action level, we define the value functions as follows. The state-value function is defined as 

V π (s) = Eτ ∼p(τ |π)

"H−1X

> t=0

γtR(st, a t) | st = s

#

, (3) which represents expected return starting from state s. The state–action value function is defined as 

Qπ (s, a ) = Eτ ∼p(τ |s0=s,a 0=a,π )

"H−1X

> t=0

γtR(st, a t)

#

, (4) which represents the expected return starting from state s and taking action a. The advantage function measures the relative value of an action compared to the average value of a state, and is defined as 

Aπ (s, a ) = Qπ (s, a ) − V π (s), (5) where V π (s) = Ea∼π(·| s)

Qπ (s, a ). In practice, we estimate advantages using TD(0)-based advantage estimate, which provides bias–variance trade-off for policy gradient updates. 

2.2 Policy Search Methods 

In large-scale settings, classical MDP formulations suffor from the curse of dimensionality due to exponential growth of the state–action space. To address this, modern policy search methods assume policy parameterization by a vector θ ∈ Rd. In this paper, we use the notation πθ as a shorthand for the distribution π(at | st; θ), p(τ ; θ) ≡ p(τ ; πθ ) for the trajectory distribution and J(θ) ≡ J(π) for the expected cost. For simplicity, we assume terminal cost is 0. Our aim is to find a parameter 

θ∗ ∈ arg min 

> θ∈Rd

J(θ). (6) We use the discounted occupancy measure Furmston et al. [2016] induced by a policy πθ as 

ργ (s, a ; θ) = 

> H−1

X

> t=0

γt Pr( st = s, a t = a | πθ ). (7) where, P 

> s,a ∈S×A

ργ (s, a ; θ) = (1 − γ)−1. Additionally, the total discounted reward function can be written as J(θ) = P 

> s,a ∈S×A

ργ (s, a ; θ)R(s, a ). The gradient ∇J(θ) can be written as 

∇J(θ) = 

> H−1

X

> t=0

X

> τ

γt−1R(st, a t) ∇p(τ ; θ). (8) One of the most widely used approaches in policy search follow the principles of stochastic approxi-mation Borkar [1997] commonly referred to as policy gradient methods. A canonical instance is the REINFORCE algorithm Sutton et al. [2000]. These methods rely on first-order updates given by: 

θk+1 = θk + α∇J(θk) (9) 3However, first order updates generally ignore curvature information and are often poorly scaled, leading to update directions that are sensitive to the policy parameterization. This motivates the use of preconditioning, where the gradient is rescaled before updating to account for the local geometry of the objective. In this context, stochastic Newton methods Prashanth and Bhatnagar [2018] introduce a preconditioning matrix M (θ) in (9) as given below: 

θk+1 = θk + αM (θk)∇J(θk) (10) A widely used choice of preconditioning matrix in policy optimization is given by the Natural Policy Gradient (NPG), where the preconditioning metric M (θ) is taken as the inverse of the Fisher Information matrix of the policy distribution. Specifically, the Fisher matrix is given by 

G(θ) = Est,a t∼pt

∇θ log πθ (at | st) ∇θ log πθ (at | st)⊤ , (11) and the corresponding preconditioner is M (θ) = G(θ)−1. The use of the Fisher Information Matrix can be interpreted as imposing a local norm on the parameter space, derived from a second-order approximation of the KL-divergence between trajectory distributions Coolen et al. [2005], Furmston et al. [2016]. While the Fisher information matrix captures curvature arising from the policy distribution, it does not represent the true Hessian of the objective J(θ) and therefore misses important second-order interactions in the expected return. To fully characterize the curvature of the objective, we consider the policy Hessian theorem, obtained by taking the second-order derivative of the REINFORCE objective Maniyar et al. [2024], Furmston et al. [2016]. 

2.3 Policy Hessian Decomposition 

To fully characterize the curvature of the policy optimization objective, we consider the policy Hessian theorem Baxter and Bartlett [2001], Kakade [2001], Maniyar et al. [2024], Furmston et al. [2016], obtained by differentiating the policy gradient objective a second time. 

Theorem 1 (Policy Hessian Theorem) . Consider a Markov decision process with objective J(θ) and induced trajectory distribution p(τ ; θ). For any given parameter vector θ ∈ Rd, the Hessian of the objective can be decomposed as: 

H(θ) = ∇2J(θ) = H1(θ) + H2(θ) + H12 (θ) + H⊤

> 12

(θ). (12) in which the components H1, H2 and H12 are given by: 

H1(θ) = Eργ

Q(st, a t; θ) ∇θ log πθ (at | st) ∇θ log πθ (at | st)⊤ , (13) 

H2(θ) = Eργ

Q(st, a t; θ) ∇2 

> θ

log πθ (at | st) , (14) 

H12 (θ) = Eργ

∇θ log πθ (at | st) ∇⊤ 

> θ

Q(st, a t; θ) . (15) where Eργ [·] = P 

> s,a ∈S×A

ργ (s, a ; θ)( ·). Here, H1 corresponds to the outer-product term, H2

captures the intrinsic curvature of log-policy, and H12 represents interaction terms involving gradient of state-action value function. Each term is weighted by the state–action value Q(st, a t; θ).

2.4 Actor–Critic Methods 

In the policy gradient methods especially in a monte carlo setup, the gradients are estimated using sampled trajectories, which leads to high variance due to the dependence on a long-horizon. A central challenge in policy optimization is therefore the estimation of the action–value function Qπ (s, a ).Actor–critic methods address this issue by introducing a separate function parameterization, known as the critic , to approximate the action–value function. The policy, referred to as the actor , is parameterized by πθ (a | s) and is updated using gradients of the form 

∇J(θ) = E [Qc(s, a ) ∇θ log πθ (a | s)] , (16) where Qc(s, a ) denotes the critic’s value estimate. By replacing Monte Carlo returns with learned value estimates, actor–critic methods significantly reduce variance while retaining the direction. 

Two-timescale framework: A common formulation employs a two-timescale stochastic approximation framework Konda and Tsitsiklis [2000], Borkar [1997], in which the critic parameters are updated on a faster timescale than the actor. Intuitively, the critic rapidly adapts to the current policy, providing accurate value approximation that guide the slower policy updates. This separation of timescales enables stable learning and forms the foundation for many modern policy optimization algorithms. 43 Method 

3.1 Policy Hessian and HVP formulation 

While the second-order updates incorporate curvature information and can accelerate convergence, explicitly computing the Hessian incurs O(d3) complexity. This motivates the use of Hessian–Vector Products Maniyar et al. [2024] which enable efficient computation of curvature without forming the full hessian. In the following, we reformulate the policy Hessian decomposition in terms of HVPs for efficient implementation. For simplicity, we denote the state-action value function Q(s, a ; θ) by Q,and the policy πθ (at | st) by πθ . We further denote the discounted state–action occupancy measure 

ργ (s, a ; θ) using ργ .

Hessian–Vector Product (HVP) formulation: To obtain a second-order update, we con-sider a Taylor approximation of the objective around the current policy parameter θ:

J(θ + ∆) = J(θ) + ∇J(θ)⊤∆ + 12 ∆⊤∇2J(θ)∆ + O(∥∆∥3). (17) Let ˜g be the stochastic estimate of policy gradient g derived as given below: 

g = ∇J(θ) = X

> s,a

ργ (s, a ; θ) Qπ (s, a ; θ) ∇ log πθ (a|s) = Eργ [Qπ ∇ log π] . (18) Differentiating (18) with respect to θ yields the policy hessian H = ∇2J(θ) which admits the decomposition in (12) as described in Section 2.3. Let ˜H denote stochastic estimates of H derived from (12) and let λI be a ridge term (damping) to stabilize the curvature estimate. The update direction is obtained by approximately maximizing the below local quadratic model (because J(θ)

doesn’t have any effect on ∆): 

∆⋆ = arg max 

> ∆



˜g⊤∆ + 12 ∆⊤( ˜H + λI )∆ 



. (19) Due to the computational complexity, we explicitly construct ˜H by computing its action on the current displacement ∆ using Hessian–vector products Maniyar et al. [2024] given by ˜H∆ = 

∇θ

 ˜g⊤∆.Thus, the inner-loop update for ∆ is given by 

∆k+1 = ∆ k + α

h

˜gk + ( ˜Hk + λI )∆ k

i

. (20) After the inner loop, the policy parameters are updated as θ ← θ + ∆ , yielding a stable second-order update that incorporates curvature information. Using the Hessian decomposition, we express each component in vector form. In particular, the outer-product (Fisher-like) term H1 acting on ∆ is given by 

H1 · ∆ = Eργ

Q ∇ log πθ

 ∇ log π⊤ 

> θ

∆ (21) However, for the intrinsic term H2 = Eργ

Q ∇2 

> θ

log πθ

 a direct Hessian-Vector product form computation is subtle because Q = Q(s, a ; θ) also depends on the policy parameters. In an other way, we can represent the policy hessian decomposition in the below form combining the H2 and 

H12 into one term: 

∇2J(θ) = Eργ

Q ∇ log πθ ∇ log π⊤ 

> θ

+ ∇ (Q ∇ log πθ ) (22) By the product rule, the second term in Eq (22) would be 

(H2 + H12 ) · ∆ = ∇(Q ∇ log πθ ), (23) rather than H2 · ∆ alone. This coupling makes it difficult to separately estimate the intrinsic curvature term without incurring the interaction term H12 . The motivation for isolating and approximating H2

is discussed in the next section, where we analyze the limitations of each component in the Hessian decomposition. 53.2 Limitations of Full Hessian Curvature 

Despite O(d3) complexity, the full Hessian also exhibits structural issues that hinder performance: • High-variance interaction term: The cross term H12 captures the sensitivity of state-action value function to policy parameters. Since Q depends on full trajectories, its gradient ∇Q

introduces significant variance, making the curvature estimate unstable and difficult to compute reliably. • Positive definite bias H1: The outer-product term H1 contributes a positive semi-definite component (under non-negative weighting), which introduces convex structure into the Hessian. This conflicts with the concave structure required for stable maximization, leading to distorted curvature estimates and suboptimal directions. Together, these effects make the full Hessian indefinite and poorly conditioned, making direct second order approximation unstable in practice. These challenges motivate the need for structured approximations Furmston et al. [2016] which we address in the following sections. These structural issues of second-order curvature in policy optimization have been noted in prior work. 

3.3 Two-Timescale Approximation and Actor-Critic Structure 

To stabilize the Hessian, we address the high-variance interaction term H12 :• Variance limitation: As mentioned in Section 3.2, in policy optimization, the state-action value function Q depends on the policy parameters, i.e., ∇Q̸ = 0 , making H12 difficult to estimate reliably. • HVP coupling: In addition to its high variance, H12 also complicates the computation of HVP. Differentiating the product Q∇ log π shown in (23) introduces the cross term 

(∇Q)( ∇ log π) = H12 . Since ∇Q depends on long-horizon trajectory distributions. There-fore, computing (H2 + H12 ) · ∆ without HVPs becomes computationally expensive and statistically unstable, preventing a clean isolation of the intrinsic curvature term H2.• Two-timescale insight: Under the two-timescale actor-critic framework Konda and Tsitsik-lis [2000], Borkar [1997], the action-value function Q is estimated using a separate critic neural network parameterized by ω. The critic estimate ( Qc) evolves on a faster timescale than the actor, allowing it to rapidly adapt to the current policy while the actor evolves gradually. Consequently, during actor updates, the critic can be treated as approximately stationary with respect to the actor parameters, i.e., locally quasi-stationary or almost con-verged Konda and Tsitsiklis [2000]. This motivates the approximation ∇θ Qc(s, a ; ω) ≈ 0,yielding H12 ≈ 0 and simplifying the second term in (22) (in HVP form (23) ) to H2 · ∆. A detailed justification of the approximation H12 ≈ 0 is provided in Appendix B. 

3.4 Second-Order Approximations 

As mentioned in Sec 3.3. the full Hessian contains a high variance interaction term H12 , which is difficult to estimate reliably and complicates Hessian vector product (HVP) computation. Therefore, we approximate the Hessian using the two-timescale actor-critic method , resulting in a interaction free Hessian estimate given by ˜H = H1 + H2. We now consider standard approximations of the ˜H:

3.4.1 ACGN1: Advantage-based Gauss–Newton approximation. 

We know that Q(s, a ; θ) = A(s, a ; θ) + V (s; θ) from Eq (5) . For simplicity we represent 

Q(s, a ; θ), V (s; θ) and A(s, a ; θ) as Q, V and A respectively. Under standard Fisher regularity conditions V1 = −V 2 Furmston et al. [2016] which implies the curvature terms defined using state-action function Q can be expressed in terms of the advantage function A. In particular, the interaction free Hessian satisfies 

H1 + H2 = A1 + A2 (24) where H, V and A denote the curvature decompositions derived by using the value functions Q, V 

and A respectively. In Eq (24) , A1 and A2 denote the corresponding curvature terms ( H1 and H2)defined using advantages. This leads to the advantage-based Gauss–Newton approximation 

˜HACGN1 = A1 + A2, (25) 6which incorporates both outer-product ( A1) and intrinsic curvature ( A2) components, while benefiting from variance reduction through advantage estimation. Therefore based on this approximation, the corresponding Hessian-Vector product would be the following: 

˜H · ∆ = ˜HACGN1 · ∆ = ( A1 + A2) · ∆ (26) Additionally using advantage-based Hessian estimates reduces the scale of variance in the curvature approximation and the Hessian-vector products. With appropriately chosen small step sizes for the actor updates, this residual variance is less likely to induce instability and instead yields more reliable directions. We emphasize that, as mentioned in Furmston et al. [2016], this condition doers not imply positive definiteness of the resulting curvature matrix HACGN1 or guaranteed alignment with the true Newton direction; rather, it is an empirical observation that induced updates are often well behaved in the vicinity of the local optima. 

3.4.2 ACGN2: Intrinsic curvature approximation 

An alternative approximation to address the structural bias introduced into the curvature by the outer-product term H1 is to discard H1 entirely and retain only the intrinsic curvature component H2.This yields the following intrinsic curvature Gauss-Newton approximation: 

˜H · ∆ = ˜HACGN2 · ∆ = H2 · ∆ (27) Under log-concave policy parameterizations (e.g., softmax or Gaussian policies) Furmston et al. [2016], H2 is negative semi-definite and therefore preserves the concave curvature structure required for stable maximization of the objective. By removing the positive-semidefinite outer product component H1, ACGN2 avoids introducing convex curvature bias into the actor update. The Hessian-Vector product of ACGN2 approximation would be given by: 

˜HACGN2 · ∆ = H2 · ∆ (28) While H2 is theoretically negative semi-definite under idealized conditions, in practice the stochastic estimation of Hessian-vector products and numerical approximations (e.g., conjugate gradient) may yield directions that do not strictly preserve this property. To account for this, we adapt the update direction based on the observed curvature, ensuring stable optimization even when local curvature deviates from the idealized structure. Overall, this formulation leverages the intrinsic geometry of the policy while remaining robust to practical estimation noise, leading to stable and reliable updates. 

# 4 Experimental Setup and Results 

We conduct experiments to evaluate whether second-order methods yield improved convergence relative to first-order and natural gradient baselines across diverse benchmark environments. Our evaluation spans both discrete control tasks, including CartPole and LunarLander Brockman et al. [2016], and continuous control domains from MuJoCo, including Reacher and Humanoid Todorov et al. [2012]. All experiments are conducted over 5 independent random seeds. We evaluate performance in terms of sample efficiency and convergence behavior. For each method, we report the mean return across seeds, along with standard deviation. 

4.1 Environment with Discrete action-spaces 

We evaluate the second order two-timescale actor critic methods using environments with discrete action spaces agent selects from a finite set A of predefined control actions (discrete) rather than continuous-valued forces, in each step of the episode. First, we perform experiments on the Cartpole environment provided by the OpenAI gym Brockman et al. [2016], where the observation space is defined as S ⊂ R4, consisting of the state vector 

(x, ˙x, ω, ˙ω). Here, x denotes the cart position, ˙x is the cart velocity, ω the pole angle, and ˙ω denotes the pole angular velocity. These variables are bounded according to the environment specifications. The initial state at the beginning of each episode is sampled independently from the uniform interval 

(−0.05 , 0.05) , with randomness controlled via different seeds across runs. The action space is discrete given by A = {0, 1}, where the actions correspond to applying a fixed magnitude force to push the cart either left or to the right. 7The reward function assigns a value of +1 at every time step until termination. An episode terminates when the pole angle exceeds a threshold i.e |ω| > 12 ◦, or when the cart position exceeds |x| > 2.4.Consequently the cumulative reward is directly proportional to the episode length, since a reward of 

+1 is received at each time step until termination. In our setup, we predefined the maximum episode length as 500 steps, with a maximum achievable reward of 500. 

Figure 1: SOTTAC (Second-Order Two-Timescale Actor-Critic) benchmark results across four RL environments: CartPole and LunarLander (discrete action spaces), and Reacher and Humanoid (continuous action spaces). Methods compared include Vanilla Gradient Ascent (REINFORCE, blue), Natural Gradient Ascent (NGA, green), and the proposed second-order variants ACGN1 (red) and ACGN2 (orange). We also evaluate our methods on the Lunar-Lander environment from OpenAI gym Brockman et al. [2016]. Here, the observation space is defined as S ⊂ R6 × { 0, 1}2, consisting of the state vector 

(x, y, ˙x, ˙y, θ, ω, b 1, b 2), where (b1, b 2) ∈ { 0, 1}2, where (x, y ) denote the lander position, ( ˙ x, ˙y)

the linear velocities, θ, ω represent its angle and angular velocity, and (b1, b 2) are two booleans indicate whether each leg is in contact with the ground or not. The action space is discrete, given by 

A = {0, 1, 2, 3}, where the actions correspond to firing the left orientation engine, main engine, right orientation engines or taking no action. At each time step, a scalar reward is assigned, and the episode return is the sum of the step-wise rewards. The reward encourages stable landing by favoring proximity, low velocity, and upright orientation, while penalizing fuel usage. A bonus of +10 is given per leg contact and a terminal reward of +100 or −100 is assigned for successful landing or crash, respectively. Unlike Cartpole, the LunarLander environment does not admit a fixed maximum reward due to its shaped reward structure. In practice, an environment is generally considered solved when the average return over 100 consecutive episodes exceeds 200. Unlike CartPole, the LunarLander environment does not admit a fixed maximum reward due to its shaped reward structure. In practice, performance is evaluated based on average return, and the environment is considered solved when the mean reward over 100 consecutive episodes exceeds 200. We perform simulation experiments by assuming the linear softmax (Gibbs) policy parameterization which is known to be log-concave, where the Hessian of the log probabilities is known to be negative-semidefinite. The latter parameterization provides the distribution to sample the action as follows: 

π(a | s; θ) = exp( ϕ(s, a )⊤θ)

P 

> b∈A

exp( ϕ(s, b )⊤θ) , (29) 8where ϕ(s, a ) is a one-hot encoded feature vector w.r.t the discrete action a, with the state vector 

s embedded in the corresponding action component, and θ is the trainable parameter vector of dimension dim (S) × |A| .Across Cartpole and Lunar-Lander environments, ACGN2 consistently exhibits faster convergence relative and improved sample efficiency relative to the baseline models. ACG1 also demonstrates improved performance over first-order and natural gradient approaches, although it converges slightly slower compared to ACGN2. This behavior can be attributed to the quality of curvature approximation. ACGN2 relies on the intrinsic curvature component in (12) . which preserves the negative semidefinite induced by log-concave policy. As a result it provides well-aligned second-order updates. In contrast, ACGN1 incorporates additional outer product term as in (24) which introduces a convex component into the concave structure for stable maximization, resulting in slightly slower convergence. Natural gradient methods which rely solely on Fisher-information based updates which are more conservative leading to slower learning. Vanilla gradient ascent (REINFORCE) shows the slowest convergence due to the high-variance updates in absence of curvature information. Overall, these results highlight the importance of accurately capturing the intrinsic curvature of the policy landscape for efficient and stable policy optimization in the discrete action space. 

4.2 Environment with Continuous action-spaces 

In contrast to discrete action settings, continuous environments are characterized by action spaces 

A ⊂ Rd, where the agent selects continuous control inputs rather than choosing from a finite action set. We evaluate on Reacher and Humanoid (MuJoCo environments) Todorov et al. [2012]. The Reacher environment consists of a two-jointed robotic arm operating in a planar 2-D space. The objective is to move the end-effector (tip) of the arm to a target position. The reward is defined as the negative Euclidean distance between the end-effector and the target, along with an additional penalty on the magnitude of control inputs, encouraging precise and energy-efficient movements. The Humanoid environment models a high-dimensional 3-D bipedal robot with articulated limbs, including a torso, legs, and arms. The goal is to learn stable locomotion by maximizing forward velocity while maintaining balance. The reward function primarily incentivizes forward movement, while incorporating penalties for large control inputs and deviations from an upright posture. We use a Gaussian policy parametrized by a multi-layer perceptron (MLP). The policy is defined as 

π(a | s; θ) = N (μθ (s), σ 2 

> θ

(s)) , (30) where both the mean μθ (s) and standard deviation σθ (s) are learned functions of the state. Since the policy is not strictly log-concave, the curvature information provides noisy second-order updates. However, μθ (s) is bounded using tanh activation and σθ (s) is clipped, to maintain numerical stability. As a result, we still observe consistent, albeit moderate improvements over the baselines. In the Reacher environment, ACGN2 demonstrates relatively faster convergence, but with higher variance due to sensitivity to noisy curvature estimates arising from Hessian-vector computations. This variability also affects ACGN1, leading it to converge comparably to Natural Gradient Ascent. In the Humanoid environment, the higher dimensionality of the continuous action space further amplifies the effect of noisy curvature estimates. In this setting, ACGN1 and Natural Gradient exhibit relatively more stable and faster convergence, while ACGN2 shows comparable performance with slightly higher variability, but converges to a relatively higher final return. Nevertheless, all second-order methods consistently outperform REINFORCE, indicating the benefit of incorporating curvature information even under non-concave policies. 

# 5 Conclusion 

We showed that second-order approximations for two-timescale actor-critic methods effectively lever-age curvature information for policy optimization, improving convergence and sample efficiency over first-order and natural gradient baselines across discrete and continuous control tasks. Future work will focus on scalable approximations and extending these curvature approximations to trust-region and proximal policy optimization methods such as TRPO Schulman et al. [2015] and PPO Schulman et al. [2017]. 9References 

Jonathan Baxter and Peter L. Bartlett. Infinite-horizon policy-gradient estimation. Journal of Artificial Intelligence Research , 15:319–350, 2001. Richard Bellman. Dynamic Programming . Princeton University Press, 1957. Dimitri P. Bertsekas and John N. Tsitsiklis. Neuro-Dynamic Programming . Athena Scientific, 1996. Shalabh Bhatnagar, Richard S. Sutton, Mohammad Ghavamzadeh, and Mark Lee. Natural actor-critic algorithms. Automatica , 45(11):2471–2482, 2009. Vivek S. Borkar. Stochastic approximation with two time scales. Systems & Control Letters , 29(5): 291–294, 1997. Greg Brockman, Vicki Cheung, Ludwig Pettersson, Jonas Schneider, John Schulman, Jie Tang, and Wojciech Zaremba. Openai gym, 2016. A. C. C. Coolen, R. Kühn, and P. Sollich. Theory of Neural Information Processing Systems . Oxford University Press, 2005. Thomas Furmston, Guy Lever, and David Barber. Approximate newton methods for policy search in markov decision processes. In Advances in Neural Information Processing Systems (NeurIPS) ,2016. Ronald A. Howard. Dynamic Programming and Markov Processes . MIT Press, 1960. Sham M. Kakade. A natural policy gradient. In Advances in Neural Information Processing Systems (NeurIPS) , 2001. Jens Kober, J. Andrew Bagnell, and Jan Peters. Reinforcement learning in robotics: A survey. The International Journal of Robotics Research , 2013. Vijay R. Konda and John N. Tsitsiklis. Actor-critic algorithms. Advances in Neural Information Processing Systems (NeurIPS) , 2000. Mizhaan P. Maniyar, L. A. Prashanth, Akash Mondal, and Shalabh Bhatnagar. A cubic-regularized policy newton algorithm for reinforcement learning. In Proceedings of the 27th International Conference on Artificial Intelligence and Statistics (AISTATS) , volume 238 of Proceedings of Machine Learning Research , pages 4708–4716, 2024. Brian Paden and et al. A survey of motion planning and control techniques for self-driving urban vehicles. IEEE Transactions on Intelligent Vehicles , 2016. L. A. Prashanth and Shalabh Bhatnagar. Zeroth-order optimization methods for reinforcement learning. Journal of Machine Learning Research (JMLR) , 18(1):1–45, 2018. Walter Rudin. Principles of Mathematical Analysis . McGraw-Hill, 1976. John Schulman, Sergey Levine, Philipp Moritz, Michael I. Jordan, and Pieter Abbeel. Trust region policy optimization. In Proceedings of the 32nd International Conference on Machine Learning (ICML) , pages 1889–1897, 2015. John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. Proximal policy optimization algorithms. arXiv preprint arXiv:1707.06347 , 2017. Richard S. Sutton. Learning to predict by the methods of temporal differences. Machine Learning ,1988. Richard S. Sutton and Andrew G. Barto. Reinforcement Learning: An Introduction . MIT Press, 2nd edition, 2018. Richard S. Sutton, David A. McAllester, Satinder P. Singh, and Yishay Mansour. Policy gradi-ent methods for reinforcement learning with function approximation. In Advances in Neural Information Processing Systems (NeurIPS) , 2000. 10 Emanuel Todorov, Tom Erez, and Yuval Tassa. Mujoco: A physics engine for model-based control. 

IEEE/RSJ International Conference on Intelligent Robots and Systems (IROS) , 2012. Christopher J. C. H. Watkins and Peter Dayan. Q-learning. Machine Learning , 1992. Marco Wiering. Multi-agent reinforcement learning for traffic light control. Machine Learning , 2000. 11 A Derivation of the Policy Hessian Decomposition 

Here, we derive the policy Hessian decomposition used in Eq (12) . We begin with the standard policy gradient expression for the expected return: 

∇J(θ) = E(s,a )∼ργ [Qπ (s, a )∇θ log πθ (a|s)] . (31) Taking the second derivative with respect to θ, we obtain: 

∇2J(θ) = ∇θ Eργ [Qπ (s, a )∇θ log πθ (a|s)] . (32) Using the product rule, we expand: 

∇2J(θ) = Eργ

Qπ (s, a )∇2 

> θ

log πθ (a|s) + Eργ

∇θ Qπ (s, a )∇θ log πθ (a|s)⊤

+Eργ

Qπ (s, a )∇θ log πθ (a|s)∇θ log πθ (a|s)⊤ . (33) Additionally, differentiating the log-policy gradient term yields a symmetric contribution: 

∇2J(θ) = H1(θ) + H2(θ) + H12 (θ) + H12 (θ)⊤, (34) where the components are defined as: 

H1(θ) = Eργ

Qπ (s, a )∇θ log πθ (a|s)∇θ log πθ (a|s)⊤ , (35) 

H2(θ) = Eργ

Qπ (s, a )∇2 

> θ

log πθ (a|s) , (36) 

H12 (θ) = Eργ

∇θ log πθ (a|s)∇θ Qπ (s, a )⊤ . (37) Therefore, given above is the decomposition of the policy Hessian into (i) an outer-product term H1,(ii) an intrinsic curvature term H2, and (iii) interaction terms H12 and H⊤

> 12

.

# B The Two-timescale Insight 

Under the two-timescale actor-critic framework Konda and Tsitsiklis [2000], Borkar [1997], Q is estimated using a separate function network known as the critic. The critic value estimate ( Qc)evolves on a faster timescale than the actor. As a result, during policy (actor) updates the critic can be treated as approximately stationary with respect to the actor parameters, i.e., almost converged (quasi-stationary) Konda and Tsitsiklis [2000]. This motivates the approximation ∇Qc ≈ 0, yielding 

H12 ≈ 0 and simplifying the second term in (22) (in HVP form (23) ) to just H2 · ∆. In order to prove the above insight, we make assumptions regarding the smoothness of the parameterized policy 

πθ and the parameterized critic Qc. Let ω be the parameters of the critic network and let ∥ · ∥ denote the l2−norm for vectors and operator norm for the matrices. 

(A1) (Smooth Objective): J(θ) is L-smooth i.e., 

∥∇ J(θ) − ∇ J(θ′)∥ ≤ L∥θ − θ′∥. (38) 

(A2) (Parameterization regularity): The policy πθ (a | s) is twice continuously differentiable w.r.t 

θ which implies for any choice of the parameter θ, any state-action pair (s,a), there exist a constraint 

0 < C g , C H < ∞ such that 

∥∇ log πθ (a | s)∥ ≤ Cg , and ∥∇ 2log πθ (a | s)∥ ≤ CH ∀ (s, a ) ∈ |S| × |A| (39) 

(A3) (Smooth critic): The critic Q(s, a ; ω) is differentiable in ω and its parameter gradient is uniformly bounded as 

∥∇ ω Qc(s, a ; ω)∥ ≤ Cc ∀ (s, a ) ∈ |S| × |A| (40) 

(A4) (Critic tracking): For each actor parameter θ, let ω⋆(θ) denote the critic parameter that is optimal for the current policy πθ . Under two-timescale actor–critic updates, the almost converged quasi-stationary critic parameter tracks this optimum with error 

∥ω − ω⋆(θ)∥ ≤ ϵc, (41) where ϵc is small when the critic timescale is sufficiently faster than the actor timescale. 12 (A5) (Slow actor variation): During actor update, the critic parameter ω is considered fixed with respect to the policy (actor) parameter θ:

∂ω ∂θ ≈ 0. (42) Based on the above assumptions, we now show that the actor–critic interaction term becomes small under the above assumptions. 

Proposition 1. Under Assumptions A1–A5, the actor–critic interaction term 

HAC  

> 12

(θ) = Eργ

∇θ log πθ (a|s)∇θ Qc(s, a ; ω)⊤ (43) 

satisfies 

∥HAC  

> 12

(θ)∥ ≤ Gπ GQ

∂ω ∂θ . (44) 

In particular, under the two-timescale quasi-stationary approximation ∂ω/∂θ ≈ 0, we obtain 

HAC  

> 12

(θ) ≈ 0

Proof. Using the chain rule, the dependence of the critic on the actor parameters is given by 

∇θ Qc(s, a ; ω) = ∂∂θ Qc(s, a ; ω) = 

 ∂ω ∂θ 

⊤ ∂∂ω Qc(s, a ; ω) = 

 ∂ω ∂θ 

⊤

∇ω Qc(s, a ; ω). (45) Therefore Eq (43) can be written as 

HAC  

> 12

(θ) = Eργ

∇θ log πθ (a|s)

 ∂ω ∂θ 

⊤

∇ω Qc(s, a ; ω)

!⊤ (46) 

= X

> (s,a )

ργ (s, a ; θ)∇θ log πθ (a|s)∇ω Qc(s, a ; ω)⊤ ∂ω ∂θ (47) Taking the operator-norm and applying Cauchy-Swartz inequality Rudin [1976]: 

∥H AC  

> 12

(θ)∥ ≤ X

> (s,a )

ργ (s, a ; θ) ∇θ log πθ (a|s)∇ω Qc(s, a ; ω)⊤ ∂ω ∂θ (48) 

= X

> (s,a )

ργ (s, a ; θ) ∥∇ θ log πθ (a|s)∥ ∇ω Qc(s, a ; ω)⊤ ∂ω ∂θ (49) 

≤ Eργ



Gπ GQ

∂ω ∂θ 



= Gπ GQ

∂ω ∂θ (After normalization w.r.t X

> (s,a )

ργ (s, a ; θ)) .

(50) Under a two-timescale actor–critic update, the critic evolves on a faster timescale and is treated as quasi-stationary during actor updates. So, based on (A4)-(A5) ∂ω ∂θ ≈ 0, which implies HAC  

> 12

(θ) ≈

0.Using Proposition 1, the policy Hessian in the actor–critic setting can be approximated as 

∇2J(θ) = H1(θ) + H2(θ) + H12 (θ) + H12 (θ)⊤ (51) 

≈ H 1(θ) + H2(θ). (52) Thus, under the two-timescale approximation, the interaction-free curvature estimate is 

eHAC (θ) = H1(θ) + H2(θ). (53) This justifies Hessian approximation on the second-order actor–critic update that we have proposed in this paper. The result above does not claim that the true policy-dependent value function Qπ (s, a ; θ)

is independent of θ. Rather, we state that in the two-timescale actor-critic implementation, when the critic is represented by a separate parameter vector ω and is updated on a faster timescale, the critic estimate Qc(s, a ; ω) can be treated as fixed w.r.t θ. Based on Proposition 1, we prove that in the two-timescale actor-critic method, the interaction term involving ∇θ Qc is suppressed enabling a stable HVP computation of the outer product term H1 and intrinsic curvature term H2 without incurring the high-variance interaction term H12 .13 C Curvature Properties of the Policy Hessian 

Once we remove the policy Hessian using the two-timescale insight, we approximate the policy Hessian as ACGN1 and ACGN2 as mentioned in Section ?? and ?? . In this section, we analyze the curvature structure of the policy Hessians and after approximations used in ACGN1 and ACGN2. Specifically, we characterize when the outer product H1 is positive semidefinite and when the intrinsic curvature term H2 is negative semidefinite. 

Hessian decomposition: The policy gradient can be written using the likelihood-ratio identity as mentioned in (18): 

∇θ J(θ) = Es,a ∼πθ [Qπθ (s, a )∇θ log πθ (a|s)] . (54) Differentiating again (ignoring actor–critic interaction terms based on the two-timescale insight) yields the dominant policy curvature components: 

H1 = Es,a 

Qc(s, a )∇θ log πθ (a|s)∇θ log πθ (a|s)⊤ , (55) 

H2 = Es,a 

w(s, a )∇2 

> θ

log πθ (a|s) , (56) 

Positive semidefiniteness of H1: For any vector v ∈ Rd,

v⊤H1v = Es,a 

h

w(s, a )  v⊤∇θ log πθ (a|s)2i

. (57) where w(s, a ) can either be Qc(s, a ) or A(s, a ). Since (v⊤∇θ log πθ )2 ≥ 0, it follows that 

H1 ⪰ 0 if w(s, a ) ≥ 0. (58) Thus, H1 is a weighted outer-product (Fisher-like) term and is positive semidefinite under nonnegative weighting. To ensure non-negative weighting we first use a preprocessing step during optimization in environment with non negative rewards, where we subtract with the minimum possible reward to make the reward positive before optimization. Secondly, for the ACGN1 where we use advantage estimates, we use a small learning rate for convergence as mentioned in Furmston et al. [2016]. Thirdly, we add a ridge term or a damping term to ensure that the outer product term H1 is positive definite. Additionally, we set a screening step, where only the positive definite Hessian estimates are allowed to update in the Newton step. The first 3 steps minimize the probability of the outer product term becoming indefinite, and the last step is to ensure high variance updates caused due to indefinite hessians as the curvature information will be extremely noisy and misleading in indefinite Hessians. 

Log-concavity and curvature of H2. A policy is log-concave in θ if log πθ (a|s) is concave, which implies 

∇2 

> θ

log πθ (a|s) ⪯ 0. (59) Under this condition, 

v⊤H2v = Es,a 

w(s, a ) v⊤∇2 

> θ

log πθ (a|s)v ≤ 0 (60) whenever w(s, a ) ≥ 0. Therefore, H2 ⪯ 0 for log-concave policies with nonnegative weights. 

Examples of log-concave policies. 

• Softmax policy: For linear logits zθ (s, a ) = θ⊤ 

> a

ϕ(s),

log πθ (a|s) = θ⊤ 

> a

ϕ(s) − log X

> a′

exp( θ⊤ 

> a′

ϕ(s)) , (61) where, the log-sum-exp term is convex, and its negative is concave. =⇒ ∇ 2 log πθ ⪯ 0.Since we take log before estimating curvature to be concave or convex, we call it log-concave policy. • Gaussian policy with MLP mean and learned variance: For continuous-control environ-ments, we use a Gaussian policy parameterized by an MLP: 

πθ (a | s) = N  a; μθ (s), σ 2 

> θ

(s) ,

14 where both the mean μθ (s) and the standard deviation σθ (s) are learned functions of the state. In contrast to the fixed-covariance linear Gaussian case, this policy is not guaranteed to be globally log-concave in θ, since both the nonlinear mean network and the learned variance contribute to the curvature of the log-density. Nevertheless, the log-density retains a locally quadratic structure in the action residual a − μθ (s), and the intrinsic curvature term remains informative for local policy improvement. To improve numerical stability, we bound the mean output using a tanh activation and clip the standard deviation to avoid degenerate variances. These stabilizations reduce extreme curvature values and make the damped Hessian/Gauss–Newton estimates more reliable in practice, yielding consistent but moderate gains in continuous control environments. 

# D Convergence Analysis 

We now analyze the convergence behavior of the proposed damped second-order actor update. Assumptions (A1)–(A5) ensure smoothness of the objective, bounded policy and critic derivatives, and the validity of the two-timescale quasi-stationary critic approximation. In addition, for the convergence argument, we require the damped curvature estimate used in the actor update to be uniformly positive definite. 

(A6) Positive-definite damped curvature. Let the damped estimated Hessian used in the actor update be 

bHt = eHt + λI. 

We assume that there exist constants 0 < m < M < ∞ such that 

mI ⪯ bHt ⪯ M I. 

Equivalently, 

1

M I ⪯ bH−1 

> t

⪯ 1

m I. 

This assumption states that, after damping, only well-conditioned positive definite curvature estimates are used for the actor update. 

Hessian approximation error. Let Ht denote the true curvature term and bHt denote the practical damped Gauss–Newton curvature estimate. The error in the curvature estimate is bounded as 

∥ bHt − Ht∥ ≤ ϵH + ϵGN + ϵdamp ,

where ϵH denotes the critic-induced Hessian error, ϵGN denotes the Gauss–Newton approximation error, and ϵdamp denotes the bias introduced by damping. From (A2)–(A5), the critic-tracking and two-timescale approximation imply that the actor–critic interaction term satisfies 

∥H AC  

> 12

(θ)∥ ≤ Gπ GQ

∂ω ∂θ ≈ 0.

Thus, the dominant practical curvature approximation error is controlled by critic tracking, Gauss– Newton approximation, and damping. 

Proposition 2 (Convergence of the damped second-order actor update) . Suppose assumptions (A1)– (A6) hold. Consider the actor update 

θt+1 = θt + ∆ t, ∆t = αt bH−1 

> t

bgt,

where bgt is the estimated actor gradient and bHt is the damped positive definite curvature estimate. If the step size satisfies 

αt < 2m2MM (LM − m2) = 2m2

LM − m2 ,

then the actor objective improves up to critic, curvature, Gauss–Newton, and damping approximation errors. Moreover, if these approximation errors vanish asymptotically, then 

lim inf  

> t→∞

∥∇ J(θt)∥2 = 0 .

15 Proof. By second-order Taylor expansion with an L-smooth remainder, we have 

J(θt+1 ) ≥ J(θt) + bg⊤ 

> t

∆t + 12 ∆⊤ 

> t

bHt∆t − L

2 ∥∆t∥2 − O(ϵC + ϵH + ϵGN + ϵdamp ).

Here, the last term collects the critic-tracking, Hessian-approximation, Gauss–Newton, and damping errors. Using the damped second-order actor update 

∆t = αt bH−1 

> t

bgt,

we obtain 

J(θt+1 ) ≥ J(θt) + αtbg⊤ 

> t

bH−1 

> t

bgt + α2

> t

2 bg⊤ 

> t

bH−1 

> t

bHt bH−1 

> t

bgt − Lα 2

> t

2 bH−1 

> t

bgt

> 2

− O(ϵC + ϵH + ϵGN + ϵdamp )

(62) 

= J(θt) + αtbg⊤ 

> t

bH−1 

> t

bgt + α2

> t

2 bg⊤ 

> t

bH−1 

> t

bgt − Lα 2

> t

2 bH−1 

> t

bgt

> 2

− O(ϵC + ϵH + ϵGN + ϵdamp ).

(63) By Assumption (A6), 

mI ⪯ bHt ⪯ M I, 1

M I ⪯ bH−1 

> t

⪯ 1

m I. 

Therefore, 

bg⊤ 

> t

bH−1 

> t

bgt ≥ 1

M ∥bgt∥2,

and 

bH−1 

> t

bgt

> 2

≤ 1

m2 ∥bgt∥2.

Substituting these bounds gives 

J(θt+1 ) ≥ J(θt) + αt

M ∥bgt∥2 + α2

> t

2M ∥bgt∥2 − Lα 2

> t

2m2 ∥bgt∥2 − O(ϵC + ϵH + ϵGN + ϵdamp ). (64) Hence, 

J(θt+1 ) − J(θt) ≥ αt

 1

M + αt

2M − Lα t

2m2



∥bgt∥2 − O(ϵC + ϵH + ϵGN + ϵdamp ).

For monotonic improvement up to approximation error, we require 

1

M + αt

2M − Lα t

2m2 > 0.

Rearranging, 

1

M > α t

 L

2m2 − 12M



,

which gives 

αt < 1/M  

> L
> 2m2

− 12M

= 2m2

LM − m2 .

Equivalently, if LM ≫ m2, this becomes the simpler sufficient condition 

αt < 2m2

LM .

Therefore, for any step size satisfying 

αt < 2m2

LM − m2 ,

there exists a constant G > 0 such that 

J(θt+1 ) − J(θt) ≥ Gα t∥bgt∥2 − O(ϵC + ϵH + ϵGN + ϵdamp ).

16 Summing from t = 0 to T , we obtain 

G

> T

X

> t=0

αt∥bgt∥2 ≤ J(θ⋆) − J(θ0) + O(ϵC + ϵH + ϵGN + ϵdamp ).

Dividing by G PTt=0 αt, we get 

E ∥∇ J(θt)∥2 ≤ J(θ⋆) − J(θ0)

G PTt=0 αt

+ O(ϵC + ϵH + ϵGN + ϵdamp ).

As T → ∞ , if TX

> t=0

αt → ∞ 

and the approximation errors vanish, then 

lim inf  

> t→∞

∥∇ J(θt)∥2 = 0 .

Thus, the damped second-order actor update converges to a stationary point up to critic, curvature, Gauss–Newton, and damping approximation errors. The above analysis shows that the proposed damped second-order actor update converges to a neighborhood of a first-order stationary point. The size of this neighborhood is governed by the cumulative approximation error terms arising from critic tracking, curvature approximation, Gauss– Newton approximation, and damping bias, i.e., 

O(ϵC + ϵH + ϵGN + ϵdamp ).

Under the step-size condition 

αt < 2m2

LM − m2 ,

and in particular the conservative sufficient bound 

αt < 2m2

LM ,

the update guarantees monotonic improvement up to approximation error. This yields the convergence rate 

min  

> 0≤t≤T

E ∥∇ J(θt)∥2 = O 1

PTt=0 αt

!

+ O(ϵC + ϵH + ϵGN + ϵdamp ).

In the ideal setting where the critic tracks the optimal value function exactly ( ϵC → 0) and the curvature estimate is accurate ( ϵH , ϵ GN , ϵ damp → 0), the method recovers convergence to a true stationary point: 

lim inf  

> t→∞

∥∇ J(θt)∥2 = 0 .

For ACGN2 , under log-concave policy parameterizations (e.g., softmax or Gaussian policies), the intrinsic curvature satisfies eHt ⪯ 0,

which implies that the damped matrix 

− eHt + λI 

is naturally positive definite. Consequently, ACGN2 preserves the concave curvature structure required for stable maximization and admits a well-conditioned second-order ascent direction. In contrast, for ACGN1 , the inclusion of the outer-product term introduces a positive semi-definite component that can conflict with the intrinsic curvature, resulting in an indefinite curvature estimate. Therefore, damping plays a critical role in ensuring that the effective update matrix remains positive definite, thereby guaranteeing a stable ascent direction. Overall, the analysis highlights that while both methods benefit from curvature information, ACGN2 provides a structurally more stable approximation, whereas ACGN1 relies more heavily on damping to control curvature-induced instability. 17 E Implementation Details and Runtime Analysis 

E.1 Hardware Setup and Experimental Protocol 

All experiments were conducted on a CPU-optimized workstation equipped with a multi-core processor and sufficient system memory to support parallel execution. To ensure statistical reliability, each method was evaluated across five independent random seeds: {42, 100, 2026, 777, 1234} We adopt a fully CPU-parallelized evaluation pipeline using Python’s multiprocessing and 

concurrent.futures libraries. Each seed is executed as an independent worker process, enabling near-linear scaling in wall-clock efficiency and eliminating GPU transfer overheads, which non-trivial for low-dimensional control tasks. All experiments enforce deterministic initialization by fixing seeds for PyTorch, NumPy, and Gymnasium environments. 

Figure 2: Wall-clock training time (in seconds) for different optimization methods across four environments: CartPole, LunarLander, Reacher, and Humanoid. Error bars denote standard deviation across five random seeds. Vanilla Gradient Ascent (blue) consistently exhibits the lowest computa-tional cost due to its first-order nature. Natural Gradient (orange) introduces moderate overhead from Fisher matrix computations. The proposed second-order methods ACGN2 (green) and ACGN1 (red) incur higher costs due to curvature estimation and CG-based updates, with ACGN1 being the most expensive owing to its additional outer-product structure. 

E.2 Runtime Performance and Computational Trade-offs 

Second-order methods introduce additional per-update computational cost due to Fisher/HVP com-putation, and curvature estimation with damping. While these methods improve sample efficiency, they require higher wall-clock time per iteration compared to first-order approaches such as Vanilla Gradient Ascent (REINFORCE). The results reveal a clear computational hierarchy over the methods. Vanilla Gradient Ascent remains the best in the computational hierarchy across all environments, reflecting its lightweight first-order updates. Natural Gradient Ascent introduces additional overhead due to Fisher matrix estimation, but remains relatively efficient due to structured approximations. 18 Among our proposed approximation methods, ACGN2 incurs further computational cost arising from the intrinsic curvature estimation and HVP-updates; however this overhead remains supressed due to the absence of outer-product curvature terms. In contrast, ACGN1 is consistently the most computationally expensive, because of the inclusion of both intrinsic and outer-product components which lead to higher variance in curvature estimates and consequently more expensive HVP-based gradient iterations. As shown in Fig 2, the computational gaps become more evident in higher-dimensional environments (Reacher, Humanoid), where the curvature estimation complexity scales with the policy dimensional-ity. This highlights a key trade-off: Second-order methods improve optimization quality at the expense of increased per-update computation .

• ACGN2 provides a favorable balance between computational overhead and curvature quality, offering stable second-order updates at moderate cost due to its intrinsic curvature structure, but may still exhibit sensitivity to noisy curvature estimates in high-dimensional settings. 

• ACGN1 incorporates richer curvature information through both intrinsic and outer-product terms, but this leads to higher computational overhead, increased variance in curvature estimates, and a stronger dependence on damping for stability, limiting its practicality in large-scale or high-dimensional environments. 

Interpretation: Although the proposed methods incur higher computational cost per update, the additional computation is compensated by faster convergence in substantially fewer training episodes, thereby reducing the total number of optimization steps required to reach the optimum. 

E.3 Implementation Framework 

All actor-critic methods in our two-timescale architecture were implemented in Python using: 

• PyTorch : for policy and critic networks, and automatic differentiation. 

• Gymnasium : for standardized RL environments (CartPole, LunarLander, Reacher, Humanoid). The second-order updates (Natural GA, ACGN1, ACGN2) utilize HVPs to compute update directions as mentioned in Maniyar et al. [2024] efficiently without explicit matrix inversion. 19