---
title: Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks
title_zh: 可证明通信高效且隐私保护的联邦图神经网络
authors: "Zhishuai Guo, Wenhan Wu, Chen Chen, Lei Zhang, Olivera Kotevska, Ravi K Madduri"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26243v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 通信高效的联邦GNN框架
tldr: 针对跨机构图数据因隐私限制无法共享，且现有联邦图神经网络（GNN）在处理跨客户端边时面临通信成本高和隐私泄露风险的问题，本文提出了CE-FedGNN框架。该框架通过低频交换聚合节点表征并引入移动平均估计器处理跨客户端依赖，结合度量差分隐私（metric-DP）提供形式化隐私保护。研究证明了其收敛性与极低的通信复杂度，实验表明该方法在保障隐私的同时大幅降低了通信开销并保持了优异性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26243v1/fig-001.webp\", \"caption\": \"Table 3: Statistics of the Datasets\", \"page\": 22, \"index\": 1, \"width\": 572, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2605.26243v1/fig-002.webp\", \"caption\": \"Table 1: Average F1 scores on AML datasets across high-illicit (HI) and low-illicit (LI) ratio settings. Best results in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 185}]"
motivation: 现有联邦图神经网络在处理跨客户端边时，要么牺牲模型精度，要么面临高昂的通信与隐私成本。
method: 提出CE-FedGNN框架，通过低频交换聚合表征、引入移动平均估计器处理依赖，并采用度量差分隐私（metric-DP）机制提供形式化隐私保护。
result: "理论证明算法以O(1/√T)速率收敛且通信复杂度为O(T^{3/4})，实验表明其在反洗钱和引用网络数据集上大幅降低通信开销并保持高精度。"
conclusion: CE-FedGNN成功实现了跨机构图数据的高效、隐私保护与高精度的联邦学习，有效解决了现有方法的通信与隐私瓶颈。
---

## 摘要
图神经网络（GNNs）在关系数据上取得了优异的性能，但由于隐私和政策限制，现实世界中的图通常分布在无法共享原始数据的各个组织中。现有的联邦GNN方法要么忽略跨客户端链接，导致准确率下降；要么需要频繁交换嵌入，从而产生巨大的通信和隐私成本。我们提出了CE-FedGNN，一个通信高效且隐私保护的联邦GNN框架，用于在此类耦合图上进行学习。我们的方法通过不频繁地交换聚合的节点表示，避免了共享原始数据或每轮的嵌入。为了处理跨客户端的依赖性和陈旧性，我们引入了一个移动平均估计器，该估计器持续跟踪节点表示，并使其能够在各轮之间稳定地复用。为了对发布的表示提供严格的隐私保证，我们采用了度量差分隐私（metric-DP）框架，该框架通过在学习到的嵌入空间中的距离来衡量隐私，而不是基于最坏情况下的输入扰动。这使得在标准差分隐私变得过于保守的噪声水平下，仍能提供有意义的保证。我们证明了其以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度为 $O(T^{3/4})$。此外，我们在公共队列威胁模型下，通过Rényi差分隐私组合推导出了 $(\varepsilon,δ)$-metric-DP 保证。在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN实现了优异的性能，同时显著降低了通信开销，并在隐私保护噪声下保持了鲁棒性。

## Abstract
Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

---

## 论文详细总结（自动生成）

以下是对论文《Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks》的详细结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **研究背景**：图神经网络（GNN）在处理关系数据（如社交网络、金融欺诈检测）中表现优异，但现实中的图数据往往分布在不同的机构（如不同的银行）中。由于隐私政策（如GDPR）的限制，这些机构无法将原始图数据集中起来进行训练。
*   **核心痛点**：在联邦学习（FL）环境下训练GNN面临一个两难困境：图结构中存在大量“跨客户端”的边。如果**忽略**这些边，模型会因为丢失全局拓扑结构而导致精度大幅下降；如果**利用**这些边，就需要频繁交换节点嵌入，这会带来极其高昂的通信成本，并增加隐私泄露的风险。
*   **整体含义**：本文旨在打破这一僵局，提出一个既能高效利用跨客户端图结构，又能大幅降低通信频率，并提供严格数学隐私保证的联邦GNN框架。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程
*   **核心思想（CE-FedGNN框架）**：通过**低频交换**聚合的节点表征来代替高频通信，并引入**移动平均估计器**来平滑表征的更新，从而缓解低频通信带来的表征滞后问题。同时，引入**度量差分隐私**机制，在不破坏模型效用的前提下提供严格的隐私保护。
*   **关键技术细节**：
    *   **移动平均估计器**：在本地训练时，节点并不使用当前轮次的瞬时嵌入，而是维护一个历史嵌入的移动平均值。当需要跨客户端边的信息时，直接复用对方最近一次上传的“移动平均嵌入”，从而避免了多跳跨客户端采样。
    *   **跨客户端梯度处理**：当计算涉及跨客户端边的梯度时，客户端仅对本地节点的嵌入进行反向传播，将跨客户端节点的嵌入视为固定输入。通过对边的采样概率进行加权，确保聚合后的梯度是无偏估计。同时，对梯度也采用移动平均机制以降低方差。
    *   **度量差分隐私**：传统的差分隐私针对最坏情况设计，在节点嵌入空间中需要加入破坏性极大的噪声。本文采用 metric-DP，基于嵌入空间的 L2 距离来衡量隐私。通过向需要通信的嵌入、模型参数和梯度中注入高斯噪声，结合 Rényi 差分隐私进行组合分析，在适度噪声下即可提供有意义的 $(\varepsilon, \delta)$-metric-DP 保证。
*   **算法流程**：
    1.  **初始化**：服务器初始化全局模型参数和全局嵌入缓冲区。
    2.  **本地更新**：客户端接收模型和所需的跨客户端嵌入。在本地进行 $K$ 次迭代，每次迭代中：采样小批量数据，通过移动平均规则更新节点嵌入，计算梯度并使用移动平均梯度更新本地模型。
    3.  **通信与聚合**：每 $K$ 步后，客户端将更新后的模型、梯度估计器以及**边界节点的嵌入**发送给服务器。服务器进行平均聚合后下发给所有客户端。

### 3. 实验设计：数据集、场景与对比方法
*   **数据集与场景**：
    *   **反洗钱（AML）场景**：由于真实金融数据不公开，采用了合成的银行间反洗钱基准数据集，包含 Small, Medium, Large 三种规模，以及高非法比例（HI）和低非法比例（LI）两种设定。
    *   **引文网络场景**：使用了四个广泛使用的真实基准数据集：Cora, Citeseer, PubMed, MSAcademic。
*   **对比方法**：
    *   **单客户端（SC）**：各客户端独立训练。
    *   **传统联邦**：FedAvg（仅交换模型参数）。
    *   **联邦GNN基线**：Swift-FedGNN（间歇性使用全局图）、FedGCN（仅在初始化时共享嵌入）、FedGNN-ST（朴素地共享滞后嵌入，无移动平均）、FedSage+（本地生成缺失节点）、FedPUB（学习模型更新的加权聚合）。

### 4. 资源与算力
*   **算力说明**：论文在附录中明确提到了算力配置。实验是在通过 InfiniBand 连接的集群上进行的，**每台机器使用了一块 NVIDIA A100 GPU**。
*   **训练时长**：文中给出了每轮训练的平均运行时间（例如在 HI-Large 数据集上，CE-FedGNN-PNA 每轮耗时约 164.73 秒），但未明确说明总训练的绝对小时数或集群的具体节点数量。

### 5. 实验数量与充分性
*   **实验数量**：实验涵盖了 **2大类场景、10个不同的数据集设定**。除了主实验外，还进行了：
    *   通信间隔 $K$ 的影响分析。
    *   隐私噪声强度与模型效用的权衡实验。
    *   针对移动平均组件的**消融实验**（移除全局嵌入、使用滞后嵌入、移除梯度平均）。
    *   属性推断攻击的实证隐私评估。
    *   通信字节数与运行时间的效率对比。
*   **充分性与客观性**：实验设计非常充分且客观。不仅测试了不同规模和类别不平衡的数据，还从通信效率、隐私保护度、抗攻击能力等多个维度进行了验证。消融实验清晰地证明了各个创新模块的有效性。

### 6. 论文的主要结论与发现
*   **性能优越**：CE-FedGNN 在绝大多数 AML 和引文网络数据集上的 F1 分数显著优于所有基线，特别是在类别极度不平衡（LI-AML）的情况下，其他基线往往失效（F1趋近于0），而本方法仍能保持较高精度。
*   **极高的通信效率**：理论证明了算法以 $O(1/\sqrt{T})$ 速率收敛，且通信复杂度仅为 $O(T^{3/4})$。实验证实，即使将通信间隔扩大到 $K=1024$，模型精度下降也极为有限。
*   **隐私与效用的良好平衡**：在提供严格的 metric-DP 数学保证下，注入适度的高斯噪声对模型精度的负面影响很小，证明了 metric-DP 比传统 DP 更适合于节点嵌入的隐私保护。

### 7. 优点：方法或实验设计上的亮点
*   **巧妙的滞后处理机制**：利用移动平均估计器将“滞后的跨客户端嵌入”变废为宝，既解决了跨客户端依赖，又把通信频率降到了极低。
*   **创新的隐私定义应用**：首次将 metric-DP 引入联邦 GNN 训练，巧妙避开了传统 DP 在处理节点嵌入时“最坏情况敏感度过高”导致噪声过大的缺陷。
*   **理论扎实**：不仅算法有效，还提供了严密的非凸优化收敛性证明和隐私组合定理证明。

### 8. 不足与局限
*   **数据真实性的局限**：由于金融数据的保密性，核心应用场景（反洗钱）的实验完全依赖于合成数据。尽管作者声明合成器很逼真，但合成数据与真实复杂金融网络的分布差异仍可能影响模型在真实场景中的表现。
*   **威胁模型的假设**：隐私分析基于“公共队列威胁模型”，即假设每轮更新的节点集合对 adversary 是公开的。如果引入安全聚合技术，虽然可以增强隐私，但会使当前的隐私理论分析失效（如文中附录C.2所述）。
*   **应用限制**：算法主要针对跨客户端的“一跳”邻居交互进行了优化，对于极度依赖多跳跨客户端交互的复杂图结构，其有效性未得到充分探讨。

（完）
