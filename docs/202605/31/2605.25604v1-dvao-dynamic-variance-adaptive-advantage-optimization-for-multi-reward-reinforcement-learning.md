---
title: "DVAO: Dynamic Variance-adaptive Advantage Optimization for Multi-reward Reinforcement Learning"
authors: "Guochao Jiang, Jingyi Song, Guofeng Quan, Chuzhan Hao, Guohua Liu, Yuewei Zhang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25604v1"
tags: ["query:rl"]
score: 9.0
evidence: 多奖励强化学习算法设计
tldr: 在多奖励强化学习对齐场景中，现有GRPO方法的Reward Combination易产生过大优势幅值导致训练不稳定，而Advantage Combination依赖静态权重难以捕捉目标间动态相关性。DVAO通过分析rollout group内各目标的reward方差自适应调整组合权重，放大高信噪比目标的学习信号并抑制噪声目标。实验表明在Qwen3和Qwen2.5模型的数学推理及工具使用任务中取得显著更优的Pareto前沿和稳定收敛，为多目标RL提供有效的动态自适应优化框架。
source: arxiv
selection_source: fresh_fetch
motivation: GRPO在处理多奖励RL时，传统Reward Combination易产生过大优势幅值导致训练不稳定，而Advantage Combination采用静态权重忽略目标间动态相关性。
method: DVAO根据rollout组内各目标的经验reward方差动态调整组合权重，数学证明了其优势幅值的有界性，实现自适应的跨目标正则化。
result: 在Qwen3/Qwen2.5模型的数学推理和工具使用基准上，DVAO显著超越baseline方法，获得更优的多目标Pareto前沿且训练过程更稳定。
conclusion: DVAO通过方差自适应的优势组合机制，为多奖励RL场景提供了一种稳定且高效的多目标优化解决方案。
---

## Abstract
Reinforcement Learning has become a standard paradigm for aligning Large Language Models with human intent and task requirements. While Group Relative Policy Optimization offers an efficient, value-model-free alternative to Proximal Policy Optimization, adapting it to real-world multi-reward settings remains challenging. Standard scalarization practices, such as Reward Combination and Advantage Combination, suffer from significant drawbacks: Reward Combination frequently generates advantages with excessively large squared magnitudes that lead to training instability, while Advantage Combination relies on static hyperparameters and ignores cross-objective correlations. To address these limitations, we propose Dynamic Variance-adaptive Advantage Optimization (DVAO), which dynamically adjusts combination weights based on the empirical reward variance of each objective within a rollout group, effectively up-weighting objectives with a stronger learning signal while suppressing noisy ones. We mathematically prove that DVAO maintains bounded advantage magnitudes for stable training and introduces a self-adaptive cross-objective regularization mechanism. Extensive experiments on mathematical reasoning and tool-use benchmarks using Qwen3 and Qwen2.5 models demonstrate that DVAO significantly outperforms baseline methods, achieving a superior multi-objective Pareto frontier and robust training stability.

---

## 论文详细总结（自动生成）

# DVAO: 动态方差自适应优势优化 - 论文详细总结

## 1. 论文的核心问题与整体含义

### 研究背景
- 大语言模型（LLM）的强化学习对齐已成为主流范式，GRPO（Group Relative Policy Optimization）因其无需价值模型、内存开销小、训练流程简单而备受关注
- 然而，现实应用很少只优化单一目标，通常涉及多目标需求，如：答案准确性 + 长度约束、代码bug率、幻觉率、工具调用格式等

### 核心问题
现有两种标准标量化方法均存在显著缺陷：

| 方法 | 问题 |
|------|------|
| **Reward Combination（RC）** | 直接组合原始奖励，常产生过大优势幅值（squared magnitude），导致策略梯度不稳定 |
| **Advantage Combination（AC）** | 各目标独立归一化后组合，依赖静态超参数，完全忽略目标间相关性，难以捕捉协同或对抗关系 |

### 研究动机
- 解决多奖励GRPO中**幅值爆炸**（magnitude explosion）和**目标隔离**（objective isolation）问题
- 设计一种既能保证训练稳定性、又能捕捉目标间动态相关性的方法

---

## 2. 论文提出的方法论

### 核心思想
**DVAO（Dynamic Variance-adaptive Advantage Optimization）**：根据rollout组内每个目标的经验奖励方差动态调整组合权重

- **上权**（up-weight）高方差目标 → 更强的学习信号
- **下权**（down-weight）低方差目标 → 抑制噪声

### 关键技术细节

#### 权重动态调整公式
```
~wk = wk × σik / Σl(wl × σil)
```
其中：
- σik = std{r(i,j)^k}（第k个目标在组内的标准差）
- σi^sum = std{r(i,j)^sum}（组合奖励的标准差）

#### DVAO优势计算
```
A(i,j)^DVAO = Σk ~wk × A(i,j)^k
```

### 三个数学命题（核心贡献）

| 命题 | 结论 |
|------|------|
| **命题1** | RC方法的平均平方优势 ≥ AC方法，仅当所有目标完全正相关时相等 |
| **命题2** | DVAO逐点优势幅值 ≤ RC方法，保证训练稳定性 |
| **命题3** | DVAO的梯度敏感性依赖交叉项 A^DVAO × A^k，实现跨目标正则化；AC仅依赖自身孤立性能 |

### 自适应正则化机制
- AC中，第k个目标的梯度贡献由 (A^k)² 调制（孤立）
- DVAO中，梯度贡献由交叉项 A^DVAO × A^k 调制（全局信息聚合）
- 结果：DVAO自动根据整体多目标性能调整各目标学习信号，防止贪婪优化单一目标

---

## 3. 实验设计

### 场景一：数学推理
**数据集**：DAPO-MATH-17K（17k提示词）

**Benchmark**：
- AIME-2024、AIME-2025、MATH500、OlympiadBench、AMC23

**优化目标**：
- 准确性（Accuracy）
- 长度约束（Length ≤ 4000 tokens）

**模型**：
- Qwen3-4B-Base
- Qwen3-8B-Base

### 场景二：工具使用
**数据集**：ToolRL训练集（来自ToolACE 2k + Hammer 1k + xLAM 1k）

**Benchmark**：
- BFCL-v4（Berkeley Function Call Leaderboard）
  - 涵盖：单步推理、多步工具使用、实时执行、无关工具拒绝、同时多工具选择、多工具执行

**优化目标**：
- 工具使用正确性（Accuracy）
- 格式合规性（Format）

**模型**：
- Qwen2.5-3B-Instruct
- Qwen2.5-7B-Instruct

### 对比方法
| 方法 | 说明 |
|------|------|
| **GRPO** | 单奖励基线 |
| **RC（Reward Combination）** | 原始奖励直接线性组合 |
| **AC（Advantage Combination）** | 各目标独立归一化优势后组合 |
| **GDPO** | 组奖励解耦归一化策略优化 |
| **DVAO（ Ours）** | 动态方差自适应优势优化 |

---

## 4. 资源与算力

### 硬件配置
| 资源 | 规格 |
|------|------|
| GPU | 8 × NVIDIA H20-3e |
| CPU | Intel Xeon Platinum 8575C |
| 训练步数 | 500步 |

### 超参数设置
| 参数 | 值 |
|------|-----|
| 优化器 | AdamW |
| 学习率 | 1 × 10⁻⁶（常数） |
| Rollout组大小 G | 16 |
| Prompt批大小 | 128 |
| 最大生成长度 | 8,192 tokens |
| 评估温度 | 0.6 |
| Top-p | 0.95 |

### 训练时长
论文未明确说明具体训练时长（小时/分钟），但明确了500步的训练配置。

---

## 5. 实验数量与充分性

### 实验规模
| 实验类型 | 数量/范围 |
|----------|-----------|
| 数学推理基准 | 5个（AIME-2024, AIME-2025, MATH500, OlympiadBench, AMC23） |
| 工具使用基准 | 3个（Live, Non-Live, Multi-Turn） |
| 模型规模 | 4个（4B, 8B, 3B-Instruct, 7B-Instruct） |
| 权重扫描（Pareto分析） | 5个权重点 {0.1, 0.3, 0.5, 0.7, 0.9} |
| 消融实验 | 训练动态曲线（均值+标准差） |

### 实验充分性评估
| 维度 | 评估 |
|------|------|
| **多场景覆盖** | ✓ 数学推理 + 工具使用两种不同任务 |
| **多模型规模** | ✓ 4B~8B参数，覆盖Base和Instruct变体 |
| **Pareto前沿分析** | ✓ 完整权重扫描，而非单点对比 |
| **消融验证** | ✓ 训练曲线展示收敛性和方差变化 |
| **公平性** | ✓ 所有方法使用相同等权重初始化 |
| **潜在不足** | △ 仅在双目标场景验证，n>2的理论未实验 |

---

## 6. 论文的主要结论与发现

### 核心结论
1. **Pareto最优性**：DVAO是唯一在准确性和长度/格式合规性上同时达到最高分的方法
2. **训练稳定性**：DVAO显著降低奖励方差（std），收敛更平稳
3. **无超参数调优**：动态权重机制完全数据驱动，无需手动调整

### 具体发现

#### 数学推理任务（Qwen3-4B）
| 指标 | DVAO vs 最佳Baseline |
|------|---------------------|
| 平均准确率 | 42.19% vs 39.91%（GRPO） |
| 平均长度合规率 | 99.91% vs 98.71%（RC） |

#### 工具使用任务（Qwen2.5-7B）
| 指标 | DVAO vs 最佳Baseline |
|------|---------------------|
| 平均准确率 | 63.00% vs 60.13%（GDPO） |
| 平均格式合规率 | 79.21% vs 76.42%（RC） |

#### 训练动态观察
- **准确性奖励**：DVAO始终高于基线，方差下降更快
- **长度奖励**：DVAO更快速趋近目标值1.0，方差下降最陡
- **响应长度**：DVAO达到最高最终长度（约1700-1800 tokens），但曲线振荡更明显（反映更积极的探索）

---

## 7. 优点

### 方法层面
1. **理论完备性**：三个数学命题严格证明稳定性、有界性和跨目标正则化机制
2. **自适应机制**：完全数据驱动，无需人工权重调优，避免主观偏差
3. **简单有效**：仅修改优势组合权重计算，改动小、易集成

### 实验层面
4. **多维度对比**：覆盖准确率、长度、格式、方差等多个指标
5. **Pareto分析**：完整展示不同权重下的权衡曲线，证明全局优势
6. **训练曲线可视化**：均值+标准差双视角，直观展示稳定性差异

### 工程层面
7. **基于成熟框架**：基于verl框架实现，代码可复现性强
8. **资源适中**：8×H20 GPU配置对中等规模模型训练合理

---

## 8. 不足与局限

### 实验覆盖局限
| 局限 | 说明 |
|------|------|
| **双目标限制** | 实验仅验证n=2场景，n>2的复杂奖励空间未验证 |
| **单领域扩展** | 仅测试数学推理和工具使用，其他领域（如对话、摘要）未覆盖 |
| **模型规模** | 最大仅8B，未测试34B+等更大模型 |

### 方法局限
| 局限 | 说明 |
|------|------|
| **方差估计依赖组大小** | G=16时效果稳健，但G≤4时估计可能噪声过大 |
| **奖励质量依赖** | 若辅助奖励本身噪声大方差高，DVAO会错误上权 |
| **静态初始化** | 权重仍需初始设定（论文用等权重），动态调整仅在训练中生效 |

### 潜在偏差风险
1. **选择偏差**：所有方法共享相同初始化权重，可能对DVAO有利
2. **评估指标局限**：仅关注准确率和合规率，未涉及生成质量、流畅性等主观指标
3. **任务特异性**：工具使用任务中的格式合规性可能较容易归一化，泛化性待验证

### 未来改进方向
- 引入历史动量或跨批次移动平均稳定方差估计
- 扩展至n>2的多目标场景
- 结合奖励质量评估器过滤低质量辅助奖励

---

（完）
