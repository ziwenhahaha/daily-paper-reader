---
title: "RL2ML: Finite-Rollout Surrogate Objectives from Reinforcement Learning to Maximum Likelihood"
title_zh: RL2ML：有限 rollout 替代目标——从强化学习到最大似然
authors: Yifu Zheng
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30154v1"
tags: ["query:rl"]
score: 9.0
evidence: 直接研究强化学习理论和有限 rollout 代理目标
tldr: 开发 RL2ML 框架，将强化学习与最大似然估计用无偏梯度估计器连接起来。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30154v1/fig-001.webp\", \"caption\": \"\", \"page\": 7, \"index\": 1, \"width\": 2396, \"height\": 994}, {\"url\": \"assets/figures/arxiv/2605.30154v1/fig-002.webp\", \"caption\": \"\", \"page\": 9, \"index\": 2, \"width\": 2358, \"height\": 1634}, {\"url\": \"assets/figures/arxiv/2605.30154v1/fig-003.webp\", \"caption\": \"\", \"page\": 10, \"index\": 3, \"width\": 2234, \"height\": 1432}, {\"url\": \"assets/figures/arxiv/2605.30154v1/fig-004.webp\", \"caption\": \"\", \"page\": 21, \"index\": 4, \"width\": 1546, \"height\": 1014}]"
motivation: 直接研究强化学习理论和有限 rollout 代理目标。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
基于正确性的可验证奖励强化学习（RLVR）通过二元反馈对采样输出训练语言模型，但在期望中优化的目标与有限 rollout 组诱导的随机更新几何经常被混为一谈。本文提出 RL2ML，一类有限 rollout 代理目标，具备闭式、严格无偏的梯度估计器。该族在固定 rollout 预算下保持估计器‑目标对齐的同时，连续地桥接了标准强化学习、类最大似然训练以及超最大似然目标。我们引入组级更新尺度来刻画在观察到经验成功计数后 rollout 组如何被重新赋权，揭示了在仅使用总体目标记号时被隐藏的次临界‑超临界更新尺度相变。在此区分的基础上，经过校准的度量‑增益分析和精确的方差分解表明，最佳的代理目标选择既不取决于与最大似然的接近程度，也不单独取决于总体层面的权重。相反，它共同取决于评估指标、局部敏感性和估计器方差。因此，代理目标族中剩余的自由度可以表述为一个一维优化问题，而不是作为无约束超参数来处理。

## Abstract
Correctness-based Reinforcement Learning with Verifiable Rewards (RLVR) trains language models from binary feedback on sampled outputs, but the objective optimized in expectation and the stochastic update geometry induced by finite rollout groups are often conflated. This paper develops RL2ML, a family of finite-rollout surrogate objectives with a closed-form, exactly unbiased gradient estimator. The family continuously connects standard reinforcement learning, maximum-likelihood-like training, and beyond-maximum-likelihood objectives while preserving estimator-objective alignment under a fixed rollout budget. We introduce the group-level update scale to characterize how a rollout group is reweighted after its empirical success count is observed, revealing a subcritical-supercritical update-scale transition that is hidden by population-level objective notation alone. Building on this distinction, calibrated metric-gain analysis and exact variance decomposition show that the best choice of surrogate objective is determined neither by proximity to maximum likelihood nor by the population-level weight alone. Instead, it depends jointly on the evaluation metric, local sensitivity, and estimator variance. The remaining degree of freedom in the surrogate objective family can therefore be formulated as a one-dimensional optimization problem rather than treated as an unconstrained hyperparameter.

---

## 论文详细总结（自动生成）

## RL2ML：有限 Rollout 替代目标——从强化学习到最大似然

---

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：基于正确性的可验证奖励强化学习（RLVR）通过二元反馈（正确/错误）对语言模型的采样输出进行训练。这类方法使用 rollout 采样和二元验证器奖励，而非直接的似然标签。在期望中优化的目标与有限 rollout 组诱导的随机更新几何经常被混为一谈。
- **核心问题**：传统 RL（如 REINFORCE）、MaxRL（最大似然近似）和最大似然（ML）目标之间的关系并不清晰；在有限 rollout 预算下，到底应该选择哪一个代理目标？论文指出：proximity to maximum likelihood 并不是选择最佳代理目标的充分依据。
- **研究动机**：MaxRL 通过展开最大似然目标得到计算受限的代理目标，在大 rollout 极限下趋近 ML，但这并不意味着它就是有限训练周期下的最优选择。因此需要一类能够连续桥接 RL、ML 和超 ML 目标的有限 rollout 代理目标族，并配套无偏梯度估计器和系统性的目标选择框架。

---

### 2. 论文提出的方法论

#### 2.1 核心思想

- **RL2ML 目标族**：通过幂似然函数 φ_γ(p) = p^(1−γ)/(1−γ) 定义一族代理目标，其梯度为 ∇_θ J_RL2ML^γ(x) = (p_θ(x))^(-γ) ∇_θ p_θ(x)。
  - γ = 0 恢复普通 RL 梯度（权重 = 1）
  - γ = 1 恢复 ML 梯度（权重 = 1/p）
  - 0 < γ < 1 为次临界（subcritical）区：低成功 prompt 仍被衰减但比 RL 轻
  - γ > 1 为超临界（supercritical）区：低成功 prompt 被放大，超过 ML 的权重

#### 2.2 截断 RL2ML 目标（与 rollout 预算 N 对齐）

- 对于 rollout 预算 N，定义截断目标：
  - J_RL2ML^γ,N(x) = Σ_{k=1}^N ((γ)_{k-1} / (k−1)! k) · pass@k(x)
  - 对应总体层面权重：w_RL2ML^γ,N(p) = Σ_{m=0}^{N-1} ((γ)_m / m!) (1−p)^m
- **闭式无偏估计器**：通过 Bernstein 多项式表示理论（Theorem 2.1），任何只依赖成功计数 K 的估计器（即 K-only 估计器）都可以表示为 Bernstein 基的线性组合，其总体权重为 Bernstein 多项式。

#### 2.3 关键系数闭式推导

- 成功计数 K 对应的系数为：
  - β(γ,N)_{K-1} = Γ(N+γ)Γ(N) / (Γ(K)Γ(K+γ))
  - 组级更新尺度 α(γ,N)_K = K/N · β(γ,N)_{K-1} = Γ(N+γ)Γ(N+1) / (Γ(K+1)Γ(K+γ))
- **严格无偏估计器**：ˆg_RL2ML^γ,N(x) = α(γ,N)_K · S̄_K，其中 S̄_K 是 K 个成功 rollouts 的平均得分函数，满足 E[ˆg | x] = ∇_θ J_RL2ML^γ,N(x)。
- 可使用控制变量（score baseline 减 1）来降低方差而不破坏无偏性。

#### 2.4 次临界‑超临界相变（由 γ = 1 界定）

- α(γ,N)_{K+1} / α(γ,N)_K = (K+1)/(K+γ)
- 当 0 ≤ γ < 1 时，α 随 K 增大，在 K < N 时满足 K/N < α_K < 1
- 当 γ = 1 时，α_K ≡ 1（MaxRL 边界）
- 当 γ > 1 时，α 随 K 增大而减小，在 K < N 时满足 α_K > 1

#### 2.5 有限周期下的 γ 选择（一维外层优化）

- **有效学习率校准**：不同 γ 会同时改变系数、组级尺度和总体权重。为公平比较，需要对每个 γ 归一化期望更新方向，使参数更新幅度固定为 c。
- **校准度量增益**：U(γ) = A(γ) / √B(γ)，其中 A(γ) = Σ_x v'_x(p_x) w_γ,N(p_x) ℓ_x（与验证度量的一阶对齐），B(γ) = Σ_x w_γ,N(p_x)^2 ℓ_x（更新方向的平方范数），ℓ_x = ‖∇_θx p_x‖²。
- **方差惩罚**：精确方差分解显示超临界区会放大方差（分为计数方差和组内方差），引入方差项 R(γ)，最终选择准则为：
  - γ*_λ = arg max_{γ∈Γ} [U(γ) − λ_var R(γ)^{1/2}]
- **实现细节**：在 Verl 风格框架中实现只需添加系数表 {β_{K-1}}，通过控制变量构造 advantage，使用 Newton 法或网格搜索求解一维外层优化。

---

### 3. 实验设计

- **数据集 / 场景**：论文以理论分析为主，主要讨论了 pass@k、smoothed log-success 等不同验证指标下的行为，并配有示意图（图2展示校准后的度量增益曲线可能有内部最优点，图3展示方差随 γ 的增长曲线）。
- **Benchmark**：无大规模经验实验 Benchmark；主要通过解析推导和数值图示来说明理论结论（例如图1展示不同 γ 下的总体权重和组级更新尺度，图2展示校准与未校准的选择曲线差异）。
- **对比方法**：理论层面将 RL2ML 与普通 RL（γ=0）、MaxRL（γ=1）和 ML 进行比较。

---

### 4. 资源与算力

- **未明确报告算力**：论文为纯理论工作，无实际训练实验和 GPU 使用报告，也无大规模实证 Benchmark 评估。附录 B 提供了算法伪代码和实现细节，但未涉及实际计算资源消耗。

---

### 5. 实验数量与充分性

- **实验数量**：论文未提供传统意义上的实验（无消融实验、无多数据集对比）。主要通过解析推导、图示和示例性曲线来说明理论结果。
- **充分性**：作为理论工作，其充分性体现在数学证明的完整性和推导的系统性。闭式系数、控制变量形式、次临界‑超临界相变、校准度量增益、方差分解等所有核心结论均给出了完整证明。但缺乏实际经验验证是明显的短板，理论结论在真实 LLM 训练场景中的有效性需要进一步实证检验。

---

### 6. 论文的主要结论与发现

1. **代理目标族连续性**：RL2ML 通过参数 γ 连续桥接了普通 RL、ML 和超 ML 目标。
2. **组级更新尺度揭示相变**：总体权重 w(p) 仅反映期望梯度，而组级更新尺度 α_K 决定实际观察到的更新几何。γ = 1 是区分次临界与超临界的结构边界。
3. **与最大似然的接近程度不充分**：校准度量增益 U(γ) 表明，最佳 γ 联合取决于评估指标、局部敏感性和估计器方差，而非单纯选择离 ML 最近的点。
4. **超临界区有明确噪声代价**：超临界放大低成功 prompt 伴随可量化的有限 rollout 噪声增长。
5. **γ 选择是一维优化**：将 γ 视为外层控制变量而非无约束超参数，可在 oracle 设定下表述为方差感知的一维优化问题，并在 Verl 框架中通过校正 shard 或在线 batch 近似实现。

---

### 7. 优点

- **理论框架完整性**：从代理目标的定义、闭式无偏估计器的推导、次临界‑超临界相变的揭示，到校准度量增益和方差分解，形成了完整的理论闭环，所有关键结论均有数学证明支撑。
- **估计器‑目标对齐保持**：RL2ML 保持了 MaxRL 的核心特性——有限 rollout 代理目标与有限 rollout 无偏估计器之间的精确对齐。
- **控制变量设计优雅**：提供了直接估计器和控制变量变体两种形式，控制变量形式仅通过减 1 即可降低方差，保持无偏性。
- **实践指导具体**：附录 B 提供了完整的 Python 实现代码，包括系数表预计算、advantage 构建、success count 收集和 γ* 在线选择，具备直接可操作性。
- **区分总体权重与实际更新尺度**：通过 Bernstein 表示和组级更新尺度 α_K 的概念，清晰区分了两个常被混淆的层面。

---

### 8. 不足与局限

- **缺乏大规模实证验证**：论文以理论分析为主，无实际 LLM 训练实验，所有结论仅通过解析推导和图示说明，真实场景下的有效性未经检验。
- **算力需求未评估**：闭式系数和方差分解在理论上优雅，但实际训练中控制 variate 的方差减少效果、对训练稳定性的影响，以及与 PPO/GRPO 等主流优化器的配合效果均未经验证。
- **假设理想化**：方差分解中的 μ_x、Σ_x 需要在实践中用估计值替代；校准 shard 和在线 batch 的统计可靠性依赖于环境稳定性。
- **与 RLHF 流程的兼容性**：论文专注于 correctness-based RLVR（可验证奖励），未讨论在无法获得精确二元反馈的标准 RLHF（人类偏好）场景下的适用性。
- **一维优化依赖外层设计**：γ* 的选择依赖于对 λ_var（方差容忍系数）的设定，该参数本身缺乏系统化的选择指南。

（完）
