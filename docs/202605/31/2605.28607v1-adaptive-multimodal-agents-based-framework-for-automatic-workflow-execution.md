---
title: Adaptive Multimodal Agents-Based Framework for Automatic Workflow Execution
authors: "Susanna Cifani, Mario Luca Bernardi, Marta Cimitile"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28607v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体自动工作流执行框架
tldr: 多模态多智能体框架实现自主工作流导航。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28607v1/fig-001.webp\", \"caption\": \"\", \"page\": 4, \"index\": 1, \"width\": 1773, \"height\": 1349}]"
motivation: 多智能体自动工作流执行框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Modern information systems require autonomous agents capable of navigating complex workflows, yet current methodologies often struggle with the transition from structured metadata parsing to general environmental perception. While the integration of MLLMs has enabled agents to interact directly with GUIs, existing approaches typically treat task sequences as discrete, linear episodes. This fragmentation prevents agents from capturing the underlying transition topology, limiting their effectiveness in novel or non-stationary scenarios. To address this, we propose a novel multimodal multi-agent framework that achieves automatic workflow execution through a distinct two-phase pipeline. First, during an offline discovery phase, the architecture adaptively constructs a topological knowledge base from fragmented execution logs. During inference, agents leverage Adaptive Retrieval-Augmented Generation (RAG) over this fixed, pre-established graph, coupled with a closed-loop collaborative verification protocol to dynamically self-correct and navigate. This graph-based approach facilitates superior task decomposition and adaptive navigation performance. We validate our framework in a real-world context, demonstrating its ability to maintain high reliability and semantic awareness even with limited training data.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现代信息系统中，自主智能体需要在复杂工作流驱动的环境中执行任务，但现有方法通常将任务序列视为离散的、线性的片段，无法捕获系统状态之间的底层转换拓扑结构，导致在面对新颖或非平稳界面时适应性不足。
- **研究背景**：早期方法依赖解析结构化元数据（如HTML树、API模式），但难以应对动态界面；多模态大语言模型（MLLMs）的出现使得智能体能够直接分析GUI视觉景观，但现有方案（如SeeClick、Mind2Web）仍以线性episode方式处理任务，缺乏对全局转换拓扑的感知，限制了泛化能力和错误恢复能力。
- **整体含义**：提出一种**图结构化的多模态多智能体框架**，通过离线构建拓扑知识库和在线自适应RAG与闭环验证机制，实现更鲁棒的工作流自动执行。

## 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

### 核心思想
- **两阶段Pipeline**：
  1. **离线发现阶段**：从碎片化的执行日志中自适应构建拓扑知识库（工作流图）。
  2. **在线推理阶段**：基于预建图使用自适应RAG，并结合闭环协作验证协议进行动态自纠正和导航。
- **关键洞察**：将历史任务执行视为全局拓扑结构的路径采样，而非孤立轨迹，从而支持增量学习和拓扑推理。

### 关键技术细节
- **图结构知识库**：构建工作流图 \( G = (V, E) \)，节点 \(V\) 表示唯一GUI状态，边 \(E\) 表示状态转换动作。采用BGE-M3语义嵌入和FAISS索引进行节点相似度检查（Top-4候选），确保图的简洁性和无冗余。
- **自适应RAG Pipeline**：给定任务查询 \(q\)，通过检索函数 \(R\) 获取Top-k相似轨迹，构建增强上下文 \(c\)，优化公式为：  
  \[
  R(q, K) \rightarrow T_{\text{top-k}} = \arg \top_k \sum_{T \in K} \text{sim}(e_q, e_T)
  \]
- **多智能体执行系统**（三层架构）：
  - **规划层**：Global Planner生成高层策略 \(G\)；Sub-goal Planner分解为即时子目标 \(g_t\)，并基于积累的历史 \(h\) 动态调整。
  - **执行层**：Observation Agent将当前GUI状态 \(s_t\) 编码为语义表示；Decision Agent结合子目标和状态提出原子动作 \(a_t\)。
  - **验证层**：Verifier Agent评估动作一致性，返回APPROVE/REJECT及反馈 \(f\)：  
    \[
    v(s_t, a_t, g_t) = \begin{cases}
    \text{APPROVE} & \text{if } C(s_t, a_t, g_t) = \text{True} \\
    \text{REJECT}(f) & \text{otherwise}
    \end{cases}
    \]
    验证失败时触发递归修正，最多尝试 \(M=4\) 次。
- **目标感知叙事历史机制**：避免上下文丢失和无限循环。将原子动作转化为语义过渡叙述（如“滚动以显示完整歌词，这些是...”），而非简单动作标签。
- **闭环自纠正**：通过验证层的反馈循环，实现从开环到闭环范式的转变，减少错误传播和逻辑幻觉。

### 算法流程（Algorithm 1概述）
1. 检索相关轨迹并构建增强上下文。
2. 全局规划生成高层策略 \(G\)。
3. 主循环：获取子目标 → 观测当前状态 → 决策提议动作 → 验证（失败则修正子目标并重试，最多M次） → 执行动作 → 更新叙事历史。
4. 任务完成则终止。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：GUIOdyssey基准，包含**1,666个跨App移动导航任务** episodes，分为五个功能域：
  - Information Management（信息管理）
  - Web Shopping（网购）
  - Media Entertainment（媒体娱乐）
  - Social Sharing（社交分享）
  - Multi-Apps（多App协作，最复杂）
- **评估指标**：
  - **AMS（Action Matching Score）**：细粒度步骤精度，衡量每个step的动作匹配程度。
  - **SR（Success Rate）**：任务完成率，二元指标，要求所有步骤正确。
- **对比方法**：
  - GeminiProVision、CogAgent、GPT-4V（视觉语言模型基线）
  - Qwen2.5-VL-72B（强大VL模型）
  - PG-Agent（72B和30B版本，图结构记忆基线）
- **消融实验**：在100个采样episodes上对比Full模型、Context-only（仅历史机制）、Verifier-only（仅验证机制）的性能。
- **替代骨干实验**：在16个episodes子集上测试Qwen3-VL-32B-Thinking和GLM-4.6V-Flash。

## 4. 资源与算力

- **硬件配置**：4 × NVIDIA A100 GPU（40GB），使用vLLM框架和tensor并行。
- **计算开销**：
  - 处理完整1,666 episodes耗时约**80小时**（平均每episode约173秒）。
  - 每步上下文长度4,000–8,300 tokens，平均约6,200 tokens。
- **模型规模**：主方法使用Qwen2.5-VL-30B（72B模型因显存限制未测试：约144GB权重）。
- **采样**：离线图构建阶段采用1/50的随机采样比例（遵循PG-Agent和AITW实践）。

## 5. 实验数量与充分性

- **实验规模**：
  - 完整评估：1,666 episodes。
  - 消融实验：100个随机采样episodes。
  - 替代骨干实验：16个episodes子集。
- **充分性分析**：
  - **优势**：包含完整benchmark评估、多种对比基线、消融分析和替代模型测试，覆盖多个功能域和指标。
  - **局限**：消融实验仅用100 episodes（占总数6%），替代骨干实验仅16 episodes，统计意义有限；仅在一个数据集上验证，可能影响泛化性结论的稳健性。

## 6. 论文的主要结论与发现

- **性能提升显著**：
  - AMS达到56.41%，比最佳基线（PG-Agent 72B的47.7%）**绝对提升+12.85%**。
  - SR从0.42%提升至1.55%，**超过三倍**增长。
  - **关键发现**：仅用30B参数的模型即可超越72B基线，说明架构创新（语义历史+闭环验证）可优于单纯模型放大。
- **消融实验洞察**：
  - 信息性历史机制贡献最大（AMS提升+6.52pp）。
  - 验证机制单独使用时提升有限（+0.07pp），且在某些域反而降低性能；可能因为缺乏充分语义基础时修正反馈不准确。
  - 有趣的是，Full模型（AMS 53.65%）反而低于Context-only（AMS 60.17%），表明验证反馈可能干扰正确的动作序列。
- **失败模式分析**：
  - 视觉定位错误（38%）：UI元素定位不准确。
  - 跨App状态丢失（26%）：跨应用边界信息维护不足。
  - 动态内容处理（22%）：异步加载或元素位置变化。
  - 验证器误报（14%）：正确动作被错误拒绝。

## 7. 优点

- **拓扑感知优势**：通过图结构化知识库捕获状态转换关系，支持动态路径寻找和拓扑推理，克服线性episode的“隧道视野”。
- **自适应RAG增强**：结合结构化图拓扑和语义检索，为智能体提供全局上下文，显著减少幻觉。
- **闭环自纠正机制**：验证层实现动作一致性检查和递归修正，降低错误传播风险。
- **目标感知历史**：语义化叙事历史机制有效防止上下文丢失和无限循环，对复杂跨App任务尤为重要。
- **架构可扩展性**：通过分层智能体设计（规划/执行/验证），模块化程度高，易于集成新模块。
- **实验验证充分**：在真实大规模benchmark上评估，对比多种SOTA方法，包含消融和替代骨干实验。

## 8. 不足与局限

- **任务完成率仍低**：尽管提升三倍，SR仅1.55%，距离实际部署仍有较大差距。
- **验证器干扰问题**：消融实验显示验证机制在Full模型中可能干扰历史机制，导致Full模型AMS低于Context-only，需进一步研究自适应验证阈值。
- **计算可扩展性限制**：离线图构建和在线多智能体推理的计算开销限制了在大规模动作空间上的应用。
- **单一基准验证**：仅在GUIOdyssey上评估，可能无法泛化到所有GUI导航场景（如Web、桌面）。
- **视觉 grounding 瓶颈**：38%的失败源于视觉定位错误，表明当前VL模型的细粒度视觉理解仍需加强。
- **骨干模型依赖**：框架性能受限于底层MLLM能力，更换不同骨干可能导致性能波动。

（完）
