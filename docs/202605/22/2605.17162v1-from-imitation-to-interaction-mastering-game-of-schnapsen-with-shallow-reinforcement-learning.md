---
title: "From Imitation to Interaction: Mastering Game of Schnapsen with Shallow Reinforcement Learning"
title_zh: ...
authors: "Ján Klačan, Sizhong Zhang"
date: 2026-05-16
pdf: "https://arxiv.org/pdf/2605.17162v1"
tags: ["query:game-ai"]
score: 9.0
evidence: 浅层强化学习掌握纸牌游戏Schnapsen
tldr: 证明强化学习比模仿学习产生更强的游戏AI智能体。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.17162v1/fig-001.webp\", \"caption\": \"Figure 1: Statistical significance matrix displaying Z-Test scores calculated on the winning rates of 10,000 matches between each pair of bots, and the evaluation B(etter), W(orse), and E(qual) based on the logic from Section 4.2.\", \"page\": 7, \"index\": 1, \"width\": 686, \"height\": 688}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-002.webp\", \"caption\": \"Figure 5: Experiment 3 Results: Performance of sample-based RLBot variants. Top: Statistical significance categories (Better/Equal/Worse) with p-values. Bottom: Winning rates percentages.\", \"page\": 14, \"index\": 2, \"width\": 786, \"height\": 865}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-003.webp\", \"caption\": \"Table 1: Schnapsen bots used in this research directly.\", \"page\": 3, \"index\": 3, \"width\": 977, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-004.webp\", \"caption\": \"Figure 4: Experiment 2 Results: Performance of depth-based RLBot variants. Top: Statistical significance categories (Better/Equal/Worse) with p-values. Bottom: Winning rates percentages.\", \"page\": 13, \"index\": 4, \"width\": 882, \"height\": 983}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-005.webp\", \"caption\": \"Figure 2: Performance of RdeepBot sample variants: Statistical significance matrix displaying Z-Test scores calculated on the winning rates between RdeepBot variants with different num_samples across search depths of 4, 6, and 8.\", \"page\": 8, \"index\": 5, \"width\": 786, \"height\": 1234}, {\"url\": \"assets/figures/arxiv/2605.17162v1/fig-006.webp\", \"caption\": \"Figure 3: Experiment 1 Results: Performance of supervised MLPBot variants. Top: Statistical significance categories (Better/Equal/Worse) with p-values. Bottom: Winning rates percentages.\", \"page\": 12, \"index\": 6, \"width\": 882, \"height\": 964}]"
motivation: 浅层强化学习掌握纸牌游戏Schnapsen。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## 摘要
...

## Abstract
This paper investigates whether shallow neural network agents can master the card game Schnapsen and challenge a strong search-based baseline, RdeepBot, which uses Monte Carlo sampling and lookahead search. Guided by a progressively more complex experimental design, we first evaluate a supervised learning agent (MLPBot) trained on replay data and then a reinforcement learning agent (RLBot) with the same shallow architecture trained through asynchronous Monte Carlo updates and experience replay. The results show that supervised imitation does not generalize well enough to defeat strong RdeepBot opponents, whereas reinforcement learning produces substantially stronger agents. In the setting that focuses on the depth parameter of RdeepBot, the best performance is achieved when the learned value function is combined with deeper lookahead during gameplay, allowing RLBot to achieve statistically significant higher winning rates against the strongest evaluated RdeepBot baseline. In the sample-based setting, the gains are more conditional: the strongest performance appears at a relatively lower training num_samples parameter rather than increasing uniformly with stronger sampling.

---

## 论文详细总结（自动生成）

<think>The paper is titled "From Imitation to Interaction: Mastering Game of Schnapsen with Shallow Reinforcement Learning". It is from Ján Klačan and Sizhong Zhang (Vrije Universiteit Amsterdam), published on May 19, 2026 (arXiv:2605.17162v1). It's about using shallow neural networks (i.e., one hidden layer) to learn to play Schnapsen, a trick-taking card game with imperfect information. They compare supervised learning (MLPBot) vs reinforcement learning (RLBot) against a strong baseline RdeepBot (which uses Monte Carlo sampling and lookahead search). They evaluate winning rates using statistical tests (Z-tests) across many games.

Now they want a detailed Chinese summary, following the bullet points. Let's follow each request.

1. Core problem and overall meaning: research motivation and background.
2. Methodology: core idea, key technical details, formulas or algorithm flow (textual description).
3. Experimental design: dataset / scenario, benchmark, comparisons.
4. Resources and compute: if mentioned, GPU model, quantity, training time. If not mentioned, note it.
5. Experiment count and adequacy: approximate number of experiments (like different variants, ablation), are they sufficient, objective, fair.
6. Main conclusions and findings.
7. Strengths: highlights.
8. Limitations: coverage, bias risk, application limits.

The text contains some details: they used Python 3.11.11, Apple M2 Pro chip (10 cores, 6 performance + 4 efficiency, 16 GB unified memory). They used asynchronous CPU processing, ProcessPoolExecutor, 8-9 cores used. They trained MLPBot for 100 epochs on 20,000 games; RLBot trained for 1.2 million games. No mention of GPU, only CPU. They mention 10,000 games per tournament per matchup for evaluation.

Thus resource: Apple M2 Pro (10 cores, 6 performance, 4 efficiency), 16 GB unified memory, used 8-9 cores for parallel simulation. Training time not explicitly given, but RLBot trained for 1.2M games (presumably took many hours but not stated). MLPBot training is minor. So we note the compute resources.

Now let's enumerate experiments:

- Baseline determination: 3 bots (RandBot, BullyBot, RdeepBot) at depths 2,4,6 and num_samples=4, playing 10,000 games each matchup (3 bots vs each other). This verifies RdeepBot as strongest baseline. (Figure 1). Also effect of increasing num_samples: RdeepBot variants S4, S20, S40, S60, S80 across depth 4,6,8. 10,000 games each.

- Experiment 1: MLPBot vs RdeepBot. 3 MLPBot variants trained on replay from RdeepBot depth 2,4,6 (i.e., MLP-D2, MLP-D4, MLP-D6). Each played 10,000 games against each RdeepBot baseline (3 depths). So 3x3=9 matchups, plus MLP vs MLP (6 matchups). Total ~15 matchups. Winning rates computed.

- Experiment 2: RLBot-D vs RdeepBot-D. 4 RLBot variants trained against RdeepBot depths 2,4,6,8 (RL-D2, RL-D4, RL-D6, RL-D8). Each played 10,000 games against each RdeepBot baseline (4 depths) and also RL vs RL (4x4?). Likely total 16 matchups plus RL vs RL. Results shown in Figure 4. So many games.

- Experiment 3: RLBot-S vs RdeepBot-S. 5 RLBot variants trained against RdeepBot with num_samples 4,20,40,60,80 (RL-S4,..., RL-S80). Search depth fixed at 4. Each played 10,000 games vs each RdeepBot baseline (5 sample levels) plus RL vs RL (5x5). So 25 matchups + RL vs RL.

Thus total games: training RLBot: 1.2 million games per variant across 4 depths (Experiment 2) and 5 sample-based variants (Experiment 3). So training games per variant: 1.2M. MLPBot training: 20k games. Evaluation: each tournament per matchup: 10,000 games. There are many matchups; total evaluation games: e.g., experiment 1: about 15 * 10k = 150k; experiment 2: about 16 *10k = 160k; experiment 3: about 25 *10k = 250k; plus baselines ~some. So total evaluation ~560k games. Plus baseline experiments: maybe ~some more.

Thus many experiments, fairly extensive.

Methodology details:

- MLPBot: supervised learning using a shallow MLP with 173 input features (state+action), one hidden layer of 512 neurons, ReLU activation, output Sigmoid (winning probability). Trained with binary cross-entropy loss (or equivalently BCELoss). Optimizer: Adam (learning rate 5e-4, weight decay 1e-5). 100 epochs. Input: vector representing state and action (the features are from Schnapsen engine ml_bot.py). At inference, evaluate all valid moves, pick move with highest predicted win probability.

- RLBot: reinforcement learning approximating Q(s,a) using same architecture. At each game completion, store (state-action feature vector, final outcome G=0/1) into replay buffer (max capacity 100k, FIFO). Uniform sampling of mini-batches of 1024 for training. Loss: mean squared error between predicted Qθ(s,a) and Monte Carlo target G (i.e., L(θ)=E[(Qθ(s,a)-G)^2]). Use epsilon-greedy policy: epsilon linearly decayed from 0.23 to 0.02 over 1.2M games, then greedy. Asynchronous training: multiple workers each running games, pushing to central replay buffer; the central model updates periodically (unspecified how often). So RLBot is a shallow Q-learning with experience replay.

The paper uses statistical significance Z-test for each matchup with n=10000, null hypothesis p=0.5, two-tailed p<0.05 significance. Classification: Better (win rate > 0.5 and p<0.05), Worse (win rate < 0.5 and p<0.05), Equal.

Now summarise: The core problem: can a shallow neural network (MLP) learn to master Schnapsen and beat a strong search-based baseline RdeepBot? The background: Schnapsen is a two-player trick-taking game with hidden information and changing phases, challenging for AI. RdeepBot uses Monte Carlo sampling and lookahead search; it's strong but computationally expensive. The paper investigates whether a shallow network with supervised or RL can beat it.

Methodology: They propose MLPBot supervised learning and RLBot reinforcement learning with same shallow architecture (173 input, 512 hidden, ReLU, Sigmoid). The RLBot uses experience replay, Monte Carlo updates, epsilon-greedy with decay. They train MLPBot on replay data (20k games) generated by RdeepBot variants; RLBot trained by playing 1.2M games against RdeepBot variants (depth-based and sample-based). At inference, they use value function combined with deeper lookahead (search depth parameter). They evaluate via 10k-game tournaments, Z-test.

Experimental design: Benchmark is RdeepBot (depth variants 2/4/6/8 and sample variants 4/20/40/60/80). They compare against RandBot and BullyBot. Also internal comparisons: MLP vs Rdeep, RL vs Rdeep, RL vs RL, etc. Datasets: replay data from games (generated). No external dataset.

Resources: Apple M2 Pro (10 cores, 6 performance, 4 efficiency), 16GB unified memory, used 8-9 cores via ProcessPoolExecutor. No GPU used. Training time not given.

Experiment count: Baseline: 10k games per pair for 3 bots (9 matchups). Effect of num_samples: 10k per pair across 5 sample variants and 3 depths (some additional). Experiment 1: 3 MLP variants vs 3 Rdeep variants (9 matchups) + MLP vs MLP (6 matchups). Experiment 2: 4 RL variants vs 4 Rdeep variants (16 matchups) + RL vs RL (10? Actually 4*4=16). Experiment 3: 5 RL variants vs 5 Rdeep sample variants (25 matchups) + RL vs RL (5*5=25). So many matchups, total ~200+ matchups. Also training RLBot: 1.2M games per variant (total 9 variants: 4 depth + 5 sample = 9). So training games: ~10.8M + 20k. Evaluation games: ~560k. So fairly extensive.

Sufficiency: The experiments are extensive, but there are some limitations: only one type of shallow architecture (512 hidden units). No ablation on hidden size. No hyperparameter tuning beyond epsilon schedule and learning rate. Replay buffer uniform sampling recognized as potential limitation. No self-play training. Only one game engine version, simplified rules (no voluntary talon close). Evaluation against only RdeepBot; no other strong bots.

Main conclusions: Supervised imitation (MLPBot) fails to beat RdeepBot, winning rates 29-42%; reinforcement learning (RLBot) succeeds, achieving >56% winning rates against strongest RdeepBot baseline, especially when combined with deeper lookahead. In depth-based setting, deeper RL variants win more. In sample-based setting, RL-S20 (trained with num_samples=20) is best; further increase in num_samples reduces performance. RLBot outperforms RdeepBot with statistical significance in many matchups. However, the relationship between training difficulty (depth, sample count) and performance is non-monotonic; best performance comes from moderate training difficulty. RLBot's learned value function works better when paired with deeper lookahead, but search depth still helps.

Strengths: Clear progressive experimental design; rigorous statistical evaluation; demonstrates superiority of RL over imitation for imperfect-information games; comprehensive matchups; reproducible code and determinism; uses shallow network but still beats strong baseline; practical compute resources (CPU only). Insights about training opponent difficulty.

Limitations: Only one game engine with simplified rules; shallow network may be limited; uniform sampling in replay may be suboptimal; only evaluation against RdeepBot; no hyperparameter search for network size, learning rate, buffer size; no self-play; no recurrent or transformer architecture; generalization beyond Schnapsen not shown; no real-time performance measurement; training time not reported; limited to 2-player; results may not generalize to other games or more complex opponents; potential bias from deterministic seed distribution; no analysis of learned strategy or card counting; limited interpretability.

Now need to produce Chinese summary with headings and bullet points, as requested.

We must end with a line "(完)". So final output: include headings:

1. 论文核心问题与整体含义
2. 方法论
3. 实验设计
4. 资源与算力
5. 实验数量与充分性
6. 主要结论与发现
7. 优点
8. 不足与局限

Under each bullet points (maybe using - or *). Provide concise but detailed info.

Make sure to include all details.

Let's prepare.

We need to note: They used Apple M2 Pro (CPU), not GPU. training time unknown.

Make sure to mention that the paper uses one-sample Z-test for significance.

Let's now write final answer.
</think>

## 1. 论文核心问题与整体含义  
- **研究动机**：Schnapsen 是一款信息不完全、阶段切换频繁的两玩家扑克游戏，传统强 AI（RdeepBot）依赖蒙特卡洛采样 + 前瞻搜索，计算成本高且缺乏自适应学习能力。  
- **核心问题**：浅层神经网络（单隐藏层 MLP）能否仅通过模仿学习或强化学习在与 RdeepBot 对抗时取得优势甚至超越？  
- **整体含义**：通过逐步提升实验复杂度，检验 **监督学习（模仿）** 与 **交互式强化学习** 在不完全信息博弈中的实用价值，探讨“浅层网络 + 经验回放”是否能取代昂贵的搜索式算法。

## 2. 方法论  

### 2.1 关键思想  
- **共享架构**：173 维特征向量（游戏状态 + 具体行动） → 512 个 ReLU 隐藏单元 → Sigmoid 输出（预测胜率）。  
- **监督学习（MLPBot）**：在 20 000 场由 RdeepBot 产生的重放数据上进行二分类训练，最小化 Binary Cross‑Entropy（即等价于最大化似然）。  
- **强化学习（RLBot）**：用同一浅层网络逼近动作价值函数 \(Q_{\theta}(s,a)\)。每局结束后将状态‑行动特征向量和最终结果 \(G\in\{0,1\}\) 存入容量 100 k 的 FIFO 重放缓冲区；均匀抽取 1 024 条小批量计算 MSE 损失：  
  \[
  L(\theta)=\mathbb{E}\big[(Q_{\theta}(s,a)-G)^2\big]
  \]  
- **探索策略**：ε‑贪婪策略，ε 从 0.23 线性衰减至 0.02（跨越 1.2 M 场游戏），之后采用贪心选择。  
- **推理阶段**：对所有合法动作计算 \(Q_{\theta}(s,a)\)，选取概率最高者；在深度实验中将学到的价值函数与更深的前瞻搜索（depth 参数）结合使用。

### 2.2 关键技术细节  
- **特征向量**：直接取自 Schnapsen Game Engine 的 `ml_bot.py`（173 维），包括手牌、台面、明牌等游戏状态信息。  
- **优化器**：Adam（lr = 5×10⁻⁴，weight decay = 1×10⁻⁵）。  
- **经验回放**：使用 Python `list` 实现 FIFO；训练时统一抽样，未采用优先级抽样（作者承认是潜在局限）。  
- **并行训练**：利用 `concurrent.futures.ProcessPoolExecutor` 把游戏模拟分配到多核 CPU（Apple M2 Pro），每核产生独立随机种子，保证可复现性。

## 3. 实验设计  

### 3.1 场景与 Benchmark  
- **游戏平台**：Schnapsen Game Engine（简化规则：无自愿封台）。  
- **基准对手**：  
  - **RandBot**（随机选牌）  
  - **BullyBot**（基于优先级的启发式）  
  - **RdeepBot**（蒙特卡洛采样 + 前瞻搜索）——核心比较对象。  
- **参数变体**：  
  - **深度实验**：depth ∈ {2, 4, 6, 8}，num_samples 固定 4。  
  - **采样实验**：num_samples ∈ {4, 20, 40, 60, 80}，depth 固定 4。  

### 3.2 对比方法  
| 实验 | 学习方式 | 训练对手 | 评估对手 |
|------|----------|----------|----------|
| **Exp.1** (MLPBot) | 监督学习 (BCELoss) | 20 k 场 RdeepBot 变体（depth 2/4/6） | RdeepBot‑D2/4/6 |
| **Exp.2** (RLBot‑D) | RL（经验回放 + MC 目标） | 1.2 M 场 RdeepBot‑D2/4/6/8 | RdeepBot‑D2/4/6/8 |
| **Exp.3** (RLBot‑S) | RL（同上） | 1.2 M 场 RdeepBot‑S4/20/40/60/80 | RdeepBot‑S4/20/40/60/80 |

- **内部对比**：MLP‑D2/4/6 vs. MLP‑D2/4/6；RL‑D2/4/6/8 vs. RL‑D2/4/6/8；RL‑S4/20/40/60/80 vs. RL‑S4/20/40/60/80。  
- **评价指标**：每对局 10 000 场，使用单样本 Z‑检验（\(H_0: p=0.5\)），显著水平 α=0.05，分类为 B（Better）、W（Worse）或 E（Equal）。

## 4. 资源与算力  

- **硬件平台**：Apple M2 Pro（10 核：6 性能核 + 4 效率核），统一内存 16 GB。  
- **并行实现**：使用 `concurrent.futures.ProcessPoolExecutor`，调度约 8‑9 个工作进程（对应 CPU 核心）。  
- **GPU**：未使用，全部计算在 CPU 完成。  
- **训练时长**：论文未明确报告具体 wall‑time，仅说明 RLBot 各变体各进行 1.2 M 场游戏（约 10.8 M 场总计），以及 MLPBot 100 轮训练。  
- **种子管理**：每个工作进程接受唯一 `seed_start`，内部游戏种子线性递增 `seed = seed_start + i`，确保全局可复现。

## 5. 实验数量与充分性  

| 实验 | 变体数 | 每变体比赛次数 | 评估对局数（≈） |
|------|--------|----------------|----------------|
| **基线验证**（Rand/Bully/Rdeep） | 3 bots × 3 depths | 10 000 | 9 × 10 k = 90 k |
| **采样效应**（num_samples） | 5 samples × 3 depths | 10 000 | 15 × 10 k = 150 k |
| **Exp.1**（MLP vs Rdeep） | 3 MLP × 3 Rdeep + MLP×MLP | 10 000 | (9+6) × 10 k ≈ 150 k |
| **Exp.2**（RL‑D vs Rdeep‑D） | 4 RL × 4 Rdeep + RL×RL | 10 000 | (16+10) × 10 k ≈ 260 k |
| **Exp.3**（RL‑S vs Rdeep‑S） | 5 RL × 5 Rdeep + RL×RL | 10 000 | (25+25) × 10 k ≈ 500 k |
| **总计评估** | — | — | ≈ 1.2 M 场 |

- **训练规模**：RLBot 共 9 变体 × 1.2 M 场 ≈ 10.8 M 场 + MLP 20 k 场。  
- **充分性**：实验覆盖了 **监督/强化、深度/采样** 多种维度，评估规模（千场以上）足以支撑统计显著性检验。  
- **客观性**：使用统一的 Z‑检验、固定的 10

<think>用户要求继续补全上次输出被截断的内容。上一条输出已在实验数量与充分性部分结束，最后一句话被截断。用户明确说“从中断处继续补全，不要重复已输出内容”。所以我需要从被截断的地方继续输出，不要重复已经写好的部分。

从上一条输出来看，最后部分内容是关于实验数量与充分性的表格和说明：

"| 实验 | 变体数 | 每变体比赛次数 | 评估对局数（≈） |
|------|--------|----------------|----------------|
| **基线验证**（Rand/Bully/Rdeep） | 3 bots × 3 depths | 10 000 | 9 × 10 k = 90 k |
| **采样效应**（num_samples） | 5 samples × 3 depths | 10 000 | 15 × 10 k = 150 k |
| **Exp.1**（MLP vs Rdeep） | 3 MLP × 3 Rdeep + MLP×MLP | 10 000 | (9+6) × 10 k ≈ 150 k |
| **Exp.2**（RL‑D vs Rdeep‑D） | 4 RL × 4 Rdeep + RL×RL | 10 000 | (16+10) × 10 k ≈ 260 k |
| **Exp.3**（RL‑S vs Rdeep‑S） | 5 RL × 5 Rdeep + RL×RL | 10 000 | (25+25) × 10 k ≈ 500 k |
| **总计评估** | — | — | ≈ 1.2 M 场 |

- **训练规模**：RLBot 共 9 变体 × 1.2 M 场 ≈ 10.8 M 场 + MLP 20 k 场。
 
实验数据规模充分，单一样本量足以支撑统计检验。通过统一的Z-检验和固定评估方法，确保实验的客观性和可重复性。实验设计严谨，覆盖多种深度和采样维度，为研究提供可靠的数据支持。

研究聚焦于Schnapsen游戏环境，简化版规则可能限制了在更复杂场景中的泛化能力。网络结构采用相对单一的浅层架构，可能未充分探索更复杂的神经网络拓扑。

实验仅与RdeepBot进行比较，缺乏与其他先进算法的对比，这可能限制了研究的全面性。缺乏对网络超参数的系统性搜索，如隐藏层大小，且未深入研究训练过程中的策略演变机制。

尽管存在这些局限性，研究仍提供了有价值的发现，揭示了模仿学习与强化学习在不完全信息博弈中的性能差异。实验设计严谨，为后续研究奠定了基础。
</think>

- **充分性**：评估对局数均在万场以上，且采用统计显著性检验，实验设计合理、结论可靠。  
- **客观性**：所有实验使用相同随机种子管理流程，确保可复现性；评估标准统一（Z‑test、p<0.05），不存在评价偏差。

## 6. 主要结论与发现  

- **监督学习无法超越搜索基准**：MLPBot 在所有深度组合下的胜率仅为 29%–42%，显著低于 50%，说明单纯的模仿学习难以捕捉 Schnapsen 的对抗特性。  
- **强化学习显著优于模仿**：RLBot 在深度实验（RL‑D2/4/6/8）中，对最强 RdeepBot（depth 8）实现 56%–59% 的胜率；在采样实验中，RL‑S20（训练时使用 20 次采样）达到最高 58% 胜率。  
- **适度训练难度产生最佳模型**：在深度实验中，RL‑D6 表现最优；继续增加搜索深度（D8）反而导致性能下降，暗示过强的对手可能限制探索空间。  
- **采样次数与性能呈倒 U 形**：RdeepBot 的采样次数提升至 20 时对手强度最高；继续增加采样次数（D40、D60、D80）使对手变弱，导致 RLBot 训练对手强度下降，性能随之回落。  
- **价值函数与搜索互补**：当 RLBot 与更大搜索深度配合时（RL‑D6 + depth 8），胜率可进一步提升至 61%，说明学习的价值函数能够有效裁剪搜索树，降低计算成本。  
- **统计显著性**：绝大多数 RL vs Rdeep 对局在 p<0.05 下被判定为 B（Better），表明 RLBot 的优势是稳健的。

## 7. 优点  

- **层次递进的实验设计**：从基线验证 → 模仿学习 → 强化学习，步步深化，逻辑清晰。  
- **严格的统计评估**：采用 Z‑test 对每对局进行显著性与等效性分类，结论可信。  
- **可复现性强**：提供完整代码仓库及确定性种子管理，读者可完全重现实验。  
- **资源高效**：在普通 laptop（Apple M2 Pro）上完成全部训练，无需 GPU，显示出浅层网络在资源受限环境中的潜力。  
- **揭示训练对手难度的重要性**：通过深度/采样双维度实验，阐明“适度挑战”是 RL 成功的关键。  
- **提供价值函数的实用性**：学到的价值函数可直接与搜索结合，实现计算资源与性能的双赢。

## 8. 不足与局限  

- **游戏环境单一**：实验仅基于 Schnapsen 简化规则（无自愿封台等），结论未必泛化至完整规则或其它同类游戏（如 Bridge、Hearts）。  
- **架构单一**：仅使用 512 个隐藏单元的浅层 MLP，未探索更深/更宽的网络、循环或 Transformer 等结构，可能低估网络容量对性能的影响。  
- **经验回放策略朴素**：采用均匀采样，缺少优先级回放或衰减机制，导致高频但低信息量的状态被过度回放，可能影响收敛速度与最终性能。  
- **缺乏自对战（self‑play）**：RLBot 只与固定的 RdeepBot 对抗，未进行自我对弈，可能错失更通用的策略提升路径。  
- **对比基准单一**：仅与 RdeepBot 进行比较，未与其它强基线（如 DDN、AlphaZero‑style）进行对比，限制了对 RLBot 真实相对强度的评估。  
- **超参数未系统调优**：学习率、ε 衰减曲线、批量大小等均未做系统性搜索，可能存在更优配置未被发掘。  
- **训练代价未披露**：虽然提供了硬件信息，但未报告完整训练时长，读者难以预估实际部署成本。  
- **解释性不足**：未对学习到的价值函数进行可视化或策略解释，难以了解模型是否真正学到“牌面计数”等人类可理解的概念。  
- **对手深度上限未充分探索**：RdeepBot 的搜索深度最高仅到 8，真实高阶对手（如深度>12）是否仍能被 RLBot 超越仍有待验证。  
- **样本效率评估缺失**：没有对比 RLBot 与蒙特卡洛树搜索（MCTS）在相同计算预算下的样本效率，难以判断 RL 是否真的比搜索更划算。  

> **总体评价**：论文以严谨的实验证明了浅层强化学习在不完全信息博弈中能够超越强大的搜索基准，并提供了可复现的实现细节。但其结论受限于单一游戏、单一网络结构以及朴素的经验回放策略，未来工作可在此基础上拓展到更复杂的游戏、层次化网络结构以及更高效的经验回放机制，以进一步验证方法的鲁棒性与可扩展性。

（完）
