Title: A Contractive Feedback Semantics for Reinforcement Learning

URL Source: https://arxiv.org/pdf/2605.24759v1

Published Time: Tue, 26 May 2026 01:10:09 GMT

Number of Pages: 35

Markdown Content:
# A Contractive Feedback Semantics for Reinforcement Learning 

Zuyuan Zhang 

The George Washington University 

zuyuan.zhang@gwu.edu 

# Abstract 

Discounted reinforcement learning is usually presented through Bellman equations on closed Markov decision processes. This paper develops a compositional view: a one-step decision process is treated as an open stochastic component, and infinite-horizon policy evaluation is obtained by closing a contractive feedback loop. The resulting semantics assigns typed Bellman transformers to open components, inter-prets series and parallel wiring as composition and tensoring of transformers, and interprets feedback as an admissible guarded Banach trace realized by a unique fixed point. This perspective yields three theoretical consequences. First, approxi-mate component equivalence is a contextual congruence for admitted well-typed guarded one-hole contexts: local operator error remains controlled after plugging the component into a surrounding circuit that uses the hole once and whose feed-back nodes have certified uniform guardedness. Second, exact and approximate state abstractions become commuting or near-commuting coalgebraic diagrams, giving value-preservation and explicit sup-norm distortion bounds. Third, under monotone ω-continuous contract-transformer semantics, safety, risk, and resource specifications can be represented as quantale-valued contracts, where local induc-tive bounds lift through wiring and feedback by least-fixed-point reasoning. Its central claim is not that all RL morphisms form a global traced monoidal category, but that discounted Bellman evaluation admits a contractive feedback semantics on the admissible class of guarded circuits. 

# 1 Introduction 

Reinforcement learning (RL) is commonly formalized as dynamic programming on a closed Markov decision process: fix a policy, write down a Bellman operator, and study the fixed point of that operator [Bellman, 1966, Puterman, 2014, Bertsekas and Tsitsiklis, 1995, Sutton et al., 1998]. This view is effective, but it hides a structural fact. The one-step transition is not intrinsically closed. It is an open component with an input interface, an output interface, a reward channel, and a continuation. The infinite-horizon value appears only after the continuation is fed back into the component. In this paper we make this feedback operation explicit. The proposed viewpoint is simple but deliberately restricted: Bellman recursion is a guarded fixed-point trace . A policy closes the action interface of a one-step stochastic component; discounting makes the continuation map contractive; and policy evaluation is the Banach fixed point obtained by closing the remaining value wire. This converts a familiar analytic fact into a compositional semantics. Series wiring becomes composition of continuation transformers, parallel wiring becomes tensor/product semantics, and feedback becomes a guarded fixed-point trace in the sense of traced monoidal reasoning [Joyal et al., 1996, Hasegawa, 1997, Selinger, 2010] and iteration/fixed-point semantics [Bloom and Ésik, 1993], specialized here to contractive maps. We use “trace” in this guarded sense throughout: only maps whose feedback wire is uniformly contractive are traceable, and all trace laws are invoked only inside this admissible class. 

Preprint. 

> arXiv:2605.24759v1 [cs.LG] 23 May 2026

This shift matters because modern decision systems are not monolithic MDPs. They are assembled from modules: perception or representation maps, local controllers, reward shapers, safety monitors, belief-state filters, and off-policy evaluation components. Belief-state filtering is the standard semantic reduction for partially observed control [Smallwood and Sondik, 1973, Kaelbling et al., 1998], while off-policy evaluation is naturally phrased as a change of measure between trajectory laws [Precup et al., 2000, Jiang and Li, 2016, Thomas and Brunskill, 2016]. A theory that only describes the final closed MDP has limited language for asking whether a local replacement is valid inside an arbitrary surrounding system. The semantic problem is therefore contextual: if two open components are close at their interface, when are they close after arbitrary composition and feedback? We answer this by treating open decision components as typed semantic objects. Each component induces a Bellman transformer from output continuations to input values. The transformer type records the direction in which value information flows. Once types are explicit, composition is no longer an informal engineering operation: it is ordinary function composition, with discount factors recording the depth at which each local reward or perturbation enters the feedback loop. This yields quantitative contextual equivalence bounds for the admitted class of well-typed linear one-hole contexts: the hole is used once, side components are fixed, tensoring does not copy the hole, and every feedback node satisfies a uniform guardedness certificate. The same semantics also clarifies abstraction. A deterministic representation map is a coalgebraic morphism when it commutes with the one-step transition and preserves rewards [Rutten, 2000, Jacobs, 2017]. In RL and planning, closely related abstraction, homomorphism, and bisimulation principles have been used to characterize when reduced models preserve decision-relevant behavior [Givan et al., 2003, Ravindran and Barto, 2004, Li et al., 2006, Ferns et al., 2004, 2011]. Exact commutation gives value preservation by an operator-intertwining proof. Approximate commutation gives an explicit sup-norm distortion bound controlled by reward mismatch and total-variation transition mismatch. Because the error is stated at the transformer level, it propagates through admitted larger decision circuits by the contextual congruence theorem. Finally, the framework separates metric fixed-point reasoning from order-theoretic contract reasoning. Discounted real-valued returns use Banach contraction. Safety, risk, and resource constraints are often better expressed as ordered guarantees. We therefore add a quantale-valued contract layer [Lawvere, 1973, Eklund et al., 2018, Yetter, 1990]. In this layer, a circuit is admitted when its open semantics is a monotone ω-continuous contract transformer and its wiring obeys the compilation laws stated later. Under these assumptions, closed loops are interpreted as least fixed points, and local pre-fixed-point certificates lift compositionally through wiring and feedback. This layer connects the paper to classical lattice-theoretic and fixed-point semantics [Tarski, 1955, Kleene, 1952, Studer, 2008, Cousot and Cousot, 1977]. The contribution is not a new learning algorithm and not an empirical claim. It is a theoretical reframing of RL semantics around contractive feedback. The main results are as follows. • Guarded traced Bellman semantics. We formulate open discounted decision components and prove that policy evaluation is the guarded Banach trace of an admissible feedback circuit; no global trace operation on arbitrary maps is assumed. • Typed compositionality. We prove that series and parallel wiring correspond to composition and tensoring of Bellman transformers, with explicit discount bookkeeping. • Contextual equivalence. We define an operator metric on invariant value balls and prove that approximate equivalence is preserved by admitted well-typed linear contexts generated by composition, tensoring, and guarded trace. • Coalgebraic abstraction. We prove exact value preservation under commuting homomor-phisms and quantitative value distortion under approximate commutation. • Quantale contracts. We prove that inductive safety/risk/resource contracts lift through se-ries, parallel, and feedback wiring whenever the contract-transformer semantics is monotone, 

ω-continuous, and compatible with the stated wiring laws. • Extensions and sanity checks. We show how the same interfaces cover belief-state POMDP lifting, decomposable change of measure under explicit factorization assumptions, time-varying operator tracking, and minimal modular robustness examples. 2The paper is organized as follows. Section 2 gives the semantic principles. Section 3 fixes the categorical, metric, and order-theoretic interfaces. Section 4 proves Bellman-as-guarded-trace. Sec-tion 5 proves compositionality and contextual congruence. Section 6 develops exact and approximate abstraction. Section 7 gives the quantale contract layer. Sections 8–10 record extensions and explicit robustness examples. All proofs are collected in Appendix A. 

# 2 Background and Motivation 

The paper is based on three semantic principles. 

Feedback principle. Infinite-horizon value is not merely a solution of an equation; it is the result of feeding a continuation output back into a one-step component. Discounting makes this feedback guarded, hence contractive, so the closed-loop value is uniquely defined by Banach’s theorem [Banach, 1922]; in standard RL notation, this is exactly the contraction argument behind discounted Bellman evaluation [Puterman, 2014, Sutton et al., 1998]. 

Contextual principle. Components should be compared at their interfaces. If two components induce close typed transformers, then their difference should remain controlled after the component is inserted into a larger decision circuit. This is the decision-theoretic analogue of contextual equivalence in denotational semantics, but with explicit sup-norm sensitivity constants induced by discounted Bellman operators. 

Contract principle. Many correctness requirements are upper bounds rather than rewards. Such bounds should compose locally: a guarantee for a subcomponent should lift to a guarantee for a larger wired system. This is naturally expressed by monotone transformers and least fixed points on ordered value spaces [Tarski, 1955, Kleene, 1952, Studer, 2008, Cousot and Cousot, 1977]. The technical development uses Markov kernels as morphisms in the stochastic layer [Friˇ c and Papˇ co, 2010, Panangaden, 2009, Fritz, 2020], standard Borel assumptions to avoid pathologies in conditional probability and disintegration [Kallenberg, 1997], Banach contraction for discounted real-valued evaluation, and quantale-enriched complete lattices for order-valued contracts [Lawvere, 1973, Eklund et al., 2018]. The point of combining these ingredients is not to replace standard MDP theory, but to expose the compositional structure that standard closed-MDP notation suppresses. 

# 3 Preliminaries 

This section fixes the mathematical interfaces used throughout the paper. Our denotational viewpoint has two layers: (i) the stochastic layer in the Markov category Stoch , where one-step dynam-ics/policies are composable morphisms; and (ii) the value layer , where a closed-loop induces a Bellman transformer acting on a Banach (or ordered) value space. We therefore (a) present dis-counted MDPs in a kernel-theoretic form so that “one-step dynamics” becomes a morphism in 

Stoch ; (b) introduce quantale-enriched value scalars Q to uniformly model additive returns and more general ordered guarantees (risk/resource/safety); and (c) recall traced symmetric monoidal categories, emphasizing the Banach trace as a concrete feedback semantics realized by a unique contractive fixed point. These choices are precisely what makes Bellman recursion an instance of categorical feedback in Section 4. 

Standing measurability assumptions. To keep the development measure-theoretic but lightweight, we assume all spaces ( S, A, R and their finite products) are standard Borel measurable spaces. This ensures the usual closure properties for Markov kernels and expectations used below. 

3.1 Discounted MDPs 

A discounted discrete-time MDP, following the classical Bellman–MDP dynamic-programming formulation [Bellman, 1966, Puterman, 2014, Bertsekas and Tsitsiklis, 1995, Sutton et al., 1998], is 

M = ( S, A, P, r, γ ) with γ ∈ (0 , 1) , where S and A are measurable spaces, P (· | s, a ) is a Markov kernel on S (equivalently P : S × A → ∆( S)), and r : S × A → R is a bounded measurable reward. A stationary policy is a Markov kernel π(· | s) on A (equivalently π : S → ∆( A)). Given s0 = s,3trajectories are generated by at ∼ π(· | st) and st+1 ∼ P (· | st, a t). The (discounted) value is 

V π (s) = Eπ

h P 

> t≥0

γtr(st, a t) s0 = s

i

.

Lemma 3.1 (Boundedness of discounted values) . If |r(s, a )| ≤ Rmax for all (s, a ), then for every stationary policy π, V π is bounded and satisfies ∥V π ∥∞ ≤ Vmax := Rmax  

> 1−γ

.

3.2 The Markov category Stoch 

Let Meas denote measurable spaces and measurable maps, and write ∆( Y ) for probability measures on Y . This kernel-based presentation follows the standard measure-theoretic treatment of Markov processes and its categorical formulation via stochastic/Markov categories [Friˇ c and Papˇ co, 2010, Panangaden, 2009, Fritz, 2020, Kallenberg, 1997]. 

Definition 3.2 (Markov kernel) . A Markov kernel k : X → ∆( Y ) assigns to each x ∈ X aprobability measure k(· | x) on Y such that x 7 → k(C | x) is measurable for every measurable 

C ⊆ Y .

Deterministic maps as kernels. Every measurable map f : X → Y induces a deterministic kernel 

Dirac( f ) : X → ∆( Y ) defined by Dirac( f )( · | x) := δf (x). We use this embedding implicitly whenever a deterministic wiring map appears inside Stoch compositions. 

Definition 3.3 (Stoch ). Stoch has measurable spaces as objects and Markov kernels as morphisms. Composition is Chapman–Kolmogorov: 

(ℓ ◦ k)( C | x) := 

Z

> Y

ℓ(C | y) k(dy | x).

The symmetric monoidal product ⊗ is the cartesian product of measurable spaces, with unit object 

I = {⋆}.

Closed-loop pairing kernel. Given a policy kernel π : S → ∆( A), the canonical kernel ⟨id S , π ⟩ :

S → ∆( S ⊗ A) is ⟨id S , π ⟩(d(s, a ) | s0) := δs0 (ds ) π(da | s0). Intuitively, it copies the current state to the first output wire and samples an action from π on the second wire. An MDP transition P (· | s, a ) is exactly a Stoch -morphism P : S ⊗ A → S, and a policy is a 

Stoch -morphism π : S → A. Hence one-step closed-loop dynamics becomes P π := P ◦ ⟨ id S , π ⟩ :

S → S in Stoch , which is the categorical backbone of our compositional wiring results later. 

3.3 Quantale-enriched value spaces 

Additive discounted return is only one instance of a value algebra. Safety, risk, and resource budgets are naturally phrased as ordered guarantees (e.g., “value is below a specification bound”). Quantales provide a minimal algebraic interface that supports (i) composition of guarantees via a monoidal product and (ii) joins capturing choice/nondeterminism [Lawvere, 1973, Eklund et al., 2018, Yetter, 1990]; Section 7 builds the contract layer on this interface. 

Definition 3.4 (Quantale) . A quantale is (Q, ≤, W, ⊗, e ) where (Q, ≤) is a complete lattice, (Q, ⊗, e )

is a monoid, and ⊗ distributes over arbitrary joins: 

q ⊗

 _ 

> i

qi



= _

> i

(q ⊗ qi),

 _ 

> i

qi



⊗ q = _

> i

(qi ⊗ q).

Definition 3.5 (Discount endomorphism) . A discount is a monotone map δ : Q → Q that preserves joins and is a monoid homomorphism: 

δ

 _ 

> i

qi



= _

> i

δ(qi), δ(q ⊗ q′) = δ(q) ⊗ δ(q′), δ(e) = e. 

For ordered contracts, our running quantitative example is 

Q = [0 , ∞], _ = sup , q ⊗ q′ = q + q′, e = 0 ,

4with the convention q + ∞ = ∞ and discount endomorphism δ(q) = γq for γ ∈ (0 , 1) , where 

δ(∞) = ∞. This gives a well-defined complete quantale for nonnegative risk, cost, violation, or resource bounds. Real-valued discounted return is handled separately. It is not modeled as the complete quantale 

(R, +, 0) , since +∞ + ( −∞ ) is not a well-defined monoid operation. When we analyze standard rewards and Banach fixed points (Section 4), we instead work on the Banach space of bounded real-valued functions 

B(S) := {V : S → R measurable and bounded }, ∥V ∥∞ := sup 

> s∈S

|V (s)|.

Lemma 3.1 ensures V π ∈ B (S) under bounded rewards. 

Contract value space. For a state space S and a fixed contract quantale Q, define the pointwise ordered function space 

V(S) := QS , (V ≤ W ) ⇐⇒ (∀s ∈ S, V (s) ≤ W (s)) .

The notation V(S) is used only for the order-valued contract layer; the Banach layer uses B(S).

Lemma 3.6 (Banachness of bounded measurable functions) .  B(S), ∥ · ∥ ∞

 is a Banach space. 

3.4 Traces and guarded fixed-point feedback Definition 3.7 (Guarded Banach trace) . Let X, Y, Z be complete metric spaces. A map f : X × Z →

Y × Z, f = ( fY , f Z ), is Z-guarded if its feedback component is uniformly contractive in the feedback variable: there exists c ∈ [0 , 1) such that dZ

 fZ (x, z ), f Z (x, z ′) ≤ c d Z (z, z ′) ∀x ∈

X, z, z ′ ∈ Z. For a Z-guarded map f and each x ∈ X, Banach’s fixed-point theorem gives a unique point zx ∈ Z satisfying zx = fZ (x, z x). The guarded Banach trace is the partial operation 

Tr ZX,Y (f )( x) := fY (x, z x). It is defined only for guarded maps. Thus Tr in this paper denotes a guarded fixed-point operation, not a total trace on all maps. 

Scope of the trace terminology. We use the word “trace” in the restricted fixed-point sense above. The Bellman transformers considered below are not claimed to form a global traced symmetric monoidal category. Whenever we use trace-style identities such as naturality, superposing, or vanishing, the identity is invoked only for expressions whose relevant feedback coordinates are guarded on the stated invariant balls. This is the only trace structure needed for discounted Bellman evaluation: the discount factor supplies the contraction, and Banach’s theorem supplies the unique closed-loop value. 

Interpretation. Tracing out Z closes a feedback loop on Z by solving a unique contractive fixed point. In Section 4, Z is a value-function space and the contraction is exactly the discount factor. Appendix A.1 records the fixed-point facts and the guarded trace laws used in the sequel. 

# 4 Open Decision Components and Traced Bellman Semantics 

Building on Section 3, we package a one-step decision process as an open stochastic component (a morphism in Stoch ) and show that the usual infinite-horizon value semantics is obtained by closing a feedback loop . Concretely, we (i) define open discounted decision components (ODDCs) as kernels producing next state and one-step reward; (ii) derive the induced Bellman backup operator on a Banach value space; and (iii) show that the Bellman fixed point is exactly the trace of a feedback circuit, realized as a contractive fixed point (Banach trace). This section focuses on the standard 

additive discounted return so the feedback semantics is fully explicit; general quantale contracts are developed later. The component lives in Stoch (kernel-level wiring). Fixing a policy turns the component into a 

Bellman transformer T acting on B(S). The feedback/trace argument is carried out in CcMet ctr 

on the value space: this is where contractive fixed points provide a canonical notion of “closing the loop.” 54.1 Open discounted decision components 

Throughout this section, S and A are measurable spaces (state/action), R is a measurable space of one-step reward signals, and K : S ⊗ A → S ⊗ R is a Markov kernel in Stoch that outputs a pair 

(s′, r ) given (s, a ). A policy is a kernel π : S → A in Stoch .

Definition 4.1 (Open discounted decision component (ODDC)) . An open discounted decision component is M = ( S, A, R, K, ρ, γ ) where K : ( S ⊗ A) → (S ⊗ R) is a kernel in Stoch ,

ρ : R → R is a measurable scalarization (e.g., ρ(r) = r when R = R), and γ ∈ (0 , 1) is the discount factor. 

Closing the loop with a policy. Given π : S → A, define the closed-loop one-step kernel 

Kπ := K ◦ ⟨ id S , π ⟩ : S → (S ⊗ R), so that (s′, r ) ∼ Kπ (· | s) corresponds to a ∼ π(· | s)

followed by (s′, r ) ∼ K(· | s, a ).

Value space. Let B(S) denote the Banach space of bounded measurable functions V : S → R

equipped with ∥ · ∥ ∞ (Lemma 3.6). 

Bellman backup (additive discounted return). Define TM,π : B(S) → B (S) by 

(TM,π V )( s) := 

Z

> S⊗R



ρ(r) + γV (s′)



Kπ (d(s′, r ) | s). (1) This is the standard expectation E[ρ(r) + γV (s′)] , written in kernel notation so that later wiring statements can be expressed purely through Stoch composition/tensoring. 

Lemma 4.2 (Well-definedness and boundedness) . Assume the scalarized one-step reward is uniformly bounded: |ρ(r)| ≤ Rmax for all r ∈ R. Then TM,π maps B(S) to itself and ∥T M,π V ∥∞ ≤

Rmax + γ∥V ∥∞. In particular, TM,π is a self-map on the closed ball {V : ∥V ∥∞ ≤ Vmax } with 

Vmax := Rmax  

> 1−γ

.

Lemma 4.3 (Monotonicity and affine Lipschitz structure) . Under Lemma 4.2, TM,π is monotone w.r.t. the pointwise order: if V ≤ W then TM,π V ≤ T M,π W . Moreover, for any constant c ∈ R (identified with the constant function s 7 → c), 

TM,π (V + c) = TM,π (V ) + γc, ∥T M,π V − T M,π W ∥∞ ≤ γ∥V − W ∥∞.

Proposition 4.4 (Trajectory semantics agrees with fixed-point semantics) . Let (st+1 , r t) ∼ Kπ (· | st)

and assume the scalarized reward satisfies |ρ(r)| ≤ Rmax for all r ∈ R. Define the closed-loop trajectory value V π (s) := E

hP 

> t≥0

γtρ(rt) s0 = s

i

. Then V π ∈ B (S) and is the unique fixed point of TM,π on B(S).

4.2 Why Bellman equals trace 

Equation (1) describes a one-step continuation update: given a continuation V for the next state, 

TM,π V is the resulting current-state value. Infinite-horizon evaluation closes the loop by demanding consistency of the continuation with its own update: V = TM,π V . A traced symmetric monoidal category axiomatizes exactly this “wire the output back to the input” operation. When the feedback variable is contractive (here via γ ∈ (0 , 1) under ∥ · ∥ ∞), the trace is realized concretely by a unique fixed point (Banach trace). 

Lemma 4.5 (Contraction of the Bellman backup) . Under the assumptions of Lemma 4.2, for all 

V, W ∈ B (S),

∥T M,π V − T M,π W ∥∞ ≤ γ ∥V − W ∥∞.

Hence TM,π is a γ-contraction on  B(S), ∥ · ∥ ∞

.

Theorem 4.6 (Value iteration converges for policy evaluation) . Under Lemma 4.2, TM,π has a unique fixed point V π . Moreover, for any initial V0 ∈ B (S), the iterates Vk+1 := TM,π Vk satisfy 

∥Vk − V π ∥∞ ≤ γk∥V0 − V π ∥∞,

so the convergence is geometric with rate γ.

6M1 M2       

> S⊗AS⊗(R1⊗R2)
> (a) Series: wiring by composition

M1 M2          

> S1⊗A1S1⊗R1
> S2⊗A2S2⊗R2
> (b) Parallel: wiring by tensor product

Figure 1: Series and parallel wiring. Series rewards are scalarized at the value layer. 

Theorem 4.7 (Bellman recursion as a guarded Banach trace) . Assume the conditions of Lemma 4.2. Define the feedback map 

fπ : I × B (S) −→ B (S) × B (S), fπ (⋆, V ) :=  TM,π V, TM,π V .

The feedback component of fπ is γ-contractive by Lemma 4.5, so fπ is admissible for the guarded Banach trace of Definition 3.7. Its trace is well-defined and satisfies Tr B(S)

> I, B(S)

(fπ )( ⋆) = V π , where 

V π is the unique Bellman fixed point (equivalently, the discounted return in Proposition 4.4). 

This theorem is a representation theorem for standard discounted policy evaluation. It does not assert that arbitrary stochastic components or arbitrary Bellman transformers carry a total trace. It says that the usual Bellman fixed point is exactly the result of closing the continuation wire of the one-step Bellman transformer, provided the feedback loop is guarded by discounting. By Definition 3.7, Tr B(S)

> I, B(S)

(fπ )( ⋆) solves the unique fixed point of the feedback map V 7 →

πZ fπ (⋆, V ) = TM,π V , and outputs the corresponding Y -component, which equals TM,π V . Thus the traced value is exactly the Bellman fixed point. 

# 5 Compositionality and Contextual Equivalence 

Section 4 established the value-layer semantics of an open one-step component: after fixing a policy, a kernel induces a Bellman transformer T : B(out-state ) → B (in-state ), and closing feedback corresponds to a trace. This section proves the next pillar: compositionality . We first show that wiring open components in string diagrams (series/parallel) is reflected exactly by structural operations on their induced transformers, with the correct discount bookkeeping. We then formalize contextual equivalence : if two open components are close (in an operator metric), they remain close after being plugged into any admissible well-formed circuit context generated by ◦, ⊗, Tr . Crucially, we make the stability constants explicitly computable from the context structure, rather than existential. 

5.1 String-diagram wiring: series and parallel 

In Stoch , the monoidal product is cartesian product. Thus “parallel” wiring is modeled by a prod-uct kernel, while “series” wiring is modeled by kernel composition through an interface object. Throughout this subsection, series means temporal serial wiring: the first component advances one micro-step and the second component advances a subsequent micro-step. Consequently, rewards or errors generated by the second component appear one discount factor deeper. If two engineering modules are merely internal subroutines of the same environment step, they should first be fused into a single primitive one-step transformer rather than assigned an additional factor of γ by the series rule. When composing rewards, the stochastic layer naturally aggregates one-step signals as a pair 

(r1, r 2) ∈ R1 ⊗ R2 ∼= R1 × R2, which is then mapped to a scalar at the value layer (e.g., by an additive scalarization ρ⊗(r1, r 2) = ρ1(r1) + ρ2(r2)). 

5.2 Bellman transformers with explicit types 

A component with kernel K : X ⊗ A → Y ⊗ R, scalarization ρ : R → R, and discount γ ∈ (0 , 1) 

induces (after fixing a policy πX : X → A) a closed-loop kernel KπX := K ◦ ⟨ id X , π X ⟩ : X →

Y ⊗ R. The induced Bellman transformer is the typed map 

TK,π X : B(Y ) → B (X), (TK,π X V )( x) := 

Z 

> Y⊗R

 ρ(r) + γV (y) KπX (d(y, r ) | x).

The key point is the continuation direction : V lives on the output state Y and is pulled back to X.This is exactly why series wiring becomes ordinary function composition. 75.3 Compositional Bellman semantics 

Consider two temporal micro-step components connected through an interface U :

K1 : S ⊗ A1 → U ⊗ R1, K2 : U ⊗ A2 → S ⊗ R2,

with scalarizations ρ1 : R1 → R and ρ2 : R2 → R. Let πS : S → ∆( A1) and πU : U → ∆( A2) be policies for the two micro-steps. The two action interfaces are allowed to differ; if a model supplies a deterministic interface controller or a transported policy, it is included as part of πU .Define the micro-step transformers 

T1 := TK1,π S : B(U ) → B (S), T2 := TK2,π U : B(S) → B (U ).

Their series wiring yields a macro-step transformer on B(S): Tseries := T1 ◦ T 2 : B(S) → B (S).

Unfolding the definitions makes the discount bookkeeping explicit: the second-stage reward is discounted once more, and the continuation is discounted twice. 

Theorem 5.1 (Compositionality under series wiring) . Assume bounded scalar rewards |ρi(ri)| ≤ 

Rmax ,i and common discount γ ∈ (0 , 1) . For series wiring through U as above, for every V ∈ B (S)

and s ∈ S,

(Tseries V )( s) = E

h

ρ1(r1) + γ ρ 2(r2) + γ2V (s2) s0 = s

i

,

where (u1, r 1) ∼ KπS 

> 1

(· | s0) and (s2, r 2) ∼ KπU 

> 2

(· | u1). Equivalently, Tseries = TK1,π S ◦ T K2,π U .

Moreover, Tseries is a γ2-contraction on  B(S), ∥ · ∥ ∞

.

For parallel wiring, the stochastic layer forms the product kernel K⊗ := K1 ⊗ K2 in Stoch together with the product policy π⊗ := π1 ⊗ π2. The resulting Bellman operator acts on B(S1 ⊗ S2) and uses the additive scalarization ρ⊗(r1, r 2) := ρ1(r1) + ρ2(r2). To connect this to the values of the two subsystems, we use the standard separable embedding below. 

Definition 5.2 (Separable embedding) . For V1 ∈ B (S1) and V2 ∈ B (S2), define 

(V1 ⊕ V2)( s1, s 2) := V1(s1) + V2(s2), (s1, s 2) ∈ S1 ⊗ S2.

Theorem 5.3 (Additive factorization under parallel wiring) . Assume the two subsystems are inde-pendent in the sense that K⊗ = K1 ⊗ K2 and π⊗ = π1 ⊗ π2, and use the additive scalarization 

ρ⊗(r1, r 2) = ρ1(r1) + ρ2(r2) with a common discount γ ∈ (0 , 1) . Let T⊗ denote the Bellman operator induced by (K⊗, π ⊗) on B(S1 ⊗ S2), and let Ti be the Bellman operators induced by 

(Ki, π i) on B(Si).Then the separable subspace is invariant: 

T⊗(V1 ⊕ V2) = ( T1V1) ⊕ (T2V2) for all V1, V 2.

Consequently, if V πi is the unique fixed point of Ti for i ∈ { 1, 2}, then the unique fixed point of T⊗

satisfies V π⊗ (s1, s 2) = V π1 (s1) + V π2 (s2).

Theorems 5.1–5.3 state that our denotational semantics is structural : series wiring corresponds to ordinary composition of continuation transformers (with explicit discount depth), while parallel wiring preserves an additive/separable value structure under independence. This structurality is the foundation for contextual reasoning: once the semantic constructors are Lipschitz and every feedback node carries a guardedness certificate, approximate equivalences propagate through admitted linear circuit contexts. Here “linear” means that the hole is used exactly once; contexts that duplicate, merge, or nonlinearly inspect the hole require separate sensitivity bounds. 

5.4 Contextual equivalence as congruence 

To avoid the trivial sup V = ∞ issue, discrepancies are always measured on typed invariant balls. For a state space X and radius M > 0, write 

BM (X) := {V ∈ B (X) : ∥V ∥∞ ≤ M }.

For two typed transformers T, T ′ : BMY (Y ) → BMX (X) define 

dX←Y 

> ∞

(T, T ′) := sup    

> V∈BMY(Y)

∥T V − T ′V ∥∞,X . (2) When X = Y = S and MX = MY = M , this reduces to the closed-operator metric d(S,M ) 

> ∞

. All Bellman transformers below are assumed to come with such invariant balls; by Lemma 4.2, this is automatic after choosing a radius at least as large as the corresponding value bound. 8Lemma 5.4 (Fixed-point stability for contractions) . Let T, T ′ : BM (S) → BM (S) be κ-contractions in ∥ · ∥ ∞ for some κ ∈ (0 , 1) , with unique fixed points V ⋆ and V ⋆′. Then 

∥V ⋆ − V ⋆′∥∞ ≤ 11 − κ d(S,M ) 

> ∞

(T, T ′).

Definition 5.5 (Certified admitted linear guarded context) . Fix a hole type BMY (Y ) −→ BMX (X)

in the continuation direction. A linear one-hole context C[·] is a well-typed expression generated by the grammar 

C ::= [ ·] | T0 ◦ C | C ◦ T0 | C ⊗ T0 | T0 ⊗ C | Tr Z (D),

where T0 ranges over fixed side transformers and D is itself a one-hole pre-trace expression of product type X × Z → Y × Z on the relevant invariant balls. The word linear means syntactic single-use of the hole: no diagonal copying, merging of two copies, deletion followed by reintroduction, or nonlinear inspection of the hole is allowed. Tensor products use the product sup metric. If a later scalarization adds, maximizes, thresholds, or otherwise post-processes tensor outputs, that post-processing must be represented as an additional fixed Lipschitz transformer T0 in the grammar. A context is admitted if every subexpression carries a certificate consisting of: 1. source and target invariant balls; 2. a Lipschitz constant for every fixed side transformer; 3. for each trace node, uniform constants αZ < 1, η Z , β Z , a X satisfying the inequalities in Lemma 5.7; 4. a structurally computed perturbation gain L(C) < ∞;5. for a closed context, a contraction modulus κ(C) < 1 on the final invariant ball. The same certificate must be valid for both plugged components when two circuits are compared. 

Definition 5.6 (Context gain and context contraction modulus) . For a certified admitted context C[·],the context gain L(C) is the finite perturbation-amplification constant generated by the structural rules of Lemma 5.7. Thus, for all compatible components M, N with the same hole type, 

d(S,M )

> ∞

 TC[M], TC[N]

 ≤ L(C) dX←Y 

> ∞

(TM, TN).

We write κ(C) for a certified contraction modulus of the induced closed operator on its invariant ball. Thus L(C) measures perturbation amplification, whereas κ(C) measures closed-loop feedback sensitivity. 

Lemma 5.7 (Structural recursion for L(C) and κ(C)). Assume every primitive transformer in the context maps its typed invariant ball to the next typed invariant ball and has a known Lipschitz constant. For each trace subexpression, write the pre-trace feedback map as 

F : X × Z → Y × Z, F (x, z ) = ( FY (x, z ), F Z (x, z )) .

Assume that, on the relevant balls, 

dZ (FZ (x, z ), F Z (x, z ′)) ≤ αZ dZ (z, z ′), αZ < 1,dZ (FZ (x, z ), F Z (x′, z )) ≤ ηZ dX (x, x ′),dY (FY (x, z ), F Y (x, z ′)) ≤ βZ dZ (z, z ′),dY (FY (x, z ), F Y (x′, z )) ≤ aX dX (x, x ′).

Then the traced map Tr Z (F ) : X → Y is Lipschitz with Lip( Tr Z (F )) ≤ aX + βZ ηZ 

> 1−αZ

. A traced closed-loop subexpression is admitted only when the resulting external modulus is strictly smaller than one. The perturbation gain is certified by the following structural rules: 

• Hole. L([ ·]) = 1 .

• Left composition. L(T0 ◦ C ) ≤ Lip( T0)L(C).

9• Right composition. L(C◦ T0) ≤ L(C), provided T0 maps the source ball into the discrepancy ball on which the hole-side transformer is compared. 

• Tensor with a fixed side context. L(C ⊗ T0) ≤ L(C), L(T0 ⊗ C ) ≤ L(C), under the product sup metric, provided the side transformer T0 is identical in the two compared circuits and the tensor constructor does not copy the hole. 

• Guarded trace. Suppose the two pre-trace feedback maps have product-metric discrepancy at most δ and share the same certified constants αZ , β Z at that trace node. Then tracing amplifies the discrepancy by at most 1 + βZ 

> 1−αZ

. Consequently, if the pre-trace subcontext has gain L(D), then L(Tr Z (D)) ≤



1 + βZ

> 1−αZ



L(D).

The closed-context modulus κ(C) is obtained by the same structural certificate: composition multiplies Lipschitz constants, tensoring takes the maximum under the product sup metric, and each closed trace node is admitted only when its certified external modulus is strictly smaller than one. 

Theorem 5.8 (Contextual equivalence for certified admitted guarded contexts) . Let C[·] be a certified admitted linear guarded one-hole context in the sense of Definition 5.5. Let M and N be compatible components with the same typed hole interface, and assume that all side components and all certificates are identical in the two compared circuits. Assume also that the resulting closed-loop operators are self-maps of a common invariant ball and are contractions with certified modulus 

κ(C) < 1.If dX←Y 

> ∞

(TM, TN) ≤ ε, then the contextual fixed-point values satisfy ∥VC[M] − VC[N]∥∞ ≤ L(C)1−κ(C) ε. 

Theorem 5.8 is an error-propagation law for decision circuits: a local typed semantic mismatch 

ε is amplified first by the linear context gain L(C) and then by the closed-loop sensitivity factor 

1/(1 − κ(C)) . The theorem is deliberately typed: it never compares value functions living on different state spaces until an explicit adapter has transported them to a common interface. 

# 6 Coalgebraic Abstraction and Representation 

Sections 4–5 developed a typed, compositional Bellman semantics: open one-step components induce continuation transformers, and closing feedback yields a fixed-point value. This section connects that semantics to representation and abstraction . We proceed in three steps. First, we restate an MDP as a coalgebra (a one-step dynamics map into a structured functor), so that an abstraction becomes a (near-)coalgebra morphism in the sense of universal coalgebra [Rutten, 2000, Jacobs, 2017]. Second, we formalize deterministic state abstraction as a commuting diagram between concrete and abstract kernels; exact commutation implies value preservation via an operator-intertwining argument, while approximate commutation yields an explicit simulation-style value bound. Third, we expose a precise bridge to Section 5: the abstraction error appears as a typed transformer-level mismatch, and therefore composes through arbitrary circuit contexts by Theorem 5.8. 

6.1 MDP homomorphisms as commuting diagrams 

Fix a reward signal space R (e.g., R = R) and an action space A. A one-step controlled kernel 

K : S ⊗ A → S ⊗ R may be viewed, pointwise in the action, as a family 

ca : S −→ ∆( S ⊗ R), ca(s) := K(· | s, a ).

When A is finite, or when a suitable measurable function-space structure is fixed, this family can be packaged as a coalgebra c : S → (∆( S ⊗ R)) A for the functor F (X) = (∆( X ⊗ R)) A. To avoid relying on function-space measurability, the formal results below use the equivalent pointwise kernel-commutation conditions in a. Under this lens, an abstraction map ϕ : S → bS should make the one-step dynamics and rewards commute after pushing forward along ϕ; the remainder of this section makes this concrete and quantitative. For a measurable map ϕ : S → bS and a probability measure μ on S, the pushforward ϕ#μ is defined by ϕ#μ(B) := μ(ϕ−1(B)) for measurable B ⊆ bS. We use total variation distance 

dTV (μ, ν ) := sup  

> ∥f∥∞≤1

Eμ[f ] − Eν [f ] ,

10 so that for any bounded measurable f ,

Eμ[f ] − Eν [f ] ≤ ∥ f ∥∞ dTV (μ, ν ). (3) 

Definition 6.1 (Reward-preserving MDP homomorphism) . Let M = ( S, A, P, r, γ ) and cM =( bS, A, bP , br, γ ) share the same action space. A measurable surjection ϕ : S → bS is a (state) homomorphism if for all s ∈ S and a ∈ A,

br(ϕ(s), a ) = r(s, a ), ϕ#P (· | s, a ) = bP (· | ϕ(s), a ).

Equivalently, in Stoch the following kernel-level square commutes: 

S ⊗ A S

bS ⊗ A bS 

> P
> ϕ⊗id Aϕ
> bP

and rewards are constant on fibers of ϕ. 

Given an abstract policy bπ : bS → ∆( A), we define its lifted concrete policy by π(a | s) := bπ(a |

ϕ(s)) . Conceptually, bπ acts on a representation bs = ϕ(s) and is then pulled back to the concrete state. Define the pullback operator ϕ∗ : B( bS) → B (S) by ϕ∗ bV := bV ◦ ϕ. Then ∥ϕ∗ bV ∥∞ ≤ ∥ bV ∥∞, so comparing values via pullback never amplifies sup-norm errors. 

Lemma 6.2 (Bellman intertwining under a homomorphism) . Let ϕ be a homomorphism and let bπ

and π = bπ ◦ ϕ be as above. Let T π and bT bπ denote the policy-evaluation Bellman operators on B(S)

and B( bS) respectively. Then for all bounded bV ∈ B ( bS), T π (ϕ∗ bV ) = ϕ∗( bT bπ bV ).

The identity is an operator-level commuting diagram: pull back an abstract value bV to the concrete space and apply the concrete Bellman backup, or apply the abstract backup first and then pull back— the results coincide. This is exactly the categorical/coalgebraic idea that the abstraction commutes with one-step dynamics. 

Theorem 6.3 (Exact abstraction preserves policy values) . Assume ϕ is a homomorphism. For any abstract policy bπ : bS → ∆( A) and its lift π = bπ ◦ ϕ,

V π (s) = bV bπ (ϕ(s)) ∀s ∈ S, i.e., V π = ϕ∗ bV bπ .

By Lemma 6.2, the pullback ϕ∗ bV evolves under T π exactly as bV evolves under bT bπ . Since both are 

γ-contractions in ∥ · ∥ ∞ (Section 4), they admit unique fixed points, hence the fixed points correspond by pullback. To state optimal-value preservation without technical distractions, we record a mild assumption that ensures the optimality operator is well-defined on bounded measurable functions. 

Assumption 6.4 (Well-posed optimality backup) . Either (i) A is finite, or (ii) A is a standard Borel space and the MDP satisfies conditions under which the supremum in the optimality operator admits a measurable selection (so the Bellman optimality operator is well-defined on B(S)). 

Lemma 6.5 (Optimality intertwining) . Under Assumption 6.4, define the optimality operators 

(T ⋆V )( s) := sup 

> a∈A



r(s, a ) + γ

Z

V (s′) P (ds ′ | s, a )



,

( bT ⋆ bV )( bs) := sup 

> a∈A

br(bs, a ) + γ

ZbV (bs′) bP (dbs′ | bs, a )



.

If ϕ is a homomorphism, then for all bV ∈ B ( bS), T ⋆(ϕ∗ bV ) = ϕ∗( bT ⋆ bV ).

Corollary 6.6 (Exact abstraction preserves optimal values) . Under Assumption 6.4 and the homo-morphism conditions of Definition 6.1, 

V ⋆ = ϕ∗ bV ⋆, i.e., V ⋆(s) = bV ⋆(ϕ(s)) ∀s ∈ S. 

11 6.2 Approximate commutation and value distortion 

Exact commutation is often too strict for learned representations: ϕ may be trained from data, and the abstract kernel ( bP , br) may be fit only approximately. This mirrors the motivation behind approximate MDP homomorphisms, bisimulation metrics, and state-abstraction bounds [Givan et al., 2003, Ravindran and Barto, 2004, Li et al., 2006, Ferns et al., 2004, 2011]. We therefore quantify abstraction quality by a uniform reward mismatch and a uniform pushforward transition mismatch in total variation. 

Theorem 6.7 (Approximate abstraction yields an explicit value bound) . Assume there exist br, bP such that 

sup 

> s∈S, a ∈A

|r(s, a ) − br(ϕ(s), a )| ≤ εr , sup 

> s∈S, a ∈A

dTV 

 ϕ#P (· | s, a ), bP (· | ϕ(s), a ) ≤ εP .

Assume also a common reward bound |r(s, a )| ≤ Rmax and |br(bs, a )| ≤ Rmax , and set Vmax := 

Rmax /(1 − γ). Then for any abstract policy bπ and its lift π = bπ ◦ ϕ,

∥V π − ϕ∗ bV bπ ∥∞ ≤ εr + γV max εP

1 − γ .

There are two error sources, and both propagate geometrically through discounting. The immediate reward mismatch contributes at most εr per step, hence εr /(1 − γ) overall. The transition mismatch only affects the next-value expectation : by (3) with f = bV bπ and ∥ bV bπ ∥∞ ≤ Vmax , the one-step expectation error is at most γV max εP ; summing over time yields the second term. 

Corollary 6.8 (Approximate commutation implies an intertwining defect bound) . Under the as-sumptions of Theorem 6.7, let T := T π on B(S) and bT := bT bπ on B( bS). Then on the ball 

BVmax ( bS) := { bV : ∥ bV ∥∞ ≤ Vmax },

sup    

> bV∈BVmax (bS)

T (ϕ∗ bV ) − ϕ∗( bT bV ) ∞ ≤ εr + γV max εP .

Corollary 6.8 is a typed transformer mismatch . To avoid mixing concrete and abstract interfaces, define two adapter-level transformers with the same type 

BVmax ( bS) −→ BVmax (S) : Aϕ(T )( bV ) := T (ϕ∗ bV ), bAϕ( bT )( bV ) := ϕ∗( bT bV ).

Corollary 6.8 says precisely that 

d∞

 Aϕ(T ), bAϕ( bT ) ≤ εr + γV max εP

on this common adapter type. Therefore abstraction can be inserted into a larger circuit only after these adapters have made the concrete and abstract blocks type-compatible. 

Proposition 6.9 (Typed abstraction error propagates through circuit contexts) . Fix a well-typed one-hole context C[·] generated from ◦, ⊗, Tr whose hole has the common adapter type BVmax ( bS) →

BVmax (S) and whose induced closed-loop operator is contractive. Let ε := εr + γV max εP denote the local adapter-level defect from Corollary 6.8. Then the contextual fixed-point values produced by plugging the adapted concrete block Aϕ(T ) and the adapted abstract block bAϕ( bT ) into C[·] lie in the same external value space and satisfy 

VC[Aϕ(T )] − VC[ bAϕ( bT )] ∞ ≤ L(C)1 − κ(C) ε, 

with L(C) and κ(C) as in Definition 5.6 and Lemma 5.7. In particular, the proposition compares only values that have first been transported to a common typed interface; no norm is taken directly between a function on S and a function on bS.

6.3 Remark: symmetry as (approximate) commuting diagrams 

Remark 6.10 . Approximate symmetries (including learned linear or nonlinear transforms) fit the same near-commutation template. To incorporate action relabelings, consider a pair (ϕ, η ) with ϕ : S → S

and η : A → A measurable. The symmetry condition is the exact commutation 

r(ϕ(s), η (a)) = r(s, a ), ϕ#P (· | s, a ) = P (· | ϕ(s), η (a)) ,

12 and approximate symmetries replace these equalities by uniform sup-norm and TV errors. This is precisely an (approximate) homomorphism of M to itself with action relabeling, so Theorem 6.7 provides an immediate value distortion bound, which then composes further through any circuit context via Proposition 6.9. 

# 7 Quantale Contracts and Correct-by-Construction Safety 

Sections 4–6 developed the additive discounted semantics via Banach (metric) fixed points. Many safety/resource/risk requirements, however, are most naturally phrased as order constraints: “the induced closed-loop guarantee is below a specification bound”. This section introduces an order-theoretic contract layer over a quantale-valued value space. The development has three steps. (i) We define quantale-valued value functions and (open) contract transformers in continuation-passing style, matching the typed Bellman transformers of Section 5. (ii) We recall least fixed points on complete lattices and show that inductive contracts (pre-fixed points) soundly bound the closed-loop semantics. (iii) We state explicit wiring laws for transformers under ◦, ⊗, Tr and derive a correct-by-construction rule: local inductive bounds lift compositionally to global guarantees. 

7.1 Why safety is contract lifting 

Safety/resource/risk constraints are upper bounds in an ordered algebra of guarantees. Quantales provide a minimal compositional structure [Lawvere, 1973, Eklund et al., 2018, Yetter, 1990]: ⊗Q

composes step-wise guarantees, and joins W capture choice/nondeterminism (e.g., demonic worst-case or specification disjunction). If the closed-loop semantics is given by a monotone operator admitting a least fixed point, then a local inductive inequality T (C) ≤ C certifies a global bound 

lfp( T ) ≤ C, following the same lattice-theoretic logic behind fixed-point semantics and abstract interpretation [Tarski, 1955, Kleene, 1952, Studer, 2008, Cousot and Cousot, 1977]. This is the precise sense in which safety is contract lifting .

Notation. We use ⊗ for the monoidal product of system interfaces (cartesian product in Stoch ), and 

⊗Q for the monoidal product in the quantale (applied pointwise on value functions when needed). 

Definition 7.1 (Quantale-valued value space) . Fix a quantale (Q, ≤, W, ⊗Q, e ) and a state space S.Define the value space V(S) := QS with pointwise order. Let ⊥ ∈ V (S) denote the pointwise least element. 

Definition 7.2 (Closed-loop contract) . A (closed-loop) contract is a bound C ∈ V (S). A closed loop 

(M, π ) satisfies C if its semantic value exists and V π ≤ C pointwise on S. 

For compositional reasoning we work with open components and their continuation transformers. Intuitively, an open transformer takes a desired post-condition on the output state and returns a sufficient pre-condition on the input state. 

Definition 7.3 (Contract transformer) . An open component with input state space X and output state space Y is assigned a monotone map T : V(Y ) → V (X), called its contract transformer . Given a post-contract CY ∈ V (Y ), the induced pre-contract is CX := T (CY ).

Interpretation (why this matches Bellman transformers). This contract layer is the order-valued analogue of the Banach Bellman layer developed earlier. For example, when Q = [0 , ∞] with 

⊗Q = + , a value V ∈ V (S) can represent a nonnegative accumulated badness, risk, resource usage, or violation budget. A contract transformer T : V(Y ) → V (X) maps a post-bound on the output interface to a sufficient pre-bound on the input interface. Thus the inequality T (CY ) ≤ CX is a one-step proof obligation: assuming the continuation satisfies CY , the current component satisfies the stronger or equal bound CX .Standard signed discounted reward is not treated as a quantale-valued contract in this section; it is handled by the Banach fixed-point semantics of Sections 4–5. 

7.2 Least fixed points and inductive contracts 

A closed loop induces a monotone endomorphism on V(S): TM,π : V(S) → V (S), and its meaning is defined as a least fixed point whenever that exists: V π := lfp( TM,π ). This order-theoretic view is 13 particularly natural for “badness” measures (e.g., risk, constraint-violation probability, accumulated resource usage), where smaller is safer. 

Definition 7.4 (ω-continuity) . A monotone map T : V(S) → V (S) is ω-continuous if for every increasing chain V0 ≤ V1 ≤ · · · , T

 W 

> n≥0

Vn



= W 

> n≥0

T (Vn).

Lemma 7.5 (Least fixed point via Kleene iteration) . If T : V(S) → V (S) is monotone and ω-continuous, then T admits a least fixed point lfp( T ) = W 

> n≥0

T n(⊥).

Lemma 7.6 (Inductive contract check: pre-fixed points bound the least fixed point) . Let T : V(S) →V(S) be monotone and admit a least fixed point lfp( T ). If C ∈ V (S) satisfies T (C) ≤ C, then 

lfp( T ) ≤ C.

Think of C as a candidate invariant bound. The inequality T (C) ≤ C states: assuming the future behavior respects bound C, one step of closed-loop evolution produces a bound no larger than C

again. Lemma 7.6 then formalizes correct-by-construction safety: the least fixed point (the strongest semantics consistent with the dynamics) is automatically below C.To mirror the Banach trace from Section 4 in an order-theoretic setting, we use a least-fixed-point trace. 

Definition 7.7 (Least-fixed-point trace) . Let CPO ω, ⊥ be the category of pointed ω-complete partial orders and ω-continuous maps, with cartesian product as the monoidal product. For a morphism f : X × Z → Y × Z in this category, write f = ( fY , f Z ) and, for each x ∈ X,define gx : Z → Z, gx(z) := fZ (x, z ). Because fZ is jointly ω-continuous, each gx is ω-continuous and the parameterized least-fixed-point map x 7 → zx := lfp( gx) is ω-continuous by the standard parameterized Kleene construction. Define Tr ZX,Y (f )( x) := fY (x, z x), zx := lfp( gx) = W 

> n≥0

gnx (⊥Z ). This is the least-fixed-point trace used in the contract layer; it is applied only to morphisms for which the pre-trace map is jointly ω-continuous. In the discounted additive case of Section 4, the Bellman operator is a γ-contraction on a Banach space, hence has a unique fixed point. Whenever it is also monotone on the pointwise order, that unique fixed point coincides with the least fixed point on the corresponding complete lattice (restricted to an invariant order interval), so Lemma 7.6 applies unchanged. 

7.3 Contract lifting under wiring 

The general theorem below is not meant to be vacuous: the following running instance satisfies the required laws for temporal series, separable parallel products, and least-fixed-point feedback. 

Proposition 7.8 (Concrete additive contract transformer) . Let Q = [0 , ∞] with ⊗Q = + , and let K(dy | x) be a Markov kernel from X to Y . For this concrete probabilistic instance, use the measurable contract space V+(S) := {C : S → [0 , ∞] | C is measurable }, ordered pointwise. For a nonnegative measurable one-step cost c : X → [0 , ∞] and discount γ ∈ (0 , 1) , define 

(TK,c C)( x) := c(x) + γ

Z

> Y

C(y) K(dy | x), C ∈ V +(Y ),

where the integral is the extended nonnegative integral. Then TK,c : V+(Y ) → V +(X) is monotone and ω-continuous. Moreover: 1. If K1 : X → ∆( Y ) and K2 : Y → ∆( Z) are wired as two temporal micro-steps with costs 

c1 and c2, then the macro transformer equals TK1,c 1 ◦ T K2,c 2 ; hence the second cost and continuation are one discount factor deeper. 2. For independent parallel components with product kernel K1 ⊗ K2, separable cost c1 ⊗Q c2

(pointwise addition), and separable measurable contracts C1 ⊗Q C2, the product transformer satisfies 

TK1⊗K2, c 1⊗Qc2 (C1 ⊗Q C2) = ( TK1,c 1 C1) ⊗Q (TK2,c 2 C2).

3. For a feedback circuit whose pre-trace order-valued transformer is a morphism in CPO ω, ⊥,closing the feedback wire is interpreted by the least-fixed-point trace of Definition 7.7. 

14 For the remainder of the section we state the contract-lifting theorem for any semantics satisfying the same compilation laws. This separates the concrete probabilistic instance above from the purely order-theoretic proof. 

Assumption 7.9 (Compositional transformer laws) . There is a semantics assignment J·K mapping any well-typed open circuit to a monotone, ω-continuous contract transformer between the relevant pointed ω-cpos. Moreover, whenever a feedback constructor is used, the corresponding pre-trace transformer is a morphism in CPO ω, ⊥; equivalently, its components are jointly ω-continuous in the post-contract and feedback-contract arguments. Finally, J·K respects wiring as follows (up to canonical reindexing of interfaces): 

JM2 ◦ M1K = JM1K ◦ JM2K,

JM1 ⊗ M2K = JM1K ⊗ JM2K,

JTr (F)K = Tr (JFK),

where the rightmost Tr is the least-fixed-point trace from Definition 7.7. It is the order-theoretic analogue of Section 5: typed open semantics is continuation passing, series wiring is function composition, parallel wiring is product/tensor, and closing feedback is trace. Once these laws hold, contract lifting follows by pure order reasoning. 

Theorem 7.10 (Contract lifting under composition and feedback) . Assume Assumption 7.9. Then inductive contracts are preserved by wiring in the following sense. 

• (Series / assume–guarantee). Let M1 : X → Y and M2 : Y → Z be open circuits with transformers T1 := JM1K : V(Y ) → V (X) and T2 := JM2K : V(Z) → V (Y ). Suppose there exist bounds CZ ∈ V (Z), CY ∈ V (Y ), and CX ∈ V (X) such that T2(CZ ) ≤

CY , T1(CY ) ≤ CX . Then the composed circuit satisfies the induced bound JM2 ◦

M1K(CZ ) = ( T1 ◦ T 2)( CZ ) ≤ CX . In particular, if X = Z = S and CX = CZ =: CS ,this yields an inductive closed-loop contract (T1 ◦ T 2)( CS ) ≤ CS .

• (Parallel / separable product contracts). Let Mi : Xi → Yi with transformers Ti : V(Yi) →V(Xi). For two contracts C1 ∈ V (Y1) and C2 ∈ V (Y2), write their separable product contract as 

(C1 ⊗Q C2)( y1, y 2) := C1(y1) ⊗Q C2(y2).

If Ti(CYi ) ≤ CXi for i = 1 , 2, then for the parallel circuit 

(T1 ⊗ T 2)( CY1 ⊗Q CY2 ) ≤ CX1 ⊗Q CX2 ,

pointwise on X1 ⊗X2. This statement is for separable product contracts; arbitrary contracts on Y1 ⊗ Y2 need not decompose in this form. 

• (Feedback / trace). Let F be an open circuit with a feedback wire of type Z, and write its transformer in components as 

F := JFK : V(Y ) × V (Z) −→ V (X) × V (Z), F (CY , C Z ) = ( FX (CY , C Z ), F Z (CY , C Z )) .

Fix a post-bound CY ∈ V (Y ). If there exist bounds CX ∈ V (X) and CZ ∈ V (Z) such that FZ (CY , C Z ) ≤ CZ , FX (CY , C Z ) ≤ CX , then the traced transformer Tr Z (F ) is well-defined by the least-fixed-point trace and satisfies Tr Z (F )( CY ) ≤ CX .

The series and parallel cases are immediate from monotonicity and the transformer wiring equalities in Assumption 7.9. For feedback, Definition 7.7 reduces closing a loop to a least fixed point of the 

Z-component; the inductive inequality ensures this fixed point is bounded by CZ via Lemma 7.6, and the Y -projection then yields the traced bound. Complete proofs are deferred to Appendix A.26. 

# 8 Extensions 

The previous sections establish a typed, compositional semantics for discounted decision components: open circuits denote continuation-style transformers (Sections 4–5), feedback closure is a fixed point (Section 4 and Section 7), and representation maps are commuting diagrams with quantitative mismatch bounds (Section 6). This section shows that the same semantic viewpoint extends to 15 three ubiquitous “real-world” wrinkles in a way that remains compatible with contextual congruence (Theorem 5.8): (i) partial observability becomes a change of the state object from S to the belief object ∆( S), so the same fixed-point semantics applies after lifting; (ii) off-policy evaluation becomes a change-of-measure on trajectory space, and under genuine circuit decompositions the Radon– Nikodym derivative admits a module-wise factorization (enabling modular control of importance-weight dispersion); (iii) nonstationarity becomes a time-indexed path of semantic operators, and fixed-point stability yields tracking bounds that compose with our operator-mismatch metrics. 

8.1 Partial observability as belief-state lifting 

A discounted POMDP is (S, A, O, P, G, r, γ, ν 0), following the standard partially observable control formulation [Smallwood and Sondik, 1973, Kaelbling et al., 1998]. Here S is the state space, A is the action space, O is the observation space, P (· | s, a ) is the state-transition kernel on S, G(· | s′, a ) is the observation kernel on O, r : S × A → R is bounded measurable, γ ∈ (0 , 1) , and ν0 ∈ ∆( S) is the initial belief. 

Assumption 8.1 (Belief-lifting regularity) . The spaces S, A, O are standard Borel. The transition and observation kernels are measurable Markov kernels, and regular conditional distributions are chosen so that there exists a measurable Bayes update τ : ∆( S) × A × O → ∆( S). For every prior belief b ∈ ∆( S) and action a ∈ A, τ (b, a, o ) is a version of the posterior law of s′ given the observation o under the joint kernel b(ds ) P (ds ′ | s, a ) G(do | s′, a ). On observations with zero predictive probability, τ is defined arbitrarily; this does not change any induced trajectory law. Let B := ∆( S) be the belief space equipped with its standard measurable structure. Given (b, a ) ∈

B × A, the predictive observation law is 

ℓ(E | b, a ) := 

Z

> S

Z

> S

G(E | s′, a ) P (ds ′ | s, a ) b(ds ), E ⊆ O measurable .

The posterior belief is b′ = τ (b, a, o ). This induces a belief transition kernel PB (· | b, a ) on B by pushing ℓ(· | b, a ) through τ :

PB (C | b, a ) := 

Z

> O

1{τ (b, a, o ) ∈ C} ℓ(do | b, a ), C ⊆ B measurable .

The one-step reward is lifted linearly: rB (b, a ) := R 

> S

r(s, a ) b(ds ), which remains bounded with 

∥rB ∥∞ ≤ ∥ r∥∞.We use the convention that the initial belief b0 = ν0 is specified before any initial observation is incorporated. After action at and observation ot+1 , the history is ht+1 = ( a0, o 1, . . . , a t, o t+1 ),

and the belief is bt+1 = P(st+1 ∈ · | ht+1 ). Equivalently, bt is the posterior belief available when choosing at. The induced belief policy may be randomized and time-dependent; the statement is equality in law, not pathwise equality for every realized history. 

Lemma 8.2 (History policies induce nonstationary belief policies) . Under Assumption 8.1, fix an initial belief ν0. For any randomized history-dependent POMDP policy π, there exists a possibly time-dependent Markov kernel πB,t : B → ∆( A) such that, for every t, (bt, a t) d

= (bBt , a Bt ),

where the right-hand side is generated by the belief process with transition PB and policy sequence 

(πB,t )t≥0. Consequently, the expected one-step rewards agree at every time t, and the discounted expected returns coincide. 

Theorem 8.3 (Belief-state lifting yields an equivalent fully observed process) . Under Assump-tion 8.1, define the belief MDP MB = ( B, A, P B , r B , γ ) as above. Then for any randomized history-dependent policy π in the original POMDP, there exists a possibly time-dependent ran-domized belief policy (πB,t )t≥0 such that the discounted return from the initial belief b0 = ν0

agrees: V π

> POMDP

(ν0) = V (πB,t ) 

> MB

(ν0). Conversely, any such belief policy is implementable by a history-dependent POMDP policy because bt is a measurable function of the observation-action history. Hence optimizing over history-dependent POMDP policies is equivalent to optimizing over randomized nonstationary belief policies: V ⋆

> POMDP

(ν0) = V ⋆ 

> MB,nonstat

(ν0). For stationary belief policies, the fixed-point semantics of Sections 4–5 applies directly with state object B.

Belief lifting changes the state object from S to B = ∆( S). Once lifted, the belief dynamics is Markov, so our fixed-point semantics and the wiring/compositional results apply without modification, with S replaced by B throughout. Proof details are deferred to Appendix A.27. 16 8.2 Off-policy evaluation as compositional change-of-measure 

Let μ(· | s) be a behavior policy and π(· | s) a target policy on the same MDP. Off-policy evaluation commonly controls the resulting trajectory-law change via likelihood ratios and related importance-sampling or doubly robust estimators [Precup et al., 2000, Jiang and Li, 2016, Thomas and Brunskill, 2016]. Let Pμ and Pπ denote the induced trajectory measures on τ = ( s0, a 0, r 0, s 1, a 1, r 1, . . . ), with the same initial state distribution. 

Lemma 8.4 (Finite-prefix trajectory likelihood ratio in an MDP) . Fix a horizon T < ∞ and let PTμ

and PTπ denote the induced laws of the trajectory prefix (s0, a 0, r 0, . . . , s T ) with the same initial state distribution. If π(· | s) ≪ μ(· | s) for all s, then PTπ ≪ PTμ and 

dPTπ

dPTμ

= 

> T−1

Y

> t=0

dπ (· | st)

dμ (· | st) (at).

For infinite-horizon trajectory laws, the same products define the finite-prefix likelihood-ratio mar-tingale; the infinite-horizon derivative is its almost-sure limit whenever the limit exists in the usual Radon–Nikodym sense. 

Lemma 8.4 always factorizes over time . Factorization over modules requires additional structural assumptions: the closed-loop trajectory measure must decompose according to the circuit wiring, and the policies must decompose in the matching way. We make these assumptions explicit below. 

Proposition 8.5 (Compositional change-of-measure for decomposable decision circuits) . Fix a finite horizon T < ∞ and consider a closed decision circuit built from series/parallel wiring of subcircuits (Section 5). Assume the behavior and target closed-loop prefix measures admit a genuine 

decomposition matching the wiring: 

• Parallel case. The global state decomposes as S = S1 × S2, the transition and reward streams factorize under μ and π into independent subsystems, and the policies factorize as product policies μ = μ1 ⊗ μ2 and π = π1 ⊗ π2.

• Series case. There exists an interface random variable (or interface state) Ut such that, conditional on Ut, the upstream/downstream subtrajectories satisfy the corresponding con-ditional independence implied by the series wiring, and the policies factorize conditionally along Ut (chain-rule form). Then the global finite-prefix Radon–Nikodym derivative equals the product of the corresponding local derivatives: 

dPTπ

dPTμ

= Y

> j∈modules

dPT, (j)

> π

dPT, (j)

> μ

,

where in the series case the product is interpreted via the chain rule for conditional RN derivatives along the interface. 

This proposition is a conditional factorization statement: the contribution is not that every circuit factorizes, but that once the closed-loop trajectory law genuinely respects the circuit interface, the Radon–Nikodym chain rule preserves that modular structure. Module-wise factorization turns global importance-weight dispersion into a sum of local dispersions in log-space when the module weights are independent (e.g., parallel independent subsystems). Without independence one still obtains upper bounds via standard inequalities, but the clean additivity becomes an inequality. 

Corollary 8.6 (Second-moment additivity under independent module weights) . Under the parallel-case assumptions in Proposition 8.5, if the finite-prefix module-wise importance weights are indepen-dent under PTμ , then 

log Eμ

h dPTπ

> dPTμ

2i

= X

> j

log Eμ

h dPT , (j)

> π
> dPT , (j)
> μ

2i

.

Lemma 8.4 is the universal MDP-level change-of-measure. Proposition 8.5 identifies additional structural conditions under which the change-of-measure respects circuit structure, enabling modular reasoning about OPE variance and failure modes. Proof sketches are provided in Appendix A.27. 17 8.3 Nonstationarity as a time-indexed operator family 

Let Rmax bound one-step rewards uniformly over time, and set Vmax = Rmax /(1 − γ). Let 

B := {V : ∥V ∥∞ ≤ Vmax }. For each time t, let Tt : B → B be a γ-contraction. Define the drift (operator mismatch) by ηt := sup V ∈B ∥T t+1 V − T tV ∥∞. This is exactly the d∞-metric from Section 5.4 applied to successive operators. 

Theorem 8.7 (Fixed-point tracking under time-varying contractions) . Let {T t}t≥0 be γ-contractions on B with fixed points V ⋆t . Then for every t, ∥V ⋆t+1 − V ⋆t ∥∞ ≤ ηt 

> 1−γ

. Moreover, the cumulative drift satisfies ∥V ⋆T − V ⋆ 

> 0

∥∞ ≤ P 

> t<T ηt
> 1−γ

.

Theorem 8.7 is a direct instance of Lemma 5.4 with κ = γ and d∞(Tt+1 , Tt) = ηt. Thus, any compositional operator-mismatch bound (including contextual congruence, Theorem 5.8) can be plugged in to control ηt.Often one does not compute V ⋆t exactly; instead, one applies a small number of backups per time step. The next corollary quantifies the tracking error of the simplest update Vt+1 := TtVt.

Corollary 8.8 (Iterate tracking under drifting contractions) . Let Vt+1 := TtVt and let V ⋆t be the fixed point of Tt. Then 

∥Vt+1 − V ⋆t+1 ∥∞ ≤ γ ∥Vt − V ⋆t ∥∞ + ηt

1 − γ .

Iterating yields 

∥VT − V ⋆T ∥∞ ≤ γT ∥V0 − V ⋆ 

> 0

∥∞ + X

> t<T

γT −1−t ηt

1 − γ .

Nonstationarity is captured by a time-indexed family of semantic operators. Theorem 8.7 controls how the ground-truth fixed point drifts, while Corollary 8.8 controls the additional error incurred by limited computation. Both statements are expressed in the same operator metric used by contextual congruence, so they compose seamlessly with our circuit-level mismatch bounds. 

# 9 Scope and Limitations 

The results in this paper are intentionally limited to settings where the semantic fixed points are well-defined. In the Banach layer we assume bounded rewards, standard-Borel measurable spaces, and a discount factor γ ∈ (0 , 1) . These assumptions make the Bellman backup a contraction on bounded value functions. Undiscounted, average-reward, unbounded-reward, or risk-sensitive objectives may require different value spaces or different fixed-point principles. The guarded trace used here is partial. A feedback loop is admitted only when the feedback coordinate is uniformly contractive, and contextual trace laws are used only under this admissibility condition. Thus the paper does not claim that all stochastic kernels, all open decision components, or all Bellman transformers form a total traced symmetric monoidal category. The paper is also not an algorithmic or empirical contribution. It does not propose a new RL optimizer, estimator, exploration method, or benchmark result. Its purpose is to give a typed compositional semantics for existing discounted Bellman evaluation and to derive error-propagation, abstraction, and contract-lifting theorems from that semantics. Finally, the extensions in Section 8 are interface checks rather than full replacements for specialized theories. Belief-state lifting recovers the standard fully observed belief process under the usual measurability assumptions. The off-policy factorization result requires genuine decomposition of the trajectory law and policy along the circuit interfaces; without those assumptions, importance weights need not factorize module-wise. 

# 10 A Minimal Example and Modular Robustness 

This section instantiates the abstract semantics on a minimal yet fully explicit testbed whose purpose is twofold. First, we give a sanity check that our parallel wiring semantics recovers the classical additive value decomposition for independent subsystems with additive rewards and product policies 18 (Section 10.1). Second, we provide a concrete two-module series circuit in which we can track how 

local model perturbations propagate to the global fixed-point value through the exact wiring calculus: (TV/reward mismatch) ⇒ (single-module operator mismatch) 

⇒ (macro mismatch under series wiring) ⇒ (fixed-point deviation) .

The resulting bound makes a depth-discount phenomenon explicit: perturbations deeper in the feedback loop are geometrically attenuated by additional factors of γ (Section 10.2). 

10.1 Parallel product MDP (sanity check) 

Let Mi = ( Si, A i, P i, r i, γ ) be two discounted MDPs on measurable spaces with bounded rewards 

|ri| ≤ R(i)max . Define the product MDP on S = S1 × S2 and A = A1 × A2 by the product transition kernel: for measurable rectangles C1 × C2,

P (C1 × C2 | (s1, s 2), (a1, a 2)) = P1(C1 | s1, a 1) P2(C2 | s2, a 2),

extended uniquely to the product σ-algebra, and by the additive reward 

r(( s1, s 2), (a1, a 2)) := r1(s1, a 1) + r2(s2, a 2).

For a product policy π = π1 ⊗ π2 (independent actions across subsystems), the closed-loop trajectory law factorizes across subsystems, and expectation linearity yields the value decomposition. 

Proposition 10.1 (Additive value factorization for independent parallel products) . Under the product dynamics and product policy above, 

V π (s1, s 2) = V π1 (s1) + V π2 (s2) ∀(s1, s 2) ∈ S1 × S2.

Equivalently, the fixed point of the parallel Bellman operator factorizes additively. 

The decomposition is structural: it requires both (i) product dynamics P = P1 ⊗ P2 and (ii) product policy π = π1 ⊗π2. If either coupling is present (shared noise, coupled rewards, or correlated actions), the continuation transformer no longer decomposes as a sum of two independent subtransformers, and cross-terms generally appear. 

10.2 Two-module series robustness (depth-discount) 

Consider two open kernels wired in series through an interface U (cf. Section 5.3): 

K1 : S ⊗ A1 → U ⊗ R1, K2 : U ⊗ A2 → S ⊗ R2,

with scalarizations ρi : Ri → R and a common discount γ ∈ (0 , 1) . Fix (possibly distinct) policies 

πS : S → ∆( A1) and πU : U → ∆( A2), and let T1 : B(U ) → B (S) and T2 : B(S) → B (U ) denote the induced micro-step Bellman transformers (Section 5.2). We write eTi for perturbed versions of these transformers under a perturbed model. Assume the scalarized rewards are uniformly bounded, |ρi(ri)| ≤ Rmax for all ri ∈ Ri, i =1, 2, so Vmax := Rmax /(1 − γ) bounds all policy-evaluation values in sup norm. We will work on the closed ball BS := {V ∈ B (S) : ∥V ∥∞ ≤ Vmax } and similarly BU .For each module i ∈ { 1, 2}, assume a uniform bound on the scalar one-step reward mismatch and the transition-kernel mismatch: 

sup 

> x,a

˜ri(x, a ) − ri(x, a ) ≤ ε(i) 

> r

, sup 

> x,a

dTV 

  eP (i)(· | x, a ), P (i)(· | x, a ) ≤ ε(i) 

> P

,

where x denotes the appropriate state ( x ∈ S for i = 1 , x ∈ U for i = 2 ). (When rewards are emitted as random signals Ri, ˜ri(x, a ) and ri(x, a ) are understood as the scalarized conditional expectations; this convention eliminates ambiguity in the mismatch statement.) 

Lemma 10.2 (TV controls Bellman expectation mismatch) . Let T and eT be Bellman transformers of the same type, with discount γ and bounded values. If the one-step scalar reward mismatch and transition TV mismatch are bounded by (εr , ε P ) as above, then on the ball B := {V : ∥V ∥∞ ≤

Vmax },

d∞(T, eT ) := sup  

> V∈B

∥T V − eT V ∥∞ ≤ εr + γV max εP =: ϵ. 

19 The two-micro-step series circuit induces a macro Bellman operator on B(S):

Tser := T1 ◦ T2 : BS → BS , eTser := eT1 ◦ eT2 : BS → BS .

Since each micro-step transformer is γ-Lipschitz in the continuation argument, Tser and eTser are 

γ2-contractions. 

Lemma 10.3 (Series mismatch propagation (depth attenuation)) . Let ϵi := d∞(Ti, eTi) be the local operator mismatch of module i, where the sup is taken over the corresponding balls BU or BS . Then 

d∞(Tser , eTser ) ≤ ϵ1 + γ ϵ 2.

Let V π and eV π be the unique fixed points of Tser and eTser , respectively. These fixed points coincide with the infinite-horizon micro-step value function, but are expressed here through the macro (two-step) operator to expose depth effects algebraically. 

Lemma 10.4 (Two-module robustness with depth discount) . Let ϵi denote the local mismatch bounds from Lemma 10.2: ϵi := ε(i) 

> r

+ γV max ε(i) 

> P

. Then the fixed-point values of the original and perturbed two-module series circuit satisfy ∥ eV π − V π ∥∞ ≤ ϵ1+γ ϵ 2 

> 1−γ2

.

Lemma 10.2 turns local model perturbations into local operator mismatch. Lemma 10.3 shows that, under series wiring, the mismatch of the deeper module (here module 2) is attenuated by an additional factor γ because its effect must pass through one extra discounted continuation. Finally, Lemma 10.4 applies contraction stability (Lemma 5.4 with κ = γ2) to convert macro operator mismatch into a fixed-point deviation bound. Full proofs are provided in Appendix A.34. 

# References 

Richard Bellman. Dynamic programming. science , 153(3731):34–37, 1966. Martin L Puterman. Markov decision processes: discrete stochastic dynamic programming . John Wiley & Sons, 2014. Dimitri P Bertsekas and John N Tsitsiklis. Neuro-dynamic programming: an overview. In Proceedings of 1995 34th IEEE conference on decision and control , volume 1, pages 560–564. IEEE, 1995. Richard S Sutton, Andrew G Barto, et al. Reinforcement learning: An introduction , volume 1. MIT press Cambridge, 1998. André Joyal, Ross Street, and Dominic Verity. Traced monoidal categories. In Mathematical proceedings of the cambridge philosophical society , volume 119, pages 447–468. Cambridge University Press, 1996. Masahito Hasegawa. Recursion from cyclic sharing: traced monoidal categories and models of cyclic lambda calculi. In International Conference on Typed Lambda Calculi and Applications , pages 196–213. Springer, 1997. Peter Selinger. A survey of graphical languages for monoidal categories. In New structures for physics , pages 289–355. Springer, 2010. Stephen L Bloom and Zoltán Ésik. Iteration theories. In Iteration Theories: The Equational Logic of Iterative Processes , pages 159–213. Springer, 1993. Richard D Smallwood and Edward J Sondik. The optimal control of partially observable markov processes over a finite horizon. Operations research , 21(5):1071–1088, 1973. Leslie Pack Kaelbling, Michael L Littman, and Anthony R Cassandra. Planning and acting in partially observable stochastic domains. Artificial intelligence , 101(1-2):99–134, 1998. Doina Precup, Richard S Sutton, and Satinder Singh. Eligibility traces for off-policy policy evaluation. 2000. Nan Jiang and Lihong Li. Doubly robust off-policy value evaluation for reinforcement learning. In International conference on machine learning , pages 652–661. PMLR, 2016. Philip Thomas and Emma Brunskill. Data-efficient off-policy policy evaluation for reinforcement learning. In 

International conference on machine learning , pages 2139–2148. PMLR, 2016. Jan JMM Rutten. Universal coalgebra: a theory of systems. Theoretical computer science , 249(1):3–80, 2000. 

20 Bart Jacobs. Introduction to coalgebra , volume 59. Cambridge University Press, 2017. Robert Givan, Thomas Dean, and Matthew Greig. Equivalence notions and model minimization in markov decision processes. Artificial intelligence , 147(1-2):163–223, 2003. Balaraman Ravindran and Andrew G Barto. Approximate homomorphisms: A framework for non-exact minimization in markov decision processes. 2004. Lihong Li, Thomas J Walsh, and Michael L Littman. Towards a unified theory of state abstraction for mdps. 

AI&M , 1(2):3, 2006. Norm Ferns, Prakash Panangaden, and Doina Precup. Metrics for finite markov decision processes. In Uai ,volume 4, pages 162–169, 2004. Norm Ferns, Prakash Panangaden, and Doina Precup. Bisimulation metrics for continuous markov decision processes. SIAM Journal on Computing , 40(6):1662–1714, 2011. F William Lawvere. Metric spaces, generalized logic, and closed categories. Rendiconti del seminario matématico e fisico di Milano , 43(1):135–166, 1973. Patrik Eklund, Javier Gutiérrez García, Ulrich Höhle, and Jari Kortelainen. Fundamentals of quantales. In 

Semigroups in Complete Lattices: Quantales, Modules and Related Topics , pages 45–202. Springer, 2018. David N Yetter. Quantales and (noncommutative) linear logic. The Journal of Symbolic Logic , 55(1):41–64, 1990. Alfred Tarski. A lattice-theoretical fixpoint theorem and its applications. 1955. Stephen Cole Kleene. Introduction to metamathematics. 1952. Thomas Studer. On the proof theory of the modal mu-calculus. Studia Logica , 89(3):343–363, 2008. Patrick Cousot and Radhia Cousot. Abstract interpretation: a unified lattice model for static analysis of programs by construction or approximation of fixpoints. In Proceedings of the 4th ACM SIGACT-SIGPLAN symposium on Principles of programming languages , pages 238–252, 1977. Stefan Banach. Sur les opérations dans les ensembles abstraits et leur application aux équations intégrales. 

Fundamenta mathematicae , 3(1):133–181, 1922. Roman Friˇ c and Martin Papˇ co. A categorical approach to probability theory. Studia Logica , 94(2):215–230, 2010. Prakash Panangaden. Labelled Markov Processes . World Scientific, 2009. Tobias Fritz. A synthetic approach to markov kernels, conditional independence and theorems on sufficient statistics. Advances in Mathematics , 370:107239, 2020. Olav Kallenberg. Foundations of modern probability . Springer, 1997. 

# A Proofs 

This appendix proves the claims in the order in which they appear in the main text. All spaces are understood to satisfy the standing standard-Borel/measurability assumptions from Section 3. We use the sup norm on bounded real-valued value spaces and the pointwise order on quantale-valued value spaces. 

A.1 Proof of the guarded Banach trace facts Lemma A.1 (Uniform fixed point for a guarded parameterized map) . Let (Z, d Z ) be complete, and let g : X × Z → Z satisfy 

dZ (g(x, z ), g (x, z ′)) ≤ cd Z (z, z ′) ∀x ∈ X, z, z ′ ∈ Z

for a constant c ∈ [0 , 1) . Then for every x ∈ X there is a unique zx ∈ Z such that zx = g(x, z x).

21 Proof. Fix x ∈ X and define gx : Z → Z by gx(z) = g(x, z ). The displayed inequality says exactly that gx is a contraction with modulus at most c. Since Z is complete, Banach’s fixed-point theorem gives existence of a fixed point zx. If z and z′ are both fixed points, then 

dZ (z, z ′) = dZ (gx(z), g x(z′)) ≤ cd Z (z, z ′).

Subtracting the right-hand side gives (1 − c)dZ (z, z ′) ≤ 0. Because 1 − c > 0 and distances are nonnegative, dZ (z, z ′) = 0 , hence z = z′. This proves uniqueness. 

Proposition A.2 (Guarded fixed-point identities used in the paper) . On the class of maps for which the displayed feedback operation is guarded, the operation in Definition 3.7 is natural under pre- and post-composition, compatible with products by side maps, satisfies vanishing for the monoidal unit, and satisfies yanking for the symmetry map. We do not claim a global traced-monoidal structure on all partial maps; any additional trace identity must be used only when both sides of the identity are guarded. Proof. Write a guarded map f : X × Z → Y × Z as f = ( fY , f Z ). For each x, the trace uses the unique solution zx = fZ (x, z x) and returns fY (x, z x).Naturality follows by direct substitution. Let u : X′ → X and v : Y → Y ′ be maps, and define 

f ′ = ( v × id Z ) ◦ f ◦ (u × id Z ).

The feedback component of f ′ at x′ is z 7 → fZ (u(x′), z ). Its unique fixed point is therefore zu(x′),the fixed point used by f at u(x′). Hence 

Tr Z (f ′)( x′) = v fY (u(x′), z u(x′)) = ( v ◦ Tr Z (f ) ◦ u)( x′).

Compatibility with products is also pointwise. If f : X × Z → Y × Z and h : X′ → Y ′ does not involve the feedback variable, then the feedback equation for f ×h is the same equation z = fZ (x, z );the traced output is (fY (x, z x), h (x′)) , which is (Tr Z f × h)( x, x ′).Vanishing for the unit follows because the monoidal unit is a singleton. If Z = I = {⋆}, the only possible feedback point is ⋆, so tracing simply removes the unit coordinate: 

Tr I (f )( x) = πY f (x, ⋆ ).

Yanking is the special case of the symmetry map σZ,Z : Z × Z → Z × Z, σ(z1, z 2) = ( z2, z 1).Tracing the second wire gives the equation z2 = z1, whose unique solution is z2 = z1. The output is therefore z1, so Tr Z (σ) = id Z .These are exactly the guarded fixed-point identities used in the main text. 

A.2 Proof of Lemma 3.1 

Proof. Fix a stationary policy π and an initial state s0 = s. Let 

Gn := 

> n

X

> t=0

γtr(st, a t)

be the n-step discounted return. Since |r(s, a )| ≤ Rmax and γ ∈ (0 , 1) ,

|Gn| ≤ 

> n

X

> t=0

γt|r(st, a t)| ≤ Rmax 

> n

X

> t=0

γt = Rmax 

1 − γn+1 

1 − γ ≤ Rmax 

1 − γ .

Also, for m > n ,

|Gm − Gn| ≤ Rmax 

> m

X

> t=n+1

γt ≤ Rmax 

γn+1 

1 − γ .

The right-hand side tends to zero, so (Gn) is Cauchy in L∞ and converges almost surely and in L∞

to 

G := X

> t≥0

γtr(st, a t).

The same uniform bound passes to the limit: |G| ≤ Rmax /(1 − γ) almost surely. Therefore 

|V π (s)| = |Eπ [G | s0 = s]| ≤ Eπ [|G| | s0 = s] ≤ Rmax 

1 − γ .

Taking the supremum over s gives ∥V π ∥∞ ≤ Rmax /(1 − γ) = Vmax .22 A.3 Proof of Lemma 3.6 

Proof. Let (Vn)n≥1 be a Cauchy sequence in (B(S), ∥ · ∥ ∞). For each fixed s ∈ S,

|Vn(s) − Vm(s)| ≤ ∥ Vn − Vm∥∞,

so (Vn(s)) n≥1 is Cauchy in R. Define V (s) := lim n→∞ Vn(s).We first show that V is bounded. Since (Vn) is Cauchy, choose N such that ∥Vn − VN ∥∞ ≤ 1 for all 

n ≥ N . Then for all s and all n ≥ N ,

|Vn(s)| ≤ | VN (s)| + 1 ≤ ∥ VN ∥∞ + 1 .

Taking n → ∞ gives |V (s)| ≤ ∥ VN ∥∞ + 1 , so V is bounded. We next show measurability. A pointwise limit of measurable real-valued functions is measurable: for example, 

{s : V (s) < a } = [

> q<a, q ∈Q
> ∞

[ 

> N=1

\

> n≥N

{s : Vn(s) < q },

which is measurable because it is built from countable unions and intersections of measurable sets. Hence V ∈ B (S).Finally, fix ε > 0 and choose N such that ∥Vn − Vm∥∞ < ε for all m, n ≥ N . For n ≥ N and every 

s,

|Vn(s) − V (s)| = lim  

> m→∞

|Vn(s) − Vm(s)| ≤ ε. 

Taking the supremum over s gives ∥Vn − V ∥∞ ≤ ε for all n ≥ N . Thus Vn → V in sup norm, so 

B(S) is complete. 

A.4 Auxiliary measurability and total-variation facts Lemma A.3 (Kernel integration) . Let k : X → ∆( Y ) be a Markov kernel. If f : Y → R is bounded and measurable, then 

x 7 →

Z

> Y

f (y)k(dy | x)

is measurable and bounded by ∥f ∥∞ in absolute value. Proof. For f = 1B , the integral is k(B | x), measurable by definition of a kernel. By linearity, the claim holds for nonnegative simple functions. For a nonnegative bounded measurable f , choose simple fn ↑ f ; the corresponding integrals are measurable and converge pointwise to the integral of 

f by monotone convergence. A pointwise limit of measurable functions is measurable. For general bounded f , write f = f + − f −. The bound follows from 

Z

f dk (x) ≤

Z

|f | dk (x) ≤ ∥ f ∥∞.

Lemma A.4 (Total-variation test-function bound) . With the convention 

dTV (μ, ν ) = sup  

> ∥f∥∞≤1

Z

f dμ −

Z

f dν ,

for every bounded measurable f ,

Z

f dμ −

Z

f dν ≤ ∥ f ∥∞dTV (μ, ν ).

Proof. If ∥f ∥∞ = 0 , the claim is immediate. Otherwise define g = f / ∥f ∥∞. Then ∥g∥∞ ≤ 1, so by the definition of dTV , Z

g dμ −

Z

g dν ≤ dTV (μ, ν ).

Multiplying by ∥f ∥∞ proves the result. 23 A.5 Proof of Lemma 4.2 

Proof. Fix V ∈ B (S) and define 

f (s′, r ) := ρ(r) + γV (s′).

The map f is measurable because ρ and V are measurable. Under the uniform boundedness hypothe-sis, |ρ(r)| ≤ Rmax for all r ∈ R. Hence, for every (s′, r ) ∈ S ⊗ R,

|f (s′, r )| ≤ Rmax + γ∥V ∥∞

Thus the integrand is globally bounded and measurable. The integral defining (TM,π V )( s) is therefore finite and absolutely bounded by the same constant. By Lemma A.3, the function s 7 → (TM,π V )( s) is measurable. Taking the supremum over s gives 

∥T M,π V ∥∞ ≤ Rmax + γ∥V ∥∞.

Thus TM,π maps B(S) to itself. If ∥V ∥∞ ≤ Vmax := Rmax /(1 − γ), then 

Rmax + γ∥V ∥∞ ≤ Rmax + γ Rmax 

1 − γ = Rmax 

1 − γ = Vmax ,

so the closed ball of radius Vmax is invariant. 

A.6 Proof of Lemma 4.3 

Proof. If V ≤ W pointwise, then ρ(r) + γV (s′) ≤ ρ(r) + γW (s′) for every (s′, r ) because γ > 0.Integration against the probability kernel Kπ (· | s) preserves order, so (TM,π V )( s) ≤ (TM,π W )( s)

for every s.For a constant c,

(TM,π (V + c))( s) = 

Z

[ρ(r) + γ(V (s′) + c)] Kπ (d(s′, r ) | s)=

Z

[ρ(r) + γV (s′)] Kπ (d(s′, r ) | s) + γc 

Z

1 Kπ (d(s′, r ) | s)= ( TM,π V )( s) + γc. 

Finally, 

|T M,π V (s) − T M,π W (s)| =

Z

γ(V (s′) − W (s′)) Kπ (d(s′, r ) | s)

≤ γ

Z

|V (s′) − W (s′)|Kπ (d(s′, r ) | s)

≤ γ∥V − W ∥∞.

Taking the supremum over s proves the Lipschitz bound. 

A.7 Proof of Proposition 4.4 

Proof. Let 

Gn := 

> n

X

> t=0

γtρ(rt)

be the finite-horizon scalarized return generated by the closed-loop kernel Kπ . By the uniform boundedness assumption, 

|Gn| ≤ 

> n

X

> t=0

γt|ρ(rt)| ≤ Rmax 

> n

X

> t=0

γt ≤ Rmax 

1 − γ .

Moreover, for m > n ,

|Gm − Gn| ≤ Rmax 

> m

X

> t=n+1

γt ≤ Rmax γn+1 

1 − γ .

24 Hence (Gn) is Cauchy in L∞ and converges almost surely and in L∞ to 

G := X

> t≥0

γtρ(rt),

with |G| ≤ Rmax /(1 − γ) almost surely. Therefore V π is well-defined and 

∥V π ∥∞ ≤ Rmax 

1 − γ .

Now split the infinite discounted return into its first reward and tail: 

G = ρ(r0) + γ X

> t≥0

γtρ(rt+1 ).

Conditioning on (s1, r 0) and using the Markov property under Kπ , the conditional expectation of the tail given s1 is V π (s1). Hence, for every initial state s,

V π (s) = E[ρ(r0) + γV π (s1) | s0 = s]=

Z

> S⊗R

[ρ(r) + γV π (s′)] Kπ (d(s′, r ) | s)= ( TM,π V π )( s).

Thus V π is a fixed point. By Lemma 4.5, the same operator is a γ-contraction in the sup norm. Since B(S) is complete by Lemma 3.6, Banach’s fixed-point theorem implies that the fixed point is unique. Hence the trajectory value and the fixed-point value agree. 

A.8 Proof of Lemma 4.5 

Proof. This is the Lipschitz part of Lemma 4.3. For completeness, for all s,

|T M,π V (s) − T M,π W (s)| ≤ γ∥V − W ∥∞.

Taking the supremum over s gives ∥T M,π V − T M,π W ∥∞ ≤ γ∥V − W ∥∞. Since 0 < γ < 1, this is a contraction. 

A.9 Proof of Theorem 4.6 

Proof. By Lemma 4.2, TM,π is a self-map of B(S), and by Lemma 4.5 it is a γ-contraction. Since 

B(S) is complete, Banach’s theorem gives a unique fixed point V π .For the convergence bound, use induction. Since V π = TM,π V π ,

∥Vk+1 − V π ∥∞ = ∥T M,π Vk − T M,π V π ∥∞ ≤ γ∥Vk − V π ∥∞.

Iterating this inequality gives 

∥Vk − V π ∥∞ ≤ γk∥V0 − V π ∥∞.

A.10 Proof of Theorem 4.7 

Proof. The feedback component of fπ is the map 

V 7 → πB(S)fπ (⋆, V ) = TM,π V. 

By Lemma 4.5, this map is γ-contractive on the complete metric space B(S). Hence fπ is guarded and Definition 3.7 applies. Let z⋆ be the unique fixed point of the feedback component. Then 

z⋆ = TM,π z⋆.

By Theorem 4.6, the unique fixed point of TM,π is V π , so z⋆ = V π . The traced output is 

Tr B(S)

> I, B(S)

(fπ )( ⋆) = πB(S)fπ (⋆, z ⋆) = TM,π z⋆ = z⋆ = V π .

This is exactly the Bellman fixed point, which equals the trajectory value by Proposition 4.4. 25 A.11 Proof of Theorem 5.1 

Proof. For V ∈ B (S),

(T2V )( u) = 

Z

> S⊗R2

[ρ2(r2) + γV (s2)] KπU 

> 2

(d(s2, r 2) | u).

Applying T1 gives 

(T1T2V )( s) = 

Z 

> U⊗R1

[ρ1(r1) + γ(T2V )( u1)] KπS 

> 1

(d(u1, r 1) | s)=

Z 

> U⊗R1

ρ1(r1)KπS 

> 1

(d(u1, r 1) | s)+ γ

Z 

> U⊗R1

Z

> S⊗R2

[ρ2(r2) + γV (s2)] KπU 

> 2

(d(s2, r 2) | u1)KπS 

> 1

(d(u1, r 1) | s).

This is precisely the expectation generated by first sampling (u1, r 1) from KπS 

> 1

(· | s) and then sampling (s2, r 2) from KπU 

> 2

(· | u1):

(Tseries V )( s) = E[ρ1(r1) + γρ 2(r2) + γ2V (s2) | s0 = s].

Thus Tseries = T1 ◦ T 2.For the contraction claim, use the one-step Lipschitz bounds: 

∥T 1T2V − T 1T2W ∥∞ ≤ γ∥T 2V − T 2W ∥∞ ≤ γ2∥V − W ∥∞.

A.12 Proof of Theorem 5.3 

Proof. For a separable continuation V1 ⊕ V2, the parallel Bellman backup is 

T⊗(V1 ⊕ V2)( s1, s 2)=

Z

[ρ1(r1) + ρ2(r2) + γV 1(s′

> 1

) + γV 2(s′

> 2

)]( Kπ1 

> 1

⊗ Kπ2 

> 2

)( d(s′

> 1

, r 1), d (s′

> 2

, r 2) | s1, s 2).

Because the kernel is the product kernel and the integrand is a sum of a function of subsystem 1 and a function of subsystem 2, the integral splits: 

T⊗(V1 ⊕ V2)( s1, s 2) = 

Z

[ρ1(r1) + γV 1(s′

> 1

)] Kπ1 

> 1

(d(s′

> 1

, r 1) | s1)+

Z

[ρ2(r2) + γV 2(s′

> 2

)] Kπ2 

> 2

(d(s′

> 2

, r 2) | s2)= ( T1V1)( s1) + ( T2V2)( s2)= (( T1V1) ⊕ (T2V2))( s1, s 2).

This proves invariance of the separable subspace. If V πi is the fixed point of Ti, then the identity above gives 

T⊗(V π1 ⊕ V π2 ) = V π1 ⊕ V π2 .

The parallel Bellman operator is a γ-contraction on B(S1 ⊗ S2), hence has a unique fixed point. Therefore the unique fixed point is V π1 ⊕ V π2 .

A.13 Proof of Lemma 5.4 

Proof. Since V ⋆ = T V ⋆ and V ⋆′ = T ′V ⋆′,

∥V ⋆ − V ⋆′∥∞ = ∥T V ⋆ − T ′V ⋆′∥∞

≤ ∥ T V ⋆ − T V ⋆′∥∞ + ∥T V ⋆′ − T ′V ⋆′∥∞

≤ κ∥V ⋆ − V ⋆′∥∞ + d(S,M ) 

> ∞

(T, T ′).

Moving the first term to the left gives 

(1 − κ)∥V ⋆ − V ⋆′∥∞ ≤ d(S,M ) 

> ∞

(T, T ′),

and division by 1 − κ > 0 proves the bound. 26 A.14 Proof of Lemma 5.7 

Proof. The proof is by structural induction on the certified linear one-hole context. For the hole context, plugging in M or N gives exactly the two primitive transformers. Therefore the gain is L([ ·]) = 1 .For left composition, compare T0 ◦ TM and T0 ◦ TN. If T0 has Lipschitz constant Lip( T0) on the relevant invariant ball, then 

d(T0TM, T 0TN) ≤ Lip( T0)d(TM, T N).

Thus the gain is multiplied by at most Lip( T0).For right composition, compare TM ◦ T0 and TN ◦ T0. Since T0 maps the source ball into the discrepancy ball on which the hole-side operators are compared, 

sup 

> V

∥TM(T0V ) − TN(T0V )∥∞ ≤ sup 

> U

∥TMU − TNU ∥∞.

Hence right composition by a fixed well-typed transformer does not increase the gain. For tensor side contexts, equip the product value space with the product sup metric. Tensoring by a fixed side transformer does not introduce a second occurrence of the hole. Therefore the discrepancy between the two plugged tensor expressions is exactly the discrepancy on the component containing the hole, possibly after canonical reindexing. Thus the gain is not increased. Any subsequent additive or nonlinear scalarization must be counted separately as a fixed Lipschitz post-composition, as required by Definition 5.5. It remains to justify the trace estimates. First consider a single guarded feedback map 

F : X × Z → Y × Z, F = ( FY , F Z ),

satisfying the Lipschitz assumptions in the statement. For each x, let zx be the unique solution of 

zx = FZ (x, z x).

For x, x ′ ∈ X,

dZ (zx, z x′ ) = dZ (FZ (x, z x), F Z (x′, z x′ )) 

≤ dZ (FZ (x, z x), F Z (x, z x′ )) + dZ (FZ (x, z x′ ), F Z (x′, z x′ )) 

≤ αZ dZ (zx, z x′ ) + ηZ dX (x, x ′).

Since αZ < 1,

dZ (zx, z x′ ) ≤ ηZ

1 − αZ

dX (x, x ′).

Using the output estimate, 

dY (Tr Z (F )( x), Tr Z (F )( x′)) = dY (FY (x, z x), F Y (x′, z x′ )) 

≤ dY (FY (x, z x), F Y (x, z x′ )) + dY (FY (x, z x′ ), F Y (x′, z x′ )) 

≤ βZ dZ (zx, z x′ ) + aX dX (x, x ′)

≤



aX + βZ ηZ

1 − αZ



dX (x, x ′).

This proves the displayed Lipschitz bound for the traced map. Now compare two pre-trace maps 

FM, F N : X × Z → Y × Z

induced after plugging M and N into the same certified trace context. Let 

δ := sup 

> x,z

dY ×Z

 FM(x, z ), F N(x, z ).

For a fixed x, let zM and zN be the corresponding feedback fixed points. By the shared uniform guardedness certificate, the Z-component of FN is αZ -contractive in the feedback variable. Therefore 

dZ (zM , z N ) = dZ (FM,Z (x, z M ), F N,Z (x, z N )) 

≤ dZ (FM,Z (x, z M ), F N,Z (x, z M )) + dZ (FN,Z (x, z M ), F N,Z (x, z N )) 

≤ δ + αZ dZ (zM , z N ).

27 Thus 

dZ (zM , z N ) ≤ δ

1 − αZ

.

If the output component is βZ -Lipschitz in the feedback variable, then 

dY (Tr FM(x), Tr FN(x)) = dY (FM,Y (x, z M ), F N,Y (x, z N )) 

≤ δ + βZ dZ (zM , z N )

≤



1 + βZ

1 − αZ



δ. 

Therefore a trace node amplifies the pre-trace discrepancy by at most 

1 + βZ

1 − αZ

.

Combining this estimate with the induction hypothesis for the pre-trace context gives the stated recursive rule for L(Tr Z (D)) .The contraction modulus κ(C) is obtained by the same structural induction on the certified Lipschitz constants: composition multiplies constants, tensoring takes the maximum under the product sup metric, and a closed trace node is admitted only when its certified external modulus is strictly smaller than one. Hence every admitted closed context has a structurally certified modulus κ(C) < 1.

A.15 Proof of Theorem 5.8 

Proof. By Lemma 5.7, plugging M and N into the same certified admitted linear context gives closed-loop operators satisfying 

d(S,M ) 

> ∞

(TC[M], TC[N]) ≤ L(C)dX←Y 

> ∞

(TM, TN) ≤ L(C)ε. 

The same certificate gives a common contraction modulus κ(C) < 1 for the two closed-loop operators. Applying Lemma 5.4 with T = TC[M] and T ′ = TC[N] yields 

∥VC[M] − VC[N]∥∞ ≤ 11 − κ(C) d(S,M ) 

> ∞

(TC[M], TC[N]) ≤ L(C)1 − κ(C) ε. 

A.16 Proof of Lemma 6.2 

Proof. Fix s ∈ S and bV ∈ B ( bS). Since π(a | s) = bπ(a | ϕ(s)) ,

(T π (ϕ∗ bV ))( s) = 

Z

> A



r(s, a ) + γ

Z

> S

bV (ϕ(s′)) P (ds ′ | s, a )



π(da | s)=

Z

> A

br(ϕ(s), a ) + γ

Z

> bS

bV (bs′) bP (dbs′ | ϕ(s), a )

bπ(da | ϕ(s)) .

The first equality is the concrete Bellman backup applied to the pulled-back value. In the second equality, reward preservation gives r(s, a ) = br(ϕ(s), a ), and transition commutation gives 

Z

> S

bV (ϕ(s′)) P (ds ′ | s, a ) = 

Z

> bS

bV (bs′) bP (dbs′ | ϕ(s), a ).

The final expression is ( bT bπ bV )( ϕ(s)) = ( ϕ∗ bT bπ bV )( s).

A.17 Proof of Theorem 6.3 

Proof. Let bV bπ be the unique fixed point of bT bπ . By Lemma 6.2, 

T π (ϕ∗ bV bπ ) = ϕ∗( bT bπ bV bπ ) = ϕ∗ bV bπ .

Thus ϕ∗ bV bπ is a fixed point of the concrete policy-evaluation operator T π . The concrete operator is a 

γ-contraction, so its fixed point is unique. Therefore V π = ϕ∗ bV bπ , i.e. V π (s) = bV bπ (ϕ(s)) .28 A.18 Proof of Lemma 6.5 

Proof. Fix s ∈ S. By the homomorphism assumptions, for each action a,

r(s, a ) + γ

Z

> S

bV (ϕ(s′)) P (ds ′ | s, a ) = br(ϕ(s), a ) + γ

Z

> bS

bV (bs′) bP (dbs′ | ϕ(s), a ).

Taking the supremum over the same action set A on both sides gives 

(T ⋆(ϕ∗ bV ))( s) = sup 

> a∈A



r(s, a ) + γ

Z

> S

bV (ϕ(s′)) P (ds ′ | s, a )



= sup 

> a∈A

br(ϕ(s), a ) + γ

Z

> bS

bV (bs′) bP (dbs′ | ϕ(s), a )



= ( bT ⋆ bV )( ϕ(s)) .

This is exactly T ⋆(ϕ∗ bV ) = ϕ∗( bT ⋆ bV ).

A.19 Proof of Corollary 6.6 

Proof. Let bV ⋆ be the unique fixed point of bT ⋆. Lemma 6.5 gives 

T ⋆(ϕ∗ bV ⋆) = ϕ∗( bT ⋆ bV ⋆) = ϕ∗ bV ⋆.

Thus ϕ∗ bV ⋆ is a fixed point of T ⋆. The optimality operator is a γ-contraction under Assumption 6.4, so this fixed point is unique. Hence V ⋆ = ϕ∗ bV ⋆.

A.20 Proof of Theorem 6.7 

Proof. Let T := T π and bT := bT bπ . For any bV with ∥ bV ∥∞ ≤ Vmax and any s ∈ S,

T (ϕ∗ bV )( s) − ϕ∗( bT bV )( s)

≤

Z

> A

r(s, a ) − br(ϕ(s), a ) π(da | s)+ γ

Z

> A

Z

> S

bV (ϕ(s′)) P (ds ′ | s, a ) −

Z

> bS

bV (bs′) bP (dbs′ | ϕ(s), a ) π(da | s).

The reward term is at most εr . The transition term compares the expectation of bV under ϕ#P (· | s, a )

with its expectation under bP (· | ϕ(s), a ). By Lemma A.4, it is at most Vmax εP . Hence 

∥T (ϕ∗ bV ) − ϕ∗( bT bV )∥∞ ≤ εr + γV max εP =: ε. 

Apply this with bV = bV bπ . Since bT bV bπ = bV bπ ,

∥T (ϕ∗ bV bπ ) − ϕ∗ bV bπ ∥∞ ≤ ε. 

The concrete fixed point V π satisfies T V π = V π . Therefore 

∥V π − ϕ∗ bV bπ ∥∞ = ∥T V π − ϕ∗ bV bπ ∥∞

≤ ∥ T V π − T (ϕ∗ bV bπ )∥∞ + ∥T (ϕ∗ bV bπ ) − ϕ∗ bV bπ ∥∞

≤ γ∥V π − ϕ∗ bV bπ ∥∞ + ε. 

Rearranging gives 

∥V π − ϕ∗ bV bπ ∥∞ ≤ εr + γV max εP

1 − γ .

29 A.21 Proof of Corollary 6.8 

Proof. The first half of the proof of Theorem 6.7 did not use the fixed-point property. It showed directly that for every bV in the ball ∥ bV ∥∞ ≤ Vmax ,

∥T (ϕ∗ bV ) − ϕ∗( bT bV )∥∞ ≤ εr + γV max εP .

Taking the supremum over that ball gives the claimed operator mismatch bound. 

A.22 Proof of Proposition 6.9 

Proof. Corollary 6.8 states that the adapted concrete block Aϕ(T ) and the adapted abstract block 

bAϕ( bT ) have local typed transformer discrepancy at most 

ε = εr + γV max εP .

The adapters give both blocks the same external type, so they can be substituted into the same context. Theorem 5.8 applied to this local discrepancy gives 

∥VC[Aϕ(T )] − VC[ bAϕ( bT )] ∥∞ ≤ L(C)1 − κ(C) ε. 

Substituting the expression for ε proves the proposition. 

A.23 Proof of Lemma 7.5 

Proof. Let 

V∞ := _

> n≥0

T n(⊥).

The sequence T n(⊥) is increasing: ⊥ ≤ T (⊥) by minimality of ⊥, and monotonicity gives T n(⊥) ≤

T n+1 (⊥) for all n. By ω-continuity, 

T (V∞) = T

 _

> n≥0

T n(⊥)

 = _

> n≥0

T n+1 (⊥).

Because the chain starts at ⊥, adding or deleting the first term does not change its supremum, so 

T (V∞) = V∞. Thus V∞ is a fixed point. If W is any fixed point of T , then ⊥ ≤ W . By monotonicity, T n(⊥) ≤ T n(W ) = W for every n.Taking the supremum over n gives V∞ ≤ W . Therefore V∞ is the least fixed point. 

A.24 Proof of Lemma 7.6 

Proof. The value space V(S) is a complete lattice because Q is complete and the order is pointwise. Let 

P := {D ∈ V (S) : T (D) ≤ D}

be the set of pre-fixed points of T . The hypothesis says C ∈ P . Let A := V P , the meet of all pre-fixed points. Since A ≤ D for every D ∈ P , monotonicity gives T (A) ≤ T (D) ≤ D for every 

D ∈ P . Hence T (A) ≤ V P = A, so A ∈ P .Because T (A) ≤ A and T is monotone, T (T (A)) ≤ T (A), so T (A) is also a pre-fixed point. Since 

A is the meet of all pre-fixed points, A ≤ T (A). Combining A ≤ T (A) with T (A) ≤ A gives 

T (A) = A. Thus A is a fixed point. Moreover, every fixed point is a pre-fixed point, so A ≤ W for every fixed point W . Therefore A = lfp( T ).Since C ∈ P and A = V P , we have lfp( T ) = A ≤ C, as claimed. 30 A.25 Proof of Proposition 7.8 

Proof. For C ∈ V +(Y ), the map x 7 → R 

> Y

C(y)K(dy | x) is measurable by the defining measurability property of Markov kernels and the standard monotone-class argument for nonnegative measurable integrands. Hence TK,c C ∈ V +(X).Monotonicity follows from monotonicity of the extended nonnegative integral: if C ≤ D, then 

Z

C(y)K(dy | x) ≤

Z

D(y)K(dy | x)

for every x, hence TK,c C ≤ T K,c D. For ω-continuity, let Cn ↑ C pointwise with Cn ∈ V +(Y ). The monotone convergence theorem gives 

Z

C(y)K(dy | x) = sup 

> n

Z

Cn(y)K(dy | x),

and therefore TK,c (W 

> n

Cn) = W 

> n

TK,c (Cn).For series wiring, substitute the definition twice: 

(TK1,c 1 TK2,c 2 C)( x) = c1(x) + γ

Z

> Y



c2(y) + γ

Z

> Z

C(z)K2(dz | y)



K1(dy | x).

This is exactly the two-micro-step transformer with the second cost and continuation discounted by one additional factor. For parallel wiring, product kernels and separability give 

TK1⊗K2, c 1⊗Qc2 (C1 ⊗Q C2)( x1, x 2)= c1(x1) + c2(x2) + γ

Z  C1(y1) + C2(y2)(K1 ⊗ K2)( d(y1, y 2) | x1, x 2)= ( TK1,c 1 C1)( x1) + ( TK2,c 2 C2)( x2).

The feedback clause is Definition 7.7: a jointly ω-continuous pre-trace transformer is a morphism in 

CPO ω, ⊥, so its feedback component has a parameterized least fixed point by Kleene iteration, and tracing returns the output component at that fixed point. 

A.26 Proof of Theorem 7.10 

Proof. For series, Assumption 7.9 gives 

JM2 ◦ M1K(CZ ) = ( T1 ◦ T 2)( CZ ).

The hypotheses give T2(CZ ) ≤ CY and T1(CY ) ≤ CX . Since T1 is monotone, 

(T1 ◦ T 2)( CZ ) ≤ T 1(CY ) ≤ CX .

If X = Z = S and CX = CZ = CS , the same inequality reads (T1 ◦ T 2)( CS ) ≤ CS , which is an inductive closed-loop contract. Lemma 7.6 then gives the closed-loop guarantee. For parallel composition, Assumption 7.9 gives the tensor transformer. Monotonicity of each Ti gives 

Ti(CYi ) ≤ CXi .

The quantale tensor is monotone in each argument because it distributes over joins and is a monoid operation on the complete lattice. Therefore, pointwise on X1 ⊗ X2,

(T1 ⊗ T 2)( CY1 ⊗Q CY2 ) = T1(CY1 ) ⊗Q T2(CY2 ) ≤ CX1 ⊗Q CX2 .

For feedback, fix CY and define the feedback functional 

g(Z′) := FZ (CY , Z ′).

By Assumption 7.9, g is monotone and ω-continuous, so the traced feedback value is Z∗ := lfp( g).The hypothesis FZ (CY , C Z ) ≤ CZ says exactly that CZ is a pre-fixed point of g. Lemma 7.6 gives 

Z∗ ≤ CZ . Monotonicity of FX in the feedback argument yields 

Tr Z (F )( CY ) = FX (CY , Z ∗) ≤ FX (CY , C Z ) ≤ CX .

Thus the traced circuit satisfies the claimed bound. 31 A.27 Proof of Lemma 8.2 

Proof. Let b0 = ν0. For t ≥ 1, let 

ht = ( a0, o 1, . . . , a t−1, o t)

and let bt denote the conditional distribution of st given ht. Thus bt is the posterior belief available when action at is chosen. Given the original history-dependent policy π, define a regular conditional distribution 

πB,t (· | b) := Pπ (at ∈ · | bt = b),

which exists under the standard-Borel assumptions. This is a Markov kernel from the belief space B

to A.We prove by induction on t that the law of (bt, a t) under the original POMDP policy equals the law under the belief process controlled by (πB,t ). At t = 0 , both processes start from the same prior belief b0 = ν0 and choose actions according to the same conditional distribution by construction. Assume the equality holds at time t. Given (bt, a t), the predictive observation law and Bayes update determine the law of bt+1 through the same kernel PB in both processes. Then at+1 is sampled from 

πB,t +1 (· | bt+1 ), which by definition matches the conditional law of the original action given the belief. Hence the joint law matches at time t + 1 .The lifted reward is rB (b, a ) = R r(s, a )b(ds ), so the conditional expected reward given (bt, a t) is the same in the original and belief processes. Since the joint laws of (bt, a t) match for every fixed t,the expected one-step rewards match for every t. Boundedness of r and γ < 1 justify exchanging expectation with the absolutely convergent discounted sum. Therefore the discounted expected returns coincide. 

A.28 Proof of Theorem 8.3 

Proof. By Lemma 8.2, every history-dependent POMDP policy induces a possibly time-dependent belief policy with the same law of belief-action pairs at each time and the same expected one-step reward at each time. Therefore the expected discounted returns from ν0 are equal: 

V π

> POMDP

(ν0) = V (πB,t ) 

> MB

(ν0).

Conversely, any belief policy is a valid POMDP history policy because the belief bt is a measurable function of the observation-action history. Thus the two policy classes generate the same achievable return values when expressed at the belief state. Taking suprema over the corresponding policy classes gives 

V ⋆

> POMDP

(ν0) = V ⋆ 

> MB,nonstat

(ν0).

For a stationary belief policy, the belief process is a fully observed discounted MDP with state space 

B, transition kernel PB , reward rB , and discount γ, so the fixed-point semantics from the main text applies directly. 

A.29 Proof of Lemma 8.4 

Proof. For a finite horizon T , the prefix density under a policy α ∈ { μ, π } factors as 

dPTα = dλ 0(s0) 

> T−1

Y

> t=0

α(da t | st)P (ds t+1 | st, a t)Rt(dr t | st, a t, s t+1 ),

where λ0 is the common initial distribution and Rt denotes the reward kernel or deterministic reward emission. The transition and reward factors are identical under π and μ; only the action kernels differ. Since π(· | s) ≪ μ(· | s), each one-step action density dπ (· | s)/dμ (· | s) exists. Cancelling the common factors gives 

dPTπ

dPTμ

= 

> T−1

Y

> t=0

dπ (· | st)

dμ (· | st) (at).

When the action kernels admit densities with respect to a common reference measure, the derivative can be written as the familiar product QT −1 

> t=0

π(at | st)/μ (at | st).32 For the infinite-horizon statement, the finite-prefix likelihood ratios form a nonnegative martingale under Pμ with mean one. If the infinite-horizon Radon–Nikodym derivative exists, it is the almost-sure limit of this martingale by the standard martingale convergence characterization of projective-limit likelihood ratios. 

A.30 Proof of Proposition 8.5 

Proof. In the parallel case, the finite-prefix behavior and target measures factorize as products over modules: 

PTμ = O

> j

PT, (j) 

> μ

, PTπ = O

> j

PT, (j) 

> π

.

For product measures, Radon–Nikodym derivatives multiply. Therefore 

dPTπ

dPTμ

= Y

> j

dPT, (j)

> π

dPT, (j)

> μ

.

In the series case, the assumed interface variable Ut gives a chain-rule decomposition of the prefix measure into conditional subtrajectory laws. If 

dPTα = dQ (1)  

> α

dQ (2)  

> α

(· | U ) · · · dQ (m) 

> α

(· | U )

for α ∈ { μ, π }, then the Radon–Nikodym chain rule gives 

dPTπ

dPTμ

=

> m

Y

> j=1

dQ (j)

> π

dQ (j)

> μ

,

with each factor interpreted conditionally on the relevant interface variables. This is the stated module-wise factorization. 

A.31 Proof of Corollary 8.6 

Proof. Let 

Wj := dPT, (j)

> π

dPT, (j)

> μ

be the module-wise finite-prefix importance weights. Proposition 8.5 gives W = Q 

> j

Wj for the global weight. If the Wj are independent under PTμ , then 

Eμ[W 2] = Eμ

Y

> j

W 2

> j

 = Y

> j

Eμ[W 2 

> j

].

Taking logarithms gives 

log Eμ[W 2] = X

> j

log Eμ[W 2 

> j

],

which is the claimed identity. 

A.32 Proof of Theorem 8.7 

Proof. For each t, apply Lemma 5.4 to T = Tt and T ′ = Tt+1 . Both are γ-contractions and their fixed points are V ⋆t and V ⋆t+1 . The operator discrepancy is exactly 

ηt = sup  

> V∈B

∥T t+1 V − T tV ∥∞.

Thus 

∥V ⋆t+1 − V ⋆t ∥∞ ≤ ηt

1 − γ .

For the cumulative bound, use the triangle inequality: 

∥V ⋆T − V ⋆ 

> 0

∥∞ ≤ 

> T−1

X

> t=0

∥V ⋆t+1 − V ⋆t ∥∞ ≤ X

> t<T

ηt

1 − γ .

33 A.33 Proof of Corollary 8.8 

Proof. By definition, Vt+1 = TtVt, while V ⋆t = TtV ⋆t . Therefore 

∥Vt+1 − V ⋆t ∥∞ = ∥T tVt − T tV ⋆t ∥∞ ≤ γ∥Vt − V ⋆t ∥∞.

Then 

∥Vt+1 − V ⋆t+1 ∥∞ ≤ ∥ Vt+1 − V ⋆t ∥∞ + ∥V ⋆t − V ⋆t+1 ∥∞

≤ γ∥Vt − V ⋆t ∥∞ + ηt

1 − γ ,

where the second term uses Theorem 8.7. Iterating this scalar recursion gives 

∥VT − V ⋆T ∥∞ ≤ γT ∥V0 − V ⋆ 

> 0

∥∞ + X

> t<T

γT −1−t ηt

1 − γ .

A.34 Proof of Proposition 10.1 

Proof. The proof is the trajectory version of Theorem 5.3. Under product dynamics and product policy, the two coordinate processes are independent conditional on their initial states. Therefore 

V π (s1, s 2) = E

X

> t≥0

γt(r1(s1,t , a 1,t ) + r2(s2,t , a 2,t )) | s1,0 = s1, s 2,0 = s2



= E

X

> t≥0

γtr1(s1,t , a 1,t ) | s1,0 = s1

 + E

X

> t≥0

γtr2(s2,t , a 2,t ) | s2,0 = s2



= V π1 (s1) + V π2 (s2).

Linearity of expectation justifies splitting the sum; bounded rewards justify exchanging expectation and the absolutely convergent discounted series. 

A.35 Proof of Lemma 10.2 

Proof. For any V with ∥V ∥∞ ≤ Vmax and any state-action input (x, a ),

˜r(x, a ) + γ

Z

V (y) eP (dy | x, a ) − r(x, a ) − γ

Z

V (y)P (dy | x, a )

≤ | ˜r(x, a ) − r(x, a )| + γ

Z

V d eP −

Z

V dP 

≤ εr + γV max εP ,

where the last step uses Lemma A.4. Integrating over the fixed policy kernel cannot increase the uniform bound. Taking the supremum over states and over V ∈ B gives 

d∞(T, eT ) ≤ εr + γV max εP .

A.36 Proof of Lemma 10.3 

Proof. For any V ∈ BS ,

∥T1T2V − eT1 eT2V ∥∞ ≤ ∥ T1T2V − eT1T2V ∥∞ + ∥ eT1T2V − eT1 eT2V ∥∞

≤ ϵ1 + γ∥T2V − eT2V ∥∞

≤ ϵ1 + γϵ 2.

The first term is bounded by the definition of ϵ1 on the relevant ball. The second term uses the 

γ-Lipschitz property of eT1 and the definition of ϵ2. Taking the supremum over V ∈ BS proves the result. 34 A.37 Proof of Lemma 10.4 

Proof. By Lemma 10.2, the local module mismatches satisfy 

ϵi ≤ ε(i) 

> r

+ γV max ε(i) 

> P

.

By Lemma 10.3, the two-step macro-operator mismatch satisfies 

d∞(Tser , eTser ) ≤ ϵ1 + γϵ 2.

Both macro operators are γ2-contractions, because each is a composition of two γ-Lipschitz micro-step transformers. Lemma 5.4 with κ = γ2 therefore yields 

∥ eV π − V π ∥∞ ≤ ϵ1 + γϵ 2

1 − γ2 .

Substituting the explicit local bounds gives the stated inequality. 35