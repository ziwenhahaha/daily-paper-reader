---
title: Pure Exploration for a Good Policy in Reinforcement Learning with Bandit Feedback
title_zh: bandit反馈强化学习中良好策略的纯探索
authors: "Zitian Li, Wang Chi Cheung"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23182v1"
tags: ["query:rl"]
score: 9.0
evidence: 情景强化学习中的纯探索理论
tldr: 研究针对强化学习中的纯探索问题，提出良好策略识别(GPI)目标，区别于传统最佳策略识别(BPI)。当存在预期奖励不低于阈值μ₀的策略时，只需识别此类策略，否则声明无。提出BEE-GPI算法，在固定置信度设定下最小化样本复杂度。理论证明其样本复杂度上界与状态空间大小无关，并给出下界验证near-optimality。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23182v1/fig-001.webp\", \"caption\": \"Figure 1: Numerical comparison between BEE-GPI and BPI-UCRL (the lower the better).\", \"page\": 9, \"index\": 1, \"width\": 620, \"height\": 295}]"
motivation: 实际应用中识别一个足够好的策略往往足矣，无需精确识别最优策略。
method: 提出BEE-GPI算法，通过引入奖励阈值μ₀，对正实例识别满足阈值的策略，对负实例正确声明无。
result: "获得样本复杂度上界，正实例的log(1/δ)系数为O(H²/(V*-μ₀)²)，且与状态空间大小无关。"
conclusion: "理论分析和实验验证表明BEE-GPI高效且接近最优，证明了1/(V*-μ)²项的必要性。"
---

## 摘要
研究针对强化学习中的纯探索问题，提出良好策略识别(GPI)目标，区别于传统最佳策略识别(BPI)。当存在预期奖励不低于阈值μ₀的策略时，只需识别此类策略，否则声明无。提出BEE-GPI算法，在固定置信度设定下最小化样本复杂度。理论证明其样本复杂度上界与状态空间大小无关，并给出下界验证near-optimality。

## Abstract
Pure exploration in episodic Reinforcement Learning has primarily focused on Best Policy Identification (BPI), which seeks to identify a (near)-optimal policy with high confidence. Motivated by practical settings where a ``good enough'' policy suffices, we study an alternate objective of Good Policy Identification (GPI). For a given reward threshold $μ_0$, GPI only requires identifying a policy with expected reward in an episode at least $μ_0$ if such a policy exists (positive instance), or declaring None if no such policy exists (negative instance). We formalize GPI under the fixed-confidence setting. We require the output to be correct with probability $\geq 1-δ$, and seek to minimize the expected sample complexity, which is the expected number of episodes explored for the output. We propose a novel algorithm BEE-GPI, and derive theoretically-grounded upper bounds on its sample complexity for positive and negative instances. Notably, for positive instances, the coefficient of $\log 1/δ$ in our upper bound is $O(H^2/(V^* - μ_0)^2)$, where $H$ is the episode length and $V^*$ is the optimal expected reward in an episode. The coefficient does not depend on the action and state space sizes otherwise, in sharp contrast to the sample complexity in BPI. We further establish lower bound results to show the near-optimality of BEE-GPI and the necessity of the $1/(V^* -μ)^2$ term. Numerical experiments further validate the efficiency of our approach.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

**研究动机**：传统的强化学习纯探索问题主要聚焦于**最佳策略识别（Best Policy Identification, BPI）**，即以高置信度识别一个近似最优策略。然而在实际应用中，找到一个"足够好"的策略往往就足够了，无需精确找到全局最优。

**核心问题——良好策略识别（Good Policy Identification, GPI）**：
- 给定奖励阈值 μ₀，识别一个期望奖励不低于 μ₀ 的策略（正实例）
- 若不存在这样的策略，则正确声明 "None"（负实例）
- 在固定置信度设定下，以高概率（≥ 1-δ）保证输出正确性，同时最小化期望样本复杂度

**应用场景**：
- 医疗决策：评估新治疗方案是否达到已知标准疗法（SoC）的效果阈值
- 云计算：确保资源分配策略满足服务等级协议（SLA）的最低标准
- 金融投资：验证新交易策略是否达到最低夏普比率或投资回报率基准

## 2. 论文提出的方法论

### 核心思想
提出 **BEE-GPI算法**（Balanced Exploration-Exploitation for Good Policy Identification），将GPI问题分解为**探索阶段**和**利用阶段**，利用早期停止的BPI子程序作为Oracle来实现高效样本复杂度。

### 关键技术细节

**算法流程（三步骤）**：

| 步骤 | 操作 | 说明 |
|------|------|------|
| **Step 0** | 初始化 | 设置超参数：εₖ = 1/2ᵏ, δₖ = 1/3ᵏ, αₖ = 5ᵏ，累积历史 Hₑᵉ, Hₑₜ |
| **Step 1** | 探索阶段 | 调用 ES-BPI-UCRL（Algorithm 2），使用乐观采样策略估计转移概率，构建置信区间，通过停止准则（3）和（4）决定是否终止 |
| **Step 2** | 决策分支 | 根据输出类型决定：继续下一阶段 / 输出None / 进入利用阶段 |
| **Step 3** | 利用与验证 | 对候选策略 ˆπₖ 执行独立采样，构建置信区间验证其是否满足阈值 |

**关键停止准则**：
- 正实例停止条件：V_{t,π_t}(s₀; δₖ) - (C-1)(V_t(s₀; δₖ) - V_{t,π_t}(s₀; δₖ)) > μ₀
- 负实例停止条件：V_t(s₀; δₖ) < μ₀

**理论结果**：

对于**正实例**，样本复杂度上界为：
```
E[τ] ≤ O(H² log(1/δ) / (V* - μ₀)²) + O(poly(S,A,H) / (V* - μ₀)²)
```

对于**负实例**，样本复杂度上界为：
```
E[τ] ≤ O((H+1)⁴[SA log(1/δ) + S²A log log(1/δ)] / (V* - μ₀)²) + ...
```

**关键发现**：对于正实例，log(1/δ) 的系数为 **O(H²/(V* - μ₀)²)**，**与状态空间大小 S 和动作空间大小 A 无关**。

## 3. 实验设计

### 数据集与场景
- **Single Chain MDP**：初始状态固定为状态0
- **Double Chain MDP**：初始状态固定为状态1
- 状态空间 S = {0, 1, 2, 3}，动作空间 A = {left, right}
- 转移动态：intended方向概率0.9，reverse方向概率0.1

### Benchmark设置
- 阈值 μ₀ ∈ {1, 1.5, 2.0, 2.5, 3.0}
- 失败概率容忍度 δ = 0.001
- 剧集长度 H = 8

### 对比方法
- **BEE-GPI**（本文提出）
- **BPI-UCRL**（Kaufmann et al., 2021）：作为对比的 ε-最优BPI算法

为公平对比，为BPI-UCRL提供Oracle参数 ε = V* - μ₀（即已知最优值）。

### 实验设置
- 每种设置进行 **20次独立试验**
- 使用 **MOSEK优化求解器** 实现算法

## 4. 资源与算力

| 资源类型 | 规格 |
|---------|------|
| **CPU** | Intel i5-14500 |
| **计算时长** | 约 72 小时 |
| **GPU** | 未使用（基于CPU实验） |

实验在相对有限的计算资源下完成，总运行时间约72小时，说明算法实现效率较高。

## 5. 实验数量与充分性

### 实验规模
- **2种MDP环境**（Single Chain、Double Chain）
- **5个阈值设置**（1.0, 1.5, 2.0, 2.5, 3.0）
- **20次重复** × 2种环境 × 5个阈值 = **200次实验运行**
- 每次运行报告平均值和3σ标准差

### 充分性分析
**优点**：
- 实验覆盖了不同的阈值场景
- 使用独立重复试验评估稳定性
- 标准差很小（error bar显示3σ），说明终止时间方差低

**不足**：
- 仅使用2个简单的表格MDP环境
- 缺少大规模或复杂MDP场景的验证
- **未进行消融实验**来验证各组件（探索、利用、停止准则）的独立贡献
- 未测试算法在部分可观测或函数近似场景下的性能
- 与其他非BPI方法的对比（如基于Q-learning的探索策略）缺失

## 6. 论文的主要结论与发现

### 理论贡献
1. **正式定义GPI问题**：在固定置信度设定下，给出正实例和负实例的形式化定义
2. **BEE-GPI算法**：提出结合早期停止BPI子程序的探索-利用平衡算法
3. **样本复杂度上界**：
   - 正实例：log(1/δ) 系数 **O(H²/(V* - μ₀)²)**，与 S, A 无关
   - 负实例：复杂度与状态-动作空间多项式相关
4. **下界证明**：通过 Uniform Instance 和 Tree Instance 构造，证明了 1/(V* - μ₀)² 项的必要性

### 实验发现
- **BEE-GPI 始终优于 BPI-UCRL**：在所有测试阈值下，终止剧集数显著更少
- 性能差距随阈值降低而增大，说明识别"足够好"策略比"最优"策略效率更高

### 关键洞见
- GPI问题的样本复杂度可以**与最优策略识别问题的难度解耦**
- 对于正实例，主要开销来自验证候选策略是否满足阈值，而非全局搜索最优策略

## 7. 优点

### 方法设计亮点
1. **问题定义的实用性**：区分"足够好"与"最优"，更贴近实际应用需求
2. **理论保证严格**：建立上下界，证明了BEE-GPI的near-optimality（与S, A, H的多项式因子内最优）
3. **log(1/δ)系数解耦**：正实例中成功将与置信度相关的复杂度与状态空间大小解耦，显著降低样本需求
4. **早期停止机制**：通过 ES-BPI-UCRL 的停止准则，避免不必要的过度探索

### 实验设计亮点
1. **与SOTA方法对比**：选择当前最优的BPI-UCRL作为baseline
2. **提供先验知识给对比算法**：为BPI-UCRL提供真实的 ε 参数，确保公平对比
3. **清晰的评估指标**：使用剧集数（而非总步数），符合episodic设定

## 8. 不足与局限

### 实验覆盖不足
- **仅测试简单表格MDP**：状态空间仅4个状态，未涉及大规模问题
- **缺乏真实环境验证**：如医疗或金融场景的实际数据
- **未进行消融实验**：无法验证各组件（如置信度调整、停止准则）的独立贡献

### 偏差风险
- **Oracle对比优势**：BPI-UCRL获得真实 ε 参数，实际中可能不可知
- **初始化依赖**：算法性能可能对超参数选择（C, αₖ 等）敏感
- **均匀实例构造的特殊性**：Uniform Instance 和 Tree Instance 具有特定结构，泛化性需进一步验证

### 应用限制
- **表格设定**：算法设计针对有限状态-动作空间，不适用于函数近似或深度RL
- **确定性奖励假设**：奖励函数确定性（r ∈ [0,1]），随机奖励场景未考虑
- **同质初始分布**：假设初始状态分布已知且固定
- **计算复杂度**：O(H⁴) 量级的复杂度在 H 较大时可能不实用

### 扩展性未探索
- 未研究奖励函数近似、状态表示学习等现代RL技术
- 与在线学习或适应式探索的结合未讨论
- 多智能体或合作场景未涉及

---

（完）
