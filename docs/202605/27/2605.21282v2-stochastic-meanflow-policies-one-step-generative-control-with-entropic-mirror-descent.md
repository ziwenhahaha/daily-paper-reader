---
title: "Stochastic MeanFlow Policies: One-Step Generative Control with Entropic Mirror Descent"
title_zh: 随机MeanFlow策略：基于熵镜像下降的单步生成控制
authors: "Zeyuan Wang, Da Li, Yulin Chen, Yuehu Gong, Yanming Guo, Ye Shi, Liang Bai, Tianyuan Yu, Yanwei Fu"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21282v2"
tags: ["query:rl"]
score: 9.0
evidence: 基于熵正则化和镜像下降的在线离策略强化学习
tldr: 提出随机MeanFlow策略(SMFP)，通过MeanFlow变换和高斯重参数化实现可处理的熵代理，在离策略镜像下降框架中统一优化，7个MuJoCo基准上超越高斯和生成式基线
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21282v2/fig-001.webp\", \"caption\": \"Table 4: Recommended Hyperparameter Search Space for New Environments\", \"page\": 21, \"index\": 1, \"width\": 790, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2605.21282v2/fig-002.webp\", \"caption\": \"Table 6: Hyperparameters in Experiments\", \"page\": 22, \"index\": 2, \"width\": 732, \"height\": 1018}, {\"url\": \"assets/figures/arxiv/2605.21282v2/fig-003.webp\", \"caption\": \"Table 1: Comparison of SMFP and 15 other online RL algorithms in evaluation results under 30 random seeds.\", \"page\": 8, \"index\": 3, \"width\": 802, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2605.21282v2/fig-004.webp\", \"caption\": \"Figure 4: Comparison of evaluation performance across 7 benchmarks. All methods are trained for 1M environment steps. The x-axis denotes environment steps, and the y-axis denotes episodic reward.\", \"page\": 8, \"index\": 4, \"width\": 827, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2605.21282v2/fig-005.webp\", \"caption\": \"Figure 6: Sensitivity analysis of the entropy temperature parameter α and the mirror descent regularisation coefficient λ on MuJoCo benchmarks. Curves show the mean over 5 seeds.\", \"page\": 9, \"index\": 5, \"width\": 820, \"height\": 331}, {\"url\": \"assets/figures/arxiv/2605.21282v2/fig-006.webp\", \"caption\": \"Figure 3: Comparison of policy parameterisations and action-distribution evolution on Push-T. (a) Gaussian, conventional flow-based, and SMFP policies. Unlike flow policies that estimate the instantaneous velocity v(s, at, t), SMFP estimates the average velocity over [b, t], enabling onestep action generation with a tractable entropy lower bound. (b) Training evolution of generated action distributions. Red contours indicate target modes and blue samples indicate generated actions. Gaussian policies tend to cover a single dominant mode, while conventional flow matching captures multimodality but loses exploration as samples concentrate. SMFP covers all target modes while preserving broad stochastic exploration.\", \"page\": 5, \"index\": 6, \"width\": 763, \"height\": 414}]"
motivation: 。
method: 。
result: 。
conclusion: 。
---

## 摘要
在线离策略强化学习（RL）受两个相互耦合的选择塑造：策略类和更新规则。高斯策略快速且具有可处理的熵，但在多模态动作分布上表现困难。生成式策略更具表现力，但通常需要迭代采样或缺乏可处理的熵估计。在优化方面， SAC 风格的软策略改进和镜像下降（MD）可以被视为最小化不同的 KL 散度：前者将策略推向由价值诱导的玻尔兹曼分布，而后者在每次更新时对前一策略进行正则化。将熵正则与 MD 约束相结合因此很有吸引力，因为它支持探索同时稳定策略改进；然而，得到的目标可能是多模态的，而单模态的高斯策略难以很好地匹配。我们提出随机 MeanFlow 策略（SMFP），一种单步生成策略类，它通过 MeanFlow 变换将高斯噪声映射到动作。这种随机重参数化产生了可处理的熵代理，使得 MeanFlow 策略能够在离策略镜像下降框架中以统一目标进行训练，从而实现探索性且稳定的改进。在七个 MuJoCo 基准上，SMFP 在保持单步推理效率的同时超越了高斯和生成式基线。

## Abstract
Online off-policy reinforcement learning (RL) is shaped by two coupled choices: the policy class and the update rule. Gaussian policies are fast and have tractable entropy, but struggle with multimodal action distributions. Generative policies are more expressive, but often require iterative sampling or lack tractable entropy estimates. On the optimisation side, SAC-style soft policy improvement and mirror descent (MD) can be viewed as minimising different KL divergences: the former moves the policy towards a value-induced Boltzmann distribution, while the latter regularises each update against the previous policy. Combining entropy regularisation with an MD constraint is therefore attractive, as it supports exploration while stabilising policy improvement; however, the resulting target can be multimodal and is poorly matched by unimodal Gaussian policies. We propose Stochastic MeanFlow Policies (SMFP), a one-step generative policy class that maps Gaussian noise to actions through a MeanFlow transformation. This stochastic reparameterisation yields a tractable entropy surrogate and allows MeanFlow policies to be trained within off-policy mirror descent under a unified objective for exploratory yet stable improvement. Across seven MuJoCo benchmarks, SMFP improves over Gaussian and generative baselines while retaining single-step inference efficiency.

---

## 论文详细总结（自动生成）

# 论文总结：随机MeanFlow策略（SMFP）

## 1. 核心问题与整体含义

### 研究背景
在线离策略强化学习（RL）存在两个相互耦合的设计维度：**策略表示**（如何建模动作）和**优化方法**（如何更新策略）。

### 核心矛盾
- **高斯策略**：推理快速、熵估计易处理，但无法建模多模态动作分布
- **生成式策略**（扩散/流模型）：表达力强，能捕捉复杂行为，但通常需要迭代采样或缺乏可处理的熵估计

### 优化层面的张力
- **SAC风格**：软策略改进，将策略推向由价值诱导的玻尔兹曼分布（鼓励探索）
- **镜像下降（MD）**：约束每次更新接近前一策略（保证稳定性）

### 关键问题
将SAC熵探索与MD稳定性结合会产生**多模态目标分布**，而单模态高斯策略无法很好地匹配这一目标。这引发了一个根本性问题：**能否用表达力强的生成式策略来填补这一空白？**

---

## 2. 方法论

### 核心思想
提出**随机MeanFlow策略（SMFP）**，将MeanFlow的噪声到动作映射与**高斯重参数化**结合，实现：
1. **单步推理**：一次前向传播即可生成动作
2. **可处理的熵代理**：通过条件熵下界实现熵正则化
3. **与离策略镜像下降兼容**：统一目标支持探索性和稳定的策略改进

### 关键技术细节

#### 随机重参数化
将MeanFlow公式扩展为：
```
gθ(at, b, t) = at - uθ(at, b, t) + σθ(at, b, t) ⊙ ε
```
其中：
- `at = (1-t)a + te` 是插值路径
- `uθ` 预测均值，`σθ` 预测标准差
- `ε ~ N(0, I)` 提供可处理的随机性
- 单步推理设置：`b=0, t=1, at=e`，得到 `â = gθ(e, 0, 1)`

#### 可处理的熵代理
通过信息论分解，推导出**条件熵下界**：
```
Ĥ(πθ|e) = E_ε[Σ_i log σ_θ^(i)(at, b, t)]
```
实际使用**hinge风格的熵正则器**：
```
L_Ent(θ) = E[ReLU(κ_σ - (1/d)Σ_i log σ_θ^(i)(at, b, t))]
```
其中 `κ_σ` 是最小归一化对数尺度的阈值。

#### 镜像下降优化
结合SAC目标和MD KL约束，推导出**优势加权MeanFlow回归目标**：
```
L_MD(θ) = E_{a~π_old}[ω_adv(s, a) L_SMF(θ; a, s)]
```
其中：
```
ω_adv(s, a) = max(0, Q(s, a) - V(s))  // 截断优势加权
L_SMF = Huber损失(预测值 - 目标值)
```

#### 最终目标函数
```
L(θ) = E_{s~D}[-Q(s, g_θ(·)) + αL_Ent(θ) + λL_MD(θ)]
```

#### 值引导动作选择
使用Best-of-K策略：从K个候选动作中选择Q值最高的，平衡质量与效率。

---

## 3. 实验设计

### 数据集与环境
- **7个MuJoCo基准环境**：Hopper-v4, Walker2D-v4, HalfCheetah-v4, Ant-v4, Humanoid-v4, HumanoidStandup-v4, Swimmer-v4
- 特点：多样化动力学特性、不同状态动作空间

### 评估方法
- 30个随机种子的均值 episodic reward 和标准差
- 训练预算：1M环境步

### 对比方法（15种）
| 类别 | 方法 |
|------|------|
| **高斯策略RL** | PPO, SPO, TD3, SAC |
| **扩散策略RL** | DIPO, QSM, QVPO, DIME, MaxEntDP, DPMD |
| **流策略RL** | SAC Flow-T, SAC Flow-G, FlowRL, FPMD-R, FPMD-M |

### 消融实验
1. 移除熵正则器的变体
2. 不同 `κ_σ` 设置（-1, -3, -5, -7）
3. 移除镜像下降项的变体
4. 超参数敏感性分析（`α`, `λ`）
5. 候选数量K的影响
6. Q值聚合策略（min vs mean）

---

## 4. 资源与算力

**硬件配置**：
- 推理速度测试：NVIDIA RTX 5090
- 具体型号和数量未明确说明

**训练时间**（Ant-v4环境）：
| 方法 | 训练时间(小时) | 推理时间(ms) |
|------|--------------|--------------|
| SMFP | 2.7 | 0.42 |
| TD3 | 0.4 | 0.27 |
| SAC | 2.2 | 0.13 |

- 推理延迟：SMFP仅需**0.42ms**（单步推理，NFE=1）
- 相比迭代扩散方法（16-20步采样）有显著优势

**注意**：论文未详细说明训练使用的GPU数量和配置信息。

---

## 5. 实验数量与充分性

### 实验规模
- **主实验**：7个环境 × 30个随机种子
- **消融实验**：5组不同的消融分析
- **敏感性分析**：多组超参数变体

### 充分性评估
✅ **公平性保障**：
- 采用"保守最优可用协议"：对每个基线报告最强结果（原论文或复现）
- 学习曲线仅展示复现结果，确保同一实现下的公平比较

✅ **全面性**：
- 覆盖多种策略类型（高斯、扩散、流）
- 包含关键组件的消融验证
- 参数敏感性分析充分

⚠️ **潜在不足**：
- 仅在MuJoCo环境验证，未涉及复杂视觉输入任务
- 未在真实机器人硬件上验证

---

## 6. 主要结论与发现

### 核心发现
1. **SMFP在7个基准中实现最佳综合性能**，超越高斯和生成式基线
2. **单步推理效率**：推理延迟仅0.42ms，显著优于迭代采样方法
3. **各组件有效性**：熵正则器和镜像下降项都对性能有重要贡献

### 关键洞察
- **表达力至关重要**：结合SAC探索和MD稳定性的收益只有在表达力强的生成式策略下才能实现
- **熵正则化作用**：维持最小动作噪声尺度对探索和MD策略改进都重要
- **镜像下降的稳定效果**：在高维任务（如Humanoid）上增益更明显

### 性能对比亮点
| 环境 | SMFP表现 |
|------|----------|
| Ant-v4 | 15769.3 ± 125.5 |
| Humanoid-v4 | 376132.6 ± 1107.4 |
| HalfCheetah-v4 | 7518.3 ± 77.1 |

---

## 7. 优点

### 方法创新
1. **精巧的问题转化**：将熵控制和MD稳定性统一到同一框架
2. **可处理的熵代理**：通过高斯重参数化突破MeanFlow的熵不可处理性限制
3. **保留两种优势**：兼具生成式策略的表达力和高斯策略的可处理性

### 实验设计
1. **全面对比**：覆盖15种方法，包含多种策略类型
2. **充分的消融实验**：验证每个组件的贡献
3. **公平比较机制**：采用保守最优可用协议
4. **敏感性分析**：系统性地探索超参数影响

### 工程实现
1. **端到端可微**：支持直接的Q梯度传播
2. **高效并行**：利用JAX vmap实现Best-of-K并行处理
3. **实际有效**：首次使熵正则化MD在实际在线RL中生效

---

## 8. 不足与局限

### 框架局限性
1. **依赖特定结构**：可处理代理目标依赖MeanFlow的重参数化结构，扩展到其他生成式策略可能需要策略特定的推导
2. **固定权重策略**：为隔离贡献，`α`和`λ`固定训练全程，可能存在更优的自适应方案

### 实验覆盖
1. **环境单一**：仅在MuJoCo基准验证，未测试复杂视觉输入或真实机器人任务
2. **规模有限**：未在大规模或长期训练场景中验证

### 应用限制
1. **超参数敏感性**：方法对`κ_σ`、`α`、`λ`等参数敏感，需要针对新环境进行调优
2. **数值稳定性**：Q值聚合策略对性能有影响，需要谨慎选择

### 未来方向（论文自述）
- 探索自适应权重方案
- 集成更强的critic架构（CrossQ、分布式Q学习）
- 扩展到其他生成式策略类

---

（完）
