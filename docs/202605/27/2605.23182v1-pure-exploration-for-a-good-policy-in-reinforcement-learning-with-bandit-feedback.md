---
title: Pure Exploration for a Good Policy in Reinforcement Learning with Bandit Feedback
title_zh: 在带臂反馈的强化学习中寻找良好策略的纯探索
authors: "Zitian Li, Wang Chi Cheung"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23182v1"
tags: ["query:rl"]
score: 9.0
evidence: 强化学习理论用于良好策略识别
tldr: "针对强化学习纯探索问题，传统方法聚焦于识别最优策略（BPI），但实际应用中“足够好”的策略即可满足需求。本研究提出好策略识别（GPI）问题：给定奖励阈值μ₀，需识别期望奖励≥μ₀的策略（正实例）或声明不存在（负实例）。作者设计BEE-GPI算法，在固定置信度设置下最小化样本复杂度。理论证明正实例样本复杂度中log 1/δ系数为O(H²/(V*−μ₀)²)，且与状态动作空间大小无关，显著优于BPI的依赖性。数值实验验证了算法的高效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23182v1/fig-001.webp\", \"caption\": \"Figure 1: Numerical comparison between BEE-GPI and BPI-UCRL (the lower the better).\", \"page\": 9, \"index\": 1, \"width\": 620, \"height\": 295}]"
motivation: 实际应用中识别一个“足够好”而非绝对最优的策略更具实用价值，但现有研究主要关注最佳策略识别。
method: 提出好策略识别（GPI）问题，设计BEE-GPI算法，通过优化样本复杂度下界实现固定置信度的策略识别。
result: "对于正实例，样本复杂度上界中log 1/δ系数为O(H²/(V*−μ₀)²)，且不依赖状态和动作空间大小，与BPI方法形成鲜明对比。"
conclusion: "理论下界和数值实验均验证了BEE-GPI的近似最优性，表明1/(V*−μ₀)²项是样本复杂度的必要组成部分。"
---

## 摘要
在片段式强化学习的纯探索研究中，主要关注的是最优策略识别（BPI），即以高置信度识别一个（近）最优策略。鉴于实际情形中‘足够好’的策略已能满足需求，我们研究了另一种目标——良好策略识别（GPI）。给定奖励阈值 $\mu_0$，GPI 仅要求在存在满足期望奖励不低于 $\mu_0$ 的策略的情况下识别出这样一个策略（正向实例），若不存在则声明无（负向实例）。我们在固定置信度设置下形式化 GPI。我们要求输出正确概率不低于 $1-\delta$，并旨在最小化期望样本复杂度，即为得到输出而探索的期望回合数。我们提出了一种新算法 BEE-GPI，并为其在正向实例和负向实例上的样本复杂度推导出有理论依据的上界。值得注意的是，对于正向实例，我们上界中 $\log 1/\delta$ 的系数为 $O(H^2/(V^* - \mu_0)^2)$，其中 $H$ 为回合长度，$V^*$ 为一回合中的最优期望奖励。该系数不依赖于动作空间和状态空间的大小，这与 BPI 中的样本复杂度形成鲜明对比。我们进一步给出下界结果，以证明 BEE-GPI 的近乎最优性以及 $1/(V^* -\mu)^2$ 项的必要性。数值实验进一步验证了我们方法的有效性。

## Abstract
Pure exploration in episodic Reinforcement Learning has primarily focused on Best Policy Identification (BPI), which seeks to identify a (near)-optimal policy with high confidence. Motivated by practical settings where a ``good enough'' policy suffices, we study an alternate objective of Good Policy Identification (GPI). For a given reward threshold $μ_0$, GPI only requires identifying a policy with expected reward in an episode at least $μ_0$ if such a policy exists (positive instance), or declaring None if no such policy exists (negative instance). We formalize GPI under the fixed-confidence setting. We require the output to be correct with probability $\geq 1-δ$, and seek to minimize the expected sample complexity, which is the expected number of episodes explored for the output. We propose a novel algorithm BEE-GPI, and derive theoretically-grounded upper bounds on its sample complexity for positive and negative instances. Notably, for positive instances, the coefficient of $\log 1/δ$ in our upper bound is $O(H^2/(V^* - μ_0)^2)$, where $H$ is the episode length and $V^*$ is the optimal expected reward in an episode. The coefficient does not depend on the action and state space sizes otherwise, in sharp contrast to the sample complexity in BPI. We further establish lower bound results to show the near-optimality of BEE-GPI and the necessity of the $1/(V^* -μ)^2$ term. Numerical experiments further validate the efficiency of our approach.

---

## 论文详细总结（自动生成）

<think>The user wants me to generate a detailed Chinese summary of this academic paper based on the provided content. The paper is about "Pure Exploration for a Good Policy in Reinforcement Learning with Bandit Feedback" (在带臂反馈的强化学习中寻找良好策略的纯探索).

Let me analyze the paper content and structure my response according to the 8 points requested:

1. Core problem and overall meaning (research motivation and background)
2. Methodology proposed: core ideas, key technical details, algorithms
3. Experimental design: datasets, benchmarks, comparison methods
4. Resources and computation: GPU, training time, etc.
5. Experiment quantity and sufficiency
6. Main conclusions and findings
7. Advantages: highlights in methodology or experimental design
8. Shortcomings and limitations

Let me go through the paper carefully:

**1. Core problem and background:**
- Pure exploration in episodic RL traditionally focuses on Best Policy Identification (BPI)
- BPI aims to identify near-optimal policies with high confidence
- The paper argues that in practical settings, a "good enough" policy suffices
- They propose Good Policy Identification (GPI): given a reward threshold μ₀, identify a policy with expected reward ≥ μ₀ if one exists, or return None
- Examples: medical treatment protocols, cloud computing SLA, investment benchmarks

**2. Methodology:**
- BEE-GPI algorithm (Balanced Exploration-Exploitation for Good Policy Identification)
- Fixed-confidence setting: output must be correct with probability ≥ 1-δ
- Goal: minimize expected sample complexity
- Uses ES-BPI-UCRL (Early-Stopping BPI-UCRL) as oracle
- Algorithm proceeds in phases with exploration and exploitation stages

**3. Experimental design:**
- Single Chain and Double Chain MDPs
- Fixed δ = 0.001, H = 8, S = {0,1,2,3}, A = {left, right}


- Transition dynamics: intended direction with probability 0.9, reverse with 0.1
- Threshold values μ₀ ∈ {1, 1.5, 2.0, 2.5, 3.0}
- Compare against BPI-UCRL (given oracle parameter ǫ = V* - μ₀ as significant prior-knowledge advantage)
- 20 independent trials

**4. Resources and computation:**
- Intel i5-14500 CPU
- Total computation time approximately 72 hours
- MOSEK optimization solver

**5. Experiment quantity and sufficiency:**
- 20 independent trials per setting
- Tested across different threshold configurations to validate algorithm performance under varying conditions

**6. Main conclusions:**
- BEE-GPI consistently outperforms BPI-UCRL with fewer required episodes
- For positive instances, sample complexity scales as O(H² log(1/δ)/(V* - μ₀)²), independent of state and action space dimensions
- Near-optimal performance achieved within polynomial factors of S, A, and H

**7. Advantages:**
- Innovation in identifying "good enough" policies rather than optimal ones
- Sample complexity for positive instances decouples log(1/δ) from state-action space dimensions
- Demonstrates near-optimality through lower bounds

**8. Shortcomings:**
- Experiments limited to small MDPs (S = 4, H = 8)
- No evaluation of negative instances or scalability across varying state/action space sizes
- Benchmark relies on perfect knowledge of V* - μ₀, which is unlikely in practice
- No investigation into function approximation scenarios or other theoretical variants</think>

# 论文总结：Pure Exploration for a Good Policy in Reinforcement Learning with Bandit Feedback

## 1. 核心问题与整体含义（研究动机和背景）

- **研究背景**：强化学习中的纯探索（Pure Exploration）问题传统上聚焦于最优策略识别（BPI），即以高置信度识别一个近最优策略。然而在许多实际应用中，找到一个"足够好"的策略即可满足需求，无需精确找到最优策略。

- **核心问题**：作者提出**良好策略识别（Good Policy Identification, GPI）**问题。具体而言，给定奖励阈值 μ₀，GPI 要求在存在期望奖励不低于 μ₀ 的策略时识别出这样一个策略（正向实例）；若不存在则声明 None（负向实例）。这是在固定置信度设置下，要求输出正确概率 ≥ 1 - δ，并最小化期望样本复杂度。

- **实际动机**：论文列举了多个应用场景——医学治疗方案（与标准护理比较）、云计算服务等级协议（SLA）、投资策略的夏普比率基准等，在这些场景中只需判断新策略是否"达标"，而非寻找全局最优。

## 2. 论文提出的方法论

### 核心思想

- **BEE-GPI 算法**（Balanced Exploration-Exploitation for Good Policy Identification）：一种新颖的 δ-PAC 算法，通过早期停止的 BPI 子程序作为 oracle 来实现高效的样本复杂度。

### 关键技术细节

- **双阶段设计**：算法分为探索阶段（Exploration）和利用阶段（Exploitation）：
  1. **探索阶段**：调用 ES-BPI-UCRL（Early-Stopping BPI-UCRL），基于乐观采样策略估计转移概率，使用置信区间构建乐观值函数 V 和悲观值函数 V。
  2. **决策分支**：根据 oracle 输出决定终止、利用或进入下一阶段。
  3. **利用阶段**：验证候选策略是否真正满足阈值 μ₀，通过收集多个样本估计其真实价值并构建置信区间。

- **停止条件**：定义了两个关键停止准则：
  - 正向实例条件：V_{t,π_t}(s0) - (C-1)(V_t(s0) - V_{t,π_t}(s0)) > μ₀
  - 负向实例条件：V_t(s0) < μ₀

### 算法流程（简述）

1. 初始化参数，每轮递减精度 ε_k 和容错率 δ_k
2. 调用 ES-BPI-UCRL 进行探索，检查是否满足停止条件
3. 若找到候选策略，进行利用阶段验证
4. 若满足阈值则输出策略，否则进入下一阶段

## 3. 实验设计

### 数据集与场景

- **两个经典 MDP 环境**：
  - **Single Chain**：始终从状态 0 开始
  - **Double Chain**：始终从状态 1 开始
- **参数设置**：S = {0, 1, 2, 3}（共 4 个状态），A = {left, right}（2 个动作），H = 8，δ = 0.001
- **转移动态**：意图方向概率 0.9，反向概率 0.1

### Benchmark 与对比方法

- **对比算法**：BPI-UCRL（来自 Kaufmann et al. 2021）
  - 给定 oracle 参数 ε = V* - μ₀，作为显著的先验知识优势（这在实践中不太可能获得）
- **阈值设置**：μ₀ ∈ {1, 1.5, 2.0, 2.5, 3.0}，确保所有实例为正向实例

### 实现细节

- 使用 MOSEK 优化求解器实现
- 所有测试实例均为正向实例（V* > μ₀）

## 4. 资源与算力

- **处理器**：Intel i5-14500 CPU
- **总计算时间**：约 72 小时
- **实验规模**：20 次独立运行（每个阈值设置），总计约 1440 个实验实例（20 trials × 5 thresholds × 2 environments，但论文未明确说明）

## 5. 实验数量与充分性

### 实验数量

- **20 次独立试验**（每个设置）
- **5 个阈值**（1.0, 1.5, 2.0, 2.5, 3.0）
- **2 个环境**（Single Chain, Double Chain）

### 实验充分性评估

**相对充分的方面**：
- 独立重复实验保证了统计稳定性（误差条仅显示 3 个标准差，较小）

**不充分的方面**：
- **环境规模过小**：仅测试了 S=4, A=2, H=8 的小规模 MDP，未验证算法在更大状态/动作空间下的表现
- **仅测试正向实例**：未展示负向实例的实验结果
- **缺乏消融实验**：未对算法的各个组件（如探索预算 T^ee_k、利用阶段参数等）进行 ablation study
- **对比方法不公平**：BPI-UCRL 被给予完美的先验知识（ε = V* - μ₀），这在实际中不可行
- **缺乏与其他基线的比较**：仅对比了一种方法

## 6. 论文的主要结论与发现

1. **BEE-GPI 在所有阈值设置下均显著优于 BPI-UCRL**，需要更少的回合数来识别合格策略。

2. **理论结果**：
   - 对于正向实例，样本复杂度上界中 log(1/δ) 的系数为 O(H²/(V* - μ₀)²)，且**不依赖状态和动作空间大小**
   - 对于负向实例，样本复杂度仍与 S 和 A 相关
   - 通过下界证明了 1/(V* - μ₀)² 项的必要性

3. **关键发现**：通过将 log(1/δ) 系数与状态-动作空间大小解耦，BEE-GPI 实现了比标准 BPI 归约更紧致的样本复杂度。

## 7. 优点

1. **问题定义新颖**：首次系统性地形式化了 GPI 问题，为"足够好"策略识别的实际需求提供了理论框架。

2. **理论贡献突出**：
   - 证明了对于正向实例，样本复杂度的 log(1/δ) 系数与状态/动作空间大小无关
   - 建立了接近最优的下界，显示上界与下界之间仅相差 S、A、H 的多项式因子

3. **算法设计巧妙**：通过早期停止机制和探索-利用平衡，有效降低了样本复杂度。

4. **实验验证清晰**：通过清晰的图表和数值结果验证了理论发现。

## 8. 不足与局限

1. **实验覆盖不足**：
   - 仅在小规模 MDP（S=4, A=2）上验证，大规模场景的适用性未知
   - 仅展示正向实例结果，未验证负向实例的实验表现

2. **对比不够公平**：BPI-UCRL 被给予完美的先验知识（知道 V* - μ₀），这在实践中是不可能的，导致对比偏向有利于 BEE-GPI。

3. **缺乏鲁棒性验证**：未进行消融实验验证各组件的贡献。

4. **应用场景假设过强**：
   - 假设奖励函数确定且有界 [0,1]
   - 未考虑部分可观测或连续空间场景

5. **未来工作方向**：论文提到的未来研究方向包括推导出更紧致的实例依赖边界，以及使用函数近似扩展到大尺度 MDP，但当前版本未涉及这些。

（完）
