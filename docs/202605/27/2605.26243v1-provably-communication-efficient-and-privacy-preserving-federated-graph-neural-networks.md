---
title: Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks
title_zh: 可证明通信高效且隐私保护的联邦图神经网络
authors: "Zhishuai Guo, Wenhan Wu, Chen Chen, Lei Zhang, Olivera Kotevska, Ravi K Madduri"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26243v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 通信高效的联邦GNN框架
tldr: 针对跨机构图数据因隐私限制无法共享，且现有联邦图神经网络（GNN）在处理跨客户端边时面临通信开销大和隐私泄露风险的问题，本文提出了CE-FedGNN框架。该框架通过低频交换聚合节点表征并引入移动平均估计器处理跨客户端依赖，结合度量差分隐私（metric-DP）提供严格的隐私保护。研究证明了其收敛性与极低的通信复杂度，实验表明该方法在保障隐私的同时大幅降低了通信成本并保持了优异性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26243v1/fig-001.webp\", \"caption\": \"Table 3: Statistics of the Datasets\", \"page\": 22, \"index\": 1, \"width\": 572, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2605.26243v1/fig-002.webp\", \"caption\": \"Table 1: Average F1 scores on AML datasets across high-illicit (HI) and low-illicit (LI) ratio settings. Best results in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 185}]"
motivation: 现有联邦图神经网络在处理跨客户端边时，要么牺牲模型精度，要么面临高昂的通信开销与隐私泄露风险。
method: 提出CE-FedGNN框架，利用移动平均估计器低频交换聚合节点表征以处理跨客户端依赖，并采用度量差分隐私（metric-DP）机制提供严格的隐私保障。
result: "理论证明该算法以O(1/√T)的速率收敛且通信复杂度仅为O(T^{3/4})，实验显示其在反洗钱和引用网络数据集上大幅降低通信开销并保持高精度。"
conclusion: CE-FedGNN成功在跨机构耦合图数据上实现了通信高效与隐私保护的完美平衡，为联邦图学习提供了一种极具潜力的实用解决方案。
---

## 摘要
图神经网络（GNNs）在关系数据上取得了优异的性能，但由于隐私和政策限制，现实世界中的图通常分布在无法共享原始数据的各个组织中。现有的联邦GNN方法要么忽略跨客户端链接，导致准确率下降；要么需要频繁交换嵌入，从而产生巨大的通信和隐私成本。我们提出了CE-FedGNN，一个通信高效且隐私保护的联邦GNN框架，用于在此类耦合图上进行学习。我们的方法通过不频繁地交换聚合的节点表示，避免了共享原始数据或每轮的嵌入。为了处理跨客户端的依赖性和滞后性，我们引入了一个移动平均估计器，该估计器持续跟踪节点表示，并使其能够在各轮次中稳定复用。为了对发布的表示提供严格的隐私保证，我们采用了度量差分隐私（metric-DP）框架，该框架通过在学习到的嵌入空间中的距离来衡量隐私，而不是基于最坏情况的输入扰动。这使得在标准差分隐私变得过于保守的噪声水平下，仍能提供有意义的保证。我们证明了其以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度为 $O(T^{3/4})$。此外，我们在公共队列威胁模型下，通过Rényi差分隐私组合推导出了 $(\varepsilon,δ)$-metric-DP 保证。在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN在实现优异性能的同时，显著降低了通信开销，并在隐私保护噪声下保持了鲁棒性。

## Abstract
Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **核心背景**：图神经网络（GNN）在处理关系数据时表现优异，但在现实场景（如反洗钱检测）中，图数据通常分布在不同的机构（客户端）中。由于隐私法规（如GDPR）和商业机密限制，各机构无法直接共享原始图数据。
* **现有痛点**：在联邦学习（FL）环境下训练GNN面临“跨客户端边”的挑战。现有的联邦GNN方法存在严重缺陷：
  1. **忽略跨客户端边**：导致图结构信息丢失，模型准确率大幅下降。
  * **频繁交换嵌入**：每轮迭代都交换节点表征，导致通信开销巨大，且极易引发隐私泄露。
* **整体含义**：本文旨在解决跨机构耦合图数据在联邦环境下的训练难题，寻求在**模型精度**、**通信效率**和**隐私保护**三者之间实现最优平衡。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程
* **核心思想**：提出 **CE-FedGNN** 框架。通过**低频交换**聚合的节点表征来捕获全局图结构，并引入**移动平均估计器**处理跨客户端依赖，同时结合**度量差分隐私**提供严格的隐私保障。
* **关键技术细节**：
  * **移动平均估计器**：针对跨客户端节点，客户端不进行实时通信，而是复用该节点最近一次共享的表征。为了缓解表征滞后带来的误差，引入移动平均机制持续跟踪并平滑更新节点表征。
  * **跨客户端梯度处理**：当采样到跨客户端的边时，两端客户端各自计算本地节点的梯度分量，将跨客户端节点的表征视为固定输入。通过适当的采样加权，聚合后的更新可恢复为无偏的全局梯度估计。
  * **度量差分隐私**：传统DP针对最坏情况添加噪声，导致实用性极差。本文采用 metric-DP，基于学习到的嵌入空间中的 L2 距离来衡量隐私，在适度噪声下即可提供有意义的 $(\varepsilon, \delta)$-metric-DP 保证。
* **算法流程**：
  1. **初始化**：服务器初始化全局模型参数和全局嵌入缓冲区。
  2. **本地更新**：客户端接收模型和所需的跨客户端表征。在本地进行 $K$ 次迭代，每次迭代通过小批量采样，利用移动平均规则更新节点嵌入和梯度估计器，并更新本地模型。
  3. **通信与聚合**：每 $K$ 步通信一次。客户端将更新后的模型、梯度估计器和边界节点嵌入发送给服务器。服务器进行平均聚合，并将嵌入路由给相关的邻居客户端。
  4. **隐私注入**：在通信传输前，对共享的节点嵌入、模型参数和梯度分别注入高斯噪声。

### 3. 实验设计：数据集、场景与对比方法
* **数据集与场景**：
  * **反洗钱（AML）场景**：采用合成的银行间金融交易网络（包含 Small, Medium, Large 三种规模，以及高/低非法交易比例 HI/LI 两种设定）。
  * **引文网络场景**：采用四个真实世界的公开基准数据集。
* **对比方法**：
  * **单客户端（SC）**：本地独立训练。
  * **传统联邦**：FedAvg（仅传模型）。
  * **联邦GNN基线**：Swift-FedGNN（间歇性全局图训练）、FedGCN（仅初始化时共享嵌入）、FedGNN-ST（朴素共享滞后嵌入）、FedSage+（本地生成缺失节点）、FedPUB（加权聚合模型更新）。

### 4. 资源与算力
* **明确说明的算力资源**：实验在一个通过 InfiniBand 连接的集群上进行，集群中的每台机器配备了**单张 NVIDIA A100 GPU**。
* **训练时长**：文中给出了每轮训练的平均运行时间（例如在 HI-Large 数据集上，CE-FedGNN-PNA 每轮耗时约 164.73 秒），但未明确说明总训练时长或总 GPU 卡时消耗。

### 5. 实验数量与充分性
* **实验数量**：实验涵盖了 2 大类场景（金融反洗钱、引文网络），共计 10 个不同的数据集设定；对比了至少 7 种基线方法；进行了组件消融实验、通信间隔 $K$ 的影响实验、隐私噪声 $\sigma_0$ 的鲁棒性实验、属性推断攻击（AIA）防御测试以及运行时间对比。
* **充分性与客观性**：实验设计**非常充分且客观公平**。除了常规的精度对比，还专门针对通信字节数、运行时间进行了权衡分析；在隐私评估方面，不仅给出了理论保证，还通过实际的攻击实验（AIA）验证了经验隐私强度；超参数设置在对比方法中保持了一致。

### 6. 论文的主要结论与发现
* **理论证明**：算法以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度仅为 $O(T^{3/4})$。
* **性能优越**：在绝大多数 AML 和引文网络数据集上，CE-FedGNN 的 F1 分数显著优于所有基线，特别是在类别极度不平衡（LI 设定）的情况下，其他基线大多崩溃失效，而本方法仍保持鲁棒。
* **通信高效**：即使将通信间隔扩大到 $K=1024$，模型性能下降极小，证实了移动平均机制能有效支持嵌入的长效复用。
* **隐私与效用兼顾**：在提供严格 metric-DP 保证的噪声水平下，模型性能仅出现轻微下降，且对属性推断攻击表现出了极强的抵抗力。

### 7. 优点：方法或实验设计上的亮点
* **创新的优化机制**：巧妙地将移动平均估计器应用于节点表征和梯度计算，成功解耦了跨客户端的多跳依赖，大幅降低了通信频率。
* **合理的隐私定义**：摒弃了过于保守的传统 DP，引入 metric-DP 来保护发布在嵌入空间中的表征，这在联邦GNN的中间件隐私保护中非常贴合实际。
* **坚实的理论基础**：在复杂的耦合图和多层级组合优化场景下，给出了严格的收敛性和隐私组合保证。
* **极具现实意义的验证**：针对金融反洗钱这一典型且困难的联邦图学习场景进行了深入验证，证明了其实用价值。

### 8. 不足与局限
* **数据真实性局限**：由于现实金融数据的强监管限制，AML 实验完全依赖于模拟器生成的合成数据，尽管模拟器力求真实，但仍可能与真实的洗钱拓扑分布存在偏差。
* **隐私增强的缺失**：文中明确指出，由于协议中客户端明确广播更新，因此未利用泊松子采样放大等隐私增强技术。若能结合安全聚合或匿名混洗，隐私边界还可以进一步收紧。
* **仅限一跳交互**：为了控制复杂度，跨客户端交互被严格限制在一跳邻居节点，对于需要深层多跳跨域交互的复杂图模式，其表达能力可能受到一定限制。

（完）
