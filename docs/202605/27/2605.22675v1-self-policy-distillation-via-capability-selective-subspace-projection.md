---
title: Self-Policy Distillation via Capability-Selective Subspace Projection
title_zh: 自策略蒸馏：通过能力选择性子空间投影
authors: "Guangya Hao, Yitong Shang, Yunbo Long, Zhuokai Zhao, Hanxue Liang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22675v1"
tags: ["query:self-distillation"]
score: 9.0
evidence: 通过子空间投影实现LLM自蒸馏
tldr: 自蒸馏通过模型自身生成进行训练，但现有方法依赖外部信号或缺乏能力选择性，导致信号稀释。本文提出自策略蒸馏（SPD），从模型梯度的正确定义令牌中提取低阶能力子空间，在自我生成时将KV激活投影到该子空间，随后用标准语言建模损失微调。实验表明SPD在代码生成、数学推理和多选QA等任务上取得显著改进，无需外部信号即可实现通用且具有能力选择性的提升，并展现出卓越的跨域泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22675v1/fig-001.webp\", \"caption\": \"Figure 1: SPD comparisons and performance summary. Top left: comparison with existing selfdistillation methods across three key axes. Top right: average improvement of SPD over the base model for each of the five LLM backbones, computed across three capability domains and six datasets. Bottom: SPD improves base model performance across domain-specific benchmarks, in-domain transfer, and out-domain transfer settings. All metrics in the figure are reported as accuracy (%).\", \"page\": 2, \"index\": 1, \"width\": 799, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2605.22675v1/fig-002.webp\", \"caption\": \"Table 1: Domain-specific and in-domain generalization results under different backbone models.\", \"page\": 7, \"index\": 2, \"width\": 830, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2605.22675v1/fig-003.webp\", \"caption\": \"Figure 3: Self-generated data output comparisons under Qwen2.5-0.5B-Instruct. Black text denotes task-relevant content, while red text highlights answer-irrelevant or low-value content.\", \"page\": 8, \"index\": 3, \"width\": 816, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2605.22675v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of SPD. SPD operates in two phases: Phase 1 (top) (§3.2) extracts low-rank K/V capability subspaces from gradients computed on a small calibration set using correctness-aligned loss, and Phase 2 (bottom) (§3.3) uses these subspaces as projection hooks to steer self-generation without modifying model parameters. The hooked model produces raw completions, after which the hooks are removed and the original model is fine-tuned on the resulting prompt–completion pairs using standard next-token prediction loss.\", \"page\": 4, \"index\": 4, \"width\": 819, \"height\": 424}, {\"url\": \"assets/figures/arxiv/2605.22675v1/fig-005.webp\", \"caption\": \"Table 7: SPD hyperparameters.\", \"page\": 16, \"index\": 5, \"width\": 871, \"height\": 325}]"
motivation: 现有自蒸馏方法依赖外部信号或忽略能力选择性，导致难以针对性地提升模型特定能力。
method: 从模型梯度中提取正确定义令牌的低阶能力子空间，在自我生成时将KV激活投影到该子空间，随后进行标准语言建模微调。
result: "SPD在代码生成、数学推理和多选QA等任务上分别实现最高13%的提升，并在跨域泛化设置中提升15%。"
conclusion: 无需外部信号即可实现通用且具有能力选择性的自蒸馏，显著提升模型性能并展现卓越泛化能力。
---

## 摘要
自蒸馏通过在模型自身的生成上进行训练来引导大型语言模型（LLMs）。然而，现有方法要么依赖外部信号来筛选自生成输出（例如正确性过滤、执行反馈和奖励搜索），这些信号成本高昂且在表现最佳的尖端模型上不可用；要么完全跳过筛选，直接在所有原始输出上进行训练，这种方法往往是领域特定的，难以泛化。两者还有一个更深层的弱点：自生成输出将任务相关的能力与其他因素（如风格模式、格式伪影和模型特定的错误）纠缠在一起，从而稀释了针对特定能力的改进信号。本文提出自策略蒸馏（SPD），在不依赖任何外部信号的情况下实现可泛化、选择性的能力提升。具体而言，SPD从模型在正确性定义 token 上的梯度中提取低秩能力子空间，在自生成阶段将键值（KV）激活投影到该子空间，并使用标准的下一个 token 预测损失对得到的原始输出进行微调。在代码生成、数学推理和多选问答等多个任务上的广泛实验表明，SPD在不使用外部信号的前提下，相比最先进的自蒸馏方法提升高达13%，相比预训练基线提升高达16%。值得注意的是，SPD在域外泛化设置下表现出色，性能提升约15%。

## Abstract
Self-distillation bootstraps large language models (LLMs) by training on their own generations. However, existing methods either rely on external signals to curate self-generated outputs (e.g., correctness filtering, execution feedback, and reward search), which are costly and unavailable for the best-performing frontier models, or skip curation entirely and train on all raw outputs, an approach that is often domain-specific and hard to generalize. Both also share a deeper weakness that self-generated outputs entangle task-relevant capability with others, such as stylistic patterns, formatting artifacts, and model-specific errors, diluting the signal for the specific capability one aims to improve. In this paper, we propose Self-Policy Distillation (SPD), which achieves generalizable, capability selective without any external signal. Specifically, SPD extracts a low-rank capability subspace from the model's own gradients on correctness-defining tokens, projects key-value (KV) activations into this subspace during self-generation, and fine-tunes on the resulting raw outputs with standard next-token prediction loss. Through extensive experiments across code generation, mathematical reasoning, and multiple-choice QA, we show that SPD achieves up to 13% improvement over state-of-the-art self-distillation methods without external signals and up to 16% improvement over pre-trained baselines. Notably, SPD demonstrates superior generalizability, achieving 15% better performance under out-of-domain generalization settings.

---

## 论文详细总结（自动生成）

<think>让我仔细阅读这篇论文，然后按照要求生成详细的中文总结。

论文标题：Self-Policy Distillation via Capability-Selective Subspace Projection（自策略蒸馏：通过能力选择性子空间投影）

作者：Guangya Hao, Yitong Shang, Yunbo Long, Zhuokai Zhao, Hanxue Liang（来自剑桥大学、香港科技大学、芝加哥大学）

核心内容：
1. 研究动机：现有自蒸馏方法存在问题，要么依赖外部信号（成本高、不适用于最强模型），要么跳过筛选（领域特定、难以泛化），且自生成输出会将任务相关能力与其他因素纠缠在一起。

2. 提出的方法：Self-Policy Distillation (SPD)
   - Phase 1：从模型在正确性定义token上的梯度中提取低秩能力子空间
   - Phase 2：在自生成时将KV激活投影到该子空间，然后微调

3. 实验：在代码生成（MBPP、CodeAlpaca）、数学推理（GSM8K、SVAMP）、多选QA（MMLU、BBH）上测试

4. 主要发现：SPD在不依赖外部信号的情况下取得显著提升，跨域泛化能力优秀

让我按照要求的8个要点来组织内容。
</think>

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

### 研究问题
自蒸馏（Self-distillation）通过在模型自身生成上进行训练来提升大型语言模型（LLMs）的性能，但现有方法存在以下核心问题：

- **外部信号依赖**：传统自蒸馏方法依赖外部信号（如正确性过滤、执行反馈、奖励搜索）来筛选自生成输出，这些信号成本高昂，且对于表现最佳的尖端模型不可用
- **领域特定性**：简单自蒸馏（SSD）等方法跳过筛选直接在原始输出上训练，但仅针对代码生成设计，跨领域泛化能力不明确
- **信号稀释问题**：自生成输出将任务相关能力与风格模式、格式伪影、模型特定错误等因素纠缠在一起，稀释了针对特定能力的改进信号

### 核心挑战
如何在**不依赖任何外部信号**的前提下，实现**可泛化**且**具有能力选择性**的自蒸馏？

### 论文目标
提出 Self-Policy Distillation (SPD)，实现：
- ✅ 无需外部信号
- ✅ 可泛化至不同领域
- ✅ 能力选择性（针对目标能力）

---

## 2. 论文提出的方法论

### 核心思想
SPD 将自生成过程本身进行内部转换：先从模型自身的梯度中提取"能力子空间"，再通过投影钩子引导自生成，使输出更聚焦于目标能力，最后用标准语言建模损失微调。

### 关键技术细节

#### 2.1 正确性对齐损失（Correctness-Aligned Loss）
不同于标准语言建模损失（覆盖所有 token），SPD 仅在**正确性定义 token** 上计算损失：
- 代码生成：使用 assertion 相关的 token（5-20个）
- 数学推理：使用最终数值答案 token（1-3个）
- 多选QA：使用答案字母 token（1个）

$$
\mathcal{L}_{\text{align}}(q^{(i)}, y^{(i)}) = -\frac{1}{|S^{(i)}|}\sum_{t \in S^{(i)}} \log p_{\theta_{\text{old}}}(z_t^{(i)} | z_{<t}^{(i)})
$$

#### 2.2 梯度收集与子空间提取
- 对每个校准样本执行一次前向-后向传播
- 在目标层收集 K/V 激活相对于对齐损失的梯度
- 汇总为梯度矩阵 $G_K^{(\ell)} \in \mathbb{R}^{M \times d_k}$ 和 $G_V^{(\ell)} \in \mathbb{R}^{M \times d_v}$
- 对梯度矩阵进行 SVD 分解，保留 top-r 右奇异向量
- 构建投影矩阵：$P_K^{(\ell)} = V_K^{(\ell,r)}(V_K^{(\ell,r)})^\top$

#### 2.3 投影钩子自生成
在自回归生成时，在目标层插入投影钩子：
$$
\tilde{K}^{(\ell)} = K^{(\ell)}P_K^{(\ell)}, \quad \tilde{V}^{(\ell)} = V^{(\ell)}P_V^{(\ell)}
$$
钩子仅过滤中间 KV 激活，不修改模型参数。

#### 2.4 两阶段算法流程

| 阶段 | 操作 | 说明 |
|------|------|------|
| **Phase 1** | 能力子空间提取 | 使用校准集计算对齐损失 → 收集梯度 → SVD → 投影矩阵 |
| **Phase 2** | 能力选择性蒸馏 | 应用投影钩子 → 自生成原始输出 → 移除钩子 → LoRA 微调 |

---

## 3. 实验设计

### 数据集与场景

| 能力领域 | 训练/校准数据集 | 测试数据集 |
|----------|----------------|------------|
| 代码生成 | MBPP | MBPP, CodeAlpaca-20k |
| 数学推理 | GSM8K | GSM8K, SVAMP |
| 多选问答 | MMLU | MMLU, BBH |

### 评估指标
- **MBPP**：PASS@1（正确性）
- **GSM8K/SVAMP**：精确匹配准确率
- **MMLU**：答案字母准确率
- **BBH**：标准化精确匹配
- **CodeAlpaca**：负对数似然（NLL，越低越好）

### 对比方法
1. **Base Model**：原始预训练学生模型
2. **PSR (Plain Self-Retraining)**：在原始自生成输出上直接微调，无投影钩子
3. **SSD (Simple Self-Distillation)**：使用截断解码的自蒸馏基线

### 模型后端
- Qwen2.5-0.5B-Instruct
- Qwen2.5-7B-Instruct
- Qwen2.5-14B-Instruct
- Qwen3-4B-Instruct
- Llama-3.1-8B-Instruct

### 实验设置
- **域内泛化测试**：同一能力领域内从源数据集迁移到目标数据集
- **跨域泛化测试**：从一个能力领域（如QA）迁移到其他领域（如Math、Code）

---

## 4. 资源与算力

论文明确说明：
- **硬件**：4× NVIDIA A100-80G GPUs
- **训练时长**：所有模型训练 5 个 epoch
- **LoRA 配置**：rank=8, α=8, dropout=0.05, 学习率 1×10⁻⁵
- **校准集规模**：每个领域使用 50 个标注样本
- **训练集规模**：代码 464, 数学 7473, QA 2000

⚠️ 论文未详细说明每个实验的具体耗时或总 GPU 小时数。

---

## 5. 实验数量与充分性

### 实验规模

| 实验类型 | 数量 | 说明 |
|----------|------|------|
| 主实验（表格1） | 5个模型 × 6个数据集 | 域内性能和泛化 |
| 跨域泛化（表格2） | 1个模型 × 6个数据集 | QA校准 → Math/Code |
| 生成数据质量（表格3） | 3种生成策略 × 6个数据集 | 生成前对比 |
| 微调后性能（表格4） | 3种方法 × 6个数据集 | 微调后对比 |
| 消融实验（表格5） | 2种损失函数 × 6个数据集 | 对齐损失 vs 全文损失 |
| 校准集大小实验（图4） | 5种规模 × 4个数据集 | 20~500样本 |

### 实验充分性评估

✅ **充分性**：
- 多模型后端覆盖（5个不同规模/系列的模型）
- 多能力领域覆盖（代码、数学、QA）
- 域内和跨域泛化测试完备
- 包含定性和定量分析
- 消融实验验证了核心设计（对齐损失）

⚠️ **潜在不足**：
- 未在更多高风险/复杂任务上验证
- 未使用最大规模模型（如70B+）
- 校准集大小实验仅测试了20-500范围，未探索极小样本场景

---

## 6. 论文的主要结论与发现

### 6.1 性能提升
- **相比最优自蒸馏方法（无外部信号）**：提升高达 **13%**
- **相比预训练基线**：提升高达 **16%**
- **跨域泛化**：提升高达 **15%**

### 6.2 关键发现

| 发现 | 详细说明 |
|------|----------|
| **能力选择性有效** | 跨域实验中，仅用QA校准提取的子空间即可提升Math和Code性能，说明提取的是可迁移的推理信号 |
| **对齐损失优于全文损失** | 消融实验显示，只在正确性定义token上计算损失显著优于全文损失 |
| **校准数据效率高** | 仅需20-500个标注样本即可提取有效的能力子空间 |
| **生成质量改善** | SPD生成的数据更紧凑、更聚焦，减少了任务无关的噪声 |

### 6.3 定性分析亮点
- **代码领域**：SPD生成更简洁、更实现聚焦的代码，减少冗余解释和误导性说明
- **数学领域**：输出更结构化、答案导向，遵循逐步推理格式
- **QA领域**：更好地保持局部问题类型和目标推理区域，避免泛化为通用数据集风格问题

---

## 7. 优点

### 7.1 方法设计亮点

| 优点 | 说明 |
|------|------|
| **无需外部信号** | 完全自包含，突破了对正确性过滤、执行反馈、奖励模型的依赖 |
| **能力选择性** | 通过正确性对齐损失和KV子空间投影精准聚焦目标能力 |
| **可泛化** | 在域内和跨域设置中均展现出强大的迁移能力 |
| **计算高效** | 投影钩子仅增加少量矩阵乘法开销，校准阶段只需小规模标注数据 |
| **即插即用** | 投影钩子仅在生成时使用，微调阶段恢复标准自蒸馏流程 |

### 7.2 实验设计亮点

| 优点 | 说明 |
|------|------|
| **多领域覆盖** | 代码、数学、QA 三个能力领域 |
| **多模型验证** | 覆盖 0.5B 到 8B 多种规模，两个模型家族（Qwen、Llama） |
| **对比基线全面** | 包含原始模型、直接微调基线、SSD等 |
| **消融实验完整** | 验证了核心组件（对齐损失）的必要性 |
| **定性与定量结合** | 既有数值性能对比，也有生成样例的直观展示 |

### 7.3 理论贡献
- 提出了"内部自策略转换"的新视角，将蒸馏目标定义为模型自身经过能力过滤的策略
- 建立了梯度空间与能力表示的联系，证明低秩SVD方向足以捕捉任务相关信号

---

## 8. 不足与局限

### 8.1 实验覆盖局限
- ❌ 未在更大规模模型（如 Llama-3.1-70B、Qwen-72B）上验证
- ❌ 未测试更多样化和高风险任务（如代码漏洞修复、复杂数学证明）
- ❌ 跨域泛化仅测试了一个方向（QA→Math/Code），未测试反向或其他组合

### 8.2 方法局限
- ❌ **正确性定义 token 需要人工指定**：对于新任务需要领域知识来定义正确性span
- ❌ **投影秩 r 需调参**：虽默认使用一半秩，但未系统探索最优秩选择
- ❌ **目标层选择需先验**：默认选择中间层和后层，未验证其他选择的影响

### 8.3 偏差与泛化风险
- ⚠️ **校准集偏差**：如果校准集不具代表性，提取的能力子空间可能偏向特定模式
- ⚠️ **任务类型限制**：主要验证了代码、数学、QA任务，其他能力（如对话、摘要）未测试
- ⚠️ **投影可能丢失有用信息**：低秩投影可能在抑制噪声的同时过滤掉部分有用信号

### 8.4 应用限制
- ❌ 对于无法定义明确"正确答案"的任务（如开放式生成）不适用
- ❌ 需要至少几十个标注样本来构建校准集，冷启动场景受限
- ❌ 未在实际生产环境（如API调用成本）中验证成本效益

---

## 总结

本文提出的 Self-Policy Distillation (SPD) 通过**从模型自身梯度中提取能力子空间**，并利用**KV投影钩子**引导自生成过程，成功实现了**无需外部信号**、**可泛化**且**具有能力选择性**的自蒸馏。实验覆盖代码、数学、QA三个领域，在5个模型后端上验证了方法的有效性，并展现出强大的跨域迁移能力。方法的核心创新在于将"能力选择"前置到生成阶段，而非仅在筛选阶段过滤。

（完）
