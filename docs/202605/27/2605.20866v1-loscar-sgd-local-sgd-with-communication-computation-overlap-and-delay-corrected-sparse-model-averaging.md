---
title: "LOSCAR-SGD: Local SGD with Communication-Computation Overlap and Delay-Corrected Sparse Model Averaging"
title_zh: LOSCAR-SGD：具备通信-计算重叠与延迟校正稀疏模型平均的局部SGD
authors: "Yassine Maziane, Ammar Mahran, Artavazd Maranjyan, Peter Richtárik"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20866v1"
tags: ["query:dgnn-nas"]
score: 8.0
evidence: 结合通信压缩与计算通信重叠的Local SGD方法
tldr: 在分布式学习中，通信开销是主要瓶颈。本文针对异构计算环境提出了LOSCAR-SGD算法，创新性地结合了通信压缩、局部训练和通信计算重叠三种技术。通过引入延迟校正的稀疏模型平均合并规则，算法在通信进行时能继续优化且不丢失进度。研究首次为这三种技术的结合提供了理论收敛保证，并证实了其在减少训练时间方面的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20866v1/fig-001.webp\", \"caption\": \"Figure 11: Negative stress test with strongly heterogeneous data and very slow communication. The data are partitioned by a label-Dirichlet split with α = 0.03, and the overlap methods use M = 1, ζ = 96, and p = 0.01. The overlap methods make rapid initial progress but then plateau below the blocking local-sparse baseline, indicating that too many local steps during communication can drive the methods toward poor local optima when worker objectives are highly heterogeneous.\", \"page\": 38, \"index\": 1, \"width\": 822, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-002.webp\", \"caption\": \"Table 1: Comparison of representative distributed learning methods with theoretical analysis related to our setting. The columns indicate whether a method explicitly incorporates local steps, compression, communication-computation overlap, and worker-specific local-step counts. A checkmark indicates the presence of the corresponding feature. To the best of our knowledge, our method is the first analyzed method that combines all four features.\", \"page\": 2, \"index\": 2, \"width\": 812, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-003.webp\", \"caption\": \"Figure 12: CIFAR-10 convolutional-network experiment in the normal communication regime, with p = 0.25, M = 4, and ζ = 6. The overlap methods improve over blocking sparse averaging in logical time, while the processed-example curves show that this extra overlapped computation does not reduce accuracy at a fixed amount of data processed.\", \"page\": 39, \"index\": 3, \"width\": 818, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-004.webp\", \"caption\": \"Figure 7: Very heterogeneous worker-speed regime (M = 2, ζ = 20, worker times (1, 2, 10, 20)). Delay correction keeps a moderate advantage over overwrite, showing that the corrected merge remains useful when worker speeds vary substantially.\", \"page\": 34, \"index\": 4, \"width\": 822, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-005.webp\", \"caption\": \"Figure 15: Tiny ImageNet experiment in the communication-stress regime, with p = 0.10, M = 2, and ζ = 24. The longer communication delay makes the advantage of overlap much larger: both overlap variants substantially outperform blocking local sparse averaging in logical time and also remain ahead when compared by processed examples.\", \"page\": 42, \"index\": 5, \"width\": 818, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-006.webp\", \"caption\": \"Figure 5: Long-compute, short-delay regime (M = 8, ζ = 6, worker times (1, 2, 3, 6)). Delay correction gives a small but persistent improvement over overwrite on both training and validation loss.\", \"page\": 33, \"index\": 6, \"width\": 822, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-007.webp\", \"caption\": \"Figure 6: Short-compute, long-delay regime (M = 2, ζ = 24, worker times (1, 2, 3, 6)). This is the regime with the largest gap: overwrite discards a substantial amount of overlap-phase progress, while delay correction preserves it.\", \"page\": 33, \"index\": 7, \"width\": 822, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-008.webp\", \"caption\": \"Figure 8: Sparsity-level ablation on a9a. We vary p ∈ {0.001, 0.01, 0.1, 1.0} with M = 4, ζ = 6, worker times (1, 2, 3, 6), and otherwise identical training settings. Smaller p dramatically reduces the cumulative number of communicated bits while preserving a similar loss trajectory, showing that sparse parameter averaging gives a strong communication-accuracy tradeoff in this homogeneous-data regime.\", \"page\": 35, \"index\": 8, \"width\": 826, \"height\": 563}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-009.webp\", \"caption\": \"Figure 13: CIFAR-10 convolutional-network experiment in the communication-stress regime, with p = 0.10, M = 2, and ζ = 24. The larger communication delay makes the logical-time benefit of overlap more pronounced, while the processed-example plot checks that the additional local steps remain useful when compared at the same number of consumed training examples.\", \"page\": 40, \"index\": 9, \"width\": 818, \"height\": 600}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-010.webp\", \"caption\": \"Figure 9: Ablation on the local computation budget. The blocking local-sparse baseline is sensitive to the local computation budget in early logical time, while the overlap methods are more stable. The delay-corrected rule gives the strongest collapse across the tested budgets.\", \"page\": 36, \"index\": 10, \"width\": 826, \"height\": 614}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-011.webp\", \"caption\": \"Figure 4: Loss curves under equivalent resource parametrizations for the controlled sparse-overlap experiment. Since all three sparse methods use the same round duration and per-round communication volume, logical time and communication cost preserve the round-based ordering.\", \"page\": 32, \"index\": 11, \"width\": 826, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-012.webp\", \"caption\": \"Figure 3: Training gradient norm for the controlled sparse-overlap experiment on a9a. The stationarity metric follows the same ordering as the accuracy plots: delay correction makes the fastest progress, followed by overwrite and then blocking local sparse averaging.\", \"page\": 32, \"index\": 12, \"width\": 516, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-013.webp\", \"caption\": \"Figure 14: Tiny ImageNet experiment in the normal communication regime, with p = 0.25, M = 4, and ζ = 6. Both overlap variants improve over blocking sparse averaging in logical time and in rounds. When plotted against processed examples, the curves are much closer, indicating that the main benefit is the use of communication time for additional useful computation.\", \"page\": 41, \"index\": 13, \"width\": 818, \"height\": 533}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-014.webp\", \"caption\": \"Figure 10: Training loss versus cumulative logical time for the communication-delay ablation. The zero-delay case is omitted because all three methods coincide when ζ = 0. For positive delay, overlap improves over blocking local sparse averaging, and the delay-corrected merge is consistently strongest; the advantage is especially pronounced when the communication delay is large.\", \"page\": 37, \"index\": 14, \"width\": 822, \"height\": 249}, {\"url\": \"assets/figures/arxiv/2605.20866v1/fig-015.webp\", \"caption\": \"Figure 2: Accuracy comparison for the controlled sparse-overlap experiment on a9a. The delaycorrected overlap rule reaches higher accuracy earlier than overwrite, and both overlap rules improve over the blocking local-sparse baseline.\", \"page\": 31, \"index\": 15, \"width\": 826, \"height\": 498}]"
motivation: 分布式学习中通信开销巨大，且现有研究缺乏将通信压缩、局部训练和通信计算重叠这三种标准降本技术结合的理论分析。
method: 提出LOSCAR-SGD算法，在异构计算节点进行局部训练时仅传输稀疏模型参数，并在通信期间继续计算，同时引入延迟校正合并规则以保留重叠阶段的优化进度。
result: 在平滑非凸目标函数下提供了理论收敛保证，明确了稀疏度、重叠和节点异构性对收敛率的影响，实验表明该算法有效缩短了训练时间且优于直接覆盖的合并方式。
conclusion: 本文首次为融合通信压缩、局部训练和通信计算重叠的分布式算法提供了理论基础，并验证了延迟校正机制在提升大规模训练效率方面的优越性。
---

## 摘要
通信是分布式学习中的主要瓶颈，尤其是在大规模场景以及具有慢速链路的联邦学习环境中。降低这一开销的三种标准方法是通信压缩、局部训练以及通信-计算重叠。结合这些要素的方法已在实践中得到应用，并被证实对大规模训练行之有效，但针对结合了所有这三者的方法，目前仍缺乏相关理论。我们研究了一种异构计算环境，其中不同的计算节点（worker）可能执行不同数量的局部步数，并提出了LOSCAR-SGD，这是一种仅通信模型坐标的稀疏子集，并在通信进行期间继续进行优化的局部SGD方法。其关键要素是一项延迟校正合并规则，该规则在合并延迟的同步信息时，不会丢弃在重叠阶段所取得的优化进展。我们针对平滑非凸目标函数给出了收敛性保证，并揭示了稀疏性、重叠以及节点异构性如何影响收敛速率。据我们所知，这是针对此类要素组合的首个理论研究。实验进一步表明，通信-计算重叠减少了训练时间，并且延迟校正合并策略优于简单的直接覆盖。

## Abstract
Communication is a major bottleneck in distributed learning, especially in large-scale settings and in federated learning environments with slow links. Three standard ways to reduce this cost are communication compression, local training, and communication-computation overlap. Methods that combine these ingredients are used in practice and have been found to be effective for large-scale training, but there is little theory for methods that combine all three. We study a heterogeneous-compute setting in which different workers may take different numbers of local steps, and we propose LOSCAR-SGD, a Local SGD method that communicates only a sparse subset of model coordinates and continues optimizing while communication is in flight. A key ingredient is a delay-corrected merge rule that incorporates delayed synchronized information without discarding the progress made during the overlap phase. We give convergence guarantees for smooth non-convex objectives and show how sparsity, overlap, and worker heterogeneity affect the rate. To the best of our knowledge, this is the first theory for this combination of ingredients. Experiments further show that communication-computation overlap reduces training time and that the delay-corrected merge outperforms naive overwriting.

---

## 论文详细总结（自动生成）

以下是对论文《LOSCAR-SGD: 具备通信-计算重叠与延迟校正稀疏模型平均的局部SGD》的详细结构化总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：在分布式机器学习和联邦学习中，节点间的**通信开销**是限制训练速度的主要瓶颈。
*   **研究动机**：目前降低通信开销的主流技术有三种：**通信压缩**（如稀疏化）、**局部训练**（如Local SGD，多次本地更新后再同步）和**通信-计算重叠**（在通信时不让GPU闲置，继续进行本地计算）。虽然业界已经在实践中组合使用这三种技术，但**学术界此前缺乏将这三者结合起来的严格理论分析**。
*   **整体含义**：本文旨在填补这一理论空白，针对计算节点算力异构的场景，提出了一种全新的分布式优化算法，不仅提供了严谨的数学收敛证明，还通过实验验证了其在大规模训练中缩短训练时间的有效性。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程
*   **核心思想**：提出 **LOSCAR-SGD** 算法。允许不同算力的节点在同一个时间窗口内执行不同数量的本地步数；在通信阶段，只传输模型的稀疏子集；并且节点在等待通信完成的过程中继续进行本地优化。
*   **关键技术细节：延迟校正合并规则**
    *   **痛点**：在通信-计算重叠机制下，当服务器返回经过平均的稀疏模型时，本地节点已经向前走了几步（产生了更新的模型）。如果直接用服务器的旧模型**覆盖**本地的新模型，会白白浪费重叠期间的计算进度。
    *   **解决方案**：提出“延迟校正合并”。在收到服务器发来的稀疏平均模型时，不直接覆盖，而是计算一个“校正向量”（即全局平均模型与本地旧模型的差值），将这个校正值**叠加**到本地最新计算的模型上。这样既对齐了全局模型，又保留了重叠期间取得的本地优化进度。
*   **算法流程（文字说明）**：
    1.  **本地预计算**：节点 $i$ 根据自身算力执行 $N_i$ 步本地SGD，得到准备通信的模型 $y_i$。
    2.  **稀疏通信与重叠计算**：节点对 $y_i$ 进行随机稀疏化掩码处理并发送给服务器。在服务器聚合和广播的 $\zeta$ 秒延迟期间，节点**不闲置**，而是从 $y_i$ 开始继续执行 $Q_i$ 步本地SGD，得到更进一步的模型 $z_i$。
    3.  **服务器聚合**：服务器收集所有节点的稀疏消息，计算平均值并广播。
    4.  **延迟校正合并**：节点收到广播后，对于参与通信的坐标，使用公式 $x_{new} = z_i + (\bar{y} - y_i)$ 更新模型；对于未参与通信的坐标，直接保留本地最新模型 $z_i$。

### 3. 实验设计：数据集、场景、基准与对比方法
*   **数据集与场景**：
    *   **主实验**：LIBSVM 中的 `a9a` 数据集（逻辑回归二分类任务）。
    *   **泛化实验**：CIFAR-10（卷积神经网络分类）和 Tiny ImageNet（ResNet-18 架构分类）。
    *   **场景设定**：模拟了异构计算环境（设定4个节点的计算时间比例分别为 1, 2, 3, 6），以及不同的通信延迟和稀疏度。
*   **基准与对比方法**：
    *   `Sync SGD`：无局部步数、无稀疏化、无重叠的同步SGD。
    *   `FedAvg-Full`：有局部步数，但全量模型平均，无重叠。
    *   `Local Sparse`：有局部步数，有稀疏化，但**无重叠**（通信时阻塞等待）。
    *   `Overlap overwrite`：本文方法的变体，有重叠和稀疏化，但收到通信后**直接覆盖**本地参数。
    *   `Overlap delay-corrected`：**本文提出的完整方法**，采用延迟校正合并。

### 4. 资源与算力
*   **未明确说明**：本文的实验主要基于 **PyTorch 逻辑时间模拟器**在单进程内确定性运行的。作者为了排除真实硬件和网络的噪声，纯粹验证算法层面的数学和逻辑效果，并未使用真实的物理分布式集群，因此**文中没有明确提及物理 GPU 型号、数量或真实训练时长**。实验图表多以“逻辑时间”或“通信轮数”作为横轴。

### 5. 实验数量与充分性
*   **实验数量**：进行了大量的对比与消融实验，包括：
    *   3个不同规模的数据集（a9a, CIFAR-10, Tiny ImageNet）。
    *   稀疏度 $p$ 的消融实验（从 0.001 到 1.0）。
    *   本地计算预算 $M$ 的消融实验。
    *   通信延迟 $\zeta$ 的长短消融实验。
    *   极端异构数据分布的压力测试。
*   **充分性与客观性**：实验设计**非常充分且客观**。作者不仅展示了本方法的优势，还专门设计了“极端异构数据分布压力测试”来暴露方法的缺陷和局限性，这种不回避自身算法弱点的求真态度极大地提升了论文的客观性。

### 6. 论文的主要结论与发现
*   **理论证明**：首次在平滑非凸目标函数下，为融合了“局部训练+稀疏压缩+通信重叠”的算法提供了收敛保证。证明了重叠和稀疏化带来的额外误差是可控的高阶项，不会破坏算法的线性加速比。
*   **重叠机制绝对有效**：在逻辑时间维度上，通信-计算重叠机制一致且显著地优于阻塞式的本地稀疏训练。
*   **延迟校正优于直接覆盖**：在通信延迟较大时，直接覆盖会丢弃大量重叠期间的有用计算，而延迟校正合并规则能很好地保留这些进度，表现最佳。
*   **局限性发现**：在数据极度异构的 Non-IID 场景下，如果通信极其缓慢且稀疏度极高，过多的重叠本地步数会导致模型陷入糟糕的局部最优，此时重叠机制反而不如阻塞式训练。

### 7. 优点：方法或实验设计上的亮点
*   **理论突破**：填补了分布式优化领域对“计算-通信重叠+稀疏化”缺乏理论分析的空白。
*   **巧妙的合并规则**：延迟校正合并规则设计得非常直观且数学上优雅，几乎不增加额外计算成本，就解决了重叠带来的“陈旧梯度/模型”问题。
*   **高度贴合实际**：考虑了计算节点的异构性，允许不同节点根据自身算力执行不同的迭代步数，这非常符合真实的分布式集群或联邦学习场景。

### 8. 不足与局限
*   **理论局限**：收敛性分析目前仅限于**数据同构**设定，即所有节点的数据分布是相同的。对于更普遍的异构数据缺乏理论保证。
*   **应用场景限制**：正如作者在压力测试中承认的，在数据高度 Non-IID 且通信极度受限的场景下，重叠机制可能会产生反作用（即本地模型偏离全局目标太远）。
*   **缺乏真实物理系统验证**：所有实验均在逻辑时间模拟器中完成。虽然这有利于验证算法纯理论性质，但在真实网络波动、网卡带宽瓶颈和 GPU 显存限制下，算法是否依然能完美发挥重叠优势，仍有待验证。

（完）
