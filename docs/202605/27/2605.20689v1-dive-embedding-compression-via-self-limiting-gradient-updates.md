---
title: "DIVE: Embedding Compression via Self-Limiting Gradient Updates"
title_zh: DIVE：通过自限梯度更新实现嵌入压缩
authors: Dongfang Zhao
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.20689v1"
tags: ["query:self-distillation"]
score: 8.0
evidence: LLM嵌入压缩技术
tldr: 提出DIVE方法通过自限铰链三元组损失和头-wise NT-Xent对比损失解决嵌入压缩在小数据集上的过拟合问题，在6个BEIR数据集上超越所有基线
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.20689v1/fig-001.webp\",\"caption\": \"Figure 1: Architecture of DIVE. During training, the adapter maps each frozen embedding to H projection heads; the self-limiting triplet loss supervises head 1 only, while the NT-Xent contrastive loss applies to all H heads. At inference, heads 2 through H are discarded and only head 1 is used for retrieval.\", \"page\": 2, \"index\": 1, \"width\": 943, \"height\": 492}, {\"url\": \"assets/figures/arxiv/2605.20689v1/fig-002.webp\", \"caption\": \"Table 2: nDCG@10 at 128 dimensions across six BEIR datasets. SMEC is re-implemented on LLM2VecMistral-7B following Zhang et al. (2025); the original authors report results on LLM2Vec-Qwen2-7B. DIVE results are mean ± std over multiple independent runs.\", \"page\": 7, \"index\": 2, \"width\": 465, \"height\": 466}, {\"url\": \"assets/figures/arxiv/2605.20689v1/fig-003.webp\", \"caption\": \"Table 7: nDCG@10 and Recall@10 at 128 dimensions on the LLM2Vec-Sheared-LLaMA-1.3B backbone. The 2048d frozen column is the uncompressed baseline. DIVE outperforms all three baselines on every dataset under both metrics, and is the only method that never falls below the frozen 2048d baseline. Results are consistent with those on LLM2Vec-Mistral-7B (Table 2).\", \"page\": 14, \"index\": 3, \"width\": 950, \"height\": 219}]"
motivation: LLM高维嵌入带来巨大存储和计算成本，现有压缩方法在标注数据稀缺时严重过拟合
method: 自限铰链损失在满足边界条件后产生零梯度，NT-Xent将多个投影视为隐式视图提供密集自监督梯度
result: 在每个评估压缩率和每个BEIR数据集上均超越基线，是唯一不会低于冻结2048d基线的方法
conclusion: DIVE通过平衡约束和隐式视图集成有效解决了小数据集上的过拟合问题 
---


## 摘要
...

## Abstract
High-dimensional embeddings from large language models impose significant storage and computational costs on vector search systems. Recent embedding compression methods, including Matryoshka-Adaptor (EMNLP 2024), Search-Adaptor (ACL 2024), and SMEC (EMNLP 2025), enable dimensionality reduction through lightweight residual adapters, but their training objectives cause severe overfitting when labeled data is scarce, degrading retrieval performance below the frozen baseline. We propose \textsc{DIVE} (\textbf{D}imensionality reduction with \textbf{I}mplicit \textbf{V}iew \textbf{E}nsembles), a compression adapter that addresses this failure through two mechanisms. First, a self-limiting hinge-based triplet loss produces zero gradient once a triplet satisfies the margin constraint, bounding the total perturbation applied to the pretrained embedding space. Second, a head-wise NT-Xent contrastive loss treats multiple learned projections of each embedding as implicit views, providing dense self-supervised gradients that compensate for the sparsity of the triplet signal on small datasets. Across six BEIR datasets, \textsc{DIVE} outperforms all three baseline adapters on every dataset and at every evaluated compression ratio, with a 14M-parameter open-source implementation.