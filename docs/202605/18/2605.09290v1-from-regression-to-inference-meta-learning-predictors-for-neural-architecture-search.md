---
title: "From Regression to Inference: Meta-Learning Predictors for Neural Architecture Search"
title_zh: 从回归到推断：用于神经架构搜索的元学习预测器
authors: "Liping Deng, MingQing Xiao"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09290v1"
tags: ["query:dgnn-nas"]
score: 8.0
evidence: 用于神经架构搜索的元学习性能预测器
tldr: 在神经架构搜索（NAS）中，基于预测器的方法常因训练样本有限而导致过拟合和泛化能力差。为此，本文提出一种全新的条件函数推断范式，利用具有元学习能力的卷积神经过程（ConvNP）来解决性能预测问题。该方法通过在合成任务集上进行上下文-目标划分训练，显式优化了数据稀缺下的泛化能力，并设计了针对单元架构的元特征。实验表明，该方法显著提升了架构排序质量，在小样本下实现了最优的架构选择。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09290v1/fig-001.webp\", \"caption\": \"Figure 3: The comparisons on Recall@k over NAS201.\", \"page\": 8, \"index\": 1, \"width\": 813, \"height\": 196}, {\"url\": \"assets/figures/arxiv/2605.09290v1/fig-002.webp\", \"caption\": \"Table 2: The comparisons on validation and test accuracies over NAS201.\", \"page\": 7, \"index\": 2, \"width\": 798, \"height\": 296}, {\"url\": \"assets/figures/arxiv/2605.09290v1/fig-003.webp\", \"caption\": \"Table 4: The performance of the ConvNP predictor under various training set sizes.\", \"page\": 9, \"index\": 3, \"width\": 794, \"height\": 196}]"
motivation: 现有的NAS性能预测器通常在有限样本上进行监督回归训练，容易导致过拟合且对未知架构的泛化能力较差。
method: 提出将性能预测建模为条件函数推断问题，利用具有元学习能力的卷积神经过程（ConvNP）从部分观测中推断性能，并设计了针对单元架构的元特征。
result: 在NAS-Bench-101和NAS-Bench-201上的大量实验表明，该方法持续提升了Top-K排序质量，并在有限样本下实现了最优的架构选择。
conclusion: 通过将NAS预测转化为元学习推断问题，该方法有效克服了数据稀缺下的过拟合问题，显著增强了预测器在未知架构上的泛化能力。
---

## 摘要
基于预测的方法在神经架构搜索（NAS）中得到了广泛应用，其中预测器通过估计候选架构的性能来指导选择。然而，现有的预测器通常通过在有限样本上进行监督回归来训练，这会导致过拟合以及对未见架构的泛化能力较差。在这项工作中，我们提出了一种根本不同的表述方式，使用具有元学习能力的卷积神经过程（ConvNP）将性能预测建模为条件函数推断问题。我们的方法没有对有限样本拟合固定的映射，而是通过在一组合成的任务中进行上下文-目标划分的训练，元学习从部分观测中推断性能，明确优化了数据稀缺下的泛化能力，并使训练过程与NAS中的部署设置保持一致。我们进一步为基于单元的架构设计了简单而有效的元特征，并在NAS-Bench-101和NAS-Bench-201上评估了我们的方法。大量实验表明，我们的方法持续提升了Top-K排序质量，并在使用有限样本的情况下实现了最先进的架构选择。

## Abstract
Prediction-based approaches are widely used in neural architecture search (NAS), where a predictor estimates the performance of candidate architectures to guide selection. However, existing predictors are typically trained via supervised regression on limited samples, leading to overfitting and poor generalization to unseen architectures. In this work, we propose a fundamentally different formulation that models performance prediction as a conditional function inference problem using a Convolutional Neural Process (ConvNP) with meta-learning capabilities. Instead of fitting a fixed mapping to limited samples, our approach meta-learns to infer performance from partial observations by training with context-target splits across a group of synthesized tasks, explicitly optimizing for generalization under data scarcity and aligning the training procedure with the deployment setting in NAS. We further design simple yet effective meta-features for cell-based architectures and evaluate our method on NAS-Bench-101 and NAS-Bench-201. Extensive experiments show that our approach consistently improves top-K ranking quality and achieves the state-of-the-art architecture selection using limited samples.

---

## 论文详细总结（自动生成）

以下是对该论文的详细结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
* **研究背景**：在神经架构搜索（NAS）中，基于预测器的方法通过评估候选架构的性能来指导搜索，从而避免对海量候选架构进行全量训练，大幅降低计算成本。
* **核心问题**：现有的性能预测器通常基于有限的样本进行传统的监督回归学习（学习固定的映射关系）。在NAS极度的数据稀缺场景下，这种方法容易导致过拟合，对未知的架构泛化能力极差。此外，传统的回归训练模式与NAS实际部署时的条件推断场景（基于部分已知去推断未知）存在偏差。
* **整体含义**：本研究提出跳出传统的“回归拟合”范式，将NAS性能预测重新定义为“条件函数推断”问题，引入元学习机制来显式优化模型在数据稀缺下的泛化能力。

### 2. 提出的方法论：核心思想与技术细节
* **核心思想**：采用具有元学习能力的卷积神经过程（Convolutional Neural Process, ConvNP）。不再让模型学习固定的架构到性能的映射，而是学习“如何根据部分观测样本（上下文）去推断未知样本（目标）的性能”。
* **元特征设计**：
  * 针对基于单元的架构，手工设计了一组简单有效的元特征向量（NAS-Bench-101为13维，NAS-Bench-201为10维）。
  * 特征包含三大类：统计信息（操作数量、节点数）、架构复杂度（路径权重复杂度、可训练参数量）、结构信息（基于邻接矩阵的边数、最大出入度等）。
* **合成任务生成（数据增强）**：
  * 由于只有极少的真实观测数据，算法通过“有放回重采样”和“随机打乱”将单一数据集构建为大量（如10,000个）合成任务。
  * 在每个任务中，数据被划分为上下文集和目标集，以此模拟NAS中基于部分观测进行推断的真实场景。
* **学习目标与算法流程**：
  * **损失函数**：放弃了ConvNP默认的最大似然损失，转而采用回归损失（MSE）与排序损失（基于边际的成对排序损失）的结合：$L_{total} = L_{MSE} + \lambda L_{rank}$。这既保证了预测的准确性，又强化了候选架构的相对排序能力。
  * **推断流程**：在预测阶段，将已知架构作为上下文输入，预测未知架构的性能。通过多次潜变量采样生成多个预测排序，取平均排名来选出Top-K个最优架构进行真实评估。

### 3. 实验设计
* **数据集 / 场景**：
  * **NAS-Bench-101**：包含约423k个候选架构（CIFAR-10）。
  * **NAS-Bench-201**：包含约15k个候选架构（跨CIFAR-10、CIFAR-100和ImageNet16-120三个数据集）。
* **评估 Benchmark（指标）**：
  * 预测准确性：验证集/测试集准确率。
  * 全局排序质量：Kendall’s tau（肯德尔相关系数）。
  * **实用选择质量（核心关注点）**：Recall@k（Top-K召回率），即在前K个预测中包含真正优秀架构的比例。
* **对比方法**：
  * 传统回归模型：线性回归（LR）、岭回归（RR）、多层感知机（MLP）、随机森林（RF）、XGBoost（XGB）。
  * 基于图神经网络（GCN）的先进预测器：NP、CAP、CARL。
  * 搜索算法基线：随机搜索（RS）、正则化进化（RE）。

### 4. 资源与算力
* 论文中明确提到了算力环境：**所有的实验均在单张 NVIDIA P100 GPU (16GB显存) 上进行**。
* 训练效率极高：由于采用了元特征而非原始图数据，结合合成任务的训练策略，模型通常只需训练 **1个 Epoch** 即可收敛。

### 5. 实验数量与充分性
* **实验数量**：
  * **主实验**：在2个基准测试的4个不同数据集场景下进行了对比实验。
  * **不同采样规模实验**：测试了从50到860个不同训练样本量级下的模型表现（共7组设置）。
  * **消融实验**：设计了6组变体实验，分别验证了不同损失函数组合（MSE、Ranking、ML、ML+Ranking）以及核心组件（去除上下文划分、去除合成任务）的有效性。
* **充分性与公平性**：
  * 实验设计非常充分，覆盖了低/中/高数据量场景，且消融实验逻辑严密。
  * 对比实验较为公平：对于所有传统回归模型，统一使用本文提出的元特征进行训练，并进行严格的5折交叉验证调参；GCN模型则遵循其官方实现。

### 6. 论文的主要结论与发现
* **方法有效性**：ConvNP在极度数据稀缺下表现出色，其Top-K选择质量（Recall@k）和预测准确率均达到或超越了现有SOTA预测器。
* **重要洞察（反直觉发现）**：**全局排序指标（如Kendall’s tau）高，并不代表实际架构选择效果好**。例如，某些GCN方法（如CARL）在全局排序得分上高于ConvNP，但在实际挑选Top-K个最优架构时的召回率却不如ConvNP。因此，NAS预测器应更关注Top-K等选择导向的指标。

### 7. 优点
* **范式创新**：成功将元学习和条件推断引入NAS预测器，解决了小样本下的过拟合问题，且训练目标与实际部署场景高度契合。
* **轻量且高效**：采用简单的人工设计元特征代替复杂的图神经网络，在单张P100上即可快速完成搜索，降低了计算门槛。
* **评估视角的纠偏**：明确指出了NAS领域过度依赖全局排序指标的误区，强调了Top-K召回率在实际应用中的核心价值。

### 8. 不足与局限
* **全局排序能力仍有欠缺**：由于模型推断过程中引入了潜变量的随机采样，导致其预测结果带有一定随机性，其全局排序指标（Kendall’s tau）略逊于专门的排序模型（如CARL）。
* **特征工程的泛化限制**：本文的元特征是针对NAS-Bench-101和201的特定空间手工设计的。面对更庞大、更复杂的搜索空间，这种特征提取方式可能难以直接迁移。
* **基准测试的局限性**：实验仅在相对成熟且规模较小的标准Benchmark上进行，未在真实的、极大规模的现代搜索空间（如基于ResNet/DenseNet变体的空间）中验证其扩展性。

（完）
