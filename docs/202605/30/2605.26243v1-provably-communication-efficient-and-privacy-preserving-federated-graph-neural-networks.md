---
title: Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks
title_zh: 可证明通信高效且隐私保护的联邦图神经网络
authors: "Zhishuai Guo, Wenhan Wu, Chen Chen, Lei Zhang, Olivera Kotevska, Ravi K Madduri"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26243v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 通信高效且隐私保护的联邦图神经网络框架
tldr: 针对跨机构图数据联邦学习中存在的跨客户端边处理、高昂通信开销与隐私泄露问题，本文提出了CE-FedGNN框架。该框架通过低频交换聚合节点表征并引入移动平均估计器处理跨客户端依赖，结合度量差分隐私（metric-DP）提供严格的隐私保护。研究不仅证明了其收敛性与极低的通信复杂度，还在反洗钱和引用网络实验中验证了其高效性与鲁棒性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26243v1/fig-001.webp\", \"caption\": \"Table 3: Statistics of the Datasets\", \"page\": 22, \"index\": 1, \"width\": 572, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2605.26243v1/fig-002.webp\", \"caption\": \"Table 1: Average F1 scores on AML datasets across high-illicit (HI) and low-illicit (LI) ratio settings. Best results in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 185}]"
motivation: 现有的联邦图神经网络方法在处理跨机构图数据时，往往面临忽略跨客户端边导致精度下降，或频繁交换嵌入向量导致通信与隐私成本过高的问题。
method: 提出CE-FedGNN框架，利用移动平均估计器低频交换并稳定复用聚合节点表征，同时采用度量差分隐私（metric-DP）框架在公共队列威胁模型下提供形式化的隐私保障。
result: "理论证明该算法以O(1/√T)的速率收敛且通信复杂度仅为O(T^{3/4})，并在反洗钱和引用网络数据集上展现出高精度、低通信开销及强抗噪鲁棒性。"
conclusion: CE-FedGNN成功在跨机构耦合图学习中实现了通信效率与隐私保护的有效平衡，为隐私敏感的分布式关系数据分析提供了可靠的解决方案。
---

## 摘要
图神经网络（GNNs）在关系数据上取得了优异的性能，但由于隐私和政策限制，现实世界中的图通常分布在无法共享原始数据的各个组织中。现有的联邦GNN方法要么忽略跨客户端链接，导致准确率下降；要么需要频繁交换嵌入，从而产生巨大的通信和隐私成本。我们提出了CE-FedGNN，一个通信高效且隐私保护的联邦GNN框架，用于在此类耦合图上进行学习。我们的方法通过不频繁地交换聚合的节点表示，避免了共享原始数据或每轮的嵌入。为了处理跨客户端的依赖性和陈旧性，我们引入了一个移动平均估计器，该估计器持续跟踪节点表示，并使其能够在各轮之间稳定地复用。为了对发布的表示提供严格的隐私保证，我们采用了度量差分隐私（metric-DP）框架，该框架通过在学习到的嵌入空间中的距离来衡量隐私，而不是基于最坏情况下的输入扰动。这使得在标准差分隐私变得过于保守的噪声水平下，仍能提供有意义的保证。我们证明了其以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度为 $O(T^{3/4})$。此外，我们在公共队列威胁模型下，通过Rényi差分隐私组合推导出了 $(\varepsilon,δ)$-metric-DP 保证。在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN实现了优异的性能，同时显著降低了通信开销，并在隐私保护噪声下保持了鲁棒性。

## Abstract
Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **研究背景**：图神经网络（GNN）在处理关系数据（如金融交易、社交网络）上表现优异，但现实中的图数据往往分布在不同的机构（如不同银行）中。由于隐私法规（如GDPR）的限制，各机构无法直接共享原始图数据，这使得联邦学习（FL）成为跨机构联合训练GNN的必然选择。
* **核心问题**：现有的联邦GNN方法面临一个“不可能三角”困境：
  1. **精度受损**：直接忽略跨客户端的边，导致全局图结构割裂，模型性能大幅下降。
  2. **通信开销大**：为了保留跨客户端连接，每轮迭代都交换节点嵌入，带来难以承受的通信成本。
  3. **隐私泄露风险**：交换的中间嵌入向量可能被逆向攻击，泄露敏感信息；而传统的差分隐私（DP）在处理节点嵌入时往往过于保守，加入的噪声会直接摧毁模型效用。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程
* **核心思想**：提出 **CE-FedGNN** 框架，通过**低频交换**聚合的节点表征来降低通信成本，利用**移动平均估计器**解决表征滞后问题，并引入**度量差分隐私**在适度噪声下提供严格的隐私保护。
* **关键技术细节**：
  * **移动平均估计器**：针对跨客户端节点，客户端不再每轮获取其最新嵌入，而是维护一个该节点嵌入的移动平均值。在本地训练时，直接复用该估计值，从而将跨客户端交互严格限制在一阶邻居，消除了多跳跨客户端采样的必要性。
  * **跨客户端梯度处理**：当采样到跨客户端的边时，客户端仅对本地拥有的节点进行链式求导和梯度回传，将跨客户端节点的嵌入视为固定输入。通过在两端客户端进行合理的采样加权，聚合后的梯度可以无偏地估计真实梯度。
  * **度量差分隐私**：传统DP基于最坏情况下的输入扰动，而metric-DP基于学习到的嵌入空间中的L2距离来衡量隐私。这意味着它保证目标节点的嵌入与其最近的 $k$ 个邻居在统计上不可区分，从而在适度噪声下提供有意义的隐私界限。
* **算法流程**：
  1. **服务器初始化**：全局模型参数和全局嵌入缓冲区。
  2. **本地更新**：客户端接收模型和所需的跨客户端嵌入。在本地进行 $K$ 次迭代，每次迭代通过移动平均规则更新节点嵌入，计算随机梯度，并使用移动平均梯度更新本地模型。
  3. **全局聚合**：每 $K$ 步通信一次，客户端将更新的模型、梯度估计器和边界节点嵌入发送给服务器。服务器进行平均聚合，并将嵌入路由给相关的邻居客户端。

### 3. 实验设计：数据集、场景与对比方法
* **数据集与场景**：
  * **反洗钱（AML）场景**：采用合成的银行间金融交易网络，包含小、中、大三种规模，并分为高违法比例（HI）和低违法比例（LI）两种极度不平衡的场景。
  * **引文网络场景**：采用四个真实的公开引用网络数据集。
* **Benchmark与对比方法**：
  * **对比基线**：单客户端本地训练（SC）、经典联邦学习、以及多种前沿联邦GNN方法（如Swift-FedGNN、FedGCN、FedGNN-ST、FedSage+、FedPUB）。
  * **评估指标**：主要使用所有客户端的平均宏观 F1 分数。

### 4. 资源与算力
* **算力配置**：论文在附录中明确说明了算力配置。实验在一个通过 InfiniBand 连接的集群上进行，**每台机器使用单块 NVIDIA A100 GPU**。
* **训练时长**：文中给出了每轮训练的平均运行时间（秒），但未明确提及总训练时长或总集群规模。

### 5. 实验数量与充分性
* **实验数量**：实验涵盖了 2 大类场景（金融反洗钱 + 引文网络），共 10 个数据集变体；对比了至少 7 种基线方法；进行了通信间隔 $K$ 和噪声强度 $\sigma_0$ 的参数敏感性实验；包含了详细的消融实验（移除全局嵌入、使用陈旧嵌入、移除梯度移动平均）；并额外进行了属性推断攻击（AIA）的安全性测试和具体的 Metric-DP 隐私界限计算。
* **充分性与客观性**：实验设计**非常充分且客观**。不仅证明了在典型图学习任务上的有效性，还针对其提出的两大核心贡献（通信效率和隐私保护）设计了专门的验证实验。特别是在隐私实验中，明确指出了未使用子采样放大，并在公共队列威胁模型下进行公平测试。

### 6. 论文的主要结论与发现
* **理论收敛性**：算法以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度仅为 $O(T^{3/4})$。
* **性能与通信效率**：CE-FedGNN 在绝大多数数据集上显著超越了现有基线。即使将通信间隔 $K$ 扩大到 1024，模型精度依然保持高位，证实了其极高的通信效率。
* **隐私与效用的平衡**：在注入满足 metric-DP 标准的高斯噪声后，模型性能下降平缓。相比于传统DP，metric-DP 能够在较小的噪声下提供具有现实意义的（如 $k$-匿名风格）隐私保证。

### 7. 优点：方法或实验设计上的亮点
* **解耦通信与依赖的创新设计**：巧妙地将移动平均估计器应用于节点嵌入，完美解决了联邦GNN中“低频通信导致的信息滞后”与“GNN依赖邻居最新状态”之间的矛盾。
* **贴合实际的隐私框架**：首次将 metric-DP 引入联邦GNN训练。相比于虚无缥缈的最坏情况DP，基于嵌入空间距离的 metric-DP 在金融反欺诈等场景中具有更强的现实指导意义。
* **严谨的理论证明**：在复杂的跨客户端耦合、随机梯度偏差和隐私噪声三重挑战下，依然给出了严密的数学收敛性和隐私界限证明。

### 8. 不足与局限
* **数据真实性的局限**：受限于金融数据的隐私法规，反洗钱（AML）实验完全依赖于模拟器生成的合成数据。尽管模拟器力求真实，但与真实的金融交易网络仍可能存在分布偏差。
* **威胁模型的假设**：隐私分析基于“公共队列”威胁模型，即假设每轮更新的节点集合是公开可观察的。如果引入安全聚合或匿名混洗，隐私界限可以进一步收紧，但本文未涉及此部分的拓展。
* **仅限一阶跨客户端交互**：为了通信效率，方法明确限制跨客户端交互仅在一阶邻居进行。对于高度依赖多跳全局拓扑信息的特定图任务，这种截断可能会带来潜在的信息损失。

（完）
