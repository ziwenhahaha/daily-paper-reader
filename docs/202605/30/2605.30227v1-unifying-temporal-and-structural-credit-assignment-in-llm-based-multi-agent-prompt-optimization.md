---
title: Unifying Temporal and Structural Credit Assignment in LLM-Based Multi-Agent Prompt Optimization
authors: "Wenwu Li, Yuran Song, Mingze Zhao, Bo Jin, Wenhao Li"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30227v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体信用分配优化
tldr: 提出时序与结构信用分配方法用于LLM多智能体提示优化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30227v1/fig-001.webp\", \"caption\": \"\", \"page\": 8, \"index\": 1, \"width\": 2529, \"height\": 1046}, {\"url\": \"assets/figures/arxiv/2605.30227v1/fig-002.webp\", \"caption\": \"\", \"page\": 8, \"index\": 2, \"width\": 2400, \"height\": 1500}]"
motivation: 多智能体信用分配优化。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
While Multi-Agent Systems (MAS) empower Large Language Models to tackle complex reasoning tasks through collaborative interaction, optimizing their dynamics remains a formidable challenge due to the discrete, non-differentiable nature of the computation graph and the sparsity of global supervisory signals. Existing black-box optimizers struggle to attribute trajectory-level failure to specific local components, resulting in inefficient, high-variance exploration. We argue that tractable MAS optimization needs structural inductive biases to disentangle error signals. We propose temporal and structural credit assignment, which decomposes the objective along two axes: (i) temporal credit, using state-space bottlenecks to identify critical rounds, and (ii) structural credit, using stationary role policies to isolate agent contributions. Leveraging these decomposed signals, we introduce a discrete, verbalized block coordinate descent algorithm for iterative refinement. Rather than indiscriminate global updates, it alternates between optimizing role prompts and aggregation protocols, using LLM-generated "proxy gradients" to target only the identified weak links. Across diverse reasoning benchmarks, our approach substantially reduces query complexity while improving performance, providing a principled and interpretable path toward self-improving MAS.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

### 研究动机

- **多智能体系统（MAS）与大语言模型的结合**：LLM-based MAS 通过多个专业化智能体（如提议者、辩论者、综合者）的协作，能够处理单模型难以解决的复杂推理任务。
- **核心挑战**：MAS 的计算图是**离散、不可微分**的，监督信号通常稀疏（仅有终端状态的单个标量分数）且全局（作用于整个轨迹），导致**信用分配问题（Credit Assignment Problem）**尤为严重。
- **现有方法的局限**：现有黑盒优化器（如基于 DSPy MIPRO、进化算法的方法）难以将轨迹级失败归因到特定局部组件，导致探索效率低下、方差高。

### 研究目标

提出一种可解释的、基于结构归纳偏置的优化方法，将混乱的交互图转化为结构化的优化景观，实现对多智能体推理系统的有效调试与自改进。

---

## 2. 论文提出的方法论

### 核心思想

将 MAS 提示优化问题沿两个正交轴分解为**时序信用**和**结构信用**：
1. **时序信用（Temporal Credit）**：识别关键推理轮次
2. **结构信用（Structural Credit）**：隔离各智能体的贡献

### 关键技术细节

#### 2.1 状态空间瓶颈（State-Space Bottleneck）

- **机制**：在每个回合引入聚合模块（Aggregation Module），将智能体输出聚合为共享系统状态 $S_t = f_t(U_t)$，形成 MDP 中的"状态瓶颈"。
- **作用**：使时序信用可计算，将全局信号分解到各轮次和各智能体。

#### 2.2 参数共享（Stationary Policy）

- **约束**：每个角色在整个交互过程中共享同一提示 $\phi_{i,1} = \phi_{i,2} = \cdots = \phi_{i,R} \triangleq \phi_i$。
- **作用**：减少搜索空间，使信用可跨轮次累积，实现可靠的结构信用分配。

#### 2.3 信用计算

- **结构信用** $C_{\text{role}_i} = \frac{1}{R} \sum_{t=1}^{R} c_{\text{role}_{i,t}}$，其中 $c_{\text{role}_{i,t}} = \lambda q_{i,t} + (1-\lambda) p_{i,t}$（$q$ 为直接评分，$p$ 为同伴评分）。
- **时序信用** $C_{\text{time}_t} = q_{\text{agg}_t}$，为该轮聚合输出的评分。

#### 2.4 verbalized BCD 算法

将优化问题分解为两个块：
- **结构块**：角色提示 $\Phi_{\text{role}}$（如 $\{\phi_i\}$）
- **时序块**：聚合提示 $\Psi = \{\psi_t\}^R_{t=1}$

**算法流程**：
1. **初始化**：设置 $\{\phi^{(0)}_i\}$ 和 $\{\psi^{(0)}_t\}$，$k \leftarrow 0$
2. **循环直到收敛**：
   - **Phase A（结构优化）**：固定 $\Psi^{(k)}$，计算 $\{C_{\text{role}_i}\}$，选择低信用角色，仅更新其提示
   - **Phase B（时序优化）**：固定 $\Phi_{\text{role}}^{(k+1)}$，计算 $\{C_{\text{time}_t}\}$，选择低信用轮次，仅更新其聚合提示
3. **返回**：优化后的 $\{\phi^{(k)}_i\}$ 和 $\{\psi^{(k)}_t\}$

### 公式汇总

- **基本目标**：$\max_{\Phi} \mathbb{E}_{x \sim \mathcal{D}} \mathbb{E}_{\tau \sim p(\cdot|x;\Phi)} [J(\tau;x)]$
- **引入聚合后的目标**：$\max_{\Phi,\Psi} \mathbb{E}_{x \sim \mathcal{D}} \mathbb{E}_{\tau \sim p(\cdot|x;\Phi,\Psi)} [J(\tau;x)]$
- **参数共享后的最终目标**：$\max_{\{\phi_i\}_{i=1}^N,\Psi} \mathbb{E}_{x \sim \mathcal{D}} \mathbb{E}_{\tau \sim p(\cdot|x;\{\phi_i\},\Psi)} [J(\tau;x)]$

---

## 3. 实验设计

### 数据集

| 数据集 | 领域 | 样本类型 |
|--------|------|----------|
| **AQuA** | 数学推理 | 多选 |
| **MedMCQA** | 医学问答 | 多选 |
| **GPQA** | 研究生水平问答 | 多选 |
| **MMLU** | 通用知识 | 多选 |

### 模型

- **Qwen2.5-7B-Instruct**
- **LLaMA3-8B-Instruct**
- **Gemma-7B-Instruct**

### 基准框架

- **LLM-Debate**：辩论式多智能体框架
- **DyLAN**：动态智能体网络框架

### 对比基线

1. **未优化基线**：使用原始/默认提示
2. **黑盒优化**：DSPy MIPRO（多阶段指令与演示优化），执行智能体级非知情提示搜索

### 优化设置

- 每个数据集随机选择 100 个样本作为**优化集**，用于提示搜索和优化
- 所有指标在更大的、分离的**测试集**上评估
- 优化过程不访问测试集

---

## 4. 资源与算力

论文**未明确说明** GPU 型号、数量或训练时长等算力信息。

已知信息：
- 使用**开源权重**的指令跟随 LLM（Qwen2.5-7B、LLaMA3-8B、Gemma-7B）
- 解码设置在基线和本文方法间保持一致
- 为**无模型参数更新**的两阶段提示优化流程

---

## 5. 实验数量与充分性

### 实验规模

- **4 个数据集** × **3 个模型** × **2 个框架** × **3 种条件**（基线/黑盒/ Ours）
- 每种配置进行**多次运行**（报告均值和标准差）
- 包含**不同随机种子**的稳定性评估

### 消融实验

- **仅结构优化**（Role-only）：仅优化最差表现的角色的提示
- **仅时序优化**（Aggregator-only）：仅优化低信用轮次的聚合器提示
- **组合优化**：同时进行结构和时序优化

### 实验充分性与客观性

- ✅ **优化集与测试集完全分离**：无测试集泄露
- ✅ **固定随机种子**：结果可复现
- ✅ **多数据集、多模型验证**：覆盖不同领域和规模
- ✅ **消融实验完整**：验证各组件贡献
- ✅ **标准差报告**：反映稳定性
- ✅ **预测转移分析**：对比正确→错误和错误→正确的比例

---

## 6. 论文的主要结论与发现

### RQ1：有效性

- 本文方法在所有数据集和模型上**一致提升准确率**
- 例如：
  - MedMCQA + LLaMA3-8B：提升 **7.0%**
  - GPQA + Qwen2.5-7B：提升 **3.57%**
  - MMLU + LLaMA3-8B：提升 **6.00%**

### RQ2：各组件贡献

- **结构优化**在角色敏感数据集（如 MedMCQA）贡献更大
- **时序优化**在聚合关键任务（如 GPQA）更有效
- **组合优化**在 MedMCQA 和 MMLU 上达到最佳性能

### RQ3：效率与稳定性

- 本文方法在**更少迭代次数**内收敛至更高准确率
- 相比 DSPy MIPRO，方差更低
- 超参数（时序信用阈值 $\tau$、缓冲区大小 $m$）提供稳定性-可塑性平衡控制

### RQ4：可解释性

- 本文方法产生更多**错误→正确修复**，更少**正确→错误退化**
- 结构信用与错误类型对齐
- 时序信用 $\alpha_t$ 与聚合器可靠性相关

---

## 7. 优点

1. **理论基础扎实**：引入状态瓶颈和参数共享的结构归纳偏置，将连续优化思想迁移到离散提示空间
2. **问题分解优雅**：将复杂的全局优化问题分解为时序和结构两个可解释的维度
3. **算法稳定**：BCD 交替优化避免联合优化的不稳定性
4. **可解释性强**：信用信号提供可操作的诊断，指导下一步优化
5. **实验全面**：多数据集、多模型、多框架验证，含完整消融
6. **效率提升**：针对性更新减少查询复杂度，避免无差别全局更新
7. **无需参数更新**：纯提示优化，适用于任何固定权重的 LLM

---

## 8. 不足与局限

1. **仅适用于完成轨迹**：方法依赖完整的推理轨迹进行归因，实时或早期干预场景可能受限
2. **依赖评估信号**：当前方法依赖 LLM-based critics 的评估质量，评估器本身的偏差可能传递
3. **长视野计算开销**：时序信用在长交互轮次场景下可能计算密集
4. **固定角色假设**：当前框架假设固定角色集合和集中式聚合，未涉及层级、工具增强或开放式协调
5. **无跨域泛化声明**：优化是数据集特定的，未声称跨数据集迁移能力
6. **未明确算力信息**：缺乏可复现的硬件和训练时间细节
7. **解析规则依赖**：答案提取依赖固定的解析规则，可能在自由形式输出中丢失有效信息

---

（完）
