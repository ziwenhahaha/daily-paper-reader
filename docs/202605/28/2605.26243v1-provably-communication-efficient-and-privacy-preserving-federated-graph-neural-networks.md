---
title: Provably Communication-Efficient and Privacy-Preserving Federated Graph Neural Networks
title_zh: 可证明通信高效且隐私保护的联邦图神经网络
authors: "Zhishuai Guo, Wenhan Wu, Chen Chen, Lei Zhang, Olivera Kotevska, Ravi K Madduri"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26243v1"
tags: ["query:dgnn-nas"]
score: 9.0
evidence: 提出CE-FedGNN，一种通信高效的联邦GNN框架，减少嵌入交换
tldr: 针对跨机构图数据因隐私限制无法共享，而现有联邦图神经网络（GNN）在处理跨客户端边时面临通信成本高和隐私泄露风险的问题，本文提出了CE-FedGNN框架。该框架通过低频交换聚合节点表征并引入移动平均估计器处理跨客户端依赖，结合度量差分隐私（metric-DP）提供严格的隐私保护，在大幅降低通信开销的同时保证了模型性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26243v1/fig-001.webp\", \"caption\": \"Table 3: Statistics of the Datasets\", \"page\": 22, \"index\": 1, \"width\": 572, \"height\": 274}, {\"url\": \"assets/figures/arxiv/2605.26243v1/fig-002.webp\", \"caption\": \"Table 1: Average F1 scores on AML datasets across high-illicit (HI) and low-illicit (LI) ratio settings. Best results in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 185}]"
motivation: 现有联邦图神经网络在处理跨客户端图结构时，要么忽略跨客户端连接导致精度下降，要么需要频繁交换嵌入表征从而带来巨大的通信和隐私成本。
method: 提出CE-FedGNN框架，通过低频交换聚合表征来降低通信量，利用移动平均估计器处理跨客户端依赖与滞后性，并采用度量差分隐私（metric-DP）框架提供形式化的隐私保证。
result: "理论证明该算法以O(1/√T)的速率收敛且通信复杂度为O(T^{3/4})，在反洗钱和引用网络的实验中，显著降低了通信开销并在隐私噪声下保持了优异性能。"
conclusion: CE-FedGNN成功解决了跨机构联邦图学习中的通信效率与隐私保护难题，实现了强模型性能、低通信开销与严格隐私保证的有效平衡。
---

## 摘要
图神经网络（GNNs）在关系数据上取得了优异的性能，但由于隐私和政策限制，现实世界中的图通常分布在无法共享原始数据的各个组织中。现有的联邦GNN方法要么忽略跨客户端链接，导致准确率下降；要么需要频繁交换嵌入，从而产生巨大的通信和隐私成本。我们提出了CE-FedGNN，一个通信高效且隐私保护的联邦GNN框架，用于在此类耦合图上进行学习。我们的方法通过不频繁地交换聚合的节点表示，避免了共享原始数据或每轮的嵌入。为了处理跨客户端的依赖性和滞后性，我们引入了一个移动平均估计器，该估计器持续跟踪节点表示，并使其能够在各轮次中稳定复用。为了对发布的表示提供严格的隐私保证，我们采用了度量差分隐私（metric-DP）框架，该框架通过在学习到的嵌入空间中的距离来衡量隐私，而不是基于最坏情况的输入扰动。这使得在标准差分隐私变得过于保守的噪声水平下，仍能提供有意义的保证。我们证明了其以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度为 $O(T^{3/4})$。此外，我们在公共队列威胁模型下，通过Rényi差分隐私组合推导出了 $(\varepsilon,δ)$-metric-DP 保证。在合成银行间反洗钱基准和引文网络上的实验表明，CE-FedGNN实现了优异的性能，同时显著降低了通信开销，并在隐私保护噪声下保持了鲁棒性。

## Abstract
Graph neural networks (GNNs) achieve strong performance on relational data, but real-world graphs are often distributed across organizations that cannot share raw data due to privacy and policy constraints. Existing federated GNN methods either ignore cross-client links, leading to degraded accuracy, or require frequent embedding exchanges, incurring substantial communication and privacy costs. We propose CE-FedGNN, a communication-efficient and privacy-preserving federated GNN framework for learning over such coupled graphs. Our approach avoids sharing raw data or per-round embeddings by infrequently exchanging aggregated node representations. To handle cross-client dependency and staleness, we introduce a moving-average estimator that continuously tracks node representations and enables their stable reuse across rounds. To provide formal privacy guarantees for the released representations, we adopt the metric differential privacy (metric-DP) framework, which measures privacy with respect to distances in the learned embedding space rather than worst-case input perturbations. This yields meaningful guarantees at noise levels where standard differential privacy becomes overly conservative. We establish convergence to a stationary point at a rate of $O(1/\sqrt{T})$ with $O(T^{3/4})$ communication complexity. In addition, we derive $(\varepsilon,δ)$-metric-DP guarantees via Rényi differential privacy composition under a public-cohort threat model. Experiments on synthetic interbank anti-money laundering benchmarks and citation networks demonstrate that CE-FedGNN achieves strong performance while significantly reducing communication and maintaining robustness under privacy-preserving noise.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
* **研究背景**：图神经网络（GNN）在处理关系数据（如金融交易、社交网络）上表现优异，但现实中的图数据往往分布在不同的机构（如不同的银行）中。由于隐私政策（如GDPR）和数据安全限制，各机构无法将原始图数据集中起来进行训练。
* **核心痛点**：现有的联邦图神经网络在处理跨机构的“跨客户端边”时面临两难境地：
  1. **忽略跨客户端边**：会导致全局图结构信息丢失，模型准确率大幅下降。
  2. **频繁交换节点嵌入**：虽然能保留全局信息，但会带来极其高昂的通信成本，且频繁共享中间表征容易引发严重的隐私泄露。
* **整体含义**：本文旨在解决跨机构联邦图学习中**通信效率**与**隐私保护**难以兼得的问题，提出了一种既能高效捕获全局跨客户端图结构，又能提供严格数学隐私保证的新框架。

### 2. 论文提出的方法论：核心思想、关键技术细节与算法流程
* **核心思想**：提出 **CE-FedGNN** 框架。通过**低频交换**聚合的节点表征来代替高频交换，并引入**移动平均估计器**来平滑表征的滞后性误差；同时引入**度量差分隐私**，在适度噪声下提供有意义的隐私保护。
* **关键技术细节**：
  * **移动平均估计器**：针对跨客户端节点表征在本地更新时出现的滞后问题，算法在本地前向传播时，对节点表征和随机梯度分别维护移动平均估计器。这使得客户端可以复用邻居节点“最近一次共享的表征”，从而消除多跳跨客户端采样的必要性。
  * **跨客户端梯度处理**：当计算涉及跨客户端边的梯度时，客户端仅对本地拥有的节点进行反向传播，将跨客户端节点的表征视为固定输入。通过对边的采样概率进行适当加权，聚合后的更新可恢复为全梯度的无偏估计。
  * **度量差分隐私**：传统的差分隐私（DP）在最坏情况下需要极大的噪声，导致模型失效。本文采用 metric-DP，基于学习到的嵌入空间中的 L2 距离来衡量隐私，而非最坏情况的输入扰动。通过向共享的表征、模型参数和梯度注入高斯噪声，在公共队列威胁模型下通过 Rényi DP 组合推导出 $(\varepsilon, \delta)$-metric-DP 保证。
* **算法流程**：
  1. **服务器初始化**：全局模型参数和全局嵌入缓冲区。
  2. **本地更新（客户端并行）**：客户端接收当前模型和所需的跨客户端表征；在本地进行 $K$ 次迭代，每次采样小批量数据，使用移动平均规则更新节点表征和梯度估计器，并更新本地模型。
  3. **通信与聚合**：每 $K$ 步通信一次，客户端将更新的模型、梯度及边界节点表征发送给服务器；服务器进行平均聚合，并将表征路由给需要的相邻客户端。

### 3. 实验设计：数据集、场景与对比方法
* **数据集与场景**：
  * **反洗钱（AML）场景**：由于真实金融数据受限，采用了真实的交易模拟器生成合成银行间金融网络数据。包含小、中、大规模数据集，并分为高非法率（HI）和低非法率（LI）两种极度不平衡的设定。
  * **引文网络场景**：采用四个广泛使用的真实基准数据集：Cora, Citeseer, PubMed, MSAcademic。
* **对比方法**：
  * **单客户端（SC）**：本地独立训练。
  * **传统联邦学习**：FedAvg（仅传模型）。
  * **联邦GNN基线**：Swift-FedGNN（间歇性全局图训练）、FedGCN（仅在初始化共享嵌入）、FedGNN-ST（朴素共享滞后嵌入）、FedSage+（本地生成缺失节点）、FedPUB（加权聚合模型更新）。

### 4. 资源与算力
* **算力说明**：论文在附录 E 中明确提到了算力配置。实验是在通过 InfiniBand 连接的集群上进行的，**每台机器使用单块 NVIDIA A100 GPU**。
* **训练时长**：文中给出了平均每轮训练的时间（例如在 HI-Large 数据集上，CE-FedGNN-PNA 每轮约需 164.73 秒），但未明确说明总训练时长或总 GPU 小时数。

### 5. 实验数量与充分性
* **实验数量**：实验涵盖了 2 大类场景（金融反洗钱、引文网络），共计 10 个数据集变体；对比了至少 7 种基线方法；进行了通信间隔 $K$ 和噪声强度 $\sigma_0$ 的敏感性分析；包含了组件消融实验、属性推断攻击（AIA）隐私实证测试以及具体的 metric-DP 隐私界限计算。
* **充分性与客观性**：实验设计非常充分且客观。不仅测试了常规的准确率，还针对联邦学习的痛点（通信字节数、运行时间）进行了详细对比。消融实验清晰地验证了移动平均估计器的必要性。隐私实验不仅停留在理论证明，还通过 AIA 攻击和经验性 $\rho$ 距离分布验证了实际的隐私保护效果。

### 6. 论文的主要结论与发现
* **理论收敛性**：算法以 $O(1/\sqrt{T})$ 的速率收敛到驻点，且通信复杂度仅为 $O(T^{3/4})$，证明了其极高的通信效率。
* **性能与鲁棒性**：在绝大多数数据集上，CE-FedGNN 的 F1 分数显著优于所有基线。特别是在低非法率（LI）的极度不平衡金融场景下，其他基线大多崩溃退化为零，而本文方法依然保持较高准确率。
* **通信与隐私权衡**：即使将通信间隔拉长至 $K=1024$，模型性能下降依然极小；在注入满足 metric-DP 的适度高斯噪声后，模型性能退化平缓，成功在通信效率、模型精度和隐私保护三者间取得了最优平衡。

### 7. 优点
* **直击痛点且理论扎实**：针对跨客户端依赖和表征滞后问题设计的“移动平均估计器”精巧且有效，并配备了严密的数学收敛证明。
* **创新的隐私框架应用**：首次将 metric-DP 引入联邦 GNN 训练。相比于传统 DP 在保护节点嵌入时因敏感度过高而失效，metric-DP 基于嵌入空间的语义距离，使得在适度噪声下提供不“空洞”的隐私保证成为可能。
* **极强的实用性**：算法仅要求交换一跳边界节点的表征，且支持小批量训练，能够轻松扩展到包含上亿条边的超大图（如 HI-Large）。

### 8. 不足与局限
* **数据真实性的局限**：受限于金融监管，反洗钱实验完全依赖合成数据。尽管模拟器力求真实，但与包含复杂真实噪声的金融交易网络仍可能存在差异。
* **威胁模型的假设**：隐私分析基于“公共队列威胁模型”，即假设每轮更新的节点集合对攻击者是公开的。如果引入安全聚合或匿名混洗，虽然能进一步增强隐私，但本文的 RDP 组合并未覆盖这部分隐私增益。
* **通信内容的限制**：为了处理跨客户端边，算法依然需要传输节点的中间表征。虽然施加了 metric-DP，但在对数据隐私要求达到极端严苛（完全零泄露）的场景下，任何表征的共享都可能面临合规性挑战。

（完）
