Title: Memory-Augmented Reinforcement Learning Agent for CAD Generation

URL Source: https://arxiv.org/pdf/2605.19748v1

Published Time: Wed, 20 May 2026 01:41:52 GMT

Number of Pages: 26

Markdown Content:
# Memory-Augmented Reinforcement Learning Agent for CAD Generation 

## Yin Xiaolong Zhejiang University 

> yinxiaoloong@zju.edu.cn

## Liu Yu Ocean University of China 

> liuyu5585@stu.ouc.edu.cn

## Shen Jiahang Zhejiang University 

> jiahang.shen@zju.edu.cn

## Lu Xingyu Zhejiang University 

> xingyulu@zju.edu.cn

## Ni Jinzhe Zhejiang University 

> nijingzhe@zju.edu.cn

## Fan Fengxiao Zhejiang University 

> 12521197@zju.edu.cn

## Sang Fan Zhejiang University 

> sangfan@zju.edu.cn

## Abstract 

Automatic generation of computer-aided design (CAD) models is a core technology for enabling intelligence in advanced manufacturing. Existing generation methods based on large language models (LLMs) often fall short when handling complex CAD models characterized by long operation sequences, diverse operation types, and strong geometric constraints, primarily because reason-ing chains break and effective error-correction mecha-nisms are lacking. To address this problem, this paper proposes a memory-augmented reinforcement learning framework for CAD generation agents. The frame-work encapsulates the underlying geometric kernel into a structured toolchain callable by the agent and builds a closed-loop mechanism of design intent understanding, global planning, execution, and multi-dimensional ver-ification. It also designs a dual-track memory module consisting of a case library and a skill library, and pro-poses a dynamic utility retrieval algorithm. By intro-ducing reinforcement learning into retrieval and policy optimization, the agent can effectively avoid retrieval traps in which examples are semantically similar but geometrically infeasible, enabling online self-correction and continual evolution without additional large-scale annotated data. Experiments show that the proposed method significantly improves both the success rate and geometric consistency on complex CAD model genera-tion tasks. 

## 1. Introduction 

Computer-aided design (CAD) software is a fundamen-tal tool in advanced manufacturing fields such as auto-mobiles, aerospace, and medical devices. Its paramet-ric modeling capability directly affects the efficiency of product design iteration, manufacturability verifi-cation, and engineering knowledge reuse. Although mainstream CAD platforms, including CATIA, UG NX, PTC Creo, OnShape, AutoCAD, and SolidWorks, are already highly mature in geometric kernels, assem-bly constraints, and feature modeling, modeling work-flows in engineering practice still rely mainly on in-teractive manual modeling by experienced engineers. Complex parts often require frequent feature decompo-sition, constraint adjustment, and local reconstruction. As a result, the modeling barrier is high, the design cy-cle is long, and experience and modeling strategies are difficult to transfer across teams at low cost. These issues have become important factors limiting design efficiency and knowledge accumulation. To reduce modeling cost, researchers have recently begun to explore data-driven automatic CAD genera-tion methods. These methods enable models to learn modeling patterns from existing CAD data and thereby reduce human intervention. They usually regard the CAD modeling process as a sequence of parameterized geometric operations, and use representation learning and sequence modeling on historical design data to achieve a certain degree of automatic generation. With the progress of large language models (LLMs) in pro-gram synthesis and tool-use reasoning [4, 26, 38], gen-erating CAD modeling scripts from natural language has gradually become a feasible path. Design intent is expected to be directly mapped to executable model-ing programs, thereby supporting rapid transformation from intent to operation and then to geometric results. However, current LLM-based CAD generation stud-ies are still largely limited to a single-shot open-loop generation paradigm. The model often outputs a com-plete modeling script at once and lacks real-time aware-

> arXiv:2605.19748v1 [cs.AI] 19 May 2026

ness of geometric-kernel execution and online correc-tion. When the target expands from low-complexity standard parts to complex CAD models with long se-quences, multiple operation types, strong constraints, and strong inter-step dependencies, this paradigm faces three problems in executability and geometric consis-tency. First, the execution state is unknown. Open-loop generation cannot explicitly track implicit geomet-ric states and local topological changes, and geometric-kernel feedback is often coarse-grained. Subtle excep-tions such as Boolean failures, entity reference or selec-tion drift, and constraint conflicts therefore cannot be located and resolved in time; they are then amplified in later steps and trigger cascading failures. Second, semantic retrieval is mismatched with engineering util-ity. Existing retrieval-augmented methods mostly rely on static semantic similarity, which makes it difficult to identify invalid cases that are semantically relevant but geometrically infeasible under specific geometric con-texts and constraints. They also lack mechanisms for dynamic strategy adjustment and retrieval reranking based on execution results, causing erroneous exam-ples to be repeatedly recalled and to further disturb generation. Third, skill abstraction and internaliza-tion remain insufficient. The model is still unable to distill high-frequency subtasks into reusable and pa-rameterized high-level skills in the way human experts do. It therefore has to repeat low-level reasoning in every task, which is inefficient and more likely to for-get global geometric constraints and design intent in long contexts, reducing the stability and recoverability of long-sequence modeling. To address these problems, this paper proposes a memory-augmented [41, 42] reinforcement learning framework for CAD generation agents. Unlike one-shot open-loop generation, this work treats the geo-metric kernel as an interactive environment and or-ganizes the modeling process as closed-loop reasoning over planning, execution, multi-dimensional verifica-tion, and correction. At each step, the agent produces a local operation and invokes geometric tools for exe-cution, while obtaining execution states and geometric feedback in real time. When an exception or geomet-ric inconsistency is detected, targeted local diagnosis and rollback correction are triggered, so that the step-wise verification mechanism suppresses error propaga-tion and improves the stability and recoverability of long-sequence generation. To give the closed-loop process an accumulative and transferable capability for continual improvement, this paper further introduces long-term memory and dy-namic utility retrieval. The framework builds two types of memory, namely a case library and a skill library. The case library stores historical successful experience in the form of intent, operation trajectory, and geomet-ric feedback or result, and provides directly reusable references for planning and local repair in new tasks. The skill library abstracts reusable operation patterns for high-frequency subtasks into parameterized skills, reducing the search space and randomness of long-sequence generation. Unlike static retrieval based only on semantic similarity, this paper uses reinforcement learning signals to evaluate and update the utility of memory entries online. Retrieval therefore shifts from examples that merely look similar to examples that are more likely to succeed in the current context. This re-duces repeated recall of misleading cases that are se-mantically relevant but geometrically infeasible, and improves the convergence efficiency and reliability of closed-loop repair. Compared with existing open-loop script generation frameworks or frameworks that perform only super-ficial iterative verification, the proposed method has three characteristics. First, stepwise verification and correction driven by geometric-kernel execution feed-back controls errors within local steps and reduces the risk of cascading failures in long sequences. Second, a memory mechanism that can be continuously updated through task interaction enables the system to accumu-late experience and improve its strategy without rely-ing on additional large-scale annotated data. Third, case reuse and skill abstraction improve the controlla-bility and reusability of reasoning, better matching en-gineering requirements for executable, verifiable, and repairable generation. 

## 2. Related Work 

CAD model generation aims to generate B-Rep mod-els or parametric modeling command sequences from inputs such as multi-view images, boundary repre-sentation (B-Rep) models, text descriptions, or point clouds. Along the technical evolution, related studies can be roughly summarized into three stages: early learning-based generation centered on quantization and encoder-decoder models; code-generation methods represented by large language model or multimodal model fine-tuning; and recent agent-based frameworks emphasizing tool invocation, collaborative planning, and iterative verification. 

2.1. Learning-Based Generation with Quantization and Encoder-Decoder Models 

Before large language models were widely applied, CAD reconstruction and generation mainly relied on representation learning and sequence modeling. One line of work constructs latent-space representations around B-Rep or command sequences, and then per-forms autoregressive or diffusion generation in that la-tent space. For example, SolidGen [6] organizes B-Rep vertices, edges, and faces into an indexed hierar-chical structure and autoregressively generates topo-logical entities. BrepGen [37] combines vector vari-ational autoencoders with latent diffusion and uses structured latent variables to represent geometric in-formation for B-Rep generation. DTGBrepGen [12] further decouples topology generation from geome-try generation to improve B-Rep structural validity. BrepDiff [10] and Stitch-A-Shape [15] improve the ef-ficiency and interpretability of direct B-Rep genera-tion from the perspectives of single-stage diffusion and bottom-up stitching, respectively. Work on command sequences emphasizes discretizing or quantizing the modeling process and applying neural encoding before generating long sequences autoregressively or through diffusion. Examples include DeepCAD [31], Skex-Gen [35], HNC-CAD [36], VQ-CAD [27], and Mamba-CAD [18], which learn generative program represen-tations through codebooks, hierarchical encoding, dis-crete tokenization, or state-space models. Pointer-CAD [23] uses pointer-style edge and face selection to incorporate B-Rep geometric information into CAD command sequence generation, allowing more complex editing operations while substantially reducing topol-ogy errors caused by quantization. Other studies in-fer command sequences directly from sketches or en-gineering drawings, such as Free2CAD [11] and Draw-ing2CAD [24]. The advantage of this line of research is that the training objective is clear, the generated form is structured, and the output can be directly aligned with engineering-friendly representations such as com-mand sequences and geometric entities. However, the bottlenecks are also evident. On the one hand, quan-tization and discrete tokens often introduce precision loss and error accumulation, making it difficult to meet strict industrial requirements on dimensions and toler-ances. On the other hand, the operation sets and con-straint expressions are usually basic, such as extrusion, revolution, and simple Boolean operations, and their ability to handle complex assembly constraints, topo-logical robustness, and strong cross-step dependencies remains limited. 

2.2. Generation Based on Large Language Models and Multimodal Fine-Tuning 

With breakthroughs of large language models in code generation and tool-use reasoning, CAD generation has increasingly shifted toward a route in which executable programs express design intent. In this route, inputs such as text, images, sketches, point clouds, or B-Reps are mapped to parametric modeling code or com-mand sequences. Representative works include CAD-LLM [32] and CadVLM [33], which fine-tune large models to generate parametric models or sketch pro-grams from sketches or images. CAD-GPT [29] dis-cretizes continuous-space parameters into spatial to-kens that can be predicted by multimodal large mod-els, enhancing spatial reasoning from images or text to CAD sequences. CAD-Recode [25] targets reverse engineering of CAD code from point clouds. CAD-Crafter [3] conditions latent diffusion on image geo-metric features to generate compilable CAD sequences from uncontrolled images. Methods that introduce vi-sual feedback to improve text-to-CAD executability and consistency have also emerged, such as Text-to-CAD with visual feedback [28]. Meanwhile, multi-modal alignment and instruction tuning further en-hance conditional generation. CAD-MLLM [34] at-tempts to unify multimodal conditions such as text, images, and point clouds. CAD-Llama [14] improves high-fidelity generation using finer-grained annotations and instruction data. LLM4CAD [16] explores para-metric CAD program generation through prompt-program paired data and large-model fine-tuning. Fur-ther, researchers have begun to introduce geometric en-gines or modeling kernels into the training loop. RL-CAD [39] incorporates geometric execution into a re-inforcement learning training environment to improve geometric accuracy in B-Rep to command-sequence re-construction. cadrille [9] explores an online reinforce-ment learning paradigm to improve multimodal CAD reconstruction through interactive training. Retrieval-augmented and self-improving directions, such as Seek-CAD [17], as well as reinforcement-learning-enhanced vision-language CAD generation, such as ReCAD [13], also show potential in improving controllability and customization. VideoCAD [20] learns CAD interactive modeling directly from videos. Although methods in this stage have significantly improved the feasibility of converting natural language or multimodal input into executable modeling programs, they generally depend more heavily on high-quality training data that cov-ers complex engineering distributions. Existing open-source CAD datasets [8, 30] still have relatively lim-ited model complexity and failure modes, constraining generalization and stable execution in industrial long-sequence, strongly constrained scenarios. 

2.3. Agent-Based CAD Model Generation 

To reduce dependence on large-scale retraining and ex-ploit the system-level planning capability of large mod-els, recent research has gradually shifted from single-model end-to-end generation to agent-based planning-execution-verification frameworks. These systems of-ten use CAD kernels, renderers, and geometric analyz-ers as callable tools and progressively approach exe-cutable results through multi-turn interaction. CAD-Assistant [19] proposes a tool-augmented VLLM as a general CAD task solver, emphasizing the collabora-tion between planner and tool invocation to obtain zero-shot generalization. Query2CAD [2] further trans-forms natural language queries into Python modeling code and triggers regeneration using visual question an-swering scores. 3D-PreMise [40] proposes a framework that uses LLMs to manipulate 3D software through program synthesis and generate text-driven paramet-ric industrial shapes. CADCodeVerify [1] uses vision-language models to perform attribute-level checks on generated results and provide feedback for correction. Other studies propose more explicit collaborative de-sign agent frameworks, decomposing requirement anal-ysis, modeling execution, and quality assurance into multiple roles [5, 22]. The key value of these frame-works is that they can improve output quality through iterative verification, reflection, and tool invocation without explicitly retraining the model, making them especially suitable for engineering applications across multiple tasks and scenarios. However, in complex model generation tasks, such as parts containing many structural faces and strong topological dependencies, if verification signals mainly come from rendered images or natural-language descriptions, the feedback often cannot precisely locate root causes at the underlying topology, constraint, or parameter level. Once fine-grained errors such as entity reference drift, Boolean failure, or constraint conflict occur, they may still be amplified in subsequent steps, limiting generation suc-cess rate and geometric consistency. Unlike the above methods, this paper builds an agent closed-loop framework that uses feedback from the underlying geometric kernel as the real reward en-vironment and centers on long-term memory retrieval and reinforcement-learning-based dynamic utility eval-uation. The goal is to fundamentally suppress cascad-ing failures in long program generation and to accu-mulate transferable experience and strategies during interaction. 

## 3. Agent Framework 

Automatic generation of complex CAD models requires not only that modeling scripts be executable at the syn-tax level, but also that intermediate features, topolog-ical references, and geometric constraints remain con-sistent across long sequences of operations. To ad-dress the difficulty of one-shot open-loop generation in sensing execution states in time and the tendency of errors to propagate through subsequent steps, this paper models the CAD geometric kernel as an inter-active and observable external environment, and on this basis constructs a memory-augmented closed-loop generation agent. The framework uses a large lan-guage model as the core for policy reasoning and the FreeCAD MCP (Model Context Protocol) interface as the execution and observation channel. It gradually generates parametric modeling scripts through a cy-cle of planning, execution, verification, and correction. The overall system architecture is shown in Fig. 1. 

3.1. Overall Framework 

This paper uses the MCP protocol to wrap the FreeCAD geometric kernel into a toolized interface, en-abling the agent to submit modeling actions as tool calls and obtain structured feedback from the envi-ronment. Actions in one interaction include Python modeling script fragments, parametric feature opera-tions, file management instructions, and verification instructions. Environment feedback includes script ex-ecution status, exception logs, entity topology changes, B-Rep statistics, key dimensional measurements, and constraint-checking results. Compared with generation methods that output only a complete script, this inter-action mechanism makes implicit geometric states in CAD modeling explicit, providing observable evidence for subsequent local diagnosis and policy correction. In the concrete workflow, the framework decomposes a long-sequence CAD modeling task into the following four stages. 1. Design intent understanding : The agent first semantically parses the user input and extracts the main structure, functional semantics, dimensional constraints, manufacturing constraints, and possi-ble modeling primitives of the target object. This stage outputs a structured requirement representa-tion, providing a unified input for subsequent plan-ning, memory retrieval, and construction of verifi-cation metrics. 2. Global planning : After obtaining the struc-tured requirements, the agent generates a hierar-chical modeling blueprint and decomposes the tar-get model into a set of dependent subtasks. The blueprint not only gives the operation order, but also explicitly records the reference planes, sketches, boundaries, and parameter constraints on which each step depends, thereby reducing entity reference drift and topology selection errors that commonly occur in long-sequence modeling. 3. Stepwise execution : In the execution stage, the agent follows the ReAct paradigm for local reason-ing and tool invocation. It first determines the Figure 1. System architecture. 

next operation intent based on the current state and the global blueprint, then generates executable FreeCAD Python code and submits it to the ge-ometric kernel through the MCP interface. The toolchain covers three types of functions: session management, geometric modeling, and geometric verification. They are used respectively to create, save, and roll back models; generate structures such as sketches, features, Boolean operations, arrays, and chamfers; and check topological validity, con-straint consistency, and key dimensional deviations. 4. Multi-dimensional verification and local cor-rection : After each tool call finishes, the system performs local quality inspection according to en-vironment feedback. Verification checks whether the script executed successfully, whether the tar-get entity was generated, whether the topology is valid, whether key dimensions satisfy constraints, and whether necessary geometric statistics are within reasonable ranges. When exceptions such as Boolean failure, constraint conflict, entity reference invalidation, or local self-intersection are detected, the agent locates the failure cause according to er-ror logs and geometric feedback. If necessary, it rolls back to the latest snapshot that passed verification and reselects parameters or an alternative modeling path. The above workflow transforms model generation from one-shot text prediction into a closed-loop deci-sion process constrained by the geometric kernel. Be-cause every intermediate step must be executed and verified in a real CAD environment, errors can be han-dled within local operations, reducing the risk of cas-cading failures in long-sequence modeling and improv-ing the recoverability of the generation process and the geometric consistency of the final model. 

3.2. Memory 

Stable operation of the closed-loop agent depends on external memory that can be accumulated and up-dated. CAD modeling tasks usually contain many re-curring local structures and operation patterns. If a large language model reasons from scratch to gener-ate the complete script each time, the reasoning cost is high and API misuse, constraint omissions, and inter-step reference errors can easily occur. To address this issue, this paper designs a dual-track memory consist-ing of a case library and a skill library, which respec-tively store historical successful trajectories and param-eterized high-level modeling operations. The external memory is denoted as 

Mext = Mcase ∪ M skill ,

where Mcase denotes the case library and Mskill de-notes the skill library. The two jointly provide con-textual priors for the agent during retrieval, and are updated according to feedback after execution. 

3.2.1. Case Library Design 

The case library stores modeling experience that has been successfully verified and provides reference tra-jectories for planning and error repair in new tasks. This paper represents a case as 

C = ⟨I , T , O, ⟩,

where I is the user intent and its structured parsing result; T is the tool invocation trajectory used to im-plement that intent, recording key parameters, inter-mediate object references, rollback and repair records, and other information; and O is a summary of execu-tion artifacts and verification feedback, recording the final model, passed verification items, key geometric statistics, and the causes repaired after failures. On the input side, the agent retrieves the case li-brary during global planning and local correction, and injects cases that are relevant to the current task and have high historical utility into the context. Recalled cases do not directly replace current reasoning, but pro-vide reusable modeling orders, parameter selection evi-dence, and error-handling experience. To avoid false re-calls caused by pure semantic similarity, Section 4 fur-ther introduces dynamic utility evaluation based on re-inforcement learning feedback, so that retrieval results gradually shift from semantic relevance to a higher like-lihood of success under the current geometric state. On the output side, after a modeling task is com-pleted and passes multi-dimensional verification, the system extracts effective tool invocation sequences, fi-nal model summaries, and quality evaluation results from execution logs, and writes a new case entry into the case library. Thus, the case library is not only a static knowledge base, but also continuously expands its coverage through task interaction. 

3.2.2. Skill Library Design 

The skill library stores reusable high-frequency model-ing subprocesses. Its goal is to wrap stable low-level operation patterns into parameterized high-level tools. This paper represents a skill as 

K = ⟨Script , Doc , Θ, Ω, ⟩,

where Script is a directly executable FreeCAD Python script, Doc is the skill documentation, Θ denotes pa-rameters, and Ω denotes applicable conditions and con-straint boundaries. The documentation should de-scribe the function of the skill, the physical meaning of input parameters, applicable geometric objects, pre-conditions, and potential failure modes, so that the large language model can judge whether the skill fits the current state before invocation. During planning and correction, the system retrieves candidate skills from the skill library and injects the corresponding documentation into the context. The agent decides whether to call a skill according to the current geometric state, the global blueprint, and the skill applicability conditions. If it decides to call the skill, it only needs to generate the parameter config-uration instead of generating the low-level modeling code line by line. This macro-operation invocation can shorten the long-sequence generation path, reduce lo-cal reasoning randomness, and improve consistency in constructing high-frequency structures. The skill library also contains an automatic internal-ization mechanism. After a task is successfully com-pleted, the system post-processes the execution tra-jectory, identifies continuous operation fragments with reuse value, and attempts to encapsulate them as new parameterized scripts, documentation, and metadata. After verification, new skills are registered in the skill library and participate in subsequent retrieval. Thus, the case library accumulates complete task-level expe-rience, while the skill library distills callable capabili-ties at the local operation level. Together, they support continual improvement of the agent in complex CAD generation tasks. 

## 4. Memory-Driven Reinforcement Learning Retrieval Mechanism 

Section 3 presents the overall architecture of the closed-loop generation agent. This section further discusses how external memory participates in decision making and how execution feedback is used to optimize re-trieval preferences online. This paper does not fine-tune the parameters of the large language model. In-stead, the learnable component is restricted to value estimation and utility updates for external memory. Specifically, in each task round, the system first re-trieves cases and skills according to the current state, and then the large language model generates model-ing actions conditioned on the retrieved results. After task completion, terminal rewards produced by geo-metric verification and multi-view feedback are prop-agated back to the memory module to update subse-quent retrieval rankings. This mechanism allows the system to improve stability in complex CAD generation tasks through experience accumulation while keeping the base model unchanged. 

4.1. Definition of Memory Retrieval M-MDP 

To characterize the coupling between retrieval decisions and modeling execution, this paper formalizes the CAD generation process as a memory-augmented Markov de-cision process (M-MDP): 

P = ( X , A, T , R, Mext , γ, π ).

Here, X denotes the state space. The state st ∈ X at time t consists of structured user requirements, the cur-rent geometric state, historical tool invocations, error logs, verification feedback, and summaries of retrieved memories. This state contains both design intent at the natural-language level and executable state and topol-ogy information returned by the geometric kernel. 

A denotes the action space. The action at consists of two parts. The first is the memory selection action, namely selecting memory units from the case library or skill library for context injection or tool invocation. The second is the CAD modeling action, namely a script fragment or standardized skill call generated by the large language model and submitted to FreeCAD for execution. The state transition function 

T : X × A → X 

is determined by the real execution result of the geo-metric kernel. After each action is executed, the system obtains a new geometric state, exception information, and verification result through the MCP interface, and updates st+1 accordingly. The reward function R measures the terminal qual-ity of a modeling episode. This paper adopts a binary reward given by geometric verification: 

r =

{

1, all execution and verification checks pass ,

0, the script fails to execute or any verification item fails .

The verification items include topological validity, con-straint consistency, key dimensional deviation, and necessary rendering-consistency checks. Mext is the external memory space defined in Section 3. Each memory unit m ∈ M ext is associated with a state-dependent value estimate Q(s, m ), which represents the expected probability of task success after using that memory in state s. The policy π includes both the memory retrieval policy and the modeling action pol-icy. This paper focuses on optimizing the retrieval pol-icy, while the base large language model is responsible for conditioned action generation. 

4.2. Case Library Retrieval Algorithm Design and Optimization 

4.2.1. Algorithm Design 

Memory units in the case library store complete histori-cal modeling trajectories, including input intent, struc-tured requirements, tool invocation sequences, execu-tion results, verification feedback, and success marks. To reduce the interference of irrelevant implementation details in semantic recall, the system mainly uses Query + Requirement as the index text when constructing case vectors. Let xs be the query text of the current state, xm be the index text of case m, and ϕ(·) be the embedding model. Then 

es = ϕ(xs), em = ϕ(xm).

This paper uses the unified embedding model 

BAAI/bge-m3 and persistently stores case vectors in the vector database. To prevent failed trajectories from in-terfering with the candidate set in early recall, the first stage performs semantic recall only from the set of his-torical successful cases Mcase  

> +

:

ssem (s, m ) = cos (es, em), m ∈ M case  

> +

.

The K0 cases with the highest semantic similarity are then selected to form the initial candidate set 

CK0 (s) = TopK K0 {ssem (s, m ) | m ∈ M case  

> +

}.

In the current experimental setting, the initial candi-date size is K0 = 20 , and the final number of injected cases is k = 5 .Semantic similarity alone cannot fully reflect the ac-tual usability of a case under the current geometric state. Therefore, this paper introduces a parameter-ized value network fθ on the candidate set to estimate the success probability after a case is adopted. The network input is obtained by concatenating the query vector, the case vector, their difference, and their ele-mentwise product: 

z(s, m ) = [ es; em; |es − em|; es ⊙ em].

This feature encodes semantic similarity, difference di-rection, and dimension-level matching relations at the same time. The value network is a multi-layer per-ceptron with LayerNorm, GELU, and Dropout. The hidden dimensions are [512 , 128] , and the output is 

sval (s, m ) = fθ (z(s, m )) ∈ [0 , 1] .

Because the value network has not yet obtained suf-ficient feedback during cold start, this paper ranks can-didates using a dynamically fused score of semantic similarity and value estimation: Score case (s, m ) = αt ˜ssem (s, m ) + (1 − αt)˜ sval (s, m ).

Here, ˜ssem and ˜sval denote scores after min-max nor-malization within the current candidate set. The fusion coefficient uses linear annealing: 

αt = αstart + ( αend − αstart ) min 

( tTdecay 

, 1

)

.

This paper sets αstart = 0 .9, αend = 0 .35 , and Tdecay =400 . Thus, the retrieval policy relies mainly on sta-ble semantic similarity at early stages, and gradually increases the influence of the value network after accu-mulating sufficient episode feedback. Finally, the system constructs a temperature-scaled retrieval policy distribution based on the fused score: 

πcase ret (m | s) = exp (Score case (s, m )/ τc)

∑  

> m′∈C K0(s)

exp (Score case (s, m ′)/ τc) .

The system samples k cases without replacement from this distribution as injected context and adds lightweight exploration with ϵ = 0 .05 . The tempera-ture coefficient in the experiments is τc = 0 .8. This randomized strategy prevents retrieval results from concentrating too early on a small number of high-frequency cases and preserves diversity in case usage. 

4.2.2. Optimization 

The supervision signal for the case retrieval module comes from terminal verification after each closed-loop execution. If the generated model passes all verification items, then r = 1 ; otherwise, r = 0 . This reward is used to construct online training samples for the value network. For successful episodes, the actually selected cases usually make positive contributions to the current task and are therefore used as positive samples. Unselected candidates cannot simply be regarded as negative sam-ples, because some of them may also provide feasible alternative paths. To reduce the risk of false labeling, this paper samples a small number of negative samples only from unselected candidates with low fused scores. Let the selected case set in this round be Csel and the candidate set be CK0 (s). The sample construction for a successful episode is 

D+ = Csel ,

D− ∼ Sample (Bottom-N (CK0 (s) \ C sel )) .

In the current setting, five negative samples are drawn from the Bottom-20 candidate pool. For failed episodes, the system regards the selected cases in this round as negative samples: 

D− = Csel , D+ = ∅.

The value network is trained with binary cross-entropy and an entropy regularization term to main-tain exploration in the retrieval policy: 

Lcase = − ∑

> (s,m,y )∈D

[

y log fθ (z(s, m )) + (1 − y) log (1 − fθ (z(s, m )) )]

− β H(πcase ret (· | s)) .

Here, y ∈ { 0, 1} is the sample label, and β is the en-tropy regularization coefficient, set to 0.03 in the exper-iments. The entropy term is calculated directly from the retrieval distribution of the current candidate cases, keeping the value-learning objective consistent with the final injection policy. After each update, the system saves the latest network parameters and advances the annealing step of αt once. In addition to updating the value network, the case library itself expands after successful episodes. The system writes the input requirements, tool invocation sequence, execution result, multi-view renderings, ver-ification report, and modeling process summary of the current round back to the case library, and assigns an initial success mark. Therefore, the evolution of case memory contains two parts: re-estimation of the state-dependent values of existing cases, and continuous ac-cumulation of newly successful trajectories. 

4.3. Skill Library Retrieval Algorithm Design and Optimization 

4.3.1. Algorithm Design 

The main difference between skill memory and case memory is that skills are directly invoked during ex-ecution, so low-quality skills impose a higher failure cost. This paper represents skill memory as 

mskill = ⟨Script , Doc , Θ, Ω, U, stat ⟩,

where Θ denotes parameters, Ω denotes applicable con-straints, Uj ∈ [0 , 1] is the current utility estimate, and stat records statistics such as number of uses, number of successes, number of failures, most recent reward, and frozen state. When a skill enters the vector database, the system uses the Query + Requirement of the source task as the main index text and combines it with the func-tional description in the skill documentation to form a vector representation. Before candidate recall, the system first performs eligibility filtering: 

Mskill eligible = {m ∈ M skill | U ≥ Umin , ¬frozen (m)} .

The default threshold is Umin = 0 .5. This filtering mechanism excludes skills with poor historical perfor-mance or frozen status from the recall stage, reduc-ing the probability that invalid skills are repeatedly invoked. For skills that pass filtering, the system computes semantic similarity between the current state vector and the skill vector. Let em be the embedding of skill 

m. Then 

sskill sem (s, m ) = 11 + (1 − cos (es, em)) .

This formula converts cosine distance into a bounded similarity score. The system selects the K skills with the highest semantic similarity as candidates, with 

K = 15 in the experiments. During reranking, this paper no longer trains an ad-ditional neural value network for skills. Instead, his-torical utility is used as a structured prior and linearly fused with semantic similarity: Score skill (s, m ) = λsem sskill sem (s, m ) + λU Uj .

The experiments use λsem = 0 .7 and λU = 0 .3. This design is based on the following consideration: skill us-ability depends largely on the implementation quality of the script and whether applicability boundaries are satisfied, and these factors can be directly character-ized by historical utility. Finally, the system selects the Top-k skills with the highest scores as candidate injection results. In addition, the system introduces a short-term masking mechanism during local correction. If a

run_skill call fails in the previous execution round, the corresponding skill_id is temporarily excluded from retrieval in the next round. This mechanism does not permanently reject the skill, but prevents closed-loop correction from repeatedly trying the same failed path within a short period. 

4.3.2. Optimization 

The skill library uses episode-level incremental updates and updates only the skills actually called in the cur-rent round. The system extracts all run_skill records from the tool invocation log and updates their utility estimates according to the terminal reward r ∈ { 0, 1}:

Uj,t +1 = Uj,t + η(r − Uj,t 

),

where η is the learning rate, set to η = 0 .1 in this pa-per. This update is equivalent to an exponential mov-ing estimate of skill success rate: consecutive successes increase Uj , while consecutive failures decrease Uj .The system also maintains interpretable statistics for skills. Let n be the number of invocations of skill 

m, and ℓ be the most recent reward. Then 

n ← n + 1 , ℓ ← r. 

If r = 1 , the number of successes is increased by one; otherwise, the number of failures is increased by one. These statistics can be used for subsequent diagnosis, manual review, and skill restart decisions. To prevent inefficient scripts from continuously in-terfering with retrieval during long-term operation, the system executes a skill disposition strategy af-ter updates. Let the low-utility threshold be Uprune 

and the minimum observation count be nmin . When 

U < U prune and n ≥ nmin ,

mj ←

{

freeze (m), default policy ,

delete (m), optional policy .

This paper adopts the freeze policy by default and sets 

Uprune = 0 .5. Freezing keeps the skill script, documen-tation, and statistics, but removes the skill from the re-trievable set. Deletion further removes related files and index records. The freeze policy balances automatic re-trieval quality control and later manual review. Besides updating the utility of old skills, the skill library also supports new skill generation. When-ever a modeling round succeeds, the system post-processes the complete tool invocation trajectory. It first restores the actual invocation sequence into in-dependently executable FreeCAD Python code, and then judges whether reusable continuous operation fragments exist. If reuse and parameterization con-ditions are satisfied, the system automatically gen-erates the script, documentation, parameter schema, and metadata, and writes them into the skill library and vector database. Through this mechanism, the skill library can continuously extract stable operation patterns from successful tasks and gradually reduce the reasoning complexity of subsequent long-sequence modeling. 5. Experiments 

5.1. Experimental Setup 

Dataset and task split To evaluate the executabil-ity and geometric consistency of the proposed method on complex CAD modeling tasks, this paper selects abstract-level text descriptions, labels, and correspond-ing CAD models from the Text2CAD [7] dataset as experimental samples. The original samples are first cleaned and deduplicated by removing duplicate sam-ples, entries with incomplete descriptions, and entries whose target models cannot be parsed, yielding 1200 candidate samples. The samples are then further fil-tered according to the model complexity range required by the experiments, ultimately forming an experimen-tal dataset containing 1000 samples. To avoid infor-mation leakage among training, memory construction, and testing, the experimental data are divided into the following mutually exclusive subsets: • Basic knowledge base (400 samples): used to organize FreeCAD modeling syntax, common API invocation patterns, and basic script examples, and to construct the initial retrieval-augmented genera-tion knowledge base; • Memory construction and training set (400 samples): used to generate and filter entries for the case library and skill library, and for online updates of memory utility estimation and retrieval policy; • Test set (200 samples): used only for final perfor-mance evaluation and method comparison, and not involved in constructing the basic knowledge base, generating memory entries, or updating the policy. 

Experimental procedure For each test sample, the system uses its text description or label information as input, generates a FreeCAD modeling script or tool invocation sequence, and executes it through the ge-ometric kernel to obtain the model result, execution log, and verification feedback. Ablation experiments use the same base model, the same test set, and the same maximum number of correction rounds, chang-ing only memory configuration or retrieval strategy to analyze the contribution of each module to closed-loop generation stability. Comparison experiments report the output performance of different methods on the same test split. For methods that do not contain an explicit closed-loop correction process, only applicable evaluation metrics are reported. 

Evaluation metrics This paper evaluates generated results from two aspects: geometric similarity and 

process stability . The former focuses on shape close-ness between the final model and the ground-truth model, while the latter focuses on whether script gen-eration and closed-loop correction can converge sta-bly. They reflect different levels of performance, so ex-ecutability is not directly equated with geometric cor-rectness in the result analysis. 

(1) Geometric similarity metrics: After uni-form scale normalization, geometric alignment, and surface sampling of the generated model and the ground-truth model, the following three shape similar-ity metrics are used: • IoU (Intersection over Union, higher is better): measures the volume overlap between the gener-ated model and the ground-truth model in voxelized space; • CD (Chamfer Distance, lower is better): measures the average nearest-neighbor distance between the surface-sampled point cloud of the generated model and that of the ground truth; • HD (Hausdorff Distance, lower is better): measures the maximum deviation between two sets of surface-sampled points and reflects large local geometric er-rors. 

(2) Process stability metrics: To evaluate the executability of long-sequence script generation and the efficiency of closed-loop correction, this paper uses the following process metrics: • SUC (Success Rate, higher is better): the propor-tion of samples for which the final generated result can be successfully executed by the geometric kernel and pass the basic topological validity check within a limited number of correction rounds; • Pass@1 (first-attempt pass rate, higher is better): the proportion of samples whose first-round gener-ated result passes execution and verification without triggering the error-correction loop; • AVG Re (Average Retries, lower is better): the average number of closed-loop corrections triggered among finally successful samples. This metric re-flects only the correction overhead of successful sam-ples and should be analyzed together with SUC. 

5.2. Ablation Study 1: Stability Analysis with and without Memory Libraries 

To analyze the impact of memory-library configuration on system stability, this paper compares four settings under the same base model, test set, and evaluation metrics: wo-s-c , only-case , only-skill , and both-memory . The first setting uses neither the case li-brary nor the skill library; the next two enable only one type of memory; and both-memory enables both the case library and the skill library. The compar-ison focuses on how different memory configurations affect first-round generation quality, closed-loop cor-rection efficiency, and final convergence success rate.                      

> Table 1. Process stability comparison under different memory-library configurations with GPT-5.2-codex. Setting SUC ↑Pass@1 ↑AVG Re ↓
> wo-s-c 0.9494 0.7528 0.3018 only-case 0.9800 0.8200 0.2347 only-skill 0.9850 0.8100 0.2437 both-memory 0.9950 0.8300 0.3467

Table 1 shows that introducing memory libraries im-proves overall system stability. Compared with wo-s-c, which uses no memory, only-case and only-skill increase the final success rate to 0.9800 and 0.9850, respectively. This indicates that both case memory and skill memory can provide effective external sup-port for complex CAD tasks, thereby improving the executability and robustness of the closed-loop gener-ation system. A further comparison between the two single-memory configurations shows that only-case has aPass@1 of 0.8200, higher than 0.8100 for only-skill, and an AVG Re of 0.2347, lower than 0.2437 for only-skill. This suggests that the case library is more helpful in di-rectly providing historical references similar to the cur-rent task during the first-round generation stage. The model is therefore more likely to produce an executable solution on the first attempt and maintain lower ad-justment cost in subsequent correction. In contrast, although the skill library also improves the overall suc-cess rate, its effect is reflected more in supporting local construction patterns than in directly improving the overall quality of first-round generation. The AVG Re of both-memory is 0.3467, slightly higher than those of only-case and only-skill, but its SUC reaches 0.9950 and its Pass@1 reaches 0.8300, the highest among the four groups. This indicates that dual-track memory does not work simply by reduc-ing the number of corrections. Instead, it improves the probability that the system ultimately converges to correct modeling code by providing richer case ref-erences and skill reuse support during the closed-loop process. In other words, the advantage of dual-track memory mainly lies in the final success rate. Even if more necessary intermediate corrections are required, they are exchanged for higher overall stability and con-vergence. 

5.3. Ablation Study 2: Stability Analysis of Re-trieval Algorithms 

To evaluate the effect of the retrieval strategy itself on closed-loop stability, this paper compares two retrieval  

> Figure 2. Example comparison of generated results from different methods.

methods under the same memory configuration, test set, and evaluation metrics: learned retrieval and 

semantic retrieval . The former introduces execution feedback and utility reranking into the retrieval pro-cess, while the latter uses pure semantic similarity for candidate recall and ranking. This experiment aims to analyze whether different retrieval strategies signifi-cantly affect memory usability and the final closed-loop convergence performance of the system when the same memory resources are available.          

> Table 2. Comparison of system process stability under dif-ferent retrieval algorithms. Retrieval Setting SUC ↑Pass@1 ↑AVG Re ↓
> learned retrieval 0.9950 0.8300 0.3467 semantic retrieval 0.9650 0.7850 0.3161

Table 2 shows that learned retrieval outperforms se-mantic retrieval in both final success rate and first-round pass rate. Its SUC is 0.9950, higher than 0.9650 for semantic retrieval, and its Pass@1 is 0.8300, also higher than 0.7850 for semantic retrieval. This re-sult indicates that although semantic similarity alone can retrieve memories with similar text descriptions, these memories cannot always be directly converted into high-quality and executable generation paths un-der the current geometric context in complex CAD long-sequence modeling tasks. In contrast, learned retrieval introduces execution feedback and Q-value reranking, enabling it to more effectively identify cases and skills that are practically usable under the current task state. It therefore performs better in both overall Model a circular  fl at washer as a  fl at annular structure, with a circular through-hole in the center and smooth,    

> fl at top and bottom surfaces.
> Model an ellipsoid, with an overall smooth three-dimensional ellipsoidal structure and a continuous, rounded outer surface.
> Model a hex head bolt, including a hexagonal prism-shaped bolt head, a cylindrical shank, and an external threaded structure on the surface of the shank. The overall component is used for connecting and fastening mechanical parts.
> Model a three-dimensional “H”-shaped structure, composed of two vertical rectangular prism columns and one horizontal rectangular prism connecting beam in the middle.
> A slender rectangular connecting strip, with an overall fl at long-strip structure, multiple circular holes evenly distributed along its length, and right-angled ends.
> A dome-topped cylinder with a circular base, featuring a hemispherical dome on the upper part, a cylindrical body in the middle, and a fl at circular disk base at the bottom, with an overall smooth surface.
> A curved pipe fl ange joint, with square
> fl ange plates at both ends, a circular through-hole at the center of each
> fl ange, bolt holes distributed at the four corners, and a curved circular pipe connecting section in the middle.
> A rectangular frame plate, with a large rectangular through-hole in the middle, multiple small circular holes evenly distributed along the outer edge, and slightly rounded corners.

⼀个圆形⻮轮盘，外圆周均匀分布细密⻮⽛，盘⾯中⼼附近

分布多个圆孔，整体为 薄圆盘

结构。

⼀个多向圆管连接件，由⽔平

圆管、竖直圆管和斜向圆管交

汇组成，前端带有较⼩的同轴

圆柱接⼝。

> A circular rolling bearing, consisting of an outer ring, an inner ring, a central circular hole, and an annular raceway, with multiple balls evenly distributed within the raceway. The overall structure is a thick circular disk.

⼀个圆柱形滚花旋钮，侧⾯均

匀分 布竖向防滑⻮纹，顶部为

圆形平 ⾯，中⼼有⼀个圆孔。Figure 3. Example CAD models generated from text and image inputs. 

success rate and first-round generation quality. On the other hand, the AVG Re of semantic re-trieval is 0.3161, slightly lower than 0.3467 for learned retrieval. This indicates that for samples that eventu-ally converge successfully, the two retrieval strategies have similar costs in subsequent closed-loop correction, and semantic retrieval even has a slightly lower cor-rection cost among successful samples. However, its SUC and Pass@1 are clearly lower, indicating that the problem is not whether successful samples are harder to correct, but that semantic retrieval prevents more samples from entering the final successful set. In other words, the closed-loop cost of pure semantic retrieval on successful samples is acceptable, but it is weaker than learned retrieval in improving overall converga-bility. In summary, the advantage of learned retrieval is not a significant reduction in the correction overhead of every successful sample. Instead, by introducing ex-ecution feedback and utility information, it more accu-rately distinguishes between memories that are merely semantically relevant and memories that are truly us-able in the current geometric context, thereby increas-ing the probability that more samples converge suc-cessfully within a limited number of iterations. This indicates that for CAD agents, the key of retrieval is not only finding similar memories, but also identifying memories that can effectively support generation and correction under the current task state. 

5.4. Comparison Experiments 

To further evaluate the geometric consistency of CAD models generated by the proposed method, this paper conducts comparative evaluation using the test set di-vided in Section 5.1. For each test sample, the experi-ment uses the text description as input, performs infer-ence using the public weights or official configurations of each baseline method and the proposed method, gen-erates the corresponding CAD results, and then calcu-lates the metrics. This paper selects three represen-tative CAD generation or reconstruction methods as comparison baselines: • Text2CAD : an end-to-end text-to-CAD generation method, representing the supervised-learning-based text-conditioned generation route; • cadrille : a multimodal CAD reconstruction method combined with online reinforcement learn-ing, representing the learning-based CAD recon-struction route; • CADCodeVerify : a CAD code generation method with a code verification process, representing agent-style generation with execution checking. Because different methods do not have exactly the same input modalities, output representations, and in-ference processes, the comparison is mainly based on geometric similarity between the final generated model of each method and the ground-truth model, with em-phasis on geometric metrics. 

> Table 3. Overall performance comparison of different meth-ods on the test set.

Method Geometric Similarity IoU ↑ CD ↓ HD ↓

Text2CAD 0.1155 0.1098 0.3609 cadrille 0.0230 0.1524 0.4699 CADCodeVerify 0.1925 0.0827 0.2871 

Ours 0.2972 0.0788 0.1216 

Table 3 shows that the proposed method achieves the best results in this group of experiments on all three geometric similarity metrics. Under the cur-rent test set and unified evaluation procedure, the pro-posed method obtains relatively better geometric con-sistency. This improvement is related to the joint ef-fect of closed-loop execution feedback, case memory, and skill memory. Geometric-kernel feedback helps discover local modeling errors, while memory retrieval provides reusable historical experience for generating and correcting complex structures. Figure 2 compares the generated results of Text2CAD, cadrille, CADCodeVerify, and the pro-posed method. From the visualization, Text2CAD and CADCodeVerify can preserve the correspondence be-tween input descriptions and output models to a cer-tain extent, but still show deviations in complex lo-cal structures and overall shape consistency. cadrille can generate structurally valid CAD models in some samples, but the semantic match between its outputs and abstract text inputs is unstable. It produces syn-tactically valid but semantically wrong outputs, show-ing that generalizing from expert-level instructions in training data to abstract-level descriptions remains dif-ficult. The proposed method shows more stable input-alignment capability in the illustrated examples. For samples containing repeated structures, local details, or strong geometric constraints, relying only on one-shot generation without feedback can easily cause miss-ing structures or local deformation. In contrast, the proposed method uses geometric-kernel feedback in time through closed-loop verification and combines it with existing modeling patterns in the case library and skill library, providing clearer constraints for generat-ing complex structures. Therefore, under the current test setting, the proposed method can better balance model generability with semantic and geometric con-sistency. 

## 6. Conclusion 

Figure 3 shows examples in which the proposed method generates CAD models from text and image inputs. This paper focuses on problems in complex CAD model generation, including unstable long-sequence reasoning, insufficient use of geometric feedback, and difficulty in reusing historical experience. It proposes a memory-augmented reinforcement learning framework for CAD generation agents. The framework wraps the FreeCAD geometric kernel as an interactive envi-ronment and improves the observability and recover-ability of script generation through a closed-loop pro-cess of planning, execution, verification, and correc-tion. It also constructs two types of external memory, namely a case library and a skill library, and uses ex-ecution feedback to dynamically update retrieval util-ity, allowing the system to invoke historical experience more selectively in subsequent tasks. Experimental results show that after introducing memory libraries and a learned retrieval strategy, the system improves in both final success rate and first-round pass rate. In comparisons with existing methods, the proposed method performs well on geometric similarity metrics, indicating that geometric-kernel feedback and memory-assisted correction help improve the shape consistency of generated models. It should be noted that the pro-posed method still depends on the coverage of candi-date memories and the quality of geometric-kernel feed-back, and the closed-loop correction process introduces additional overhead. Future work will further study finer-grained modeling of failure causes, tool adapta-tion across CAD platforms, and more efficient memory compression and update mechanisms to improve appli-cability in real engineering scenarios. Acknowledgments 

We thank the freecad-mcp [21] open-source project. The MCP tools described in this paper are improved based on that project. 

## References 

[1] K. Alrashedy, P. Tambwekar, Z. Zaidi, M. Langwasser, W. Xu, and M. Gombolay. Generating cad code with vision-language models for 3d designs. arXiv preprint arXiv:2410.05340 , 2024. [2] Akshay Badagabettu, Sai Sravan Yarlagadda, and Amir Barati Farimani. Query2cad: Generating cad models using natural language queries. arXiv preprint arXiv:2406.00144 , 2024. [3] Cheng Chen, Jiacheng Wei, Tianrun Chen, Chi Zhang, Xiaofeng Yang, Shangzhan Zhang, Bingchen Yang, Chuan-Sheng Foo, Guosheng Lin, Qixing Huang, et al. Cadcrafter: Generating computer-aided design mod-els from unconstrained images. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pat-tern Recognition , pages 11073–11082, 2025. [4] Mark Chen, Jerry Tworek, Heewoo Jun, Qiming Yuan, Henrique Ponde De Oliveira Pinto, Jared Kaplan, Harri Edwards, Yuri Burda, Nicholas Joseph, Greg Brockman, et al. Evaluating large language models trained on code. arXiv preprint arXiv:2107.03374 ,2021. [5] Fengxiao Fan, Jingzhe Ni, Xiaolong Yin, Sirui Wang, Xingyu Lu, Qiang Zou, Ruofeng Tong, Min Tang, and Peng Du. Caddesigner: Conceptual design of cad mod-els based on general-purpose agent. arXiv preprint arXiv:2508.01031 , 2025. [6] Pradeep Kumar Jayaraman, Joseph G Lambourne, Nishkrit Desai, Karl DD Willis, Aditya Sanghi, and Nigel JW Morris. Solidgen: An autoregressive model for direct b-rep synthesis. arXiv preprint arXiv:2203.13944 , 2022. [7] Mohammad Sadil Khan, Sankalp Sinha, Talha Ud-din Sheikh, Didier Stricker, Sk Aziz Ali, and Muham-mad Zeshan Afzal. Text2cad: Generating sequen-tial cad designs from beginner-to-expert level text prompts. In Advances in Neural Information Process-ing Systems , pages 7552–7579. Curran Associates, Inc., 2024. [8] Sebastian Koch, Albert Matveev, Zhongshi Jiang, Francis Williams, Alexey Artemov, Evgeny Burnaev, Marc Alexa, Denis Zorin, and Daniele Panozzo. Abc: A big cad model dataset for geometric deep learning. In The IEEE Conference on Computer Vision and Pat-tern Recognition (CVPR) , 2019. [9] M. Kolodiazhnyi, D. Tarasov, D. Zhemchuzhnikov, A. Nikulin, I. Zisman, A. Vorontsova, A. Konushin, V. Kurenkov, and D. Rukhovich. cadrille: Multi-modal cad reconstruction with online reinforcement learning. 

arXiv preprint arXiv:2505.22914 , 2025. [10] Mingi Lee, Dongsu Zhang, Clément Jambon, and Young Min Kim. Brepdiff: Single-stage b-rep diffusion model. In Proceedings of the Special Interest Group on Computer Graphics and Interactive Techniques Con-ference Conference Papers , pages 1–11, 2025. [11] Changjian Li, Hao Pan, Adrien Bousseau, and Niloy J Mitra. Free2cad: Parsing freehand drawings into cad commands. ACM Transactions on Graphics (TOG) ,41(4):1–16, 2022. [12] Jing Li, Yihang Fu, and Falai Chen. Dtgbrep-gen: A novel b-rep generative model through decou-pling topology and geometry. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pat-tern Recognition , pages 21438–21447, 2025. [13] J. Li, Y. Luo, Y. Lou, and X. Zhou. Recad: Reinforce-ment learning enhanced parametric cad model gen-eration with vision-language models. arXiv preprint arXiv:2512.06328 , 2025. [14] J. Li, W. Ma, X. Li, Y. Lou, G. Zhou, and X. Zhou. Cad-llama: Leveraging large language models for computer-aided design parametric 3d model gen-eration. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) ,pages 18563–18573, 2025. [15] Pu Li, Wenhao Zhang, Jinglu Chen, and Dongming Yan. Stitch-a-shape: Bottom-up learning for b-rep generation. In Proceedings of the Special Interest Group on Computer Graphics and Interactive Tech-niques Conference Conference Papers , pages 1–12, 2025. [16] Xingang Li, Yuewan Sun, and Zhenghui Sha. Llm4cad: Multi-modal large language models for 3d computer-aided design generation. In International Design Engineering Technical Conferences and Computers and Information in Engineering Conference , page V006T06A015. American Society of Mechanical En-gineers, 2024. [17] X. Li, J. Li, Y. Song, Y. Lou, and X. Zhou. Seek-cad: A self-refined generative modeling for 3d parametric cad using local inference via deepseek. arXiv preprint arXiv:2505.17702 , 2025. [18] Xueyang Li, Yunzhong Lou, Yu Song, and Xiang-dong Zhou. Mamba-cad: State space model for 3d computer-aided design generative modeling. In Pro-ceedings of the AAAI Conference on Artificial Intelli-gence , pages 5013–5021, 2025. [19] D. Mallis, A. S. Karadeniz, S. Cavada, D. Rukhovich, N. Foteinopoulou, K. Cherenkova, A. Kacem, and D. Aouada. Cad-assistant: Tool-augmented vllms as generic cad task solvers. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , pages 7284–7294, 2025. [20] Brandon Man, Ghadi Nehme, Md Ferdous Alam, and Faez Ahmed. Videocad: A dataset and model for learning long-horizon 3d cad ui interactions from video. In The Thirty-ninth Annual Conference on Neural Information Processing Systems Datasets and Benchmarks Track .[21] neka-nat. freecad-mcp. https://github.com/neka-nat/freecad-mcp , 2026. Accessed: 2026-05-15. [22] Felix Ocker, Stefan Menzel, Ahmed Sadik, and Thi-ago Rios. From idea to cad: a language model-driven multi-agent system for collaborative design. arXiv preprint arXiv:2503.04417 , 2025. [23] Dacheng Qi, Chenyu Wang, Jingwei Xu, Tianzhe Chu, Zibo Zhao, Wen Liu, Wenrui Ding, Yi Ma, and Shenghua Gao. Pointer-cad: Unifying b-rep and com-mand sequences via pointer-based edges & faces selec-tion. In CVPR , 2026. [24] Feiwei Qin, Shichao Lu, Junhao Hou, Changmiao Wang, Meie Fang, and Ligang Liu. Drawing2cad: Sequence-to-sequence learning for cad generation from vector drawings. In Proceedings of the 33rd ACM In-ternational Conference on Multimedia , pages 10573– 10582, 2025. [25] D. Rukhovich, E. Dupont, D. Mallis, K. Cherenkova, A. Kacem, and D. Aouada. Cad-recode: Reverse engi-neering cad code from point clouds. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , pages 9801–9811, 2025. [26] Timo Schick, Jane Dwivedi-Yu, Roberto Dessì, Roberta Raileanu, Maria Lomeli, Eric Hambro, Luke Zettlemoyer, Nicola Cancedda, and Thomas Scialom. Toolformer: Language models can teach themselves to use tools. Advances in neural information processing systems , 36:68539–68551, 2023. [27] Hanxiao Wang, Mingyang Zhao, Yiqun Wang, Weize Quan, and Dong-Ming Yan. Vq-cad: Computer-aided design model generation with vector quantized diffu-sion. Computer Aided Geometric Design , 111:102327, 2024. [28] R. Wang, Y. Yuan, S. Sun, and J. Bian. Text-to-cad generation through infusing visual feedback in large language models. arXiv preprint arXiv:2501.19054 ,2025. [29] Siyu Wang, Cailian Chen, Xinyi Le, Qimin Xu, Lei Xu, Yanzhou Zhang, and Jie Yang. Cad-gpt: Synthesis-ing cad construction sequence with spatial reasoning-enhanced multimodal llms. In Proceedings of the AAAI Conference on Artificial Intelligence , pages 7880–7888, 2025. [30] K. D. Willis, Y. Pu, J. Luo, H. Chu, T. Du, J. G. Lam-bourne, A. Solar-Lezama, and W. Matusik. Fusion 360 gallery: A dataset and environment for programmatic cad construction from human design sequences. ACM Transactions on Graphics , 40(4):1–24, 2021. [31] R. Wu, C. Xiao, and C. Zheng. Deepcad: A deep gen-erative network for computer-aided design models. In 

Proceedings of the IEEE/CVF International Confer-ence on Computer Vision (ICCV) , pages 6772–6782, 2021. [32] S. Wu, A. Khasahmadi, M. Katz, P. K. Jayaraman, Y. Pu, K. Willis, and B. Liu. Cad-llm: Large language model for cad generation. In Advances in Neural In-formation Processing Systems (NeurIPS) , 2023. [33] S. Wu, A. H. Khasahmadi, M. Katz, P. K. Jayaraman, Y. Pu, K. Willis, and B. Liu. Cadvlm: Bridging lan-guage and vision in the generation of parametric cad sketches. In European Conference on Computer Vision (ECCV) , pages 368–384. Springer, 2024. [34] J. Xu, C. Wang, Z. Zhao, W. Liu, Y. Ma, and S. Gao. Cad-mllm: Unifying multimodality-conditioned cad generation with mllm. arXiv preprint arXiv:2411.04954 , 2024. [35] X. Xu, K. D. Willis, J. G. Lambourne, C. Y. Cheng, P. K. Jayaraman, and Y. Furukawa. Skex-gen: Autoregressive generation of cad construction se-quences with disentangled codebooks. arXiv preprint arXiv:2207.04632 , 2022. [36] X. Xu, P. K. Jayaraman, J. G. Lambourne, K. D. Willis, and Y. Furukawa. Hierarchical neural coding for controllable cad model generation. arXiv preprint arXiv:2307.00149 , 2023. [37] X. Xu, J. Lambourne, P. Jayaraman, Z. Wang, K. Willis, and Y. Furukawa. Brepgen: A b-rep generative diffusion model with structured latent geometry. ACM Transactions on Graphics , 43(4):1–14, 2024. [38] Shunyu Yao, Jeffrey Zhao, Dian Yu, Nan Du, Izhak Shafran, Karthik Narasimhan, and Yuan Cao. React: Synergizing reasoning and acting in language models. 

arXiv preprint arXiv:2210.03629 , 2022. [39] X. Yin, X. Lu, J. Shen, J. Ni, H. Li, R. Tong, M. Tang, and P. Du. Rlcad: Reinforcement learning training gym for revolution involved cad command sequence generation. Computer-Aided Design , page 104027, 2026. [40] Zeqing Yuan, Haoxuan Lan, Qiang Zou, and Junbo Zhao. 3d-premise: Can large language models generate 3D shapes with sharp features and parametric control? 

arXiv preprint arXiv:2401.06437 , 2024. [41] Huichi Zhou, Yihang Chen, Siyuan Guo, Xu Yan, Kin-Hei Lee, Zihan Wang, Ka Yiu Lee, Guchun Zhang, Kun Shao, Linyi Yang, and Jun Wang. Me-mento: Fine-tuning llm agents without fine-tuning llms. ArXiv , abs/2508.16153, 2025. [42] Huichi Zhou, Siyuan Guo, Anji Liu, Zhongwei Yu, Ziqin Gong, Bowen Zhao, Zhixun Chen, Menglong Zhang, Yihang Chen, Jinsong Li, Runyu Yang, Qiang Liu, Xinle Yu, Jianming Zhou, Na Wang, Chunyang Sun, and Jun Wang. Memento-skills: Let agents de-sign agents. 2026. 记忆增强的强化学习 CAD 生成智能 体

摘要： 计算机辅助设计（ CAD ）模型的自动生成是高端 制造领域实现智能化的核心技术。现有基于大语言模 型（ LLM ）的生成方法在面对长序列、多操作类型、强 几何约束的复杂 CAD 模型时，常因推理链条断裂和缺 乏有效纠错机制而导致结果不及预期。针对此问题，本 文提出了一种记忆增强的强化学习 CAD 生成智能体 框架。该框架将底层几何内核封装为智能体可调用的 结构化工具链，构建了“设计意图理解—全局规划—执 行—多维验证”的闭环机制。同时，设计了包含案例库 与技能库的双轨记忆体模块，并提出动态效用检索算 法。通过将强化学习引入检索与策略优化过程，智能体 能够有效规避“语义相似但几何不可行”的检索陷阱， 实现无需额外大规模标注数据的在线自纠错与持续进 化。实验表明，该方法在复杂 CAD 模型生成任务上的 成功率和几何一致性均有显著提升。 

## 1. 引言 

计算机辅助设计（ Computer-Aided Design ，CAD ）软 件是汽车、航空航天、医疗器械等高端制造领域的关 键基础工具，其参数化建模能力直接影响产品设计迭 代、工艺可制造性验证与工程知识复用效率。尽管主流 

CAD 平台 （如 CATIA 、UG NX 、PTC Creo 、OnShape 、

AutoCAD 、SolidWorks 等）在几何内核、装配约束与 特征建模等方面已高度成熟，但工程实践中的建模流 程仍主要依赖具备专业经验的工程师进行交互式手工 建模：复杂零部件往往需要频繁的特征拆解、约束调整 与局部重构，建模门槛高、周期长，且经验与建模策略 难以在团队间低成本迁移，成为制约设计效率与知识 沉淀的重要因素。 为降低建模成本，近年来研究者开始探索数据驱动 的 CAD 自动生成方法，使模型能够从既有 CAD 数据 中学习建模规律，从而减少人工干预。这类方法通常将 

CAD 建模过程视为一系列可参数化的几何操作序列， 通过对历史设计数据进行表示学习与序列建模， 实现一 定程度的自动生成。随着大语言模型（ Large Language Models ，LLMs ）在程序合成与工具调用推理方面的进 展 [4, 26, 38] ，基于自然语言生成 CAD 建模脚本逐渐 成为可行路径：设计意图有望被直接映射为可执行的 建模程序，进而支撑“意图—操作—几何结果”的快速 转换。 然而，当前基于大语言模型的 CAD 生成研究仍多 局限于“单次开环生成”范式：模型往往一次性输出完 整建模脚本，缺乏对几何内核执行过程的实时感知与 在线修正。当生成目标从低复杂度标准件扩展到复杂 

CAD 模型（长序列、多操作类型、强约束、步骤间强 依赖）时，该范式在可执行性与几何一致性上面临三重 问题：首先是执行状态的不可知性，开环生成难以显式 追踪隐式几何状态与局部拓扑变化，且几何内核反馈 往往粒度有限，导致诸如布尔运算失败、实体引用 /选择漂移、约束冲突等细微异常无法被及时定位与消解， 进而在后续步骤中被持续放大，引发级联失效；其次 是语义检索与工程效用的失配，现有检索增强多依赖 静态语义相似度，难以在具体几何上下文与约束条件 下甄别“语义相关但几何不可行”的无效案例，同时缺 乏依据执行结果进行动态策略调整与检索再排序的机 制，使得错误示例被反复召回并进一步干扰生成；最后 是缺乏技能抽象与内化能力，模型尚难像人类专家那 样将高频子任务沉淀为可复用、可参数化的高阶技能， 从而不得不在每次任务中重复底层推理，不仅效率低 下，也更易在长上下文中遗忘全局几何约束与设计意 图，降低长序列建模的稳定性与可恢复性。 针对上述问题，提出一种记忆增强 [41, 42] 的强化学 习 CAD 生成智能体框架。与一次性开环生成不同，本 文将几何内核视作可交互的环境，将建模过程组织为 “规划—执行—多维验证—修正”的闭环推理：智能体 在每一步产生局部操作并调用几何工具执行，实时获 取执行状态与几何反馈；当检测到异常或几何不一致 时，触发针对性的局部诊断与回退修正，从而以步进式 验证机制抑制错误扩散，提升长序列生成的稳定性与 可恢复性。 为使闭环过程具备可积累、可迁移的持续改进能力， 本文进一步引入长期记忆与动态效用检索机制。框架 构建案例库与技能库两类记忆：案例库以“意图—操作 轨迹—几何反馈 /结果”的形式沉淀历史成功经验，为 新任务的规划与局部修复提供可直接复用的参考；技 能库面向高频子任务将可复用的操作模式抽象为可参 数化技能，降低长序列生成的搜索空间与随机性。不同 于仅基于语义相似度的静态检索，本文通过强化学习 信号对记忆条目进行效用评估与在线更新，使检索从 “看起来相似”转向“在当前上下文更可能成功” ，从而 减少“语义相关但几何不可行”的误导案例被反复召 回，提升闭环修复的收敛效率与生成可靠性。 与现有开环脚本生成或仅进行表层迭代验证的框架 相比，本文方法的特点体现在：其一，通过几何内核执 行反馈驱动的步进式验证与修正，将错误控制在局部 步骤内，降低长序列中的级联失效风险；其二，引入可 随任务交互持续更新的记忆机制，使系统在不依赖额 外大规模标注数据的条件下实现经验积累与策略改进； 其三，通过案例复用与技能化抽象提升推理的可控性 与可复用性，更贴近工程场景对“可执行、可验证、可 修复”的生成需求。 

## 2. 相关工作 

CAD 模型生成旨在从多角度视图、边界表示模型（ B-Rep ）、文本描述或点云等输入中，生成 B-Rep 模型或 参数化建模命令序列。沿技术演进脉络，相关研究大致 可归纳为三个阶段：早期以量化与编解码器为核心的 学习式生成；以大语言模型 /多模态模型微调为代表的 代码生成式方法；以及近期强调工具调用、协同规划与 迭代验证的智能体化框架。 2.1. 基于量化与编解码器的学习式生成 在大语言模型广泛应用之前， CAD 重建与生成主要依 赖表示学习与序列建模：一类工作围绕 B-Rep 或命令 序列构建隐空间表征， 再在隐空间中进行自回归或扩散 生成。 例如， SolidGen [6] 将 B-Rep 的点、 边、 面组织为 索引化层次结构并自回归生成拓扑实体； BrepGen [37] 

将向量变分自编码器与隐空间扩散结合， 用结构化潜变 量刻画几何信息以生成 B-Rep ；DTGBrepGen [12] 进一步将拓扑生成与几何生成解耦，以提升 B-Rep 结构 有效性； BrepDiff [10] 和 Stitch-A-Shape [15] 则分别从 单阶段扩散和自底向上缝合的角度改善直接 B-Rep 生成的效率与可解释性。 面向命令序列的工作更强调将建 模过程离散化 /量化并进行神经编码，再以自回归或扩 散方式生成长序列， 例如 DeepCAD [31] 、SkexGen [35] 、

HNC-CAD [36] 、VQ-CAD [27] 与 Mamba-CAD [18] 等通过码本、分层编码、离散 token 化或状态空间模型 来学习可生成的程序表示； Pointer-CAD [23] 用“指 针式”边 /面选择把 B-Rep 几何信息融入 CAD 命令 序列生成中，让模型能做更复杂的编辑操作，同时显 著减少量化带来的拓扑错误；同时也有工作探索从草 图或工程图直接推断命令序列，如 Free2CAD [11] 和

Drawing2CAD [24] 。这一路线的优势在于训练目标明 确、生成形式结构化，且可直接对齐到“命令序列 /几何要素”等工程友好表征；但其共同瓶颈也较突出：一 方面，量化与离散 token 往往带来精度损失与误差累 积，难以满足工业级对尺寸与公差的严苛要求；另一方 面，操作集合和约束表达通常偏基础（如拉伸、旋转、 简单布尔等） ，对复杂装配约束、拓扑鲁棒性以及跨步 骤的强依赖建模能力有限。 

2.2. 基于大语言模型与多模态模型微调的生成 随着大语言模型在代码生成与工具调用推理方面的突 破， CAD 生成开始更多转向“用可执行程序表达设计 意图” 的路线， 即将输入 （文本 /图像 /草图 /点云 /B-Rep 

等）映射为参数化建模代码或命令序列。代表性工作 包括： CAD-LLM [32] 与 CadVLM [33] 通过微调大模 型实现从草图 /图像到参数化模型或草图程序的生成； 

CAD-GPT [29] 将连续空间参数离散为可由多模态大 模型预测的空间 token ，以增强图像 /文本到 CAD 序列 的空间推理； CAD-Recode [25] 面向点云反求 CAD 代码； CADCrafter [3] 则利用图像几何特征条件化潜空间 扩散，以从非受控图像生成可编译 CAD 序列。引入视 觉反馈以提升文本到 CAD 的可执行性与一致性的方 法也在出现，例如 Text-to-CAD （融入视觉反馈） [28] 。同时，多模态对齐与指令微调进一步增强了模型的条 件生成能力： CAD-MLLM [34] 尝试统一文本、图像、 点云等多模态条件； CAD-Llama [14] 通过更细粒度的 标注与指令数据提升生成的高保真度； LLM4CAD [16] 

则通过提示—程序配对数据和大模型微调探索参数化 

CAD 程序生成。更进一步地，研究者开始将几何引 擎/建模内核引入训练闭环： RLCAD [39] 将几何执行 过程纳入强化学习训练环境，以提升从 B-Rep 到命令 序列重建的几何精确性； cadrille [9] 探索在线强化学习 范式，以交互式训练改善多模态 CAD 重建；结合检索 增强与自我改进的方向（如 SeekCAD [17] ）以及强化 学习增强的视觉语言 CAD 生成（如 ReCAD [13] ）也 在提升可控性与定制性方面展现潜力。 VideoCAD [20] 

直接从视频中学习 CAD 交互建模。尽管该阶段方法显 著提高了“自然语言 /多模态输入到可执行建模程序” 的可行性，但它们普遍更依赖高质量、覆盖复杂工程分 布的训练数据；而现有开源 CAD 数据集 [8, 30] 的模 型复杂度与失败模式仍相对有限，使得模型在工业级 长序列、强约束场景下的泛化与稳定执行能力仍受到 制约。 

2.3. 基于智能体的 CAD 模型生成 为减少对大规模重训练的依赖，并利用大模型的系 统级规划能力，近期研究逐步从“单模型端到端生 成”转向“智能体化的规划—执行—验证”框架：系 统往往将 CAD 内核、渲染器、几何分析器等作为可 调用工具，通过多轮交互逐步逼近可执行结果。 CAD-Assistant [19] 提出工具增强的 VLLM 作为通用 CAD 

任务求解器，强调规划器与工具调用协同以获得零样 本泛化能力； Query2CAD [2] 进一步将自然语言查询 转化为 Python 建模代码，并借助视觉问答分数触发重 生成； 3D-PreMise [40] 提出了一种利用 LLM 通过程 序合成操控 3D 软件、生成文本驱动参数化工业形状 的框架； CADCodeVerify [1] 则通过视觉语言模型对生 成结果进行属性级检查并反馈修正。此外，也有工作提 出更显式的协同设计智能体框架，将需求分析、建模执 行和质量保证拆分为多个角色协作完成 [5, 22] 。这类 框架的关键价值在于：它们能在不显式重训练模型的 情况下，通过迭代验证、反思与工具调用来提升输出 质量，尤其适合面向多任务、跨场景的工程应用设定。 然而，在复杂模型生成任务中（例如包含大量结构面、 强拓扑依赖的零件） ，若验证信号主要来自渲染图像或 自然语言描述，其反馈往往难以精准定位底层拓扑 /约束/参数层面的根因；一旦出现实体引用漂移、布尔失 败或约束冲突等细粒度错误，仍可能在后续步骤中放 大，导致生成成功率与几何一致性受限。 与上述方法不同，本文构建了以底层几何内核反馈 为真实奖励环境、以长期记忆检索与强化学习动态效 用评估为核心的智能体闭环框架，旨在从根本上抑制 长程序生成中的级联失败，并在交互过程中实现可积 累、可迁移的经验与策略改进。 

## 3. 智能体框架 

复杂 CAD 模型的自动生成不仅要求建模脚本在语法 层面可执行，还要求中间特征、拓扑引用和几何约束在 长序列操作中保持一致。针对一次性开环生成难以及 时感知执行状态、错误易沿后续步骤传播的问题，本文 将 CAD 几何内核建模为可交互、可观测的外部环境， 并在此基础上构建记忆增强的闭环生成智能体。该框 架以大语言模型为策略推理核心，以 FreeCAD MCP 

（Model Context Protocol ）接口为执行与观测通道，通 过“规划—执行—验证—修正”的循环过程逐步生成参 设计 意图       

> “带中⼼孔和凸
> 台的六孔法
> 兰，并增加凸
> 起环”
> ⽂本
> 草图
> 多视图
> 意图理解
> 多模态
> 约束提取
> 建模源语识别
> 全局规划
> 分层蓝图
> 约束提取
> 建模源语识别
> 智能体+FreeCAD MCP
> 结构化⼯具链MCP
> 创建
> ⽂档
> 创建
> 对象
> 编辑
> 对象
> 执⾏
> 代码
> 案例记忆
> C=< 意图，⼯具轨迹，结果>
> 混合检索：语义相似度+成功率
> 强化学习驱动的效⽤更新
> 技能记忆
> S=< 脚本，⽂档>
> 参数化宏模版
> 动态效⽤检索与注⼊
> 记忆驱动的强化学习检索
> 检索⽣成反馈更新
> 避免语义相似但 ⼏何不
> 可⾏的检索结 果
> ⼏何内核反馈
> 执⾏状态
> 拓扑 变化
> 约束检查
> B-Rep/ 尺⼨信息
> 多维度验证
> 通过 不通过
> 诊断分析
> 回滚操作
> 替代⽅案
> 可执⾏CAD 模型
> 更⾼的成功率
> 更好的⼏何⼀致性
> 推理
> 修复执⾏
> 验证观察

图 1. 系统架构示意图 

数化建模脚本。系统总体结构如图 1所示。 

3.1. 整体框架 本文采用 MCP 协议对 FreeCAD 几何内核进行工具 化封装，使智能体能够以工具调用形式提交建模动作， 并从环境中获得结构化反馈。一次交互中的动作包括 

Python 建模脚本片段、参数化特征操作、文件管理指 令以及验证指令等；环境反馈则包括脚本执行状态、异 常日志、实体拓扑变化、 B-Rep 统计信息、关键尺寸测 量结果和约束检查结果，架构如图所示。与仅输出完整 脚本的生成方式相比，该交互机制将 CAD 建模中的隐 式几何状态显式化，为后续的局部诊断和策略修正提 供了可观测依据。 在具体流程上，框架将一次长序列 CAD 建模任务 分解为以下四个阶段。 

1. 设计意图理解 ：智能体首先对用户输入进行语义解 析，抽取目标对象的主要结构、功能语义、尺寸约 束、制造约束以及可能涉及的建模基元。该阶段输 出结构化需求表示，为后续规划、记忆检索和验证 指标构造提供统一输入。 

2. 全局规划 ：在获得结构化需求后，智能体生成分层 建模蓝图，将目标模型拆解为一组具有依赖关系的 子任务。蓝图不仅给出操作顺序，还显式记录各步 骤所依赖的参考面、草图、边界和参数约束，从而 减少长序列建模中常见的实体引用漂移与拓扑选择 错误。 

3. 步进式执行 ：在执行阶段，智能体遵循 ReAct 范式进行局部推理与工具调用：首先根据当前状态 和全局蓝图确定下一步操作意图，再生成可执行的 

FreeCAD Python 代码，并通过 MCP 接口提交至 几何内核运行。工具链覆盖会话管理、几何建模和 几何验证三类功能，分别用于新建 /保存 /回滚模型， 生成草图、特征、布尔、阵列和倒角等结构，以及检 查拓扑有效性、约束一致性和关键尺寸偏差。 

4. 多维验证与局部修正 ：每次工具调用完成后，系统 依据环境反馈执行局部质检。验证内容包括脚本是 否成功执行、目标实体是否生成、拓扑结构是否有 效、关键尺寸是否满足约束以及必要的几何统计量 是否处于合理范围。当检测到布尔失败、约束冲突、 实体引用失效或局部自交等异常时，智能体根据错 误日志和几何反馈定位失败原因，并在必要时回滚 至最近一次通过验证的快照，重新选择参数或替代 建模路径。 上述流程使模型生成由一次性文本预测转化为受几 何内核约束的闭环决策过程。由于每个中间步骤均需 在真实 CAD 环境中执行并通过验证，错误能够被限制 在局部操作内处理，从而降低长序列建模中的级联失 效风险，提高生成过程的可恢复性和最终模型的几何 一致性。 

3.2. 记忆体 闭环智能体的稳定运行依赖可积累和可更新的外部记 忆。 CAD 建模任务通常包含大量重复出现的局部结构 和操作模式；若每次均由大语言模型从零推理完整脚 本，不仅推理成本较高，也容易出现 API 误用、约束 遗漏和步骤间引用错误。为此，本文设计了由案例库和 技能库组成的双轨记忆体，分别用于保存历史成功轨 迹和可参数化的高阶建模操作。外部记忆记为 

Mext = Mcase ∪ M skill ,

其中 Mcase 表示案例库， Mskill 表示技能库。二者在 检索阶段共同为智能体提供上下文先验，在执行完成 后根据反馈进行更新。 

3.2.1. 案例库设计 案例库用于保存已经验证成功的建模经验，并为新任 务的规划和错误修复提供可参照轨迹。本文将案例表 示为 

C = ⟨I , T , O, ⟩,

其中 I 为用户意图及其结构化解析结果； T 为实现该 意图的工具调用轨迹，记录关键参数、中间对象引用、 回滚与修复记录等信息； O 为执行产物和验证反馈摘 要，记录最终模型、通过的验证项、关键几何统计量以 及失败后修复的原因。 在输入侧，智能体于全局规划阶段和局部修正阶段 检索案例库，将与当前任务相关且历史效用较高的案 例作为上下文注入。被召回的案例不直接替代当前推 理，而是提供可复用的建模顺序、参数选择依据和错误 处理经验。为避免单纯语义相似度导致的误召回，本文 在第四章进一步引入基于强化学习反馈的动态效用评 估，使检索结果由“语义相关”逐步转向“在当前几何 状态下更可能成功” 。在输出侧，当一次建模任务完成并通过多维验证后， 系统从执行日志中提取有效工具调用序列、最终模型 摘要和质量评估结果，生成新的案例条目写入案例库。 由此，案例库不仅承担静态知识库的作用，还能够随任 务交互不断扩充覆盖范围。 

3.2.2. 技能库设计 技能库用于保存可复用的高频建模子过程，其目标是 将稳定的底层操作模式封装为可参数化的高阶工具。 本文将技能表示为 

K = ⟨Script , Doc , Θ, Ω, ⟩,

其中 Script 为可直接运行的 FreeCAD Python 脚本， 

Doc 为技能说明文档， Θ 参数， Ω 为适用条件与约束 边界。说明文档需要描述技能功能、输入参数的物理含 义、适用几何对象、前置条件和潜在失败模式，以便大 语言模型在调用前判断技能是否适配当前状态。 在规划和修正阶段，系统从技能库中召回候选技能， 并将相应说明文档注入上下文。智能体根据当前几何 状态、全局蓝图和技能适用条件决定是否调用技能；若 决定调用，则仅需生成参数配置，而无需逐行生成底层 建模代码。这种宏操作式调用能够缩短长序列生成路 径，降低局部推理随机性，并提升高频结构构造的一致 性。 技能库还包含自动内化机制。 当任务成功完成后， 系统对执行轨迹进行后处理，识别具有复用价值的连续 操作片段，并尝试将其参数化封装为新的脚本、文档和 元数据。经验证后，新技能被注册至技能库并参与后续 检索。由此，案例库负责积累完整任务层面的经验，技 能库负责沉淀局部操作层面的可调用能力，二者共同 支撑智能体在复杂 CAD 生成任务中的持续改进。 

## 4. 记忆驱动的强化学习检索机制 

第三章给出了闭环生成智能体的总体架构。本节进一 步讨论外部记忆如何参与决策，以及如何利用执行反 馈对检索偏好进行在线优化。本文不对大语言模型参 数进行微调，而是将可学习部分限制在外部记忆的价 值评估与效用更新上。具体而言，系统在每轮任务中先 依据当前状态检索案例和技能，再由大语言模型结合 检索结果生成建模动作；任务完成后，几何验证和多视 角反馈产生的终局奖励被回传至记忆模块，用于更新 后续检索排序。该机制使系统能够在保持基础模型不 变的前提下，通过经验积累改善复杂 CAD 生成任务中 的稳定性。 

4.1. 记忆检索 M-MDP 定义 为刻画检索决策与建模执行之间的耦合关系，本文 将 CAD 生成过程形式化为记忆增强马尔可夫决策过 程（ Memory-augmented Markov Decision Process ，M-MDP ）：

P = ( X , A, T , R, Mext , γ, π ).

其中， X 表示状态空间。时刻 t 的状态 st ∈ X 由结 构化用户需求、当前几何状态、历史工具调用、错误日 志、验证反馈以及已检索记忆摘要共同组成。该状态既 包含自然语言层面的设计意图，也包含几何内核返回 的可执行状态和拓扑信息。 

A 表示动作空间。动作 at 由两部分构成：一是记忆 选择动作，即从案例库或技能库中选择用于上下文注 入或工具调用的记忆单元；二是 CAD 建模动作，即大 语言模型生成并提交给 FreeCAD 执行的脚本片段或标 准化技能调用。状态转移函数 

T : X × A → X 由几何内核的真实执行结果决定。每次动作执行后，系 统通过 MCP 接口获得新的几何状态、异常信息和验证 结果，并据此更新 st+1 。奖励函数 R 用于度量一次建模回合的终局质量。本 文采用由几何验证结果给出的二元奖励： 

r =

{

1, 脚本成功执行且生成模型通过全部验证 ,

0, 脚本执行失败或任一验证项未通过 .

其中验证项包括拓扑有效性、约束一致性、关键尺寸偏 差和必要的渲染一致性检查。 Mext 为第三章定义的外 部记忆空间，每个记忆单元 m ∈ M ext 均关联一个状 态相关的价值估计 Q(s, m )，表示在状态 s 下使用该记 忆后任务成功的期望概率。策略 π 同时包含记忆检索 策略和建模动作策略；本文优化的重点是检索策略，而 基础大语言模型负责条件化动作生成。 

4.2. 案例库检索算法设计与优化 

4.2.1. 算法设计 案例库中的记忆单元保存完整的历史建模轨迹，包括 输入意图、结构化需求、工具调用序列、执行结果、 验证反馈和成功标记。为降低无关实现细节对语义 召回的干扰，系统构建案例向量时主要采用 Query + Requirement 作为索引文本。设当前状态的查询文本 为 xs，案例 m 的索引文本为 xm，嵌入模型为 ϕ(·)，则 有

es = ϕ(xs), em = ϕ(xm).

本文采用统一嵌入模型 BAAI/bge-m3 ，并将案例向量 持久化存储于向量库。 为避免失败轨迹在早期召回阶段 干扰候选集合， 第一阶段仅在历史成功案例集合 Mcase 

> +

中进行语义召回： 

ssem (s, m ) = cos (es, em), m ∈ M case  

> +

.

随后选取语义相似度最高的 K0 个案例形成初始候选 集

CK0 (s) = TopK K0 {ssem (s, m ) | m ∈ M case  

> +

}.

当前实验设置中，初始候选规模 K0 = 20 ，最终注入案 例数 k = 5 。仅依赖语义相似度无法充分反映案例在当前几何状 态下的实际可用性。为此，本文在候选集上引入参数化 价值网络 fθ ，用于估计案例被采用后的成功概率。网 络输入由查询向量、案例向量、二者差分和逐元素乘积 拼接得到： 

z(s, m ) = [ es; em; |es − em|; es ⊙ em].

该特征同时编码语义相似性、差异方向和维度级匹配 关系。 价值网络采用带 LayerNorm 、GELU 和 Dropout 

的多层感知机，隐藏层维度为 [512 , 128] ，输出为 

sval (s, m ) = fθ (z(s, m )) ∈ [0 , 1] .

由于冷启动阶段的价值网络尚未获得充分反馈，本 文采用语义相似度和价值估计的动态融合分数进行排 序： 

Score case (s, m ) = αt ˜ssem (s, m ) + (1 − αt)˜ sval (s, m ).

其中， ˜ssem 与 ˜sval 均表示在当前候选集内经过 min-max 归一化后的分数。融合系数采用线性退火： 

αt = αstart + ( αend − αstart ) min 

( tTdecay 

, 1

)

.

本文设置 αstart = 0 .9、αend = 0 .35 、Tdecay = 400 。因 此，检索策略在早期主要依赖稳定的语义相似度，在积 累足够回合反馈后逐步提高价值网络的影响权重。 最终，系统基于融合分数构造带温度系数的检索策 略分布： 

πcase ret (m | s) = exp (Score case (s, m )/ τc)

∑  

> m′∈C K0(s)

exp (Score case (s, m ′)/ τc) .

系统从该分布中无放回采样 k 个案例作为注入上下 文，并辅以 ϵ = 0 .05 的轻量探索。实验中温度系数取 

τc = 0 .8。该随机化策略能够避免检索结果过早集中于 少量高频案例，并保持案例利用的多样性。 

4.2.2. 优化 案例检索模块的监督信号来自每轮闭环执行后的终局 验证。若生成模型通过所有验证项，则记 r = 1 ；否则 记 r = 0 。该奖励被用于构造价值网络的在线训练样本。 对于成功回合，被实际选中的案例通常对当前任务 具有正向贡献，因此作为正样本；未被选中的候选并不 能简单视为负样本，因为其中可能存在同样可行的替 代路径。为降低误标风险，本文仅从未被选中且综合分 数较低的候选中抽取少量负样本。设本轮选中案例集 合为 Csel ，候选集合为 CK0 (s)，则成功回合的样本构造 为

D+ = Csel ,

D− ∼ Sample (Bottom-N (CK0 (s) \ C sel )) .

当前设置中，从 Bottom-20 候选池中抽取 5 个负样本。 对于失败回合，系统将本轮选中案例视为负样本： 

D− = Csel , D+ = ∅.

价值网络采用二元交叉熵进行训练，并加入熵正则 项以维持检索策略的探索性： 

Lcase = − ∑

> (s,m,y )∈D

[

y log fθ (z(s, m )) + (1 − y) log (1 − fθ (z(s, m )) )]

− β H(πcase ret (· | s)) .

其中， y ∈ { 0, 1} 为样本标签， β 为熵正则系数，实验 中取 0.03 。熵项直接由当前候选案例的检索分布计算， 使价值学习目标与最终注入策略保持一致。每次更新 后，系统保存最新网络参数，并推进一次 αt 的退火步 数。 除价值网络更新外，案例库本身也会在成功回合后 扩充。系统将本轮输入需求、工具调用序列、执行结 果、多视角渲染图、验证报告和建模流程摘要写回案例 库，并赋予初始成功标记。由此，案例记忆的演化包含 两部分：一是对已有案例状态相关价值的重估，二是对 新增成功轨迹的持续积累。 

4.3. 技能库检索算法设计与优化 

4.3.1. 算法设计 技能记忆与案例记忆的主要区别在于，技能会在执行 阶段被直接调用，因此低质量技能会带来更高的失败 代价。本文将技能记忆表示为 

mskill = ⟨Script , Doc , Θ, Ω, U, stat ⟩,

其中， Θ 为参数， Ω 为适用约束， Uj ∈ [0 , 1] 为当前效 用估计， stat 记录使用次数、成功次数、失败次数、最 近奖励以及冻结状态等统计信息。 技 能 进 入 向 量 库 时， 以 来 源 任 务 的 Query + Requirement 为主要索引文本，并结合技能文档中的 功能描述形成向量表示。候选召回前，系统首先执行可 用性过滤： 

Mskill eligible = {m ∈ M skill | U ≥ Umin , ¬frozen (m)} .

默认阈值 Umin = 0 .5。该过滤机制能够将历史表现较 差或已冻结的技能排除在召回阶段之外，从而降低无 效技能被反复调用的概率。 对于通过过滤的技能，系统计算当前状态向量与技 能向量之间的语义相似度。设技能 m 的嵌入为 em，则 

sskill sem (s, m ) = 11 + (1 − cos (es, em)) .

该式将余弦距离转换为有界相似度分数。系统选取语 义相似度最高的 K 个技能作为候选，实验中 K = 15 。在重排阶段，本文不再为技能训练额外的神经价值 网络，而是将历史效用作为结构化先验与语义相似度 线性融合： 

Score skill (s, m ) = λsem sskill sem (s, m ) + λU Uj .

实验中取 λsem = 0 .7、λU = 0 .3。该设计基于如下考虑： 技能的可用性在较大程度上取决于脚本实现质量和适 用边界是否满足，而这些因素能够由历史效用进行直 接刻画。最终，系统选择得分最高的 Top-k 技能作为 候选注入结果。 此外，系统在局部修正阶段引入短期屏蔽机制。若 某个 run_skill 调用在上一轮执行中失败，则对应 

skill_id 会在下一轮检索中被临时排除。该机制并不 永久否定该技能，而是避免闭环修正在短时间内重复 尝试同一失败路径。 

4.3.2. 优化 技能库采用回合式增量更新，且只更新本轮实际调用 过的技能。 系统从工具调用日志中提取所有 run_skill 

记录，并根据终局奖励 r ∈ { 0, 1} 更新其效用估计： 

Uj,t +1 = Uj,t + η(r − Uj,t 

),

其中， η 为学习率，本文取 η = 0 .1。该更新等价于对技 能成功率进行指数滑动估计：连续成功会提高 Uj ，连 续失败则会降低 Uj 。系统同时维护技能的可解释统计信息。设 n 为技能 

m 的调用次数， ℓ 为最近一次奖励，则 

n ← n + 1 , ℓ ← r. 

若 r = 1 ，则成功次数加一；否则失败次数加一。这些 统计量可用于后续诊断、人工审核和技能重启判断。 为避免长期运行过程中低效脚本持续干扰检索，系 统在更新后执行技能处置策略。设低效阈值为 Uprune ，最小观测次数为 nmin ；当 U < U prune 且 n ≥ nmin 时， 

mj ←

{

freeze (m), 默认策略 ,

delete (m), 可选策略 .

本文默认采用冻结策略，并设置 Uprune = 0 .5。冻结会 保留技能脚本、文档和统计信息，但将其从可检索集合 中移除；删除则进一步清理相关文件和索引记录。冻结 策略兼顾了自动检索质量控制和后续人工复核需求。 除旧技能效用更新外，技能库还支持新技能生成。 每当一轮建模成功后，系统对完整工具调用轨迹进 行后处理：首先将实际调用序列还原为可独立运行的 

FreeCAD Python 代码；随后判断其中是否存在可复用 的连续操作片段；若满足复用性和参数化条件，则自动 生成脚本、说明文档、参数模式和元数据，并写入技能 库及向量数据库。通过上述机制，技能库能够从成功任 务中持续抽取稳定操作模式，并逐步降低后续长序列 建模的推理复杂度。 

## 5. 实验 

5.1. 实验设置 

数 据 集 与 任 务 划 分 为 评 估 本 文 方 法 在 复 杂 CAD 

建 模 任 务 中 的 可 执 行 性 与 几 何 一 致 性， 本 文 选 取 

Text2CAD [7] 数据集中的抽象级别的文本描述、标 签信息及对应 CAD 模型作为实验样本。首先对原始样 本进行清洗与去重，剔除重复样本、描述信息不完整或 目标模型不可解析的条目，得到 1200 个候选样本；随 后依据实验所需的模型复杂度范围进一步筛选，最终 形成包含 1000 个样本的实验数据集。为避免训练、记 忆构建与测试过程之间的信息泄漏，实验数据按用途 划分为以下互斥子集： 

• 基础知识库（ 400 个） ：用于整理 FreeCAD 建模语 法、常用 API 调用方式和基础脚本示例，构建初始 检索增强生成知识库； • 记忆构建与训练集（ 400 个） ：用于生成并筛选案例 库、技能库条目，同时用于记忆效用估计与检索策 略的在线更新； 

• 测试集（ 200 个） ：仅用于最终性能评估和方法对比， 不参与基础知识库构建、记忆条目生成或策略更新。 

实验流程 对每个测试样本，系统以文本描述或标签 信息作为输入，生成 FreeCAD 建模脚本或工具调用序 列，并通过几何内核执行得到模型结果、执行日志和验 证反馈。消融实验在相同基础模型、相同测试集和相同 最大修正轮数下，仅改变记忆配置或检索策略，以分析 各模块对闭环生成稳定性的影响。对比实验则在同一 测试划分上统计不同方法的输出表现；对于不包含显 式闭环修正过程的方法，仅报告其可适用的评价指标。 

评价指标 本文从 几何相似度 和过程稳定性 两个方面 评价生成结果。前者关注最终模型与真值模型之间的 形状接近程度，后者关注脚本生成和闭环修正过程是 否能够稳定收敛。二者分别反映不同层面的性能，因此 在结果分析中不将可执行性直接等同于几何正确性。 

(1) 几 何 相 似 度 指 标： 对 生 成 模 型 与 真 值 模 型 （Ground Truth ）进行统一尺度归一化、几何对齐与 表面采样后，采用以下三类形状相似度指标： 

• IoU （Intersection over Union ，交并比，越大越好） ：衡量生成模型与真值模型在体素化空间中的体积重 合程度； 

• CD （Chamfer Distance ，倒角距离，越小越好） ：衡 量生成模型表面采样点云与真值表面采样点云之间 的平均最近邻距离； 

• HD （Hausdorff Distance ，豪斯多夫距离，越小越 好） ：衡量两组表面采样点之间的最大偏差，用于反 映局部较大几何误差。 

(2) 过程稳定性指标： 为评估长序列脚本生成的可 执行性与闭环修正效率，本文采用以下过程指标： 

• SUC （Success Rate ，最终成功率，越大越好） ：在 限定修正轮数内，最终生成结果能够被几何内核成 功执行并通过基本拓扑有效性检查的样本比例； 

• Pass@1 （一次生成通过率，越大越好） ：不触发错 误修正闭环时，首轮生成结果即通过执行与验证的 样本比例； 

• AVG Re （Average Retries ，平均重试次数，越小 越好） ：在最终成功的样本中，系统触发闭环修正的 平均次数。该指标仅反映成功样本的修正开销，需 结合 SUC 共同分析。 

5.2. 消融实验 1：是否使用记忆库的稳定性分析 为分析记忆库配置对系统稳定性的影响， 本文在相同基 础模型、相同测试集和相同评价指标下，比较 wo-s-c 、

only-case 、only-skill 以及 both-memory 四种设置。 前者表示不使用案例库和技能库，后两者分别表示仅 启用单一类型记忆，而 both-memory 表示同时启用案 例库与技能库。比较重点在于考察不同记忆配置对首 轮生成质量、闭环修正效率以及最终收敛成功率的影 响。                  

> 表1. GPT-5.2-codex 下不同记忆库配置的过程稳定性对比 设置 SUC ↑Pass@1 ↑AVG Re ↓
> wo-s-c 0.9494 0.7528 0.3018 only-case 0.9800 0.8200 0.2347 only-skill 0.9850 0.8100 0.2437 both-memory 0.9950 0.8300 0.3467

从表 1 可以看出，引入记忆库能够整体提升系统稳 定性。与不使用记忆的 wo-s-c 相比， only-case 和 only-skill 在最终成功率上分别提升至 0.9800 和 0.9850 ，说 明无论是案例记忆还是技能记忆，都能够为复杂 CAD 

任务提供有效的外部支持，从而提升闭环生成系统的 可执行性和鲁棒性。 进一步比较两种单记忆配置， only-case 的 Pass@1 

为 0.8200 ，高于 only-skill 的 0.8100 ，同时其 AVG Re 

为 0.2347 ，低于 only-skill 的 0.2437 。这表明案例库更 有利于首轮生成阶段直接提供与当前任务相近的历史 参考，使模型更容易在第一次尝试中给出可执行方案， 并在后续修正中保持较低的调整成本。相比之下，技能 库虽然同样能够提升整体成功率，但其作用更多体现 为对局部构造模式的支持，而非直接改善首轮整体生 成质量。 

both-memory 的 AVG Re 为 0.3467 ，略高于 only-case 和 only-skill ，但其 SUC 达到 0.9950 ，Pass@1 也达到四组中的最高值 0.8300 ，说明双轨记忆并不是单 纯通过减少修正次数来发挥作用，而是通过在闭环过 程中提供更充分的案例参考与技能复用支持，提升系 统最终收敛到正确建模代码的概率。换言之，双轨记忆 的优势主要体现在最终成功率层面，即使需要经历更 多必要的中间修正，仍然能够换来更高的整体稳定性 与收敛性。 

5.3. 消融实验 2：检索算法的稳定性分析 为评估检索策略本身对闭环稳定性的影响，本文在 相同记忆配置、相同测试集和相同评价指标下，比较 

learned retrieval 与 semantic retrieval 两种检索 方式。前者在检索过程中引入执行反馈与效用重排机 制，后者则采用纯语义相似度进行候选召回与排序。该 实验旨在分析：在拥有相同记忆资源的前提下，不同检 索策略是否会显著影响记忆的可用性以及系统最终的 闭环收敛表现。            

> 表2. 不同检索算法下系统过程稳定性的对比 检索设置 SUC ↑Pass@1 ↑AVG Re ↓
> learned retrieval 0.9950 0.8300 0.3467 semantic retrieval 0.9650 0.7850 0.3161

表 2 显示， learned retrieval 在最终成功率和首轮通 该物体是⼀个 L 形 CAD 模

> 型，具有矩形顶部和两条垂
> 直⽀腿。顶部靠近其中⼀条
> ⽀腿的位置有⼀个⼩圆孔。
> ⼀个简单的⽅形桌⼦，有四
> 条腿。桌⾯和桌腿的宽度相
> 同。
> 该物体是⼀个三 层架⼦，具
> 有矩形底座 和三层⽔平搁
> 板。整体结构对称，设计 简
> 约。
> ⼀把圆柱形扳⼿，⼀端有六
> 边形开 ⼝，另⼀端有圆形开
> ⼝。该扳⼿具有扁平的矩 形
> 横截⾯。
> 本⽂⽅法
> Text2CAD CADCodeverify cadrille

图 2. 不同方法生成结果示例对比 

过率上均优于 semantic retrieval ：其 SUC 为 0.9950 ，高于 semantic retrieval 的 0.9650 ；Pass@1 为 0.8300 ，也高于 semantic retrieval 的 0.7850 。该结果表明，仅 依赖语义相似度虽然能够召回文本描述上相近的记忆， 但在复杂 CAD 长序列建模任务中，这些记忆并不总能 在当前几何上下文中直接转化为高质量、可执行的生 成路径。相比之下， learned retrieval 通过引入执行反 馈与 Q 值重排，能够更有效地识别在当前任务状态下 更具实际可用性的案例和技能，因此在整体成功率与 首轮生成质量上都表现更优。 另一方面， semantic retrieval 的 AVG Re 为 0.3161 ，略低于 learned retrieval 的 0.3467 。这说明对于最终能 够成功收敛的样本而言，两种检索策略在后续闭环修 正成本上差距并不大， semantic retrieval 在成功样本上 的修正代价甚至略低。然而，其 SUC 和 Pass@1 明显 更低， 说明问题并不在于 “成功样本是否更难修正” ，而在于 semantic retrieval 让更多样本未能进入最终成功 集合。换言之，纯语义检索在成功样本上的闭环成本是 可接受的，但它在提升整体可收敛性方面弱于 learned retrieval 。综上所述， learned retrieval 的优势不在于显著降低 每一个成功样本的修正开销，而在于通过执行反馈与 效用信息的引入，更准确地区分“语义相关”与“当前 几何上下文中真正可用”的记忆，从而提升更多样本在 有限迭代次数内成功收敛的概率。这表明，对于 CAD 

智能体而言，检索机制的关键不仅是找到相似记忆，更 是识别那些能够在当前任务状态下有效支持生成与修 正的记忆。 

5.4. 对比实验 为进一步评估本文方法生成 CAD 模型的几何一致性， 本文使用第 5.1 节中划分的测试集进行对比评估。对于 每个测试样本，实验以文本描述信息作为输入，分别使 用各基线方法的公开权重或官方配置以及本文方法进 行推理，生成对应 CAD 结果后再进行指标计算。本文 选取三种具有代表性的 CAD 生成或重建方法作为对 比基线： 

• Text2CAD ：端到端文本到 CAD 生成方法，代表 基于监督学习的文本条件生成路线； 

• cadrille ：结合在线强化学习的多模态 CAD 重建方 法，代表学习式 CAD 重建路线； 

• CADCodeVerify ：带有代码验证过程的 CAD 代码生成方法，代表引入执行检查的智能体式生成路 线。 由于不同方法的输入模态、输出表示和推理流程并 不完全相同，因此主要依据各方法最终生成模型与真 值模型之间的几何相似度进行比较，重点考察生成结 果在几何指标上的表现。 

表 3. 不同方法在测试集上的整体性能对比 方法 几何相似度 

IoU ↑ CD ↓ HD ↓

Text2CAD 0.1155 0.1098 0.3609 cadrille 0.0230 0.1524 0.4699 CADCodeVerify 0.1925 0.0827 0.2871 

本文方法（ Ours ） 0.2972 0.0788 0.1216 

从表 3 中可以看出，本文方法在三项几何相似度指 标上均取得了该组实验中的最优结果，在当前测试集 和统一评价流程下，本文方法能够获得相对更好的几 何一致性。该改进与闭环执行反馈、案例记忆和技能记 忆共同作用有关：几何内核反馈有助于发现局部建模 错误，记忆检索则为复杂结构的生成与修正提供了可 复用的历史经验。 图 2 比较了 Text2CAD 、cadrille 、CADCodeVerify 

与本文方法的生成结果。从可视化结果看， Text2CAD 

和 CADCodeVerify 能够在一定程度上保持输入描述与 输出模型之间的对应关系，但在复杂局部结构和整体 形状一致性方面仍存在偏差。 cadrille 在部分样本中能 够生成结构上有效的 CAD 模型，但其输出与抽象文本 输入之间的语义匹配并不稳定，生成了语法有效但语 义错误的输出，说明从训练数据中的专家级指令泛化 到抽象级别的描述仍具有一定难度。 本文方法在图示样例中表现出更稳定的输入对齐能 力。对于包含重复结构、局部细节或较强几何约束的样 本，单纯依赖一次性无反馈生成容易造成结构缺失或 局部变形；而本文方法通过闭环验证及时利用几何内 核反馈，并结合案例库和技能库中已有的建模模式，为 复杂结构的生成提供了更明确的约束。因此，本文方法 在当前测试设置下能够较好地兼顾模型可生成性和语 义几何一致性。 

## 6. 结语 

图 3展示了本文方法使用文本和图片作为输入，生成 

CAD 模型的示例。 建模⼀个圆形平 垫圈，整体为 

> 扁平圆环结构，中间有圆形通
> 孔，上下 表⾯平整。
> 建模⼀个椭圆球体，整体为 光
> 滑的三维椭球形结构，外表⾯
> 连续圆润。
> 建模⼀个六⻆头螺栓，包含六
> 棱柱形螺栓头、圆柱形螺杆以
> 及螺杆表⾯的外螺纹结 构，整
> 体⽤于机械零部件的连接与紧
> 固。
> 建模⼀个⽴体“H” 形结构，由两
> 根竖直⻓⽅体柱和中间⼀根⽔
> 平⻓⽅体连接梁组成
> ⼀个细⻓矩形连接条，整体为
> 扁平⻓条结构，沿⻓度⽅向均
> 匀分 布多个圆孔，两 端为直⻆
> 边。
> ⼀个带圆形底座 的圆顶柱体，上部为半球形圆顶，中间为圆
> 柱体，下 ⽅为扁平圆盘底座 ，
> 整体表⾯圆滑。
> ⼀个弯曲管道法兰接头，两 端
> 为⽅形法兰盘，法兰中⼼有圆
> 形通孔，四⻆分布螺栓孔，中
> 间为弯曲圆管连接段。
> ⼀个矩形框架板 ，中间有⼤矩
> 形通孔，外圈边缘均匀分 布多
> 个⼩圆孔，四⻆略带圆⻆。
> ⼀个圆形⻮轮盘，外圆周均匀分布细密⻮⽛，盘⾯中⼼附近
> 分布多个圆孔，整体为 薄圆盘
> 结构。
> ⼀个多向圆管连接件，由⽔平
> 圆管、竖直圆管和斜向圆管交
> 汇组成，前端带有较⼩的同轴
> 圆柱接⼝。
> ⼀个圆形滚动轴承，包含外
> 圈、内圈、中⼼圆孔和环形滚
> 道，滚道内均匀分 布多个滚
> 珠，整体为 厚圆盘结构。
> ⼀个圆柱形滚花旋钮，侧⾯均
> 匀分 布竖向防滑⻮纹，顶部为
> 圆形平 ⾯，中⼼有⼀个圆孔。

图 3. 使用文本和图片作为输入生成模型示例 

本文围绕复杂 CAD 模型生成中长序列推理不稳定、 几何反馈利用不足以及历史经验难以复用等问题，提 出了一种记忆增强的强化学习 CAD 生成智能体框架。 该框架将 FreeCAD 几何内核封装为可交互环境，通过 “规划—执行—验证—修正”的闭环流程提高脚本生成 过程的可观测性和可恢复性；同时构建案例库与技能 库两类外部记忆，并利用执行反馈对检索效用进行动 态更新，使系统能够在后续任务中更有针对性地调用 历史经验。实验结果表明，引入记忆库和学习式检索策 略后，系统在最终成功率和首轮通过率上均有所提升； 在与现有方法的对比中，本文方法在几何相似度指标 上表现较好，说明几何内核反馈和记忆辅助修正有助 于改善生成模型的形状一致性。需要指出的是，本文 方法仍依赖候选记忆的覆盖范围和几何内核反馈质量， 且闭环修正过程会带来一定额外开销。后续工作将进 一步研究更细粒度的失败原因建模、跨 CAD 平台的工 具适配以及更高效的记忆压缩与更新机制，以提升方 法在真实工程场景中的适用性。 

## 致谢 

感谢 freecad-mcp [21] 开源项目，本文所述的 MCP 工具在其基础上改进而来。 

## 参考文献 

[1] K. Alrashedy, P. Tambwekar, Z. Zaidi, M. Langwasser, W. Xu, and M. Gombolay. Generating cad code with vision-language models for 3d designs. arXiv preprint arXiv:2410.05340 , 2024. [2] Akshay Badagabettu, Sai Sravan Yarlagadda, and Amir Barati Farimani. Query2cad: Generating cad models using natural language queries. arXiv preprint arXiv:2406.00144 , 2024. [3] Cheng Chen, Jiacheng Wei, Tianrun Chen, Chi Zhang, Xiaofeng Yang, Shangzhan Zhang, Bingchen Yang, Chuan-Sheng Foo, Guosheng Lin, Qixing Huang, et al. Cadcrafter: Generating computer-aided design mod-els from unconstrained images. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pat-tern Recognition , pages 11073–11082, 2025. [4] Mark Chen, Jerry Tworek, Heewoo Jun, Qiming Yuan, Henrique Ponde De Oliveira Pinto, Jared Kaplan, Harri Edwards, Yuri Burda, Nicholas Joseph, Greg Brockman, et al. Evaluating large language models trained on code. arXiv preprint arXiv:2107.03374 ,2021. [5] Fengxiao Fan, Jingzhe Ni, Xiaolong Yin, Sirui Wang, Xingyu Lu, Qiang Zou, Ruofeng Tong, Min Tang, and Peng Du. Caddesigner: Conceptual design of cad mod-els based on general-purpose agent. arXiv preprint arXiv:2508.01031 , 2025. [6] Pradeep Kumar Jayaraman, Joseph G Lambourne, Nishkrit Desai, Karl DD Willis, Aditya Sanghi, and Nigel JW Morris. Solidgen: An autoregressive model for direct b-rep synthesis. arXiv preprint arXiv:2203.13944 , 2022. [7] Mohammad Sadil Khan, Sankalp Sinha, Talha Ud-din Sheikh, Didier Stricker, Sk Aziz Ali, and Muham-mad Zeshan Afzal. Text2cad: Generating sequen-tial cad designs from beginner-to-expert level text prompts. In Advances in Neural Information Process-ing Systems , pages 7552–7579. Curran Associates, Inc., 2024. [8] Sebastian Koch, Albert Matveev, Zhongshi Jiang, Francis Williams, Alexey Artemov, Evgeny Burnaev, Marc Alexa, Denis Zorin, and Daniele Panozzo. Abc: A big cad model dataset for geometric deep learning. In The IEEE Conference on Computer Vision and Pat-tern Recognition (CVPR) , 2019. [9] M. Kolodiazhnyi, D. Tarasov, D. Zhemchuzhnikov, A. Nikulin, I. Zisman, A. Vorontsova, A. Konushin, V. Kurenkov, and D. Rukhovich. cadrille: Multi-modal cad reconstruction with online reinforcement learning. 

arXiv preprint arXiv:2505.22914 , 2025. [10] Mingi Lee, Dongsu Zhang, Clément Jambon, and Young Min Kim. Brepdiff: Single-stage b-rep diffusion model. In Proceedings of the Special Interest Group on Computer Graphics and Interactive Techniques Con-ference Conference Papers , pages 1–11, 2025. [11] Changjian Li, Hao Pan, Adrien Bousseau, and Niloy J Mitra. Free2cad: Parsing freehand drawings into cad commands. ACM Transactions on Graphics (TOG) ,41(4):1–16, 2022. [12] Jing Li, Yihang Fu, and Falai Chen. Dtgbrep-gen: A novel b-rep generative model through decou-pling topology and geometry. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pat-tern Recognition , pages 21438–21447, 2025. [13] J. Li, Y. Luo, Y. Lou, and X. Zhou. Recad: Reinforce-ment learning enhanced parametric cad model gen-eration with vision-language models. arXiv preprint arXiv:2512.06328 , 2025. [14] J. Li, W. Ma, X. Li, Y. Lou, G. Zhou, and X. Zhou. Cad-llama: Leveraging large language models for computer-aided design parametric 3d model gen-eration. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) ,pages 18563–18573, 2025. [15] Pu Li, Wenhao Zhang, Jinglu Chen, and Dongming Yan. Stitch-a-shape: Bottom-up learning for b-rep generation. In Proceedings of the Special Interest Group on Computer Graphics and Interactive Tech-niques Conference Conference Papers , pages 1–12, 2025. [16] Xingang Li, Yuewan Sun, and Zhenghui Sha. Llm4cad: Multi-modal large language models for 3d computer-aided design generation. In International Design Engineering Technical Conferences and Computers and Information in Engineering Conference , page V006T06A015. American Society of Mechanical En-gineers, 2024. [17] X. Li, J. Li, Y. Song, Y. Lou, and X. Zhou. Seek-cad: A self-refined generative modeling for 3d parametric cad using local inference via deepseek. arXiv preprint arXiv:2505.17702 , 2025. [18] Xueyang Li, Yunzhong Lou, Yu Song, and Xiang-dong Zhou. Mamba-cad: State space model for 3d computer-aided design generative modeling. In Pro-ceedings of the AAAI Conference on Artificial Intelli-gence , pages 5013–5021, 2025. [19] D. Mallis, A. S. Karadeniz, S. Cavada, D. Rukhovich, N. Foteinopoulou, K. Cherenkova, A. Kacem, and D. Aouada. Cad-assistant: Tool-augmented vllms as generic cad task solvers. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , pages 7284–7294, 2025. [20] Brandon Man, Ghadi Nehme, Md Ferdous Alam, and Faez Ahmed. Videocad: A dataset and model for learning long-horizon 3d cad ui interactions from video. In The Thirty-ninth Annual Conference on Neural Information Processing Systems Datasets and Benchmarks Track .[21] neka-nat. freecad-mcp. https://github.com/neka-nat/freecad-mcp , 2026. Accessed: 2026-05-15. [22] Felix Ocker, Stefan Menzel, Ahmed Sadik, and Thi-ago Rios. From idea to cad: a language model-driven multi-agent system for collaborative design. arXiv preprint arXiv:2503.04417 , 2025. [23] Dacheng Qi, Chenyu Wang, Jingwei Xu, Tianzhe Chu, Zibo Zhao, Wen Liu, Wenrui Ding, Yi Ma, and Shenghua Gao. Pointer-cad: Unifying b-rep and com-mand sequences via pointer-based edges & faces selec-tion. In CVPR , 2026. [24] Feiwei Qin, Shichao Lu, Junhao Hou, Changmiao Wang, Meie Fang, and Ligang Liu. Drawing2cad: Sequence-to-sequence learning for cad generation from vector drawings. In Proceedings of the 33rd ACM In-ternational Conference on Multimedia , pages 10573– 10582, 2025. [25] D. Rukhovich, E. Dupont, D. Mallis, K. Cherenkova, A. Kacem, and D. Aouada. Cad-recode: Reverse engi-neering cad code from point clouds. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , pages 9801–9811, 2025. [26] Timo Schick, Jane Dwivedi-Yu, Roberto Dessì, Roberta Raileanu, Maria Lomeli, Eric Hambro, Luke Zettlemoyer, Nicola Cancedda, and Thomas Scialom. Toolformer: Language models can teach themselves to use tools. Advances in neural information processing systems , 36:68539–68551, 2023. [27] Hanxiao Wang, Mingyang Zhao, Yiqun Wang, Weize Quan, and Dong-Ming Yan. Vq-cad: Computer-aided design model generation with vector quantized diffu-sion. Computer Aided Geometric Design , 111:102327, 2024. [28] R. Wang, Y. Yuan, S. Sun, and J. Bian. Text-to-cad generation through infusing visual feedback in large language models. arXiv preprint arXiv:2501.19054 ,2025. [29] Siyu Wang, Cailian Chen, Xinyi Le, Qimin Xu, Lei Xu, Yanzhou Zhang, and Jie Yang. Cad-gpt: Synthesis-ing cad construction sequence with spatial reasoning-enhanced multimodal llms. In Proceedings of the AAAI Conference on Artificial Intelligence , pages 7880–7888, 2025. [30] K. D. Willis, Y. Pu, J. Luo, H. Chu, T. Du, J. G. Lam-bourne, A. Solar-Lezama, and W. Matusik. Fusion 360 gallery: A dataset and environment for programmatic cad construction from human design sequences. ACM Transactions on Graphics , 40(4):1–24, 2021. [31] R. Wu, C. Xiao, and C. Zheng. Deepcad: A deep gen-erative network for computer-aided design models. In 

Proceedings of the IEEE/CVF International Confer-ence on Computer Vision (ICCV) , pages 6772–6782, 2021. [32] S. Wu, A. Khasahmadi, M. Katz, P. K. Jayaraman, Y. Pu, K. Willis, and B. Liu. Cad-llm: Large language model for cad generation. In Advances in Neural In-formation Processing Systems (NeurIPS) , 2023. [33] S. Wu, A. H. Khasahmadi, M. Katz, P. K. Jayaraman, Y. Pu, K. Willis, and B. Liu. Cadvlm: Bridging lan-guage and vision in the generation of parametric cad sketches. In European Conference on Computer Vision (ECCV) , pages 368–384. Springer, 2024. [34] J. Xu, C. Wang, Z. Zhao, W. Liu, Y. Ma, and S. Gao. Cad-mllm: Unifying multimodality-conditioned cad generation with mllm. arXiv preprint arXiv:2411.04954 , 2024. [35] X. Xu, K. D. Willis, J. G. Lambourne, C. Y. Cheng, P. K. Jayaraman, and Y. Furukawa. Skex-gen: Autoregressive generation of cad construction se-quences with disentangled codebooks. arXiv preprint arXiv:2207.04632 , 2022. [36] X. Xu, P. K. Jayaraman, J. G. Lambourne, K. D. Willis, and Y. Furukawa. Hierarchical neural coding for controllable cad model generation. arXiv preprint arXiv:2307.00149 , 2023. [37] X. Xu, J. Lambourne, P. Jayaraman, Z. Wang, K. Willis, and Y. Furukawa. Brepgen: A b-rep generative diffusion model with structured latent geometry. ACM Transactions on Graphics , 43(4):1–14, 2024. [38] Shunyu Yao, Jeffrey Zhao, Dian Yu, Nan Du, Izhak Shafran, Karthik Narasimhan, and Yuan Cao. React: Synergizing reasoning and acting in language models. 

arXiv preprint arXiv:2210.03629 , 2022. [39] X. Yin, X. Lu, J. Shen, J. Ni, H. Li, R. Tong, M. Tang, and P. Du. Rlcad: Reinforcement learning training gym for revolution involved cad command sequence generation. Computer-Aided Design , page 104027, 2026. [40] Zeqing Yuan, Haoxuan Lan, Qiang Zou, and Junbo Zhao. 3d-premise: Can large language models generate 3D shapes with sharp features and parametric control? 

arXiv preprint arXiv:2401.06437 , 2024. [41] Huichi Zhou, Yihang Chen, Siyuan Guo, Xu Yan, Kin-Hei Lee, Zihan Wang, Ka Yiu Lee, Guchun Zhang, Kun Shao, Linyi Yang, and Jun Wang. Me-mento: Fine-tuning llm agents without fine-tuning llms. ArXiv , abs/2508.16153, 2025. [42] Huichi Zhou, Siyuan Guo, Anji Liu, Zhongwei Yu, Ziqin Gong, Bowen Zhao, Zhixun Chen, Menglong Zhang, Yihang Chen, Jinsong Li, Runyu Yang, Qiang Liu, Xinle Yu, Jianming Zhou, Na Wang, Chunyang Sun, and Jun Wang. Memento-skills: Let agents de-sign agents. 2026.