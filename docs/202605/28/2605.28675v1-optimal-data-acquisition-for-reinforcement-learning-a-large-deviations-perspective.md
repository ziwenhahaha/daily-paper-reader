---
title: "Optimal Data Acquisition for Reinforcement Learning: A Large Deviations Perspective"
authors: "Mingjie Hu, Jian-Qiang Hu, Enlu Zhou"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28675v1"
tags: ["query:rl"]
score: 9.0
evidence: 基于大偏差理论的强化学习数据获取框架
tldr: 为无限时域强化学习开发了大偏差理论框架以实现最优数据获取。
source: arxiv
selection_source: fresh_fetch
motivation: 基于大偏差理论的强化学习数据获取框架。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Data acquisition efficiency is a central challenge in deploying reinforcement learning in business and healthcare operations, where interactions are costly, slow, and often involve humans in the loop. This paper develops a unified large deviations framework for data acquisition in infinite-horizon reinforcement learning. We introduce the exponential decay rate of the policy-selection error probability as a principled efficiency metric and derive a variational characterization of this rate via large deviations theory for Markov chains, yielding a nested optimization problem. Based on this characterization, we formalize two complementary notions of optimality in terms of the optimal solution of the nested problem. Because the resulting program is implicit and generally intractable, we propose a tractable convex relaxation with explicit constraints. We then develop a lazy one-step projected subgradient method to solve the relaxed problem and use its iterates to construct an adaptive data acquisition policy. We prove that the resulting reinforcement learning algorithm is near-robustly optimal under our optimality criterion, up to a constant factor. Finally, we extend the framework to linear function approximation to improve scalability, and numerical experiments support the effectiveness of the proposed approach.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义（研究动机和背景）

- **研究背景**：在商业和医疗运营中部署强化学习时，数据获取效率是关键挑战。这些场景中，数据收集需要现场实验或人在循环中的交互，每个样本都成本高昂且耗时。
- **核心问题**：固定预算下如何高效获取数据以识别最优策略。传统强化学习算法忽视操作实验成本，导致效率低下。
- **研究目标**：提出新的效率度量——策略选择错误概率的指数衰减率，并建立最优性理论框架。

## 论文提出的方法论

### 核心思想
- 建立统一的大偏差理论框架，利用无限时域折扣MDP中马尔可夫链的大偏差理论推导出错误衰减速率的变分特征。
- 提出精确最优性和鲁棒最优性两种最优性概念作为评估基准。

### 关键技术细节
- **速率函数推导**：通过Lemma 1-2将全局错误事件分解为局部状态-动作偏差事件，推导速率函数 G_{s,a} 的变分表示。
- **凸松弛**：为解决隐式嵌套优化问题，提出可处理的凸松弛方法（Theorem 2），得到优化问题：max_{ω∈W} min_{s,a≠π*(s)} L_{sa}(ω, M)^{-1}。
- **懒惰一次投影次梯度算法**：采用稀疏更新策略，仅在选定时间执行一次投影次梯度步，降低计算开销。更新间隔线性增长 Γ_n = ⌈c̃n⌉。

### 算法流程（LazyGradient）
1. 用初始策略收集 n0 个样本估计经验MDP。
2. 循环直到预算耗尽：
   - 每隔 Γ_n 步，基于当前经验MDP计算一次次梯度并执行投影更新。
   - 行为策略混合均匀探索与优化导向采样：π_t = ε_t π_u + (1-ε_t) π_e。
   - 收集数据更新经验MDP。

## 实验设计

### 数据集/场景
1. **Gridworld**：16个状态、4个动作，γ=0.99，随机转移，奖励范围[-0.08, 0.20]，高斯噪声方差[0.006, 0.02]。
2. **运营实验设计案例**：50个状态、30个动作的折扣MDP，模拟产品发布实验，奖励范围[0.06, 0.40]。

### Benchmark与对比方法
- **基准算法**：Q-learning、Actor-Critic、PPO、TRPO、PSRL（后验采样）、QOCBA（优化引导）。
- **评估指标**：正确选择概率（PCS）、策略价值。

## 资源与算力

- 论文未明确说明GPU型号、数量或训练时长等算力信息。

## 实验数量与充分性

- Gridworld：50次独立宏观重复，交互预算T=600, 800, 1000, 1200。
- 运营案例：5次独立宏观重复，T=2000。
- 消融实验：比较不同初始化策略（最大覆盖vs均匀）和不同初始化预算 n0 ∈ {150, 250, 350, 450, 550}。
- 不同状态规模实验：测试S=40, 50, 60, 70时的性能。
- 实验覆盖较充分，包含不同场景、预算和参数设置，但运营案例重复次数较少（5次）。

## 论文的主要结论与发现

1. LazyGradient在Gridworld中T=800时达到92% PCS，T≥1000时达到100% PCS，显著优于所有基准方法。
2. 在运营案例中，LazyGradient策略价值最高（27.37±0.97），超过PSRL（23.46）和QOCBA等方法。
3. 最大覆盖初始化策略优于均匀初始化，尤其是在小预算时。
4. 初始化预算 n0 对最终性能影响较小，说明算法对初始化不敏感。
5. LazyGradient在不同状态规模下均保持性能优势，具有良好的可扩展性。

## 优点

- **理论贡献突出**：首次将大偏差理论应用于强化学习数据获取，建立变分特征框架，提供原则性效率度量。
- **算法设计巧妙**：懒惰一次投影次梯度方法兼顾计算效率与优化精度，显著降低计算开销。
- **鲁棒最优性保证**：证明算法在常数因子（1-γ）内达到鲁棒最优。
- **扩展性强**：成功推广至线性函数近似设置，处理大规模问题。
- **实验对比全面**：涵盖6个基准算法，覆盖多种场景和配置。

## 不足与局限

- **最优性损失**：仅能达到近鲁棒最优（存在1-γ因子损失），不是完全最优。
- **实验重复次数**：运营案例仅5次重复，统计可靠性有限。
- **线性MDP扩展的理论空白**：对线性函数近似仅提出计算可行的代理优化问题，最优性保证未建立。
- **假设限制**：假设最优策略唯一，未讨论多最优解情形。
- **通用性验证不足**：仅在Gridworld和运营案例验证，其他应用场景（如机器人、医疗）未测试。
- **算力信息缺失**：无法评估方法的可复现性和计算成本。

（完）
