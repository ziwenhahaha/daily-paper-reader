Title: Deep Reinforcement Learning for Flexible Job Shop Scheduling with Random Job Arrivals

URL Source: https://arxiv.org/pdf/2605.22773v1

Published Time: Fri, 22 May 2026 01:29:25 GMT

Number of Pages: 7

Markdown Content:
# Deep Reinforcement Learning for Flexible Job Shop Scheduling with Random Job Arrivals 

Yu Tang 1, Muhammad Zakwan 2, Efe C. Balta 2, John Lygeros 3, Alisa Rupenyan 1

Abstract — The Flexible Job Shop Scheduling Problem (FJSP) is the optimal allocation of a set of jobs to machines. Two primary challenges persist in FJSP: the unpredictable arrival of future jobs and the combinatorial complexity of the problem, rendering it intractable for conventional mixed-integer linear programming solvers. This paper proposes an event-based Deep Reinforcement Learning (DRL) approach to solve FJSP with random job arrivals. Specifically, we employ the Proximal Policy Optimization algorithm and use lightweight Multi-Layer Perceptrons to train the DRL agent for minimizing the total completion time of all jobs. We design the state representation to be directly accessible from the environment, and limit the learning agent to selecting from among a set of well-established dispatching rules. Simulations show that our DRL approach outperforms any of the individual dispatching rules on datasets with varying heterogeneity and job arrival rates. We benchmark our DRL against an arrival-triggered mixed-integer linear programming solution and show that our method achieves good performance especially when the datasets are heterogeneous. 

I. I NTRODUCTION 

Personalized orders and mass customization have in-creased the importance of scheduling problems such as the Job Shop Scheduling Problems (JSPs), which focus on allo-cating job orders to machines. In the JSPs, each job consists of a sequence of operations that must follow predefined precedence constraints. The Flexible Job Shop Scheduling Problems (FJSPs) generalize the classical JSPs by allowing each operation to be processed on one of several compatible machines, possibly with different processing times. This additional flexibility significantly enlarges the solution space, making FJSPs NP-hard optimization problems [1]. Static FJSPs assume full prior knowledge of shop floor information and a deterministic environment. They can be formulated as Mixed Integer Linear Programming (MILP) [2] or constraint programming problems [3], which can be solved using modern optimization solvers such as Gurobi [4] or OR-Tools CP-SAT [5], respectively. However, to address dynamic and uncertain conditions in real production envi-ronments, scheduling schemes must be continuously adapted [6]. Here, we aim to minimize the makespan of FJSPs with random job arrivals, by modeling the problem as a Markov Decision Process (MDP) [7] and constructing an event-based Deep Reinforcement Learning (DRL) environment. A standard Proximal Policy Optimization (PPO) algorithm [8] is trained using lightweight Multi-Layer Perceptron (MLP)    

> 1Centre for Artificial Intelligence, Zurich University of Applied Sci-ences, Switzerland
> 2Inspire AG, Zurich, Switzerland
> 3Automatic Control Laboratory, ETH Zurich, Switzerland

for both the actor and critic networks, and the DRL agent learns to make sequential actions by implicitly assigning available jobs to machines via selecting from among a set of well-established dispatching rules. 

Related work: Industrial scheduling schemes for dynamic FJSPs commonly employ heuristic dispatching rules, which define priority functions to assign available jobs to machines [9]. Typically, the scheduling process is decomposed into job sequencing and machine routing, and dispatching rules are designed for each of the two. A combined job sequencing and machine routing rule is then used as the overall dispatching strategy. Although dispatching rules are fast and react imme-diately to shop floor changes, they rules are usually myopic and highly dependent on the dataset structure. Moreover, no single dispatching rule performs best across all FJSPs scenarios. Thus, more advanced scheduling approaches are required. Another widely adopted method to handle scheduling with dynamic events is event-triggered rescheduling. In this paradigm, the scheduler monitors shop floor events such as job arrivals or machine breakdowns, and triggers a reschedul-ing procedure using metaheuristics or mathematical opti-mization based on the updated floor state. Metaheuristics are high-level search frameworks that guide subordinate heuris-tics to explore large combinatorial solution spaces efficiently, using stochastic or population-based mechanisms. For ex-ample, particle swarm optimization, a swarm-intelligence metaheuristic, was used to minimize energy consumption in FJSPs environments with machine breakdowns [10]. Genetic algorithms, an evolutionary metaheuristic, have been applied for rescheduling under dynamic disturbances [11]. On the other hand, an iterative MILP approach was proposed to solve identical parallel machine rescheduling problems with future job arrivals [12]. An event-triggered MILP was used for rescheduling under machine breakdowns or raw material shortages while minimizing deviations from the original schedule [13]. A similar idea was applied to reschedule pro-duction lines to handle dynamic disturbances [14]. Although event-triggered rescheduling methods can often produce high-quality schedules, they are typically computationally expensive for real-time scheduling scenarios. Reinforcement Learning (RL) has emerged as a promising direction for dynamic scheduling because it can balance scheduling quality with real-time decision making. In RL, an agent continuously observes the shop floor state and takes scheduling actions to maximize a cumulative reward. Early attempts to apply RL to scheduling problems date back to the 1990s [15]. In the context of FJSPs, classical tabular Q-

> arXiv:2605.22773v1 [cs.AI] 21 May 2026

learning was used to select dispatching rules in environments with random job arrivals [16], and a multi-agent tabular Q-learning framework was adopted to coordinate machine selection and job sequencing decisions [17]. However, tab-ular RL approaches rely on explicit lookup tables, which scale poorly as the state space grows, limiting their ap-plicability in complex scheduling environments. To address these limitations, recent studies have explored DRL. By using neural networks for function approximation, DRL can handle larger state spaces and capture complex features. For instance, [18] utilized deep Q-learning to maximize machine utilization of a FJSPs under random machine breakdowns; while [19] leveraged an actor-critic network to solve FJSPs with stochastic processing times to minimize makespan. Specifically for FJSPs with random job arrivals, seven elaborately-designed state features were introduced and a two-hierarchy deep Q network was trained to select hand-designed composite rules at each rescheduling point [20]. Similarly, [21] categorized the 25 abstract features into 6

channels and developed a hierarchical DRL framework with a double deep Q-network for job arrivals with constant rates. More recently, PPO with an attention-based policy network was employed to directly assign pending jobs for the total tardiness minimization [22]. A heterogeneous graph neural network is deployed to create embeddings for PPO to solve FJSPs with random job arrivals and limited transportation resources [23]. Despite these promising results, several limitations remain. First, some DRL methods adopt complex state representa-tions and sophisticated network architectures to encode shop floor information [22], [23], which increases model com-plexity and training difficulty. Additionally, most approaches consider constant job arrival rates and assume homogeneous machines and jobs [20], [21], [24]. Here, we provide an event-based DRL framework, where the agent only applies actions at each event (i.e., operation done or new job arrived). The framework adopts concise state and action representa-tions, and is trained with PPO using lightweight MLPs as the actor and critic networks. We benchmark our DRL against an event-based online rescheduling method, which we call Arrival Triggered Mixed Integer Linear Programming (AT-MILP), demonstrating the efficacy of the proposed DRL framework for dynamic FJSPs. The main contributions of this work are:  

> •

We propose an event-based DRL environment for FJSPs with random job arrivals aimed at minimizing makespan.  

> •

We modify the traditional MILP method into an event-based method for dealing with dynamic job arrivals, and use it as a strong baseline for the proposed DRL.  

> •

We study the performance of the proposed DRL and AT-MILP on datasets with varying heterogeneity, and show that our DRL method achieves good performance, especially when the datasets are heterogeneous. II. P ROBLEM FORMULATION 

The FJSPs we consider comprise a finite set of jobs J =

{J1, . . . , J n} and a set of machines M = {M1, . . . , M m}.Each job Ji ∈ J is composed of a sequence of operations 

Oi = {Oi1, . . . , O in i }. The operation Oij of job Ji can be processed on any machine selected from a set of compatible machines Mij ⊆ M. The processing time of operation 

Oij on a compatible machine Mk ∈ M ij is denoted by 

pkij ∈ R+, and can vary among different machines. Each job Ji has a stochastic arrival time, Ai, which follows a probability distribution discussed in Section IV. The goal of FJSPs in this work is to minimize the final makespan Cmax .The scheduling and processing of operations in FJSPs must satisfy the following constraints:  

> •

Each machine can process at most one operation at a time (no overlap constraint).  

> •

The execution order of the set of operations within the same job between successive operations must be respected (precedence constraint).  

> •

No operation from any job has priority and each op-eration should be processed without interruption (non-preemption and no interruption constraint).  

> •

The processing time of each operation on each machine is known and deterministic.  

> •

Machine setup time and job materials transportation time are neglected.  

> •

Job arrivals Ai are exogenous random variables revealed on-line. The FJSPs with a realization of job arrivals is mathemat-ically formulated as:                                                      

> min
> x,b,c Cmax
> s.t. X
> k∈M ij
> xkij = 1 ,∀i, j (1a)
> cij =bij +X
> k∈M ij
> xkij pkij ,∀i, j (1b)
> bi,j +1 ≥cij ,∀i, j (1c)
> bi1≥Ai,∀i(1d)
> Cmax ≥cij ,∀i, j (1e)
> ∀(i, j )̸= ( p, q ),∀k∈ M ij ∩ M pq :(xkij = 1 ∧xkpq = 1) ⇒(bpq ≥cij ∨bij ≥cpq )(1f)
> xkij ∈ { 0,1},bij , c ij ≥0.(1g)

The objective is to minimize the makespan Cmax subject to several constraints. Equation (1a) ensures that each oper-ation is assigned to exactly one compatible machine, where 

xkij ∈ { 0, 1} is a binary decision variable indicating whether operation Oij is assigned to machine Mk. Equation (1b) defines the completion time cij ∈ R+ of each operation based on its start time bij ∈ R+ and processing time pkij ∈

R+ on the assigned machine Mk. Constraint (1c) enforces the precedence constraint within each job, while (1d) ensures that the first operation of each job does not start before its arrival time Ai ∈ R+. Equation (1e) defines the makespan as the maximum completion time across all operations. Constraint (1f) enforces machine capacity using a logical disjunctive constraint. If operations Oij and Opq are both assigned to machine Mk, then either Oij precedes Opq or vice versa. For computational implementation in this work, the disjunctive constraint in (1f) is linearized using binary sequencing variables and standard big-M constraints. III. P ROPOSED METHODS FOR FJSP S WITH RANDOM JOB ARRIVALS 

A FJSP can be modeled as an MDP, making it amenable to DRL methods. 

A. MDP formulation of dynamic scheduling environment 

An MDP model consists of a tuple (S, A, P, r, γ ) , where 

S is the state space, A the action space, P (s′|s, a ) the tran-sition kernel, r(s, a, s ′) the immediate reward, and γ ∈ [0 , 1] 

the discount factor. The goal is to find a policy π : S → A 

maximizing the discounted expected return 

G(π) = Eπ,P 

h T −1X

> t=0

γtr(st, a t, s t+1 )

i

.

We model the FJSPs with random job arrivals as a discrete-event MDP. The index t ∈ N denotes decision events, occurring at job arrivals or operation completions. The elapsed physical time between consecutive decision events is variable. 

1) State: A state 

st = ( qt, m t, τ t) ,

where  

> •

qt: a vector of size |J | indicating the next operation index of each job.  

> •

mt: a vector of size |M| indicating the next free time of each machine.  

> •

τt: a vector of size |J | indicating the next available time for each job. For any job Ji that has not arrived, its state is set to a large value. Note that for FJSPs with random job arrivals, the job state cannot be observed for any job that has not arrived, making the problem a Partially Observable Markov Decision Process (POMDP) as described below in III-C. 

2) Action: In the FJSPs literature, two main approaches are used to define an action at: 

> •

directly selecting a compatible job–machine pair 

(Ji, M k); 

> •

selecting from a pool of dispatching rules, either tailor-made or well-established. Directly selecting a (Ji, M k) can be optimal [22], [25], but the training complexity grows rapidly as the problem size increases. With dispatching rules as the action space, on the other hand, the training is much easier to converge as shown in Section IV. Moreover, the action space remains interpretable and can be arbitrarily enlarged by appending additional rules, ameliorating the suboptimality issue. Dispatching rules determine scheduling decisions through two steps. First, a job sequencing rule prioritizes the next operation among the available jobs based on simple criteria such as processing time or arrival order. Second, a machine routing rule assigns the selected operation to one of its compatible machines, for example by choosing the machine with the earliest completion time. In this work, we adopt this rule-based approach and use five dispatching rules for job sequencing (Shortest Processing Time (SPT), Longest Processing Time (LPT), First In First Out (FIFO), Last In First Out (LIFO), Most Work Remaining (MWR)), and two dispatching rules for machine routing (Minimum Processing Time (MIN), Minimum Completion Time (MINC)). There-fore, the action space consists of 5 × 2 = 10 dispatching rule combinations. 

3) Transition kernel: The transition kernel is stochastic and is defined as 

P (s′ | s, a ) = Pr( st+1 = s′ | st = s, a t = a),

where the probability distribution of st+1 is determined by the random job arrival process, given that the operation processing times are assumed to be deterministic. 

4) Expected return: The objective of FJSPs is to minimize the final makespan, which is naturally sparse (only obtainable at the end of each episode). We adopt the reward shaping strategy that defines the reward r(st, a t, s t+1 ) = C(st) −

C(st+1 ) as the difference between the current makespan of the (partial) schedule at st and st+1 . We adopt the episodic setting where an episode terminates when all operations are finished. We set the discount factor γ = 1 , and the expected return in a single episode is 

G(π) = Eπ,P 

h T −1X

> t=0

γtr(st, a t, s t+1 )

i

= 

> T−1

X

> t=0

 C(st) − C(st+1 )

= C(s0) − C(sT )= −C(sT ).

Because we always assume that we start with an empty schedule, maximizing the expected return under this reward shaping is equivalent to minimizing the final makespan. 

B. PPO algorithm 

The actor-critic PPO [8] optimizes a stochastic policy πθ

via actor while estimating a critic value function Vϕ. The probability of sampling an action at given the current state 

st from the policy πθ is denoted by πθ (at | st). PPO uses a surrogate objective to prevent large policy updates; one common objective is the clipped loss :

LCLIP (θ) = Et

h

min  ρt(θ) ˆAt, clip( ρt(θ), 1 − ϵ, 1 + ϵ) ˆAt

i

,

where 

ρt(θ) = πθ (at | st)

πθold (at | st)

is the importance ratio between the updated policy πθ and the behavior policy πθold used for collecting trajectories. After the rollout, the parameters θ are updated for several epochs while keeping θold fixed. The clipping operator restricts ρt(θ) to the interval [1 − ϵ, 1 + ϵ], preventing overly large policy updates and improving training stability. The advantage estimate ˆAt

is computed using Vϕ, where ϕ is updated by minimizing a squared value error. In this work, we employ Generalized Advantage Estimation (GAE) [26], which estimates the ad-vantage from temporal-difference residuals 

δt = rt + γV ϕ(st+1 ) − Vϕ(st),

and aggregates them using a smoothing parameter λ ∈ [0 , 1] 

to balance bias and variance. 

C. Framework of the event-based DRL 

The overall framework of our method is depicted in Figure 1. This framework builds the FJSPs environment by first taking the specifications (e.g., processing time of each job order, job-machine compatibility, sampled job arrival time) and the required constraints (e.g., job order precedence, no overlap) from a job shop scheduling dataset. It then defines the state observation, action space, reward, and action transition. The composed MDP trajectories are stored in a rollout buffer, which feeds mini-batches to train the PPO agent. The PPO agent employs lightweight MLPs for both the actor and critic networks. Each MLP comprises two hidden layers with 256 and 128 neurons, respectively, and uses the Rectified Linear Unit (ReLU) activation function after each hidden layer. Based on the state observations, the PPO agent outputs action logits through the policy network. A softmax function is applied to convert the logits into a probability distribution over dispatching rules, from which the agent samples an action that implicitly places an operation block onto the current schedule Gantt chart. A key design choice is the observation space of the POMDP. The observation space includes the job state and machine state as discussed in Section III-A.1. The agent observes the job state vector of the first l arrived and unfinished jobs. If fewer than l jobs have arrived, the remaining entries are padded by setting the job available time to 1, corresponding to a large value after normalization. The hyperparameter l is fixed, which ensures a constant input dimension for the MLPs. We set l = 40 as a sufficiently large value. Additionally, the observation space includes the average processing time of each job’s next operation, the current makespan, and the current event time to provide useful global context for scheduling. We model the FJSPs as schedule building problems where the agent takes action at each event . An event is defined as an operation done or a new job arrived. A critical parameter that controls when the agent sees the new job arrival and when the next operation can be placed is the event time of the environment. The event time at each action step is the physical time of the earliest event on the current schedule. The event time is updated after each agent action, and it can remain unchanged if the earliest available machine is not scheduled at this action. In case there is no available oper-ation to schedule at the current event time, the environment advances to the next event time. 

Fig. 1: The framework of our DRL for FJSPs with random job arrivals. IV. S IMULATIONS 

The proposed event-based framework is implemented with Python 3.9 on a PC with Apple M3 Pro 12 Cores and 36 GB RAM. The PPO algorithm is implemented with Stable-Baselines3 (SB3) [27]. In this section, we present the results of two different datasets with respect to the job types and machine types: homogeneous datasets and heterogeneous datasets. The homogeneous datasets used in the simulation are generated similarly to [24]. Each job comprises a random number of operations sampled from a discrete uniform distribution Unif {1, . . . , 4}. For any operation Oij and any compatible machine Mk, the processing time is drawn from a continuous uniform distribution Unif[1 , 100] , and the number of compatible machines is sampled from a discrete uniform distribution Unif {1, . . . , M }. The shop initially contains nini 

jobs and subsequently receives nadd new jobs. These arrivals follow a Poisson process, and therefore the inter-arrival time ti between two successive jobs follows an exponential distribution with rate λ (hence E[ti] = 1 

> λ

and V ar [ti] = 1 

> λ2

).To emulate realistic heterogeneity, in the heterogeneous datasets, jobs are classified as short (85%) or long (15%). Short jobs use base processing times Unif[10 , 30] , while long jobs use Unif[75 , 300] . Machines are divided into one 

fast machine and the remaining slow machines. When an operation is assigned to a slow machine, its processing time is multiplied by a slowness penalty f . We also enforce several operations to be only compatible with one machine, to encode the bottleneck machine. Similarly to homogeneous datasets, job arrivals follow a Poisson process. Full configu-ration details are in Table I. The PPO agent is trained and evaluated across the config-urations listed in Table I, with the training hyperparameter values in Table II. A systematic grid search of the hyperpa-rameter space was not performed due to high computational cost. Figure 2 shows a representative learning curve for a homogeneous instance with M = 6 , nini = 5 , nadd = 15 ,and λ = 0 .2. The mean episode reward increases steadily and stabilizes after approximately 3000 rollouts, indicating stable and effective training under the selected settings. TABLE I: Parameter settings of job shop configuration used for DRL training. 

(a) Homogeneous datasets [24]              

> Parameter Value
> Number of machines ( M)6
> Number of available machines of each operation
> Unif {1, . . . , M }
> Number of initial jobs ( nini ){5,15 }
> Number of future jobs ( nadd ){15 ,25 }
> Number of operations belonging to a job Unif {1, . . . , 4}
> Processing time of an operation on an available machine
> Unif[1 ,100]
> Rate parameter of the arrival exponential distribution ( λ)
> {0.2,0.05 ,0.02 }

(b) Heterogeneous datasets                     

> Parameter Value
> Number of machines ( M)6
> Number of fast machines ( Mfast )1
> Number of available machines of each op-eration (except the bottleneck operations)
> Unif {1, . . . , M }
> Number of initial jobs ( nini )5
> Number of future jobs ( nadd )15
> Machine slowness penalty ( f)3
> Ratio of job types (short / long) 0.85 /0.15
> Number of operations per job Unif {1, . . . , 4}
> Base processing time per operation (short / long)
> Unif[10 ,30] /Unif[75 ,300]
> Rate parameter of the arrival exponential distribution ( λ)
> {0.2,0.05 ,0.02 }

TABLE II: Hyperparameter values for the PPO training. The values are the same across the homogeneous and the heterogeneous datasets.               

> Hyperparameter Value
> Number of training steps 1e7
> Entropy coefficient 0.02
> Training epochs for updating network 3
> Clipping threshold ϵ0.2
> Learning rate 0.0001
> Rollout size 256
> Minibatch size 64
> Discount factor γ1
> Smoothing factor λ0.95
> Value function coefficient 0.5

A. Baseline methods and datasets 

Ideally, a scheduling policy would be evaluated against the optimal schedule obtained with full knowledge of all job arrivals in hindsight by solving a single MILP over the entire planning horizon. However, computing such a clairvoyant benchmark is computationally infeasible for the problem sizes considered in this study. Therefore, we resort to two practical baselines: Arrival Triggered Mixed Integer Linear Programming (AT-MILP) and Best Heuristics in Hindsight (Best HH). The AT-MILP computes an initial MILP solution based on the initial available jobs on the floor, and then triggers a new MILP problem whenever a new job arrives. At the moment of any new job arrival, the finished and in-progress operations from the previous solves cannot be changed, while only the 0 500 1000 1500 2000 2500 3000 3500 4000 

> Number of Rollouts
> −390
> −380
> −370
> −360
> −350
> −340
> Mean Episode Reward

Fig. 2: Mean episode cumulative reward as a function of the rollout number during training. The figure shows a learning curve for a homogeneous instance with M = 6 , nini = 5 ,

nadd = 15 , and λ = 0 .2.remaining and newly arrived operations can be scheduled at each rescheduling. The objective of every AT-MILP solution is to minimize the makespan based on the current jobs on the shop floor. The solutions of the AT-MILP are generated by the Gurobi solver [4], and the solver optimization time of each rescheduling is limited to 60 seconds for a balance between scheduling performance and online applicability. On the other hand, the Best HH is obtained by applying all 10 dispatching rules to each test instance and selecting the one with the smallest final makespan. The winning rule may vary across shop floors and job arrival realizations, and hence cannot be known in advance. The Best HH is at least as good as any single dispatching rule, and strictly better unless one rule already achieves the smallest makespan in every instance. To show that the Best HH is a strong baseline, we also present evaluation results for individual dispatching rules that each perform best in at least some test instances: FIFO+MINC (Rule 1), LIFO+MINC (Rule 2), LPT+MIN (Rule 3). Detailed results are given in the following section. 

B. Scheduling performance 1) Homogenous datasets: We evaluate the proposed DRL agent under multiple job shop configurations with differ-ent job arrival rate parameters. For each configuration, the dataset specifications remain identical to those used during training, while the job arrival realizations (i.e., arrival se-quences and times) are resampled independently. Table III shows the average and the standard deviation of the results for five independent runs of each experiment. The DRL policy consistently outperforms or matches the Best HH across all problem settings. This indicates that the learned policy can identify dispatching decisions comparable to the best dispatching rule in hindsight. Furthermore, the AT-MILP achieves the smallest makespan in every testing scenario, even with a short time limit at each optimization ( 60 sec-onds). A possible explanation is that the considered shop floors have homogeneous jobs and machines (i.e., sampled from identical distributions) and a constant job arrival rate. Under such conditions, the local optimum (or feasible solu-tion) obtained by AT-MILP at each decision step is already a near-optimal solution. Moreover, when the arrival rate λ = 0 .02 , all methods exhibit similar performance for both problem scales. In this case, the expected inter-arrival time between successive jobs is 1 

> λ

= 50 , which is comparable to the average job pro-cessing length. Consequently, many previously released jobs are nearly completed before new jobs arrive, significantly reducing scheduling conflicts and making the overall FJSPs easier to solve. From a practical perspective, the AT-MILP approach is suitable when the job shop environment and arrival patterns are relatively homogeneous and the online decision time budget is sufficiently flexible (e.g., when the inter-arrival time between successive jobs exceeds the optimization time limit). Furthermore, when job arrivals are infrequent relative to the average job duration, simple dispatching rules can already achieve competitive performance. TABLE III: Rounded mean makespan and the standard deviation of different methods under different problem sizes and varying job arrival rates λ. Each scenario is tested over 5 independent runs, and the best result is in bold font. 

(a) Small-scale problem (6 machines, 20 jobs)                                                             

> Methods λ= 0 .2λ= 0 .05 λ= 0 .02
> AT-MILP 307 ±14 416 ±35 763 ±174
> DRL (Ours) 338 ±18 420 ±36 764 ±173 Best HH 378 ±15 421 ±42 765 ±173 Rule 1 396 ±22 428 ±43 765 ±173 Rule 2 419 ±15 429 ±44 765 ±173 Rule 3 409 ±32 434 ±30 770 ±171

(b) Large-scale problem (6 machines, 40 jobs)                                                             

> Methods λ= 0 .2λ= 0 .05 λ= 0 .02
> AT-MILP 560 ±14 596 ±28 1380 ±175
> DRL (Ours) 595 ±23 627 ±39 1380 ±175
> Best HH 619 ±16 634 ±15 1380 ±175
> Rule 1 658 ±33 684 ±44 1380 ±175
> Rule 2 657 ±34 652 ±11 1380 ±175
> Rule 3 644 ±33 705 ±58 1380 ±175

2) Heterogeneous datasets: Next, we train and evaluate our DRL on heterogeneous datasets described in Table I with a shop floor configuration of 6 machines, 5 initial jobs, and 15 upcoming jobs, with different job arrival rates. Each rate parameter is evaluated over 15 independent runs. The resulting average makespan and its standard deviation are reported in Table IV for the different arrival rates. The results show that this time, the AT-MILP performs significantly worse, particularly for higher job arrival rates 

λ. This performance degradation can be attributed to the myopic nature of the AT-MILP when facing heterogeneous shop floors combined with stochastic job arrivals. In such cases, the AT-MILP may already occupy fast machines with relatively short jobs when longer and critical jobs arrive, leading to suboptimal scheduling decisions. In contrast, the proposed DRL approach learns policies that better adapt to heterogeneous shop floor conditions. To further illustrate this behavior, the schedules from all methods for one test run are visualized using Gantt charts in Figure 3. In this example, machine M5 is the only fast machine, while several operations are exclusively compatible with the bottleneck machine M1. Under this configuration, the proposed DRL achieves the smallest makespan, whereas the AT-MILP struggles due to its myopic decision-making at individual arrival events. 

Fig. 3: Schedules of three methods on one heterogeneous case with M = 6 , nini = 5 , nadd = 15 , and λ = 0 .05 .The Best HH in this run is SPT for job sequencing and MINC for machine routing. The vertical dashed lines in red indicate the incoming job arrival times. Jobs are represented with different colors on the Gantt chart, and the blocks with the same color represent different operations of the same job. TABLE IV: Rounded mean makespan and standard deviation of different methods under the shop floor of 6 machines and 20 jobs with varying job arrival rates λ. Each scenario is tested over 15 independent runs, and the best result is in bold font.                                                        

> Methods λ= 0 .2λ= 0 .05 λ= 0 .02
> AT-MILP 1642 ±14 1660 ±19 1757 ±109 DRL (Ours) 1478 ±52 1550 ±86 1753 ±174
> Best HH 1499 ±83 1564 ±104 1797 ±197 Rule 1 1558 ±107 1620 ±140 1817 ±201 Rule 2 1535 ±102 1623 ±125 1820 ±211 Rule 3 2014 ±50 2029 ±59 2120 ±92

V. C ONCLUSION 

We develop an event-based DRL framework to tackle dynamic job arrivals in FJSPs. We design the action space as a set of well-established dispatching rules; the action space can be arbitrarily enlarged by appending other dispatch-ing rules. We employ the actor-critic PPO algorithm with lightweight MLPs for policy training, making our framework readily interpretable and adaptable. The simulation results show that the scheduling performance of our DRL beats or ties with the best heuristic dispatching rule in hindsight. Moreover, we benchmark against a method called AT-MILP, which resolves online a new MILP problem when jobs arrive. In practice, the employment of this AT-MILP needs to be decided carefully. When the datasets are homogeneous and the online solution time limit is not tight, using AT-MILP is favored; otherwise, deploying our DRL improves performance. An interesting direction for future research is to investigate different job arrival patterns and additional types of heterogeneous datasets to enhance the robustness of this framework. REFERENCES [1] L. Lv, C. Zhang, J. Fan, and W. Shen, “Deep reinforcement learning for job shop scheduling problems: A comprehensive literature review,” 

Knowledge-Based Systems , p. 113633, 2025. [2] A. S. Manne, “On the job-shop scheduling problem,” Operations research , vol. 8, no. 2, pp. 219–223, 1960. [3] P. Baptiste, C. Le Pape, and W. Nuijten, Constraint-based scheduling: applying constraint programming to scheduling problems . Springer Science & Business Media, 2001, vol. 39. [4] L. Gurobi Optimization, Gurobi Optimizer Reference Manual , 2024, available at https://www.gurobi.com. [5] L. Perron and P. Schaus, “The CP-SAT solver: A new state-of-the-art constraint programming solver,” Google Research, Tech. Rep., 2020, technical Report. [6] H. Wang, B. R. Sarker, J. Li, and J. Li, “Adaptive scheduling for assembly job shop with uncertain assembly times based on dual q-learning,” International Journal of Production Research , vol. 59, no. 19, pp. 5867–5883, 2021. [7] M. L. Puterman, Markov decision processes: discrete stochastic dy-namic programming . John Wiley & Sons, 2014. [8] J. Schulman, F. Wolski, P. Dhariwal, A. Radford, and O. Klimov, “Proximal policy optimization algorithms,” arXiv preprint arXiv:1707.06347 , 2017. [9] C. D. Geiger, R. Uzsoy, and H. Aytu˘ g, “Rapid modeling and discovery of priority dispatching rules: An autonomous learning approach,” 

Journal of Scheduling , vol. 9, no. 1, pp. 7–34, 2006. [10] M. Nouiri, A. Bekrar, and D. Trentesaux, “Towards energy efficient scheduling and rescheduling for dynamic flexible job shop problem,” 

Ifac-PapersOnline , vol. 51, no. 11, pp. 1275–1280, 2018. [11] M. Zhang, F. Tao, and A. Nee, “Digital twin enhanced dynamic job-shop scheduling,” Journal of Manufacturing Systems , vol. 58, pp. 146– 156, 2021. [12] A. Tighazoui, C. Sauvey, and N. Sauer, “Predictive-reactive strategy for identical parallel machine rescheduling,” Computers & Operations Research , vol. 134, p. 105372, 2021. [13] A. J. Figueroa, R. Poler, and B. Andres, “Adaptive production rescheduling system for managing unforeseen disruptions,” Mathemat-ics , vol. 12, no. 22, p. 3478, 2024. [14] B. Fu, M. Bi, S. Umeda, T. Nakano, Y. Nonaka, Q. Zhou, T. Matsui, D. M. Tilbury, and K. Barton, “Digital twin-based smart manufac-turing: Dynamic line reconfiguration for disturbance handling,” IEEE Transactions on Automation Science and Engineering , 2025. [15] S. Riedmiller and M. Riedmiller, “A neural reinforcement learning approach to learn local dispatching policies in production scheduling,” in IJCAI , vol. 2, 1999, pp. 764–771. [16] M. E. Aydin and E. ¨Oztemel, “Dynamic job-shop scheduling using reinforcement learning agents,” Robotics and Autonomous Systems ,vol. 33, no. 2-3, pp. 169–178, 2000. [17] W. Bouazza, Y. Sallez, and B. Beldjilali, “A distributed approach solving partially flexible job-shop scheduling problem with a q-learning effect,” IFAC-PapersOnLine , vol. 50, no. 1, pp. 15 890– 15 895, 2017. [18] B. Waschneck, A. Reichstaller, L. Belzner, T. Altenm¨ uller, T. Bauern-hansl, A. Knapp, and A. Kyek, “Optimization of global production scheduling with deep reinforcement learning,” Procedia Cirp , vol. 72, pp. 1264–1269, 2018. [19] L. Zhang, Y. Feng, Q. Xiao, Y. Xu, D. Li, D. Yang, and Z. Yang, “Deep reinforcement learning for dynamic flexible job shop scheduling prob-lem considering variable processing times,” Journal of Manufacturing systems , vol. 71, pp. 257–273, 2023. [20] S. Luo, L. Zhang, and Y. Fan, “Dynamic multi-objective scheduling for flexible job shop by deep reinforcement learning,” Computers & Industrial Engineering , vol. 159, p. 107489, 2021. [21] R. Liu, R. Piplani, and C. Toro, “Deep reinforcement learning for dynamic scheduling of a flexible job shop,” International Journal of Production Research , vol. 60, no. 13, pp. 4049–4069, 2022. [22] L. Zhao, J. Fan, C. Zhang, W. Shen, and J. Zhuang, “A drl-based reactive scheduling policy for flexible job shops with random job arrivals,” IEEE Transactions on Automation Science and Engineering ,vol. 21, no. 3, pp. 2912–2923, 2023. [23] M. Zhang, L. Wang, F. Qiu, and X. Liu, “Dynamic scheduling for flexible job shop with insufficient transportation resources via graph neural network and deep reinforcement learning,” Computers & Industrial Engineering , vol. 186, p. 109718, 2023. [24] S. Luo, “Dynamic scheduling for flexible job shop with new job insertions by deep reinforcement learning,” Applied Soft Computing ,vol. 91, p. 106208, 2020. [25] X. Liu, L. Han, L. Kang, J. Liu, and H. Miao, “Preference learning based deep reinforcement learning for flexible job shop scheduling problem,” Complex & Intelligent Systems , vol. 11, no. 2, p. 144, 2025. [26] J. Schulman, P. Moritz, S. Levine, M. Jordan, and P. Abbeel, “High-dimensional continuous control using generalized advantage estima-tion,” arXiv preprint arXiv:1506.02438 , 2015. [27] A. Raffin, A. Hill, A. Gleave, A. Kanervisto, M. Ernestus, and N. Dormann, “Stable-baselines3: Reliable reinforcement learning im-plementations,” Journal of machine learning research , vol. 22, no. 268, pp. 1–8, 2021.