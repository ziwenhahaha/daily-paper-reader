---
title: Federated Cross-Client Subgraph Pattern Detection
title_zh: 联邦跨客户端子图模式检测
authors: "Selin Ceydeli, Rui Wang, Kubilay Atasu"
date: 2026-05-07
pdf: "https://arxiv.org/pdf/2605.06433v1"
tags: ["query:dgnn-nas"]
score: 7.0
evidence: 跨分区分布式图的逐层嵌入交换
tldr: 现有的图神经网络（GNN）在集中式图上进行子图模式检测效果良好，但在图数据分布在多方时，局部计算会产生表示等价性差距。本文提出一种按步、逐层的嵌入交换框架，通过在正向传播中同步中间节点表示，在不暴露原始数据的前提下，有效弥合了这一差距，恢复了集中式GNN的表示能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.06433v1/fig-001.webp\", \"caption\": \"Figure 1: Three money laundering patterns spanning two institutions (Inst. 1 and Inst. 2). Solid black edges denote intra-institution transfers, while dashed gray edges cross institutional boundaries.\", \"page\": 2, \"index\": 1, \"width\": 622, \"height\": 211}, {\"url\": \"assets/figures/arxiv/2605.06433v1/fig-002.webp\", \"caption\": \"Table 5: Approximate compute resources required for the main experimental settings.\", \"page\": 14, \"index\": 2, \"width\": 672, \"height\": 321}, {\"url\": \"assets/figures/arxiv/2605.06433v1/fig-003.webp\", \"caption\": \"Figure 2: Layer-wise embedding exchange on a 4-cycle (a → b → c → d → a) split across two clients. (a) The global graph: node colors preview the partition (Client 1: blue, Client 2: orange); cross-client edges are dashed gray and intra-client edges are solid black. (b) The distributed view: each client materializes solid copies of its owned nodes and dashed copies of the remote endpoints; the same node appears at the same coordinates across panels for visual continuity. After every layer\", \"page\": 5, \"index\": 3, \"width\": 574, \"height\": 237}]"
motivation: 现有基于GNN的子图模式检测方法依赖集中式图数据，在图数据分布式存储时会导致跨越分区边界的子图模式无法被局部识别，产生表示等价性差距。
method: 提出一种按步、逐层的嵌入交换框架，在正向传播的每一层同步中间节点表示，且不暴露原始特征或标签。
result: 实验证明，嵌入交换与联邦参数聚合是互补的，只要每步交换的嵌入保持最新，两者的结合就能恢复大部分的表示差距。
conclusion: 该框架在保护隐私的同时，成功解决了分布式图中的结构可观测性问题，使联邦GNN的节点表示等价于集中式GNN。
---

## 摘要
子图模式检测旨在揭示图中复杂的交互结构。然而，基于最先进图神经网络（GNN）的解决方案假设能够对整个图进行集中式访问。当图分布在多方时，客户端本地的GNN计算会与集中式模型产生偏离，从而导致表示等价性差距。我们将其形式化为一个结构可观测性问题，其中跨越划分边界的子图模式在局部变得不可识别。为了弥合这一差距，我们提出了一个逐步、逐层的嵌入交换框架，在该框架中，客户端在前向传播的每一层同步中间节点表示，而无需暴露原始特征或标签。在扩展子图假设以及客户端间共享模型参数的条件下，该框架能够恢复与在全图上运行的集中式GNN相同的节点表示。在包含环、二分团和散射-聚集模式的合成有向多重图上的实验表明，嵌入交换与联邦参数聚合是互补的而非可互换的：只要交换的嵌入在每步都是最新的，而不是在每个epoch中是陈旧的，它们的组合就能恢复大部分的表示差距。

## Abstract
Subgraph pattern detection aims to uncover complex interaction structures in graphs. However, state-of-the-art graph neural network (GNN)-based solutions assume centralized access to the entire graph. When graphs are instead distributed across multiple parties, client-local GNN computations diverge from those of a centralized model, resulting in a representation-equivalence gap. We formalize this as a structural observability problem, where subgraph patterns crossing partition boundaries become locally unidentifiable. To bridge this gap, we propose a per-step, layer-wise embedding exchange framework in which clients synchronize intermediate node representations at each layer of the forward pass, without exposing raw features or labels. Under an extended-subgraph assumption and shared model parameters across clients, this framework recovers the same node representations as a centralized GNN over the full graph. Experiments on synthetic directed multigraphs with cycles, bicliques, and scatter-gather patterns show that embedding exchange and federated parameter aggregation are complementary rather than interchangeable: their combination recovers most of the representation gap, provided exchanged embeddings are fresh per-step rather than stale per-epoch.