---
title: "Don't Forget the Critic: Value-Based Data Rehearsal for Multi-Cyclic Continual Reinforcement Learning"
authors: "Benjamin Poole, Andrew Quinn, Li Yang, Minwoo Lee"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22454v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体强化学习与持续学习
tldr: 提出Qreg+NWLU方法，用于多循环持续强化学习中的基于价值的数据排练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.22454v1/fig-001.webp\", \"caption\": \"\", \"page\": 17, \"index\": 1, \"width\": 506, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2605.22454v1/fig-002.webp\", \"caption\": \"\", \"page\": 17, \"index\": 2, \"width\": 570, \"height\": 1004}]"
motivation: 多智能体强化学习与持续学习。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Data rehearsal has emerged as a leading approach for mitigating catastrophic forgetting in Continual Reinforcement Learning (CRL). However, existing work remains confined to policy gradient frameworks, regularizing only actors due to the performance degradation incurred by critic regularization. This actor-centric approach overlooks the potential of data rehearsal for value function approximation. Moreover, existing evaluations in CRL rarely consider multi-cyclic environments where task sequences repeat, a critical real-world scenario that exacerbates forgetting and plasticity. We investigate data rehearsal for Deep Q-Networks using Q-value regularization in multi-cyclic settings and propose Qreg+NWLU which introduces two simple modifications: (1) continuous data rehearsal that dynamically collects and updates stored Q-values throughout training, and (2) "No-Wait" regularization that applies immediately rather than after the first task. Together, these modifications yield improvements in learning efficiency, forgetting mitigation, and knowledge transfer over Qreg and conventional CRL methods within value function approximation settings.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义

- **研究背景**：持续强化学习（Continual Reinforcement Learning, CRL）面临两大核心挑战——灾难性遗忘（catastrophic forgetting）和可塑性损失（plasticity loss）。
- **现有研究的局限**：
  - 当前数据排练（data rehearsal）方法主要局限于策略梯度框架，仅对演员（actor）进行正则化，而回避评论家（critic）正则化，因为后者会导致性能下降。
  - 价值函数近似（value function approximation）在持续学习中的潜力未被充分探索。
  - 现有评估多在单循环设置下进行，忽视了多循环场景（任务序列重复出现）——这在实际应用中更为常见。
- **核心问题**：如何将数据排练与正则化有效适配到DQN等基于价值的方法中，以缓解价值函数近似中的灾难性遗忘？
- **研究动机**：探索价值-based数据排练在多循环CRL设置中的有效性，填补actor-critic独占领域与多循环评估缺失的研究空白。

---

### 2. 论文提出的方法论

#### 2.1 核心思想

论文提出 **Qreg+NWLU（No-Wait Qreg with Live-Updates）**，通过两处简单但关键的改进，使Q值正则化在持续学习中更加稳定和高效。

#### 2.2 关键技术细节

- **Q值正则化（Qreg）**：数据排练样本存储在独立的回放缓冲（RRB）中，正则化通过最小化当前Q值与存储Q值之间的L2距离实现：
  - 公式：`Qreg = λ/NRBS × Σ(Qθ(s,a) - Q_RRB)²`
  - 其中NRBS为正则化批次大小，λ为正则化系数，Q_RRB为存储的Q值。

- **Live策略（持续数据采集）**：在训练过程中以固定频率持续向RRB添加样本，而非仅在任务完成时一次性采样。
  - 优势：减少对训练末期样本的偏向，提高样本多样性，避免过时或次优Q值的存储。

- **Updates策略（Q值动态更新）**：周期性地刷新RRB中当前任务对应样本的Q值。
  - 解决存储Q值过时或不准的问题，防止向陈旧目标正则化。

- **No-Wait正则化**：一旦有回放样本可用立即应用正则化，而非等待第一任务完成后才开始。
  - 有助于稳定初始任务学习，减少学习初期的不稳定性。

#### 2.3 算法流程

1. 初始化DQN网络、目标网络、回放缓冲和正则化回放缓冲（RRB）。
2. 对每个循环的每个任务：
   - 持续与环境交互，存储状态转移及任务ID。
   - 按固定频率更新RRB中的样本Q值（Updates）。
   - 按固定频率从回放缓冲中采样并添加新样本到RRB（Live）。
   - 每隔固定步数进行一次训练：
     - 从回放缓冲采样计算DQN损失。
     - **立即**从RRB采样计算Qreg正则化损失（No-Wait）。
     - 合并更新网络参数。
3. 不在任务或循环之间重置权重、回放缓冲或优化器状态。

---

### 3. 实验设计

#### 3.1 数据集与场景

- **Minihack Room序列**（5任务）：Room Random → Room Dark → Random Monsters → Random Trap → Room Ultimate
  - 特点：任务难度递增，观测空间变化较大，hard转移特征明显。
- **PyGame Learning Environment (PLE)**：
  - Flappy Bird序列（5任务）：管道间隙从100递减5。
  - Catcher序列（5任务）：物体速度递增。
  - 特点：soft转移，任务难度逐步提升。

#### 3.2 Benchmark设置

- **训练配置**：4个循环，共600万步（每任务30万步），每60K步评估一次。
- **回放缓冲**：50K容量（不足以存储完整任务），强调CRL算法有效性。

#### 3.3 对比方法

| 方法类别 | 具体算法 |
|----------|----------|
| 基线 | DQN, DDQN, Perfect Memory (PM) |
| 权重正则化 | L2, EWC |
| 架构正则化 | PackNet |
| 元学习方法 | MER |
| 数据排练 | Qreg, Qreg+NWLU（本文） |

#### 3.4 评估指标

- **平均回报（G）**：综合性能指标。
- **最终转移（F）**：任务间迁移的最终效果（正值=正迁移，负值=遗忘）。
- **最差转移（W）**：训练过程中的最差表现（捕捉训练中期遗忘）。

---

### 4. 资源与算力

- **GPU节点类型**（四类）：
  - NVIDIA A40（Intel Xeon Gold 6326 CPU）
  - NVIDIA A100（Intel Xeon Gold 6326 或 AMD EPYC 7502 CPU）
  - NVIDIA L40s（Intel Xeon Gold 6338 / Platinum 8362 CPU）
  - NVIDIA H200（AMD EPYC 9555 CPU）
- **内存**：所有节点64 GB RAM。
- **训练规模**：10个随机种子 × 3个环境序列。
- **训练时长**：每序列约600万步×10种子，集群并行执行。

> 注：论文未明确说明具体GPU数量、总训练小时数或具体算力成本估算。

---

### 5. 实验数量与充分性

#### 5.1 主实验

- **3个任务序列**（Room、Flappy、Catcher）× **9种方法** × **10个随机种子**
- 每个任务在每个循环末进行评估，生成学习曲线、转移矩阵等大量数据。

#### 5.2 消融实验

针对Qreg+NWLU的五个变体进行消融分析：
- Qreg+U（仅Updates）
- Qreg+L（仅Live）
- Qreg+LU（Live+Updates）
- Qreg+NWL（No-Wait+Live）
- Qreg+NWLU（完整版本）

#### 5.3 实验充分性评估

- **充分性**：
  - 多样化环境（gridworld、简单街机、复杂探索）覆盖不同任务转移类型。
  - 丰富的对比基线（传统CRL方法全覆盖）。
  - 完整的消融分析验证各组件贡献。
  - Q值范数分析从优化动态角度提供额外证据。
- **公平性**：
  - 所有方法使用相同的DQN架构和超参数搜索空间。
  - 仅对特定方法做合理调整（如PackNet重置缓冲和优化器）。
  - 10个随机种子提供统计显著性。

---

### 6. 论文的主要结论与发现

1. **Qreg+NWLU整体最优**：在Room环境中，Qreg+NWLU实现接近最优性能（任务1-5平均回报约0.87-0.94），且标准误差极低（接近0），稳定性远优于Qreg。

2. **Qreg的二元失效现象**：在Room序列中，Qreg呈现高方差——部分运行表现优异，部分完全失败，表现为primacy bias类似效应（初始任务学习失败连锁影响后续任务）。

3. **Q值范数稳定性**：Qreg+NWLU使Q值范数在任务转换时更平滑，而Qreg出现大幅跳跃，验证了Live和Updates策略对优化动态的稳定作用。

4. **各组件贡献**：
   - 在Room中，No-Wait+Live足以实现接近最优性能。
   - 在Flappy/Catcher中，Live+Updates表现更优，No-Wait主要帮助减少首个任务的遗忘。
   - 三者结合（NWLU）在所有环境中保持稳健。

5. **基线对比**：传统CRL方法（DQN, DDQN, MER等）在多循环设置下表现出明显的循环遗忘；PM虽在Catcher中表现良好，但在Room中会遗忘；Qreg和Qreg+NWLU均显著优于大部分基线。

---

### 7. 优点

1. **填补研究空白**：首次系统探索价值函数正则化在CRL数据排练中的潜力，突破actor-only的局限。
2. **方法简洁有效**：仅添加Live采样和周期性更新两处改进，无需复杂架构或额外网络。
3. **多循环评估范式**：强调现实场景中的重复任务设置，提供更全面的遗忘/迁移分析。
4. **全面的实验验证**：
   - 三种不同特征的环境序列。
   - 丰富的基线对比和方法变体消融。
   - 多维度评估指标（F/W/G）。
   - Q值范数等优化动态分析。
5. **分析深入**：揭示Qreg不稳定性的根源（Q值范数跳跃），并针对性地设计解决方案。
6. **可复现性强**：详细的伪代码、算法流程、超参数设置。

---

### 8. 不足与局限

1. **任务序列受限**：
   - 仅评估固定顺序的soft/hard转移任务。
   - 未探索随机化任务序列或完全异构任务。
2. **Updates依赖任务标签**：需要显式存储和追踪任务ID，在实际应用中可能需要额外的任务识别机制。
3. **未覆盖Actor-Critic框架**：价值网络在actor-critic中的交互更为复杂，Qreg+NWLU的直接迁移可能面临额外挑战。
4. **替代方法未探索**：仅分析Qreg，未尝试其他RRB利用方式（如知识蒸馏、经验回放策略改进等）。
5. **算力信息不透明**：未公开具体GPU数量、总训练时间或成本估算，难以评估实验规模。
6. **超参数敏感性**：Updates和Live的频率设置依赖于任务，可能需要针对新场景重新调优。

---

（完）
