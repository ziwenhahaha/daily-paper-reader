---
title: "Multi-Agent Systems are Mixtures of Experts: Who Becomes an Influencer?"
authors: "Franka Bause, Jonas Niederle, Martin Pawelczyk, Rebekka Burkholz"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25929v1"
tags: ["query:mas"]
score: 9.0
evidence: 多智能体LLM deliberation机制
tldr: 通过Friedkin-Johnsen观点动力学研究多智能体deliberation影响模式。
source: arxiv
selection_source: fresh_fetch
motivation: 多智能体LLM deliberation机制。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
The effectiveness of multi-agent LLM deliberation depends not only on the agents' individual predictions, but also on how they communicate and collaborate. We study this mechanism through the lens of Friedkin-Johnsen (FJ) opinion dynamics, a tractable model for analyzing stubbornness, influence, and opinion change in multi-agent systems that captures empirically observed deliberation patterns. We show that the FJ parameters are input-dependent, turning multi-agent deliberation into a mixture of experts. This perspective implies that multi-agent systems can outperform single agents and static ensembles when routing reflects agent competence. Since competence is latent in practice, we analyze how influence is established through observable proxies: agents' self-assessed confidence, their perceived confidence, and initial alignment with other agents' views.

---

## 论文详细总结（自动生成）

# 论文总结

## 1. 论文核心问题与整体含义

**研究动机**：多智能体大型语言模型（MAS） deliberation 的有效性不仅取决于智能体的个体预测，还取决于它们如何沟通与协作。然而，关于哪些智能体更具影响力、影响如何在 deliberation 过程中形成，尚缺乏系统的理论框架。

**核心问题**：在多智能体 LLM deliberation 系统中，是什么机制决定了哪些智能体更具影响力？这种影响模式能否被形式化分析？

**背景意义**：现有研究 [27, 39, 31] 表明 MAS 相比单智能体或静态集成模型的性能提升参差不齐，亟需深入理解其设计原则。

---

## 2. 论文方法论

### 2.1 核心思想

将 Friedkin-Johnsen（FJ）观点动力学模型引入多智能体 LLM deliberation 分析，将其解释为一种**隐式混合专家（MoE）系统**，其中路由（routing）是自动通过 deliberation 实现的。

### 2.2 Friedkin-Johnsen 模型

设 $G = (V, E)$ 为包含 $n = |V|$ 个智能体的网络，每个智能体 $i$ 在时刻 $t$ 持有信念 $b_i(t) \in \Delta^d$（$d$ 维单纯形）。模型更新规则为：

$$b_i(t+1) = \gamma_i s_i + (1 - \gamma_i) \alpha_i b_i(t) + (1 - \gamma_i)(1 - \alpha_i) \sum_{j \in N_i} w_{ij} b_j(t)$$

- $\gamma_i \in [0,1]$：**固执度**（stubbornness），对先天信念的依赖程度
- $\alpha_i \in [0,1]$：对上一状态的权重
- $W = [w_{ij}]$：行随机影响矩阵

### 2.3 关键理论结果

**命题 2.1**：当 $\rho(H) < 1$ 时，FJ 动力学收敛至唯一均衡 $B^* = (I - H)^{-1} \Gamma S$，其中 $M = (I - H)^{-1} \Gamma$ 为非负行随机矩阵。因此最终信念是初始信念的凸组合：

$$b_i^* = \sum_{j=1}^n M_{ij} s_j$$

**假设 2.2**：MAS 等价于 MoE，其中路由权重 $\pi_j(x)$ 依赖于输入 $x$：

$$b^*(x) = \sum_{j=1}^n \pi_j(x) s_j(x)$$

**定理 2.4**：当**专业化增益 + 局部多样性 > 路由损失**时，MoE 优于最佳单智能体。

**定理 2.5**：当**局部路由增益 > 多样性损失**时，MoE 优于固定集成。

### 2.4 置信路由机制

定义基于初始信念熵的置信度：

$$C_j(S) = 1 + \frac{1}{\log d} \sum_{c=1}^d s_{j,c} \log s_{j,c}$$

相对置信度 $R_j(S) = \frac{C_j(S)}{C_{(n-1)}(S)}$，其中 $C_{(n-1)}$ 为第二高置信度。

---

## 3. 实验设计

### 3.1 数据集

| 数据集 | 样本数 | 描述 |
|--------|--------|------|
| MMLU-Pro | 7300 题 | 多任务语言理解基准 |
| BBQ | 7300 题 | 偏置基准测试 |
| CommonsenseQA (CSQA) | 100 题 | 常识问答（来自 [1] 的子集）|

### 3.2 模型

- **GPT-5.4 Mini**（OpenAI）
- **Qwen2.5-14B-Instruct**（阿里）

### 3.3 多样化策略（Prompt 类型）

- **neutral**：无额外提示
- **roles**：角色扮演（如教师、数学家、医生、粗心学生等）
- **communication styles**：通信风格（简洁、详细、逐步推理、平衡、情感化）
- **experts**：MMLU-Pro 超类别专家

### 3.4 对比基线

- **Baseline**：取初始信念最大值的答案
- **FJ Ensemble**：拟合任务无关的固定 FJ 参数
- **MAS**：完整的多智能体 deliberation 系统

### 3.5 实验配置

- 智能体数量：5
- 网络结构：完全图
- 迭代轮数：5 轮
- 重复次数：3 种不同随机种子

---

## 4. 资源与算力

文中明确列出了计算资源需求：

| 模型 | 硬件需求 | 单次实验时长 |
|------|----------|--------------|
| Qwen2.5-14B-Instruct | 需要 A100 GPU | ~3.5 小时 |
| GPT-5.4 Mini | 仅需 OpenAI API 访问（无需专用硬件） | ~2.5 小时 |

- 若无并行化，全部组合约需 **72 小时**
- 可在单 GPU 或多 GPU 上并行运行

---

## 5. 实验数量与充分性

### 5.1 主要实验

- **模型拟合验证**：KL 散度和 MSE 评估 FJ 模型拟合质量（表 2）
- **性能对比**：跨 3 个数据集、4 种提示类型、2 种模型的全矩阵对比（表 3）
- **路由条件验证**：定理 2.4 的样本级验证（混淆矩阵，图 17）
- **特征重要性分析**：随机森林回归/分类、FJ 参数变异性、置信度-影响力相关性

### 5.2 充分性与客观性

| 评估维度 | 结论 |
|----------|------|
| 数据集覆盖 | 覆盖问答和偏置检测两大类，较为全面 |
| 模型多样性 | 仅两种模型规模，缺乏更大规模模型验证 |
| 提示多样性 | 4 种类型、跨 STEM/社会/人文学科，覆盖较好 |
| 重复性 | 每配置 3 个随机种子，提供置信区间 |
| 公平性 | 基线方法定义清晰，MAS 仅改变 deliberation 过程 |

**总体评价**：实验设计较为系统，但受限于资源仅测试了两种模型规模，缺乏对更大规模 LLM 的验证。

---

## 6. 主要结论与发现

### 6.1 核心发现

1. **FJ 模型准确捕获 deliberation 动态**：KL 散度 ~0.053，MSE ~0.002，表明模型拟合良好

2. **MAS 确实实现 MoE 行为**：
   - FJ 参数随输入变化（参数变异性，图 1a）
   - 初始信念多样但最终趋于共识（共识倾向，图 1b）
   - 影响集中在少数固执智能体

3. **置信度是主要路由驱动力**：
   - 相对置信度比绝对置信度更能预测影响力
   - 自信智能体更固执（γ 更高）
   - 自信智能体较少改变自己的答案

4. **专业化路由条件**：
   - 当专业化增益 + 局部多样性 > 路由损失时，MoE 优于单智能体
   - 约 70-80% 的样本满足该条件

5. **通信风格影响感知置信**：
   - 不同角色/风格提示影响智能体的同行影响力
   - 通信风格影响 FJ 动态中的感知置信

### 6.2 性能结果摘要

| 配置 | Baseline | FJ Ensemble | MAS |
|------|----------|-------------|-----|
| MMLU-Pro (GPT, experts) | 81.7% | 82.0% | **83.7%** |
| BBQ (GPT, roles) | 90.4% | 91.8% | **91.5%** |
| CSQA (Qwen, experts) | 78.7% | 80.3% | **80.0%** |

MAS 在大多数配置下优于或持平于基线和 FJ 集成。

---

## 7. 优点

1. **理论扎实**：基于 FJ 观点动力学提供了严格的理论框架，将 MAS 形式化为 MoE

2. **可解释性强**：揭示了影响形成机制（置信度、对齐度、固执度），而非黑箱观察

3. **实证验证充分**：跨多个数据集、模型、提示类型的系统实验

4. **实践指导明确**：指出成功 MAS 需要：
   - 多样化智能体
   - 可靠的路由机制将影响导向局部胜任的智能体
   - 良好校准的置信度

5. **创新性视角**：首次将 FJ 动力学与 MoE 框架结合，解释了为何 MAS 的效果不一致

---

## 8. 不足与局限

1. **模型规模局限**：
   - 仅测试了小型/中型模型（GPT-5.4 Mini ~？参数，Qwen-14B）
   - 未验证更大规模 LLM（如 GPT-4、Claude、Llama-70B+）的行为

2. **网络结构单一**：
   - 仅使用完全图结构
   - 未探索层次化、环形或星型网络拓扑对路由的影响

3. **智能体数量受限**：
   - 仅 5 个智能体
   - 未研究规模扩展性（10+ 智能体）

4. **路由机制隐式**：
   - 未直接分析 deliberation 过程中的信息传递路径
   - 缺乏对话级别（turn-by-turn）的细粒度分析

5. **校准问题**：
   - 假设置信度可作为能力代理，但过度自信的智能体可能误导路由
   - 未提供校准修正方案

6. **缺乏真实世界任务**：
   - 仅测试标准问答基准
   - 未验证在复杂任务（编码、规划、谈判）中的有效性

7. **资源密集型**：
   - 全量实验需 ~72 小时（无并行）
   - 限制了其可复现性和大规模验证

---

（完）
