---
title: Self-Policy Distillation via Capability-Selective Subspace Projection
authors: "Guangya Hao, Yitong Shang, Yunbo Long, Zhuokai Zhao, Hanxue Liang"
date: 2026-05-21
pdf: "https://arxiv.org/pdf/2605.22675v1"
tags: ["query:self-distillation"]
score: 9.0
evidence: 自蒸馏方法让大语言模型从自身输出中学习
tldr: 自我策略蒸馏通过能力选择性子空间投影训练大语言模型利用自身输出。
source: arxiv
selection_source: fresh_fetch
motivation: 自蒸馏方法让大语言模型从自身输出中学习。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
Self-distillation bootstraps large language models (LLMs) by training on their own generations. However, existing methods either rely on external signals to curate self-generated outputs (e.g., correctness filtering, execution feedback, and reward search), which are costly and unavailable for the best-performing frontier models, or skip curation entirely and train on all raw outputs, an approach that is often domain-specific and hard to generalize. Both also share a deeper weakness that self-generated outputs entangle task-relevant capability with others, such as stylistic patterns, formatting artifacts, and model-specific errors, diluting the signal for the specific capability one aims to improve. In this paper, we propose Self-Policy Distillation (SPD), which achieves generalizable, capability selective without any external signal. Specifically, SPD extracts a low-rank capability subspace from the model's own gradients on correctness-defining tokens, projects key-value (KV) activations into this subspace during self-generation, and fine-tunes on the resulting raw outputs with standard next-token prediction loss. Through extensive experiments across code generation, mathematical reasoning, and multiple-choice QA, we show that SPD achieves up to 13% improvement over state-of-the-art self-distillation methods without external signals and up to 16% improvement over pre-trained baselines. Notably, SPD demonstrates superior generalizability, achieving 15% better performance under out-of-domain generalization settings.

---

## 论文详细总结（自动生成）

# 论文总结：Self-Policy Distillation via Capability-Selective Subspace Projection

## 1. 论文的核心问题与整体含义（研究动机和背景）

**核心问题**：大语言模型（LLM）的自蒸馏（self-distillation）方法面临两个主要困境：

- **依赖外部信号**：现有方法需要正确性过滤、执行反馈、奖励搜索等外部信号来筛选自生成数据，这些信号成本高昂，且对最强的前沿模型可能不可用。
- **无筛选方法**：另一类方法直接使用全部原始输出训练，但通常局限于代码领域，难以跨领域泛化。

**深层弱点**：自生成输出将任务相关能力与风格模式、格式伪影、模型特定错误等混在一起，稀释了针对特定能力的监督信号。

**研究目标**：能否在不依赖任何外部信号的情况下，实现可跨领域泛化、且针对特定能力的自蒸馏？

---

## 2. 论文提出的方法论

### 核心思想

提出 **Self-Policy Distillation (SPD)**，一种无需外部信号即可实现通用、针对特定能力的自蒸馏框架。

### 关键技术细节

**两阶段流程**：

**阶段一：能力子空间提取**

- 使用小规模校准集（calibration set），仅在正确性定义token位置计算损失（correctness-aligned loss）
- 收集关于K/V激活的梯度，按公式构建梯度矩阵 $G_K^{(\ell)}$ 和 $G_V^{(\ell)}$
- 对梯度矩阵进行奇异值分解（SVD），保留top-r右奇异向量
- 构建投影矩阵：$P_K^{(\ell)} = V_{K,r}^{(\ell)}(V_{K,r}^{(\ell)})^\top$ 和 $P_V^{(\ell)} = V_{V,r}^{(\ell)}(V_{V,r}^{(\ell)})^\top$

**阶段二：能力选择性蒸馏**

- 在自生成时插入投影hook，将KV激活投影到能力子空间：$\tilde{K}^{(\ell)} = K^{(\ell)}P_K^{(\ell)}$, $\tilde{V}^{(\ell)} = V^{(\ell)}P_V^{(\ell)}$
- 使用hook后的模型生成自训练数据
- 移除hook，使用标准next-token预测损失在生成数据上微调原始模型

**正确性定义span**：
- 代码生成：assertion相关token（5-20个token）
- 数学推理：最终数值答案span（1-3个token）
- QA：Answer:后的答案字母（1个token）

---

## 3. 实验设计

### 数据集与Benchmark

| 领域 | 训练/评估数据集 | 泛化评估数据集 |
|------|----------------|----------------|
| 代码生成 | MBPP | CodeAlpaca-20k |
| 数学推理 | GSM8K | SVAMP |
| QA | MMLU | BBH |

### 对比方法

- **Base model**：原始预训练模型
- **PSR (Plain Self-Retraining)**：直接在自己原始输出上微调，无筛选
- **SSD (Simple Self-Distillation)**：基于截断解码的自蒸馏基线
- **SPD (本文方法)**：能力选择性子空间投影蒸馏

### 评估指标

- MBPP: PASS@1准确率
- GSM8K/SVAMP: exact-match准确率
- MMLU: 答案字母准确率
- BBH: 标准化exact-match
- CodeAlpaca: 负对数似然（NLL，越低越好）

---

## 4. 资源与算力

**文中明确信息**：
- GPU: 4× NVIDIA A100-80G
- 训练轮次: 5 epochs
- 优化器: AdamW，lr=1e-5，weight decay=0.01
- LoRA配置: rank=8, α=8, dropout=0.05
- 应用层: Q/K/V/O投影模块

**未明确信息**：
- 各实验的具体训练时长
- 校准集大小对计算量的影响

---

## 5. 实验数量与充分性

### 实验规模

- **5个后端模型**：Qwen2.5-0.5B-Instruct、Qwen2.5-7B-Instruct、Qwen2.5-14B-Instruct、Qwen3-4B-Instruct、Llama-3.1-8B-Instruct
- **6个数据集**：MBPP、CodeAlpaca、GSM8K、SVAMP、MMLU、BBH
- **3个领域**：代码、数学、QA

### 实验类型

| 实验类型 | 数量 | 内容 |
|----------|------|------|
| 主实验 | 15项×5模型 | 各领域性能与领域内泛化 |
| 跨领域泛化 | 1组(6项) | QA校准→代码/数学 |
| 消融-损失函数 | 6项 | Full-sequence loss vs. Correctness-aligned loss |
| 消融-校准集大小 | 4规模×4指标 | 20/50/100/200/500样本 |
| 自生成数据质量 | 3类×6指标 | 生成前评估 |
| 微调对比 | 3类×6指标 | 微调后评估 |

### 充分性与客观性

- ✅ 覆盖多个模型规模和家族（Qwen、Llama）
- ✅ 领域内和跨领域泛化测试完整
- ✅ 消融实验验证关键组件贡献
- ✅ 与多种基线方法公平对比
- ⚠️ 部分大模型实验数据未完整展示

---

## 6. 论文的主要结论与发现

### 性能提升

- **最高13%** 提升超越最优无外部信号自蒸馏方法（SSD）
- **最高16%** 提升超越预训练基线
- **最高15%** 提升在跨领域泛化设置下

### 关键发现

1. **能力选择性有效**：通过正确性对齐损失提取的能力子空间能有效过滤任务无关噪声
2. **小校准集足够**：20-500样本的校准集均能稳定估计有效能力子空间
3. **跨领域迁移**：QA领域校准的能力子空间可迁移到数学和代码任务
4. **生成质量改善**：SPD生成数据在微调前已表现出更好的质量（GSM8K +7.0%, BBH +2.6%）

### 具体数据亮点（Qwen2.5-0.5B-Instruct）

| 任务 | 基线 | SPD | 提升 |
|------|------|------|------|
| MBPP | 17.0% | 25.5% | +8.5% |
| GSM8K | 11.0% | 22.0% | +11.0% |
| MMLU | 46.0% | 49.0% | +3.0% |

---

## 7. 优点

1. **无需外部信号**：完全依赖模型自身信息，不依赖正确性标签、执行器或奖励模型
2. **跨领域泛化能力强**：在领域内和跨领域设置下均表现优异
3. **能力选择性**：通过正确性定义token聚焦目标能力，过滤噪声
4. **数据效率高**：小规模校准集（50样本）即可提取有效能力子空间
5. **实现简洁**：仅需插入投影hook，不修改模型参数
6. **实验全面**：多模型、多领域、多维度消融验证

---

## 8. 不足与局限

1. **实验覆盖有限**：
   - 主要在0.5B-14B规模模型验证，未涵盖更大规模（70B+）模型
   - 仅在代码、数学、QA三个领域测试，未涉及对话、摘要等任务

2. **偏差风险**：
   - 正确性定义span的选择依赖人工设计，可能因任务而异
   - 投影rank r的选择（默认取半秩）未经系统优化

3. **应用限制**：
   - 需要可区分正确性的任务定义，对开放生成任务可能不适用
   - KV投影增加了生成时的计算开销

4. **理论分析缺失**：
   - 缺乏对为何能力子空间能泛化的理论解释
   - SVD保留top-r向量的直觉未严格证明

5. **长期遗忘问题**：
   - 未讨论SPD训练对模型其他能力的影响

（完）
