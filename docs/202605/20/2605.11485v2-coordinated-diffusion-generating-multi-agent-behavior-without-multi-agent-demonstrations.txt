Title: Coordinated Diffusion: Generating Multi-Agent Behavior Without Multi-Agent Demonstrations

URL Source: https://arxiv.org/pdf/2605.11485v2

Published Time: Mon, 18 May 2026 00:16:31 GMT

Number of Pages: 16

Markdown Content:
> 1

# Coordinated Diffusion: Generating Multi-Agent Behavior Without Multi-Agent Demonstrations 

Lasse Peters ∗, Laura Ferranti †§ , Andrea Bajcsy ‡§ , Javier Alonso-Mora †§ ∗University of California, Berkeley †Delft University of Technology ‡Carnegie Mellon University 

Fig. 1: Coordinated Diffusion (CoDi) is a method for generating multi-agent behavior from single-agent demonstrations. 

Abstract —Imitation learning powered by generative models has proven effective for modeling complex single-agent behaviors. However, teaching multi-agent systems, like multiple arms or vehicles, to coordinate through imitation learning is hindered by a fundamental data bottleneck: as the joint state-action space grows exponentially with the number of agents, collecting a suffi-cient amount of coordinated multi-agent demonstrations becomes extremely costly. In this work, we ask: how can we leverage 

single-agent demonstration data to learn multi-agent policies? We present Coordinated Diffusion (CoDi), a framework that couples independently trained single-agent diffusion policies through a user-defined multi-agent cost function, without requiring any coordinated demonstrations. We derive a new diffusion-based sampling scheme wherein the diffusion score function decomposes into independent, single-agent pre-trained base policies plus a cost-driven guidance term that coordinates these base policies into cohesive multi-agent behavior. We show that this guidance term can be estimated in a gradient-free manner, making CoDi applicable to black-box, non-differentiable cost functions without additional training. Theoretically and empirically, we analyze the conditions under which this composition can faithfully approx-imate a target multi-agent behavior. We find a complementary role for demonstration data versus the cost function: single-agent demonstrations must cover the support of the desired multi-agent behavior, while the cost function must promote desired behavior from this product of single-agent policies. Our results in simulation and hardware experiments of a two-arm manipulation task show that CoDi discovers robust coordinated behavior from single-agent data, is more data-efficient than multi-agent baselines, and highlights the importance of joint guidance, base-policy support, and cost design. Code: https://autonomousrobots.nl/paper websites/codi. 

Index Terms —Cooperating Robots, Diffusion Policies, Learn-ing from Demonstration, Multi-Robot Systems 

> §Equal advising.

I. I NTRODUCTION 

Imitation learning (IL), combined with advances in genera-tive modeling such as diffusion policies [1–4], has become a powerful way for single-agent robotic systems to learn com-plex behaviors from expert demonstrations. However, many real-world tasks such as tabletop cleaning or furniture assem-bly require multi-agent interaction: for example, consider the two fixed-base robotic manipulators in Figure 1 where each arm must rely on the other to grab an out-of-reach object. A key challenge in solving complex multi-agent tasks via imitation is data scarcity, which can be attributed to two main issues: (i) the product space of states and actions grows exponentially with the number of agents, making it challenging to get sufficient coverage and (ii) the logistical challenges of acquiring data for multi-agent systems are inevitably higher than for single-agent systems (e.g., simultaneously control-ling 2 or more robotic arms). Meanwhile, the availability of single-agent expert demonstrations has been rapidly increasing via large-scale datasets like Open-X Embodiment [5] and DROID [6]. To tackle these challenges, in this work we ask: 

How can we leverage single-agent demonstrations to learn multi-agent policies? 

Our key insight is that multi-agent success largely relies on low-level capabilities also found in single-agent tasks; the added challenge stems from high-level coordination of these single-agent skills. In the out-of-reach pick-and-place setup, for instance, the core challenge lies in the coordination between agents (e.g., which robot should pick up the object and where to place it next) rather than on the low-level skill of object interaction (e.g., how to pick an object). As a result, single-agent data can serve as a strong prior for learning multi-

> arXiv:2605.11485v2 [cs.RO] 14 May 2026 2

agent policies. 1

In this paper we introduce Coordinated Diffusion (CoDi), a novel method for generating samples from the multi-agent behavior distribution by guiding multiple single-agent dif-fusion policies towards coordinated behavior. CoDi follows a three-stage procedure summarized in Figure 1. First, as in standard imitation learning, a user provides single-agent demonstrations of basic single-agent skills (e.g., picking and placing objects). This data is used to train N single-agent diffusion policies (base policies) that model the low-level skills of each agent. Next, the user designs a multi-agent cost function that incentivizes desired multi-agent behavior (e.g., rewarding object movement towards the goal location while penalizing collisions with other agents). We construct a KL-regularized cooperative game from these ingredients, in which agents jointly minimize their expected cost while regularizing towards their single-agent base policies. We show that this game’s solution can bridge the gap between the marginal (single-agent) diffusion policies and desired multi-agent behavior, and propose a centralized guidance model to sample from it at deployment time. By construction, CoDi effectively inverts the conventional 

centralized training, decentralized execution paradigm [7]: we enable decentralized (single-agent) pre-training at the expense of requiring centralized execution. We contend that in many settings, this trade-off is preferable; enabling centralized exe-cution via multi-agent communication is often less demanding than collecting multi-agent demonstration data. In simulation and hardware experiments with a collaborative two-arm manipulation task, we validate the following key claims: (C1) CoDi discovers collaboration strategies from single-agent demonstrations, (C2) is robust enough for hard-ware deployment, and (C3) is more data-efficient than methods trained on multi-agent demonstrations; (C4) joint guidance is critical for safe and efficient coordination; (C5) our proposed guidance estimator is necessary for effective coordination; and 

(C6) guided behavior depends on the base-policy support, making cost design most critical when coordination is absent from the pre-training data. II. R ELATED WORK 

Diffusion Generative Models. A diffusion model is a neural-network-parameterized stochastic process trained to reverse the corruption of data with noise. This enables sampling from the complex data distribution by starting from a simple (Gaus-sian) noise distribution [8–11]. While early work formulated the diffusion generative process in discrete time [8], more recent approaches formulate denoising dynamics in continuous time [10–12]. Albergo et al. [13] and Gao et al. [14] show that the subsequently developed flow-matching framework [15] can be understood as an alternative perspective on the same underlying paradigm. Originally popularized in image genera-tion, where diffusion models quickly outperformed generative adversarial networks (GANs) [12, 16], these models have since 

> 1While we will use multi-agent manipulation as a running example through-out this paper, the method applies more generally to multi-agent tasks in other domains, like navigation.

been extended to many other domains, including audio [17], video [18], and 3D point clouds [19]. In this work, we adopt a continuous-time formulation of diffusion that closely follows the score-based perspectives of Song et al. [11] and Karras et al. [12], apply these models to multi-agent policy learning, a problem that we review next. 

Diffusion Policies for Imitation Learning. Diffusion models have been widely adopted for robotic policy learning, model-ing action and state-trajectory distributions to capture expres-sive, multimodal behavior in visuomotor control, planning, and goal-conditioned imitation [1–4]. In imitation-learning settings, these policies are trained with the same score-matching objective used in image generation. They have also been extended to multi-agent settings to learn the joint dis-tribution over multi-agent trajectories, enabling coordination in tasks such as multi-agent manipulation and multi-robot navigation [20, 21]. These priors on multi-agent diffusion rely on multi-agent demonstrations and achieve decentralization at test-time via the centralized training with decentralized execution (CTDE) paradigm [7]. Our work falls within the multi-agent policy learning setting but inverts the CTDE training paradigm: we pre-train on single-agent rather than multi-agent demonstrations (facilitating independent training), and use a cost function to centrally coordinate these single-agent policies at test-time into cohesive multi-agent behavior. 

Factored Diffusion Models. Prior work has investigated approximating complex target distributions as products of simpler diffusion-based factors, for example in image genera-tion [22, 23]. In single-agent policy learning, this idea has been applied to compose complex skills from simpler base poli-cies [24]. Our work applies this factorization approach along a different axis: we decompose the joint multi-agent distribution into a product of single-agent marginals, enabling training from single-agent rather than multi-agent demonstrations. In this respect, the closest prior work is Shaoul et al. [25], which also leverages single-agent diffusion policies for multi-agent motion planning. However, their method constructs joint motion plans through sequential conditioning and is limited to collision-avoidance settings because it fundamentally re-quires that each state component be influenced by only one robot’s action. CoDi, by contrast, guides single-agent policies 

simultaneously , allowing it to be applied to settings in which multiple robots can influence the same state component, as in the manipulation tasks we study. 

Diffusion Beyond Imitation: Learning from Reward Feed-back. Imitation learning via a denoising score-matching loss is not the only way to train a diffusion policy. More recent works go beyond imitation objectives, instead learning diffusion policy parameters from reward signals [26, 27]; either from scratch [26], or by fine-tuning a pre-trained im-itation policy [27, 28]. Extensions of this diffusion model training paradigm to a multi-agent CTDE paradigm have also been explored [29, 30]. In our work, we pre-train on single-agent demonstrations via a vanilla denoising score-matching objective and use reward feedback for guidance (reviewed below), rather than fine-tuning. 3

Guidance for Diffusion. Diffusion models admit test-time steering by modifying the denoising dynamics, enabling con-ditioning on additional information without retraining [16, 31]. In image generation, this property is widely exploited to improve sample quality and to condition on class labels or text prompts [16, 31, 32]. Most relevant to our work is classifier guidance [16], which steers generation via gradi-ents of a learned classifier. In single-agent policy learning, analogous techniques have been applied to guide policies via reward signals [3]. In our work, we develop a guidance scheme closely related to classifier guidance, with two key differences. First, we forgo training a classifier altogether: instead of computing guidance via the gradient of a learned classifier through automatic differentiation, we approximate the guidance signal directly from cost evaluations of online samples in a gradient-free manner. Second, rather than guiding a single monolithic policy, we apply guidance to a product of single-agent diffusion policies, enabling coordination without requiring multi-agent demonstrations. III. BACKGROUND : F ORWARD - AND REVERSE -TIME 

DIFFUSION PROCESSES 

This section provides a brief background on diffusion poli-cies and their theoretical underpinnings. For a comprehensive treatment of these foundations, we refer the reader to the tutorial by Lai et al. [33]. 

A. Forward-time Diffusion Processes: From Data to Noise 

Forward-time diffusion processes model the act of incre-mentally corrupting structured data x ∈ X ⊆ Rnx drawn from a target distribution p∗(x) with noise . To formalize this, let t ∈ [0 , T ] denote a virtual noise-time that increases as noise is added (so that t = 0 is associated with noise-free data), and let qt(x(t) | x) denote a time-varying perturbation kernel. Given these quantities, noise-corrupted data at time t

satisfies: 

x(t) ∼ p∗ 

> t

(x(t)) := 

Z

> X

p∗(x)qt(x(t) | x)d x. (1) When choosing a Gaussian perturbation kernel, i.e., qt(x(t) |

x) := N (x(t); x, t 2I), at monotonically increasing isotropic variance t2, the process {x(t)}t∈[0 ,T ] satisfies the stochastic differential equation (SDE) 

dx(t) = √2tdwt, (2) where wt is a standard Wiener process, and the initial condi-tion of the process, x(0) , follows the target distribution since we have p∗ ≡ p∗

> 0

.

B. Reverse-time Diffusion Processes: From Noise to Data 

Generative diffusion models [8–11] are based on the idea of reversing the time direction in Equation (2). Intuitively, while the forward-time diffusion process takes structured data as input and adds noise to it (as in Equation (2)) until it is indistinguishable from a (high-variance) Gaussian, the reverse-time diffusion process takes noise as input and transforms it into samples from the structured data distribution p∗.This time reversal is enabled by the result of Anderson [34], which establishes that the following reverse-time SDE governs the noise-corrupted data x(t) as noise-time flows backwards: 

dx(t) = −2t∇x log p∗ 

> t

(x(t))d t + √2td ¯ wt,

x(T ) ∼ p∗ 

> T

(x(T )) . (3) Here, ¯wt is a standard Wiener process when time flows backwards and dt is a negative time differential. Generative diffusion models leverage this SDE to sample from the target distribution p∗(x) by learning a model sθ (x(t); t) of the only unknown in this equation: the term ∇x log p∗ 

> t

(x(t)) —commonly referred to as the score .

Training. Vincent [35] show that, when samples from the data distribution p∗(x) are available, a score model sθ (x(t); t) can be trained by minimizing the denoising score matching (DSM) loss 

L(θ) := E 

> x∼p∗(x)
> t∼W
> ϵ∼N (0,I)

h

t2sθ (x + tϵ ; t) + tϵ 2i

(4) where θ are the trainable parameters of the score model sθ ,and W is a distribution over the noise-time t following [12]. 

Generating Samples. Once the score model is trained, it satisfies sθ (x(t); t) ≈ ∇x log p∗ 

> t

(x(t)) . Additionally, when the terminal noise scale T is sufficiently large, we have 

p∗ 

> T

(x(T )) ≈ N (x(T ); 0, T 2I). Therefore, we can generate samples from the target distribution p∗(x) by integrating the reverse-time SDE 

dx(t) = −2t   

> ≈∇ xlog p∗
> t(x(t))

z }| {

sθ (x(t); t) dt + √2td ¯ wt,

x(T ) ∼ N (x; 0, T 2I)

| {z }  

> ≈p∗
> T(x(T))

(5) backwards in time from T to 0 (cf. Equation (3)). 

Remarks on Notation and Properties. Equation (3) defines an implicit distribution over the target domain X that depends on the score model sθ . We denote this implicit distribution 

pθ (x). Note that, while we can draw samples x ∼ pθ (x)

via Equation (5), we cannot evaluate its density pθ (·) ana-lytically. 

C. Diffusion Policies for Robotics 

While diffusion models have been popularized in the context of image generation [9, 16], in this work we will use them to represent a stochastic policy for decision-making as in [1–4]; i.e., a distribution over action sequences (chunks) a ∈ A ⊆ 

Rna×K conditioned on the state s ∈ S ⊆ Rns , pθ (a | s).One simple way of training diffusion policies is via IL: given a dataset D := {(s(d), a(d))}Dd=1 of D state-action pairs (s(d), a(d)) with a(d) ∼ π∗(a | s(d)), one can train a score model sθ (a(t); t, s) via DSM as in Equation (4) whose implicit distribution pθ (a | s) approximates the true distribution π∗(a | s). Mirroring the sampling procedure of Section III-B, sampling actions from the policy amounts to integrating the reverse-time SDE with the learned score model for a given state s, which approximates the true distribution π∗.4

IV. C OMPOSING MULTI -A GENT BEHAVIOR FROM 

SINGLE -A GENT DIFFUSION POLICIES 

In this work, we seek to synthesize policies that allow multi-ple agents to collaboratively interact with each other. In theory, the concept of diffusion policies introduced in Section III-C naturally extends to such a multi-agent setting. However, naive training of such a multi-agent diffusion policy via IL would require vast amounts of multi-agent demonstrations: expert demonstrations of how all agents should behave jointly in a given state. Therefore, a naive extension of diffusion policies to the multi-agent setting (i.e., training a joint policy from multi-agent expert demonstrations) faces two key challenges: (i) the joint state-action space grows exponentially with the number of agents, making it difficult to ensure good coverage of the entire space with expert demonstrations, (ii) providing demonstrations for multi-agent systems is cum-bersome, even simply due to the logistical challenge of controlling N agents simultaneously. To address these challenges, this section introduces our main contribution : a framework for composing multi-agent behav-ior from single-agent diffusion policies. 

Notation: Single-Agent vs. Multi-Agent Quantities. Before we proceed to our main contribution, we introduce the follow-ing notation to distinguish between single-agent and multi-agent quantities. We use superscripts, i.e., (·)(i), to denote quantities relating to the i-th of N agents and omit such superscripts when we refer to quantities pertaining to all N

agents jointly. Following this convention, we use  

> •

s(i) ∈ S (i) to denote the state of the i-th agent, and s ∈ S 

to denote the joint state of all N agents, both of which are related via the mapping f (i) 

> single

: S → S (i) so that 

s(i) = f (i)

> single

(s), 

> •

a(i) ∈ A (i) to denote the action of the i-th agent, a := (a(1) , . . . , a(N )) ∈ A := QNi=1 A(i) to denote the joint action of all N agents,  

> •

π(a | s) to denote the multi-agent policy that character-izes a distribution over joint actions given a joint state,  

> •

and D(i) = {(s(i)(d), a(i)(d))}Di 

> d=1

to denote a dataset of single-agent demonstrations for agent i.

A. Key Idea: Multi-Agent Learning with Single-Agent Data Constraints 

Consider the scenario shown in Figure 1. Here, two robotic arms are separated on a wide table and must move an object from one location to another. Since each robot can only reach a limited area of the table, they need to collaborate to complete the task. Nevertheless, several skills required to accomplish this task are relevant even to a single agent, like the ability to pick up objects and place them elsewhere. Once both robots possess these skills, more complex behaviors—such as passing an object to another agent who cannot otherwise reach the object—can emerge through careful coordination of individual skills across agents. With this intuition, we propose Coordinated Diffusion (CoDi) which consists of three key steps (Figure 1). 

Step 1: Single-Agent Pretraining. The user provides single-agent demonstrations D(i) of basic skills for each agent (in our running example, picking and placing objects), from which we fit a single-agent diffusion policy p(i) 

> θ

(hereafter, 

base policy ) capturing a probabilistic “library” of base-skills. When agents are homogeneous, the same policy can be shared across agents. 

Step 2: Guidance Design. The user designs a

multi-agent cost function J: S × A → R, that penalizes deviations from desired multi-agent behavior (in our running example, rewarding object movement towards the goal location while penalizing collisions with other agents). 

Step 3: Deployment via Multi-Agent Policy Composition. 

At test time, we generate coordinated multi-agent behavior by composing the single-agent diffusion policies with the multi-agent cost function into a multi-agent diffusion policy, π.Mathematically, CoDi uses the ingredients from Step 1 (the single-agent base policies p(i) 

> θ

) and Step 2 (the multi-agent cost function J) to solve the following KL-regularized cooperative game in Step 3: 

π(a | s) ∈ arg min  

> ˜π∈P{A|S}

E

> a∼˜π

h

J (a, s)

i

− λD KL 



˜π p1:N

> θ



,

where p1:N 

> θ

(a | s) := 

> N

Y

> i=1

p(i) 

> θ

(a(i) | f (i)

> single

(s)) ,

> learned from single-agent data
> user-defined multi-agent cost

(6) Intuitively, in this game, agents jointly seek a policy ˜π ∈P{A | S} from the space of joint policies that minimizes their expected cost under J while staying close—in KL-divergence—to the product of their independently trained base policies p1:N 

> θ

.Via the Donsker-Varadhan variational formula [36], this cooperative game has the unique closed-form solution: 

π(a | s) := 1

Z(s) exp 

 − J (s, a)

λ

 p1:N 

> θ

(a | s). (7) Here, p1:N 

> θ

(a | s) takes the role of a prior, structured as the product of (independent) single-agent diffusion policies, and exp  −J(s, a)λ−1 takes the role of a coupling term, assigning higher probability to joint actions that result in lower cost, with λ ∈ R≥0 controlling the strength of this coupling and the scalar Z(s) := R 

> A

exp  −J(s, a)λ−1 da being the normalization constant. The structure of Equation (7) naturally raises two further questions: Does the product prior p1:N 

> θ

impose an undesirable independence assumption across agents? And what class of multi-agent behaviors can this structure actually represent? The following sections address each of these questions in turn. 

B. Can CoDi Capture Interdependent Behavior? 

A natural concern is that the product prior p1:N 

> θ

might en-force an undesirable independence assumption across agents. A useful lens for this issue comes from copula theory [37], 5

which studies how to build joint distributions from marginal models while explicitly modeling dependence. Its core result, Sklar’s theorem [38], states that any joint density ρ(z1, . . . , z d)

with continuous marginals ρj (zj ) admits the unique factoriza-tion 

ρ(z1, . . . , z d) = c F1(z1), . . . , F d(zd) ·

> d

Y

> j=1

ρj (zj ), (8) where Fj (zj ) := R zj 

> −∞

ρj (z′)d z′ is the j-th marginal cumu-lative distribution function and c : [0 , 1] d → (0 , ∞) is the 

copula density , which encodes the full dependence structure of the joint distribution independently of the marginals. 2

The proposed policy structure π in Equation (7) mirrors this form, but replaces the true marginals of the target joint dis-tribution with user-demonstrated single-agent policies. Below, we characterize the conditions under which this substitution still allows us to recover a desired multi-agent behavior. 

C. What Multi-Agent Behaviors Can We Express with Single-Agent Base Policies? 

Let us take a moment to understand what kind of multi-agent behavior we can capture via the proposed policy struc-ture of Equation (7). The key question guiding this analysis is: 

where does the “knowledge” of the multi-agent behavior really come from? Is it baked into the single-agent demonstrations provided by the user? Or is it encoded in the user-defined multi-agent cost function? To formalize this analysis, let π∗ denote a hypothetical multi-agent target policy that we wish to approximate with 

π via our proposed approach. Note that this target policy can be of arbitrary structure, i.e., it can be any distribution over the joint action space A; not limited to the composite form of Equation (7). Under which conditions can our learned policy, 

π, approximate such a target policy π∗?

Decomposing the Approximation Error. To answer this question, we can decompose the Kullback–Leibler (KL)-divergence between the target policy π∗ and our learned policy 

π as follows: 

DKL (π∗∥π) (9a) 

= E

> a∼π∗

h

log π∗(a | s) − log π(a | s)

i

= E

> a∼π∗

log π∗(a | s)

p1:N 

> θ

(a | s)

| {z }

> =DKL

(π∗∥p1:N 

> θ

)≥0

+ log Z + E

> a∼π∗

 J(s, a)

λ



| {z }̸

> =0

.

(9b) Here, the first corresponds to the KL-divergence between the target policy π∗ and the data-driven component p1:N 

> θ

,and the second corresponds to the contribution of the user-defined multi-agent cost function J.          

> 2While the original Sklar’s theorem has been developed for scalar
> marginals, extensions to the multivariate case, so called vector copulas ,exist [39].

Analyzing the Approximation Error. From this decom-position, it is clear that, if the data-driven component p1:N

> θ

(learned from single-agent demonstrations) does not capture the target behavior π∗ perfectly, i.e., DKL (π∗∥p1:N 

> θ

) > 0, then the multi-agent cost function J must compensate for this error. In Appendix A-A we analyze this case, revealing that error compensation is achievable when the target behavior π∗ is absolutely continuous w.r.t. the data-driven component p1:N

> θ

(π∗ ≫ p1:N 

> θ

); i.e., p1:N 

> θ

(a | s) = 0 must imply π∗(a | s) = 0 

for all a ∈ ˜A ⊆ A for which ˜A has non-zero measure under π∗. Intuitively, this means that the product of single-agent policies must be an over-approximation of the target multi-agent behavior. Given such an over-approximation, the role of the cost function then is to promote desired multi-agent behavior from the product of single-agent policies while suppressing any joint actions that amount to coordination failures (e.g., collisions). Assuming absolute continuity, the optimally compensating multi-agent cost function is given by 

J∗(s, a) = −λ log Z(s) + λ log p1:N 

> θ

(a | s)

π∗ (a | s) . (10) This result highlights that the role of J is to account for the log-likelihood gap between π∗ and p1:N 

> θ

. If the demonstrations are perfectly informative, we would have log p1:N  

> θ(a|s)
> π∗(a|s)

= 0 

and it is easy to see that J∗(s, a) ∝ 1. The larger the KL-gap between p1:N 

> θ

and π∗ is, the more information must be encoded in the cost function to compensate for this error. 

Key Insight. In conclusion, this analysis reveals two impor-tant results when trying to approximate multi-agent behavior 

π∗ with π in the proposed form of Equation (7) with our method. First, it reveals an important requirement for the single-agent demonstrations provided by the user: The single-agent demonstrations must be sufficiently rich to cover the support of the target behavior π∗. And second, Equation (10) reveals that the more informative the single-agent demonstra-tions are for the targeted multi-agent behavior, the less design effort needs to go into J.V. S AMPLING FROM CODI POLICIES 

So far, we have established what policy structure CoDi uses, and analyzed when this structure can accurately approxiamte multi-agent behavior. The remaining question is how to gener-ate samples from this policy so that agents can actually act at test time. Sampling from the multi-agent policy in Equation (7) is non-trivial for several reasons. First, even computing the correct normalizing constant is challenging. Moreover, since the data-driven component is represented by diffusion policies, its distribution is only known in terms of a generative model: generating samples is easy but computing the density function is not (cf. Section III). In this section, we show how to exploit the structure of Equation (7) to directly construct a generative model for the multi-agent policy, thereby tackling the challenges of sampling from the multi-agent policy. We present this key result in two steps. First, in Section V-A, we derive a reverse-time SDE following the structure of Equation (3) whose numerical inte-gration generates samples from the multi-agent policy π(a | s).6

Then, in Section V-B, we show how to construct estimators for the required ingredients of this generative model. 

A. Constructing a Generative Multi-Agent Policy 

To construct a generative model for the multi-agent policy, we seek a SDE of the form of Equation (3) whose solution matches the target distribution π(a | s).

The Multi-Agent Reverse-Time SDE. In Section III-B, we saw that a generative diffusion model for a distribution p∗(x)

can be constructed by learning a score model that satisfies 

sθ (x; t) ≈ ∇ x log p∗ 

> t

(x; t) and solving the reverse-time SDE in Equation (5) backwards in time from T to 0. By symmetry, in order to construct a generative model for π(a | s), we need a score model that satisfies sπθ (a; t, s) ≈ ∇ a log πt(a; s), where 

πt(a; s) := R 

> A

π(a | s)qt(a(t) | a)d a and solve 

da(t) = −2t  

> ≈∇ a(t)log πt(a(t); s)

z }| {

sπθ (a(t); t, s) dt + √2td ¯ wt

a(T ) ∼ N (x; 0, T 2I)

| {z }  

> ≈πT(a(T); s)

. (11) The only unknown in this equation is the multi-agent score model sπθ (a; t, s). Unlike in conventional diffusion models, however, we cannot trivially learn this score model via DSM from samples as in Equation (4) because we precisely do 

not have access to any samples from the multi-agent target policy. Next, we discuss how to overcome this challenge by directly constructing the multi-agent score sπθ (a; t, s) from known ingredients. 

Recognizing the Single-Agent Score Contribution to the Multi-Agent Score. To find an estimator of the multi-agent score model, we first decompose it in terms of the single-agent score models. To this end, as we detail in Appendix A-B, we can decompose the multi-agent score as follows: 

∇a(t) log πt(a(t); s) = (12) 

∇a(t) log p1:N 

> θ,t

(a(t) | s)

+ ∇a(t) log E 

> a∼p1:N
> θ(a|a(t),s,t )



exp 

 −J(s, a)

λ

 

| {z } 

> gJ(a(t); s,t ):=

.

Analyzing the first term reveals that it is the score of the data-driven component p1:N 

> θ

whose score model can be trivially con-structed from the scores learned by the single-agent diffusion policies: 

∇a log p1:N 

> θ,t

(a(t) | s)= ∇a log 

> N

Y

> i=1

p(i)

> θ,t

(a(i) | f (i)

> single

(s)) (13a) 

=



∇a(1) log p(1)  

> θ,t

(a(1) | f (1) 

> single

(s)) 

...

∇a(N ) log p(N ) 

> θ,t

(a(N ) | f (N )

> single

(s)) 

 (13b) 

=



s(1)  

> θ

(a(1) ; t, f (1) 

> single

(s)) 

...

s(N ) 

> θ

(a(N ); t, f (N )

> single

(s)) 

 (13c) 

=: s(1: N ) 

> θ

(a; t, s),

where s(i) 

> θ

denotes the score model underpinning the i-th agent’s single-agent diffusion policy p(i) 

> θ

. This reveals that we do not need to approximate the multi-agent score from scratch: we can re-use the single-agent score models already available from the underpinning base policies. Consequently, the only missing ingredient is the second term in Equation (12) which encodes the contribution of the user-defined multi-agent cost function J. As we show in Appendix B, this term can be related to the framework of classifier guidance [16]. We therefore refer to gJ (a(t); s, t ) as the guidance score. 

Key Insight. We can sample from our multi-agent policy π

in Equation (7) by numerically integrating the reverse diffu-sion SDE in Equation (11). The score model involved in this SDE can be constructed from the single-agent scores (which we already have from pre-training the single-agent diffusion policies) plus an extra guidance score term. Constructing an estimator for this guidance score term will be the focus of the next section. 

B. Multi-Agent Guidance Score Estimation 

Having established that the only missing ingredient required for sampling from the multi-agent policy is the guidance score, 

gJ (a(t); s, t ), in Equation (12), we construct an estimator for this term. 

Design Constraints. When constructing this estimator, we specifically consider two design constraints: (i) the user-provided cost function J may not be differentiable; and (ii) we do not have access to samples from the multi-agent target policy p1:N 

> θ

(a | s). Hence, this estimator must be constructed from point-wise evaluations of the black-box cost J, without access to its derivatives. 

1) Sampling-Based Score Estimation: Our guidance score estimator approximates gJ (a(t); s, t ) in a sampling-based (i.e., gradient-free) manner. To achieve this, as shown in Ap-pendix A-C, we rewrite gJ (a(t); s, t ) as follows: 

gJ (a(t); s, t ) = (14) 

E 

> a∼p1:N
> θ(a|a(t),s,t )

w(a(t), a, s, t )∇a(t) log qt(a(t) | a)7

where 

w(a(t), a, s, t ) := exp 

 −J(a;s)

> λ



Ea∼p1:N 

> θ(a|a(t),s,t )

h

exp 

 −J(a;s)

> λ

i − 1

(15) This formulation side-steps the need to compute gradients of 

J.3 Next, we approximate the expectations in Equation (14) in a sampling-based manner using Monte Carlo integration. This requires tractable generation of samples from the posterior 

p(a | a(t), s, t ) (sampling noise-free data a given noisy 

data a(t)). To this end, we construct a Gaussian posterior approximation using Tweedie’s formula [40, 41] as shown in Appendix A-D, admitting 

p(a | a(t), s, t ) ≈ N (a; μcodi , Σcodi ), (16) where μcodi := a(t) + t2s(1: N ) 

> θ

(a(t); t, s),

Σcodi := t2I + t4∇a(t)s(1: N ) 

> θ

(a(t); t, s)

Observe that the parameters μcodi and Σcodi of this distribution are constructed directly from the single-agent score models characterizing s(1: N ) 

> θ

(a(t); t, s) (cf. Equation (13a)), requiring no additional training. In practice, we find that the higher-order term t4∇2

> a(t)

s(1: N ) 

> θ

(a(t); t, s) is negligible, side-stepping the need to compute the score-model’s Jacobian at runtime. 

Key Insight. In summary, exploiting Equations (14) to (16) and applying Monte Carlo integration, we can approximate the guidance score of Equation (38) with M samples as 

gJ (a(t); s, t ) ≈ gJ

> codi

(a(t); s, t ) := (17) 

1

M

> M

X

> m=1

wJ

> codi

(a(m), s)∇a(t) log qt(a(t) | a(m))

where a(m) are sampled from Equation (16) and 

wJ

> codi

(a, s) := exp 

 −J(s, a)

λ



/μ w − 1, (18) with μw := 1

M

> M

X

> m=1

exp 

 −J(s, a(m))

λ



. (19) This estimator can be evaluated for varying choices of J

without retraining. VI. H ARDWARE EXPERIMENTS 

We begin our evaluation by demonstrating the qualitative behavior of CoDi. To this end, we instantiate the out-of-reach manipulation task from Figure 1 in hardware on two 7-DoF Franka Research 3 arms. These hardware experiments aim to demonstrate the practical applicability of our method and the collaborative strategies it discovers. 

Task. The robots are tasked with moving the object (a cube with an edge length of 5 cm ) to the goal location (marked by a green star in Figure 3) within a tolerance of 15 cm .                 

> 3When Jis differentiable in a, we can alternatively compute a lower-variance estimate of the expectation in Equation (14) using the reparameter-ization trick. Here, however, we are interested in the general case where the user-provided multi-agent cost need not be differentiable. hor izontal pos. / m
> −0.5 0.0 0.5 v ertical pos. / m  −0.5 0.0 0.5 arm 1 base arm 1/2 reachable area arm 2 base
> (a) Full training data. hor izontal pos . / m
> −0.5 0.0 0.5 verti  cal  pos.  / m −0.50.00.5(b) 25% training data.

Fig. 2: Distribution of initial cube positions used for training. Throughout the experiment, we repeatedly reset the cube to an arbitrary position on the left side of the workspace. Note that the dimensions of the table (width 1.8 m , depth 1.2 m )are such that no single robot can reach across the entire table. Therefore, both robots must collaborate to complete the task, e.g., place the object at an intermediate location reachable by the other robot, which then completes the task by placing the object at the goal. 

Demonstration Data. We train our method entirely in simula-tion by replicating our hardware setup in the high-fidelity Isaac Gym simulator [42]. We collect pick-and-place demonstrations using a scripted controller that drives the end-effector via inverse kinematics to randomly sampled goal positions. The dataset is split equally between pick demonstrations, where the robot interacts with reachable cubes, and yield demonstrations, where the robot returns to its home pose to leave room for the other arm at test time. Finally, to learn recovery behaviors, we reset the cube to a random position on the table at each time step with a 1% probability, prompting the expert controller to re-grasp the cube if it is within reach. A video example of these demonstrations is available in the supplementary material. The set of initial cube positions used for training is visualized in Figure 2a; goal positions are sampled from the same distribution. The resulting dataset comprises 1k demonstrations, split into 500k receding-horizon segments of 16 action predictions at a rate of 10 Hz .

Cost Function Design. The demonstration data only encodes knowledge of the base skills (here, the ability to pick up and place the cube); not of the exact task to be performed (here, the position where the cube should be placed). Instead, the task-specific information is encoded in the cost function. This setup targets a scenario where the user may want to use the same base policy for multiple different tasks, side-stepping the need to provide task-specific demonstrations. This philosophy is consistent with the single-agent approach of Janner et al. [3]. The cost function used for guidance takes the form of a weighted sum of cost components, 

J(s, a) = wgoal Jgoal (s, a) + wcollision Jcollision (s, a) (20) 

+ wengage Jengage (s, a).

Here, Jgoal (s, a) measures the distance of the object from the goal resulting from applying action-sequence a from state s;

Jcollision (s, a) is a binary indicator, equal to one if end-effectors are closer than a given safety distance as a result of taking the 8

Fig. 3: Qualitative example of multi-agent behavior generated by CoDi. Our approach guides the left robot to pick up the cube and place it at an intermediate location reachable by the right robot, which places it at the goal marked by the green star. joint action sequence a from state s, and zero otherwise; and 

Jengage (s, a) penalizes the distance of the closer robot to the object. Appendix C provides additional implementation details on this cost function. Note that, in order to evaluate Jgoal , we need to make a prediction about the combined effect of all agents’ actions on the future position of the object. Since such a prediction is not part of the diffusion policies’ output, we obtain the prediction by simulating the outcome of the joint action sequence a from state s using Isaac Gym. Note that this simulation can be done for many samples in parallel during guidance score estimation of Equation (17). 

Policy Representation. We represent the score model un-derpinning single- and multi-agent diffusion policies via a denoising UNet architecture as in Chi et al. [1]. The single-agent diffusion policy observes the ego-robot’s end-effector pose, velocity, and gripper width together with the cube pose, and predicts desired end-effector translational and rotational velocities along with gripper width. Policy outputs are con-verted to joint velocities via inverse kinematics. 

Additional Implementation Details. We attach ArUco mark-ers to the cube, which are tracked via a wrist-mounted camera on each arm. Combined with the forward kinematics of both arms, this yields a complete state observation. 

Results: On Collaboration Strategies Discovered by CoDi. 

The supplementary material includes a 12-minute hardware video in which a human repeatedly places the object on the left side of the workspace while the robots are tasked with moving it to the right. Figure 3 shows representative video frames of this qualitative evaluation. CoDi composes the independent single-agent policies into a cohesive multi-agent policy: the left robot picks up the object and places it at an intermediate location within reach of the right robot, which then completes the task. The attached video also includes qualitative examples in which we replace the cube with other objects, demonstrating a degree of robustness of the learned behavior. 

Key Takeaways. In sum, these results support the claims that (C1) CoDi discovers collaboration strategies despite pre-training only on single-agent demonstrations, and that 

(C2) CoDi is sufficiently robust for hardware deployment. VII. S IMULATION EXPERIMENTS 

Section VI demonstrated the qualitative capabilities of our approach. We now turn to simulation for a more systematic 

quantitative evaluation of our approach, including comparison to both multi-agent and single-agent baselines and analysis of key design parameters. 

A. Experimental Setup 

Task. Our simulation task matches the hardware setup of Section VI: two simulated 7-DoF Franka arms must move a cube to a goal position at the right end of the table, with initial cube positions sampled uniformly across the table. All experiments are conducted in the Isaac Gym simulator [42]. 

Data Generation. For all methods that rely on single-agent demonstrations (including our own), we use the same scripted generation strategy as in Section VI. Some of the comparisons discussed below, however, involve baselines that require multi-agent demonstration data. To generate this data, we use a scripted expert controller that follows the same procedure as in Section VI but randomly prioritizes one robot when multiple robots can reach the cube. Unless otherwise stated, for a fair comparison, we train all base policies on the same amount of data (1k demonstrations). 

Evaluation and Metrics. For each method, we perform closed-loop simulations for 50 episodes with initial configura-tions sampled from the same distribution as visualized in Fig-ure 2. We measure manipulation accuracy as the minimum distance of the cube to the goal and task efficiency as the task completion time. 

B. How does CoDi’s performance compare to methods that utilize multi-agent demonstrations? 

We first study how CoDi, which uses only single-agent data, compares to methods which use multi-agent demonstration data. Since multi-agent demonstration data is qualitatively richer but arguably more expensive to collect, we also study how the size of the single-agent demonstration data influences our ability to generate multi-agent behaviors. 

Multi-Agent Baselines. We instantiate several multi-agent baselines by applying established diffusion guidance schemes to a joint base policy, effectively treating both arms as a single “macro-agent”. Classifier guidance ( CG-Joint ): This method is in spirit closest to CoDi: like our approach, it achieves test-time behavior synthesis via guidance. Beyond relying on a joint 

base policy, however, it differs from CoDi in how the guidance 9

signal is computed. Following Janner et al. [3], CG-Joint first trains a noise-conditioned cost model to predict the expected cost for a given noisy state-action pair and then uses this cost model to guide the multi-agent base policy. The remaining three baselines are instances of fine-tuning rather than guid-ance. Diffusion Policy Mirror Descent ( DPMD-Joint ): This method applies the mirror descent algorithm of Ma et al. [26] to iteratively fine-tune the diffusion policy while penalizing deviations from the previous policy iterate. Soft Diffusion Actor-Critic ( SDAC-Joint ): This method fine-tunes the base policy by minimizing the cost while regularizing for policy entropy [26]. Expressive Policy Optimization ( EXPO-Joint ): Following [27], this method iteratively searches for local improvements around the current diffusion policy to distill them back into the diffusion policy. Appendix D provides additional details on all baselines. 

Results: On the Utility of Single-Agent Pre-training. Fig-ure 4 summarizes the results of CoDi compared to the four multi-agent baselines described above. Despite the less infor-mative demonstration data, we find that CoDi outperforms all baselines in terms of both task efficiency and manipulation accuracy: CoDi completes the task more quickly and places the cube closer to the goal than all baselines. As an ablation, we also apply our guidance scheme directly to the multi-agent 

base policy (reported as CoDi-Joint in Figure 4). We find that the performance advantage can indeed be attributed to the single-agent pre-training, as opposed to the guidance scheme itself: our guidance scheme improves the performance of the multi-agent base policy, but falls short of the performance achieved by CoDi with single-agent pre-training. This suggests that, due to the reduced size of the single-agent state-action space, single-agent pre-training gives rise to better manipula-tion skills than multi-agent training on the same data budget. 

Results: On the Sensitivity to Demonstration Data Density. 

Next, we want to understand our approach’s sensitivity to the amount of demonstration data provided compared to a baseline that uses multi-agent demonstrations. To study this, we train variants of both methods on only 20% of the original data, i.e., 200 instead of 1k demonstrations (cf. Figure 2b), and repeat the above closed-loop evaluation. For ease of exposition, we only compare against CG-Joint here, which was one of the strongest baselines in the above experiments. Figure 5 summarizes the results of this comparison. Despite training on only 20% of the single-agent data, we find that CoDi still achieves performance close to the full-data version of CG-Joint: it achieves a similar task success rate (88% vs. CG-Joint’s 92% in Figure 5a) while still achieving much better manipulation accuracy (cf. Figure 5b). By contrast, joint classifier guidance trained on only 20% of the multi-agent demonstrations degrades to only a 54% task success rate due to insufficient coverage of the multi-agent state-action space. 

Key Takeaways. In sum, these results support the claim that 

(C3) given the same data budget, CoDi is more data-efficient than methods that rely on multi-agent demonstrations and achieves better manipulation accuracy and task efficiency at the same data budget, despite its demonstrations being cheaper to provide. Co Di CoDi-Joint CG-Joint SD A C-Joint DPMD-Joint EXPO-Joint Completion Time S t ep 0 100 200 300 400 500 Cu m u la  tiv e Pr o ba b il  it  y0 .00 .51 .0            

> (a) Task efficiency. Final G oal Distanc e/ m 0. 00 0.01 0.10 1.00 Cu m u la  tiv e Pr o ba b il  it  y0.00.51.0(b) Manipulation accuracy.

Fig. 4: Comparison with baselines that utilize multi-agent demonstrations. CoDi CoDi (20%) CG-Joint CG-Joint (20%) Completion Time S t ep 0 100 200 300 400 500 Cu m u la  tiv e Pr o ba b il  it  y0 .00 .51 .0            

> (a) Task efficiency. Final G oal Distanc e/ m 0. 00 0.01 0.10 1.00 Cu m u la  tiv e Pr o ba b il  it  y0.00.51.0(b) Manipulation accuracy.

Fig. 5: Performance of CoDi and classifier guidance under varying demonstration data density. 

C. How does joint guidance compare to independent guidance for single-agent policies? 

A key aspect of our approach is that it generates coordinated 

joint actions for all robots from single-agent base policies. We seek to understand the importance of joint guidance in contrast to guiding single-agent base policies independently. 

Ablation: Independent Guidance. We instantiate Codi-Indep. as an ablation of our approach by dropping the cost components in Equation (20) that depend on other agents. Specifically, we drop the collision-avoidance cost and uncon-ditionally enable the engagement cost for both agents (instead of dynamically enabling it for the closer of the two robots). 

Results: On the Importance of Joint Guidance. Figure 6 summarizes the results of evaluating this ablation in closed-loop simulation for the same 50 initial configurations as in Section VII-B. Figures 6a and 6b show that, without joint coordination, single-agent guidance is still able to solve the task in many cases given enough time. However, Figures 6c and 6d reveal the main failure mode of independent single-agent guidance: when agents do not coordinate their actions, both may try to pick up the cube at the same time, which leads to collisions, delays, or even complete task failures. 

Key Takeaways. In sum, these results support the claim that 

(C4) joint guidance is critical for composing independently 10 CoD i CoDi-Indep. Completion Time S t ep 0 100 200 300 400 500 Cu m u la  tiv e Pr o ba b il  it  y0 .00 .51 .0                      

> (a) Task efficiency. Final G oal Distanc e/ m 0. 00 0.01 0.10 1.00 Cu m u la  tiv e Pr o ba b il  it  y0.00.51.0(b) Manipulation accuracy. Minimu mIn ter-Arm Distance 0. 00.5 1.0 Cu m u la  tiv e Pr o ba b il  it  y0.00.51.0
> Saf e D  i sta nce
> (c) Safety. (d) Failure Example.

Fig. 6: Comparison with independent single-agent guidance. pre-trained single-agent policies into safe and efficient coor-dinated multi-agent behavior. 

D. How should we compute the guidance signal for CoDi? 

In Section V-B, we proposed a sampling-based estimator to compute the guidance score from online cost evaluations. As noted in Section V-A and Appendix B, however, this is not the only option to compute the guidance signal: the connection to classifier guidance suggests an alternative in which a multi-agent cost model is trained and its gradient used as the guidance signal. Here we seek to understand whether the guidance signal computation matters, or whether applying classifier guidance to CoDi’s product-of-marginals base policy yields comparable performance. 

Ablation: Classifier Guidance Applied to the Product-of-Marginals Base Policy. To assess the importance of our sampling-based estimator, we construct an ablation that retains CoDi’s product-of-marginals structure but replaces the guidance score estimate in Equation (12) with the gradient of a trained multi-agent cost model, following the same procedure as CG-Joint in Section VII-B. We refer to this ablation as 

CG-Product .

Results: On the Choice of Guidance Estimator for CoDi. 

Figure 7 shows that CG-Product achieves substantially lower task success rates and worse manipulation accuracy than CoDi. We attribute this gap to the degree of tilt between the base policy and the target distribution. In the joint base policy setting of CG-Joint (Section VII-B), this tilt is small since joint demonstrations already encode a degree of coordination, allowing vanilla classifier guidance to perform well. The CoDi CG-Product Completion Time S t ep 0 100 200 300 400 500 Cu m u la  tiv e Pr o ba b il  it  y0 .00 .51 .0            

> (a) Task efficiency. Final G oal Distanc e/ m 0. 00 0.01 0.10 1.00 Cu m u la  tiv e Pr o ba b il  it  y0.00.51.0(b) Manipulation accuracy.

Fig. 7: Comparison with vanilla classifier guidance as alterna-tive guidance instantiation for CoDi. product-of-marginals base policy, by contrast, exhibits a larger tilt relative to the coordinated target, necessitating a dedicated guidance score estimator. 

Key Takeaways. This gap demonstrates that when guiding single-agent base policies, the choice of guidance estimator is critical: our sampling-based approach is necessary for effective sampling from Equation (7) ( C5 ). 

E. How does the support of the base policy influence guid-ance? 

Recall that in Section IV-C, we discussed how guided cost sampling cannot generate behavior outside the support of the base policy. We now empirically study this interaction between the base policy’s support and the guidance cost. 

Methods. To study the effect of the base policy support, we consider a simplified cost consisting solely of the goal term, 

Jgoal of Equation (20), isolating the role of the base policy from confounds introduced by a multi-term cost function. Using this simplified cost, we evaluate two variants of CoDi that differ only in their base policy choice: the first, CoDi-Simple , uses the product of single-agent policies as in our nominal setup; the second, CoDi-Joint-Simple , uses a joint base policy as in the multi-agent baselines of Section VII-B. By construction, these base policies differ in their support structure. The joint base policy is trained on demonstrations in which one arm always engages the cube; because it observes both end-effectors and the cube jointly, it can learn this coordination directly from data, assigning near-zero likelihood to scenarios in which neither arm is active. In contrast, the single-agent demonstrations used to train CoDi’s base policies include scenarios in which the ego robot deliberately yields to allow the other arm—unseen at training time—to handle the cube (cf. Section VI). 

Results: On the Interaction Between Base Policy Support and Guidance Cost. As shown in Figure 8, CoDi with single-agent base policies degrades substantially under the simplified cost, underscoring the critical role of the cost in composing single-agent policies into coordinated multi-agent behavior. In contrast, replacing the base policy with one trained on joint demonstrations yields consistent performance regardless 11 CoDi CoDi (simple cost) CoDi-Joint CoDi-Joint (sim ple cost) Completion Time S t ep 0 100 200 300 400 500 Cu m u la  tiv e Pr o ba b il  it  y0 .00 .51 .0            

> (a) Task efficiency. Final G oal Distanc e/ m 0. 00 0.01 0.10 1.00 Cu m u la  tiv e Pr o ba b il  it  y0.00.51.0(b) Manipulation accuracy.

Fig. 8: Comparison against variants that use a simplified cost model. of cost design, confirming that coordination is encoded in the demonstrations rather than induced by the cost. 

Key Takeaways. This result empirically confirms the design-space analysis of Section IV-C: the cost function must com-pensate for the coordination gap between the single-agent base policies and the target multi-agent behavior—a gap that multi-agent imitation learning encodes in the demonstration data in a way that single-agent demonstrations cannot (C6) .VIII. C ONCLUSION 

This work introduces CoDi, a method for synthesizing co-ordinated multi-agent behavior from single-agent demonstra-tions. By guiding the product of independently trained single-agent diffusion policies with a multi-agent cost function, CoDi bridges the gap between uncoordinated single-agent behavior and effective joint coordination—without requiring any multi-agent demonstrations. In simulation and hardware experiments on a two-arm ma-nipulation task, CoDi discovers coordinated behavior by com-bining single-agent demonstrations with a cost-based guidance signal, demonstrating greater data efficiency than multi-agent imitation learning baselines. Our ablation studies highlight the importance of joint guidance, the impact of base-policy support, and the role of cost design in achieving reliable coordination. In this work, we examined how far performance can be pushed under the extreme data constraint of no multi-agent demonstrations. In many practical scenarios, however, some 

multi-agent demonstrations may be available. This raises a natural question: how should such data be used, and how should it be combined with single-agent demonstrations? For example, multi-agent demonstrations could be used to fine-tune a base policy pretrained on single-agent data or to learn the guidance term directly, removing the need for explicit cost specification. Exploring these directions is a natural and compelling avenue for future work. Furthermore, the current version of CoDi relies on cen-tralized execution to coordinate single-agent policies at test-time. Future work should explore how to distill a decentralized multi-agent policy from our approach. Finally, this work assumes fully cooperative behavior, in which all agents optimize a shared cost function under cor-related joint action distributions. Extending CoDi to the non-cooperative setting—encompassing agents with distinct objec-tives and independent action distributions—constitutes another important avenue for future investigation. REFERENCES 

[1] C. Chi, Z. Xu, S. Feng, E. Cousineau, Y. Du, B. Burch-fiel, R. Tedrake, and S. Song, “Diffusion policy: Vi-suomotor policy learning via action diffusion,” Proc. of Robotics: Science and Systems (RSS) , 2023. [2] ——, “Diffusion policy: Visuomotor policy learning via action diffusion,” Intl. Journal of Robotics Research (IJRR) , vol. 44, no. 10-11, pp. 1684–1704, 2025. [3] M. Janner, Y. Du, J. B. Tenenbaum, and S. Levine, “Planning with diffusion for flexible behavior synthesis,” 

arXiv preprint , 2022, arXiv:2205.09991. [4] M. Reuss, M. Li, X. Jia, and R. Lioutikov, “Goal-conditioned imitation learning using score-based diffu-sion policies,” Proc. of Robotics: Science and Systems (RSS) , 2023. [5] A. O’Neill, A. Rehman, A. Maddukuri, A. Gupta, A. Padalkar, A. Lee, A. Pooley, A. Gupta, A. Mandlekar, A. Jain et al. , “Open x-embodiment: Robotic learning datasets and rt-x models: Open x-embodiment collab-oration 0,” in 2024 IEEE International Conference on Robotics and Automation (ICRA) . IEEE, 2024, pp. 6892–6903. [6] A. Khazatsky, K. Pertsch, S. Nair, A. Balakrishna, S. Dasari, S. Karamcheti, S. Nasiriany, M. K. Srirama, L. Y. Chen, K. Ellis et al. , “Droid: A large-scale in-the-wild robot manipulation dataset,” arXiv preprint arXiv:2403.12945 , 2024. [7] F. A. Oliehoek, M. T. Spaan, and N. Vlassis, “Opti-mal and approximate q-value functions for decentralized pomdps,” Journal of Artificial Intelligence Research ,vol. 32, pp. 289–353, 2008. [8] J. Sohl-Dickstein, E. Weiss, N. Maheswaranathan, and S. Ganguli, “Deep unsupervised learning using nonequi-librium thermodynamics,” in Proc. of the Int. Conf. on Machine Learning (ICML) . Proceedings of Machine Learning Research, 2015, pp. 2256–2265. [9] J. Ho, A. Jain, and P. Abbeel, “Denoising diffusion probabilistic models,” Advances in Neural Information Processing Systems (NeurIPS) , vol. 33, pp. 6840–6851, 2020. [10] Y. Song and S. Ermon, “Generative modeling by es-timating gradients of the data distribution,” Advances in Neural Information Processing Systems (NeurIPS) ,vol. 32, 2019. [11] Y. Song, J. Sohl-Dickstein, D. P. Kingma, A. Kumar, S. Ermon, and B. Poole, “Score-based generative mod-eling through stochastic differential equations,” Proc. of the Int. Conf. on Learning Representations (ICLR) , 2021. [12] T. Karras, M. Aittala, T. Aila, and S. Laine, “Elucidating the design space of diffusion-based generative models,” 12 

Advances in Neural Information Processing Systems (NeurIPS) , vol. 35, pp. 26 565–26 577, 2022. [13] M. Albergo, N. M. Boffi, and E. Vanden-Eijnden, “Stochastic interpolants: A unifying framework for flows and diffusions,” Journal of Machine Learning Research ,vol. 26, no. 209, pp. 1–80, 2025. [14] R. Gao, E. Hoogeboom, J. Heek, V. D. Bortoli, K. P. Murphy, and T. Salimans, “Diffusion meets flow matching: Two sides of the same coin,” 2024. [Online]. Available: https://diffusionflow.github.io/ [15] Y. Lipman, R. T. Q. Chen, H. Ben-Hamu, M. Nickel, and M. Le, “Flow matching for generative modeling,” in Proc. of the Int. Conf. on Learning Representations (ICLR) , 2023. [16] P. Dhariwal and A. Nichol, “Diffusion models beat gans on image synthesis,” Advances in Neural Information Processing Systems (NeurIPS) , vol. 34, pp. 8780–8794, 2021. [17] Z. Kong, W. Ping, J. Huang, K. Zhao, and B. Catanzaro, “Diffwave: A versatile diffusion model for audio synthesis,” in International Conference on Learning Representations (ICLR) , 2021. [Online]. Available: https://openreview.net/forum?id=a-xFK8Ymz5J [18] J. Ho, T. Salimans, A. Gritsenko, W. Chan, M. Norouzi, and D. Fleet, “Video diffusion models,” in Advances in Neural Information Processing Systems , S. Koyejo, S. Mohamed, A. Agarwal, D. Belgrave, K. Cho, and A. Oh, Eds., vol. 35. Curran Associates, Inc., 2022, pp. 8633–8646. [Online]. Available: https: //proceedings.neurips.cc/paper files/paper/2022/file/ 39235c56aef13fb05a6adc95eb9d8d66-Paper-Conference. pdf [19] S. Luo and W. Hu, “Diffusion probabilistic models for 3d point cloud generation,” in Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , June 2021, pp. 2837–2845. [20] D. Dong, M. Bhatt, S. Choi, and N. Mehr, “MIMIC-D: Multi-modal imitation for MultI-agent coordination with decentralized diffusion policies,” arXiv preprint arXiv:2509.14159 , 2025. [21] C. He, G. S. Camps, X. Liu, M. Schwager, and G. Sar-toretti, “Latent theory of mind: A decentralized diffusion architecture for cooperative manipulation,” arXiv preprint arXiv:2505.09144 , 2025. [22] N. Liu, S. Li, Y. Du, A. Torralba, and J. B. Tenenbaum, “Compositional visual generation with composable diffu-sion models,” in Proceedings of the European Conference on Computer Vision (ECCV) . Springer, 2022, pp. 423– 439. [23] Y. Du, C. Durkan, R. Strudel, J. B. Tenenbaum, S. Diele-man, R. Fergus, J. Sohl-Dickstein, A. Doucet, and W. S. Grathwohl, “Reduce, reuse, recycle: Compositional gen-eration with energy-based diffusion models and MCMC,” in Proceedings of the International Conference on Ma-chine Learning (ICML) . PMLR, 2023, pp. 8489–8510. [24] L. Wang, J. Zhao, Y. Du, E. H. Adelson, and R. Tedrake, “PoCo: Policy composition from and for heterogeneous robot learning,” in Robotics: Science and Systems (RSS) ,2024. [25] Y. Shaoul, I. Mishani, S. Vats, J. Li, and M. Likhachev, “Multi-robot motion planning with diffusion models,” in 

International Conference on Learning Representations (ICLR) , 2025. [26] H. Ma, T. Chen, K. Wang, N. Li, and B. Dai, “Efficient online reinforcement learning for diffusion policy,” in 

International Conference on Machine Learning . PMLR, 2025, pp. 41 837–41 853. [27] P. Dong, Q. Li, D. Sadigh, and C. Finn, “Expo: Stable reinforcement learning with expressive policies,” arXiv preprint arXiv:2507.07986 , 2025. [28] A. Z. Ren, J. Lidard, L. L. Ankile, A. Simeonov, P. Agrawal, A. Majumdar, B. Burchfiel, H. Dai, and M. Simchowitz, “Diffusion policy policy optimization,” in International Conference on Learning Representations (ICLR) , 2025. [29] Z. Li, H. Zhong, X. Wang, Q. Xia, L. Zhang, and L. Huang, “Diffusing to coordinate: Efficient online multi-agent diffusion policies,” arXiv preprint arXiv:2602.18291 , 2026. [30] Z. Zhu, M. Liu, L. Mao, B. Kang, M. Xu, Y. Yu, S. Ermon, and W. Zhang, “MADiff: Offline multi-agent learning with diffusion models,” in Advances in Neural Information Processing Systems (NeurIPS) , 2024. [31] J. Ho and T. Salimans, “Classifier-free diffusion guid-ance,” arXiv preprint arXiv:2207.12598 , 2022. [32] A. Ramesh, P. Dhariwal, A. Nichol, C. Chu, and M. Chen, “Hierarchical text-conditional image genera-tion with clip latents,” arXiv preprint arXiv:2204.06125 ,vol. 1, no. 2, p. 3, 2022. [33] C.-H. Lai, Y. Song, D. Kim, Y. Mitsufuji, and S. Ermon, “The principles of diffusion models,” arXiv preprint ,2025, arXiv:2510.21890. [34] B. D. Anderson, “Reverse-time diffusion equation mod-els,” Stochastic Processes and their Applications , vol. 12, no. 3, pp. 313–326, 1982. [35] P. Vincent, “A connection between score matching and denoising autoencoders,” Neural computation , vol. 23, no. 7, pp. 1661–1674, 2011. [36] M. D. Donsker and S. S. Varadhan, “Asymptotic eval-uation of certain markov process expectations for large time, i,” Communications on pure and applied mathe-matics , vol. 28, no. 1, pp. 1–47, 1975. [37] R. B. Nelsen, An introduction to copulas . Springer, 2006. [38] M. Sklar, “Fonctions de r´ epartition ` a n dimensions et leurs marges,” in Annales de l’ISUP , vol. 8, no. 3, 1959, pp. 229–231. [39] Y. Fan and M. Henry, “Vector copulas,” Journal of Econometrics , vol. 234, no. 1, pp. 128–150, 2023. [40] H. E. Robbins, “An empirical bayes approach to statis-tics,” in Breakthroughs in Statistics: Foundations and basic theory . Springer Verlag, 1992, pp. 388–394. [41] B. Efron, “Tweedie’s formula and selection bias,” Journal of the American Statistical Association , vol. 106, no. 496, pp. 1602–1614, 2011. [42] V. Makoviychuk, L. Wawrzyniak, Y. Guo, M. Lu, 13 

K. Storey, M. Macklin, D. Hoeller, N. Rudin, A. All-shire, A. Handa et al. , “Isaac gym: High performance gpu-based physics simulation for robot learning,” arXiv preprint , 2021, arXiv:2108.10470. APPENDIX ADERIVATIONS 

A. Optimally Compensating Multi-Agent Cost Function 

For completeness, we begin by repeating the decomposition result of Equation (9), labeling the second term as γ [J]:

DKL (π∗∥π) = 

E

> a∼π∗

log π∗(a | s)

p1:N 

> θ

(a | s)

| {z }

> DKL

(π∗∥p1:N 

> θ

)≥0

+ log Z + E

> a∼π∗

 J(s, a)

λ



| {z } 

> := γ[J]

.

To achieve a perfect match of the target behavior, i.e., 

DKL (π∗∥π) = 0 , we need π∗(a | s) = π(a | s) for all 

a ∈ ˜A ⊆ A for which ˜A has non-zero measure under π∗.Solving for J, this yields the optimally compensating multi-agent cost function 

π∗(a | s) = 1

Z(s) exp 

 −J∗(a; s)

λ



p1:N 

> θ

(a | s) (21) 

=⇒ J∗(a; s) = −λ log Z(s) + λ log p1:N 

> θ

(a | s)

π∗ (a | s) (22) 

B. Decomposition of ππt(a(t); s) (23) 

=

Z

> A

π(a | s)qt(a(t) | a)d a

= p1:N 

> θ,t

(a(t) | s)

R 

> A

π(a | s)qt(a(t) | a)d a

p1:N 

> θ,t

(a(t) | s)= p1:N 

> θ,t

(a(t) | s) R

> A
> exp

( −J(a;s) 

> λ

)p1:N   

> θ(a|s)
> Z(s)
> qt(a(t)|a)
> p1:N
> θ,t (a(t)|s)

da

= p1:N 

> θ,t

(a(t) | s)

Z

> A

exp 

 −J(a;s)

> λ



Z(s) p1:N 

> θ

(a | a(t), s, t )d a

= p1:N 

> θ,t

(a(t) | s) 1

Z(s) E 

> a∼p1:N
> θ(a|a(t),s,t )



exp 

 −J(a; s)

λ

 

,

and thus we can express the multi-agent score as 

∇a(t) log πt(a(t); s) = (24) 

∇a(t) log p1:N 

> θ,t

(a(t) | s) − (((((( (

∇a(t) log Z(s)+ ∇a(t) log E 

> a∼p1:N
> θ(a|a(t),s,t )



exp 

 −J(a; s)

λ

 

| {z } 

> gJ(a(t); s,t ):=

.

C. Simplification of gJ (a(t); s, t )

We can simplify 

gJ (a(t); s, t ) (25) 

= ∇a(t) log E 

> a∼p1:N
> θ(a|a(t),s,t )



exp 

 −J(a; s)

λ

 

=

∇a(t) Ea∼p1:N 

> θ(a|a(t),s,t )

h

exp 

 −J(a;s)

> λ

i 

Ea∼p1:N 

> θ(a|a(t),s,t )

h

exp 

 −J(a;s)

> λ

i 

= Ea∼p1:N 

> θ(a|a(t),s,t )

h

exp 

 −J(a;s)

> λ



∇a(t) log p1:N 

> θ

(a | a(t), s, t )

i

Ea∼p1:N 

> θ(a|a(t),s,t )

h

exp 

 −J(a;s)

> λ

i 

Due to the identities 

∇a(t) log p1:N 

> θ

(a | a(t), s, t ) = (26) 

∇a(t) log qt(a(t) | a) − ∇ a(t) log p1:N 

> θ,t

(a(t) | s)

and 

∇a(t) log p1:N 

> θ,t

(a(t) | s) = (27) 

E 

> a∼p1:N
> θ(a|a(t),s,t )

∇a(t) log qt(a(t) | a)

we conclude that 

gJ (a(t); s, t ) = (28) 

E 

> a∼p1:N
> θ(a|a(t),s,t )

w(a(t), a, s, t )∇a(t) log qt(a(t) | a)

where 

w(a(t), a, s, t ) := exp 

 −J(a;s)

> λ



Ea∼p1:N 

> θ(a|a(t),s,t )

h

exp 

 −J(a;s)

> λ

i − 1

(29) 

D. Gaussian approximation of the posterior p1:N 

> θ

(a | a(t), s, t )

We can obtain a Gaussian approximation of the posterior 

p1:N 

> θ

(a | a(t), s, t ) by exploiting Tweedie’s formula [40, 41] to recover the first two moments. 

E 

> a∼p1:N
> θ(a|a(t),s,t )

[a] = a(t) + t2∇a(t) log p1:N 

> θ,t

(a(t) | s)Cov  

> a∼p1:N
> θ(a|a(t),s,t )

[a] = t2I + t4∇2 

> a(t)

log p1:N 

> θ,t

(a(t) | s)

Recall that the score model satisfies sπθ (a(t); t, s) ≈∇a(t) log p1:N 

> θ,t

(a(t) | s). Hence, we can approximate 

p1:N 

> θ

(a | a(t), s, t ) ≈ N (a; μcodi , Σcodi ), (30) where μcodi := a(t) + t2sπθ (a(t); t, s),

Σcodi := t2I + t4∇a(t)sπθ (a(t); t, s)

The Jacobian of the score model, ∇a(t)sπθ (a(t); t, s), is com-puted using automatic differentiation. 14 

The formula for the first moment arises by expanding 

∇a(t) log p1:N 

> θ,t

(a(t) | s) (31a) 

= ∇a(t)p1:N 

> θ,t

(a(t) | s)

p1:N 

> θ,t

(a(t) | s) (31b) 

=

R 

> A

p1:N 

> θ

(a | s)∇a(t)

> qt(a(t)|a)

z }| {

N (a(t) | a, t 2I) d a

p1:N 

> θ,t

(a(t) | s) (31c) 

=

R 

> A

p1:N 

> θ

(a | s)( a − a(t)) N (a(t) | a, t 2I)d a

t2p1:N 

> θ,t

(a(t) | s) (31d) 

= 1

t2 E 

> a∼p1:N
> θ(a|s)

[a − a(t)] , (31e) revealing 

E 

> a∼p1:N
> θ(a|s)

[a] = a(t) + t2∇a(t) log p1:N 

> θ,t

(a(t) | s).

The formula for the second moment arises by expanding 

∇2 

> a(t)

log p1:N 

> θ,t

(a(t) | s) = (32) 

1

p1:N 

> θ,t

(a(t) | s) ∇2

> a(t)

p1:N 

> θ,t

(a(t) | s)

− 1

p1:N 

> θ,t

(a(t) | s)2 (∇a(t)p1:N 

> θ,t

(a(t) | s))( ∇a(t)p1:N 

> θ,t

(a(t) | s)) ⊤,

and exploiting the identities ∇pp = ∇ log p and 

∇2

> a(t)

p1:N 

> θ,t

(a(t) | s) (33) 

= ∇a(t)

p1:N 

> θ,t

(a(t) | s)

t2 E 

> a∼p1:N
> θ(a|a(t),s,t )

[a − a(t)] 

!

= p1:N 

> θ,t

(a(t) | s)



− 1

t2 I + 1

t4 C



where 

C := E 

> a∼p1:N
> θ(a|a(t),s,t )

(a − a(t))( a − a(t)) ⊤ (34) to recover 

∇2 

> a(t)

log p1:N 

> θ,t

(a(t) | s) = (35) 

− 1

t2 I + 1

t4 C

− (∇a(t) log p1:N 

> θ,t

(a(t) | s))( ∇a(t) log p1:N 

> θ,t

(a(t) | s)) ⊤.

Recognizing 

C = Cov  

> a∼p1:N
> θ(a|a(t),s,t )

[a] (36) 

+ E 

> a∼p1:N
> θ(a|a(t),s,t )

[a(t)] E 

> a∼p1:N
> θ(a|a(t),s,t )

[a(t)] ⊤

we conclude that 

Cov  

> a∼p1:N
> θ(a|a(t),s,t )

[a] = t2I + t4∇2 

> a(t)

log p1:N 

> θ,t

(a(t) | s).

APPENDIX BCONNECTION TO CONVENTIONAL CLASSIFIER GUIDANCE 

Background on Classifier Guidance. A useful property of diffusion models is that their generative process can be adapted at test-time by composing multiple score models. A prominent instance of this composition is classifier-based guidance [16], which adapts a diffusion model of an uncondi-tional distribution p∗(x) to instead match a new conditional 

distribution pc(x | y), where y ∈ Rny denotes test-time context information not known during training. This is enabled by the identity 

∇x(t) log pc(x(t) | t, y) (37) 

= ∇x(t) log p∗ 

> t

(x(t)) + ∇x(t) log pc 

> t

(y | x(t)) 

− (((((( (

∇x(t) log pc 

> t

(y),

where pc 

> t

(y | x(t)) := Ex∼p∗(x|x(t),t ) [pc(y | x)] is the noise-conditioned classifier: the likelihood of y averaged over the base model’s denoising posterior at noise level t. In practice, 

pc 

> t

(y | x(t)) is estimated by regressing the clean-sample score 

pc(y | x) against noisy observations x(t) via MSE: since the MSE minimizer is the conditional expectation, this recovers 

pc 

> t

(y | x(t)) exactly from samples of the joint (x, x(t)) 

drawn forward through the diffusion process. Thus, given an unconditional score model sp∗ 

> θ

(x(t); t) ≈ ∇ x log p∗ 

> t

(x(t); t),we can obtain conditional samples from pc(x | y) by adding the classifier score ∇x(t) log pc 

> t

(y | x(t)) to sp∗ 

> θ

in the reverse SDE of Equation (5). 

Recognizing gJ as a classifier score. When we interpret 

exp ( −J(a; s)/λ ) as a classifier pc(y | a, s), we can rewrite 

gJ (a(t); s, t ) as the gradient of a noise-conditioned log clas-sifier 

gJ (a(t); s, t ) := ∇a(t) log E 

> a∼p1:N
> θ(a|a(t),s,t )



> pc(y|a(t),s,t )

z }| {

exp 

 −J(a; s)

λ



= ∇a log pc(y | a(t), s, t ) (38) making Equation (12) reminiscent of the vanilla classifier guidance framework (cf. Equation (37)) with the composed score 

∇a(t) log πt(a(t); s) (39) 

= ∇a(t) log p1:N 

> θ,t

(a(t) | s + ∇a(t) log pc(y | a(t), s, t )) 

| {z } 

> gJ(a(t); s,t ))

.

APPENDIX CCOST FUNCTION IMPLEMENTATION DETAILS 

We use the following cost function structure, repeated here for convenience: 

J(s, a) = wgoal Jgoal (s, a) + wcollision Jcollision (s, a) + wengage Jengage (s, a).

Below, we provide the implementation details for this cost structure. Table I lists the values we use for any parameters referenced below. 15 

Notation. Let τ : {1, . . . , K } → S denote the joint state trajectory resulting from applying the action-sequence a from state s over the receding-horizon window of length K so that τ (k) = sk denotes the joint state at future time step 

k. Let p(i) 

> k

∈ R3 denote the position of the ith end-effector at time step k in the workspace and pobject  

> k

∈ R3 denote the position of the object at time step k. Let dws (pa, p b) denote the Euclidean distance between two points pa ∈ R3 and pb ∈ R3

in the workspace. Let dt2p (˜ τ , ˜p) denote the minimum distance between any point along the trajectory ˜τ and the point ˜p, i.e. 

dt2p (˜ τ , ˜p) = min  

> k=1 ,...,K

p(i) 

> k

− ˜p , (40) and analogously for a pair of trajectories 

dt2t (˜ τ a, ˜τ b) = min  

> k=1 ,...,K

p(i) 

> k

− p(j) 

> k

, (41) Finally, let pgoal denote the goal position and pobject denote the position of the object associated with state s. The cost components are defined as follows. 

Goal Cost. The goal cost component penalizes the minimum distance of the object to the goal along the receding-horizon trajectory 

Jgoal (s, a) = min {dt2p (τ object , p goal ) − ∆goal , 0}. (42) where ∆goal denotes the threshold at which this penalty is applied. 

Collision-Avoidance Cost. The collision-avoidance cost com-ponent penalizes the largest safety-distance violation between both end-effectors via 

Jcollision (s, a) = min {∆collision − dt2t (τ (1) , τ (2) ), 0}, (43) where ∆collision denotes the threshold at which this penalty is applied. 

Engagement Cost. The engagement cost encourages the closer robot to engage with the object via 

Jengage (s, a) (44) 

=                                 

> (min {dt2t (τ(1) , τ object )−∆engage ,0}if p(1) 1−pobject
> 1<p(2) 1−pobject
> 1
> min {dt2t (τ(2) , τ object )−∆engage ,0}otherwise .
> Parameter Symbol Value
> Goal penalty threshold ∆goal 1 cm
> Goal reaching cost weight wgoal 1
> Engage penalty threshold ∆engage 20 cm
> Engage cost weight wengage 10
> Collision avoidance radius ∆collision 30 cm
> Collision avoidance cost weight wcollision 10

TABLE I: Cost function parameters. APPENDIX DBASELINES 

Unlike CoDi, the multi-agent baselines discussed below require access to a multi-agent demonstration dataset DMA ;both to pre-train the base policy score model sθ via imitation learning on DMA , and to supply the state samples used in the fine-tuning losses below. Each baseline fine-tunes this pre-trained policy by representing the score of its target policy via a residual structure: 

sψ (a(t); s, t ) = sθ (a(t); t, s)

| {z }

> pre-trained base

+ gψ (a(t); s, t )

| {z }

> learned residual

, (45) where sθ is a score model of the pre-trained base policy and 

gψ is a learned guidance term with trainable parameters ψ.While sharing this high-level structure, the baselines differ in how gψ is parameterized and trained, as detailed below. 

A. Multi-Agent Classifier Guidance 

The multi-agent classifier guidance baseline seeks to gener-ate samples from the distribution 

π(a | s) ∝ p(a | s) exp 



− J(s, a)

λ



, (46) where p(a | s) is the base-policy learned from multi-agent 

demonstrations. As established in Appendix B, this is achieved by sampling from a diffusion model with sψ as in Equa-tion (45) and gψ = ∇a(t) log pc 

> t

, where pc 

> t

(y | a(t), s) = 

Ea∼p1:N 

> θ(a|a(t),s,t )

[exp( −J/λ )] is the noise-conditioned classi-fier from (38). We approximate the noise-conditioned classifier by learning a noise-conditioned cost model by minimizing 

LCG (ψ) = E 

> (s,a)∼D MA
> t∼W
> ϵ∼N (0,I)

[Jϕ(s, a + tϵ ; t) − J(s, a)] 2 . (47) and use its log-gradient (obtained via automatic differentiation) to approximate the guidance score as 

gψ (a(t); s, t ) = ∇a(t) log pc 

> t

(y | a(t), s) (48) 

≈ −∇ a(t)Jϕ(s, a(t); t)

λ . (49) 

B. Diffusion-Policy Mirror Descent 

Diffusion Policy Mirror Descent (DPMD) [26] iteratively updates a diffusion policy via mirror descent. At each iteration, it solves the same KL-regularized problem as Equation (6), but regularizes towards the previous policy iterate instead of the data-driven prior: 

πMD ∈ arg min  

> ˜π

E

> a∼˜π

[J(s, a)] − λD KL 

 ˜π πold  , (50) which yields the mirror descent update 

πMD (a | s) ∝ πold (a | s) exp 

 −J(s, a)

λ



. (51) The authors show that the score of target policy πMD can be learned by minimizing the cost-weighted score matching loss 

LDPMD (ψ) ∝

E 

> s∼D MA
> a∼πold (·| s)
> t∼W
> ϵ∼N (0,I)



exp 

 −J(s, a)

λ



t2sψ (a + tϵ ; s, t ) + tϵ 2



.

(52) In our implementation of DPMD and all following base-lines, we parameterize the guidance term gψ using the same denoising U-Net architecture as the base policy. 16 

C. Soft Diffusion Actor-Critic 

Soft Diffusion Actor-Critic (SDAC) [26] seeks a policy that minimizes the cost while also regularizing towards maximum entropy, reducing the problem to: 

πsoft ∈ arg min  

> ˜π

E

> a∼˜π

[J(s, a)] − λH (˜ π), (53) where H(˜ π) := − Ea∼˜π [log ˜ π(a | s)] is the Shannon entropy of ˜π. The solution is the soft-optimal policy 

πsoft (a | s) ∝ exp 

 −J(s, a)

λ



. (54) SDAC approximates the soft optimal policy by minimizing the reweighted score matching loss 

LSDAC (ψ) ∝

E 

> s∼D MA
> t∼W
> a(t)∼ht(·| s)˜ϵ∼N (0,I)



exp 

 −J(s, a(t) + t˜ϵ)

λ



t2sπψ (a(t); s, t ) + t˜ϵ 2



,

(55) here adapted for the variance schedule σ(t) = t used by our base policy as in Section III. In this formulation, ht is a user-defined auxiliary distribution. In our experiments we obtain samples from ht by corrupting rollouts from the current policy 

πold via the forward diffusion process, i.e., a(t) = a + tϵ with 

a ∼ πold (· | s) and ϵ ∼ N (0, I), as suggested in [26]. 

D. Expressive Policy Optimization 

Expressive Policy Optimization (EXPO) [27] iteratively fine-tunes the diffusion policy by generating locally improved synthetic demonstrations and distilling them back via score matching. At each iteration, an edit policy ˆπ(ˆ a | s, a) proposes action-space edits ˆa around each base sample a ∼ πold (· | s),yielding improved synthetic demonstrations ˜a = a + ˆ a. These are distilled into the diffusion policy by minimizing 

LEXPO (ψ) ∝ E 

> s∼D MA
> a∼πold (·| s)ˆa∼ˆπ(·| s,a)
> t∼W
> ϵ∼N (0,I)

h

t2sψ (˜ a + tϵ ; s, t ) + tϵ 2i

. (56) In our implementation, we approximate ˆπ via Model Predictive Path Integral Control (MPPI): given M perturbations ˆai ∼N (0, Σ) , the edit is the importance-weighted combination 

ˆa =

> M

X

> i=1

wi ˆai, wi ∝ exp 

 −J(s, a + ˆ ai)

λ



, (57) which avoids a nested learning loop to train a separate policy network.