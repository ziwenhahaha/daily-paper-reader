Title: Security-Aware Planning and Control of Multi-Agent Systems with LTL Tasks

URL Source: https://arxiv.org/pdf/2605.13134v1

Published Time: Thu, 14 May 2026 00:50:03 GMT

Number of Pages: 8

Markdown Content:
# Security-Aware Planning and Control of Multi-Agent Systems with LTL Tasks 

Georgios Mitsos ∗ Dimos V. Dimarogonas ∗∗ Siyuan Liu ∗∗ Department of Electrical Engineering, Eindhoven University of Technology, Eindhoven, the Netherlands (e-mails: {g.mitsos, s.liu5 }@tue.nl)  

> ∗∗

Division of Decision and Control Systems, KTH Royal Institute of Technology, Stockholm, Sweden (e-mail: dimos@kth.se) 

Abstract: This paper presents a secure-by-construction planning and control framework for multi-agent systems subject to linear temporal logic (LTL) specifications. The framework protects sensitive information from a passive intruder with partial observations of the agents’ motion. Security in multi-agent coordination is captured by two notions that prevent the intruder from inferring whether a secret task has been executed and from identifying the agent responsible for its execution. The proposed framework incorporates the security constraints directly into the LTL synthesis procedure by constructing a secure finite transition system that removes all paths violating these constraints. Standard LTL synthesis is then applied to this secure abstraction to generate discrete plans, which are then refined into dynamically feasible continuous trajectories. This synthesis procedure provides formal guarantees that the resulting behavior of the multi-agent system satisfies both the global LTL specification and the security constraints. The effectiveness of the proposed framework is demonstrated through a two-drone case study. 

Keywords: Secure-by-construction synthesis, multi-agent systems, linear temporal logic, opacity, trajectory planning 1. INTRODUCTION Linear temporal logic (LTL) (Baier and Katoen, 2008) can specify complex tasks beyond point-to-point navigation (LaValle, 2006). By abstracting agent dynamics to finite-state models, model-checking techniques enable synthesis of fully automated, correct-by-design discrete plans (Smith et al., 2011), which are then refined into continuous tra-jectories via low-level controllers (Fainekos et al., 2009; Kloetzer and Belta, 2008). In multi-agent systems, LTL enables high-level planning beyond classical cooperative tasks, e.g., collision avoid-ance, consensus, and formation. However, interdependent tasks and coordination requirements make LTL synthesis over the joint state space quickly intractable. To improve scalability, various decentralized methods have been pro-posed (Kloetzer and Belta, 2007; Loizou and Kyriakopou-los, 2004; Guo and Dimarogonas, 2014). Despite these advances, information security during task execution has received comparatively less attention. Au-tonomous cyber-physical systems (CPS), such as drone swarms and intelligent transportation systems, often oper-ate in settings where task correctness and confidentiality are equally critical. This motivates secure-by-construction strategies that incorporate security constraints directly into the control synthesis process (Liu et al., 2022; Yin et al., 2021). Recent works have incorporated information-flow security in robotic path planning via the notion of 

opacity ; see, e.g., (Ma and Cai, 2021; Hadjicostis, 2018; Wang et al., 2020; Xie et al., 2021) for single-agent sys-tems and (Yu et al., 2022) for multi-agent systems. How-ever, these works do not address systems with continuous dynamics, where controllers must simultaneously ensure security and LTL satisfaction in the physical workspace. In this paper, we propose a security-aware planning frame-work for multi-agent systems with continuous affine dy-namics subject to LTL specifications. We consider the presence of an intruder modeled as a malicious observer with partial observations of the system. The main contri-butions of this work are summarized as follows: (i) We introduce two complementary information-flow security notions: Type-A Security, which prevents the intruder from inferring whether any agent has visited a secret region, and Type-B Security, which prevents identifying the agent responsible for the secret visit. These requirements can be enforced independently or jointly. (ii) Based on these notions, we construct secure Weighted Transition Systems (WTSs) and apply standard LTL syn-thesis to generate secure discrete plans, which are then refined into dynamically feasible continuous trajectories that satisfy both the LTL tasks and the security con-straints. Compared to existing LTL-based security-aware planning methods (Yang et al., 2020; Yu et al., 2022; Xie et al., 2021), where systems are modeled as finite WTSs, our framework is, to the best of our knowledge, the first directly applicable to multi-agent systems with continuous dynamics, input constraints, and complex LTL specifica-tions. Moreover, unlike (Zhong et al., 2025), which focuses on opacity and safety, our framework addresses security-aware multi-agent planning under general LTL tasks. 

> arXiv:2605.13134v1 [eess.SY] 13 May 2026

(iii) Under the proposed security notions, the resulting discrete planning algorithm achieves significantly lower complexity than that in (Yu et al., 2022) (cf. Section 4.3). The remainder of the paper is organized as follows. Sec-tion 2 introduces preliminaries and notation. Section 3 formalizes the security notions for multi-agent systems, and states the planning problem. Section 4 describes the planning and control framework, ensuring both LTL sat-isfaction and security in continuous workspaces. Section 5 demonstrates the proposed method through a two-drone case study, and Section 6 concludes the paper. 2. PRELIMINARIES 

Notation. Let R denote the set of real numbers and N

the set of positive integers. We denote R≥0 and R+ the sets of nonnegative and positive real numbers, respectively. For any n, m ∈ N, Rn denotes the set of n-dimensional column vectors and Rn×m the set of n × m real matrices. Vectors are denoted by bold lowercase letters and matrices by bold uppercase letters. A class-K function is a function 

α : R≥0 → R≥0 that is strictly increasing and with 

α(0) = 0. For any vector x ∈ Rn, ∥x∥ denotes the Euclidean norm, i.e., ∥x∥ = √x⊤x.

2.1 Polytopes 

A polytope is a bounded convex set in Rn and is full-dimensional if it has a non-empty interior in Rn. Any full-dimensional polytope can be represented as the intersec-tion of d ≥ n + 1 closed half-spaces, i.e., P = x ∈ Rn |

p⊤ 

> i

x + gi ≥ 0, i = 1 , . . . , d .A polytope P ⊂ Rn consists of lower-dimensional faces ,obtained by intersecting P with its supporting hyper-planes. A j-face has dimension j (0 ≤ j < n ); facets are (n − 1)-faces, edges are 1-faces, and vertices are 0-faces. 

2.2 System model 

Consider a multi-agent system with M agents, indexed by A = {1, . . . , M }, operating in a workspace modeled as a full-dimensional polytope P ⊂ Rn. Each Agent i ∈ A 

follows the linear affine dynamics 

˙xi = Ai xi + Bi ui + bi, (1) where xi ∈ P is Agent i’s state vector, ui ∈ Ui ⊂ Rm is the input vector, Ai ∈ Rn×n and Bi ∈ Rn×m are constant matrices, and bi ∈ Rn is a constant vector. The set Ui is a convex polytope indicating bounded control inputs. The continuous trajectory of Agent i is denoted by xi(t) : 

R≥0 → P . The global trajectory of the system is the stacked vector of all agents’ trajectories, i.e., x(t) =

x1(t)⊤ . . . xM (t)⊤⊤.

2.3 Weighted transition systems 

The goal of this paper is to synthesize a continuous trajectory for a multi-agent system as in (1) subject to security constraints (cf. Sec. 3) and LTL specifications (cf. Sec. 2.5), defined over a partitioned abstraction of the agent’s motion. The polytopic workspace is partitioned into non-overlapping sub-polytopes q1, . . . , q p such that 

P = Spj=1 qj and qj ∩ qk = ∅, for j̸ = k. Each Agent i’s motion among regions is abstracted as transitions in a finite Weighted Transition System (WTS) T i, which may differ across agents. 

Definition 1. A Weighted Transition System T i is a tuple  Qi, Q i

> 0

, −→ i, w i, AP i, L i

, where: 

• Qi = {q1, . . . , q p} is the set of states, where each qj

corresponds to Agent i being in region qj

• Qi 

> 0

⊆ Qi is the set of initial states 

• −→ i⊆ Qi ×Qi is the transition relation which collects all the feasible transitions for Agent i

• wi : Qi ×Qi → R+ is the cost function that assigns to each transition ( q, q ′) ∈−→ i a positive weight wi(q, q ′)

• AP i = {πj | j ∈ N} is the set of atomic propositions used for representing some properties of interest 

• Li : Qi → 2AP i is the labeling function specifying the properties that hold true at each state The dynamical feasibility of the transitions in −→ i w.r.t. the continuous system (1) is characterized in Sec. 4.2. The abstracted motion of all M agents, each executing one transition per time step, is represented by a Global Weighted Transition System (gWTS), defined as the syn-chronous product of the individual agents’ WTSs. 

Definition 2. A Global Weighted Transition System Tg is a tuple NMi=1 T i = ( Qg , Q g, 0, −→ g , w g , AP g , L g ), where: 

• Qg = Q1 × · · · × QM is the set of global states 

• Qg, 0 = Q10 × · · · × QM 

> 0

⊆ Qg is the set of initial states 

• −→ g ⊆ Qg × Qg is the transition relation, where, for any q, q ′ ∈ Qg , ( q, q ′) ∈−→ g if ( qi, q i ′) ∈−→ i, ∀i ∈ A • wg : Qg × Qg → R+ is the cost function, where, for any ( q, q ′) ∈−→ g , wg (q, q ′) = PMi=1 wi(qi, q i ′)

• AP g = 2 AP 1 × · · · × 2AP M

• Lg : Qg → AP g is the labeling function, where, for any q ∈ Qg , Lg (q) =  L1

 q1, . . . , L M

 qM  

Each global state q =  q1, . . . , q M  ∈ Qg collects the local states of all agents. A local path of Agent i ∈ A is a finite or infinite sequence of states τi = τi(1) τi(2) . . . in T i, where 

τi(1) ∈ Qi 

> 0

and ( τi(j), τ i(j + 1)) ∈−→ i, for all j ≥ 1.

The trace of a local path is the sequence of labels along it, i.e., trace (τi) = Li(τi) = Li(τi(1)) Li(τi(2)) . . . .A global path τ = τ (1) τ (2) . . . in Tg satisfies 

τ (1) ∈ Qg, 0 and ( τ (j), τ (j + 1)) ∈−→ g , for all j ≥ 1,

and has trace (τ ) = Lg (τ ) = Lg (τ (1)) Lg (τ (2)) . . . .The sets of all finite and infinite global paths in Tg are denoted P ath ∗(Tg ) and P ath ω (Tg ), respectively, and the set of all paths is P ath (Tg ) = P ath ω (T ) ∪ P ath ∗(T ). A special class of infinite paths is the prefix-suffix form 

τ = τ (1) . . . τ (k)[ τ (k + 1) . . . τ (N )] ω , (2) where the prefix τ (1) . . . τ (k) is executed once, and the suffix τ (k + 1) . . . τ (N ) is a cycle that repeats indefinitely. It must hold that ( τ (N ), τ (k + 1)) ∈−→ g . In the sequel, we will also write a path as τ = τ (1) →g τ (2) →g . . . .2.4 Control barrier functions 

Consider an Agent i as in (1) and a set C ⊆ P ⊂ Rn

which is defined as the superlevel set of a continuously differentiable function h : Rn → R, i.e., 

C = {x ∈ P | h(x) ≥ 0}. (3) 

Definition 3. Consider a set defined as in (3). Function 

h(x) is a control barrier function (CBF) if there exists a locally Lipschitz class-K function α s.t. for each xi ∈ P :sup 

> ui∈Ui

"

∂h ∂xi

(xi)

!⊤

(Ai xi + Bi ui + bi)

#

≥ − α(h(xi)) . (4) The CBF definition (Ames et al., 2019) will be used later in Sec. 4.2 to guarantee forward invariance properties of workspace partitions. 

2.5 Linear temporal logic 

LTL formulas are defined over a set of atomic propositions 

AP using Boolean and temporal operators. The syntax of LTL is given recursively (Baier and Katoen, 2008): 

ϕ ::= p | ¬ ϕ | ϕ1 ∧ ϕ2 | X ϕ | ϕ1 U ϕ2,

where p ∈ AP and ϕ, ϕ 1, ϕ 2 are LTL formulas; X and 

U denote the next and until operators, respectively. The Boolean disjunction ( ∨) is derived as ϕ1 ∨ ϕ2:= ¬(¬ϕ1 ∧¬ϕ2), the Boolean true as p ∨ ¬ p, while the temporal operators eventually (♢) and always (□) are defined as 

♢ϕ:= true U ϕ, and □ ϕ:= ¬♢ ¬ϕ.LTL formulas are interpreted over infinite traces. A trace 

r ∈  2AP ω satisfies an LTL formula ϕ, written as r |= ϕ,if ϕ holds over r according to the LTL semantics (Baier and Katoen, 2008), which are omitted here for brevity. 

Definition 4. A Nondeterministic B¨ uchi Automaton (NBA) is a tuple Bϕ = ( S, S 0, F, Σ, δ ), where S is a finite set of states; S0 ⊆ S is the set of initial states; F ⊆ S is the set of accepting states; Σ = 2 AP is the input alphabet; and 

δ : S × Σ → 2S is the transition relation. It has been shown in (Wolper et al., 1983) that, for any LTL formula ϕ over AP , there exists a NBA Bϕ with input alphabet Σ = 2 AP such that Bϕ accepts exactly the infinite traces that satisfy ϕ.3. SECURITY CONSTRAINTS IN MULTI-AGENT SYSTEMS AND PROBLEM FORMULATION In this section, we introduce two new security properties for multi-agent systems, capturing whether a malicious intruder modeled as an outside observer can infer certain secret information. The problem formulation follows. 

3.1 Security constraints for multi-agent systems 

Consider a multi-agent systems whose motion is described by a gWTS as in Def. 2. We assume the passive intruder has knowledge of the individual agent models, i.e., T i = Qi, Q i

> 0

, −→ i, w i, AP i, L i

, for all i ∈ A , and it can observe each Agent i through an output function Hi : Qi → Yi,where Yi is the output set of possible observation symbols. To formulate the security requirement, we aim to prevent the intruder from inferring secret information based on available observations; specifically, whether sensitive be-haviors occur and, when applicable, which agents execute them. These sensitive behaviors are modeled as visits to designated secret states QiS ⊆ Qi for each Agent i.To incorporate these security-related features, the stan-dard WTS in Def. 1 is extended as 

T i =  Qi, Q i

> 0

, −→ i, w i, AP i, L i, Y i, H i, Q iS

 .

The corresponding gWTS is rewritten as 

Tg =  Qg , Q g, 0, −→ g , w g , AP g , L g , Y g , H g

, (5) where Yg = Y1 × · · · × YM and Hg : Qg → Yg with 

Hg (q) =  H1

 q1, . . . , H M

 qM  . An output sequence is denoted as yg = yg (1) yg (2) . . . , or equivalently, yg =

yg (1) →g yg (2) →g . . . .The two types of security notions are formalized as follows. 

Definition 5. Consider the gWTS in (5). A global path 

τ ∈ P ath (Tg ) is Type-A secure if there exists a global path τ ′ ∈ P ath (Tg ) such that: 

• Hg (τ ) = Hg (τ ′), and 

• for every i ∈ A and j ≥ 1: τi(j) ∈ QiS ⇒ τ ′ 

> i

(j) /∈ QiS .Intuitively, this security property states that whenever an agent visits a secret state, there exists an alternative behavior of the system that is indistinguishable to the intruder, and in which the agent is not in a secret state at that time. This provides plausible deniability: the intruder cannot be certain if any specific agent has actually visited a secret state. In the sequel, we refer to τ as the “real path” and to τ ′ as its “copy path”. 

Remark 1. For Type-A security to be meaningful, each Agent i ∈ A must have at least one non-secret state, i.e. 

Qi \QiS̸ = ∅, and a non-secret initial state, i.e., Qi 

> 0

\QiS̸ = ∅.Otherwise, the property is violated trivially. Type-A security treats each agent independently; that is, there is no collaboration between agents to guarantee security. For each agent, satisfaction of Type-A security for all paths implies satisfaction of classical state-based opacity notions, including initial-state, current-state, and infinite-step opacity (Saboori and Hadjicostis, 2007). Note that Type-A security does not prevent the intruder from identifying an agent as the only candidate for a secret task, e.g., if the intruder knows the secret task location and only one agent produces the corresponding observation. Type-B security addresses such cases. 

Definition 6. Consider the gWTS in (5). A global path 

τ ∈ P ath (Tg ) is Type-B secure if, for every Agent i ∈ A 

and every time step j ≥ 1: 

τi(j) ∈ QiS ⇒ ∃ k̸ = i s.t. Hk(τk(j)) = Hi(τi(j)) .

Intuitively, Type-B security ensures that whenever an Agent i visits a secret state, at least one other agent k

produces the same observation at that time, preventing the intruder from identifying which agent is responsible for the secret visit. A path that satisfies both Type-A and Type-B security is called Type-A/B secure .Similar security notions for multi-agent systems have been introduced in (Yu et al., 2022), which also capture whether an intruder can infer a secret state has been visited (Type-I) and by which agent (Type-II). However, unlike Type I, which requires the alternative (copy) path to never visit any secret state, our Type-A security notion imposes this restriction only at the time steps when an agent is in a secret state. Similarly, unlike Type II, which requires that some other agent visit a secret state, our Type-B leverages the intruder’s limited knowledge of the system’s evolution and only requires that another agent produces the same observation at those steps. These relaxations reduce the size of the problem while preserving similar security guarantees, making our approach considerably more scalable than the one in (Yu et al., 2022); a detailed complexity analysis is provided in Section 4.4. We use the following simple example to illustrate the proposed security notions. 

Example 1. Consider a two-agent system operating in a workspace shown in Fig. 1, where the blue circle and the green square indicate the location of Agents 1 and 2, respectively. Secret states are marked in red. At each time step, an agent may only move between adjacent states. We present one path satisfying both security notions, and another that violates them. Consider a global (finite) path τ = (D, E ) →g (E, B ) of the system as shown in Fig. 1( i), with output sequence Hg (τ ) = ( y3, y 2) →g

(y2, y 2). Note that at the second time step, Agent 2 visits secret state B, i.e., τ2(2) ∈ Q2 

> S

. According to Def. 5, the global path τ is Type-A secure since there exists another path τ ′ = ( C, E ) →g (B, E ) with the same output sequence Hg (τ ′) = ( y3, y 2) →g (y2, y 2), where τ ′

> 2

(2) /∈ Q2 

> S

.Thus, from the intruder’s point of view, these two paths are indistinguishable. Moreover, path τ is Type-B secure according to Def. 6, since: for Agent 2 with τ2(2) ∈ Q2 

> S

,there exists Agent 1 s.t. H1(τ1(2)) = H2(τ2(2)) = y2,which prevents the intruder from identifying which agent performs the secret task. Copy paths/agents are indicated with dotted outlines in Fig. 1. Consider a global path τ = ( A, B ) →g (F, A ), as shown in Fig. 1( ii ), with output sequence Hg (τ ) = ( y1, y 2) →g

(y1, y 1). Both agents visit secret states at all time steps, i.e., τ1(1) , τ 1(2) ∈ Q1 

> S

, and τ2(1) , τ 2(2) ∈ Q2 

> S

. The path τ

is not Type-A secure, since there does not exist a “copy path” that satisfies the conditions in Def. 5. In particular, for Agent 1, there is no path τ ′ 

> 1

with τ ′

> 1

(j) /∈ QiS , j = 1 , 2, such that Hg (τ ) = Hg (τ ′). Likewise, for Agent 2, there is no τ ′

> 2

(2) /∈ Q2 

> S

such that H2(τ2(2)) = H2(τ ′

> 2

(2)). Moreover, 

τ violates Type-B security at the first step, as both agents are in secret states and their observations differ, i.e., 

H1(τ1(1)) ̸ = H2(τ2(1)). 

3.2 Problem formulation 

In the previous subsections, we defined the desired LTL formulas and security properties over discrete paths and their traces. Note that the ultimate goal is to design a planning and control scheme for agents evolving in continuous time and space. Therefore, given a discrete path, we require that every discrete transition corresponds to a realizable continuous motion between designated locations of the workspace. 

Assumption 1. For each discrete transition, Agent i moves between representative points of the corresponding regions in T i. Without loss of generality, we select these points as ABC DEF{{{ 3 

> ABCDEF{{{ 3

(i) Type-A/B secure path ABC DEF{{{ 3  

> ABCDEF{{{ 3

(ii ) Non-secure path Fig. 1. Two agents operate in a workspace. States in the same row generate the same observation yi, i = 1 , 2, 3. the region centroids. Thus, a global transition ( q, q ′) starts with all Agents i at the centroid of region qi, and ends with all agents at the centroid of region qi ′.Assumption 1 may exclude some feasible trajectories but allows us to guarantee the existence of a continuous controller for every discrete transition. The next definition formalizes how a global path is induced as the sequence of visited sub-polytopes. 

Definition 7. Let xi(t) be the trajectory of Agent i, and define fi(t) = qj when xi(t) ∈ qj . The switching times of Agent i are defined recursively as 

ti, 1 = 0 , ti,k +1 = inf 

n

t > t i,k | fi(t)̸ = f (ti,k )

o

.

This yields τi = τi(1) τi(2) . . . , with τi(j) = fi(ti,j ). Let ( sℓ)ℓ∈N be the increasing ordering of all switching times across all agents. At each sℓ, the global state 

τ (ℓ) = ( τ1(ℓ), . . . , τ M (ℓ)) is defined, and all agents i ∈ A 

transition from τi(ℓ − 1) to τi(ℓ) simultaneously. If the system eventually remains in a global state τ (k), then τ (k)repeats indefinitely, generating an infinite global path. 

Remark 2. By Def. 7, each global trajectory x induces a unique global path τ , representing the sequence of visited sub-polytopes. Since labels, observations, and secret states are constant within each sub-polytope, x(t) and τ produce identical traces. Hence, we write trace (x) |= ϕ to indicate that a continuous trajectory satisfies an LTL formula 

ϕ with a slight abuse of notations. We then have that 

trace (x) |= ϕ if and only if trace (τ ) |= ϕ, and, Type-A/B security of τ implies the same for x.This paper first aims to compute a discrete plan that satis-fies the global LTL specification and security requirements, and then synthesize a low-level (piecewise) continuous controller that implements the motion of the multi-agent system corresponding to each discrete transition. 

Problem 1. Security-aware multi-agent LTL plan-ning and control 

Given an input-constrained multi-agent system as in (1) operating in a partitioned workspace P and a global task specified by an LTL formula ϕ, synthesize a controller such that the generated global trajectory x : R≥0 → P satisfies: (1) trace (x) |= ϕ, and (2) x is Type-A and/or Type-B secure. 4. SECURE-BY-CONSTRUCTION PLANNING AND CONTROL In this section, we present the planning and control algo-rithm for solving Problem 1. First, secure discrete abstrac-tions of the multi-agent system are constructed, retaining only global paths that satisfy the security constraints. Next, the feasibility of each transition w.r.t. system dy-namics and input constraints is verified. LTL synthesis is then employed to compute a prefix-suffix path that satisfies the global task. Finally, a continuous trajectory is generated from the discrete path, guaranteeing satisfaction of both the global LTL task and the security constraints. 

4.1 Secure global transition systems Type-A security. Type-A security ensures that the in-truder can never infer that any agent is in a secret state. To enforce it, we construct two abstractions: the Twin-Global Weighted Transition System (Twin-gWTS), which captures pairs of indistinguishable global states, and the Secure Twin-gWTS, which removes all pairs where any agent’s real and copy local states are both secret. The concept of a Twin-Weighted Transition System was introduced in (Yang et al., 2020) and is extended here to multi-agent systems. 

Definition 8. Let Tg be a gWTS in (5). The corresponding Twin-gWTS is 

V =  X, X 0, −→ V , w V , AP V , L V

,

where: 

• X ⊆ Qg × Qg is the set of states, with ( q1, q 2) ∈ X if 

q1, q 2 ∈ Qg and Hg (q1) = Hg (q2)

• X0 = X ∩ (Qg, 0 × Qg, 0) is the set of initial states 

• −→ V ⊆ X × X is the transition relation, where, for any ( q1, q 2), (q′

> 1

, q ′

> 2

) ∈ X, (( q1, q 2), (q′

> 1

, q ′

> 2

)) ∈−→ V if: (q1, q ′

> 1

) ∈−→ g , ( q2, q ′

> 2

) ∈−→ g , and Hg (q′

> 1

) = Hg (q′

> 2

)

• wV : X × X → R+ is the cost function, where, for any (( q1, q 2), (q′

> 1

, q ′

> 2

)) ∈−→ V , wV (( q1, q 2), (q′

> 1

, q ′

> 2

)) = 

wg (q1, q ′

> 1

)

• AP V = AP g

• LV : X → AP V is the labeling function, where, for any ( q1, q 2) ∈ X, LV (( q1, q 2)) = Lg (q1)Each twin state q = ( q1, q 2) =    q11 , . . . , q M 

> 1

), (q12 , . . . , q M

> 2

 

in the Twin-gWTS represents a pair of indistinguishable global states, i.e., Hg (q1) = Hg (q2), where q1 is the “real state” and q2 the “copy state”. Intuitively, the system is in q1, but the intruder cannot distinguish it from q2.

Definition 9. Let V be a Twin-gWTS in Def. 8. The corresponding Secure Twin-gWTS is 

Vs =  Xs, X s, 0, −→ s, w s, AP s, L s

,

and is obtained by removing all twin states ( q1, q 2), in which there is an Agent i ∈ A such that qi

> 1

, q i 

> 2

∈ QiS . All other components are inherited from V in Def. 8. For a twin state xs = ( q1, q 2) ∈ Xs, denote Π( xs) = q1. For a twin path σ = σ(1) σ(2) · · · ∈ P ath (Vs), the projected path in P ath (Tg ) is Π( σ) = Π( σ(1))Π( σ(2)) . . . .

Type-B security. Type-B security addresses scenarios in which the intruder can identify an agent as the only candidate for performing a secret task. To enforce it, we construct the Type-B gWTS as follows. 

Definition 10. Let Tg be a gWTS in (5). The correspond-ing Type-B gWTS T Bg is 

T Bg =  QBg , Q Bg, 0, −→ Bg , w Bg , AP Bg , L Bg

,

and is obtained by restricting the global states to 

QBg =

n

(q1,. . ., q M ) ∈ Qg |qi ∈ QiS ⇒ ∃ k̸ = i : Hi(qi) = Hk(qk)

o

.

All other components are inherited from Tg in (5). 

Type-A/B security. Type-A and Type-B security can be enforced simultaneously by first constructing the Type-B gWTS T Bg , and then building the Twin-gWTS V and Secure Twin-gWTS Vs on top of it. The following propo-sitions are provided without proofs, as they hold trivially by the construction of the secure gWTSs. 

Proposition 1. By construction, every path σ ∈ P ath (Vs)satisfies both Type-A and Type-B security. 

Proposition 2. For every path τ ∈ P ath (Tg ) that satisfies both Type-A and Type-B security, there exists at least one path σ ∈ P ath (Vs) such that Π( σ) = τ .Proposition 2 shows that the constructed Vs captures all paths that satisfy both Type-A and Type-B security, yielding completeness guarantees. 

4.2 Transition feasibility 

A discrete transition ( q, q ′) ∈−→ s, with q̸ = q′, is said to be dynamically feasible w.r.t. system (1) if there exists a controller ui, for each real and copy Agent i ∈ A , such that the trajectory xi(t) : [0 , t f ] → P drives the agent from the centroid ξi of the current sub-polytope qi to the centroid 

ξ′ 

> i

of the next sub-polytope qi ′, satisfying: (i) the agent’s dynamics in (1); (ii) input constraint ui(t) ∈ Ui; (iii) initial and final centroid positions; (iv) if qi̸ = qi ′, Agent i crosses the exit facet Fc,i = qi ∩ qi ′ at time t = tc as in Def. 7; and (v) Agent i remains within qi for t ∈ T1 = [0 , t c], and within qi ′ for t ∈ T2 = [ tc, t f ]. Real and copy Agent i ∈ A 

share the same dynamics and input bounds. 

Lemma 3. Let qi ⊂ Rn be a convex polytope with facets 

Fj ∈ qi, each represented by hj (x) = p⊤ 

> j

x + gj =0. Suppose there exists a locally Lipschitz continuous controller ui : T1 → Ui s.t. for all Fj ∈ qi and t ∈ T1,

p⊤ 

> j

(Ai xi(t) + Bi ui(t) + bi) ≥ − α(hj (xi(t))) , (6) where α(r) = γ r with γ > 0. Then, qi is forward invariant 

for t ∈ T1. Likewise, if the condition holds for all Fj ∈ qi ′

and t ∈ T2, then qi ′ is forward invariant for t ∈ T2.

Proof. Note that the functions hj satisfying (6) are CBF as in Def. 3. It has been shown that any locally Lipschitz continuous controller ui(t) : [0 , t c] → Ui that satisfies the CBF constraints (6) on all facets Fj of qi renders qi forward invariant (Ames et al., 2019), i.e., xi(0) ∈ qi ⇒ xi(t) ∈

qi, ∀t ∈ T1 = [0 , t c]. The same reasoning applies to qi ′. ■

Lemma 3 ensures that by applying ui satisfying (6), the closed-loop trajectory xi remains within qi for all t ∈ T1,and qi ′ for all t ∈ T2, and thus, feasibility condition (v) is satisfied. Consequently, if qi̸ = qi ′, we have xi(tc) ∈ Fc,i =

qi ∩ qi ′, i.e., hc,i (xi(tc)) = 0, satisfying condition (iv). To determine if a transition (q, q ′) ∈−→ s is feasible, we check if there exist controllers, one for each real and each copy agent, that drive each agent between the corresponding sub-polytope centroids while satisfying conditions (i)-(v). This is done by solving a centralized optimization program for each ( q, q ′). 

Problem 2. Multi-agent trajectory optimization. 

For each real and copy Agent i, compute a trajectory 

xi and a locally Lipschitz continuous controller ui that realizes ( q, q ′) while satisfying feasibility conditions (i)–(v), by solving the following centralized quadratic program (QP): min 

> xi,ui, i ∈A

Z tf

> 0

X

> i∈A

|| ui(t)|| 2dt 

s.t. ˙xi(t) = Aixi(t) + Biui(t) + bi, ∀i ∈ A 

ui(t) ∈ U i, ∀i ∈ A 

xi(0) = ξi, xi(tf ) = ξ′

> i

, ∀i ∈ A 

hc,i (xi(tc)) = p⊤

> c,i

xi(tc) + gc,i = 0 , ∀i ∈ A 

p⊤ 

> j

(Ai xi(t) + Bi ui(t) + bi) ≥ − γ h j (xi(t))) ,

∀i ∈ A , F j ∈ qi, t ∈ [0 , t c]

p⊤ 

> j

(Ai xi(t) + Bi ui(t) + bi) ≥ − γ h j (xi(t))) ,

∀i ∈ A , F j ∈ qi ′, t ∈ [tc, t f ]

Remark 3. The above QP is solved numerically in asampled-data fashion over a fixed time horizon tf , i.e., the control input is constant over each time step, the dynamics are approximated via a forward-Euler scheme, and the objective integral is discretized as a sum. Despite employing a centralized planning approach, the optimiza-tion program is convex and can be solved efficiently. If the QP admits a solution, then the discrete transition (q, q ′) ∈−→ s is feasible: conditions (i)-(iii) are enforced by the first three constraints of the QP, while conditions (iv)-(v) follow from Lemma 3 and are enforced by the last three constraints. Otherwise, the discrete transition ( q, q ′)is considered dynamically infeasible and is discarded from the WTS. 

Remark 4. Since sub-polytopes are defined by non-strict inequalities, trajectories remaining on facets over finite time intervals may create ambiguities in labels, obser-vations, and secret status. To avoid this, a small slack variable ϵ > 0 is introduced in all CBF constraints: 

p⊤ 

> j

(Ai xi(t) + Bi ui(t) + bi) ≥ − γ (hj (xi(t))) − ϵ).

Subtracting ϵ from hj shifts the zero level set hj = 0, i.e., the sub-polytope boundary, inward, ensuring trajectories remain strictly in the interior of each sub-polytope, except at facet-crossing instants t = tc.

4.3 LTL task satisfaction 

In order to incorporate a given LTL specification ϕ, we synchronize the resulting secure Twin gWTS Vs with the NBA Bϕ that accepts ϕ to form a Product B¨ uchi Automaton (PBA). 

Definition 11. A Product B¨ uchi Automaton is AP = Vs ⊗Bϕ = ( SP , S P, 0, F P , δ P , w P ), where: 

• SP = Xs × S is the set of product states 

• SP, 0 = Xs, 0 × S0 is the set of initial states 

• FP = Xs × F is the set of accepting states 

• δP ⊆ SP × SP is the transition relation, where, (( q, s ), (q′, s ′)) ∈ δP iff ( q, q ′) ∈−→ s and ∃σ ∈ Ls(q′)such that s′ ∈ δ(s, σ )

• wP : δP → R+ is the cost function, where, for any (( q, s ), (q′, s ′)) ∈ δp, wP (( q, s ), (q′, s ′)) = ws(q, q ′)The PBA further restricts the dynamics of Vs to satisfy ϕ.A path τ in Vs satisfies ϕ if and only if it corresponds to an accepting path in the PBA, i.e., a path that starts from an initial state in SP, 0 and visits accepting states in FP

infinitely often. Furthermore, if the PBA admits at least one accepting state, it also admits at least one in prefix-suffix form (2) (Baier and Katoen, 2008). The cost of τ is defined as J(τ ) = β J prefix (τ )+(1 −β) Jsuffix (τ ), β ∈ [0 , 1], with Jprefix (τ ) = Pki=1 w(τ (i), τ (i + 1)) , and Jsuffix (τ ) = 

PN −1 

> i=k+1

w(τ (i), τ (i + 1)) + w(τ (N ), τ (k + 1)). Finding an optimal discrete plan reduces to identifying a prefix-suffix path τ in the product system AP that minimizes J(τ ). In the next subsection, we provide our planning and control algorithm to find an optimal tra-jectory that satisfies both an LTL formula and security constraints. 

4.4 Security-aware multi-agent planning and control 

This section presents Alg. 1, which computes a controller such that the resulting continuous multi-agent trajectory satisfies both the global LTL task and the security prop-erties. Before executing the algorithm, the workspace is partitioned, and the motion of multi-agent system is ab-stracted as a gWTS Tg as in Sec. 2.3. Then, a secure gWTS is constructed as in Sec. 4.1 to satisfy the security prop-erties: the Secure Twin-gWTS Vs as in Def. 9 for Type-A security, and the Type-B gWTS T Bg as in Def. 10 for Type-B. The transition feasibility in the resulting secure gWTS W (i.e., Vs or T Bg , depending on the security type) is checked via the optimization program in Sec. 4.2. In Alg. 1, line 1 converts the LTL formula ϕ to an NBA Bϕ, and line 2 builds the PBA AP from W and 

Bϕ. Line 3 initializes the optimal path and cost. Lines 4–10 iterate over all accepting states: for each qacc , the shortest prefix path to the accepting state and the shortest suffix cycle are computed using Dijkstra’s algorithm, and their total cost is calculated (lines 5-7); if it improves 

Algorithm 1 Secure Trajectory Planning and Control 

Input: LTL formula ϕ, secure gWTS W, weight β

Output: secure trajectory x⋆, control u⋆, cost J⋆ 

> 1:

Convert LTL formula ϕ to NBA Bϕ 

> 2:

Build PBA AP from W and Bϕ 

> 3:

Initialize τ ⋆ ← ∅ and J⋆ ← ∞  

> 4:

for each accepting state qacc ∈ FP do  

> 5:

Compute shortest prefix path πprefix from any initial state q0 ∈ SP, 0 to qacc using Dijkstra’s algorithm  

> 6:

Compute shortest suffix path πsuffix starting and ending at qacc using Dijkstra’s algorithm  

> 7:

Compute total cost Jtotal = β·Jprefix +(1 −β)·Jsuffix  

> 8:

if Jtotal < J ∗ then  

> 9:

J∗ ← Jtotal  

> 10:

τ ∗ ← πprefix ⊕ πsuffix  

> 11:

Reconstruct trajectory x⋆(t) and control u⋆(t) from τ ⋆ 

> 12:

return x ⋆(t), u⋆(t), J⋆the current minimum, the corresponding prefix-suffix path is stored as τ ⋆ (line 10). Finally, line 11 constructs the continuous trajectory x⋆(t) and control u⋆(t) from τ ⋆

by concatenating the continuous motion segments xq,q ′

obtained from the optimization program in Sec. 4.2, one for each discrete transition ( q, q ′) in τ ⋆. Line 12 returns them along with the optimal cost J⋆. This ensures that the resulting trajectories satisfy both the LTL specification and the desired security properties. 

Theorem 4. Consider a multi-agent system as in (1) sub-ject to an LTL formula ϕ and Type-A/B security con-straints. Alg. 1 computes a continuous trajectory x⋆(t)that: (i) satisfies ϕ, (ii) guarantees Type-A, and/or Type-B security, and (iii) is dynamically feasible. 

Proof. The optimal discrete plan τ ⋆ computed by Alg. 1 satisfies the LTL formula ϕ, as it visits an accepting state in the PBA AP infinitely often, and the desired security properties, as shown in Prop. 1. The continuous trajectory 

x⋆(t) is obtained by concatenating the trajectory segments 

xq,q ′

(t) corresponding to each discrete transition ( q, q ′) in 

τ ⋆. By construction, each segment xq,q ′

(t) respects the agents’ dynamics and input constraints, hence x⋆(t) is dynamically feasible as discussed in Sec. 4.2. Moreover, Lemma 3 guarantees fi(t) = qi for all t ∈ [0 , t c], and, 

fi(t) = q′ 

> i

for all t ∈ [tc, t f ], where fi is the function defined in Def. 7. Then, by Remark 2, the concatenated trajectory 

x⋆(t) satisfies ϕ and the same security properties as τ ⋆,which concludes the proof. ■

Complexity analysis. Consider a multi-agent system with M agents, where each agent’s abstracted motion is represented by a WTS T i with at most |Q| states. Let the NBA for the LTL formula have |S| states. The gWTS Tg is the synchronous product of all T i, yielding at most |Q|M states. For Type-A/B security, the Type-B gWTS T Bg is first constructed from Tg , followed by the Twin-gWTS V and the Secure Twin-gWTS Vs as in Defs. 8-9, resulting in at most |Q|2M states for Vs and 

|Q|2M |S| states for the PBA. Thus, Alg. 1 performs a shortest-path search over a graph with at most |Q|2M |S|

states. Note that Type-A security has the same complexity, while Type-B has at most |Q|M |S| states. Compared to the approach in (Yu et al., 2022) which requires a graph search over (2 |Q|)2M 2+M |S| states, our framework requires significantly lower computational complexity. In all cases, the dominant contribution to complexity comes from constructing the gWTS, while the contribution of the NBA is typically minor. 5. CASE STUDY We demonstrate our results in a two-drone scenario: Drone 1 scans air quality and encodes the collected data, while Drone 2 relays the data and visits a diag-nostics station for inspection. The bounded workspace 

P = {x = [x1 x2]⊤ | 0 ≤ x1 ≤ 10 , 0 ≤ x2 ≤ 5}

is partitioned into seven convex sub-polytopes q1, . . . , q 7

using a modified script from the LTLCon package (Kloet-zer and Belta, 2007). Each sub-polytope is labeled ac-cording to the atomic propositions AP = {scan , encode ,

transmit , inspect , obs } via the labeling function L. Two regions are secret: q1 containing sensitive environmental data, and q3, a secure diagnostics station. Both drones }scan encode obs q1q 3q 4q 5q6transmit inspect    

> q2q7

(a) Drone 1 (real and copy trajectories) }scan encode obs q1q 3q 4q 5q6transmit inspect    

> q2q7

(b) Drone 2 (real and copy trajectories) Fig. 2. Workspace and synthesized trajectories. start from q4 and must always avoid the obstacle region q5,which models restricted airspace due to strong turbulence. An intruder monitors the airspace, which is divided into two observation regions: y1 is the southern and y2 is the northern section. The corresponding workspace partition is shown in Fig. 2, where green, red, and gray areas denote initial, secret, and obstacle regions, respectively. The goal is to synthesize a trajectory satisfying Type A/B security and a mission specification given as an LTL formula: 

ϕ = □ ♢ 



scan 1 ∧ ♢  encode 1 ∧ ♢transmit 2 

∧

♢inspect 2 ∧ □ ¬ obs ,

enforcing a repeating sequence: Drone 1 performs the air-quality scan, then encodes the data, followed by Drone 2’s transmission. Moreover, Drone 2 must eventually execute an inspection, while both always avoid the obstacle. The superscript denotes the agent responsible for each propo-sition. Each Drone i ∈ { 1, 2} follows linear affine dynamics 

˙xi =

 0.1 −0.15 

−0.1 0.2



xi +

1 0 0 1 



ui +

0.20.3



,

where xi = [ xi, 1 xi, 2]⊤ ∈ P , ui = [ ui, 1 ui, 2]⊤ ∈ Ui and 

Ui = [ −3, 3] × [−3, 3] , ∀i ∈ { 1, 2}.The implementation was carried out in Python 3.11 on a MacBook Air M2. Polytope operations were performed using the CDD library (Fukuda, 2025). First, the discrete abstraction Tg of the system was constructed (Section 2.3). The Type-B gWTS was then obtained by removing states that violate Type-B security, while the Twin-gWTS and Secure Twin-gWTS were subsequently derived to retain only states satisfying Type-A security (Section 4.1). Tran-sition feasibility in the resulted WTS was checked by solving the QP in Python, subject to the agent’s dynamics, control bounds, facet-crossing constraints, and the CBF constraints (Section 4.2). For each transition, one CBF constraint is enforced for every facet of the current poly-tope over the interval [0 , t c] and similarly for each facet of the next polytope over [ tc, t f ]. As an example, consider the transition in which Drone 1 moves from q2 to q7,and, specifically, the facet of q2 given by [1 −1] x + 2 = 0 with the CBF constraint: [1 −1] ( A1 x1 + B1 u1 + b1) ≥−γ ([1 −1] x1 + 2) + ϵ for all t ∈ [0 , t c]. We used the con-stants γ = 1 and ϵ = 0 .01. Next, we follow Alg. 1 to gener-ate the continuous trajectory. The NBA corresponding to 

ϕ was generated using LTL2BA (Gastin and Oddoux, 2020). The secure abstraction was then synchronized with this NBA to construct the PBA (Section 4.3). The shortest pre-fix–suffix path minimizing J = 0 .5 Jprefix + 0 .5 Jsuffix was computed, and the continuous trajectories were obtained by concatenating the trajectory segments associated with the discrete transitions along the prefix–suffix path (Sec-tion 4.4). The synthesized discrete path is: ( q4, q 4) →g

(q4, q 6) →g (q4, q 7) →g (q4, q 2) →g (q6, q 3) →g (q6, q 2) →g

(q7, q 7) →g (q2, q 6) →g [( q1, q 4) →g (q2, q 6)] ω , and the copy path: ( q4, q 4) →g (q4, q 6) →g (q4, q 6) →g (q4, q 6) →g

(q6, q 6) →g (q6, q 6) →g (q6, q 6) →g (q6, q 6) →g [( q4, q 4) →g

(q6, q 6)] ω . The runtime for the complete algorithm is ap-proximately 70 sec on the MacBook Air M2. The corresponding continuous trajectories, shown in Fig. 2, demonstrate that the LTL specification and the required security properties are satisfied. When Drone 1 visits the secret region q1, the copy path simultaneously visits q4,a non-secret region with the same observation, satisfying Type-A security, while Drone 2 is also in q4, preventing the intruder from inferring which agent entered a secret state and ensuring Type-B security. Similarly, when Drone 2 visits q3, its copy path is in q6 and Drone 1 in q6, satisfying Type-A/B security. These results demonstrate that the proposed approach generates dynamically feasible, LTL-satisfying, and secure trajectories for multi-agent systems. 6. CONCLUSION In this paper, we addressed the problem of security-aware multi-agent motion planning and control under LTL specifications. We proposed a centralized framework that first computes a discrete path and then refines it into continuous, dynamically feasible trajectories that satisfy the LTL task while enforcing Type-A, Type-B, or Type-A/B security. Future work could focus on extending the approach to nonlinear dynamics, and investigating hierar-chical architectures to improve scalability for multi-agent systems. REFERENCES Ames, A., Coogan, S., Egerstedt, M., Notomista, G., Sreenath, K., and Tabuada, P. (2019). Control barrier functions: theory and applications. In Proc. 18th IEEE Eur. Conf. Control , 3420–3431. Baier, C. and Katoen, J. (2008). Principles of Model Checking . MIT Press. Fainekos, G.E., Girard, A., Kress-Gazit, H., and Pappas, G.J. (2009). Temporal logic motion planning for dy-namic robots. Automatica , 45(2), 343–352. Fukuda, K. (2025). cddlib: An efficient implementation of the double description method. Gastin, P. and Oddoux, D. (2020). LTL2BA (version 1.3). Guo, M. and Dimarogonas, D.V. (2014). Multi-agent plan reconfiguration under local ltl specifications. Int. J. Robot. Res. , 34(2), 218–235. Hadjicostis, C.N. (2018). Trajectory planning under current-state opacity constraints. IFAC-PapersOnLine ,51, 337–342. Kloetzer, M. and Belta, C. (2007). Temporal logic plan-ning and control of robotic swarms by hierarchical ab-stractions. IEEE Trans. Robot. , 23(2), 320–330. Kloetzer, M. and Belta, C. (2008). A fully automated framework for control of linear systems from temporal logic specifications. IEEE Trans. Autom. Control , 53(1), 287–297. LaValle, S.M. (2006). Planning Algorithms . Cambridge Univ. Press. Liu, S., Trivedi, A., Yin, X., and Zamani, M. (2022). Secure-by-construction synthesis of cyber-physical sys-tems. Annu. Rev. Control , 53, 30–50. Loizou, S.G. and Kyriakopoulos, K.J. (2004). Automatic synthesis of multi-agent motion tasks based on ltl spec-ifications. In Proc. 43rd IEEE Conf. Decis. Control ,volume 1, 153–158. Ma, Z. and Cai, K. (2021). Optimal secret protections in discrete-event systems. IEEE Trans. Autom. Control ,67(6), 2816–2828. Saboori, A. and Hadjicostis, C.N. (2007). Notions of security and opacity in discrete event systems. In Proc. 46th IEEE Conf. Decis. Control , 5056–5061. Smith, S.L., Tumova, J., Belta, C., and Rus, D. (2011). Optimal path planning for surveillance with temporal-logic constraints. Int. J. Robot. Res. , 30(14), 1695–1708. Wang, Y., Nalluri, S., and Pajic, M. (2020). Hyperprop-erties for robotics: Planning via hyperltl. In IEEE Int. Conf. Robot. Autom. , 8462–8468. Wolper, P., Vardi, M.Y., and Sistla, A.P. (1983). Rea-soning about infinite computation paths. In Proc. 24th Annu. Symp. Found. Comput. Sci. , 185–194. Xie, Y., Yin, X., Li, S., and Zamani, M. (2021). Secure-by-construction controller synthesis for stochastic systems under linear temporal logic specifications. In 60th IEEE Conf. Decis. Control , 7015–7021. Yang, S., Yin, X., Li, S., and Zamani, M. (2020). Secure-by-construction optimal path planning for linear tempo-ral logic tasks. In Proc. 59th IEEE Conf. Decis. Control ,4460–4466. Yin, X., Zamani, M., and Liu, S. (2021). On approximate opacity of cyber-physical systems. IEEE Trans. Autom. Control , 66(4), 1630–1645. Yu, X., Yin, X., Li, S., and Li, Z. (2022). Security-preserving multi-agent coordination for complex tem-poral logic tasks. Control Eng. Pract. , 123, 105130. Zhong, B., Liu, S., Caccamo, M., and Zamani, M. (2025). Secure-by-construction synthesis for control systems. 

IEEE Trans. Autom. Control , 70(6), 4170–4177.