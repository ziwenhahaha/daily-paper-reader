---
title: "DriveDreamer-Policy: A Geometry-Grounded World-Action Model for Unified Generation and Planning"
title_zh: DriveDreamer-Policy：一种用于统一生成与规划的几何对齐世界-动作模型
authors: "Yang Zhou, Xiaofeng Wang, Hao Shao, Letian Wang, Guosheng Zhao, Jiangnan Shao, Jiagang Zhu, Tingdong Yu, Zheng Zhu, Guan Huang, Steven L. Waslander"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01765v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 连接 VLA 和世界模型的几何接地世界动作模型
tldr: DriveDreamer-Policy 是一种统一的驾驶世界-动作模型，旨在解决现有模型缺乏几何感知的问题。它将深度生成、未来视频预测和运动规划集成在单一架构中，利用大语言模型处理多模态输入，并通过轻量级生成器输出结果。该模型在 Navsim 基准测试中表现优异，证明了显式几何学习能显著提升视频想象的质量和规划的鲁棒性。
selection_source: fresh_fetch
motivation: 现有的世界-动作模型多关注2D表征，缺乏对物理世界至关重要的几何感知能力。
method: 提出一种统一架构，结合大语言模型与三个轻量级生成器，同步实现深度图生成、未来视频预测和驾驶动作规划。
result: 在 Navsim v1 和 v2 基准测试中分别达到 89.2 PDMS 和 88.7 EPDMS，在闭环规划和世界生成任务上均优于现有模型。
conclusion: 通过引入显式深度学习增强几何感知，模型能生成更连贯的未来场景并做出更可靠的驾驶决策。
---

## 摘要
近期，世界-动作模型（WAM）的出现架起了视觉-语言-动作（VLA）模型与世界模型之间的桥梁，统一了它们的推理与指令遵循能力以及时空世界建模能力。然而，现有的 WAM 方法通常侧重于建模 2D 外观或潜表示，对几何对齐（geometric grounding）的关注有限，而几何对齐是物理世界中运行的具身系统的核心要素。我们提出了 DriveDreamer-Policy，这是一种统一的驾驶世界-动作模型，在单个模块化架构中集成了深度生成、未来视频生成和运动规划。该模型采用大语言模型来处理语言指令、多视图图像和动作，随后通过三个轻量级生成器产生深度、未来视频和动作。通过学习几何感知的世界表示，并将其用于在统一框架内指导未来预测和规划，所提模型能够产生更连贯的想象未来和更明智的驾驶动作，同时保持了模块化和可控的延迟。在 Navsim v1 和 v2 基准测试上的实验表明，DriveDreamer-Policy 在闭环规划和世界生成任务上均取得了强劲性能。特别是，我们的模型在 Navsim v1 上达到了 89.2 PDMS，在 Navsim v2 上达到了 88.7 EPDMS，在产生更高质量的未来视频和深度预测的同时，优于现有的基于世界模型的方法。消融实验进一步表明，显式深度学习为视频想象提供了互补的增益，并提高了规划的鲁棒性。

## Abstract
Recently, world-action models (WAM) have emerged to bridge vision-language-action (VLA) models and world models, unifying their reasoning and instruction-following capabilities and spatio-temporal world modeling. However, existing WAM approaches often focus on modeling 2D appearance or latent representations, with limited geometric grounding-an essential element for embodied systems operating in the physical world. We present DriveDreamer-Policy, a unified driving world-action model that integrates depth generation, future video generation, and motion planning within a single modular architecture. The model employs a large language model to process language instructions, multi-view images, and actions, followed by three lightweight generators that produce depth, future video, and actions. By learning a geometry-aware world representation and using it to guide both future prediction and planning within a unified framework, the proposed model produces more coherent imagined futures and more informed driving actions, while maintaining modularity and controllable latency. Experiments on the Navsim v1 and v2 benchmarks demonstrate that DriveDreamer-Policy achieves strong performance on both closed-loop planning and world generation tasks. In particular, our model reaches 89.2 PDMS on Navsim v1 and 88.7 EPDMS on Navsim v2, outperforming existing world-model-based approaches while producing higher-quality future video and depth predictions. Ablation studies further show that explicit depth learning provides complementary benefits to video imagination and improves planning robustness.

---

## 论文详细总结（自动生成）

### DriveDreamer-Policy 论文深度总结

#### 1. 核心问题与研究动机
自动驾驶系统正从模块化堆栈向端到端视觉-语言-动作（VLA）模型演进。然而，现有的**世界-动作模型（World-Action Models, WAM）**大多局限于 2D 外观或潜空间表征的建模，缺乏对物理世界至关重要的**几何对齐（Geometric Grounding）**。
*   **研究动机**：自动驾驶本质上是一个 3D 几何随时间演变的 4D 物理过程。缺乏几何感知的模型在处理遮挡推理、距离估计和物理一致性运动时存在局限。
*   **核心问题**：如何构建一个既能理解语义、又能感知 3D 几何，并能统一生成未来场景与规划动作的驾驶模型？

#### 2. 方法论：核心思想与技术细节
DriveDreamer-Policy 采用了一种**“大语言模型（LLM）+ 轻量级生成专家”**的模块化架构：
*   **核心架构**：
    *   **感知层（LLM）**：使用 Qwen3-VL-2B 作为骨干，处理多视图图像、语言指令和当前动作，输出固定长度的“世界查询（World Queries）”和“动作查询（Action Queries）”嵌入。
    *   **生成层（三个专家模块）**：
        1.  **深度生成器**：像素空间扩散 Transformer，生成单目深度图作为 3D 几何支架。
        2.  **视频生成器**：潜空间扩散 Transformer，生成动作条件下的未来视频。
        3.  **动作生成器**：扩散 Transformer，预测未来的行驶轨迹（坐标与朝向）。
*   **关键技术：因果注意力掩码（Causal Attention Mask）**：
    在查询组之间施加结构化的因果顺序：**深度查询 → 视频查询 → 动作查询**。视频生成可以参考深度几何信息，而动作规划则能同时利用深度和视频预测的上下文。
*   **算法流程**：采用**条件流匹配（Conditional Flow Matching, CFM）**作为训练准则，通过回归速度场 $v_\theta$ 来学习从噪声分布到数据分布的变换。

#### 3. 实验设计
*   **数据集与场景**：主要在 **Navsim v1 和 v2** 基准测试上进行，该数据集源自真实驾驶日志，提供同步的环视传感器输入。
*   **Benchmark（指标）**：
    *   **规划性能**：PDMS（v1）和 EPDMS（v2），综合评估碰撞、道路合规性、舒适度等。
    *   **生成质量**：视频使用 LPIPS、PSNR、FVD；深度使用 AbsRel（绝对相对误差）和 $\delta$ 阈值准确率。
*   **对比方法**：
    *   **视觉端到端**：TransFuser, UniAD, DiffusionDrive。
    *   **VLA 模型**：AutoVLA, DriveVLA-W0, Recogdrive。
    *   **世界模型**：PWM, Epona, FSDrive, LAW 等。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：单阶段联合多任务训练，共 **100k 步**，Batch Size 为 32。
*   **模型初始化**：LLM 采用 Qwen3-VL-2B，深度模块初始化自 PPD，视频模块初始化自 Wan-2.1-T2V-1.3B。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 Navsim 的 navtrain（10万样本）上训练，在 navtest（1.2万样本）上评估。
*   **消融实验**：
    1.  **世界学习对规划的影响**：对比了“仅动作”、“深度+动作”、“视频+动作”及全模型，证明了多模态生成的互补性。
    2.  **深度对视频生成的影响**：验证了几何先验能显著提升视频质量（FVD 降低）。
    3.  **查询数量（Query Number）**：测试了不同 Token 数量对性能的增益。
*   **评价**：实验设计较为充分，涵盖了规划、视频生成、深度估计三个维度，对比了当前主流的 SOTA 模型，结果具有客观性。

#### 6. 主要结论与发现
*   **性能领先**：在 Navsim v1 达到 89.2 PDMS，在 v2 达到 88.7 EPDMS，刷新了基于世界模型的规划记录。
*   **几何的重要性**：显式的深度学习不仅提供了 3D 结构，还显著提升了未来视频生成的连贯性和规划的安全性（如减少碰撞风险）。
*   **统一性优势**：通过单一架构实现多任务，模型能够根据需求灵活切换“仅规划”或“生成+规划”模式。

#### 7. 优点与亮点
*   **几何对齐（Geometry-Grounded）**：首次在世界-动作模型中引入显式的像素级深度生成，解决了以往模型“视觉上合理但物理上不准确”的问题。
*   **模块化设计**：通过固定大小的 Query 接口连接 LLM 与生成专家，既保留了 LLM 的推理能力，又实现了生成任务的解耦与高效控制。
*   **因果链条**：创新的 3D → 2D → 1D 信息流设计，符合物理世界的逻辑顺序。

#### 8. 不足与局限
*   **分辨率限制**：为了控制计算成本，深度和视频生成的空间分辨率较低（144 × 256），可能损失远距离小目标的细节。
*   **伪标签依赖**：深度生成的训练目标来自 Depth Anything 3（DA3）模型而非真实 LiDAR 数据，其上限受限于教师模型的精度。
*   **实时性挑战**：虽然采用了轻量级专家，但扩散模型的多步迭代推理在车载实时部署上仍面临计算压力。

（完）
