---
title: "D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay for Stable Reinforcement Learninging Robotic Manipulation"
title_zh: D-SPEAR：用于稳定机器人操作强化学习的双流优先经验自适应回放
authors: "Yu Zhang, Karl Mason"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27346v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 机器人操作的强化学习
tldr: 针对机器人操作任务中强化学习训练不稳定的问题，本文提出D-SPEAR框架。该框架通过解耦Actor和Critic的经验回放策略，让Critic利用优先采样提高学习效率，而Actor利用低误差样本增强策略稳定性。结合自适应锚点机制和Huber损失函数，D-SPEAR在robosuite基准测试中显著提升了SAC和TD3等算法的性能与稳定性。
selection_source: fresh_fetch
motivation: 现有的离策强化学习算法在复杂机器人任务中常因忽略Actor和Critic对数据需求的不同而导致训练不稳定和性能崩溃。
method: 提出双流经验回放框架，解耦Actor与Critic采样，并引入基于TD误差变异系数的自适应锚点机制及Huber损失函数。
result: 在robosuite的抬块和开门等挑战性任务中，D-SPEAR在最终性能和训练稳定性上均优于SAC、TD3和DDPG等主流基准算法。
conclusion: 解耦Actor与Critic的采样策略并结合自适应调整机制，能有效解决机器人操作中的策略振荡问题，提升强化学习的鲁棒性。
---

## 摘要
由于丰富的接触动力学、长时程以及训练不稳定性，机器人操作对于强化学习而言仍具挑战性。尽管 SAC 和 TD3 等离策（off-policy）演员-评论家（actor-critic）算法在仿真中表现良好，但在现实场景中往往会遭遇策略振荡和性能崩溃，部分原因在于经验回放策略忽略了演员和评论家对数据的不同需求。我们提出了 D-SPEAR：双流优先经验自适应回放，这是一种在维持共享经验回放池的同时，解耦演员和评论家采样的回放框架。评论家利用优先回放进行高效的价值学习，而演员则使用低误差样本进行更新，以稳定策略优化。一种自适应锚定机制根据 TD 误差的变异系数平衡均匀采样和优先采样，而基于 Huber 损失的评论家目标函数进一步提高了在异构奖励尺度下的鲁棒性。我们在 robosuite 基准测试中具有挑战性的机器人操作任务（包括抬方块和开门）上评估了 D-SPEAR。结果表明，D-SPEAR 在最终性能和训练稳定性方面均持续优于 SAC、TD3 和 DDPG 等强力离策基准算法，消融实验证实了演员端和评论家端回放流的互补作用。

## Abstract
Robotic manipulation remains challenging for reinforcement learning due to contact-rich dynamics, long horizons, and training instability. Although off-policy actor-critic algorithms such as SAC and TD3 perform well in simulation, they often suffer from policy oscillations and performance collapse in realistic settings, partly due to experience replay strategies that ignore the differing data requirements of the actor and the critic. We propose D-SPEAR: Dual-Stream Prioritized Experience Adaptive Replay, a replay framework that decouples actor and critic sampling while maintaining a shared replay buffer. The critic leverages prioritized replay for efficient value learning, whereas the actor is updated using low-error transitions to stabilize policy optimization. An adaptive anchor mechanism balances uniform and prioritized sampling based on the coefficient of variation of TD errors, and a Huber-based critic objective further improves robustness under heterogeneous reward scales. We evaluate D-SPEAR on challenging robotic manipulation tasks from the robosuite benchmark, including Block-Lifting and Door-Opening. Results demonstrate that D-SPEAR consistently outperforms strong off-policy baselines, including SAC, TD3, and DDPG, in both final performance and training stability, with ablation studies confirming the complementary roles of the actorside and critic-side replay streams.

---

## 论文详细总结（自动生成）

### 论文总结：D-SPEAR：用于稳定机器人操作强化学习的双流优先经验自适应回放

#### 1. 核心问题与研究背景
在机器人操作任务中，强化学习（RL）面临接触动力学复杂、任务时程长以及训练不稳定等挑战。现有的离策（Off-policy）演员-评论家（Actor-Critic）算法（如 SAC、TD3）虽然在仿真中表现尚可，但在复杂任务中常出现策略振荡或性能崩溃。
*   **核心矛盾**：传统的优先经验回放（PER）对所有组件使用统一的采样策略。然而，**评论家（Critic）**需要高 TD 误差的样本来快速修正价值估计，而**演员（Actor）**如果使用这些高误差（即不准确）的样本进行更新，会引入高方差的梯度，导致策略不稳定。

#### 2. 方法论
论文提出了 **D-SPEAR** 框架，其核心思想是在共享经验池的基础上，为 Actor 和 Critic 解耦采样策略：
*   **双流采样（Dual-Stream Replay）**：
    *   **评论家流（Critic Stream）**：采用标准 PER，优先采样高 TD 误差样本，以加速价值函数的收敛。
    *   **演员流（Actor Stream）**：采用“逆优先”采样，倾向于选择低 TD 误差的样本，确保策略更新基于相对准确的价值估计，从而增强稳定性。
*   **自适应锚点机制（Adaptive Anchor Mechanism）**：
    *   引入一个均匀采样的“锚点集”来保证数据的多样性。
    *   通过计算经验池中 TD 误差的**变异系数（CV）**来动态调整均匀采样比例 $\lambda$。当 TD 误差分布差异大时，增加优先采样的权重；当分布趋于均匀时，增加均匀采样的比例。
*   **鲁棒评论家目标函数**：
    *   将 Critic 的损失函数由均方误差（MSE）替换为 **Huber 损失**，以降低对异常值和异构奖励尺度的敏感性。

#### 3. 实验设计
*   **实验场景**：基于 `robosuite` 仿真基准测试，选取了两个具有挑战性的任务：
    *   **Lift（抬升）**：抓取并抬起方块，涉及长时程决策。
    *   **Door（开门）**：操作铰链门，涉及复杂的接触动力学。
*   **机器人平台**：7 自由度 Franka Panda 机器人。
*   **对比基准（Baselines）**：SAC（基础算法）、TD3、DDPG。
*   **评估指标**：回合回报（Episode Return）随训练步数的变化曲线，以及训练过程中的方差（稳定性）。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件配置（如 GPU 型号、数量）及总训练时长。
*   实验参数设定：每个任务训练 $2.5 \times 10^5$ 步，控制频率为 20Hz，每个回合上限 500 步。

#### 5. 实验数量与充分性
*   **实验规模**：每个实验均使用 **5 个随机种子**进行重复，并绘制了均值和标准差阴影图，结果具有统计学意义。
*   **消融实验**：作者设计了三组消融实验（移除双流结构、移除 Critic 高 TD 采样、移除 Actor 低 TD 采样），充分验证了各组件对性能和稳定性的贡献。
*   **客观性**：对比了多种主流算法，并在不同性质的任务（抬升 vs. 接触密集型开门）上验证，实验设计较为客观公平。

#### 6. 主要结论与发现
*   **性能领先**：D-SPEAR 在两个任务上的最终回报均显著高于 SAC、TD3 和 DDPG。在 Lift 任务中，其回报几乎是 SAC 的两倍。
*   **稳定性增强**：相比于基准算法在训练后期的性能剧烈振荡或崩溃，D-SPEAR 的学习曲线更加平滑且单调上升。
*   **互补性**：消融实验证明，Actor 端的低误差采样和 Critic 端的高误差采样是互补的，缺一不可。

#### 7. 优点
*   **逻辑清晰**：敏锐地捕捉到了 Actor 和 Critic 对数据需求本质上的“错配”问题。
*   **即插即用**：该框架作为一种经验回放的改进方案，可以轻松集成到现有的各种离策 Actor-Critic 算法中。
*   **自适应性**：通过 CV 动态调整采样比例，减少了人工调参的负担，增强了算法在不同训练阶段的灵活性。

#### 8. 不足与局限
*   **任务覆盖面**：虽然在 `robosuite` 上表现良好，但实验仅限于两个任务，尚未在更复杂的长序列任务（如多阶段装配）中验证。
*   **全局统计局限**：自适应机制基于全局 TD 误差的统计，可能无法捕捉到状态空间中局部区域的特殊需求。
*   **缺乏实机验证**：所有实验均在仿真环境中完成，尚未在真实机器人（Real-to-Sim）上验证其应对传感器噪声和现实动力学间隙的能力。

（完）
