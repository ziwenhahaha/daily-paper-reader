---
title: Boosting Vision-Language-Action Finetuning with Feasible Action Neighborhood Prior
title_zh: 利用可行动作邻域先验提升视觉-语言-动作模型微调
authors: "Haochen Niu, Kanyu Zhang, Shuyu Yin, Qinghai Guo, Peilin Liu, Fei Wen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01570v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 机器人操作的视觉-语言-动作模型微调
tldr: 针对机器人操作中存在“可行动作邻域（FAN）”这一特性，本文提出了一种基于FAN引导的正则化方法。传统VLA训练忽略了动作的等效性，导致泛化性差。该方法通过引入高斯先验，使模型输出分布在首选方向和幅度周围保持局部平滑和单峰，从而显著提升了微调阶段的样本效率以及在分布内和分布外场景下的任务成功率。
selection_source: fresh_fetch
motivation: 现有的VLA训练方法忽略了机器人操作中状态对应的可行动作邻域特性，导致模型泛化能力和样本效率低下。
method: 引入一种FAN引导的正则化器，利用高斯先验塑造模型输出分布，使其与可行动作空间的几何结构对齐。
result: 在强化微调和监督微调实验中，该方法显著提高了样本效率以及在分布内和分布外场景下的成功率。
conclusion: 通过对齐物理操作中的动作容差，FAN引导的正则化为实现高效且泛化性强的VLA模型适配提供了一种原则性且实用的方法。
---

## 摘要
在现实世界的机器人操纵中，状态通常允许一个近乎等效动作的邻域。也就是说，对于每个状态，存在一个可行动作邻域（FAN）而非单一的正确动作，在该邻域内的运动所产生的进展是难以区分的。然而，现有的 VLA 训练方法直接继承自语言设置，未能利用 FAN 特性，从而导致泛化能力差和样本效率低。为了解决这一局限性，我们引入了一种 FAN 引导的正则化器，通过塑造模型的输出分布使其与 FAN 的几何结构对齐。具体而言，我们引入了一个高斯先验，在首选方向和幅度周围促进局部平滑且单峰的预测。在强化微调（RFT）和监督微调（SFT）的大量实验中，我们的方法在样本效率以及分布内和分布外（OOD）场景的成功率方面均取得了显著提升。通过与物理操纵固有的动作容差相对齐，FAN 引导的正则化为实现高样本效率且具泛化性的 VLA 适配提供了一种有原则且实用的方法。

## Abstract
In real-world robotic manipulation, states typically admit a neighborhood of near-equivalent actions. That is, for each state, there exist a feasible action neighborhood (FAN) rather than a single correct action, within which motions yield indistinguishable progress. However, prevalent VLA training methodologies are directly inherited from linguistic settings and do not exploit the FAN property, thus leading to poor generalization and low sample efficiency. To address this limitation, we introduce a FAN-guided regularizer that shapes the model's output distribution to align with the geometry of FAN. Concretely, we introduce a Gaussian prior that promotes locally smooth and unimodal predictions around the preferred direction and magnitude. In extensive experiments across both reinforced finetuning (RFT) and supervised finetuning (SFT), our method achieves significant improvement in sample efficiency, and success rate in both in-distribution and out-of-distribution (OOD) scenarios. By aligning with the intrinsic action tolerance of physical manipulation, FAN-guided regularization provides a principled and practical method for sample-efficient, and generalizable VLA adaptation.

---

## 论文详细总结（自动生成）

### 论文总结：利用可行动作邻域先验提升视觉-语言-动作模型微调

#### 1. 核心问题与研究动机
*   **核心问题**：现有的视觉-语言-动作（VLA）模型微调方法（如 SFT 和 RFT）直接沿用了自然语言处理中的“预测下一个 Token”模式，将动作视为离散且互斥的类别。
*   **研究动机**：在物理机器人操作中，动作具有**内在容差（Tolerance）**和**近等效性**。例如，抓取物体时，向左移动 1.0cm 与移动 0.9cm 或 1.1cm 的效果几乎相同。这种特性被称为**可行动作邻域（Feasible Action Neighborhood, FAN）**。
*   **现状局限**：忽略 FAN 会导致模型在微调时出现“概率分布过尖（Spiky）”的现象，表现为对训练数据的严重过拟合，且在分布外（OOD）场景下泛化能力极差，强化学习微调时样本效率低下。

#### 2. 方法论
*   **核心思想**：引入一种 **FAN 引导的正则化器**，通过 KL 散度约束模型的输出分布，使其符合物理动作空间的几何特性（即局部平滑、单峰且连续）。
*   **关键技术细节**：
    *   **FAN 定义**：定义为在给定状态下，与最优动作 $a^*$ 产生的 Q 值差异在阈值 $\delta$ 以内的连通动作集合。
    *   **正则化项**：在损失函数中加入 $D_{KL}(\pi_{\theta} || \mathcal{N}(\mu, \Sigma))$。其中 $\mu$ 是模型预测的最优动作，$\Sigma$ 控制邻域的大小。
    *   **监督微调（FAN-SFT）**：使用**动态协方差**。正则化项根据模型当前的预测方差自适应调整，引导模型从“排他性正确”转向“加权容差”。
    *   **强化微调（FAN-PPO）**：使用**固定协方差**以保证训练稳定性。通过数学证明（Proposition 1），该目标函数使新策略在旧策略、高斯先验和指数化 Q 值之间进行几何插值。
*   **算法流程**：该方法无需修改模型架构或自回归解码方案，仅在训练目标函数中增加一个正则化项，具有“即插即用”的特点。

#### 3. 实验设计
*   **数据集与场景**：
    *   **ManiSkill**：使用 `PutOnPlateInScene25Main-v3` 任务，包含 25 种多样的拾取与放置原语。
    *   **LIBERO**：涵盖 LIBERO-Spatial、Object、Goal 和 Long 四个子集。
    *   **真实世界实验**：使用 7 自由度 JAKA 机械臂进行物体放置任务。
*   **Benchmark 与对比方法**：
    *   **基准模型**：OpenVLA（单步动作预测）和 OpenVLA-OFT（动作块预测）。
    *   **对比算法**：RL4VLA（SFT/RFT 基准）、标准 PPO、标准 SFT、标签平滑（Label Smoothing）以及熵最大化（Entropy Maximization）。
*   **评估维度**：分布内（In-Distribution）成功率、15 种 OOD 扰动（视觉、语义、执行层面）、样本效率（收敛速度）和真实世界泛化性。

#### 4. 资源与算力
*   **硬件环境**：所有实验均在 **NVIDIA A100 (80GB)** GPU 上完成。
*   **具体配置**：
    *   **SFT**：使用 2 到 4 块 A100，Batch Size 为 32-48。
    *   **RFT**：利用 ManiSkill 的 GPU 并行仿真特性，在 1 块 A100 上进行强化学习训练。
*   **训练时长**：文中未明确给出总小时数，但提到 RFT 实验中，FAN-PPO 达到 90% 成功率所需的步数仅为基准方法的约 1/3。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了仿真与实物、监督与强化、单步与块预测等多个维度，实验组数较多。
*   **充分性**：
    *   针对 15 种不同的 OOD 扰动进行了详尽测试，证明了鲁棒性。
    *   进行了**消融实验与参数敏感性分析**（如正则化系数 $\alpha$ 和标准差 $\sigma$ 的影响）。
    *   对比了标签平滑等替代方案，验证了 FAN 结构化先验的优越性。
*   **客观性**：使用了主流开源模型（OpenVLA）和标准 Benchmark，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **性能提升**：在 ManiSkill SFT 中，平均成功率提升 5.2%；在 RFT 中，平均提升 6.2%（执行类 OOD 提升超过 10%）。
*   **样本效率**：FAN 引导显著加速了 RFT 的收敛过程，样本效率提升约 3 倍。
*   **泛化能力**：在 LIBERO 空间扰动测试和真实世界实验中，FAN 方法表现出极强的空间泛化能力，能有效应对物体或容器位置的偏移。
*   **分布形态**：实验证实，更宽、更平滑的动作分布（代表更大的 FAN）与模型的泛化性能呈正相关。

#### 7. 优点
*   **物理一致性**：首次形式化了 FAN 概念，弥补了语言模型训练目标与物理操作容差之间的鸿沟。
*   **通用性强**：适用于 SFT 和 RFT，且兼容不同的 VLA 骨干网络（如 OpenVLA 和 OFT 变体）。
*   **高效简洁**：不增加推理开销，仅通过训练时的分布塑造即可获得显著的泛化增益。

#### 8. 不足与局限
*   **超参数依赖**：正则化系数 $\alpha$ 和目标方差 $\sigma$ 对性能有影响，过大的 $\alpha$ 可能导致训练崩溃，需要针对不同任务进行调优。
*   **高斯分布假设**：虽然高斯先验简单有效，但对于某些具有多峰特性的复杂动作空间，单一高斯分布可能不是最优的 FAN 建模方式（尽管文中尝试了核平滑处理，但仍有探索空间）。
*   **应用限制**：主要针对离散化动作 Token 的 VLA 模型，对于直接输出连续动作的模型（如基于 Flow Matching 的 $\pi_0$）的适用性尚未讨论。

（完）
