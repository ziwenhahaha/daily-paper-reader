---
title: "ECSEL: Explainable Classification via Signomial Equation Learning"
title_zh: ECSEL：通过正项式方程学习实现的可解释分类
authors: "Adia Lumadjeng, Ilker Birbil, Erman Acar"
date: 2026-01-29
pdf: "https://arxiv.org/pdf/2601.21789v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 直接提出了一种基于符号回归基准测试的分类符号方程学习方法。
tldr: ECSEL是一种通过学习正项式方程实现的可解释分类方法，旨在直接构建结构化的闭式表达式作为分类器。该方法在符号回归基准测试中以更低的计算开销实现了优于现有技术的目标方程恢复率，同时在分类任务中保持了与主流机器学习模型相当的准确率。ECSEL不仅提供了全局和局部的特征解释，还能在电子商务和欺诈检测等实际应用中揭示数据偏差并支持反事实推理。
motivation: 现有的符号回归和分类方法在处理具有紧凑正项式结构的数学表达式时，往往难以兼顾计算效率与模型的可解释性。
method: 提出ECSEL方法，通过学习正项式方程直接构造闭式表达式，使其同时充当分类器和可解释的结构化说明。
result: 实验表明ECSEL在符号回归任务中恢复方程的效率和准确度更高，且在分类基准测试中达到了极具竞争力的准确率。
conclusion: ECSEL证明了正项式方程在提供高性能分类的同时，能通过决策边界分析和特征归因提供具有行动价值的深刻见解。
---

## 摘要
我们引入了 ECSEL，这是一种可解释的分类方法，它学习正项式方程形式的形式化表达式。其动机在于观察到许多符号回归基准测试都具有紧凑的正项式结构。ECSEL 直接构建一个结构化的闭式表达式，既作为分类器又作为解释。在标准符号回归基准测试中，与现有的最先进方法相比，我们的方法能够恢复更大比例的目标方程，同时所需的计算量显著减少。利用这种效率，ECSEL 在不牺牲可解释性的情况下，实现了与成熟机器学习模型相当的分类准确率。此外，我们证明了 ECSEL 在全局特征行为、决策边界分析和局部特征归因方面满足一些理想的性质。在基准数据集和两个真实案例研究（即电子商务和欺诈检测）上的实验表明，学习到的方程能够揭示数据集偏差，支持反事实推理，并产生具有行动价值的见解。

## Abstract
We introduce ECSEL, an explainable classification method that learns formal expressions in the form of signomial equations, motivated by the observation that many symbolic regression benchmarks admit compact signomial structure. ECSEL directly constructs a structural, closed-form expression that serves as both a classifier and an explanation. On standard symbolic regression benchmarks, our method recovers a larger fraction of target equations than competing state-of-the-art approaches while requiring substantially less computation. Leveraging this efficiency, ECSEL achieves classification accuracy competitive with established machine learning models without sacrificing interpretability. Further, we show that ECSEL satisfies some desirable properties regarding global feature behavior, decision-boundary analysis, and local feature attributions. Experiments on benchmark datasets and two real-world case studies i.e., e-commerce and fraud detection, demonstrate that the learned equations expose dataset biases, support counterfactual reasoning, and yield actionable insights.

---

## 论文详细总结（自动生成）

以下是对论文《ECSEL: Explainable Classification via Signomial Equation Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代机器学习（如深度神经网络）在追求高预测性能时往往牺牲了可解释性。虽然符号回归（Symbolic Regression, SR）能生成人类可读的数学公式，但传统的 SR 方法（如遗传算法）计算成本高，且难以扩展到高维分类任务。
*   **研究动机**：作者观察到，许多科学发现和符号回归基准测试（如 AI Feynman）中的方程具有紧凑的**正项式（Signomial）**结构（即幂律项的有限和）。
*   **整体含义**：论文提出了 **ECSEL** 方法，旨在通过学习正项式方程，直接构建一个既能作为高性能分类器，又能作为结构化闭式解释的模型，填补了符号回归的可解释性与现代分类任务性能需求之间的空白。

### 2. 方法论：核心思想与技术细节
*   **核心思想**：将分类任务建模为学习类特定的正项式得分函数。正项式定义为：$z(x) = \sum_{k=1}^K \alpha_k \prod_{j=1}^m x_j^{\beta_{k,j}}$，其中 $\alpha$ 是系数，$\beta$ 是实值指数。
*   **关键技术细节**：
    *   **通用逼近性**：证明了正项式函数在正实数域的紧集上具有通用逼近性质，理论上可逼近任何连续函数。
    *   **优化策略**：采用基于梯度的优化（Adam 和 L-BFGS-B），而非传统的随机搜索。
    *   **稀疏性正则化**：在损失函数中对指数 $\beta$ 施加 **$L_1$ 正则化**，以诱导特征选择，生成更简洁、更具解释性的方程。
    *   **概率转换**：通过 Softmax 层将正项式得分转换为类概率。
    *   **解释性属性**：推导了三种层次的分析属性：
        1.  **全局行为**：通过闭式弹性（Elasticity）分析特征的比例敏感度。
        2.  **决策边界**：通过边际敏感度分析特征如何驱动类间竞争。
        3.  **局部归因**：在对数空间实现精确（$K=1$）或近似（$K>1$）的特征贡献分解。

### 3. 实验设计
*   **符号回归基准测试**：
    *   **数据集**：AI Feynman（物理方程）、Livermore、Jin、Korns 等共 58 个目标方程。
    *   **对比方法**：DGSR（深度生成符号回归，SOTA）、NGGP、NeSymRes、gplearn、PySR。
*   **分类基准测试**：
    *   **数据集**：11 个标准数据集（如 Iris, Seeds, Hearts, ILPD, Compas, Mammography, Loan 等），涵盖医疗、金融、司法等领域。
    *   **对比方法**：逻辑回归（LR）、随机森林（RF）、XGBoost、支持向量机（SVM）、多层感知机（MLP）。
*   **真实案例研究**：
    *   **电子商务**：在线购物意向预测（12,330 条记录）。
    *   **欺诈检测**：PaySim 大规模合成金融交易日志（约 630 万条记录）。

### 4. 资源与算力
*   **计算效率**：论文强调了 ECSEL 的高效性。在符号回归任务中，ECSEL 平均每条方程耗时 **86.4 秒**，远低于 DGSR（612.9 秒）和 NGGP（468.7 秒）。
*   **训练时长**：在处理包含 630 万条数据的 PaySim 数据集时，ECSEL 仅需约 **16 分钟** 即可完成训练。
*   **硬件说明**：文中未明确提及具体的 GPU 型号或数量，但由于其采用梯度优化且方程结构简单，推测其对硬件要求较低，可在标准工作站上运行。

### 5. 实验数量与充分性
*   **实验规模**：
    *   符号回归部分测试了 58 个方程，每个方程运行 5 个随机种子。
    *   分类部分测试了 11 个不同规模的数据集（样本量从 150 到 40 万不等）。
    *   使用了 5 折分层交叉验证，并利用 Optuna 进行 30 次超参数搜索。
*   **充分性与公平性**：实验设计较为全面，涵盖了从简单公式恢复到大规模复杂分类的场景。对比了多种类型的基线模型（线性、树模型、核方法、神经网络），并报告了准确率、F1 分数和少数类召回率，评估较为客观。

### 6. 主要结论与发现
*   **方程恢复**：ECSEL 在符号回归基准上的目标方程恢复率达到 **95.86%**，显著优于 DGSR（59.10%），且速度快数倍。
*   **分类性能**：ECSEL 的分类准确率与 XGBoost 等黑盒模型非常接近（通常在 1% 差距内），在某些数据集（如 ILPD）上甚至显著优于基准模型。
*   **业务洞察**：在欺诈检测案例中，ECSEL 发现欺诈者不成比例地针对高余额账户（呈现超线性关系 $\beta=1.42$），这种非线性模式在传统线性模型中难以捕捉，而在黑盒模型中难以解释。

### 7. 优点
*   **高效性**：利用梯度优化避开了符号回归中常见的组合爆炸问题。
*   **内在解释性**：输出的是简洁的数学公式，而非事后归因（Post-hoc），避免了不可靠的解释。
*   **数学严谨性**：提供了关于通用逼近和解释性属性（如弹性、反事实推理）的正式数学证明。
*   **稀疏性控制**：通过正则化自动筛选关键特征，生成的方程非常精简。

### 8. 不足与局限
*   **特征限制**：由于涉及实数幂运算，输入特征必须为正值（需通过仿射变换预处理）。
*   **结构先验**：需要预先指定项数 $K$，虽然这在分类中可作为超参数，但在未知结构的 SR 任务中可能受限。
*   **复杂多项式挑战**：在处理高阶单变量多项式（如 Nguyen 基准）时，可能会收敛到数值近似而非精确的整数指数。
*   **应用范围**：目前主要针对表格数据，尚未扩展到图像或序列数据。

（完）
