---
title: "ROIDS: Robust Outlier-Aware Informed Down-Sampling"
title_zh: ROIDS：鲁棒的离群值感知知情下采样
authors: "Alina Geiger, Martin Briesch, Dominik Sobania, Franz Rothlauf"
date: 2026-01-27
pdf: "https://arxiv.org/pdf/2601.19477v1"
tags: ["keyword:SR", "query:SR"]
score: 10.0
evidence: 符号回归采样技术创新
tldr: "针对符号回归中知情下采样（IDS）容易受离群点影响而导致过拟合的问题，本文提出了ROIDS（鲁棒离群点感知知情下采样）。该方法通过在采样过程中识别并排除潜在离群点，保留了IDS的性能优势并增强了鲁棒性。实验证明，ROIDS在合成及真实世界数据集中均优于IDS，在超过80%的真实问题上表现更佳，是处理含噪声数据的可靠选择。"
motivation: 传统的知情下采样（IDS）会系统性地倾向于选择离群点进入子集，导致符号回归模型过度拟合异常数据。
method: 提出ROIDS方法，通过在下采样过程中识别并排除潜在的离群点，从而在保留采样效率的同时提高模型的鲁棒性。
result: "ROIDS在超过80%的真实世界基准测试中优于IDS，并在所有对比的基准方法中取得了最佳的平均排名。"
conclusion: ROIDS是一种鲁棒且可靠的下采样方法，能够有效解决符号回归在处理含离群点数据集时的性能退化问题。
---

## 摘要
知情下采样 (IDS) 在与各种选择策略（尤其是锦标赛选择）结合时，已知能提高符号回归的性能。然而，最近的研究发现 IDS 的收益在所有问题上并不一致。我们的分析表明，对于包含离群值的问题，IDS 的性能较差。IDS 系统地倾向于在子集中包含离群值，这促使遗传规划 (GP) 寻找过拟合离群值的解。为了解决这个问题，我们引入了 ROIDS（鲁棒的离群值感知知情下采样），它在 IDS 的采样过程中排除了潜在的离群值。通过 ROIDS，可以在不产生离群值过拟合的情况下保留 IDS 的优势，并在广泛的基准问题上具有竞争力。这也反映在我们的实验中，ROIDS 在所有研究的基准问题上都表现出了预期的行为。ROIDS 在添加了离群值的合成问题以及广泛的复杂现实世界问题上始终优于 IDS，在超过 80% 的现实世界基准问题上超过了 IDS。此外，与所有研究的基准方法相比，ROIDS 在所有测试的基准问题中实现了最佳的平均排名。这种鲁棒的行为使 ROIDS 成为符号回归选择中一种可靠的下采样方法，特别是当数据集中可能包含离群值时。

## Abstract
Informed down-sampling (IDS) is known to improve performance in symbolic regression when combined with various selection strategies, especially tournament selection. However, recent work found that IDS's gains are not consistent across all problems. Our analysis reveals that IDS performance is worse for problems containing outliers. IDS systematically favors including outliers in subsets which pushes GP towards finding solutions that overfit to outliers. To address this, we introduce ROIDS (Robust Outlier-Aware Informed Down-Sampling), which excludes potential outliers from the sampling process of IDS. With ROIDS it is possible to keep the advantages of IDS without overfitting to outliers and to compete on a wide range of benchmark problems. This is also reflected in our experiments in which ROIDS shows the desired behavior on all studied benchmark problems. ROIDS consistently outperforms IDS on synthetic problems with added outliers as well as on a wide range of complex real-world problems, surpassing IDS on over 80% of the real-world benchmark problems. Moreover, compared to all studied baseline approaches, ROIDS achieves the best average rank across all tested benchmark problems. This robust behavior makes ROIDS a reliable down-sampling method for selection in symbolic regression, especially when outliers may be included in the data set.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **ROIDS**（Robust Outlier-Aware Informed Down-Sampling）的新型下采样方法，旨在解决符号回归（Symbolic Regression, SR）中遗传规划（GP）算法在处理含有离群值（Outliers）的数据集时的性能退化问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **背景**：在符号回归中，为了降低计算成本并提高泛化能力，通常使用下采样技术（如随机下采样 RDS）。**知情下采样（IDS）** 是一种更先进的方法，它通过选择对当前种群具有“信息量”的样本子集来加速收敛。
*   **核心问题**：IDS 的设计初衷是寻找能区分不同候选解的“边缘案例（Edge Cases）”。然而，作者发现 IDS 存在一个致命缺陷：它无法区分“有意义的边缘案例”和“无意义的离群值”。
*   **动机**：IDS 会系统性地频繁采样离群值，导致遗传规划模型过度拟合这些噪声数据，从而在含有离群值的现实世界问题中表现甚至不如随机采样。

### 2. 方法论：ROIDS 核心思想与流程
ROIDS 的核心思想是在执行 IDS 的距离计算之前，先动态识别并排除潜在的离群点。
*   **关键步骤**：
    1.  **种群采样**：每隔 $l$ 代，抽取一小部分父代个体。
    2.  **误差向量构建**：在全量训练集上评估这些个体，为每个训练样本生成一个误差向量（记录不同个体在该样本上的表现）。
    3.  **离群值识别**：计算每个样本的**平均误差**。ROIDS 假设离群值由于偏离规律，会导致种群普遍产生极高误差。
    4.  **排除机制**：根据预设的离群值敏感率 $\gamma$，剔除平均误差最高的 $\gamma$ 比例的样本。
    5.  **知情采样**：在剩余的“干净”样本中，利用最远优先遍历（Farthest First Traversal）算法，根据误差向量的距离选择最具代表性的子集。
*   **算法优势**：计算开销极低，仅在 IDS 基础上增加了一个简单的均值排序步骤。

### 3. 实验设计
*   **数据集**：
    *   **合成问题**：Nguyen-6、Friedman 1/2/3。作者特意设计了“均匀分布”、“非均匀分布”以及“添加 5% 离群值”的变体，以验证算法在极端情况下的鲁棒性。
    *   **真实世界问题**：Airfoil、Concrete、ENH、Housing、Redwine、Yacht（共 6 个经典基准数据集）。
*   **对比方法（Baselines）**：
    *   **RDS**：随机下采样。
    *   **IDS**：原始知情下采样。
    *   **LOF+IDS**：先用传统的局部离群因子（LOF）算法静态剔除离群值，再运行 IDS。
*   **评估指标**：测试集上的均方误差（MSE）和平均排名。

### 4. 资源与算力
*   **软件框架**：基于 Python 的 DEAP 进化计算库实现。
*   **算力说明**：论文**未明确说明**具体的硬件型号（如 GPU/CPU 核心数）或具体的训练时长。但文中提到 ROIDS 相比 IDS 几乎没有增加额外的计算负担，且下采样本身就是为了节省算力。

### 5. 实验数量与充分性
*   **实验规模**：每个问题独立运行 **100 次**，这在进化计算领域属于非常充分的样本量，足以保证统计显著性。
*   **消融与参数分析**：针对关键参数 $\gamma$（离群值敏感率）进行了专门的灵敏度分析（30 次重复实验），探讨了不同剔除比例对结果的影响。
*   **可视化分析**：利用 **UMAP** 降维技术将高维样本分布可视化，直观展示了 IDS 采样离群值而 ROIDS 避开离群值的行为，实验设计客观且说服力强。

### 6. 主要结论与发现
*   **鲁棒性显著**：在含有离群值的合成数据中，ROIDS 彻底解决了 IDS 性能崩溃的问题，表现远优于 IDS。
*   **真实世界表现优异**：在 80% 以上的真实世界基准测试中，ROIDS 优于 IDS。
*   **综合排名第一**：在所有测试方法中，ROIDS 获得了最佳的平均排名（1.7），而 RDS 为 2.5，IDS 为 3.0。
*   **动态剔除优于静态**：ROIDS 优于 LOF+IDS，说明在进化过程中根据种群反馈动态识别离群值比预处理阶段静态剔除更有效。

### 7. 优点
*   **简单高效**：算法逻辑清晰，易于在现有 GP 框架中实现，且不增加计算复杂度。
*   **解决了 IDS 的长期痛点**：通过区分“边缘案例”与“离群值”，使知情下采样技术真正具备了处理噪声数据的实用性。
*   **广泛的适用性**：实验覆盖了从简单合成函数到复杂高维现实数据的多种场景。

### 8. 不足与局限
*   **参数依赖**：虽然实验证明 $\gamma=0.05$ 在多数情况下效果良好，但对于离群值比例未知的黑盒数据集，如何自动确定最佳的 $\gamma$ 仍是一个挑战。
*   **领域局限**：目前仅在符号回归（回归问题）中进行了验证，尚未探讨其在分类问题或程序合成（Program Synthesis）中的表现。
*   **极端情况**：如果数据集本身极其稀疏，ROIDS 可能会误将真实的边缘案例识别为离群值并剔除。

（完）
