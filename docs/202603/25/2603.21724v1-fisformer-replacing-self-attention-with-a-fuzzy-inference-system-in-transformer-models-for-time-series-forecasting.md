---
title: "FISformer: Replacing Self-Attention with a Fuzzy Inference System in Transformer Models for Time Series Forecasting"
title_zh: FISformer：在时间序列预测 Transformer 模型中用模糊推理系统替换自注意力机制
authors: "Bulent Haznedar, Levent Karacan"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21724v1"
tags: ["query:urt-pf"]
score: 9.0
evidence: 在Transformer中引入模糊推理系统进行时间序列预测
tldr: 针对Transformer在时间序列预测中难以建模不确定性和非线性依赖的问题，本文提出FISformer。该模型创新性地将传统的点积自注意力机制替换为模糊推理系统（FIS）交互机制，通过可学习的隶属函数和规则推理来估计Token间的关系强度。FISformer结合了模糊逻辑的可解释性与Transformer的强大表征能力，在多个基准数据集上实现了更高的预测精度和更强的噪声鲁棒性。
selection_source: fresh_fetch
motivation: 传统Transformer的确定性点积注意力机制难以有效捕捉多变量时间序列中的不确定性和复杂的非线性依赖关系。
method: 提出FIS交互机制，利用可学习的隶属函数和模糊规则对Query-Key对进行推理，生成具有不确定性建模能力的交互权重。
result: 在多个基准数据集上的实验证明，FISformer在预测精度、抗噪能力和模型可解释性方面均显著优于现有的Transformer变体。
conclusion: 模糊推理系统可以作为传统注意力机制的有效替代方案，显著提升时间序列预测模型的性能、鲁棒性及可解释性。
---

## 摘要
Transformer 在时间序列预测领域取得了显著进展，但其对确定性点积注意力的依赖限制了其在多变量时间维度上建模不确定性和非线性依赖关系的能力。为了解决这一局限性，我们提出了 FISFormer，这是一种由模糊推理系统驱动的 Transformer，它用 FIS 交互机制取代了传统的注意力机制。在该框架中，每个查询-键（query-key）对在每个特征维度上都经过一个模糊推理过程，其中可学习的隶属度函数和基于规则的推理用于估计 token 间的关系强度。这些源自 FIS 的交互权重捕捉了不确定性，并提供了 token 之间可解释的连续映射。在 token 轴上应用 softmax 操作以归一化这些权重，随后通过逐元素乘法将其与相应的数值（value）特征结合，从而生成最终的上下文增强型 token 表示。这种设计将模糊逻辑的可解释性和不确定性建模能力与 Transformer 的表征能力相结合。在多个基准数据集上的广泛实验表明，与最先进的 Transformer 变体相比，FISFormer 具有更优的预测精度、噪声鲁棒性和可解释性，证明了模糊推理是传统注意力机制的一种有效替代方案。

## Abstract
Transformers have achieved remarkable progress in time series forecasting, yet their reliance on deterministic dot-product attention limits their capacity to model uncertainty and nonlinear dependencies across multivariate temporal dimensions. To address this limitation, we propose FISFormer, a Fuzzy Inference System-driven Transformer that replaces conventional attention with a FIS Interaction mechanism. In this framework, each query-key pair undergoes a fuzzy inference process for every feature dimension, where learnable membership functions and rule-based reasoning estimate token-wise relational strengths. These FIS-derived interaction weights capture uncertainty and provide interpretable, continuous mappings between tokens. A softmax operation is applied along the token axis to normalize these weights, which are then combined with the corresponding value features through element-wise multiplication to yield the final context-enhanced token representations. This design fuses the interpretability and uncertainty modeling of fuzzy logic with the representational power of Transformers. Extensive experiments on multiple benchmark datasets demonstrate that FISFormer achieves superior forecasting accuracy, noise robustness, and interpretability compared to state-of-the-art Transformer variants, establishing fuzzy inference as an effective alternative to conventional attention mechanisms.

---

## 论文详细总结（自动生成）

以下是对论文《FISformer: Replacing Self-Attention with a Fuzzy Inference System in Transformer Models for Time Series Forecasting》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统 Transformer 模型在处理多变量时间序列预测时，过度依赖**确定性的点积自注意力机制（Dot-product Attention）**。这种机制在面对现实世界数据中普遍存在的**不确定性**、**噪声**以及复杂的**非线性依赖**时，建模能力有限。
*   **研究动机**：
    *   点积注意力计算的是“硬”相似度，缺乏对数据模糊性的处理。
    *   标准自注意力的计算复杂度随序列长度呈平方级增长（$O(T^2)$）。
    *   缺乏可解释性，难以理解 Token 之间交互的具体逻辑。
*   **目标**：通过引入**模糊推理系统（FIS）**，结合模糊逻辑的鲁棒性、可解释性与 Transformer 的表征能力，构建一种更适应动态、不确定环境的预测模型。

### 2. 方法论：FISformer 架构
FISformer 的核心创新在于用 **FIS 交互层（FIS-based Token Interaction）** 彻底替换了传统的自注意力层。其处理流程分为四个关键阶段：
*   **模糊化（Fuzzification）**：
    *   将输入的 Query (Q) 和 Key (K) 映射到模糊空间。
    *   使用可学习的**高斯隶属函数（Gaussian Membership Functions）**，每个特征维度包含多个模糊集合（如：小、中、大），计算 Token 的隶属度。
*   **规则生成（Rule Generation）**：
    *   基于 **Sugeno 型模糊推理**。
    *   通过乘积算子（Product T-norm）组合 Q 和 K 的隶属度，计算每条模糊规则的激活强度（Firing Strength）。
*   **去模糊化与聚合（Defuzzification & Aggregation）**：
    *   采用一阶 Sugeno 模型，规则的输出（后件）是输入 Q 和 K 的线性组合。
    *   对所有规则的输出进行加权平均，生成**模糊交互图（Fuzzy Interaction Map）**。
*   **FIS 交互输出**：
    *   在 Token 轴上对模糊交互得分进行 Softmax 归一化，得到最终的交互权重。
    *   将权重与 Value (V) 矩阵进行逐元素相乘，生成增强的上下文表示。
*   **复杂度优化**：该机制将复杂度降低为 $O(T \cdot d \cdot R)$（$R$ 为规则数），在 $R \ll T$ 时，其效率远高于传统注意力的 $O(T^2)$。

### 3. 实验设计
*   **数据集**：涵盖 7 个主流基准数据集，包括 **ETT** (h1, h2, m1, m2)、**Exchange**、**Weather**、**ECL** (电力消耗)、**Traffic**、**Solar-Energy** 和 **PEMS** (交通流)。
*   **Benchmark 设置**：
    *   固定回看窗口（Lookback Window）为 96。
    *   预测长度（Horizon）设定为 {96, 192, 336, 720}（PEMS 为 {12, 24, 48, 96}）。
*   **对比方法**：
    *   经典/先进 Transformer 变体：**Informer**、**Autoformer**、**FEDformer**、**Crossformer**、**iTransformer**。
    *   模糊增强型 Transformer：**FANTF** (2025)。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA GeForce RTX 2080 Ti (12GB)** GPU。
*   **软件框架**：PyTorch。
*   **训练细节**：使用 Adam 优化器，初始学习率在 $\{10^{-4}, 10^{-3}\}$ 间搜索，Batch Size 为 32，训练 10 个 Epoch。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的对比实验，涵盖了 11 个子数据集和 4 种预测长度，总计产生了数百组性能指标（MSE/MAE）。
*   **消融实验**：
    *   验证了 FIS 交互模块相对于标准自注意力的提升。
    *   测试了不同隶属函数（高斯、三角、梯形）的影响。
    *   将 FIS 模块集成到不同架构（如 Informer）中验证通用性。
*   **充分性评价**：实验设计非常充分且客观。采用了标准的 TimesNet 预处理协议和严格的时间顺序划分，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：FISformer 在 7 个数据集中的 6 个上达到了最优性能（SOTA），尤其在低维数据集（如 ETT）上表现显著优于 iTransformer。
*   **鲁棒性强**：相比于 FANTF 等仅做表面模糊化的模型，FISformer 通过完整的规则推理过程，在噪声环境下表现出更强的泛化能力。
*   **架构兼容性**：FIS 交互机制不仅适用于变量独立 Token 化（iTransformer 风格），也适用于时间步 Token 化（Informer 风格）。
*   **效率优势**：在长序列预测中，FISformer 保持了线性计算复杂度，且推理过程更具解释性。

### 7. 优点与亮点
*   **理论融合**：成功将具有数十年历史的模糊逻辑（FIS）深度嵌入到现代 Transformer 核心中，而非简单的后处理。
*   **不确定性建模**：通过模糊隶属度捕捉时间序列的波动和模糊性，弥补了确定性计算的不足。
*   **可解释性**：模糊规则的激活状态为理解模型“为什么关注某个 Token”提供了逻辑依据。
*   **参数效率**：通过可学习的隶属函数参数，模型能自适应不同量纲和动态特性的数据。

### 8. 不足与局限
*   **超参数敏感性**：虽然论文做了学习率分析，但模糊规则的数量（$R$）和隶属函数的初始分布可能对结果有较大影响，文中未深入探讨 $R$ 的最优选择策略。
*   **极端场景验证**：实验主要集中在公开平稳数据集，对于极端异常值（Outliers）或突发性结构变化的极端非平稳序列，FIS 的表现尚需更多验证。
*   **计算开销**：虽然渐进复杂度是线性的，但在实际工程实现中，复杂的模糊推理逻辑（如多重 Gaussian 计算）可能在小规模序列上比高度优化的矩阵乘法（点积注意力）稍慢。

（完）
