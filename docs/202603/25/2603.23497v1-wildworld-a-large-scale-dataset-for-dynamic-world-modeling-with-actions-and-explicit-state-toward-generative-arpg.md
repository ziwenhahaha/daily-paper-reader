---
title: "WildWorld: A Large-Scale Dataset for Dynamic World Modeling with Actions and Explicit State toward Generative ARPG"
title_zh: WildWorld：一个面向生成式 ARPG 的具有动作和显式状态的大规模动态世界建模数据集
authors: "Zhen Li, Zian Meng, Shuwei Shi, Wenshuo Peng, Yuwei Wu, Bo Zheng, Chuanhao Li, Kaipeng Zhang"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23497v1"
tags: ["query:profile-2"]
score: 8.0
evidence: 世界建模中由动作驱动的潜在状态动力学
tldr: 本研究针对现有视频世界模型在处理复杂动作和维持长时一致性方面的不足，提出了WildWorld数据集。该数据集源自AAA级动作角色扮演游戏《怪物猎人：荒野》，包含超过1.08亿帧图像及450多种语义丰富的动作（如攻击、技能等），并提供骨骼、状态、相机位姿等显式标注。通过配套的WildBench评估，研究揭示了当前模型在动作遵循和状态对齐方面的挑战，为生成式ARPG和状态感知视频生成提供了重要基石。
selection_source: fresh_fetch
motivation: 现有世界模型数据集缺乏多样化的语义动作空间，且动作常与像素变化纠缠而非由底层状态驱动，导致长时演化不一致。
method: 从高质量ARPG游戏中自动采集大规模数据，构建包含1.08亿帧、450余种动作及多维度显式状态标注的WildWorld数据集，并提出WildBench评估基准。
result: 实验表明，现有模型在处理语义丰富的复杂动作和保持长时状态一致性方面仍面临巨大挑战。
conclusion: WildWorld填补了具备显式状态和复杂动作的大规模世界模型数据集空白，强调了状态感知在视频生成中的重要性。
---

## 摘要
动力系统理论和强化学习将世界演化视为由动作驱动的隐状态动力学，而视觉观测则提供关于状态的部分信息。最近的视频世界模型尝试从数据中学习这种以动作为条件的动力学。然而，现有数据集很少能满足这一需求：它们通常缺乏多样化且具有语义意义的动作空间，且动作直接与视觉观测挂钩，而非通过底层状态进行中介。结果导致动作往往与像素级变化纠缠在一起，使得模型难以学习结构化的世界动力学，并在长时程内保持一致的演化。在本文中，我们提出了 WildWorld，这是一个具有显式状态标注的大规模动作条件世界建模数据集，自动采集自一款写实风格的 AAA 级动作角色扮演游戏（《怪物猎人：荒野》）。WildWorld 包含超过 1.08 亿帧图像，涵盖 450 多种动作（包括移动、攻击和技能释放），并配有同步的逐帧标注，包括角色骨架、世界状态、相机位姿和深度图。我们进一步衍生出 WildBench，通过动作遵循（Action Following）和状态对齐（State Alignment）来评估模型。广泛的实验揭示了在建模语义丰富的动作和保持长时程状态一致性方面存在的持续挑战，强调了对状态感知视频生成的的需求。项目主页为 https://shandaai.github.io/wildworld-project/。

## Abstract
Dynamical systems theory and reinforcement learning view world evolution as latent-state dynamics driven by actions, with visual observations providing partial information about the state. Recent video world models attempt to learn this action-conditioned dynamics from data. However, existing datasets rarely match the requirement: they typically lack diverse and semantically meaningful action spaces, and actions are directly tied to visual observations rather than mediated by underlying states. As a result, actions are often entangled with pixel-level changes, making it difficult for models to learn structured world dynamics and maintain consistent evolution over long horizons. In this paper, we propose WildWorld, a large-scale action-conditioned world modeling dataset with explicit state annotations, automatically collected from a photorealistic AAA action role-playing game (Monster Hunter: Wilds). WildWorld contains over 108 million frames and features more than 450 actions, including movement, attacks, and skill casting, together with synchronized per-frame annotations of character skeletons, world states, camera poses, and depth maps. We further derive WildBench to evaluate models through Action Following and State Alignment. Extensive experiments reveal persistent challenges in modeling semantically rich actions and maintaining long-horizon state consistency, highlighting the need for state-aware video generation. The project page is https://shandaai.github.io/wildworld-project/.

---

## 论文详细总结（自动生成）

### WildWorld：面向生成式 ARPG 的大规模动态世界建模数据集总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的视频世界模型在学习“动作驱动的动力学”时面临挑战。主要原因在于现有数据集的动作空间简单（仅限于基本移动或相机旋转），且动作往往直接与像素变化挂钩，缺乏底层“状态”（如弹药量、生命值、骨骼姿态）的中介。
*   **研究背景**：动力系统理论将世界演化视为由动作驱动的隐状态转移。若无法解耦动作、状态与观测，模型在长时程预测中容易产生不一致性。
*   **整体含义**：本文通过引入 AAA 级游戏《怪物猎人：荒野》的大规模数据，旨在提供一个包含显式状态标注（Explicit State）的环境，推动从单纯的“像素预测”向“状态感知”的世界建模转变。

#### 2. 论文提出的方法论
*   **核心思想**：利用游戏引擎的解耦特性，同步记录动作（Action）、状态（State）和观测（Observation），构建一个结构化的世界建模数据集。
*   **关键技术细节**：
    *   **数据采集平台**：开发专用工具链，从游戏引擎中提取玩家动作、角色/怪物骨骼、位置、属性（HP、耐力）等；同时从渲染管线提取无 HUD 的 RGB 帧、深度图和相机参数。
    *   **自动化游戏流水线**：利用游戏内置的“行为树 AI”驱动 NPC 和玩家角色进行战斗，通过随机任务选择确保场景、怪物和团队组合的多样性。
    *   **分层标注系统**：利用多模态大模型（Qwen3-VL 和 Gemini 3 Flash）结合动作真值（Ground Truth），生成从动作序列级到样本级的精细化文本描述。
    *   **StateCtrl 模型**：提出一种状态感知模型，将离散状态（类型）和连续状态（坐标、数值）映射到统一嵌入空间，通过 Transformer 建模实体间关系，并引入状态预测器（State Predictor）实现自回归生成。

#### 3. 实验设计
*   **数据集规模**：WildWorld 包含 **1.08 亿帧**图像，涵盖 450 多种动作、29 种怪物、4 种武器及 5 种大型开放世界区域。
*   **评估基准（WildBench）**：
    *   **视频质量**：使用 VBench 指标（平滑度、动态度、美学、画质）。
    *   **相机控制**：评估 ATE（绝对轨迹误差）和 RPE（相对位姿误差）。
    *   **动作遵循（Action Following）**：利用 Gemini 评估生成视频与输入动作指令的一致性。
    *   **状态对齐（State Alignment）**：通过追踪生成视频中的关键点并与真值骨骼对比，计算坐标准确率。
*   **对比方法**：以 Wan2.2 为基础模型，对比了 Baseline（仅图生视频）、CamCtrl（相机控制）、SkelCtrl（骨骼控制）以及本文提出的 StateCtrl（状态控制）及其自回归版本（StateCtrl-AR）。

#### 4. 资源与算力
*   **训练配置**：模型在 544 × 960 分辨率下训练，每样本 81 帧，帧率 16 FPS。
*   **超参数**：Batch Size 为 8，学习率为 $1 \times 10^{-5}$，使用 Adam 优化器训练了 **250,000 次迭代**。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号和数量，但考虑到 1.08 亿帧的数据规模和 25 万次迭代的训练量，推测使用了大规模 GPU 集群（如 H100 或 A100 集群）。

#### 5. 实验数量与充分性
*   **实验规模**：在 WildBench 上手动筛选了 200 个具有代表性的复杂样本（涵盖协作、单挑、技能、死亡等事件）。
*   **充分性评价**：
    *   **指标验证**：专门进行了人类偏好实验，证明了“动作遵循”指标与人类判断有 85% 的一致性，验证了 Benchmark 的可靠性。
    *   **消融/变体对比**：对比了相机、骨骼、状态等多种控制信号，实验设计较为全面。
    *   **局限性**：虽然数据量巨大，但 Benchmark 的测试集（200 个样本）相对较小，可能存在一定的场景偏差。

#### 6. 主要结论与发现
*   **状态感知的重要性**：引入显式状态（StateCtrl）能显著提升动作遵循和状态对齐性能，优于单纯的相机或骨骼控制。
*   **现有指标饱和**：传统的 VBench 指标在高质量游戏数据上容易饱和（均超过 95%），无法区分模型在复杂交互上的差异，凸显了 WildBench 的必要性。
*   **长时一致性挑战**：即使有状态引导，自回归模型（StateCtrl-AR）在长序列生成中仍存在误差累积，导致动作遵循能力下降。

#### 7. 优点
*   **数据质量极高**：采用 AAA 级游戏画面，视觉保真度远超现有的 Minecraft 或简单模拟器数据集。
*   **标注维度极广**：提供了极其罕见的逐帧骨骼、深度、相机位姿及底层游戏数值，是目前最完整的交互式世界模型数据集之一。
*   **评估维度创新**：提出的“状态对齐”指标为衡量世界模型的物理/逻辑一致性提供了量化手段。

#### 8. 不足与局限
*   **领域局限性**：数据完全来自《怪物猎人：荒野》，虽然场景多样，但仍属于特定游戏风格，模型向现实世界或其他类型游戏的泛化能力有待验证。
*   **黑盒依赖**：数据采集依赖于特定的游戏引擎钩子（Hooks），其他研究者若想扩展该数据集，技术门槛较高。
*   **自回归误差**：实验显示自回归状态预测在处理极其复杂的语义动作时仍不够稳定。

（完）
