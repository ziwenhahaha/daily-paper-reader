---
title: Leveraging Large Language Models for Generalizing Peephole Optimizations
title_zh: 利用大语言模型泛化窥孔优化
authors: "Chunhao Liao, Hongxu Xu, Xintong Zhou, Zhenyang Xu, Chengnian Sun"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18477v1"
tags: ["query:llm-ec"]
score: 8.0
evidence: 利用LLM自动设计通用的窥孔优化规则
tldr: 窥孔优化是现代编译器优化的核心，但将具体优化实例泛化为通用规则通常需要大量人工和领域知识。本文提出LPG框架，利用大语言模型（LLM）的语义抽象和推理能力，结合形式化验证确保规则的正确性与收益。LPG通过闭环工作流实现符号常量、结构及约束的自动泛化，在处理LLVM真实优化问题上表现优异，显著超越了现有的程序合成工具Hydra，为自动化编译器优化提供了高效的新途径。
selection_source: fresh_fetch
motivation: 现有的窥孔优化泛化方法受限于搜索空间爆炸、泛化能力不足以及对多样化指令域支持有限等问题。
method: 提出LPG框架，利用LLM进行多维度的规则泛化，并结合语法校验、语义验证和收益检查构建闭环反馈机制。
result: 在102个LLVM真实优化案例中，LPG成功泛化了90个；在整数子集上，LPG泛化了74个规则，远超Hydra的35个。
conclusion: 结合LLM的探索性推理能力与形式化分析的严谨性，可以有效实现通用且可靠的编译器窥孔优化规则自动生成。
---

## 摘要
窥孔优化是现代优化编译器的核心组件。它将特定的指令重写为语义等价但更高效的形式。在实践中，创建新的窥孔优化通常从具体的优化实例开始，并需要将其提升为更通用的重写规则，以匹配更广泛的指令模式。这一泛化步骤对优化效果至关重要，但也非常困难：生成既正确又足够通用的规则通常需要大量的体力劳动和领域专业知识。现有方法（如 Hydra）尝试通过程序合成来自动化这一任务，但它们的泛化能力通常受到搜索空间爆炸、泛化不足以及对多样化指令域支持有限的限制。我们提出了 LPG（大语言模型辅助的窥孔优化泛化），这是一个利用大语言模型（LLM）来泛化窥孔优化的框架。LPG 的设计动机源于以下观察：LLM 擅长语义抽象和探索性推理，而形式化分析对于确保生成的规则是可靠且有利可图的则是必要的。基于这一观察，LPG 采用了一种闭环工作流，将 LLM 驱动的符号常量泛化、结构泛化、约束松弛以及位宽/精度泛化与来自语法验证、语义验证和收益检查的反馈相结合。我们在来自 LLVM 生态系统的真实窥孔优化问题上评估了 LPG。总体而言，LPG 成功泛化了 102 个优化中的 90 个。在与 Hydra 直接可比的整数子集上，LPG 泛化了 81 个优化中的 74 个，而 Hydra 仅泛化了 35 个。

## Abstract
Peephole optimizations are a core component of modern optimizing compilers. It rewrites specific instruction into semantically equivalent but more efficient forms. In practice, creating a new peephole optimization often starts from a concrete optimization instance and requires lifting it into a more general rewrite rule that matches a wider range of instruction patterns. This generalization step is critical to optimization effectiveness, but it is also difficult: producing rules that are both correct and sufficiently general typically demands substantial manual effort and domain expertise. Existing approaches such as Hydra attempt to automate this task with program synthesis, but their generalization capability is often limited by search-space explosion, under-generalization, and restricted support for diverse instruction domains.   We present LPG, large language model aided peephole optimization generalization, a framework that uses large language models (LLMs) to generalize peephole optimizations. The design of LPG is motivated by the observation that LLMs are effective at semantic abstraction and exploratory reasoning, while formal analyses are necessary to ensure that generated rules are sound and profitable. Based on this observation, LPG adopts a closed-loop workflow that integrates LLM-driven symbolic constant generalization, structural generalization, constraint relaxation, and bitwidth/precision generalization with feedback from syntactic validation, semantic verification, and profitability checking.   We evaluate LPG on real-world peephole optimization issues drawn from the LLVM ecosystem. Overall, LPG successfully generalizes 90 out of 102 optimizations. On the integer-focused subset that is directly comparable to Hydra, LPG generalizes 74 out of 81 optimizations, whereas Hydra generalizes 35.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **LPG**（Large Language Model aided Peephole optimization Generalization）的框架，旨在利用大语言模型（LLM）自动将具体的编译器优化实例泛化为通用的窥孔优化（Peephole Optimization）规则。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：窥孔优化是编译器的核心，但开发新规则通常从一个具体的代码转换实例开始，需要人工将其泛化为通用的重写规则。这一过程极其困难且容易出错，甚至导致许多有效的优化因泛化成本过高而被放弃。
*   **研究动机**：
    *   **人工泛化难**：需要深厚的领域知识来处理复杂的位运算、浮点语义和内存约束。
    *   **现有工具局限**：如 Hydra 等基于程序合成的工具面临搜索空间爆炸、泛化程度不足（Under-generalization）以及仅支持整数域（不支持浮点和内存指令）的问题。
    *   **LLM 的潜力**：LLM 擅长语义抽象和探索性推理，能够处理合成工具难以覆盖的复杂模式。

### 2. 方法论
LPG 采用**“LLM 推理 + 形式化验证”**的闭环工作流，核心思想是利用 LLM 生成候选规则，利用形式化工具确保正确性和收益。
*   **四个泛化阶段**：
    1.  **符号常量泛化**：将具体数值（如 63, 8）替换为符号变量（$C_1, C_2$），并由 LLM 推理出必要的先验条件（如 $PowerOfTwo(C_1+1)$）。
    2.  **结构泛化**：将固定的子表达式抽象为带约束的符号值，消除对特定语法树的依赖。
    3.  **约束松弛**：通过 LLM 尝试移除冗余预设条件、削弱约束（如 $C>0$ 变为 $C \ge 0$）或移除指令标志（如 `nsw`），以扩大适用范围。
    4.  **位宽/精度泛化**：将规则扩展到不同的整数位宽或浮点精度。
*   **验证机制**：
    *   **语义验证**：使用 **Alive2** 进行形式化精化检查（Refinement Check），确保转换前后语义等价。
    *   **收益检查**：使用 **llvm-mca** 模拟执行，确保优化后的指令序列在微架构层面（如 uOps 计数）更高效。
    *   **反馈循环**：验证失败的信息（如反例、语法错误）会反馈给 LLM 进行迭代修复。

### 3. 实验设计
*   **数据集**：
    *   **HydraDataset**：包含 40 个来自 Hydra 论文的整数优化案例。
    *   **LPGDataset**：包含 62 个从 LLVM GitHub Issues 中新挖掘的案例（报告时间晚于模型知识截止日期，以防数据泄露），涵盖整数、浮点和内存指令。
*   **对比方法**：最先进的自动化工具 **Hydra**。
*   **评估指标**：泛化成功率、规则的通用程度（Generality）。

### 4. 资源与算力
*   **硬件环境**：Ubuntu 22.04 服务器，Intel Xeon Gold 5217 CPU (32核)，376 GB RAM。
*   **使用模型**：Gemini3-pro-preview（默认），以及 Qwen3 系列（30B 和 235B 的 Instruct/Thinking 版本）。
*   **算力说明**：文中未明确提及具体的 GPU 型号或训练时长，因为该框架主要基于现成模型的推理（Inference）而非重新训练。

### 5. 实验数量与充分性
*   **实验规模**：共测试了 102 个真实世界的优化案例。
*   **充分性**：
    *   **多领域覆盖**：不仅对比了整数域，还扩展到了浮点和内存域，这是前人未做的。
    *   **消融实验**：详细分析了四个泛化阶段中每一个阶段的贡献度。
    *   **模型对比**：对比了不同规模和推理模式（Thinking vs. Instruct）的模型表现。
    *   **客观性**：通过 Alive2 形式化验证确保了结果的绝对正确性，避免了 LLM 幻觉导致的错误规则。

### 6. 主要结论与发现
*   **性能卓越**：LPG 在 102 个案例中成功泛化了 90 个。在整数子集上，LPG 成功 74 例，远超 Hydra 的 35 例。
*   **泛化质量更高**：在双方都成功的案例中，LPG 在 22 个案例中生成的规则比 Hydra 更通用。
*   **推理能力关键**：带有“Thinking”模式（思维链推理）的模型在处理复杂约束时显著优于普通指令微调模型。
*   **跨域能力**：LPG 证明了 LLM 可以有效处理复杂的浮点语义（如 IEEE-754 边界情况），这是传统合成工具的短板。

### 7. 优点
*   **突破领域限制**：首次将自动化泛化扩展到浮点和内存指令。
*   **闭环可靠性**：结合了 LLM 的灵活性和形式化验证的严谨性，生成的规则可直接用于工业级编译器。
*   **解决搜索爆炸**：利用 LLM 的语义理解直接定位潜在的泛化方向，避免了穷举搜索。

### 8. 不足与局限
*   **验证器瓶颈**：部分失败案例是因为 Alive2 等验证工具对某些复杂浮点 intrinsic（如 `frexp`）支持不足，而非 LPG 本身的问题。
*   **极端边界情况**：在处理极少数涉及 IEEE-754 极细微语义（如带符号零、NaN 敏感比较）时，LLM 仍可能产生错误，需依赖多次迭代。
*   **应用限制**：目前主要针对 LLVM IR，迁移到其他中间表示（IR）需要适配相应的验证工具。

（完）
