---
title: "GigaWorld-Policy: An Efficient Action-Centered World--Action Model"
title_zh: GigaWorld-Policy：一种高效的以动作为中心的世界-动作模型
authors: "Angen Ye, Boyuan Wang, Chaojun Ni, Guan Huang, Guosheng Zhao, Hao Li, Hengtao Li, Jie Li, Jindi Lv, Jingyu Liu, Min Cao, Peng Li, Qiuping Deng, Wenjun Mei, Xiaofeng Wang, Xinze Chen, Xinyu Zhou, Yang Wang, Yifan Chang, Yifan Li, Yukun Zhou, Yun Ye, Zhichao Liu, Zheng Zhu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17240v2"
tags: ["query:wam-vla"]
score: 10.0
evidence: 用于机器人策略学习的以动作为中心的世界动作模型 (WAM)
tldr: 本研究针对现有世界-动作模型（WAM）推理开销大及视觉与动作表征耦合的问题，提出了GigaWorld-Policy。该模型采用以动作为中心的架构，通过因果设计将动作预测与视频生成解耦，使推理时可选择性生成视频，从而显著提升效率。在大型机器人数据集上预训练后，该模型在保持物理一致性的同时，实现了极高的推理速度和任务成功率，为高效机器人策略学习提供了新范式。
selection_source: fresh_fetch
motivation: 现有世界-动作模型面临推理开销巨大以及动作预测精度受限于视频生成质量的瓶颈。
method: 设计了一种以动作为中心的因果架构，通过联合训练动作预测与视频生成来增强物理约束，并在推理时实现动作的独立高效解码。
result: "实验显示该模型推理速度比Motus快9倍，成功率提升7%，且在RoboTwin 2.0上的表现优于pi-0.5约95%。"
conclusion: GigaWorld-Policy通过解耦视觉动力学与动作预测，成功平衡了机器人策略的推理效率与物理一致性。
---

## 摘要
从预训练视频生成骨干网络初始化的世界-动作模型 (World-Action Models, WAM) 在机器人策略学习方面展现出了巨大的潜力。然而，现有方法面临两个阻碍性能和部署的关键瓶颈。首先，对未来视觉动力学和相应动作进行联合推理会产生巨大的推理开销。其次，联合建模往往会使视觉和运动表示纠缠在一起，导致运动预测的准确性严重依赖于未来视频预测的质量。为了解决这些问题，我们推出了 GigaWorld-Policy，这是一种以动作为中心的 WAM，它在学习 2D 像素-动作动力学的同时，实现了高效的动作解码，并支持可选的视频生成。具体而言，我们将策略训练表述为两个耦合的部分：模型在当前观测的条件下预测未来的动作序列，并同时在预测动作和相同观测的条件下生成未来视频。该策略受到动作预测和视频生成的共同监督，提供了更丰富的学习信号，并通过视觉动力学约束鼓励产生符合物理规律的动作。通过防止未来视频标记 (tokens) 影响动作标记的因果设计，显式的未来视频生成在推理时是可选的，从而在部署期间实现更快的动作预测。为了支持这一范式，我们策划了一个多样化、大规模的机器人数据集来预训练一个以动作为中心的视频生成模型，随后将其作为机器人策略学习的骨干网络。在真实机器人平台上的实验结果表明，GigaWorld-Policy 的运行速度比领先的 WAM 基准模型 Motus 快 9 倍，同时任务成功率提高了 7%。此外，与 pi-0.5 相比，GigaWorld-Policy 在 RoboTwin 2.0 上的性能提升了 95%。

## Abstract
World-Action Models (WAM) initialized from pre-trained video generation backbones have demonstrated remarkable potential for robot policy learning. However, existing approaches face two critical bottlenecks that hinder performance and deployment. First, jointly reasoning over future visual dynamics and corresponding actions incurs substantial inference overhead. Second, joint modeling often entangles visual and motion representations, making motion prediction accuracy heavily dependent on the quality of future video forecasts. To address these issues, we introduce GigaWorld-Policy, an action-centered WAM that learns 2D pixel-action dynamics while enabling efficient action decoding, with optional video generation. Specifically, we formulate policy training into two coupled components: the model predicts future action sequences conditioned on the current observation, and simultaneously generates future videos conditioned on the predicted actions and the same observation. The policy is supervised by both action prediction and video generation, providing richer learning signals and encouraging physically plausible actions through visual-dynamics constraints. With a causal design that prevents future-video tokens from influencing action tokens, explicit future-video generation is optional at inference time, allowing faster action prediction during deployment. To support this paradigm, we curate a diverse, large-scale robot dataset to pre-train an action-centered video generation model, which is then adapted as the backbone for robot policy learning. Experimental results on real-world robotic platforms show that GigaWorld-Policy runs 9x faster than the leading WAM baseline, Motus, while improving task success rates by 7%. Moreover, compared with pi-0.5, GigaWorld-Policy improves performance by 95% on RoboTwin 2.0.

---

## 论文详细总结（自动生成）

以下是对论文《GigaWorld-Policy: An Efficient Action-Centered World–Action Model》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究背景**：现有的机器人策略模型主要分为两类：**VLA（视觉-语言-动作）模型**通常面临监督信号稀疏的问题，难以学习复杂的物理规律；**WAM（世界-动作模型）**通过预测未来视频来增强物理先验，但推理时必须生成视频，导致延迟极高（数秒量级），且视频预测的微小误差会干扰动作解码。
*   **核心问题**：如何构建一个既能利用视频生成的密集监督信号（物理一致性），又能实现低延迟、高效率动作推理的机器人策略模型？

### 2. 方法论
*   **核心思想**：提出一种**以动作为中心（Action-Centered）**的世界-动作模型。在训练阶段，利用未来视频生成作为辅助任务来提供密集监督；在推理阶段，通过架构设计使得视频生成变为“可选”，从而实现直接、快速的动作解码。
*   **关键技术细节**：
    *   **因果掩码设计（Causal Masking）**：这是模型的核心创新。将观测（$T_o$）、状态（$T_s$）、动作（$T_a$）和未来视频（$T_f$）序列化。通过掩码确保动作 token 只能看到当前的观测和状态，而未来视频 token 可以看到动作。这种设计在逻辑上实现了“动作决定未来”，且在推理时可以切断视频分支独立运行。
    *   **统一架构**：基于 5B 参数的 Diffusion Transformer (DiT)，不使用混合专家（MoE）设计，而是让所有模态共享 Transformer 块的参数，增强动作与视觉特征的耦合。
    *   **三阶段课程训练**：
        1.  **通用视频预训练**：从大规模网页视频模型（Wan-2.2）初始化。
        2.  **具身数据预训练**：在约 10,000 小时的机器人和人类第一视角视频上训练，注入具身物理先验。
        3.  **目标任务微调**：在特定机器人的轨迹数据上进行指令对齐。
*   **算法流程**：采用 **Flow Matching（流匹配）** 目标函数。训练时联合优化动作流和视频流损失；推理时仅对动作流进行积分采样（ODE 求解），跳过耗时的视频 token 生成。

### 3. 实验设计
*   **数据集/场景**：
    *   **仿真环境**：使用 RoboTwin 2.0 平台，涵盖 50 多个复杂的操纵任务（如叠碗、开笔记本、转开关等），并包含大量的领域随机化（背景、光照、干扰物）。
    *   **真实世界**：在 AgileX PiPER 6-DoF 机械臂上测试了四个任务：清理桌面、扫码、叠碗、扫垃圾。
*   **Benchmark 与对比方法**：
    *   **VLA 基准**：$\pi_{0.5}$、GigaBrain-0、X-VLA。
    *   **WAM 基准**：Motus、Cosmos-Policy。

### 4. 资源与算力
*   **模型规模**：5B 参数的 Diffusion Transformer。
*   **训练算力**：预训练阶段消耗了 **6000 GPU 小时**（未明确具体型号，但推理测试在 NVIDIA **A100** 上完成）。
*   **训练参数**：全局 Batch Size 为 256，使用 AdamW 优化器，学习率采用余弦退火（$1 \times 10^{-4}$ 到 $1 \times 10^{-6}$）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   仿真实验覆盖了 50+ 任务，每个任务进行 100 次测试，数据量充足。
    *   实机实验针对 4 个任务，每个任务 20 次尝试，并采用了分级评分制（如抓取成功得 0.5，放置成功再得 0.5）。
    *   **消融实验**：非常详尽，分别验证了预训练阶段的作用、未来预测帧数的影响、因果掩码的必要性以及数据效率。
*   **客观性**：对比了当前最先进的判别式（VLA）和生成式（WAM）模型，实验设置考虑了领域随机化，具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **推理效率**：GigaWorld-Policy 的推理延迟仅为 **0.36s**，比同类 WAM 模型 Motus（3.23s）快了 **9 倍**。
*   **控制性能**：在实机任务中，成功率比 Motus 高出 **7%**，比 $\pi_{0.5}$ 高出 **14%**。在 RoboTwin 2.0 仿真中，性能比 $\pi_{0.5}$ 提升了 **95%**。
*   **数据效率**：该模型表现出极强的数据利用能力，仅需 **10%** 的训练数据即可达到传统 VLA 模型使用 100% 数据时的性能水平。
*   **物理先验**：实验证明，即使推理时不生成视频，训练时引入的视频预测任务也能显著提升动作的物理合理性和准确性。

### 7. 优点
*   **架构优雅**：通过简单的因果掩码解决了 WAM 长期以来的推理延迟痛点，实现了“训练时有视频监督，推理时无视频负担”。
*   **预训练范式**：成功展示了如何将通用的视频生成模型转化为强大的机器人策略初始化权重。
*   **实用性强**：在保持高成功率的同时达到了接近实时的控制频率，更符合工业部署需求。

### 8. 不足与局限
*   **推理延迟仍有优化空间**：虽然比其他 WAM 快，但 360ms 的延迟对于需要极高频反馈（如 >50Hz）的动态任务仍显不足。
*   **视频预测步长依赖经验**：未来帧预测的步长 $\Delta$ 需要手动调节，过密会增加计算负担，过稀则损失监督效果。
*   **泛化边界未完全探明**：虽然在 50+ 仿真任务中表现良好，但在极端未见场景（如剧烈变化的光照或全新的物体类别）下的鲁棒性仍需更多实测。

（完）
