---
title: Do World Action Models Generalize Better than VLAs? A Robustness Study
title_zh: 世界动作模型是否比 VLA 具有更好的泛化能力？一项鲁棒性研究
authors: "Zhanguang Zhang, Zhiyuan Li, Behnam Rahmati, Rui Heng Yang, Yintao Ma, Amir Rasouli, Sajjad Pakdamansavoji, Yangzheng Wu, Lingfeng Zhang, Tongtong Cao, Feng Wen, Xingyue Quan, Yingxue Zhang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22078v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 世界动作模型 (WAM) 与 VLA 的鲁棒性对比研究
tldr: 本研究对比了视觉-语言-动作模型（VLA）与新兴的世界动作模型（WAM）在机器人任务中的表现。VLA 依赖动作专家数据，而 WAM 利用大规模视频预训练的世界模型来预测未来状态并解码动作。通过在 LIBERO-Plus 和 RoboTwin 2.0-Plus 基准上进行视觉与语言扰动测试，研究发现 WAM 凭借视频中的时空先验展现出更强的泛化能力和鲁棒性。虽然部分 VLA 也能达到高性能，但往往需要更复杂的训练，这强调了视频动力学先验在提升机器人策略鲁棒性中的关键作用。
selection_source: fresh_fetch
motivation: 探究基于视频预训练的世界动作模型（WAMs）在面对未见场景和环境扰动时，是否比传统的视觉-语言-动作模型（VLAs）具有更好的泛化性和鲁棒性。
method: 在 LIBERO-Plus 和 RoboTwin 2.0-Plus 基准测试下，对比评估了多种 SOTA 的 VLA 策略与 WAM 模型在视觉和语言扰动下的成功率。
result: WAM 模型（如 LingBot-VA 和 Cosmos-Policy）在多个基准上表现出显著的鲁棒性，而 VLA 模型通常需要更大规模的数据集和多样化的学习目标才能达到同等水平。
conclusion: 视频预训练提供的显式动力学预测能力和时空先验是提升机器人模型泛化能力的关键，混合方法也证明了集成视频先验的重要性。
---

## 摘要
现实世界中的机器人动作规划具有挑战性，因为它不仅需要理解环境的当前状态，还需要预测环境将如何响应动作而演变。视觉-语言-动作（VLA）模型通过利用动作专家数据，将大规模视觉-语言模型重新用于机器人动作生成，在各种机器人任务中取得了显著成功。然而，它们的性能仍然受限于训练数据的范围，在未见场景中表现出有限的泛化能力，并且容易受到各种上下文扰动的影响。最近，世界模型作为 VLA 的替代方案被重新审视。这些被称为世界动作模型（WAMs）的模型建立在世界模型之上，而世界模型是在大规模视频语料库上训练以预测未来状态的。经过少量调整，它们的潜在表示可以被解码为机器人动作。有观点认为，它们显式的动态预测能力，结合从互联网规模视频预训练中获得的时空先验，使 WAMs 能够比 VLA 更有效地进行泛化。在本文中，我们对当前领先的 SOTA VLA 策略和最近发布的 WAMs 进行了对比研究。我们在各种视觉和语言扰动下，评估了它们在 LIBERO-Plus 和 RoboTwin 2.0-Plus 基准测试中的表现。我们的结果表明，WAMs 表现出强大的鲁棒性，其中 LingBot-VA 在 RoboTwin 2.0-Plus 上达到了 74.2% 的成功率，Cosmos-Policy 在 LIBERO-Plus 上达到了 82.2%。虽然像 $π_{0.5}$ 这样的 VLA 在某些任务上可以达到相当的鲁棒性，但它们通常需要使用多样化的机器人数据集和多种学习目标进行广泛训练。部分结合了基于视频动态学习的混合方法表现出中等水平的鲁棒性，这突显了视频先验集成方式的重要性。

## Abstract
Robot action planning in the real world is challenging as it requires not only understanding the current state of the environment but also predicting how it will evolve in response to actions. Vision-language-action (VLA), which repurpose large-scale vision-language models for robot action generation using action experts, have achieved notable success across a variety of robotic tasks. Nevertheless, their performance remains constrained by the scope of their training data, exhibiting limited generalization to unseen scenarios and vulnerability to diverse contextual perturbations. More recently, world models have been revisited as an alternative to VLAs. These models, referred to as world action models (WAMs), are built upon world models that are trained on large corpora of video data to predict future states. With minor adaptations, their latent representation can be decoded into robot actions. It has been suggested that their explicit dynamic prediction capacity, combined with spatiotemporal priors acquired from web-scale video pretraining, enables WAMs to generalize more effectively than VLAs. In this paper, we conduct a comparative study of prominent state-of-the-art VLA policies and recently released WAMs. We evaluate their performance on the LIBERO-Plus and RoboTwin 2.0-Plus benchmarks under various visual and language perturbations. Our results show that WAMs achieve strong robustness, with LingBot-VA reaching 74.2% success rate on RoboTwin 2.0-Plus and Cosmos-Policy achieving 82.2% on LIBERO-Plus. While VLAs such as $π_{0.5}$ can achieve comparable robustness on certain tasks, they typically require extensive training with diverse robotic datasets and varied learning objectives. Hybrid approaches that partially incorporate video-based dynamic learning exhibit intermediate robustness, highlighting the importance of how video priors are integrated.

---

## 论文详细总结（自动生成）

这篇论文对**世界动作模型（World Action Models, WAMs）**与**视觉-语言-动作模型（Vision-Language-Action, VLAs）**在机器人操控任务中的鲁棒性进行了深入的对比研究。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的机器人策略主要分为两类：一类是基于视觉语言模型（VLM）微调的 **VLA**，它们擅长语义理解但往往缺乏对物理世界动态的深刻理解，且在面对未见场景或环境扰动时表现脆弱；另一类是新兴的 **WAM**，它们利用在大规模视频数据上预训练的世界模型作为骨干，通过预测未来状态来辅助动作生成。
*   **核心问题**：世界模型显式预测未来状态的能力是否真的能让机器人策略比传统的 VLA 具有更好的泛化性和鲁棒性？
*   **研究动机**：验证 WAM 继承的视频时空先验（Spatiotemporal Priors）在应对视觉噪声、光照变化、布局干扰等复杂环境时的实际效用。

### 2. 方法论
*   **核心思想**：对比 WAM 和 VLA 在架构和训练目标上的差异。
    *   **VLA 范式**：通常将当前观测 $h_t$ 直接映射到动作 $a_t$，依赖于大规模机器人指令数据的模仿学习。
    *   **WAM 范式**：利用视频生成模型（如 Cosmos-Predict, Wan2.1）预测未来状态 $h_{t+1}$，并将其作为动作生成的条件。其核心公式可表示为联合预测 $p(h_{t+1}, a_t | h_t)$ 或分步预测（先预测状态，再通过逆动力学模型解码动作）。
*   **关键技术细节**：
    *   **骨干网络**：WAM 使用视频扩散模型（Diffusion）或流匹配（Flow Matching）模型；VLA 使用自回归 Token 预测模型。
    *   **时空先验**：WAM 通过在互联网规模视频上预训练，获得了对物体运动、物理接触和环境演变的先验知识。
    *   **模型实例**：研究涵盖了 **Cosmos-Policy**（联合去噪）、**LingBot-VA**（因果推理与自回归生成）、**GE-Act**（轻量级流匹配解码器）等 WAM，以及 **$\pi_{0.5}$**、**X-VLA** 等 VLA。

### 3. 实验设计
*   **数据集与基准**：
    *   **LIBERO-Plus**：针对单臂操控的鲁棒性基准，包含 40 个任务。
    *   **RoboTwin 2.0-Plus**（本文提出）：基于 RoboTwin 2.0 扩展的双手协作基准，包含 50 个协作任务。
*   **扰动维度（7大类）**：相机视角（Camera）、机器人初始状态（Robot）、语言指令（Language）、光照（Light）、背景（Background）、传感器噪声（Noise）、物体布局（Layout）。
*   **对比方法**：
    *   **VLA 类**：$\pi_0$ 系列、OpenVLA-OFT、X-VLA、UniVLA 等。
    *   **混合类**：VLA-JEPA（加入未来状态预测目标）、MOTUS（统一潜在动作世界模型）。
    *   **WAM 类**：Cosmos-Policy、GE-Act、LingBot-VA。

### 4. 资源与算力
*   **训练细节**：文中提到对 $\pi_{0.5}$ 进行了微调（JAX 实现，60k 梯度步，Batch Size 64）。
*   **算力限制说明**：论文明确指出 **DreamZero**（14B 参数）由于推理预热时间过长（超过 15 分钟）且重训成本极高（基于 Wan2.1-14B），未能在基准测试中进行大规模定量评估。
*   **推理速度**：论文详细对比了各模型的推理耗时（Wall Clock Time）。WAM 的推理速度普遍较慢，例如 LingBot-VA 在 RoboTwin 设置下每步需 5.2 秒，而 $\pi_{0.5}$ 仅需 63 毫秒。

### 5. 实验数量与充分性
*   **实验规模**：在两个基准上进行了数千次 Rollout 测试。每个任务配置运行 50 个 Episode，涵盖了 21 个子维度的扰动。
*   **充分性评价**：实验设计非常全面，不仅对比了最终成功率，还分析了不同扰动类型对模型的影响。通过对比单臂和双手任务，验证了结论的普适性。
*   **客观性**：研究者尽可能使用官方发布的 Checkpoint，并对无法直接使用的模型（如 $\pi_{0.5}$）按照标准流程进行了重训，确保了对比的公平性。

### 6. 主要结论与发现
*   **WAM 鲁棒性占优**：在视觉相关的扰动（噪声、光照、布局）下，WAM（如 LingBot-VA 和 Cosmos-Policy）表现出显著的鲁棒性，成功率远超普通 VLA。
*   **视频先验的作用**：WAM 的优势源于视频预训练提供的时空先验，使其在数据量较少的情况下也能理解复杂的物理动态。
*   **VLA 的上限**：像 $\pi_{0.5}$ 这样经过极其多样化数据（跨机器人、Web 数据）训练的顶级 VLA，在某些维度上能达到与 WAM 相当的鲁棒性，但其训练成本和数据需求更高。
*   **几何泛化瓶颈**：WAM 在处理相机视角大幅度变化和机器人初始位姿扰动时仍面临挑战，说明视频先验对几何变换的理解有限。
*   **推理效率挑战**：WAM 的推理延迟是其最大的部署障碍，通常比 VLA 慢 4.8 到 83 倍。

### 7. 优点
*   **基准贡献**：提出了 RoboTwin 2.0-Plus，填补了双手协作机器人鲁棒性评估的空白。
*   **深度分析**：不仅给出了排名，还通过可视化预测的未来图像（如 Cosmos-Policy 的去噪能力）解释了 WAM 鲁棒性的来源。
*   **分类清晰**：对 WAM、VLA 及混合模型进行了科学的分类和对比，为后续研究指明了方向。

### 8. 不足与局限
*   **实时性差**：WAM 依赖多步去噪过程，目前难以满足高频实时控制（如 50Hz）的需求。
*   **模型覆盖**：由于算力限制，未能包含参数量最大的 WAM 模型（如 DreamZero）的完整测试。
*   **泛化局限**：虽然视觉鲁棒性强，但在极端几何变换（如相机位置大幅偏移）下的表现依然不理想。
*   **应用限制**：目前的 WAM 更多是在仿真环境中验证，真实世界的复杂物理交互（如软体物体、流体）在视频先验中可能覆盖不足。

（完）
