---
title: "CUCo: An Agentic Framework for Compute and Communication Co-design"
title_zh: CUCo：一种计算与通信协同设计的智能体框架
authors: "Bodun Hu, Yoga Sri Varshan, Saurabh Agarwal, Aditya Akella"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02376v1"
tags: ["query:ahd"]
score: 8.0
evidence: 自动生成高性能CUDA算子
tldr: 针对分布式大模型训练与推理中CUDA算子开发难、计算与通信协同优化不足的问题，本文提出CUCo框架。这是一个无需训练的智能体驱动工作流，能够自动生成协同编排计算与通信的高性能CUDA算子。通过打破传统计算与通信分离优化的局限，CUCo挖掘了新的优化空间，显著提升了GPU利用率并降低了端到端延迟，为自动化高性能算子开发提供了新路径。
selection_source: fresh_fetch
motivation: 现有的CUDA算子优化多集中于计算，忽视了通信算子在总执行时间中的占比，且手动编写协同优化算子极其困难。
method: 提出一种无需训练的智能体驱动工作流，通过协同设计计算与通信逻辑来自动生成高性能CUDA内核。
result: 实验表明CUCo生成的算子性能优于现有基准，端到端延迟最高可降低1.57倍。
conclusion: CUCo证明了计算与通信的协同设计是提升分布式系统性能的关键，并为自动化高性能算子开发提供了有效路径。
---

## 摘要
自定义 CUDA 算子开发对于在大型分布式大语言模型（LLM）训练和推理中最大化 GPU 利用率至关重要，但手动编写能够同时利用计算和通信的算子仍然是一个劳动密集且易出错的过程。先前的算子优化工作几乎完全集中在计算上，而通信算子在很大程度上未被触及，尽管它们在总执行时间中占很大比例。我们介绍了 CUCo，这是一种无需训练的智能体驱动工作流，可自动生成能够协同编排计算和通信的高性能 CUDA 算子。通过协同优化这些传统上相互独立的组件，CUCo 开启了现有方法无法实现的全新优化机会，其性能优于最先进的基准测试，并将端到端延迟降低了高达 1.57 倍。

## Abstract
Custom CUDA kernel development is essential for maximizing GPU utilization in large-scale distributed LLM training and inference, yet manually writing kernels that jointly leverage both computation and communication remains a labor-intensive and error-prone process. Prior work on kernel optimization has focused almost exclusively on computation, leaving communication kernels largely untouched even though they constitute a significant share of total execution time. We introduce CUCo, a training-free agent-driven workflow that automatically generates high-performance CUDA kernels that jointly orchestrate computation and communication. By co-optimizing these traditionally disjoint components, CUCo unlocks new optimization opportunities unavailable to existing approaches, outperforming state-of-the-art baselines and reducing end-to-end latency by up to $1.57\times$.

---

## 论文详细总结（自动生成）

以下是对论文《CUCo: An Agentic Framework for Compute and Communication Co-design》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在分布式大模型（LLM）训练和推理中，计算（Compute）与通信（Communication）通常是分离优化的。虽然现代硬件支持设备端触发通信（如 NVSHMEM、NCCL Device-side API），但手动编写能将两者深度融合的高性能 CUDA 算子极其困难。
*   **研究背景**：
    *   **设计空间巨大**：开发者需要权衡通信触发时机、同步粒度、分块大小等多个维度，且这些决策受硬件拓扑（如 NVLink vs. RoCE）影响极大。
    *   **现有工具不足**：传统编译器（如 XLA、Triton）通常将通信视为内核边界外的副作用，难以实现内核级的计算-通信重叠（Overlap）。
    *   **手动调优不可持续**：针对不同硬件组合手动优化算子耗时耗力且容易出错。

### 2. 方法论
CUCo 提出了一种无需训练的智能体（Agentic）框架，核心思想是将协同设计转化为受约束的搜索问题。
*   **设计空间规范化**：定义了五个维度的优化指令 $C = B \times P \times S \times I \times G$（后端选择、放置策略、同步范围、发射粒度、分块大小），将模糊的优化意图转化为结构化的搜索空间。
*   **双路径智能体架构**：
    1.  **快速路径（Fast-Path）智能体**：**优先保证正确性**。通过静态分析提取主机端 NCCL 的依赖图，利用“LLM+裁判（Judge）”循环将主机端代码转换为设备端触发的基准代码，并标注可进化的代码块（Evolve-Blocks）。
    2.  **慢速路径（Slow-Path）智能体**：**优先追求性能**。采用基于多岛屿（Multi-Island）的进化搜索算法。
*   **关键技术细节**：
    *   **LLM 变异算子**：LLM 不再盲目生成代码，而是作为变异算子在标注区域内进行语义感知的修改（如 Diff 补丁、全量重写）。
    *   **级联评估（Cascade Evaluation）**：按“编译 -> 验证 -> 基准测试”的顺序过滤无效候选者，降低评估成本。
    *   **元总结器（Meta-Summarizer）**：定期分析进化历史，提炼成功和失败的模式，为后续变异提供策略建议。

### 3. 实验设计
*   **实验场景**：涵盖了四种具有代表性的分布式工作负载：
    1.  **Flash Attention + 上下文并行**：环形通信拓扑（Ring Attention）。
    2.  **DeepSeek-V3 MoE 调度与合并**：跨节点 RoCE 链路上的 All-to-All 通信。
    3.  **KV-Cache 传输**：分离式预填充-解码架构中的 P2P 传输。
    4.  **GEMM + AllGather**：计算后紧跟集体通信的典型模式。
*   **基准测试（Benchmark）**：主要对比主机端驱动的标准 NCCL 实现（Host-driven baseline）。
*   **对比维度**：端到端延迟、优化增益分解（如消除流水线气泡、减少 API 开销等）。

### 4. 资源与算力
*   **硬件环境**：
    *   两台服务器，每台配备 2 个 16 核 Intel Xeon Silver 4314 CPU。
    *   每台服务器含 **4 个 NVIDIA A100 (80GB) GPU**。
    *   节点间通过 **RoCE** 连接，节点内通过 **NVLink** 连接。
*   **软件与模型**：
    *   使用 CUDA 13.1 和 NCCL 2.28.9。
    *   智能体后端使用 **Anthropic 的 Claude Sonnet 4.5**（注：论文中提及的版本号可能反映了其发布时的最新模型）。
*   **训练时长**：论文未明确给出单次搜索的总耗时，但提到快速路径通常在 2-4 次迭代内收敛，慢速路径在 18 代进化内可达到近优性能。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种负载进行了多组配置实验（如不同的序列长度、头维度、Token 不平衡比例等）。
*   **消融实验**：
    *   验证了“快速路径”的必要性（对比直接进行进化搜索）。
    *   验证了“探索-利用（Explore-Exploit）”两阶段策略的有效性。
*   **充分性评价**：实验设计较为全面，涵盖了单机多卡和跨机多卡场景，且通过 Nsight Systems 进行了深度的性能剖析（Case Study），客观地解释了加速来源。

### 6. 主要结论与发现
*   **性能提升显著**：CUCo 生成的融合算子相比主机端基准，端到端延迟降低了 **5.3% 至 57%（1.57x 加速）**。
*   **核心加速原因**：
    1.  **消除主机端编排开销**：减少了内核启动间隙和 CPU 触发 NCCL 的延迟。
    2.  **细粒度流水线**：实现了 Tile 级的计算与通信重叠，这是主机端 API 无法做到的。
    3.  **硬件感知**：智能体能根据 NVLink 或 RoCE 的特性自动选择最优的同步和传输策略。

### 7. 优点与亮点
*   **协同设计新范式**：打破了计算和通信长期以来的抽象屏障，实现了真正的内核级融合。
*   **结构化搜索**：通过定义优化指令空间，解决了 LLM 在生成复杂 CUDA 代码时容易产生幻觉或违反语义约束的问题。
*   **闭环反馈**：引入元总结器和级联评估，使进化搜索具有“经验学习”能力，提高了采样效率。

### 8. 不足与局限
*   **搜索成本**：虽然比强化学习训练成本低，但进化搜索仍需多次编译和真实硬件运行，在大规模部署时可能存在评估资源消耗。
*   **模型依赖**：高度依赖高性能 LLM（如 Claude 4.5）的推理能力，较弱的模型可能无法准确执行复杂的变异操作。
*   **算子覆盖面**：目前主要针对常见的集体通信模式，对于极其复杂的自定义非规则通信模式的泛化能力尚待验证。
*   **硬件限制**：主要针对 NVIDIA A100 及以上架构，对于缺少高级设备端通信特性的旧款 GPU 支持有限。

（完）
