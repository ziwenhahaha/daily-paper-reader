---
title: "ParEVO: Synthesizing Code for Irregular Data: High-Performance Parallelism through Agentic Evolution"
title_zh: ParEVO：针对不规则数据的代码合成：通过智能体演化实现高性能并行
authors: "Liu Yang, Zeyu Nie, Andrew Liu, Felix Zou, Deniz Altinbüken, Amir Yazdanbakhsh, Quanquan C. Liu"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.02510v1"
tags: ["query:ahd"]
score: 9.0
evidence: 通过智能体进化合成高性能并行算法
tldr: ParEVO是一个旨在为不规则数据结构（如稀疏图、非平衡树）合成高性能并行代码的框架。针对大模型在处理复杂并发逻辑时易产生竞态和死锁的问题，该研究构建了Parlay-Instruct数据集，微调了多种大模型，并引入了进化编程代理（ECA），通过编译器和性能分析器的反馈进行迭代修复。实验表明，ParEVO在并行性能上显著优于现有商业模型，甚至在特定内核上达到了人类专家水平。
selection_source: fresh_fetch
motivation: 传统大模型在为不规则数据结构编写并行代码时，难以处理复杂的动态调度和数据依赖，导致性能低下或程序错误。
method: 提出了包含Parlay-Instruct语料库、针对性微调模型以及利用反馈循环进行代码修复的进化编程代理（ECA）的集成框架。
result: 在ParEval基准测试中实现了平均106倍的加速，在复杂图问题上实现13.6倍加速，性能媲美人类专家。
conclusion: 通过结合高质量指令微调与基于反馈的进化搜索，可以有效提升大模型合成高性能、正确并行代码的能力。
---

## 摘要
从串行计算向并行计算的转变对于现代高性能应用至关重要，但受限于并发编程陡峭的学习曲线。对于不规则数据结构（如稀疏图、不平衡树和非均匀网格），这一挑战更加严峻，因为静态调度会失效且数据依赖性不可预测。当前的大语言模型（LLMs）在这些任务上往往表现糟糕，生成的代码充斥着微妙的竞态条件、死锁和次优的可扩展性。我们通过 ParEVO 弥补了这一差距，这是一个旨在为不规则数据合成高性能并行算法的框架。我们的贡献包括：(1) Parlay-Instruct 语料库，这是一个包含 13,820 个任务的精选数据集，通过“批评-改进”（Critic-Refine）流水线合成，该流水线显式筛选出能有效利用 Work-Span 并行原语且具有实证高性能的算法；(2) 经过微调的专用 DeepSeek、Qwen 和 Gemini 模型，使概率生成与 ParlayLib 库的严格语义保持一致；(3) 演化编码智能体（ECA），通过利用来自编译器、动态竞态检测器和性能分析器的反馈迭代修复代码，从而提升“最后一公里”的正确性。在 ParEval 基准测试中，ParEVO 在整个套件中实现了平均 106 倍的加速（最高达 1103 倍），特别是在复杂的不规则图问题上实现了稳健的 13.6 倍加速，优于最先进的商业模型。此外，我们的演化方法达到了最先进的人类专家基准水平，在特定的高度不规则内核上实现了高达 4.1 倍的加速。源代码和数据集可在 https://github.com/WildAlg/ParEVO 获取。

## Abstract
The transition from sequential to parallel computing is essential for modern high-performance applications but is hindered by the steep learning curve of concurrent programming. This challenge is magnified for irregular data structures (such as sparse graphs, unbalanced trees, and non-uniform meshes) where static scheduling fails and data dependencies are unpredictable. Current Large Language Models (LLMs) often fail catastrophically on these tasks, generating code plagued by subtle race conditions, deadlocks, and sub-optimal scaling.   We bridge this gap with ParEVO, a framework designed to synthesize high-performance parallel algorithms for irregular data. Our contributions include: (1) The Parlay-Instruct Corpus, a curated dataset of 13,820 tasks synthesized via a "Critic-Refine" pipeline that explicitly filters for empirically performant algorithms that effectively utilize Work-Span parallel primitives; (2) specialized DeepSeek, Qwen, and Gemini models fine-tuned to align probabilistic generation with the rigorous semantics of the ParlayLib library; and (3) an Evolutionary Coding Agent (ECA) that improves the "last mile" of correctness by iteratively repairing code using feedback from compilers, dynamic race detectors, and performance profilers.   On the ParEval benchmark, ParEVO achieves an average 106x speedup (with a maximum of 1103x) across the suite, and a robust 13.6x speedup specifically on complex irregular graph problems, outperforming state-of-the-art commercial models. Furthermore, our evolutionary approach matches state-of-the-art expert human baselines, achieving up to a 4.1x speedup on specific highly-irregular kernels. Source code and datasets are available at https://github.com/WildAlg/ParEVO.

---

## 论文详细总结（自动生成）

这是一份关于论文《ParEVO: Synthesizing Code for Irregular Data: High-Performance Parallelism through Agentic Evolution》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
现代高性能计算（HPC）的性能提升主要依赖并行化，但编写高效的并行代码具有极高的门槛。特别是针对**不规则数据结构**（如稀疏图、非均匀网格、不平衡树）时，由于内存访问模式不可预测且无法进行静态负载均衡，传统大语言模型（LLM）往往表现糟糕。LLM 生成的代码通常存在“串行偏见”，容易产生微妙的竞态条件（Race Conditions）、死锁，或者因为过度加锁导致性能甚至不如串行代码。论文旨在通过结合高质量领域数据集、模型微调和基于反馈的演化智能体，解决 LLM 在合成高性能并行代码方面的局限性。

### 2. 论文提出的方法论
ParEVO 框架由三个阶段组成：
*   **阶段 1：数据合成（Parlay-Instruct 语料库）**
    *   利用“教师-学生-批评者”流水线生成了 13,820 个经过验证的并行编码任务。
    *   核心思想是引导模型使用 **ParlayLib**（一个基于 Work-Span 模型的高级并行库），通过高级原语（如 `map`, `reduce`, `scan`, `filter`）而非底层线程原语来构建算法，从而实现“构造即正确”。
*   **阶段 2：监督微调（SFT）与对齐**
    *   对 DeepSeek-6.7B、Qwen3-30B 和 Gemini-2.5-Pro 进行微调。
    *   使用 **DPO（直接偏好优化）** 来抑制失败模式，通过“快-慢代码对”让模型学习如何优化性能。
*   **阶段 3：演化编码智能体（ECA）**
    *   将代码生成视为在抽象语法树（AST）空间中的演化搜索。
    *   **反馈循环**：智能体生成候选代码，通过编译器、动态竞态检测器（Sanitizers）和硬件性能分析器（Profilers）获取真实反馈。
    *   **多样性管理**：使用 **MAP-Elites** 算法维护候选池，根据代码长度、复杂度和同步原语频率保留多样化的优胜方案，迭代修复“最后一公里”的错误。

### 3. 实验设计
*   **数据集与基准测试**：
    *   **ParEval**：评估并行代码生成的标准基准。
    *   **PBBSBench & RPB**：分别针对 C++ 和 Rust 的专家级手工编写基准，用于衡量绝对性能。
    *   **DMOJ**：从在线评测平台抽取的 20 个复杂图论问题，用于验证泛化能力。
*   **对比方法**：
    *   商业模型：GPT-5-Thinking, Gemini-3-Pro, Claude 4.5, Gemini-2.5-Flash/Pro。
    *   开源模型：DeepSeek-Coder-V2-Lite, Qwen2.5-Coder-32B, StarCoder2-15B。
    *   专家基准：人类专家编写的高性能并行实现。

### 4. 资源与算力
*   **训练资源**：
    *   DeepSeek-6.7B：在 NVIDIA RTX 5000 Ada 上进行 LoRA 微调。
    *   Qwen3-30B：在 NVIDIA H200 GPU 上进行两阶段对齐（QLoRA SFT + DPO）。
*   **推理与评估资源**：
    *   推理使用 NVIDIA H200。
    *   性能测试在双路 Intel Xeon Platinum 8562Y+ 处理器（共 64 个物理核心）上进行，配备 512GB DDR5 内存。
*   **时长**：文中未明确说明具体的训练总时长。

### 5. 实验数量与充分性
*   **实验规模**：论文在 ParEval 的 12 个类别（如图、搜索、排序、稀疏线性代数等）上进行了全面测试，并针对 C++ 和 Rust 两种语言分别验证。
*   **消融实验**：专门设计了针对 ECA（演化智能体）的消融实验，验证了迭代次数（10次 vs 30次）对性能提升的贡献。
*   **客观性**：实验采用了确定性的外部工具（编译器和分析器）作为“健身函数”，避免了“LLM 评判 LLM”的主观偏差。通过强缩放（Strong Scaling）实验展示了代码在多核环境下的扩展性，数据较为详实。

### 6. 论文的主要结论与发现
*   **性能飞跃**：ParEVO 在 ParEval 上实现了平均 **106 倍**的加速，在复杂图问题上实现了 **13.6 倍**的稳健加速。
*   **超越专家**：在某些特定内核（如最大独立集 MIS）上，ParEVO 生成的 Rust 代码比专家编写的基准快 **4.1 倍**。
*   **对齐税（Alignment Tax）**：发现微调虽然显著提升了代码正确率（Pass@1 从 0.42 升至 0.76），但有时会牺牲峰值性能，因为模型倾向于选择更安全、更高层的原语而非激进的原子操作。
*   **抽象的重要性**：证明了让 LLM 学习高级并行原语（如 ParlayLib）比学习底层线程管理更有效。

### 7. 优点
*   **闭环优化**：引入了基于真实硬件反馈的演化机制，解决了并行代码难以通过静态分析验证的痛点。
*   **高质量数据集**：Parlay-Instruct 填补了 HPC 领域高质量指令微调数据的空白。
*   **多语言支持**：同时解决了 C++ 和 Rust 在不规则并行计算中的合成问题，特别是 Rust 的安全性约束。
*   **实用性强**：生成的代码不仅是“看起来正确”，而是真正具备可扩展性和高性能。

### 8. 不足与局限
*   **推理成本**：演化智能体需要多次迭代、编译和运行，这带来了显著的推理时间开销和计算成本。
*   **架构限制**：目前主要针对共享内存的多核 CPU 架构，尚未扩展到分布式内存（MPI）或异构计算（GPU/CUDA）。
*   **领域幻觉**：在某些高度专业化的领域（如几何计算），模型仍可能产生 API 幻觉（调用不存在的库函数）。
*   **应用范围**：对于生命周期极短的代码，这种高昂的合成成本可能并不划算，更适用于需要反复运行的核心计算内核。

（完）
