---
title: "stratum: A System Infrastructure for Massive Agent-Centric ML Workloads"
title_zh: stratum：面向大规模以智能体为中心的机器学习工作负载的系统基础设施
authors: "Arnab Phani, Elias Strauss, Sebastian Schelter"
date: 2026-03-03
pdf: "https://arxiv.org/pdf/2603.03589v1"
tags: ["query:ahd"]
score: 8.0
evidence: 用于代理式流水线搜索和机器学习流水线迭代优化的基础设施
tldr: 随着大语言模型的发展，智能体驱动的机器学习流水线搜索成为新趋势，但现有的 Python 生态系统因其顺序执行和解释性限制，难以应对此类大规模探索性任务。本文提出 Stratum 系统基础设施，通过解耦执行与规划、批量编译优化执行图以及引入 Rust 高性能运行时，无缝集成现有 Python 库并显著提升执行效率。Stratum 解决了智能体工作负载与传统 ML 系统之间的不匹配，为大规模自动化机器学习提供了高效支撑。
selection_source: fresh_fetch
motivation: 现有的 Python ML 生态系统主要面向人类交互式工作流，无法高效处理 LLM 智能体在自动化流水线搜索中产生的大规模、高并发且迭代频繁的执行任务。
method: Stratum 采用解耦执行与规划的架构，将批量流水线编译为优化的执行图，并利用基于 Rust 的高性能运行时在异构后端上高效执行。
result: 初步实验结果显示，Stratum 在处理大规模智能体驱动的流水线搜索任务时，相比现有方案可实现高达 16.6 倍的性能提升。
conclusion: Stratum 成功填补了高性能 ML 系统与 Python 智能体生态之间的空白，为未来大规模智能体中心化的机器学习工作负载奠定了系统级基础。
---

## 摘要
大语言模型（LLM）的最新进展改变了机器学习（ML）流水线的开发和评估方式。LLM 催生了一种新型工作负载——智能体流水线搜索（agentic pipeline search），其中自主或半自主智能体负责生成、验证并优化完整的 ML 流水线。这些智能体主要在流行的 Python ML 库上运行，并表现出高度的探索性行为。这导致了成千上万次用于数据分析、流水线生成以及流水线阶段迭代优化的执行任务。然而，现有的基于 Python 的 ML 生态系统是围绕 Pandas 和 scikit-learn 等库构建的，这些库是为以人为中心、交互式且顺序的工作流设计的，并受到 Python 解释执行模型、库级隔离以及对执行大量流水线的运行时支持有限等因素的制约。与此同时，系统社区提出的许多高性能 ML 系统要么针对狭窄的工作负载类别，要么需要专门的编程模型，这限制了它们与 Python ML 生态系统的集成，使其在很大程度上不适用于基于 LLM 的智能体。这种日益严重的不匹配暴露了在支持大规模智能体流水线搜索方面面临的根本性系统挑战。因此，我们提出了 stratum，这是一个统一的系统基础设施，它在智能体流水线搜索过程中将流水线执行与规划和推理相解耦。Stratum 与现有的 Python 库无缝集成，将批量流水线编译为优化的执行图，并在包括新型基于 Rust 的运行时在内的异构后端上高效执行。我们展示了 stratum 的架构愿景及早期原型，讨论了关键的设计决策，并概述了开放性挑战和研究方向。最后，初步实验表明，stratum 可以显著加速大规模智能体流水线搜索，提升幅度高达 16.6 倍。

## Abstract
Recent advances in large language models (LLMs) transform how machine learning (ML) pipelines are developed and evaluated. LLMs enable a new type of workload, agentic pipeline search, in which autonomous or semi-autonomous agents generate, validate, and optimize complete ML pipelines. These agents predominantly operate over popular Python ML libraries and exhibit highly exploratory behavior. This results in thousands of executions for data profiling, pipeline generation, and iterative refinement of pipeline stages. However, the existing Python-based ML ecosystem is built around libraries such as Pandas and scikit-learn, which are designed for human-centric, interactive, sequential workflows and remain constrained by Python's interpretive execution model, library-level isolation, and limited runtime support for executing large numbers of pipelines. Meanwhile, many high-performance ML systems proposed by the systems community either target narrow workload classes or require specialized programming models, which limits their integration with the Python ML ecosystem and makes them largely ill-suited for LLM-based agents. This growing mismatch exposes a fundamental systems challenge in supporting agentic pipeline search at scale. We therefore propose stratum, a unified system infrastructure that decouples pipeline execution from planning and reasoning during agentic pipeline search. Stratum integrates seamlessly with existing Python libraries, compiles batches of pipelines into optimized execution graphs, and efficiently executes them across heterogeneous backends, including a novel Rust-based runtime. We present stratum's architectural vision along with an early prototype, discuss key design decisions, and outline open challenges and research directions. Finally, preliminary experiments show that stratum can significantly speed up large-scale agentic pipeline search up to 16.6x.

---

## 论文详细总结（自动生成）

这是一份关于论文《stratum: A System Infrastructure for Massive Agent-Centric ML Workloads》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：随着大语言模型（LLM）驱动的机器学习工程（MLE）智能体兴起，它们在搜索最优模型时会生成成千上万个 Python 机器学习流水线。然而，现有的 Python 生态（如 Pandas, scikit-learn）是为人类交互式、顺序操作设计的，存在 Python 解释器性能瓶颈、缺乏全局优化、内存管理效率低等问题。
*   **研究背景**：现有的高性能 ML 系统（如 SystemDS）通常需要专门的编程语言（DSL），LLM 难以掌握；而现有的 Python 库又无法处理智能体产生的高并发、高冗余、大规模的探索性工作负载。这导致了智能体生成的代码在执行时效率极低，经常出现内存溢出（OOM）和硬件利用率不足。

### 2. 论文提出的方法论
Stratum 的核心思想是将**流水线规划（智能体负责）与执行（系统负责）解耦**。
*   **声明式抽象**：利用 `skrub` 库将任意 Python ML 代码包装成“DataOps”算子，形成一个延迟计算的有向无环图（DAG）。
*   **逻辑优化器**：
    *   **元数据收集与重写**：提取算子属性，应用谓词下推、投影下推、公共子表达式消除（CSE）等经典数据库优化技术。
    *   **算子下放（Lowering）**：将复杂的宏算子（如交叉验证）展开为细粒度算子，以扩大优化空间。
*   **高性能后端**：
    *   **Rust 运行时**：针对频繁使用的算子开发 Rust 内核，通过 PyO3 与 Python 绑定，释放全局解释器锁（GIL），实现零拷贝数据访问。
    *   **算子选择**：根据成本估算，在 Python 原生库、Rust 内核或分布式后端（如 Dask）之间动态选择。
*   **运行时优化**：
    *   **并行化规划**：结合算子间（Inter-op）和算子内（Intra-op）并行。
    *   **中间结果复用**：针对智能体迭代中大量重复的代码，实施粗粒度和细粒度的缓存策略。

### 3. 实验设计
*   **数据集与场景**：使用 Kaggle 上的英国房价数据集（UK Housing dataset），模拟智能体进行端到端的流水线搜索。
*   **工作负载**：由 AIDE 智能体生成的真实工作负载，包含两轮迭代：第一轮探索多种预处理和模型组合，第二轮进行超参数微调。
*   **Benchmark 与对比方法**：
    *   **Base**：AIDE 智能体默认的顺序执行模式。
    *   **Base_par**：基于多进程的朴素并行执行模式。
    *   **stratum**：开启所有优化的本系统。

### 4. 资源与算力
*   **硬件环境**：单节点服务器，配备 **AMD EPYC 7443P CPU**（24 个物理核心 / 48 个逻辑线程），**256 GB RAM**。
*   **软件环境**：Ubuntu 20.04, Python 3.11。
*   **GPU 使用**：文中实验主要针对表格数据（Tabular Data），未提及 GPU 使用情况，重点在于 CPU 并行和内存优化。

### 5. 实验数量与充分性
*   **实验规模**：进行了端到端性能对比实验（随数据量缩放）和消融实验（逐项开启优化模块）。
*   **充分性评价**：作为一篇 **Vision（愿景）类论文**，其实验属于初步验证性质（Preliminary Experiments）。虽然实验场景较为单一（仅一个数据集和一种智能体），但成功证明了其设计原则（如 Rust 后端和逻辑重写）在处理智能体负载时的巨大潜力。实验设计客观，对比了朴素并行方案并分析了序列化开销。

### 6. 主要结论与发现
*   **显著加速**：Stratum 相比顺序执行（Base）实现了高达 **16.6 倍** 的加速，相比朴素并行（Base_par）实现了 **7.8 倍** 的加速。
*   **优化贡献**：消融实验显示，逻辑优化（如 CSE）可带来约 2.2 倍提升，而引入 Rust 原生算子选择则进一步带来了 4.5 倍的提升。
*   **资源效率**：Stratum 避免了多进程并行带来的巨大内存开销（Base_par 内存占用高出 8 倍）和序列化延迟。

### 7. 优点
*   **无缝集成**：不需要智能体改变编写 Python 代码的习惯，通过包装现有库实现透明优化。
*   **系统与智能体协同**：提出了“系统感知型智能体”的概念，允许智能体通过元数据引导系统优化。
*   **高性能运行时**：利用 Rust 解决 Python 的 GIL 限制，提供了更细粒度的内存控制和并行能力。

### 8. 不足与局限
*   **原型阶段**：Rust 后端目前仅覆盖了部分常用算子，尚未实现完整的算子库。
*   **成本估算困难**：由于 Python 库（如 Pandas）内部内存占用不透明，系统目前难以做出极其精确的成本预测。
*   **黑盒 UDF**：对于完全自定义的 Python 函数（UDF），系统仍将其视为黑盒，限制了优化深度。
*   **深度学习支持**：目前主要侧重于表格数据和传统 ML，对深度学习（DNN）工作负载的集成（如 CUDA 内核融合）尚在探索中。

（完）
