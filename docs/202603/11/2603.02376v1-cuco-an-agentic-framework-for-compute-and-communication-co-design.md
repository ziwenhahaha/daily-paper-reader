---
title: "CUCo: An Agentic Framework for Compute and Communication Co-design"
title_zh: CUCo：一种计算与通信协同设计的智能体框架
authors: "Bodun Hu, Yoga Sri Varshan, Saurabh Agarwal, Aditya Akella"
date: 2026-03-02
pdf: "https://arxiv.org/pdf/2603.02376v1"
tags: ["query:ahd"]
score: 9.0
evidence: 自动生成用于计算和通信的高性能CUDA算子
tldr: 针对大规模分布式大模型训练与推理中CUDA算子开发难、计算与通信协同优化不足的问题，本文提出了CUCo框架。这是一个无需训练的智能体驱动工作流，能够自动生成协同编排计算与通信的高性能CUDA算子。通过打破传统计算与通信分离优化的局限，CUCo挖掘了新的优化空间，显著提升了GPU利用率并降低了端到端延迟，为自动化高性能算子开发提供了新路径。
selection_source: fresh_fetch
motivation: 现有的CUDA算子优化多集中于计算而忽视了通信，且手动编写计算通信协同算子极其耗时且易出错。
method: 提出了一种无需训练的智能体驱动工作流CUCo，通过协同设计自动生成同时管理计算与通信的高性能CUDA内核。
result: CUCo生成的算子性能优于现有基准，在端到端延迟上最高可实现1.57倍的加速。
conclusion: CUCo证明了计算与通信的协同优化能有效解锁新的性能空间，是提升分布式系统效率的关键。
---

## 摘要
自定义 CUDA 算子开发对于在大型分布式大语言模型（LLM）训练和推理中最大化 GPU 利用率至关重要，但手动编写能够同时利用计算和通信的算子仍然是一个劳动密集且易出错的过程。先前的算子优化工作几乎完全集中在计算上，而通信算子在很大程度上未被触及，尽管它们在总执行时间中占很大比例。我们介绍了 CUCo，这是一种无需训练的智能体驱动工作流，可自动生成能够协同编排计算和通信的高性能 CUDA 算子。通过协同优化这些传统上相互独立的组件，CUCo 开启了现有方法无法实现的全新优化机会，其性能优于最先进的基准测试，并将端到端延迟降低了高达 1.57 倍。

## Abstract
Custom CUDA kernel development is essential for maximizing GPU utilization in large-scale distributed LLM training and inference, yet manually writing kernels that jointly leverage both computation and communication remains a labor-intensive and error-prone process. Prior work on kernel optimization has focused almost exclusively on computation, leaving communication kernels largely untouched even though they constitute a significant share of total execution time. We introduce CUCo, a training-free agent-driven workflow that automatically generates high-performance CUDA kernels that jointly orchestrate computation and communication. By co-optimizing these traditionally disjoint components, CUCo unlocks new optimization opportunities unavailable to existing approaches, outperforming state-of-the-art baselines and reducing end-to-end latency by up to $1.57\times$.

---

## 论文详细总结（自动生成）

以下是对论文《CUCo: An Agentic Framework for Compute and Communication Co-design》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：在分布式大模型（LLM）的训练和推理中，计算（Compute）与通信（Communication）通常是分离优化的。传统的通信由 CPU 主机端调度（Host-side orchestration），这会引入显著的同步开销和内核启动间隙，限制了计算与通信的深度重叠（Overlap）。
*   **研究动机**：
    *   手动编写能够直接在 GPU 设备端触发通信（Device-initiated）的 CUDA 算子极其困难且易出错。
    *   现有的算子生成工具（如 Triton, XLA）大多将通信视为外部副作用，缺乏协同设计的能力。
    *   硬件异构性（如 NVLink vs. RoCE）使得最优的计算-通信配比难以通过经验公式推导，必须进行自动化搜索。

### 2. 方法论
CUCo 提出了一个无需训练的智能体驱动框架，其核心思想是将算子生成转化为受约束的搜索问题。
*   **设计空间规范 (Design Space Specification)**：定义了五个关键维度：后端选择（GIN/LSA）、放置策略（重叠激进程度）、同步范围、发射粒度（线程/线程束/线程块）和块大小。
*   **快路径智能体 (Fast-Path Agent)**：
    *   **目标**：优先保证正确性。
    *   **流程**：通过静态分析提取原始代码的通信依赖图，利用 LLM-Judge 循环将主机端 NCCL 调用转换为设备端原语（如 `gin.put` 或 LSA 直接访存），生成一个保守但可运行的“种子”程序。
*   **慢路径智能体 (Slow-Path Agent)**：
    *   **目标**：追求极致性能。
    *   **流程**：采用**多岛进化算法 (Multi-island EA)**。LLM 作为变异算子，在预定义的“进化块”内修改代码。
    *   **关键机制**：引入 **Meta-Summarizer**，跨代总结成功与失败的模式，为 LLM 提供高层优化建议；使用 **MAP-Elites** 存档保持种群多样性。
*   **级联评估 (Cascade Evaluation)**：候选代码需经过“编译 -> 验证 -> 基准测试”三级过滤，确保只有正确且高效的代码进入下一轮进化。

### 3. 实验设计
*   **场景与负载**：
    1.  **Flash Attention + 上下文并行**：环形注意力机制（Ring Attention）。
    2.  **DeepSeek-V3 MoE 分发与合并**：模拟专家并行中的 All-to-All 通信。
    3.  **KV-Cache 传输**：针对分离式预填-解码（Prefill-Decode）架构。
    4.  **GEMM + AllGather**：基础的计算后集合通信。
*   **基准 (Benchmark)**：标准的主机端驱动 NCCL 集合通信实现（Host-driven baseline）。
*   **对比维度**：端到端延迟、通信重叠效率、不同专家负载偏斜下的鲁棒性。

### 4. 资源与算力
*   **硬件环境**：使用了 2 台服务器，每台配备 4 块 **NVIDIA A100 (80GB)** GPU。
*   **互联技术**：节点内使用 **NVLink**，节点间使用 **RoCE** (RDMA over Converged Ethernet)。
*   **智能体模型**：所有智能体（快路径、慢路径、反馈）均使用 **Anthropic Claude Sonnet 4.5**。
*   **训练/搜索时长**：文中未给出总搜索小时数，但提到快路径通常在 2-4 次迭代内收敛，慢路径实验展示了约 18 代的进化过程。

### 5. 实验数量与充分性
*   **实验规模**：针对 4 种代表性负载进行了多组配置实验（如不同的序列长度、头维度、专家偏斜比例）。
*   **消融实验**：
    *   验证了“快路径+慢路径”组合比单纯使用慢路径搜索效率更高、稳定性更强。
    *   验证了“探索-利用（Explore-Exploit）”两阶段策略对最终性能的提升。
*   **充分性评价**：实验设计较为客观，涵盖了节点内和节点间两种主流分布式场景，并利用 Nsight Systems 进行了深度的内核性能剖析，证明了性能提升源于真实的流水线重叠而非随机波动。

### 6. 主要结论
*   **性能提升**：CUCo 生成的融合算子相比基准测试实现了 **1.05x 到 1.57x** 的加速。
*   **优化来源**：主要通过消除主机端 API 调用开销（约占总时间的 3-5%）和实现**细粒度的流水线重叠**（如在 Flash Attention 中实现 Tile 级别的边传边算）来降低延迟。
*   **自动化能力**：证明了 LLM 智能体在结构化约束下，能够发现人类开发者难以直观构思的复杂同步和缓冲策略。

### 7. 优点与亮点
*   **协同设计范式**：打破了计算和通信的抽象边界，是自动化高性能计算领域的重要进展。
*   **结构化搜索空间**：通过定义五个维度的指令规范，有效解决了 LLM 在生成底层 CUDA 代码时的“幻觉”问题。
*   **闭环反馈**：Meta-Summarizer 实现了跨代学习，使搜索过程具有“经验积累”能力。

### 8. 不足与局限
*   **硬件覆盖有限**：实验仅在 NVIDIA A100 上完成，对于 H100/B200 等新架构或 AMD 等非 NVIDIA 平台的适配性尚未验证。
*   **搜索成本**：虽然无需模型训练，但进化搜索过程中的反复编译和多机运行基准测试仍需消耗一定的算力和时间。
*   **LLM 依赖性**：性能高度依赖于顶级 LLM 的推理能力，对于开源或较小规模的 LLM，其生成复杂同步逻辑的成功率可能大幅下降。
*   **应用限制**：目前主要针对相对规整的集合通信模式，对于极度动态或非规则的通信图支持尚不明确。

（完）
