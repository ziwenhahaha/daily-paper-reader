---
title: "FormalEvolve: Neuro-Symbolic Evolutionary Search for Diverse and Prover-Effective Autoformalization"
title_zh: FormalEvolve：面向多样化且对证明器有效的自动形式化的神经符号进化搜索
authors: "Haijian Lu, Wei Wang, Jing Liu"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19828v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 神经符号演化框架中的LLM驱动变异和交叉
tldr: FormalEvolve 是一种神经符号进化框架，旨在将自然语言数学自动转化为可编译且易于证明的形式化语句。该方法针对语义一致性与证明有效性之间的差距，结合大模型驱动的变异交叉与符号化 AST 重写，在有限预算内生成多样化的候选方案。实验表明，它在 CombiBench 和 ProofNet 上显著提升了语义命中率和下游证明性能，为自动形式化提供了高效的新路径。
selection_source: fresh_fetch
motivation: 现有的自动形式化方法生成的语句虽然语义正确，但往往难以被自动证明器处理，因此需要一种能生成多样且易于证明的形式化描述的方法。
method: 提出 FormalEvolve 框架，通过大模型驱动的变异、交叉及补丁修复，结合符号化抽象语法树（AST）重写操作，在编译门控下进行进化搜索。
result: "在 CombiBench 和 ProofNet 上，该方法在 100 次调用预算下分别达到了 58.0% 和 84.9% 的语义命中率，并显著降低了成功结果的分布集中度。"
conclusion: 神经符号进化搜索能有效提升自动形式化的多样性与证明有效性，是解决复杂数学形式化问题的有力工具。
---

## 摘要
自动形式化旨在将自然语言数学翻译为可编译、机器可检查的陈述。然而，语义一致性并不意味着对证明器有效：即使是语义一致的形式化，在证明搜索成本和成功率方面也可能存在显著差异。在这项工作中，我们将自动形式化建模为在预算限制下对语义一致性库的测试时搜索，并提出了 FormalEvolve，这是一个由编译门控的神经符号进化框架。FormalEvolve 通过大语言模型（LLM）驱动的变异和交叉以及有界补丁修复来生成多样化的候选方案，同时符号抽象语法树（AST）重写操作进一步注入了结构多样性。在 CombiBench 和 ProofNet 上，在 T = 100 的严格生成器调用预算下，FormalEvolve 的语义命中率（SH@100）分别达到 58.0% 和 84.9%，并降低了语义成功的跨问题集中度（更低的基尼系数）。在固定的证明器预算下，FormalEvolve 还提高了 CombiBench 上的下游证明性能。代码将公开。

## Abstract
Autoformalization aims to translate natural-language mathematics into compilable, machine-checkable statements. However, semantic consistency does not imply prover effectiveness: even semantically consistent formalizations can differ substantially in proof-search cost and success rate. In this work, we formulate autoformalization as a budgeted, test-time search for semantically consistent repertoires, and propose FormalEvolve, a compilation-gated neuro-symbolic evolutionary framework. FormalEvolve generates diverse candidates via LLM-driven mutation and crossover with bounded patch repair, while symbolic Abstract Syntax Tree (AST) rewrite operations further inject structural diversity. On CombiBench and ProofNet, under a strict generator-call budget of T = 100, FormalEvolve reaches semantic hit rates (SH@100) of 58.0% and 84.9%, and reduces cross-problem concentration of semantic successes(lower Gini). Under a fixed prover budget, FormalEvolve also improves downstream proving performance on CombiBench. Code will be released publicly.

---

## 论文详细总结（自动生成）

以下是对论文《FormalEvolve: Neuro-Symbolic Evolutionary Search for Diverse and Prover-Effective Autoformalization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：自动形式化（将自然语言数学翻译为机器可检查的 Lean 4 代码）中存在“语义一致性 $\neq$ 证明有效性”的问题。即：一个在语义上正确且能编译的代码，对于自动证明器（Prover）来说可能依然难以处理。
*   **研究动机**：现有的方法往往只追求生成单一的正确陈述，但证明器对陈述的表达方式非常敏感。作者认为，应该在有限的计算预算内，通过搜索生成一个**多样化的语义一致陈述库（Repertoire）**，从而增加至少有一个陈述能被证明器成功证明的概率。

### 2. 方法论：核心思想与关键技术
FormalEvolve 是一个由编译门控（Compilation-gated）的神经符号进化框架，其核心流程如下：
*   **进化搜索状态**：维护一个“编译可行存档（Archive）”，仅存储能通过 Lean 4 编译器检查的候选代码。
*   **选择机制**：采用**使用惩罚（Usage-penalized）**的选择规则和**岛屿模型（Island Model）**。通过惩罚频繁作为父代使用的陈述，强制搜索转向未探索的区域，减少模式崩溃。
*   **变异与交叉（神经算子）**：
    *   **LLM 补丁（Patching）**：包括全量重写、差异补丁（Diff）和交叉补丁（Cross，即结合存档中其他优秀陈述的特征）。
    *   **有界修复（Bounded Repair）**：当生成的代码编译失败时，利用 LLM 进行有限次数的修复尝试，这些尝试会计入总调用预算。
*   **符号化多样性（符号算子）**：
    *   **EvolAST**：在 LLM 搜索停滞时，利用符号化的抽象语法树（AST）重写规则（如交换算子顺序、等价逻辑变换）生成变体。这种操作不消耗 LLM 调用预算。
*   **预算管理**：严格限制每个问题的生成器调用次数（$T=100$），确保对比的公平性。

### 3. 实验设计
*   **数据集**：
    *   **ProofNet**：涵盖本科水平数学题的 Lean 4 移植版。
    *   **CombiBench**：专注于组合数学，具有较强的领域和风格偏移，用于测试鲁棒性。
*   **对比方法**：
    *   **基础采样（Sample）**：无修复的直接采样。
    *   **编译修复（Compile Repair）**：带编译错误修复的采样。
    *   **语义修复（Compile+Semantic Repair）**：在编译修复基础上增加一步语义纠偏。
    *   **混合控制组**：使用更强的模型（Qwen3）进行修复但无进化机制的采样。
*   **评估指标**：语义命中率（SH@T）、基尼系数（衡量成功分布的均匀度）、下游证明成功率（pass@k, complete@k）。

### 4. 资源与算力
*   **模型使用**：
    *   种子生成：Kimina-Autoformalizer-7B。
    *   补丁与修复：Qwen3-30B-A3B。
    *   语义评判（Judge）：CriticLean-Qwen3-14B。
    *   证明器（Prover）：Goedel-Prover-V2-32B。
*   **算力说明**：论文未明确给出具体的 GPU 型号、数量或总训练时长，但详细记录了每个问题的 LLM 调用预算（$T=100$）和证明尝试次数（$B=64$）。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试上进行了完整测试（ProofNet 186 题，CombiBench 100 题）。
*   **消融实验**：针对 EvolAST 算子、补丁修复机制、岛屿模型等关键组件进行了消融分析。
*   **充分性评价**：实验设计较为充分。作者不仅关注了命中率，还引入了基尼系数来分析模型是否只是在“简单题目”上刷分。通过严格的调用预算控制（Strict Accounting），确保了与基线方法对比的客观性和公平性。

### 6. 主要结论与发现
*   **性能提升**：在 $T=100$ 预算下，FormalEvolve 在 CombiBench 上的语义命中率从 46.0% 提升至 58.0%，在 ProofNet 上从 78.0% 提升至 84.9%。
*   **多样性价值**：进化搜索显著降低了成功的跨问题集中度（基尼系数下降），意味着它能解决更多样化、更难的问题。
*   **证明增益**：在 CombiBench 上，该方法生成的陈述库显著提高了下游证明器的成功率（从 8/100 提升至 13/100）。
*   **修复的重要性**：消融实验显示，有界的补丁修复是语义覆盖率提升的主要驱动力。

### 7. 优点
*   **视角独特**：首次明确区分了“语义一致性”与“证明有效性”，并针对性地通过多样化搜索来对冲证明器的脆弱性。
*   **神经符号结合**：巧妙利用符号化 AST 重写作为“零成本”的多样性来源，优化了预算分配。
*   **评估严谨**：引入了严格的调用预算审计，避免了通过无限采样堆砌性能的陷阱。

### 8. 不足与局限
*   **评判器依赖**：实验高度依赖 LLM 评判器（CriticLean）来判断语义一致性，而评判器本身可能存在误判（论文附录中承认了这一点）。
*   **证明器敏感性**：下游证明性能的提升受限于所选证明器的能力，不同证明器可能对多样性的响应不同。
*   **领域局限**：目前主要在 Lean 4 上验证，尚未扩展到 Coq 或 Isabelle 等其他形式化语言。

（完）
