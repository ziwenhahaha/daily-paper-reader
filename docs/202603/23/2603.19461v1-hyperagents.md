---
title: Hyperagents
title_zh: 超代理 (Hyperagents)
authors: "Jenny Zhang, Bingchen Zhao, Wannan Yang, Jakob Foerster, Jeff Clune, Minqi Jiang, Sam Devlin, Tatiana Shavrina"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19461v1"
tags: ["query:ahd"]
score: 8.0
evidence: 用于自动算法发现的自我改进AI系统
tldr: 本研究针对现有自我改进AI系统依赖固定元机制且受限于特定领域的局限，提出了Hyperagents框架。该框架将执行任务的代理与负责自我修改的元代理整合为单一可编辑程序，使自我修改过程本身也变得可优化。通过扩展达尔文哥德尔机（DGM）构建的DGM-H系统，在无需领域对齐假设的情况下，实现了跨领域的性能提升和元级技能（如性能跟踪）的积累与迁移，为开发能不断优化自身改进机制的开放式AI系统提供了新方向。
selection_source: fresh_fetch
motivation: 现有自我改进系统通常依赖固定的手工设计机制，且其改进能力往往局限于特定领域，限制了系统的进化速度和通用性。
method: 提出Hyperagents框架，将任务代理与元代理整合为统一的可编辑程序，使系统能够通过元认知自我修改来改进其自身的改进机制。
result: DGM-H在多个领域均优于传统基准，且其产生的元级改进（如持久内存和性能追踪）能够跨领域迁移并随运行时间持续累积。
conclusion: Hyperagents展示了开放式AI系统的潜力，即系统不仅能搜索更好的解决方案，还能持续优化其搜索改进方案的方式。
---

## 摘要
自我改进的 AI 系统旨在通过学习改进自身的学习和问题解决过程，从而减少对人工工程的依赖。现有的自我改进方法依赖于固定的、手工设计的元级机制，这从根本上限制了此类系统的改进速度。达尔文哥德尔机（DGM）通过重复生成和评估自我修改的变体，展示了在编程领域的开放式自我改进。由于评估和自我修改都是编程任务，编程能力的提升可以转化为自我改进能力的提升。然而，这种一致性在编程领域之外通常并不成立。我们引入了超代理（hyperagents），这是一种自引用代理，它将任务代理（解决目标任务）和元代理（修改自身和任务代理）集成到一个单一的可编辑程序中。至关重要的是，元级修改过程本身也是可编辑的，从而实现了元认知自我修改，不仅改进了任务解决行为，还改进了生成未来改进的机制。我们通过扩展 DGM 创建了 DGM-Hyperagents (DGM-H) 来实例化这一框架，消除了任务性能与自我修改技能之间特定领域一致性的假设，从而可能支持在任何可计算任务上实现自我加速的进展。在不同领域中，DGM-H 随着时间的推移提高了性能，并优于没有自我改进或开放式探索的基准模型，以及先前的自我改进系统。此外，DGM-H 改进了其生成新代理的过程（例如，持久化内存、性能跟踪），并且这些元级改进可以跨领域迁移并在多次运行中累积。DGM-Hyperagents 让我们一窥开放式 AI 系统，它们不仅在寻找更好的解决方案，还在不断改进其“如何改进”的搜索过程。

## Abstract
Self-improving AI systems aim to reduce reliance on human engineering by learning to improve their own learning and problem-solving processes. Existing approaches to self-improvement rely on fixed, handcrafted meta-level mechanisms, fundamentally limiting how fast such systems can improve. The Darwin Gödel Machine (DGM) demonstrates open-ended self-improvement in coding by repeatedly generating and evaluating self-modified variants. Because both evaluation and self-modification are coding tasks, gains in coding ability can translate into gains in self-improvement ability. However, this alignment does not generally hold beyond coding domains. We introduce \textbf{hyperagents}, self-referential agents that integrate a task agent (which solves the target task) and a meta agent (which modifies itself and the task agent) into a single editable program. Crucially, the meta-level modification procedure is itself editable, enabling metacognitive self-modification, improving not only the task-solving behavior, but also the mechanism that generates future improvements. We instantiate this framework by extending DGM to create DGM-Hyperagents (DGM-H), eliminating the assumption of domain-specific alignment between task performance and self-modification skill to potentially support self-accelerating progress on any computable task. Across diverse domains, the DGM-H improves performance over time and outperforms baselines without self-improvement or open-ended exploration, as well as prior self-improving systems. Furthermore, the DGM-H improves the process by which it generates new agents (e.g., persistent memory, performance tracking), and these meta-level improvements transfer across domains and accumulate across runs. DGM-Hyperagents offer a glimpse of open-ended AI systems that do not merely search for better solutions, but continually improve their search for how to improve.

---

## 论文详细总结（自动生成）

### 论文总结：Hyperagents (超代理)

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的自我改进 AI 系统（如 Darwin Gödel Machine, DGM）通常依赖于**固定且手工设计**的元级（meta-level）机制。这意味着系统虽然能改进任务执行代码，但其“如何改进”的逻辑是锁死的。
*   **研究背景**：在编程领域，任务能力与改进能力是高度对齐的（写代码和改代码是一回事）；但在非编程领域（如数学、机器人），这种对齐失效。
*   **整体含义**：论文提出了 **Hyperagents** 框架，旨在打破这种限制，使 AI 系统能够同时改进其**任务执行逻辑**和**自我改进机制**（即元认知自我修改），从而在任何可计算任务上实现自我加速的进化。

#### 2. 方法论：核心思想与算法流程
*   **核心思想**：将“任务代理”（解决目标问题）和“元代理”（修改自身和任务代理）整合为一个**单一的、可编辑的程序**。
*   **关键技术细节**：
    *   **自引用程序**：使用 Python 编写，代理可以调用基础模型（FM）、外部工具（如 Bash、文件编辑器）并修改自身的源代码。
    *   **元认知自我修改**：元代理不仅生成任务代码的补丁，还能修改元代理自身的逻辑（例如改进评估分析、增加持久化内存、优化提示词模板）。
    *   **DGM-H 架构**：扩展了达尔文哥德尔机，维护一个不断增长的超代理“存档”（Archive）。
*   **算法流程**：
    1.  **父代选择**：基于性能评分和子代成功率（多样性奖励）从存档中概率性选择父代。
    2.  **自我修改**：父代超代理分析过去的评估结果和自身代码，生成新的超代理变体。
    3.  **评估与入库**：在目标任务上评估新变体，若编译通过且有效则加入存档，作为未来的进化基石。

#### 3. 实验设计：数据集、Benchmark 与对比方法
*   **实验场景与数据集**：
    *   **编程**：Polyglot 基准测试（165 个任务）。
    *   **论文评审**：预测 AI 论文的接受/拒绝决策（基于 ICLR/NeurIPS 数据）。
    *   **机器人奖励设计**：在 Genesis 物理引擎中为四足机器人设计奖励函数。
    *   **奥数级数学评分**：IMO-GradingBench（对国际数学奥林匹克竞赛解答进行评分）。
*   **对比方法（Baselines）**：
    *   **DGM-H w/o self-improve**：固定元代理，仅优化任务代理（类似 ADAS 方法）。
    *   **DGM-H w/o open-ended exploration**：移除存档机制，仅保留最新变体。
    *   **Original DGM**：原始针对编程优化的 DGM。
    *   **DGM-custom**：人工为特定领域定制改进逻辑的 DGM。
    *   **静态基准**：如 AI-Scientist-v2 的评审员、ProofAutoGrader 等。

#### 4. 资源与算力
*   **算力说明**：论文未明确提及具体的 GPU 型号或数量，因为主要依赖于基础模型（FM）的 API 调用。
*   **消耗估算**：
    *   **模型使用**：主要使用 **Claude 4.5 Sonnet**（用于自我修改和机器人任务）、**GPT-4o**（用于论文评审）、**o3-mini/o4-mini**（用于编程和数学）。
    *   **Token 成本**：100 次迭代的自我修改阶段约消耗 **3300 万 tokens**；加上评估阶段，总消耗约 **8860 万 tokens**。

#### 5. 实验数量与充分性
*   **实验规模**：每个实验运行 5 次独立重复，报告中位数及 95% 置信区间。
*   **消融实验**：系统地对比了移除“自我改进”和“开放式探索”后的表现，证明了各组件的必要性。
*   **跨领域迁移实验**：验证了在一个领域（评审+机器人）学到的元级改进（如性能跟踪工具）能否加速另一个领域（数学评分）的学习。
*   **充分性评价**：实验设计严谨，包含了统计显著性检验（Wilcoxon 符号秩检验），且覆盖了从纯文本到物理模拟的多种任务，实验较为充分且客观。

#### 6. 主要结论与发现
*   **性能提升**：DGM-H 在所有测试领域均显著优于无自我改进或无开放式探索的基准。
*   **元级进化**：系统自主开发出了**持久化内存**、**性能跟踪基础设施**、**计算感知策略规划**和**自动偏差检测**等高级功能。
*   **跨领域迁移**：在 A 领域进化出的“改进能力”可以直接提升 B 领域的改进效率，证明了元级技能的通用性。
*   **超越人工**：在数学评分任务中，DGM-H 自动发现的 `BetterGrader` 超过了现有的最强静态基准 `ProofAutoGrader`。

#### 7. 优点：亮点与创新
*   **真正的自引用**：突破了元级逻辑固定的瓶颈，实现了“改进‘改进过程’的过程”。
*   **通用性强**：不依赖于任务与改进逻辑的领域对齐，适用于任何可计算任务。
*   **涌现行为**：观察到了 AI 自发编写复杂软件工程工具（如内存管理、日志分析）以辅助自身进化的现象。

#### 8. 不足与局限
*   **任务分布固定**：虽然代理在进化，但测试任务集是预设的，尚未实现任务与代理的协同进化（Co-evolution）。
*   **外层循环固定**：父代选择和评估协议等最外层逻辑仍是手工设计的（尽管附录尝试了修改父代选择）。
*   **成本高昂**：依赖大规模 FM 的 API 调用，Token 消耗巨大，且存在推理延迟。
*   **安全风险**：开放式自我改进系统可能演化出人类难以审计的行为，尽管文中使用了沙箱环境，但长期安全性仍需探讨。

（完）
