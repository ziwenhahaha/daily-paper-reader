---
title: "Action Draft and Verify: A Self-Verifying Framework for Vision-Language-Action Model"
title_zh: 动作草拟与验证：一种用于视觉-语言-动作模型的自验证框架
authors: "Chen Zhao, Zhuoran Wang, Haoyang Li, Shifeng Bao, Guanlin Li, Youhe Feng, Yang Li, Jie Tang, Jing Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.18091v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 视觉-语言-动作 (VLA) 模型的自我验证框架
tldr: 针对具身智能任务中扩散模型生成动作的高精度与自回归模型泛化性之间的权衡，本文提出ADV框架。该框架利用扩散动作专家生成多个候选动作块，并由视觉语言模型（VLM）通过单次前向传播的困惑度指标进行评分和筛选。这种“先草拟后验证”的机制结合了两种范式的优势，显著提升了机器人在复杂环境下的成功率和鲁棒性。
selection_source: fresh_fetch
motivation: 旨在结合扩散模型的高精度动作生成能力与自回归模型在分布外环境中的强泛化性。
method: 提出ADV框架，由扩散专家生成候选动作，再利用VLM的困惑度指标在单次推理中对候选动作进行评分和重排序。
result: "在模拟环境和真实世界中，该方法相比纯扩散基准分别提升了4.3%和19.7%的任务成功率。"
conclusion: ADV框架通过自验证机制有效融合了不同动作生成范式的优点，是提升VLA模型性能的有效方案。
---

## 摘要
视觉-语言-动作（VLA）模型近期在具身任务中展现出了强大的性能。现代 VLA 通常采用扩散动作专家来高效生成高精度的连续动作块，而自回归生成在底层控制方面可能较慢且精度较低。然而，自回归范式仍能提供互补的先验知识，从而提高在分布外（out-of-distribution）环境中的鲁棒性和泛化能力。为了利用这两种范式，我们提出了“动作草拟与验证”（Action-Draft-and-Verify, ADV）：由扩散动作专家草拟多个候选动作块，并由视觉语言模型（VLM）通过类困惑度（perplexity-style）指标在单次前向传播中对所有候选动作进行评分，从而选出其中一个。在相同的骨干网络、训练数据和动作块长度下，ADV 在模拟环境中的成功率比基于扩散的基准模型提高了 4.3 个百分点，在真实世界中提高了 19.7 个百分点，且仅增加了单次 VLM 重排序的开销。

## Abstract
Vision-Language-Action (VLA) models have recently demonstrated strong performance across embodied tasks. Modern VLAs commonly employ diffusion action experts to efficiently generate high-precision continuous action chunks, while auto-regressive generation can be slower and less accurate at low-level control. Yet auto-regressive paradigms still provide complementary priors that can improve robustness and generalization in out-of-distribution environments. To leverage both paradigms, we propose Action-Draft-and-Verify (ADV): diffusion action expert drafts multiple candidate action chunks, and the VLM selects one by scoring all candidates in a single forward pass with a perplexity-style metric. Under matched backbones, training data, and action-chunk length, ADV improves success rate by +4.3 points in simulation and +19.7 points in real-world over diffusion-based baseline, with a single-pass VLM reranking overhead.

---

## 论文详细总结（自动生成）

以下是对论文《Action Draft and Verify: A Self-Verifying Framework for Vision-Language-Action Model》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在视觉-语言-动作（VLA）模型中，如何平衡**高精度控制**与**环境泛化性**。
*   **背景与动机**：
    *   **扩散模型（Diffusion）**：擅长生成高精度、平滑的连续动作，但在面对分布外（OOD）环境时容易失效，表现为动作抖动、缺乏纠错能力。
    *   **自回归模型（Auto-regressive）**：利用预训练大语言模型（LLM）的先验知识，具有更强的鲁棒性和结构化泛化能力，但在低级控制精度和推理速度上表现较差。
    *   **研究目标**：提出一种轻量级框架，结合扩散模型的高精度草拟能力和自回归 VLM 的语义验证能力，提升机器人在复杂及未知环境下的表现。

### 2. 方法论：ADV 框架
ADV（Action Draft and Verify）框架分为两个阶段：
*   **草拟阶段（Draft）**：
    *   利用扩散动作专家（Diffusion Action Expert）基于 VLM 提取的特征，通过注入不同的噪声，并行生成 $M$ 个候选动作轨迹块（Action Chunks）。
*   **验证阶段（Verify）**：
    *   **Textual FAST 编码**：为了让 VLM 能够可靠地对动作评分，提出 Textual FAST 技术。它先用 FAST 算法压缩动作，再将其渲染为文本字符串，最后使用 VLM 原生的分词器（Tokenizer）进行处理，避免了引入低频的动作专用词汇。
    *   **类困惑度评分**：将候选动作序列输入 VLM，在单次前向传播中计算每个序列的长度归一化负对数似然（NLL）得分。
    *   **筛选决策**：选择得分最低（即 VLM 认为最符合逻辑/先验）的动作块作为最终执行指令。
*   **协同训练（Co-training）**：模型同时优化自回归损失（$L_{VLM}$）和扩散流匹配损失（$L_{dif}$），通过权重因子 $\beta$ 平衡两者的收敛速度。

### 3. 实验设计
*   **数据集与场景**：
    *   **模拟环境**：LIBERO（包含 Spatial, Object, Goal, Long 四类任务）和 RoboTwin 2.0（包含 Easy 和 Hard/OOD 模式）。
    *   **真实世界**：设计了推方块、清理桌面、指令化取放、挂杯子等 4 类任务，包含 34 种指令类型。
*   **Benchmark 与对比方法**：
    *   **基准模型**：Qwen2.5-VL-3B、InternVL3.5-2B、$\pi_{0.5}$。
    *   **对比范式**：纯自回归（Model-FAST）、纯扩散（Model-Diffusion）以及 SOTA 模型（如 OpenVLA, Octo, $\pi_0$ 等）。
*   **评估指标**：成功率（Success Rate）、平均成功步数、恢复尝试次数、抓取前环境碰撞率。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **训练参数**：提及了 Batch Size（120-144）、学习率（$2\times10^{-5}$ 到 $5\times10^{-5}$）、Epoch（32-96）。
*   **模型规模**：动作专家模型约为 5 亿参数（500M），VLM 骨干为 2B-3B 规模。

### 5. 实验数量与充分性
*   **实验规模**：
    *   模拟器实验涵盖了 LIBERO 的 1693 条轨迹和 RoboTwin 的 2500 条轨迹。
    *   真实世界实验收集了超过 2000 条轨迹，涉及 50 多种物体。
*   **充分性评价**：实验设计较为**充分且客观**。作者不仅对比了整体成功率，还通过“K-th Best”实验验证了验证器的筛选逻辑，通过噪声注入实验测试了鲁棒性，并对比了多种动作分词（Tokenization）方法。
*   **公平性**：所有对比实验均在相同的骨干网络、训练数据和动作块长度下进行，确保了“苹果对苹果”的公平比较。

### 6. 主要结论与发现
*   **性能提升**：ADV 在模拟环境中比纯扩散基准提升了 4.3% 的成功率，在真实世界中提升了 **19.7%**。
*   **验证机制的本质**：VLM 验证器的核心作用是**“过滤掉坏动作”**而非精准找到“唯一的最佳动作”。实验显示，选择前 3 名的动作效果相近，但选择排名靠后的动作时性能骤降。
*   **OOD 鲁棒性**：在分布外环境下，ADV 显著增加了机器人的“恢复尝试次数”，并降低了因动作抖动导致的无效碰撞。
*   **Textual FAST 的有效性**：将动作转化为文本表示能更好地对齐 VLM 的预训练分布，使评分更准确。

### 7. 优点与亮点
*   **即插即用**：ADV 是一种推理侧的增强框架，不需要改变 VLM 的核心架构，具有较强的通用性。
*   **高效验证**：通过单次批处理前向传播完成 $M$ 个候选动作的评分，比逐 Token 生成动作的自回归模型效率更高。
*   **协同效应**：成功结合了扩散模型在连续空间的高精度和 VLM 在语义逻辑上的强先验。

### 8. 不足与局限
*   **推理延迟**：虽然比纯自回归快，但由于需要生成多个候选块并进行 VLM 评分，其推理频率（Hz）相比纯扩散模型有所下降（例如从 94.7Hz 降至 53.3Hz）。
*   **候选质量依赖**：ADV 的上限取决于扩散专家生成的候选池。如果扩散模型在极端环境下无法生成任何可行的候选动作，验证阶段也无法挽回失败。
*   **算力细节缺失**：缺乏具体的硬件资源消耗报告，不便于其他研究者评估复现成本。

（完）
