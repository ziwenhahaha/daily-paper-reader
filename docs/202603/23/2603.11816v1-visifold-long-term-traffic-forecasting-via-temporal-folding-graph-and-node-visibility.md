---
title: "VisiFold: Long-Term Traffic Forecasting via Temporal Folding Graph and Node Visibility"
title_zh: VisiFold：基于时间折叠图与节点可见性的长期交通预测
authors: "Zhiwei Zhang, Xinyi Du, Weihao Wang, Xuanchi Guo, Wenjuan Han"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11816v1"
tags: ["query:urt-pf"]
score: 8.0
evidence: 通过时间折叠图和时空依赖进行长期交通预测
tldr: "针对长期交通预测中计算资源消耗大和时空依赖复杂的问题，本文提出VisiFold框架。该框架通过时间折叠图将多个时间快照整合为单一图结构，并引入节点可见性机制（包括掩码和子图采样）来降低计算开销。实验证明，VisiFold在显著减少资源消耗的同时，在长期预测任务上优于现有基准，即使在80%的高掩码率下也能保持性能优势。"
selection_source: carryover_cache
motivation: 现有的时空图模型在长期预测中面临计算资源膨胀和时空维度分离导致的碎片化问题。
method: 提出时间折叠图整合时间序列，并结合节点可见性机制进行掩码和采样以优化计算效率。
result: VisiFold在长期预测任务中表现优于现有模型，且在极高掩码率下仍能保持性能领先。
conclusion: 该研究通过打破时空维度的资源限制，为高效且准确的长期交通预测提供了有效的解决方案。
---

## 摘要
交通预测是智能交通系统的基石。尽管现有研究在短期预测方面取得了显著进展，但长期预测在很大程度上仍是一个尚未探索且具有挑战性的前沿领域。延长预测时界会加剧两个关键问题：计算资源消耗的不断攀升以及日益复杂的时空依赖关系。目前依赖时空图并分别处理时间与空间维度的方法，面临着快照堆叠膨胀（snapshot-stacking inflation）和跨步碎片化（cross-step fragmentation）的问题。为了克服这些局限性，我们提出了 VisiFold。我们的框架引入了一种新颖的时间折叠图，将一系列时间快照整合到单个图中。此外，我们提出了一种节点可见性机制，结合了节点级掩码和子图采样，以克服大规模节点数量带来的计算瓶颈。大量实验表明，VisiFold 不仅大幅降低了资源消耗，而且在长期预测任务中优于现有的基准模型。值得注意的是，即使在 80% 的高掩码率下，VisiFold 仍能保持其性能优势。通过有效打破时间与空间维度的资源约束，我们的工作为更具现实意义的长期交通预测铺平了道路。代码可在 https://github.com/PlanckChang/VisiFold 获取。

## Abstract
Traffic forecasting is a cornerstone of intelligent transportation systems. While existing research has made significant progress in short-term prediction, long-term forecasting remains a largely uncharted and challenging frontier. Extending the prediction horizon intensifies two critical issues: escalating computational resource consumption and increasingly complex spatial-temporal dependencies. Current approaches, which rely on spatial-temporal graphs and process temporal and spatial dimensions separately, suffer from snapshot-stacking inflation and cross-step fragmentation. To overcome these limitations, we propose \textit{VisiFold}. Our framework introduces a novel temporal folding graph that consolidates a sequence of temporal snapshots into a single graph. Furthermore, we present a node visibility mechanism that incorporates node-level masking and subgraph sampling to overcome the computational bottleneck imposed by large node counts. Extensive experiments show that VisiFold not only drastically reduces resource consumption but also outperforms existing baselines in long-term forecasting tasks. Remarkably, even with a high mask ratio of 80\%, VisiFold maintains its performance advantage. By effectively breaking the resource constraints in both temporal and spatial dimensions, our work paves the way for more realistic long-term traffic forecasting. The code is available at~ https://github.com/PlanckChang/VisiFold.

---

## 论文详细总结（自动生成）

以下是对论文《VisiFold: Long-Term Traffic Forecasting via Temporal Folding Graph and Node Visibility》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的交通预测模型在处理**长期预测**（Long-term forecasting）时面临两大瓶颈：
    1.  **快照堆叠膨胀（Snapshot-stacking inflation）**：随着预测步长增加，时空图序列导致计算资源（显存和时间）呈爆炸式增长。
    2.  **跨步碎片化（Cross-step fragmentation）**：传统方法将空间和时间维度分离处理，导致长程时间依赖被切碎在不同快照中，难以有效捕获。
*   **研究背景**：大多数研究集中在 1 小时内的短期预测，而城市管理需要更长远的预见性。现有时空图神经网络（STGNNs）在长时预测上往往精度下降且成本高昂。

### 2. 论文提出的方法论
VisiFold 框架通过重新定义输入表示和引入可见性机制来解决上述问题：
*   **时间折叠图（Temporal Folding Graph, TFG）**：
    *   **核心思想**：将一个节点在多个时间快照中的属性“折叠”到单个节点的增强属性向量中。
    *   **效果**：将 $N$ 个节点 $\times T$ 个快照的序列压缩为包含 $N$ 个节点的单一图。这消除了跨步消息传递，提高了信息密度，实现了同步的时空建模。
*   **节点可见性（Node Visibility）**：
    *   **节点级掩码（Node-level Masking）**：随机移除一定比例的节点（训练时），使其对编码器不可见。这不仅降低了计算开销，还作为隐式正则化器减少了位置依赖。
    *   **子图采样（Subgraph Sampling）**：将剩余节点划分为固定大小的子图，限制每个节点的感受野，增强计算并行性。
*   **网络架构**：
    *   **嵌入融合**：融合折叠后的属性嵌入、可学习的空间嵌入以及基于时间戳的时间嵌入（Day-of-week, Time-of-day）。
    *   **主干网络**：采用标准的 Transformer 编码器处理子图内的节点交互。
    *   **预测头**：使用简单的 MLP 生成最终预测序列。

### 3. 实验设计
*   **数据集**：使用了三个真实世界数据集：**PEMS04**、**PEMS08**（流量数据）和 **SEATTLE**（速度数据）。
*   **预测场景**：设定预测步长为 24、36 和 48 步（对应长时预测）。
*   **对比基准（Baselines）**：共对比了 12 种方法，涵盖：
    *   统计方法：HA, VAR。
    *   时空图神经网络：DCRNN, GWNet, AGCRN, DMSTGCN, STPGNN。
    *   注意力/Transformer 机制：GMAN, SSTBAN, STAEformer, STDN。
    *   MLP 类方法：STID。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA RTX 3090 (24GB)** GPU 上完成。
*   **效率表现**：
    *   **训练速度**：相比最强基准 STAEformer，VisiFold 训练速度提升了约 **17.8 倍**。
    *   **显存占用**：显存消耗降低了约 **15.7 倍**（平均值）。
    *   **推理时间**：在 PEMS04 数据集上，48 步预测的推理时间仅为 **0.86 秒**。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个数据集、3 种预测步长下进行了全面测试，并提供了误差棒（Error Bars）以展示稳定性。
*   **消融实验**：非常充分，涵盖了：
    *   嵌入组件（空间、时间嵌入）的作用。
    *   节点可见性（掩码 vs 采样）的贡献。
    *   **掩码率分析**：测试了从 0 到 0.9 的掩码率，发现即使掩码 80%-90% 的节点，性能依然稳健。
    *   **折叠策略对比**：对比了“时间折叠”与“空间折叠”，证明了时间折叠的优越性。
*   **客观性**：通过可视化 t-SNE 嵌入和误差分布图，从定性角度解释了模型捕获时空模式的能力，实验设计公平且逻辑严密。

### 6. 主要结论与发现
*   **性能领先**：VisiFold 在所有长时预测任务中均达到 SOTA 性能。
*   **冗余性发现**：交通数据存在巨大冗余，高比例的节点掩码不仅不损害性能，反而能通过强制模型学习多跳依赖来提升鲁棒性。
*   **拓扑依赖性**：研究发现，非邻近节点也可能具有极高的相似性，VisiFold 证明了在不依赖预定义物理拓扑的情况下，依然能通过注意力机制学习到有效的空间关系。

### 7. 优点
*   **极高的效率**：通过 TFG 彻底解决了长时预测中的计算膨胀问题，适合大规模路网。
*   **创新的表示学习**：将时间序列“折叠”入节点属性，简化了模型结构，无需复杂的时间卷积或循环单元。
*   **强鲁棒性**：节点可见性机制使其在数据缺失或大规模路网下具有极强的适应力。

### 8. 不足与局限
*   **纯数据驱动限制**：模型无法应对突发事件（如事故、封路），因为它仅依赖历史模式。
*   **全局信息损失风险**：虽然子图采样提高了效率，但理论上可能损失极远距离的全局依赖（尽管文中讨论认为这种损失在交通预测中影响较小）。
*   **多模态缺失**：目前仅处理单一交通信号，未整合天气、POI 等外部元数据。

（完）
