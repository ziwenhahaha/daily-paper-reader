---
title: "POET: Power-Oriented Evolutionary Tuning for LLM-Based RTL PPA Optimization"
title_zh: POET：面向功耗的演化调优，助力基于大语言模型的 RTL PPA 优化
authors: "Heng Ping, Peiyu Zhang, Zhenkun Wang, Shixuan Li, Anzhe Cheng, Wei Yang, Paul Bogdan, Shahin Nazarian"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19333v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: LLM驱动的演化框架用于多目标优化
tldr: "本研究提出POET框架，旨在解决利用大语言模型（LLM）优化RTL代码PPA时面临的幻觉导致的功能错误及多目标权衡难题。POET通过基于差异测试的测试平台生成确保功能正确性，并结合LLM驱动的进化机制，利用功耗优先的排序策略在帕累托前沿搜索低功耗方案。实验表明，POET在40个设计中均实现了100%的功能正确性和最优的功耗优化效果。"
selection_source: fresh_fetch
motivation: 针对LLM在RTL优化中存在的幻觉问题以及在PPA多目标权衡中难以系统性优先降低功耗的挑战。
method: 采用基于差异测试的验证流水线确保功能一致性，并结合非支配排序与功耗优先的进化策略引导搜索。
result: "在RTL-OPT基准测试的40个设计中，POET实现了100%的功能正确性，并在所有设计上取得了最佳的功耗优化结果。"
conclusion: POET证明了结合进化策略与LLM能有效在保证硬件功能正确性的前提下，实现显著的功耗导向PPA优化。
---

## 摘要
将大语言模型（LLMs）应用于 RTL 代码优化以提升功耗、性能和面积（PPA）面临两个关键挑战：一是在 LLM 存在幻觉的情况下确保优化设计的功能正确性；二是在多目标 PPA 权衡空间中系统地优先考虑功耗降低。我们提出了 POET（Power-Oriented Evolutionary Tuning），这是一个同时解决这两个挑战的框架。针对功能正确性，POET 引入了一种基于差分测试的测试平台（testbench）生成流水线，将原始设计视为功能参考模型（oracle），利用确定性仿真生成黄金参考（golden references），从而在验证过程中消除 LLM 幻觉。针对 PPA 优化，POET 采用了一种 LLM 驱动的演化机制，结合非支配排序、功耗优先的层内排名以及比例幸存者选择，在无需手动调整权重的情况下，引导搜索向 Pareto 前沿的低功耗区域靠拢。在包含 40 个不同 RTL 设计的 RTL-OPT 基准测试上进行的评估表明，POET 实现了 100% 的功能正确性，在所有 40 个设计中均获得了最佳功耗，并在面积和延迟改进方面具有竞争力。

## Abstract
Applying large language models (LLMs) to RTL code optimization for improved power, performance, and area (PPA) faces two key challenges: ensuring functional correctness of optimized designs despite LLM hallucination, and systematically prioritizing power reduction within the multi-objective PPA trade-off space. We propose POET (Power-Oriented Evolutionary Tuning), a framework that addresses both challenges. For functional correctness, POET introduces a differential-testing-based testbench generation pipeline that treats the original design as a functional oracle, using deterministic simulation to produce golden references and eliminating LLM hallucination from the verification process. For PPA optimization, POET employs an LLM-driven evolutionary mechanism with non-dominated sorting, power-first intra-level ranking, and proportional survivor selection to steer the search toward the low-power region of the Pareto front without manual weight tuning. Evaluated on the RTL-OPT benchmark across 40 diverse RTL designs, POET achieves 100% functional correctness, the best power on all 40 designs, and competitive area and delay improvements.

---

## 论文详细总结（自动生成）

### POET：面向功耗的演化调优，助力基于大语言模型的 RTL PPA 优化

#### 1. 核心问题与研究动机
在利用大语言模型（LLM）进行集成电路寄存器传输级（RTL）代码优化时，存在两个核心挑战：
*   **功能正确性难以保障：** LLM 固有的“幻觉”现象常导致生成的优化代码在逻辑功能上与原设计不一致。现有的验证方法（如手动编写测试平台或让 LLM 自行生成测试平台）要么效率低下，要么可靠性不足。
*   **多目标 PPA 权衡的复杂性：** 功耗（Power）、性能（Performance/Delay）和面积（Area）之间存在复杂的权衡。现有方法多采用加权求和的单一适应度函数，这需要繁琐的手动调参，且难以在不损失帕累托最优性的前提下系统性地优先降低功耗。

#### 2. 方法论
POET 框架由两个核心组件构成：
*   **基于差异测试的测试平台生成流水线：**
    *   **核心思想：** 将原始（虽然 PPA 非最优但功能正确）的设计视为“功能参考模型（Oracle）”。
    *   **流程：** 首先利用 LLM 提取原始设计的规格说明（Spec）；接着由 LLM 生成覆盖边界和典型场景的输入激励（Stimuli）；然后将激励输入原始设计进行确定性仿真，捕获黄金输出信号；最后将输入输出对组装成自动校验的测试平台。这种方法避免了让 LLM 直接推理时序电路输出，消除了幻觉干扰。
*   **面向功耗的演化优化引擎：**
    *   **演化算子：** 包含改进（Improve）、重构（Refactor）、探索（Explore）、简化（Simplify）、融合（Fusion）和交叉（Crossover）六类算子，并在提示词中嵌入了电路优化领域知识。
    *   **选择机制：** 采用非支配排序（Non-dominated Sorting）将种群划分为不同的帕累托等级。在同一等级内，根据功耗升序进行排名（功耗越低排名越高）。
    *   **比例幸存者选择：** 按照等级优先级分配幸存名额，确保高优先级（低功耗且帕累托占优）个体保留的同时，也维持种群的多样性。

#### 3. 实验设计
*   **数据集/基准测试：** 使用 **RTL-OPT** 基准测试，包含 40 个涵盖算术电路、控制逻辑和数据通路模块的专家级 RTL 设计。
*   **对比方法：**
    1.  **I/O Prompting：** 直接提示 LLM 进行优化。
    2.  **Chain-of-Thought (CoT)：** 引导 LLM 进行逐步推理优化。
    3.  **REvolution：** 最先进的基于加权适应度函数的演化框架。
*   **工具链：** 使用 GPT-4o-mini 作为底层 LLM；使用 Yosys 进行逻辑综合；OpenSTA 进行时序和功耗分析；Icarus Verilog 进行功能仿真；采用 NanGate 45nm 标准单元库。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练/运行时长。
*   实验设定中提到：为了公平对比，所有方法使用的 LLM 生成调用总数保持一致。
*   超参数设置：种群规模 $N=10$，每代产生个体数 $\lambda=10$，最大进化代数 $G=10$，修复尝试次数 $R=3$。

#### 5. 实验数量与充分性
*   **实验规模：** 在 40 个不同的 RTL 设计上进行了测试，涵盖了从简单的加法器到复杂的 FSM 和 64位 ALU。
*   **消融实验：** 针对差异测试、演化机制、功耗排名、比例分配和领域知识五个维度分别进行了消融实验，验证了各组件的有效性。
*   **客观性：** 实验通过自动化工具（Yosys, OpenSTA）获取客观的 PPA 指标，并结合了功能仿真和等效性检查，评估维度全面且客观。

#### 6. 主要结论与发现
*   **功能正确性：** POET 实现了 **100%** 的功能正确性（40/40），显著优于 REvolution (90%)、CoT (82.5%) 和 I/O (72.5%)。
*   **功耗优化：** 在所有 40 个设计中，POET 均取得了**最佳的功耗结果**。
*   **综合性能：** 在 72.5% 的设计上取得了最佳面积，在 67.5% 的设计上取得了最佳延迟。
*   **发现：** 即使在设计接近帕累托前沿时，POET 也能通过受控的权衡（例如牺牲极少量的延迟）来进一步挖掘功耗降低的空间。

#### 7. 优点
*   **验证闭环：** 巧妙地利用原始设计作为 Oracle，解决了 LLM 在硬件验证中的可靠性难题。
*   **无需调参：** 通过非支配排序和功耗优先排名，无需手动设置 PPA 权重即可实现功耗导向的优化。
*   **领域知识集成：** 在演化算子中融入了具体的电路优化策略（如门控时钟、操作数隔离等），提高了搜索效率。

#### 8. 不足与局限
*   **基准测试局限：** 虽然涵盖了 40 个设计，但主要集中在中小规模模块，对于超大规模集成电路（VLSI）全系统的优化效果尚待验证。
*   **工艺库单一：** 实验仅基于 NanGate 45nm 工艺库，不同工艺节点（如 7nm 或更先进工艺）下的优化表现可能存在差异。
*   **成本考量：** 演化算法涉及多次 LLM 调用和逻辑综合仿真，在大规模应用时可能面临较高的计算成本和 API 调用费用。

（完）
