---
title: Evaluating the Robustness of Reinforcement Learning based Adaptive Traffic Signal Control
title_zh: 评估基于强化学习的自适应交通信号控制的鲁棒性
authors: "Dickens Kwesiga, Angshuman Guin, Khaled Abdelghany, Michael Hunter"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15283v1"
tags: ["query:urt-c"]
score: 8.0
evidence: 基于强化学习的自适应交通信号控制
tldr: 本研究针对强化学习在交通信号控制中存在的相位结构简化、鲁棒性评估不足及训练效率低等问题，提出了一种支持标准八相位环障结构的RL算法。通过分布式异步训练架构提升效率，并在多种交通流量和O-D需求模式下进行测试。结果表明，该算法在减少延迟方面显著优于传统感应控制，且通过多样化需求训练的模型展现出极强的泛化能力和鲁棒性，为实际部署提供了参考。
selection_source: fresh_fetch
motivation: 现有的强化学习交通信号控制研究往往简化信号相位且缺乏在多变交通需求下的鲁棒性验证。
method: 开发了符合实地标准的八相位环障结构RL算法，并利用分布式异步架构实现多节点并行仿真训练。
result: "实验显示RL控制比优化后的感应控制减少了11-32%的平均延迟，且在处理未见过的异构需求时表现优异。"
conclusion: 采用多样化交通模式训练的强化学习模型能有效应对复杂多变的交通环境，具备替代传统控制方法的潜力。
---

## 摘要
强化学习 (RL) 因其无需模型的特性，能够直接通过与交通环境的交互来学习控制策略，在自适应交通信号控制领域引起了越来越多的关注。然而，在基于强化学习的信号控制被认为具备实地部署条件之前，仍面临若干挑战。许多现有研究依赖于简化的信号配时结构，训练模型在多变交通需求条件下的鲁棒性尚未得到充分评估，且在交通微观仿真环境中训练强化学习算法时，运行效率仍是一个挑战。本研究制定了一种基于强化学习的信号控制算法，能够表示与实地信号控制器一致的完整八相位环障 (ring-barrier) 配置。该算法在不同的交通需求条件下进行了训练和评估，并以现有的感应信号控制 (ASC) 作为基准。为了评估鲁棒性，在具有不同结构相似水平的多种交通量和起讫点 (O-D) 需求模式下进行了实验。为了提高训练效率，实施了一种分布式异步训练架构，实现了跨多个计算节点的并行仿真。案例研究交叉口的结果表明，所提出的基于强化学习的信号控制显著优于优化的感应信号控制，使各流向的平均延迟降低了 11-32%。在单一 O-D 模式下训练的模型在类似的未见需求模式下具有良好的泛化能力，但在需求条件显著不同时性能会下降。相比之下，在多样化 O-D 模式下训练的模型表现出极强的鲁棒性，即使在高度不同的未见需求场景下，其表现也始终优于感应信号控制。

## Abstract
Reinforcement learning (RL) has attracted increasing interest for adaptive traffic signal control due to its model-free ability to learn control policies directly from interaction with the traffic environment. However, several challenges remain before RL-based signal control can be considered ready for field deployment. Many existing studies rely on simplified signal timing structures, robustness of trained models under varying traffic demand conditions remains insufficiently evaluated, and runtime efficiency continues to pose challenges when training RL algorithms in traffic microscopic simulation environments. This study formulates an RL-based signal control algorithm capable of representing a full eight-phase ring-barrier configuration consistent with field signal controllers. The algorithm is trained and evaluated under varying traffic demand conditions and benchmarked against state-of-the-practice actuated signal control (ASC). To assess robustness, experiments are conducted across multiple traffic volumes and origin-destination (O-D) demand patterns with varying levels of structural similarity. To improve training efficiency, a distributed asynchronous training architecture is implemented that enables parallel simulation across multiple computing nodes. Results from a case study intersection show that the proposed RL-based signal control significantly outperforms optimized ASC, reducing average delay by 11-32% across movements. A model trained on a single O-D pattern generalizes well to similar unseen demand patterns but degrades under substantially different demand conditions. In contrast, a model trained on diverse O-D patterns demonstrates strong robustness, consistently outperforming ASC even under highly dissimilar unseen demand scenarios.

---

## 论文详细总结（自动生成）

### 论文总结：评估基于强化学习的自适应交通信号控制的鲁棒性

#### 1. 核心问题与研究动机
*   **研究背景**：强化学习（RL）在自适应交通信号控制（ATSC）中展现出巨大潜力，但距离实际部署仍有四大挑战：
    1.  **相位结构简化**：现有研究多采用简化的相位，而实际交通控制器使用复杂的“双环障”（Ring-Barrier）结构。
    2.  **鲁棒性评估不足**：模型通常在单一交通需求下训练和测试，难以应对多变的交通流模式。
    3.  **训练效率低下**：微观仿真环境与 RL 智能体之间的频繁交互导致计算开销巨大。
    4.  **基准测试不切实际**：常以“定时控制”为基准，而非行业标准的“感应控制”（ASC）。
*   **核心问题**：如何构建一个符合实地标准、训练高效且在多变 O-D（起讫点）需求下具备强鲁棒性的 RL 信号控制系统？

#### 2. 方法论
*   **核心算法**：采用 **近端策略优化（PPO）** 算法，这是一种 Actor-Critic 架构的策略梯度方法，通过剪切概率比率来保证训练的稳定性。
*   **信号结构**：实现了完整的 **八相位双环障配置**。通过 **无效动作掩码（IAM）** 技术，确保智能体仅在符合安全约束（如最小绿灯、清空时间、屏障限制）的动作空间内进行选择。
*   **状态与奖励定义**：
    *   **状态 (State)**：各车道车辆数 + 当前相位的已消耗绿灯时间。
    *   **动作 (Action)**：选择下一对兼容的信号相位。
    *   **奖励 (Reward)**：所有进出口道车辆归一化延迟的负总和。
*   **分布式架构**：开发了 **分布式异步训练架构**。由一个中央学习节点（Learner）和多个并行运行 SUMO 仿真实例的工作节点（Worker）组成，解耦了仿真采样与策略更新，显著提升了训练吞吐量。

#### 3. 实验设计
*   **仿真环境**：使用 SUMO 微观交通仿真软件。
*   **场景设置**：一个典型的四路交叉口，包含左转专用道和保护相位。
*   **基准方法 (Benchmark)**：使用 **Synchro 软件优化后的全感应信号控制 (ASC)**。
*   **对比模型**：
    *   **Model I**：仅在单一 O-D 模式（模式 A）下训练。
    *   **Model II**：在多样化 O-D 模式（模式 A, B, C）下训练。
*   **评估指标**：平均车辆延迟，并使用 **结构相似性指数（SSIM）** 量化不同 O-D 需求模式之间的差异性。

#### 4. 资源与算力
*   **算力说明**：论文提到了使用分布式架构和“多个计算节点/PC”，但**未明确说明具体的 GPU 型号、CPU 核心数或具体的硬件配置**。
*   **训练时长**：模型训练持续了 5000 个回合（Episodes），每个回合对应 3600 秒的仿真时间。Model I 在约 500 回合收敛，Model II 由于需求多样性，在约 1250 回合趋于稳定。

#### 5. 实验数量与充分性
*   **实验规模**：设计了 8 种不同的 O-D 模式（A-H），涵盖了从 0.5 到 1.05 的不同饱和度（v/c 比）。
*   **充分性**：
    *   针对每种控制策略进行了 10 次重复仿真实验以消除随机性影响。
    *   通过 SSIM 矩阵量化了测试集与训练集的“异构程度”，从而科学地评估泛化能力。
    *   实验设计较为客观，对比了优化后的行业标准 ASC，而非弱势的定时控制。

#### 6. 主要结论与发现
*   **性能提升**：RL 控制在所有流向上均显著优于优化的 ASC，平均延迟降低了 **11% 至 32%**。
*   **鲁棒性规律**：
    *   在单一模式下训练的模型（Model I）在面对高度异构（SSIM 差异大）的未见需求时，性能会显著退化，甚至不如 ASC。
    *   在多样化模式下训练的模型（Model II）表现出极强的鲁棒性，即使在完全未见的极端需求下，依然能保持优于 ASC 的表现。
*   **训练权衡**：增加训练数据的多样性虽然会减慢收敛速度，但能显著提升模型在现实复杂环境中的适用性。

#### 7. 优点与亮点
*   **工程实用性强**：直接支持标准的八相位环障结构，比大多数简化相位的 RL 研究更接近实地部署。
*   **分布式创新**：提出的异步分布式架构有效解决了微观仿真训练慢的痛点。
*   **评估维度科学**：引入 SSIM 指标来量化交通需求的结构差异，为 RL 鲁棒性评估提供了严谨的数学框架。

#### 8. 不足与局限
*   **场景单一**：仅针对单个孤立交叉口进行了研究，未探讨多交叉口协同（干线协调）下的鲁棒性。
*   **理想化假设**：假设车辆检测数据是完美的（Connected Vehicle 数据），未考虑现实中传感器故障、检测误差或通信延迟对模型的影响。
*   **网络拓扑限制**：实验基于假设的交叉口几何结构，未在具有复杂几何形状或真实路网数据的场景下验证。

（完）
