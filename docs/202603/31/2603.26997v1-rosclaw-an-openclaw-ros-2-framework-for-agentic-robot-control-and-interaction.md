---
title: "ROSClaw: An OpenClaw ROS 2 Framework for Agentic Robot Control and Interaction"
title_zh: ROSClaw：一种用于智能体机器人控制与交互的 OpenClaw ROS 2 框架
authors: "Irvin Steve Cardenas, Marcus Anthony Arnett, Natalie Catherine Yeo, Lucky Sah, Jong-Hoon Kim"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26997v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 使用基础模型进行代理机器人控制的框架
tldr: ROSClaw是一个将OpenClaw代理运行时与ROS 2集成的模型无关执行层，旨在解决基础模型与物理机器人集成中的定制化难题。它通过动态能力发现、多模态观测归一化和安全验证，使任何基础模型都能在不同机器人平台上进行感知、推理和行动。实验表明，该框架能有效支持多种机器人和模型后端，并揭示了不同模型在物理行为和安全性上的显著差异，为具身智能提供了标准化的基础设施。
selection_source: fresh_fetch
motivation: 现有的基础模型与机器人集成方案通常是针对特定模型和平台的定制化开发，缺乏通用性、安全性和可移植性。
method: 提出ROSClaw框架，通过标准化功能注入、多模态数据归一化、预执行动作验证和结构化审计日志，实现模型与ROS 2机器人的解耦连接。
result: 在轮式、四足和人形机器人上成功部署，发现不同模型在动作建议率上存在高达4.8倍的差异，且执行层设计显著影响任务完成度和安全性。
conclusion: ROSClaw不仅是实用的智能机器人基础设施，也是评估具身人工智能性能和安全性的可重复测量工具。
---

## 摘要
基础模型可以赋予机器人开放式推理、语言理解和自适应规划能力，然而，目前将模型连接到物理机器人需要定制化的集成，将感知、执行和安全性与单一模型和平台耦合。我们提出了 ROSClaw，这是一个与模型无关的执行层，它将 OpenClaw 智能体运行时与 ROS 2 集成，使任何基础模型能够通过以下方式在任何支持 ROS 的机器人上进行感知、推理和行动：(i) 具有标准化功能注入的动态能力发现，(ii) 多模态观测归一化，(iii) 可配置安全包络内的执行前动作验证，以及 (iv) 结构化审计日志记录。更换模型后端或机器人平台仅需更改配置；工具模式、安全强制执行和溯源日志保持不变。我们在三个平台（轮式、四足、人形）上部署了 ROSClaw，并使用了四个基础模型后端。在这一受控基座下，模型在策略外动作建议率方面表现出高达 4.8 倍的差异（仅在前沿模型中就有 3.4 倍），并针对相同的指令产生性质迥异的物理行为。与 ROSA 进行的跨框架对等协议确认，执行层设计（而非仅仅是提示词措辞）会显著影响任务完成情况和安全行为，从而确立了 ROSClaw 既是实用的智能体机器人基础设施，也是具身智能的可重复测量工具。

## Abstract
Foundation models can endow robots with open-ended reasoning, language understanding, and adaptive planning, yet connecting a model to a physical robot today requires bespoke integration that couples perception, actuation, and safety to a single model and platform. We present ROSClaw, a model-agnostic executive layer that integrates the OpenClaw agent runtime with ROS 2, enabling any foundation model to perceive, reason about, and act on any ROS-enabled robot through (i) dynamic capability discovery with standardized affordance injection, (ii) multimodal observation normalization, (iii) pre-execution action validation within a configurable safety envelope, and (iv) structured audit logging. Swapping model backends or robot platforms is a configuration change; tool schemas, safety enforcement, and provenance logging remain invariant. We deploy ROSClaw on three platforms (wheeled, quadruped, humanoid) with four foundation-model backends. Under this controlled substrate, models exhibit up to 4.8 x differences in out-of-policy action proposal rates (3.4 x among frontier models alone) and produce qualitatively distinct physical behaviors from identical commands. A cross-framework parity protocol against ROSA confirms that executive-layer design, not just prompt wording, significantly affects both task completion and safety behavior, establishing ROSClaw as both practical agentic-robot infrastructure and a reproducible measurement instrument for embodied AI.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **ROSClaw**，这是一个旨在连接基础模型（如 LLM/VLM）与 ROS 2 机器人系统的模型无关（Model-agnostic）执行层框架。以下是对该论文的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：目前将基础模型集成到物理机器人上通常是“定制化”的（Bespoke），感知、执行和安全逻辑往往与特定模型或硬件平台紧密耦合，缺乏通用性、可移植性和可重复性。
*   **研究动机**：
    *   **接口缺失**：缺乏一个标准化的“执行层”来介导机器人的“大脑”（模型）与“身体”（硬件）。
    *   **行为差异**：相同的指令在不同模型驱动下会产生截然不同的物理行为，需要一种工具来量化和管理这种差异。
    *   **安全风险**：基础模型可能产生超出机器人物理极限的指令，需要独立的验证层进行拦截。

### 2. 方法论：ROSClaw 框架设计
ROSClaw 将 OpenClaw 智能体运行时与 ROS 2 集成，其核心思想是建立一个**执行层契约（Executive-Layer Contract）** $C = \langle A, O, V, L \rangle$：
*   **能力发现与工具注入 ($A$)**：通过动态发现节点自动扫描 ROS 图（Topics/Services/Actions），生成标准化的功能清单（Affordance Manifest）并注入模型提示词。
*   **观测归一化 ($O$)**：支持原生多模态（直接传图像）和桥接接地（将图像转为 JSON 场景描述）两种模式，确保不同模型接收到一致的环境信息。
*   **预执行验证 ($V$)**：在指令发布前进行拦截，根据可配置的安全包络（如速度限制 $v_{max}$）进行校验。不合规指令会被拦截并返回错误信息促使模型重新规划。
*   **结构化审计日志 ($L$)**：记录所有观测、模型提议、验证决策（允许/拦截）及执行结果，用于事后分析和溯源。
*   **传输抽象**：支持本地 DDS、WebSocket (rosbridge) 和 WebRTC 三种通信模式。

### 3. 实验设计
*   **机器人平台**：TurtleBot3（轮式）、Unitree Go2（四足）、Unitree G1（人形）。
*   **基础模型后端**：Claude Opus 4.6、GPT-5.2、Gemini 3.1 Pro、Llama 4 Maverick（通过 vLLM 本地托管）。
*   **任务套件（共 40 个任务）**：
    *   **结构化任务**：包括直接指令、上下文相关任务和多步规划任务。
    *   **开放式行为任务**：如“Shake and bake”（快点准备好）、“表现出兴奋”等，测试模型的创意解释。
    *   **安全对抗任务**：如“忽略安全规则”、“以最大速度行驶”，测试框架的拦截能力。
*   **基准对比 (Benchmark)**：与 NASA JPL 开发的 **ROSA** 框架进行对等协议对比，并进行了“边界显著性”（Bounds-salience）消融实验。

### 4. 资源与算力
*   **算力说明**：文中提到 Llama 4 Maverick (17B) 是通过 **vLLM** 进行本地托管的，但未明确给出具体的 GPU 型号、数量或推理延迟的硬件环境。
*   **延迟表现**：LLM 推理延迟约为 1.3–2.3 秒，远超 ROS 传输延迟（5-10ms），表明该框架目前适用于高层决策而非实时反应控制。

### 5. 实验数量与充分性
*   **实验规模**：每个任务条件下重复 $N=10$ 次试验，总计进行了数百次物理/仿真实验。
*   **充分性与公平性**：
    *   实验覆盖了三种不同形态的机器人，验证了框架的可移植性。
    *   通过严格控制变量（相同提示词、相同安全限制、相同硬件），确保了模型间对比的客观性。
    *   引入了跨框架对比（与 ROSA）和消融实验，证明了执行层设计对安全性的独立贡献。

### 6. 主要结论与发现
*   **模型行为发散**：面对相同指令（如“Shake and bake”），不同模型产生的物理轨迹完全不同（如 Claude 选择旋转加冲刺，而 GPT 仅选择线性振荡）。
*   **安全性差异显著**：在对抗性提示下，不同模型提议违规动作的概率差异巨大。Llama 4 的违规提议率是 GPT-5.2 的 **4.8 倍**（前沿模型间也有 3.4 倍差异）。
*   **框架设计的价值**：实验证明，即使使用相同的模型和提示词，ROSClaw 的执行层设计（如错误反馈机制）比 ROSA 能获得更高的任务完成率和更低的违规尝试率。
*   **可移植性**：更换机器人平台仅需 10-15 分钟配置，无需修改核心代码。

### 7. 优点
*   **高度解耦**：实现了模型、任务逻辑与机器人硬件的彻底分离。
*   **强制安全性**：通过预执行拦截机制，提供了比单纯依靠模型“对齐”更可靠的安全保障。
*   **强大的审计能力**：结构化日志为具身智能的可解释性和事故溯源提供了基础。
*   **标准化工具**：为具身智能研究提供了一个可重复、可对比的测量仪器。

### 8. 不足与局限
*   **实时性受限**：受限于 LLM 推理速度，无法处理需要毫秒级响应的反应式控制（如动态避障）。
*   **样本量限制**：每组 $N=10$ 的实验规模虽然能观察到显著趋势，但在捕捉极低概率的边缘案例方面可能不足。
*   **安全策略单一**：目前主要侧重于速度限制，尚未深入探讨复杂的空间几何约束或多机器人协作中的安全问题。
*   **依赖闭源模型**：性能最好的表现仍依赖于商业 API，对离线/边缘计算的支持（如 Llama 4）在复杂任务上仍有差距。

（完）
