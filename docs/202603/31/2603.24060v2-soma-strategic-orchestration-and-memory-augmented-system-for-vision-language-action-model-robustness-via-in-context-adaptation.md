---
title: "SOMA: Strategic Orchestration and Memory-Augmented System for Vision-Language-Action Model Robustness via In-Context Adaptation"
title_zh: SOMA：通过上下文自适应提升视觉-语言-动作模型鲁棒性的策略编排与记忆增强系统
authors: "Zhuoran Li, Zhiyang Li, Kaijun Zhou, Jinyu Gu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24060v2"
tags: ["query:wam-vla"]
score: 10.0
evidence: 视觉-语言-动作（VLA）模型的鲁棒性与上下文自适应
tldr: 针对视觉-语言-动作（VLA）模型在处理分布外任务时鲁棒性不足的问题，本文提出了SOMA系统。该系统通过双存储检索增强生成（RAG）、LLM编排器和模型上下文协议（MCP）干预，在不微调参数的情况下实现了上下文自适应。实验表明，SOMA在多个基准测试中显著提升了任务成功率，特别是在长程任务链中表现优异，为增强机器人控制器的鲁棒性提供了新方案。
selection_source: fresh_fetch
motivation: 现有的VLA模型因缺乏长期记忆、因果失败归因和动态干预能力，在面对感知噪声和环境变化时鲁棒性受限。
method: 提出一种结合双存储RAG、LLM编排器和MCP干预的在线流水线，并辅以离线存储整合模块来提炼可靠的执行轨迹先验。
result: "在LIBERO-PRO等基准测试中，SOMA使平均成功率提升了56.6%，在长程任务链中更是实现了89.1%的显著提升。"
conclusion: SOMA证明了通过战略编排和存储增强，可以在不改变模型参数的情况下大幅增强冻结VLA模型的鲁棒性和自适应能力。
---

## 摘要
尽管视觉-语言-动作（VLA）模型作为通用机器人控制器展现出巨大潜力，但由于缺乏长期记忆、因果故障归因和动态干预能力，它们在分布外（OOD）任务中应对感知噪声和环境变化的鲁棒性仍然受到根本限制。为此，我们提出了 SOMA，一种策略编排与记忆增强系统，可在无需参数微调的情况下升级冻结的 VLA 策略，实现鲁棒的上下文自适应。具体而言，SOMA 通过由对比式双记忆检索增强生成（RAG）、归因驱动的大语言模型（LLM）编排器以及可扩展的模型上下文协议（MCP）干预组成的在线流水线运行，同时利用离线记忆整合模块将执行轨迹持续提炼为可靠的先验知识。在 LIBERO-PRO 和我们提出的 LIBERO-SOMA 基准测试中，针对三种骨干模型（pi0、pi0.5 和 SmolVLA）的实验评估表明，SOMA 实现了 56.6% 的平均绝对成功率提升。这包括在长程任务链中 89.1% 的显著绝对提升。项目页面和源代码可在以下网址获取：https://github.com/LZY-1021/SOMA。

## Abstract
Despite the promise of Vision-Language-Action (VLA) models as generalist robotic controllers, their robustness against perceptual noise and environmental variations in out-of-distribution (OOD) tasks remains fundamentally limited by the absence of long-term memory, causal failure attribution, and dynamic intervention capability. To address this, we propose SOMA, a Strategic Orchestration and Memory-Augmented System that upgrades frozen VLA policies for robust in-context adaptation without parameter fine-tuning. Specifically, SOMA operates through an online pipeline of contrastive Dual-Memory Retrieval-Augmented Generation (RAG), an Attribution-Driven Large-Language-Model (LLM) Orchestrator, and extensible Model Context Protocol (MCP) interventions, while an offline Memory Consolidation module continuously distills the execution traces into reliable priors. Experimental evaluations across three backbone models (pi0, pi0.5, and SmolVLA) on LIBERO-PRO and our proposed LIBERO-SOMA benchmarks demonstrate that SOMA achieves an average absolute success rate gain of 56.6%. This includes a significant absolute improvement of 89.1% in long-horizon task chaining. Project page and source code are available at: https://github.com/LZY-1021/SOMA.

---

## 论文详细总结（自动生成）

### SOMA：通过策略编排与记忆增强提升 VLA 模型鲁棒性的系统总结

#### 1. 核心问题与整体含义（研究动机和背景）
视觉-语言-动作（VLA）模型虽然在通用机器人控制方面表现出色，但在面对**分布外（OOD）**任务时鲁棒性极差。其核心缺陷在于：
*   **缺乏长期记忆**：模型通常是无状态的，无法从过去的失败或成功中学习。
*   **缺乏因果归因**：无法诊断执行失败的具体原因（是感知噪声还是规划偏差）。
*   **缺乏动态干预**：一旦感知流受到干扰（如光照变化、物体遮挡、指令模糊），冻结的策略往往会陷入死循环或执行错误。
**SOMA** 的提出旨在不微调模型参数的前提下，通过“外部大脑”（LLM）和“经验库”（存储）来增强现有 VLA 模型的上下文自适应能力。

#### 2. 方法论：核心思想与关键技术
SOMA 采用了解耦的架构，分为**在线执行流水线**和**离线记忆整合**：

*   **核心思想**：利用 LLM 作为编排器，根据从双存储库中检索到的历史经验，动态调用工具（MCP）对当前的感知输入或任务上下文进行修正，从而引导 VLA 模型做出正确决策。
*   **关键技术细节**：
    1.  **对比式双存储 RAG (Dual-Memory RAG)**：不仅存储成功案例，还显式存储失败案例及其归因。通过 CLIP 编码器检索最相关的历史经验，为 LLM 提供正向引导和反向警示。
    2.  **归因驱动的 LLM 编排器**：使用多模态大模型（如 Qwen3-VL-32B）分析当前状态与历史经验的差异，诊断失败原因，并生成干预策略链。
    3.  **模型上下文协议 (MCP) 干预工具箱**：
        *   *Paint-to-Action*：视觉遮罩/纹理替换，解决视觉域偏移。
        *   *Eraser*：擦除无关干扰物，解决因果混淆。
        *   *Prompt-Refiner*：重写模糊指令，解决语言歧义。
        *   *Chaining-Step*：任务分解，缩短任务时界。
        *   *Encore*：关键步骤回滚重试，解决执行停滞。
    4.  **双阶段记忆整合（离线）**：任务结束后，LLM 对轨迹进行初步诊断并存入库；随后进行跨任务差异分析，持续精炼存储的先验知识。

#### 3. 实验设计
*   **数据集/场景**：
    *   **LIBERO-PRO**：测试位置交换（Pos）和任务级（Task）的变化。
    *   **LIBERO-SOMA（自提）**：专门设计了五个 OOD 维度：视觉焦点（Visual Focus）、杂乱移除（Clutter Removal）、噪声指令（Noisy Prompt）、执行脆弱性（Execution Fragility）和长程任务链（Task Chaining）。
*   **Benchmark 与对比方法**：
    *   **骨干模型**：$\pi_0$、$\pi_{0.5}$、SmolVLA。
    *   **对比基准**：各骨干模型的原始版本（Base）。
    *   **评估指标**：任务成功率（Success Rate）、推理轮数（Turns-to-Success）。

#### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或训练时长。
*   **模型细节**：提到使用了 **Qwen3-VL-32B** 作为推理引擎，这暗示了系统在运行过程中需要相当规模的显存支持，且由于涉及多模态 LLM 的多次调用，其实时性可能受限。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了 25 个多样化任务，每个任务进行 199 次随机初始化（Rollouts），以减少偏差。
*   **消融实验**：针对“双存储库的必要性”和“记忆整合的深度”进行了详细消融，验证了成功/失败案例对比以及双阶段整合对推理效率和准确性的贡献。
*   **客观性**：实验跨越了不同规模和架构的 VLA 模型（从轻量级的 SmolVLA 到高性能的 $\pi_0$ 系列），证明了 SOMA 的通用性和即插即用特性。

#### 6. 主要结论与发现
*   **性能飞跃**：SOMA 使 VLA 模型的平均绝对成功率提升了 **56.6%**。
*   **长程任务突破**：在长程任务链中，成功率提升高达 **89.1%**，证明了任务分解和回滚机制的有效性。
*   **OOD 适应性**：在基准模型几乎完全失效（成功率接近 0%）的语义偏移和布局改变场景下，SOMA 恢复了大部分性能。
*   **记忆价值**：双存储机制显著减少了 LLM 寻找正确干预方案所需的推理轮数（从 7.4 轮降至 1.07 轮）。

#### 7. 优点
*   **无需微调**：作为一种非参数化的增强方案，避免了昂贵的机器人数据微调成本。
*   **解耦设计**：将感知修正、逻辑推理与底层控制分离，使得系统极具扩展性（可随时添加新工具）。
*   **失败学习**：通过存储失败案例并进行因果归因，克服了传统 RAG 的“成功偏差”。

#### 8. 不足与局限
*   **推理延迟**：在线流水线涉及 LLM 的多次调用和图像处理（如 SAM3 分割、图像修复），可能无法满足高频实时控制（如 50Hz+）的需求。
*   **模拟器依赖**：虽然针对真实挑战设计，但主要实验仍在仿真环境（LIBERO）中完成，真实物理世界的复杂性（如光影动态变化、触觉反馈）可能需要更多工具支持。
*   **工具库完备性**：目前的 MCP 工具箱虽然涵盖了常见失败，但对于更复杂的物理交互失败（如摩擦力不足、柔性体变形）尚缺乏针对性手段。

（完）
