---
title: "SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models"
title_zh: SABER：一种针对视觉-语言-动作（VLA）模型的隐蔽智能体黑盒攻击框架
authors: "Xiyang Wu, Guangyao Shi, Qingzi Wang, Zongxia Li, Amrit Singh Bedi, Dinesh Manocha"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24935v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 评估视觉-语言-动作 (VLA) 模型对抗对抗性攻击的鲁棒性
tldr: 针对视觉-语言-动作（VLA）模型对指令扰动的敏感性，本文提出了SABER框架。这是一种基于智能体的黑盒攻击方法，利用经过GRPO训练的ReAct攻击者，在有限的编辑预算内生成隐蔽且有效的指令扰动。实验表明，SABER能显著降低机器人任务成功率并增加违规行为，为机器人基础模型的红队测试提供了一种高效、可扩展的评估手段。
selection_source: fresh_fetch
motivation: VLA模型对自然语言指令中的微小扰动十分敏感，亟需一种系统性的黑盒攻击方法来评估其在对抗性环境下的鲁棒性。
method: 开发了SABER框架，采用GRPO训练的ReAct智能体，通过字符、标记和提示词级别的编辑工具生成受限预算下的对抗性指令。
result: "在LIBERO基准测试中，该方法使VLA模型的任务成功率降低了20.6%，同时显著增加了执行步数和约束违规，且编辑效率优于GPT基准。"
conclusion: 研究证明微小的指令编辑足以大幅削弱机器人执行性能，智能体黑盒攻击是评估机器人基础模型安全性的实用且高效的途径。
---

## 摘要
视觉-语言-动作（VLA）模型使机器人能够遵循基于视觉观测的自然语言指令，但指令通道也引入了一个关键漏洞：微小的文本扰动即可改变下游机器人的行为。因此，系统的鲁棒性评估需要一种黑盒攻击者，能够在不同的 VLA 模型中生成极小但有效的指令编辑。为此，我们提出了 SABER，这是一种以智能体为中心的方法，用于在受限的编辑预算下自动生成针对 VLA 模型的基于指令的对抗性攻击。SABER 使用经过 GRPO 训练的 ReAct 攻击者，在受限的编辑预算下，利用字符级、标记级和提示级工具生成微小且合理的对抗性指令编辑，从而诱导目标行为退化，包括任务失败、不必要的执行时长增加以及约束违反次数增多。在涵盖六种最先进 VLA 模型的 LIBERO 基准测试中，SABER 使任务成功率降低了 20.6%，动作序列长度增加了 55%，约束违反增加了 33%，同时与强大的基于 GPT 的基准模型相比，所需的工具调用减少了 21.1%，字符编辑减少了 54.7%。这些结果表明，微小且合理的指令编辑足以显著降低机器人的执行性能，并且这种基于智能体的黑盒流水线为机器人基础模型的红队测试提供了一种实用、可扩展且自适应的方法。

## Abstract
Vision-language-action (VLA) models enable robots to follow natural-language instructions grounded in visual observations, but the instruction channel also introduces a critical vulnerability: small textual perturbations can alter downstream robot behavior. Systematic robustness evaluation therefore requires a black-box attacker that can generate minimal yet effective instruction edits across diverse VLA models. To this end, we present SABER, an agent-centric approach for automatically generating instruction-based adversarial attacks on VLA models under bounded edit budgets. SABER uses a GRPO-trained ReAct attacker to generate small, plausible adversarial instruction edits using character-, token-, and prompt-level tools under a bounded edit budget that induces targeted behavioral degradation, including task failure, unnecessarily long execution, and increased constraint violations. On the LIBERO benchmark across six state-of-the-art VLA models, SABER reduces task success by 20.6%, increases action-sequence length by 55%, and raises constraint violations by 33%, while requiring 21.1% fewer tool calls and 54.7% fewer character edits than strong GPT-based baselines. These results show that small, plausible instruction edits are sufficient to substantially degrade robot execution, and that an agentic black-box pipeline offers a practical, scalable, and adaptive approach for red-teaming robotic foundation models.

---

## 论文详细总结（自动生成）

以下是对论文《SABER: A Stealthy Agentic Black-Box Attack Framework for Vision-Language-Action Models》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：视觉-语言-动作（VLA）模型虽然能通过自然语言指令控制机器人，但其指令接口存在严重的对抗性漏洞。微小的文本扰动（如错别字或词语替换）可能导致机器人执行失败、效率低下或违反安全约束。
*   **研究动机**：现有的攻击方法多依赖手动设计、固定启发式规则或高昂的 GPT 搜索，且往往需要大量的文本改写，容易被察觉。在实际应用中，VLA 模型通常是黑盒且冻结的，因此需要一种**自动化、隐蔽（低编辑量）且无需梯度信息**的黑盒攻击框架，用于系统性地评估机器人基础模型的鲁棒性。

### 2. 方法论：SABER 框架
*   **核心思想**：将攻击生成建模为一个受限预算下的**顺序决策问题**。通过一个基于智能体（Agentic）的 ReAct 架构，自动组合不同层级的编辑工具来生成对抗性指令。
*   **关键技术细节**：
    *   **ReAct 攻击智能体**：使用 Qwen2.5-3B 作为骨干模型，采用“思考-行动（Reasoning-Acting）”循环。
    *   **两阶段工作流（FIND → APPLY）**：智能体先寻找（FIND）指令中适合扰动的位置，再应用（APPLY）具体的编辑操作。
    *   **扰动工具箱**：
        *   **字符级**：模拟拼写错误（插入、删除、替换等）。
        *   **标记级**：替换、移除或添加单词/子词。
        *   **提示级**：注入短句（如增加验证步骤、不确定性条款等）。
    *   **训练算法（GRPO）**：采用群体相对策略优化（Group Relative Policy Optimization），利用机器人执行的反馈（奖励信号）进行强化学习，无需访问目标 VLA 模型的梯度。
    *   **训练阶段**：先通过 GPT-5-mini 生成的数据进行冷启动（SFT），再进行 GRPO 强化学习优化。

### 3. 实验设计
*   **数据集/场景**：使用 **LIBERO** 机器人操纵基准测试，涵盖四个子集：LIBERO-Spatial（空间）、Object（物体）、Goal（目标）和 Long（长程任务）。
*   **目标 VLA 模型**：针对 6 种最先进的模型进行攻击测试，包括 $\pi_0$、$\pi_{0.5}$、GR00T-N1.5、X-VLA、InternVLA-M1 和 DeepThinkVLA。
*   **对比方法**：主要对比了基于 **GPT-5-mini** 的冻结攻击基准（使用相同的工具接口）。
*   **评估指标**：
    *   **攻击成功率（ASR）**：诱导任务失败的比率。
    *   **动作膨胀率（AIR）**：执行步数增加的比例。
    *   **约束违反膨胀（CVI）**：碰撞或违规行为增加的比例。
    *   **隐蔽性指标**：工具调用次数和字符编辑距离。

### 4. 资源与算力
*   **模型规模**：攻击智能体基于 Qwen2.5-3B，使用了 LoRA（低秩自适应）进行微调。
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。但提到了在训练过程中使用了 ART（Agent Reinforcement Trainer）框架进行 GRPO 优化。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个 LIBERO 任务套件上进行了测试，每个套件包含多个任务，并针对 6 种不同的 VLA 模型进行了跨模型评估。
*   **消融实验**：专门进行了“冷启动（Cold-start）”必要性的消融实验，证明了 SFT 初始化对稳定强化学习的重要性。
*   **充分性评价**：实验设计较为全面，不仅关注任务是否失败（二元结果），还关注了执行效率和物理约束，这在机器人领域非常客观且具有实际意义。跨模型的测试验证了攻击的泛化能力。

### 6. 主要结论与发现
*   **攻击有效性**：SABER 平均使任务成功率降低了 **20.6%**，动作序列长度增加了 **55%**，约束违反增加了 **33%**。
*   **隐蔽性与效率**：相比 GPT-5-mini 基准，SABER 在达到更好攻击效果的同时，减少了 **21.1%** 的工具调用和 **54.7%** 的字符编辑量。
*   **策略演变**：通过强化学习，智能体学会了从昂贵的提示级扰动转向更高效、更隐蔽的标记级编辑。
*   **脆弱性分布**：规划密集型任务（如长程任务）比简单的感知任务更容易受到指令扰动的影响。

### 7. 优点
*   **隐蔽性强**：通过优化编辑预算，生成的对抗指令看起来更像自然的输入错误或微小的表达差异，难以被防御系统察觉。
*   **黑盒自适应**：无需目标模型梯度，能够通过环境反馈自主学习针对不同 VLA 模型的攻击策略。
*   **多目标优化**：不仅能导致失败，还能诱导机器人产生“软性”故障（如效率低下或不安全行为），评估维度更全面。

### 8. 不足与局限
*   **环境限制**：目前仅在模拟环境（LIBERO）中进行了验证，尚未在真实物理机器人上测试。
*   **模态局限**：攻击仅限于文本指令通道，未涉及视觉观测的对抗性扰动（如图像对抗攻击）。
*   **防御缺失**：论文重点在于“如何攻击”，对于如何利用该框架来“增强防御”（如对抗训练）的探讨较少。

（完）
