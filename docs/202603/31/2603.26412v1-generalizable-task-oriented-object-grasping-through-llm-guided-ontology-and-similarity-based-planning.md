---
title: Generalizable task-oriented object grasping through LLM-guided ontology and similarity-based planning
title_zh: 基于大语言模型引导的本体和相似性规划的可泛化面向任务物体抓取
authors: "Hao Chen, Takuya Kiyokawa, Weiwei Wan, Kensuke Harada"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26412v1"
tags: ["query:wam-vla"]
score: 9.0
evidence: 任务导向的物体抓取与操作规划
tldr: 针对任务导向型抓取（TOG）中部件识别不稳定和泛化性差的问题，本文提出一种以几何为中心的策略。该方法利用大语言模型（LLM）构建物体-部件-任务本体，通过采样几何分析识别功能部件，并结合相似性匹配框架，利用已知物体的抓取知识指导未知目标的规划。实验证明该方法在功能部件选择、识别及抓取生成方面具有高准确率，且具备跨类别泛化能力。
selection_source: fresh_fetch
motivation: 现有的视觉语言模型在任务导向型抓取中存在部件识别不稳定和视角敏感等问题，限制了其在多样化任务中的泛化能力。
method: 提出一种结合LLM引导的本体库、基于采样的几何分析以及相似性匹配框架的策略，实现从任务理解到几何驱动的抓取规划。
result: 真实世界实验表明，该方法在功能部件选择和抓取生成上具有高精度，并能通过扩展本体知识有效处理新类别物体。
conclusion: 该研究通过融合语义知识与几何分析，显著提升了机器人任务导向型抓取的鲁棒性和对未知物体的泛化适应性。
---

## 摘要
面向任务的抓取（TOG）比简单的物体抓取更具挑战性，因为它需要精确识别物体部件并仔细选择抓取区域，以确保有效且稳健的操作。虽然最近的方法通过训练大规模视觉语言模型，将部件级物体分割与任务感知抓取规划相结合，但它们在部件识别和抓取推理方面的不稳定性限制了其在不同物体和任务中的泛化能力。为了解决这一问题，我们提出了一种新颖的、以几何为中心的可泛化 TOG 策略，该策略不依赖于视觉识别的语义特征，有效地克服了基于模型的方法对视角的敏感性。我们的主要方案包括：1）一种基于直观人类指令进行功能部件选择的“物体-部件-任务”本体，该本体使用大语言模型（LLM）构建；2）一种基于采样的几何分析方法，用于从观测到的点云中识别所选物体部件，并结合了多种点分布和距离度量；3）一种用于模仿抓取规划的相似性匹配框架，利用具有预先存在的分割和抓取知识的相似已知物体作为参考，来指导未知目标的规划。我们通过真实世界实验验证了该方法在功能部件选择、识别和抓取生成方面的高准确性。此外，我们通过扩展现有的本体知识，展示了该方法对新类别物体的泛化能力，体现了其对广泛物体和任务的适应性。

## Abstract
Task-oriented grasping (TOG) is more challenging than simple object grasping because it requires precise identification of object parts and careful selection of grasping areas to ensure effective and robust manipulation. While recent approaches have trained large-scale vision-language models to integrate part-level object segmentation with task-aware grasp planning, their instability in part recognition and grasp inference limits their ability to generalize across diverse objects and tasks. To address this issue, we introduce a novel, geometry-centric strategy for more generalizable TOG that does not rely on semantic features from visual recognition, effectively overcoming the viewpoint sensitivity of model-based approaches. Our main proposals include: 1) an object-part-task ontology for functional part selection based on intuitive human commands, constructed using a Large Language Model (LLM); 2) a sampling-based geometric analysis method for identifying the selected object part from observed point clouds, incorporating multiple point distribution and distance metrics; and 3) a similarity matching framework for imitative grasp planning, utilizing similar known objects with pre-existing segmentation and grasping knowledge as references to guide the planning for unknown targets. We validate the high accuracy of our approach in functional part selection, identification, and grasp generation through real-world experiments. Additionally, we demonstrate the method's generalization capabilities to novel-category objects by extending existing ontological knowledge, showcasing its adaptability to a broad range of objects and tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《Generalizable task-oriented object grasping through LLM-guided ontology and similarity-based planning》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：传统的面向任务抓取（Task-Oriented Grasping, TOG）依赖视觉语言模型（VLM）进行部件分割，但在面对不同视角、光照变化或未见过的物体时，部件识别极不稳定。此外，现有的方法难以将人类模糊、感性的自然语言指令（如“帮我把这杯热水递过来”）转化为精确的机器人抓取动作。
*   **核心问题**：如何在不依赖大规模端到端训练的情况下，实现对未知物体功能部件的鲁棒识别，并生成符合特定任务约束的 6-DoF 稳定抓取？

### 2. 方法论
该论文提出了一种以几何为中心、由大语言模型（LLM）驱动的模块化框架：
*   **LLM 引导的本体推理（Ontology Reasoning）**：
    *   构建“物体-部件-任务”本体库。
    *   利用 GPT-4o 作为推理引擎，通过思维链（CoT）和提示词优化，将自然语言指令映射到具体的功能部件（如：指令“倒水” $\rightarrow$ 目标部件“手柄”）。
*   **模板辅助的功能部件识别**：
    *   **采样与聚类**：在观测到的点云中采样种子点并形成局部簇。
    *   **多度量相似性评价**：通过 PCA（主成分分析，评估分布）、PPD（点对点距离标准差，评估离散度）和 CCD（中心距离，评估全局相对位置）三个指标，将观测到的点云簇与预设的模板部件进行匹配。
*   **任务导向抓取规划**：
    *   **局部到全局配准**：先通过 RANSAC+ICP 对齐功能部件，再通过 512 次离散旋转优化解决整体对齐问题，从而将模板上的预设抓取知识转移到目标物体。
    *   **稳定性感知调整**：利用局部包围盒（LBB）估计，对转移后的抓取位姿进行微调，确保抓取中心位于物体表面且避开碰撞。

### 3. 实验设计
*   **实验场景**：真实世界环境，使用 UR5e 机械臂、Robotiq 2F-140 夹爪及 RealSense D435 相机。
*   **数据集/物体**：
    *   基础类别：马克杯、瓶子、剪刀（每类包含多个未见过的实例）。
    *   泛化类别：雨伞、牙膏、碗、螺丝刀、果汁盒、钳子等。
*   **对比方法（Benchmark）**：
    *   **部件识别 (PRA)**：对比视觉模型 **VLPart**。
    *   **抓取选择 (GSA)**：对比 **ShapeGrasp**（基于几何分解）和 **GraspGPT**（基于 LLM 推理）。
    *   **抓取成功率 (GSR)**：对比 **GraspNet** 和 **HGGD**（通用抓取检测基准）。
*   **消融实验**：验证了“局部到全局配准”和“位置调整策略”对抓取成功率的贡献。

### 4. 资源与算力
*   **硬件配置**：AMD Ryzen 7 5800H CPU, NVIDIA GeForce RTX 3060 GPU。
*   **算力需求**：论文未提及大规模模型训练，主要算力消耗在于实时点云处理、512 次旋转优化的并行计算以及 GPT-4o 的 API 调用。由于不涉及端到端深度学习训练，该方法对本地算力要求较低，侧重于推理和几何计算。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 3 类基础物体进行了 45 次部件识别实验和 90 次抓取选择/成功率实验。
    *   对 6 类完全新颖的物体进行了泛化性验证。
    *   进行了针对遮挡、噪声、缩放变化的鲁棒性测试（每项 45 次试验）。
*   **充分性评价**：实验设计较为全面，涵盖了从语义理解到几何匹配再到物理执行的全流程。对比实验选择了当前领域内具有代表性的 VLM 和抓取模型，且包含了消融实验，逻辑闭环清晰，实验结果具有客观性。

### 6. 主要结论与发现
*   **几何优于语义**：在部件识别上，基于 3D 几何相似性的匹配比基于 2D 图像语义的分割更具视角不变性。
*   **LLM 的角色**：LLM 在处理复杂指令和跨类别逻辑映射（如将“雨伞”类比为“带手柄的杯子”）方面表现卓越，准确率达 100%。
*   **小样本泛化**：仅需极少量（每类约 3 个）的几何模板，即可通过本体扩展实现对大量新类别物体的任务导向抓取。

### 7. 优点
*   **高解释性**：相比黑盒神经网络，该方法的每一步（从本体推理到几何匹配）都是可追踪、可调试的。
*   **强泛化能力**：通过“类比”机制，打破了传统模型必须在特定类别上训练的限制。
*   **鲁棒性**：局部到全局的配准策略有效解决了点云缺失和传感器噪声带来的对齐难题。

### 8. 不足与局限
*   **模板依赖**：如果目标物体的几何形状与库中所有模板差异极大（如完全不规则的艺术品），匹配可能失效。
*   **对称性歧义**：在处理高度对称的物体时，仅靠几何特征可能无法区分功能部件（如遥控器的前端和后端）。
*   **操作深度有限**：目前研究重点在于“抓取”这一动作，对于抓取后的后续复杂操作（如使用剪刀剪纸的具体轨迹规划）涉及较少。

（完）
