---
title: "DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion"
title_zh: DiffusionAnything：用于统一导航与抓取前运动的端到端上下文扩散学习
authors: "Iana Zhura, Yara Mahmoud, Jeffrin Sam, Hung Khang Nguyen, Didar Seyidov, Miguel Altamirano Cabrera, Dzmitry Tsetserukou"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26322v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 通过视觉-语言-动作模型实现统一的导航与操作
tldr: 针对机器人视觉运动规划中目标指定复杂及泛化性差的问题，本文提出 DiffusionAnything，一个统一的端到端图像空间扩散策略。该模型通过多尺度特征调制，仅需极少量自监督数据即可同时处理米级导航和厘米级抓取前运动。其核心在于多尺度 FiLM 条件控制、轨迹对齐深度预测及自监督注意力机制，实现了仅凭 RGB 输入的高效、零样本泛化部署。
selection_source: fresh_fetch
motivation: 传统的视觉运动规划依赖显式目标设定且计算资源消耗大，难以在陌生场景中实现零样本泛化。
method: 提出一种统一的扩散策略，结合多尺度 FiLM 调制、轨迹对齐深度预测以及基于 AnyTraverse 的自监督注意力机制。
result: 模型仅需 5 分钟自监督数据即可训练，运行内存仅 2.0 GB，支持 10 Hz 频率，并在新场景中表现出强大的零样本泛化能力。
conclusion: 该研究证明了通过轻量级扩散模型和自监督学习，可以实现跨尺度的统一机器人运动规划，且具备极高的部署效率。
---

## 摘要
直接从视觉高效预测运动规划仍然是机器人领域的一个基本挑战，其中规划通常需要明确的目标规范和特定任务的设计。最近的视觉-语言-动作（VLA）模型直接从视觉输入推断动作，但需要巨大的计算资源和大量的训练数据，且在面对新场景时无法实现零样本泛化。我们提出了一种统一的图像空间扩散策略，通过多尺度特征调制，仅需每个任务 5 分钟的自监督数据，即可处理米级导航和厘米级操作。该框架由三项关键创新驱动：(1) 基于任务模式、深度尺度和空间注意力的多尺度 FiLM 条件化，使单一模型能够实现适应任务的行为；(2) 轨迹对齐的深度预测将度量 3D 推理集中在生成的路径点上；(3) 来自 AnyTraverse 的自监督注意力实现了无需视觉语言模型和深度传感器的目标导向推理。该模型纯粹基于 RGB 输入运行（2.0 GB 内存，10 Hz），在实现对新场景的鲁棒零样本泛化的同时，仍适用于机载部署。

## Abstract
Efficiently predicting motion plans directly from vision remains a fundamental challenge in robotics, where planning typically requires explicit goal specification and task-specific design. Recent vision-language-action (VLA) models infer actions directly from visual input but demand massive computational resources, extensive training data, and fail zero-shot in novel scenes. We present a unified image-space diffusion policy handling both meter-scale navigation and centimeter-scale manipulation via multi-scale feature modulation, with only 5 minutes of self-supervised data per task. Three key innovations drive the framework: (1) Multi-scale FiLM conditioning on task mode, depth scale, and spatial attention enables task-appropriate behavior in a single model; (2) trajectory-aligned depth prediction focuses metric 3D reasoning along generated waypoints; (3) self-supervised attention from AnyTraverse enables goal-directed inference without vision-language models and depth sensors. Operating purely from RGB input (2.0 GB memory, 10 Hz), the model achieves robust zero-shot generalization to novel scenes while remaining suitable for onboard deployment.

---

## 论文详细总结（自动生成）

以下是对论文《DiffusionAnything: End-to-End In-context Diffusion Learning for Unified Navigation and Pre-Grasp Motion》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在单一轻量级模型中统一处理米级（meter-scale）的机器人导航和厘米级（centimeter-scale）的抓取前（pre-grasp）运动规划，并实现对新场景的零样本（zero-shot）泛化。
*   **研究动机**：
    *   **传统架构缺陷**：导航与操作通常被视为独立领域，导致级联架构存在误差累积、高延迟和模式切换僵硬的问题。
    *   **现有VLA模型局限**：视觉-语言-动作（VLA）大模型（如RT-2、GR00T）虽然泛化性强，但计算成本极高、推理延迟大，且在陌生场景中往往无法实现真正的零样本泛化。
    *   **空间推理需求**：机器人任务本质上是空间和几何问题，过度依赖语言推理（如VLA）在处理精确运动时效率低下。

### 2. 方法论
核心思想是构建一个**统一的图像空间扩散策略**，通过上下文感知（In-context）的特征调制来适配不同任务。
*   **多尺度 FiLM 条件化**：在 UNet 解码器的四个尺度上应用特征线性调制（FiLM）。条件向量 $c$ 包含：任务模式（导航/抓取）、深度尺度（米/厘米）和空间注意力图。这使得单一模型能根据任务自动调整特征权重（如抓取任务强化细粒度特征，导航强化粗粒度特征）。
*   **轨迹对齐的深度推理**：模型不预测全图深度，而是仅沿生成的轨迹路径点预测深度。这种设计显著降低了计算开销，同时保证了规划所需的度量 3D 信息。
*   **自监督注意力引导（AnyTraverse）**：利用 AnyTraverse 框架自动生成可通行区域和目标注意力图作为监督信号。
    *   **导航模式**：关注地面可通行区域。
    *   **抓取模式**：关注目标物体中心。
*   **扩散策略流程**：模型接收 RGB 图像和噪声轨迹，通过迭代去噪生成最终的无碰撞路径。

### 3. 实验设计
*   **实验场景**：包括室内走廊（导航）和桌面环境（抓取前运动），使用 Unitree G1 人形机器人进行实机测试。
*   **Benchmark 与对比方法**：
    *   **导航**：对比 **NoMaD**（基于扩散的视觉导航基准）。
    *   **操作**：对比 NVIDIA 的 **GR00T (n1.5/n1.6)** 模型。
*   **评估指标**：成功率（SR）、平均避障间距（Clearance）、目标距离误差（Dist）。

### 4. 资源与算力
*   **训练资源**：使用单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：在 20 分钟的视频数据上训练 50 个 epoch，耗时约 **2 小时**。针对新任务的微调仅需 15-20 分钟。
*   **推理性能**：推理时仅占用 **2.0 GB 显存**，运行频率为 **10 Hz**，非常适合机载部署。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了探索导航、目标导向导航和抓取前规划三类实验。
    *   在导航测试中，对比了自主目标选择和给定目标图像两种模式。
    *   在抓取测试中，展示了 8 组不同物体的目标预测精度分析。
*   **充分性评价**：实验设计较为全面，涵盖了从米级到厘米级的跨尺度验证，并包含了与 SOTA（顶尖水平）模型的对比。通过消融实验性质的对比（如深度尺度调制前后的精度差异）验证了核心设计的有效性。

### 6. 主要结论与发现
*   **跨尺度统一**：单一模型通过深度尺度调制，将精度从导航的 ~30cm 提升到了抓取任务的 **4.71cm**（提升约 6 倍）。
*   **零样本泛化**：在完全未见过的场景中，导航成功率达到 100%，抓取前规划成功率达到 70.6%，且均实现了 100% 的无碰撞执行。
*   **优于大模型**：相比 GR00T 等 VLA 模型，DiffusionAnything 在新场景下的泛化能力更强，因为其引入了显式的几何推理（可通行性、空间注意力），而不仅仅是模式记忆。

### 7. 优点
*   **极高的数据效率**：每个任务仅需 5 分钟的自监督数据，无需人工演示或遥操作。
*   **轻量化与实时性**：2GB 显存占用和 10Hz 的频率使其能在人形机器人硬件上直接运行。
*   **自监督闭环**：利用 AnyTraverse 实现了从数据标注到训练的自动化，降低了部署门槛。

### 8. 不足与局限
*   **监督依赖**：高度依赖 AnyTraverse 生成的初始监督信号质量，若 AnyTraverse 在极端环境失效，模型表现会受限。
*   **任务范围限制**：目前仅聚焦于“抓取前运动”（接近物体），尚未涵盖完整的闭环抓取动作（如手指闭合、力反馈控制）。
*   **传感器单一**：纯 RGB 输入在极端光照或透明物体识别上可能存在鲁棒性风险。

（完）
