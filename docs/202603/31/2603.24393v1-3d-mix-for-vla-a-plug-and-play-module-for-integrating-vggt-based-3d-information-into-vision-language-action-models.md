---
title: "3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models"
title_zh: 3D-Mix for VLA：一种将基于 VGGT 的 3D 信息集成到视觉-语言-动作模型中的即插即用模块
authors: "Bin Yu, Shijie Lian, Xiaopeng Lin, Zhaolong Shen, Yuliang Wei, Haishan Liu, Changti Wu, Hang Yuan, Bailing Wang, Cong Huang, Kai Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24393v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 将3D信息整合到视觉-语言-动作模型中
tldr: 针对视觉-语言-动作（VLA）模型在3D空间感知能力的不足，本文提出了一种名为3D-Mix的即插即用模块。通过系统研究九种VGGT集成方案，研究发现语义调节门控融合效果最佳。3D-Mix能在不修改现有模型核心组件的情况下，有效整合3D几何特征，在多个基准测试中显著提升了机器人的操作性能和泛化能力。
selection_source: fresh_fetch
motivation: 现有的VLA模型主要基于2D数据训练，缺乏处理复杂机器人操作任务所需的3D空间智能。
method: 提出3D-Mix模块，采用语义调节门控融合机制，将VGGT提供的3D几何特征自适应地集成到多种VLA架构中。
result: "在SIMPLER和LIBERO基准测试中，3D-Mix在9个模型变体上实现了性能的持续提升，其中在域外测试中平均提升了7.0%。"
conclusion: 3D-Mix为增强VLA系统的空间智能提供了一种原则性的集成方法，证明了3D几何信息对提升机器人操作任务的重要性。
---

## 摘要
视觉-语言-动作（VLA）模型利用多模态大语言模型（MLLM）进行机器人控制，但最近的研究表明，由于主要在 2D 数据上进行训练，MLLM 表现出有限的空间智能，导致其在操作任务中的 3D 感知能力不足。虽然最近的方法引入了如 VGGT 等专门的 3D 视觉模型来增强空间理解，但它们采用了多种集成机制且缺乏系统性研究，使得最优融合策略尚不明确。我们在标准化基准上进行了一项全面的初步研究，比较了九种 VGGT 集成方案，并发现语义条件门控融合在所有评估的九种融合方案中表现最强，该方案能根据任务上下文自适应地平衡 2D 语义特征和 3D 几何特征。我们提出了 3D-Mix，这是一个即插即用模块，可以集成到多种 VLA 架构（如 GR00T 风格和 π 风格）中，而无需修改现有的 MLLM 或动作专家组件。在 SIMPLER 和 LIBERO 基准上针对六个 MLLM 系列（九个模型变体，参数量为 2B-8B）进行的实验表明，3D-Mix 带来了持续的性能提升，在所有九个 GR00T 风格变体中，域外（OOD）SIMPLER 基准的平均提升达到 +7.0%，为增强 VLA 系统的空间智能建立了一种原则性方法。

## Abstract
Vision-Language-Action (VLA) models leverage Multimodal Large Language Models (MLLMs) for robotic control, but recent studies reveal that MLLMs exhibit limited spatial intelligence due to training predominantly on 2D data, resulting in inadequate 3D perception for manipulation tasks. While recent approaches incorporate specialized 3D vision models such as VGGT to enhance spatial understanding, they employ diverse integration mechanisms without systematic investigation, leaving the optimal fusion strategy unclear. We conduct a comprehensive pilot study comparing nine VGGT integration schemes on standardized benchmarks and find that semantic-conditioned gated fusion, which adaptively balances 2D semantic and 3D geometric features based on task context, achieved the strongest performance among all nine evaluated fusion schemes in our pilot study. We present 3D-Mix, a plug-and-play module that integrates into diverse VLA architectures (GR00T-style and $π$-style) without modifying existing MLLM or action expert components. Experiments across six MLLM series (nine model variants, 2B--8B parameters) on SIMPLER and LIBERO show that 3D-Mix delivers consistent performance gains, averaging +7.0% on the out-of-domain (OOD) SIMPLER benchmark across all nine GR00T-style variants, establishing a principled approach for enhancing spatial intelligence in VLA systems.

---

## 论文详细总结（自动生成）

以下是对论文《3D-Mix for VLA: A Plug-and-Play Module for Integrating VGGT-based 3D Information into Vision-Language-Action Models》的结构化深度总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前的视觉-语言-动作（VLA）模型主要依赖于在 2D 图像-文本对上预训练的多模态大语言模型（MLLM）。由于缺乏显式的 3D 几何监督，这些模型在处理需要精确空间推理、深度感知和物体定位的机器人操作任务时表现欠佳。
*   **核心问题**：虽然引入专门的 3D 视觉模型（如 VGGT）可以增强空间理解，但目前缺乏关于“如何将 3D 几何特征与 2D 语义特征进行最优融合”的系统性研究。
*   **整体含义**：本文旨在通过系统性实验确定最优的 3D 信息集成策略，并提出一个通用的、即插即用的模块来提升 VLA 模型的空间智能。

### 2. 方法论
*   **核心思想**：提出 **3D-Mix** 模块，采用**语义调节的自适应门控融合（Semantic-conditioned Adaptive Gating）**机制。该机制认为不同的操作任务对语义和几何信息的依赖程度不同，因此应根据任务上下文动态调整两者的融合比例。
*   **关键技术细节**：
    *   **特征提取与投影**：使用冻结的 VGGT 提取几何特征，并通过线性层投影到 MLLM 的隐藏空间。
    *   **语义门控生成**：对 MLLM 的隐藏状态进行全局平均池化，提取全局语义上下文，将其与几何特征拼接后通过门控网络（Sigmoid 激活）生成位置特异性的权重 $g$。
    *   **自适应融合**：利用权重 $g$ 对语义特征和几何特征进行加权混合，生成富含空间信息的特征序列。
    *   **架构兼容性**：
        *   **GR00T 风格**：在 MLLM 最后一层和动作专家（Action Expert）之间插入 3D-Mix。
        *   **$\pi$ 风格**：在 MLLM 的多个中间层与动作专家的对应层之间进行层级化融合（Layer-wise Fusion）。
*   **算法流程**：输入多视图图像 $\rightarrow$ VGGT 提取 3D 特征 + MLLM 提取语义特征 $\rightarrow$ 3D-Mix 根据语义上下文计算门控权重 $\rightarrow$ 融合特征输入 DiT 动作专家预测动作。

### 3. 实验设计
*   **数据集与场景**：
    *   **SIMPLER**：一个 Real-to-Sim 的域外（OOD）基准测试，评估模型在未见过的物体配置和视觉变化下的泛化能力。
    *   **LIBERO**：包含四个任务套件（空间、物体、目标、长程），评估域内多任务学习能力。
*   **对比方法**：
    *   **初步研究（Pilot Study）**：对比了 9 种融合方案，包括 AE-Fusion（动作专家端融合）、Early Fusion（输入端融合）、Concat/CrossAttn Fusion、3D-Tokens、Middle Layer Injection、Spatial Forcing（空间强迫对齐）和 Visual Fusion。
    *   **主实验**：对比了 6 个 MLLM 系列（Qwen2.5-VL, Qwen3-VL, RoboBrain 2.0/2.5, MimoEmbodied, RynnBrain）在加入 3D-Mix 前后的性能。

### 4. 资源与算力
*   **算力设备**：使用了 **8 张 NVIDIA H100 GPU**。
*   **训练配置**：采用 DeepSpeed ZeRO-2 优化器。
*   **训练时长**：每个模型训练 **60,000 步**。
*   **参数规模**：涵盖了从 2B 到 8B 参数量的多种 MLLM 后端。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **初步研究**：在 9 种融合方案上进行了详尽对比，确定了门控融合的最优性。
    *   **广泛验证**：在 2 种主流 VLA 架构（GR00T 风格和 $\pi$ 风格）上进行了验证。
    *   **模型覆盖**：测试了 9 个模型变体，跨越了不同的模型家族和参数规模。
    *   **消融实验**：包括 VGGT 冻结 vs. 微调、3D 信息敏感性测试（加噪/置零）、以及 $\pi$ 风格下的稀疏层融合深度测试。
*   **充分性评价**：实验设计非常充分且客观。通过跨架构、跨规模、跨基准的测试，有力地证明了 3D-Mix 的通用性和有效性。

### 6. 主要结论与发现
*   **最优策略**：语义调节的门控融合（Gated Fusion）在所有方案中表现最佳，尤其在 OOD 场景下优势明显。
*   **性能提升**：3D-Mix 在 GR00T 风格模型上使 SIMPLER 基准的平均成功率提升了 **7.0%**，在某些模型（如 RynnBrain-8B）上提升高达 **12.51%**。
*   **即插即用**：该模块无需修改 MLLM 或动作专家的内部结构，即可实现显著的性能增益。
*   **3D 信息的重要性**：消融实验证实，性能提升确实源于 VGGT 提供的几何信息，而非仅仅因为增加了参数量或特征维度。

### 7. 优点
*   **系统性强**：首次对 VGGT 与 VLA 的集成方案进行了全面的原则性研究，填补了行业空白。
*   **通用性高**：支持多种主流 VLA 架构和 MLLM 后端，具有极强的工程实用价值。
*   **轻量高效**：作为插件模块，计算开销相对较小，且在 $\pi$ 风格架构中支持稀疏融合以节省显存。

### 8. 不足与局限
*   **依赖预训练 3D 模型**：性能高度依赖于 VGGT 的质量，若 VGGT 无法捕捉某些特定场景的几何特征，3D-Mix 的效果可能受限。
*   **仿真与现实的差距**：虽然使用了 SIMPLER（Real-to-Sim）进行评估，但缺乏在真实物理机器人上的大规模部署测试。
*   **计算延迟**：虽然是即插即用，但在推理时仍需额外运行一个 VGGT 编码器，这可能会增加端到端的推理延迟。

（完）
