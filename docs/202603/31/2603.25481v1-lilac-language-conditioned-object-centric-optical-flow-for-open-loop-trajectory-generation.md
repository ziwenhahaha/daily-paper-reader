---
title: "LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation"
title_zh: LILAC：用于开环轨迹生成的语言条件化以物体为中心的光流
authors: "Motonari Kambara, Koki Seno, Tomoya Kaichi, Yanan Wang, Komei Sugiura"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25481v1"
tags: ["query:wam-vla"]
score: 10.0
evidence: 基于流的视觉-语言-动作模型用于操作任务
tldr: LILAC 是一种基于语言引导的以物体为中心的光流生成模型，旨在解决机器人操作中的开环轨迹生成问题。它通过将自然语言指令与 RGB 图像结合，生成 2D 光流并转化为 6-DoF 机械臂轨迹。该模型引入了语义对齐损失和提示条件跨模态适配器，显著提升了指令与动作的对齐精度，在多项基准测试和物理实验中表现优于现有方法。
selection_source: fresh_fetch
motivation: 现有的机器人操作任务在从预操作图像和自然语言指令生成物体轨迹时，面临指令与光流对齐困难的问题。
method: 提出了 LILAC 模型，利用语义对齐损失强化语言约束，并通过跨模态适配器融合视觉提示与图文特征来生成 2D 光流并转换为 6-DoF 轨迹。
result: 实验结果显示，LILAC 在光流生成质量和真实物理操作任务的成功率上均显著超过了现有先进方法。
conclusion: LILAC 证明了通过以物体为中心的光流进行语言引导的轨迹生成是实现高效、精准机器人操作的有效途径。
---

## 摘要
我们利用基于流的轨迹生成来解决语言条件化的机器人操纵问题，这种方法支持在人类和网络上的物体操纵视频上进行训练，且仅需极少量的特定具身数据。这一任务具有挑战性，因为根据操纵前图像和自然语言指令生成物体轨迹需要精确的指令-流对齐。为了应对这一挑战，我们提出了基于流的语言指令引导开环动作生成器（LILAC）。这种基于流的视觉-语言-动作模型（VLA）能够从 RGB 图像和自然语言指令中生成以物体为中心的二维光流，并将其转换为六自由度（6-DoF）机械臂轨迹。LILAC 包含两个关键组件：语义对齐损失（Semantic Alignment Loss），用于强化语言条件约束以生成与指令对齐的光流；以及提示条件跨模态适配器（Prompt-Conditioned Cross-Modal Adapter），用于将学习到的视觉提示与图像及文本特征对齐，为光流生成提供丰富的线索。实验结果表明，我们的方法在多个基准测试中的生成光流质量均优于现有方法。此外，在使用自由格式指令的物理物体操纵实验中，LILAC 展示了比现有方法更高的任务成功率。项目页面见 https://lilac-75srg.kinsta.page/。

## Abstract
We address language-conditioned robotic manipulation using flow-based trajectory generation, which enables training on human and web videos of object manipulation and requires only minimal embodiment-specific data. This task is challenging, as object trajectory generation from pre-manipulation images and natural language instructions requires appropriate instruction-flow alignment. To tackle this challenge, we propose the flow-based Language Instruction-guided open-Loop ACtion generator (LILAC). This flow-based Vision-Language-Action model (VLA) generates object-centric 2D optical flow from an RGB image and a natural language instruction, and converts the flow into a 6-DoF manipulator trajectory. LILAC incorporates two key components: Semantic Alignment Loss, which strengthens language conditioning to generate instruction-aligned optical flow, and Prompt-Conditioned Cross-Modal Adapter, which aligns learned visual prompts with image and text features to provide rich cues for flow generation. Experimentally, our method outperformed existing approaches in generated flow quality across multiple benchmarks. Furthermore, in physical object manipulation experiments using free-form instructions, LILAC demonstrated a superior task success rate compared to existing methods. The project page is available at https://lilac-75srg.kinsta.page/.

---

## 论文详细总结（自动生成）

这是一份关于论文《LILAC: Language-Conditioned Object-Centric Optical Flow for Open-Loop Trajectory Generation》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文针对机器人操纵中的**语言条件化轨迹生成**问题展开研究。
*   **研究动机**：现有的视觉-语言-动作模型（VLA）通常需要海量的特定机器人（具身）数据，且在零样本（Zero-shot）迁移上表现不佳。
*   **核心思路**：将操纵规划重新定义为“基于流（Flow-based）”的预测问题。通过预测以物体为中心的 2D 光流，可以利用互联网上大量的人类操作视频进行训练，降低对机器人演示数据的依赖。
*   **挑战**：如何确保生成的 2D 光流与复杂的自然语言指令精确对齐，以及如何高效地将 2D 像素运动转化为 6 自由度（6-DoF）的机械臂动作。

### 2. 方法论
LILAC 提出了一个开环（Open-loop）动作生成框架，主要包含以下关键技术：
*   **提示条件跨模态适配器（Prompt-Conditioned Multi-Modal Adapter）**：
    *   利用多模态大模型（如 GPT-4o）根据图像和指令生成“视觉提示”（Visual Prompt，即在图像上绘制代表动作趋势的箭头）和目标物体的边界框。
    *   使用 Transformer 编码器融合原始图像、语言指令和带箭头的视觉提示，缩小高层语义与底层运动模式之间的差距。
*   **语义对齐解码器（Semantic Alignment Decoder）**：
    *   采用 Transformer 解码器自回归生成 2D 光流坐标。
    *   **关键创新：语义对齐损失（Semantic Alignment Loss）**。通过强制光流序列的最后一个 Token 与 CLIP 语言特征对齐，确保生成的运动轨迹严格遵循指令语义。
*   **动作去 Token 器（Action De-Tokenizer）**：
    *   将预测的 2D 光流结合深度图和相机参数，通过刚体变换估计和 Transformer 细化，转化为 6-DoF 的机械臂末端执行器轨迹。

### 3. 实验设计
*   **数据集与基准测试**：
    *   构建了 **Robot Flow Benchmark**：基于 Fractal 和 BridgeData V2 数据集，利用 CoTracker-3 提取物体轨迹作为真值。
    *   包含 Fractal 子集（约 2.6 万个片段）和 BridgeData V2 子集（约 1 万个片段）。
*   **对比方法**：
    *   光流生成对比：Im2Flow2Act、FLIP。
    *   物理实验对比：Im2Flow2Act、FLIP 以及先进的 VLA 模型 $\pi_0$。
*   **物理实验场景**：使用丰田 HSR 机器人，执行移动放置、移动倒水、抽纸巾、开抽屉等 5 类任务，共 100 次测试。

### 4. 资源与算力
*   **硬件**：论文明确提到使用 **NVIDIA H200 GPU** 进行训练。
*   **模型调用**：视觉提示生成阶段调用了 GPT-4o 接口。
*   **训练细节**：文中未详细列出总训练时长，但指出通过将光流预测转化为分类问题（而非回归），加速了网络收敛。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量评估**：在两个大规模数据集子集上进行了详尽测试，使用了 ADE（平均距离误差）、AUC 和 P@K 等标准指标。
    *   **消融实验**：针对“语义对齐损失”和“视觉提示”分别做了消融，证明了各组件的必要性。
    *   **物理实验**：100 次真实机器人实验，涵盖了未见过的物体和环境，验证了零样本泛化能力。
*   **客观性**：实验设计涵盖了从仿真基准到真实物理环境的完整链路，对比了当前最前沿的流模型和端到端 VLA 模型，评估较为客观公平。

### 6. 主要结论与发现
*   **性能领先**：LILAC 在光流预测精度上显著优于 Im2Flow2Act 和 FLIP。
*   **物理成功率高**：在真实机器人任务中，LILAC 的平均成功率比最强基准高出 14%，且在处理全身移动操纵时表现优于 $\pi_0$。
*   **推理速度**：LILAC 的推理速度约为 376.6ms/action，比 $\pi_0$ 快约 1.4 倍，且由于是开环生成整条轨迹，避免了闭环模型逐帧编码的累积开销。
*   **视觉提示的作用**：消融实验显示，视觉提示是提升性能最关键的因素（移除后 ADE 显著上升）。

### 7. 优点
*   **数据效率**：通过光流作为中间表示，能够利用非机器人视频（如 Web 视频）进行预训练。
*   **指令对齐**：引入语义对齐损失和视觉提示，有效解决了流模型容易忽略语言指令的通病。
*   **开环高效性**：单次前向传播生成完整轨迹，降低了实时推理的计算负担。

### 8. 不足与局限
*   **静态环境假设**：采用开环生成，无法应对执行过程中环境的动态变化（如物体被突然移走）。
*   **2D 表达限制**：仅靠 2D 光流难以处理复杂的 3D 空间动作（如绕过障碍物或需要深度推理的搅拌动作）。
*   **提示词依赖**：如果前端 MLLM 生成了错误的视觉提示（如箭头方向反了），会直接导致后续光流生成失败。
*   **深度信息缺失**：2D 流不含深度维度，从 2D 到 3D 轨迹的转换仍存在固有挑战。

（完）
