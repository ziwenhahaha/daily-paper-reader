---
title: Efficient Query Rewrite Rule Discovery via Standardized Enumeration and Learning-to-Rank
title_zh: 通过标准化枚举与排序学习实现高效的查询重写规则发现
authors: "Yuan Zhang, Yuxing Chen, Yuekun Yu, Jinbin Huang, Rui Mao, Anqun Pan, Lixiong Zheng, Jianbin Qin"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.04169v1"
tags: ["query:ahd"]
score: 8.0
evidence: 自动发现数据库优化的查询重写规则
tldr: 查询重写对数据库性能优化至关重要，但现有自动枚举方法在处理复杂查询时面临搜索空间爆炸和扩展性差的挑战。本文提出SLER系统，通过标准化模板枚举结合排序学习（LTR）方法，有效消除了结构冗余并缩小了搜索范围。SLER成功构建了包含超过100万条规则的全球最大验证规则库，支持处理包含5个及以上节点的复杂查询计划，为发现深层复杂的转换规则提供了可能。
selection_source: fresh_fetch
motivation: 传统的查询重写规则自动发现方法在面对复杂查询计划时，存在搜索空间呈指数级增长、冗余严重且难以扩展到多节点场景的问题。
method: SLER采用标准化模板来消除结构冗余，并引入排序学习模型预先筛选最具潜力的模板对，从而引导高效的规则枚举。
result: 在超过1.1万个真实SQL查询的测试中，SLER自动构建了拥有100多万条规则的库，是目前已知规模最大且经过验证的重写规则库。
conclusion: SLER的模板驱动设计和学习排名机制具有良好的可扩展性，能够支持复杂算子的集成，为构建全面且自适应的下一代查询优化器奠定了基础。
---

## 摘要
查询重写对于数据库性能优化至关重要，但现有的自动化规则枚举方法面临指数级搜索空间、严重冗余以及可扩展性差的问题，特别是在处理包含五个或更多节点（节点代表计划树中的算子）的复杂查询计划时。我们提出了 SLER，这是一个可扩展的系统，通过结合标准化模板枚举与排序学习（learning to rank）方法，实现了高效且有效的重写规则发现。SLER 使用标准化模板（即保留算子结构但移除具体数据细节的查询计划抽象）来消除结构冗余并大幅缩小搜索空间。排序学习模型通过预过滤最具潜力的模板对来引导枚举过程，从而实现了针对多节点模板的可扩展规则生成。通过对来自开源和商业负载的超过 11,000 个真实 SQL 查询进行评估，SLER 自动构建了一个超过 100 万条规则的重写规则库——这是迄今为止最大的经过实证验证的重写规则库。值得注意的是，在百万级规则规模下，SLER 支持复杂度达到通道级深度的查询计划模板。这种前所未有的规模为在多样化的查询模式中发现高度复杂的转换开启了大门。关键在于，SLER 的模板驱动设计和学习排序机制具有内在的可扩展性，允许无缝集成新的复杂算子，为由全面、自适应规则空间驱动的下一代优化器铺平了道路。

## Abstract
Query rewriting is essential for database performance optimization, but existing automated rule enumeration methods suffer from exponential search spaces, severe redundancy, and poor scalability, especially when handling complex query plans with five or more nodes, where a node represents an operator in the plan tree. We present SLER, a scalable system that enables efficient and effective rewrite rule discovery by combining standardized template enumeration with a learning to rank approach. SLER uses standardized templates, abstractions of query plans with operator structures preserved but data specific details removed, to eliminate structural redundancies and drastically reduce the search space. A learn to rank model guides enumeration by pre filtering the most promising template pairs, enabling scalable rule generation for large node templates. Evaluated on over 11000 real world SQL queries from both open source and commercial workloads, SLER has automatically constructed a rewrite rule repository exceeding 1 million rules - the largest empirically validated rewrite rule library to date. Notably, at the scale of one million rules, SLER supports query plan templates with complexity up to channel level depth. This unprecedented scale opens the door to discovering highly intricate transformations across diverse query patterns. Critically, SLER's template driven design and learned ranking mechanism are inherently extensible, allowing seamless integration of new and complex operators, paving the way for next generation optimizers powered by comprehensive, adaptive rule spaces.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Query Rewrite Rule Discovery via Standardized Enumeration and Learning-to-Rank》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的数据库查询重写规则主要依赖人工定义，难以覆盖复杂的查询模式。现有的自动化规则发现工具（如 SOTA 的 WeTune）面临**搜索空间爆炸**、**规则冗余严重**（>90%）以及**扩展性差**的问题。
*   **研究动机**：WeTune 等工具在处理超过 4 个算子节点的查询计划时，枚举和验证时间呈指数级增长（5 节点需 6 个月，6 节点需 10 年以上），导致其无法发现复杂查询（如 ORM 自动生成的深层嵌套 SQL）所需的重写规则。

### 2. 方法论：SLER 系统架构
SLER 通过以下三大核心技术解决了效率与有效性平衡的问题：
*   **标准化模板枚举（Standardized Enumeration）**：
    *   **核心思想**：通过定义算子优先级（如 `Project > Distinct > Filter`），利用“算子交换”和“算子移除”规则将查询计划转换为标准型。
    *   **技术细节**：在枚举阶段直接过滤掉结构冗余的模板，将验证复杂度从指数级降低到特定场景下的多项式级。
*   **RTP 去重算法（Reduced by Template Pair）**：
    *   在枚举过程中实时进行冗余检查，而非在生成所有规则后再进行昂贵的 $O(n^2)$ 全量对比。它通过维护一个增量的非冗余规则库，利用优化器测试新规则是否能被已有规则组合替代。
*   **排序学习模型（Learning-to-Rank, LTR）**：
    *   **关键技术**：采用 **LambdaMART** 模型，结合 **TCNN（树卷积神经网络）** 提取查询计划的结构特征。
    *   **特征工程**：提取 9 维特征向量，包括 L2 距离、余弦相似度以及表达式复杂度（如变量数、逻辑算子数等）。
    *   **应用场景**：在枚举高节点（7-10 节点）模板时，预先筛选潜力最高的模板对（Top-k），从而在有限时间内发现最有效的规则。

### 3. 实验设计
*   **数据集**：
    *   **开源负载**：从 GitHub 上 15 个热门 Web 应用中提取的 8,518 个真实 SQL 查询。
    *   **商业负载**：来自 YashanDB（崖山数据库）生产环境的 3,000 个复杂金融/管理类查询。
*   **Benchmark**：以 **WeTune** 作为主要对比基准。
*   **对比维度**：枚举耗时、去重效率、规则数量、查询性能提升比例（Latency Improvement）。

### 4. 资源与算力
*   **硬件环境**：服务器配备 Intel Xeon Silver 4216 CPU (64核 @ 2.10 GHz)，376 GB 内存。
*   **软件环境**：使用 SQL Server 2019 进行模型训练和规则验证。
*   **算力投入**：
    *   4 节点枚举：SLER 耗时约 20 小时（WeTune 需 67 小时）。
    *   6 节点枚举：SLER 耗时约 395 天（WeTune 无法完成，标记为 Timeout）。
    *   排序模型推理：耗时小于 1 分钟，开销可忽略。

### 5. 实验数量与充分性
*   **实验规模**：
    *   构建了超过 **100 万条** 经过验证的重写规则（WeTune 仅为万级）。
    *   进行了 2 到 6 节点的详尽效率对比。
    *   针对 10 节点模板进行了采样过滤实验。
*   **充分性评价**：实验设计较为充分。包含了消融实验（验证标准化枚举、RTP 和 Ranker 各自的贡献）、端到端性能对比（在 MySQL 和 SQL Server 上验证）以及针对排序模型准确性的 NDCG 评估。使用真实世界 SQL 负载增强了实验的客观性和说服力。

### 6. 主要结论与发现
*   **规模突破**：SLER 构建了目前全球最大的重写规则库（1M+ 规则），是 WeTune 的 98 倍。
*   **效率提升**：在 4 节点任务中，SLER 的枚举速度是 WeTune 的 3 倍，去重速度提升了数百倍。
*   **复杂查询优化**：SLER 能够发现 5-10 节点的复杂重写规则，这些规则能将某些查询的性能提升数倍，而 WeTune 对这些查询往往无法优化或需要多步低效转换。
*   **LTR 有效性**：排序模型能准确识别高价值规则，Top 500 规则的平均性能提升达 98.6%。

### 7. 优点
*   **架构创新**：将“标准化”思想引入规则枚举，从源头上遏制了搜索空间的膨胀。
*   **可扩展性**：通过 LTR 机制，使系统具备了探索极深层级（10 节点）查询模式的能力。
*   **工业价值**：针对 ORM 产生的冗余 SQL 优化效果显著，具有很强的实用性。

### 8. 不足与局限
*   **算子覆盖范围**：目前主要支持 SPJ（选择、投影、连接）及 `Distinct`、`InSub` 等基础算子，尚未覆盖 `Group By`、窗口函数等更复杂的 OLAP 算子。
*   **验证瓶颈**：虽然减少了调用次数，但底层仍依赖 Z3 求解器，对于逻辑极其复杂的等价性证明，验证时间依然是一个挑战。
*   **长尾规则**：10 节点以上的规则虽然能被发现，但在实际负载中的触发频率（Trigger Rate）可能较低，存在一定的收益递减。

（完）
