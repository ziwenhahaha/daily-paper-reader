---
title: "POLCA: Stochastic Generative Optimization with LLM"
title_zh: POLCA：基于大语言模型的随机生成式优化
authors: "Xuanfei Ren, Allen Nie, Tengyang Xie, Ching-An Cheng"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14769v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 随机生成式优化，LLM 作为优化器来发现最佳系统
tldr: 本研究针对LLM提示词和多轮智能体等复杂系统的优化难题，提出了POLCA框架。该框架将优化过程形式化为随机生成优化问题，利用LLM作为优化器，并结合数值奖励与文本反馈。POLCA通过优先级队列平衡探索与利用，利用ε-Net保持参数多样性，并结合LLM总结器进行元学习，有效解决了反馈噪声和解空间膨胀问题。实验证明其在多个基准测试中表现优异且具有理论收敛性。
selection_source: fresh_fetch
motivation: 传统复杂系统优化依赖人工迭代，且面临随机性反馈和解空间无限扩张的挑战。
method: 提出POLCA框架，通过优先级队列管理候选方案，并结合ε-Net机制与LLM总结器实现高效的随机生成优化。
result: 在τ-bench、HotpotQA等多个基准测试中，POLCA在处理确定性和随机性问题上均显著优于现有最先进算法。
conclusion: POLCA为随机环境下的生成式优化提供了稳健、高效且具有理论保障的解决方案。
---

## 摘要
优化复杂系统（从大语言模型提示词到多轮智能体）传统上需要耗费大量人力的手动迭代。我们将这一挑战形式化为一个随机生成式优化问题，其中生成式语言模型充当优化器，在数值奖励和文本反馈的引导下寻找最佳系统。我们提出了 POLCA（Prioritized Optimization with Local Contextual Aggregation，基于局部上下文聚合的优先级优化），这是一个可扩展的框架，旨在处理优化过程中的随机性——例如噪声反馈、采样小批量和随机系统行为——同时有效管理解空间的无约束扩张。POLCA 维护一个优先级队列来管理探索与利用的权衡，系统地跟踪候选解及其评估历史。为了提高效率，我们集成了一个 $\varepsilon$-Net 机制以维持参数多样性，并使用一个 LLM 总结器（Summarizer）在历史尝试中进行元学习。我们从理论上证明了 POLCA 在随机性条件下能够收敛至近优候选解。我们在多个基准测试上评估了我们的框架，包括 $\tau$-bench、HotpotQA（智能体优化）、VeriBench（代码翻译）和 KernelBench（CUDA 内核生成）。实验结果表明，POLCA 实现了鲁棒、样本高效且时间高效的性能，在确定性和随机性问题上均持续优于最先进的算法。本工作的代码库已在 https://github.com/rlx-lab/POLCA 公开。

## Abstract
Optimizing complex systems, ranging from LLM prompts to multi-turn agents, traditionally requires labor-intensive manual iteration. We formalize this challenge as a stochastic generative optimization problem where a generative language model acts as the optimizer, guided by numerical rewards and text feedback to discover the best system. We introduce Prioritized Optimization with Local Contextual Aggregation (POLCA), a scalable framework designed to handle stochasticity in optimization -- such as noisy feedback, sampling minibatches, and stochastic system behaviors -- while effectively managing the unconstrained expansion of solution space. POLCA maintains a priority queue to manage the exploration-exploitation tradeoff, systematically tracking candidate solutions and their evaluation histories. To enhance efficiency, we integrate an $\varepsilon$-Net mechanism to maintain parameter diversity and an LLM Summarizer to perform meta-learning across historical trials. We theoretically prove that POLCA converges to near-optimal candidate solutions under stochasticity. We evaluate our framework on diverse benchmarks, including $τ$-bench, HotpotQA (agent optimization), VeriBench (code translation) and KernelBench (CUDA kernel generation). Experimental results demonstrate that POLCA achieves robust, sample and time-efficient performance, consistently outperforming state-of-the-art algorithms in both deterministic and stochastic problems. The codebase for this work is publicly available at https://github.com/rlx-lab/POLCA.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **POLCA** 的新型框架，旨在利用大语言模型（LLM）自动化优化复杂系统（如提示词、智能体和代码生成）。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机）
*   **研究背景**：优化 LLM 提示词或多轮智能体传统上依赖人工迭代。虽然现有的“生成式优化”算法（利用 LLM 作为优化器）已初见成效，但面临两大核心挑战。
*   **核心挑战**：
    1.  **随机性（Stochasticity）**：评估反馈存在噪声（如 LLM-as-a-Judge 的主观性）、小批量采样带来的方差，以及智能体本身的随机行为。
    2.  **解空间膨胀**：LLM 优化器容易生成大量语义重复的候选方案，导致搜索空间线性扩张，验证成本极高。
*   **论文意义**：将该过程形式化为“随机生成式优化问题”，并提出 POLCA 框架，旨在实现高效、稳健且具有理论收敛保障的自动化优化。

### 2. 方法论：POLCA 框架
POLCA 的核心思想是结合**优先级搜索**与**局部/全局上下文聚合**，主要包含以下关键技术：
*   **优先级队列存储（Priority Queue Memory）**：维护一个动态队列记录所有候选方案及其评估历史。通过经验均值（或理论上的 UCB 分数）对方案进行排序，平衡“探索”（尝试新方案）与“利用”（精细评估高分方案）。
*   **$\varepsilon$-Net 语义过滤机制**：利用文本嵌入（Embedding）计算候选方案间的语义距离。只有当新方案与现有方案的距离大于阈值 $\varepsilon$ 时才被接受。这在理论上限制了参数空间的规模，防止了冗余计算。
*   **LLM 总结器（Summarizer）**：除了利用当前小批量的局部反馈外，还引入一个总结器 LLM，将历史成功和失败的案例压缩为全局上下文（类似数值优化中的“动量”），引导优化器避开已知错误。
*   **算法流程**：采样小批量任务 -> 从队列选出潜力方案 -> 评估并更新统计量 -> 结合局部反馈与全局总结生成新方案 -> 经过 $\varepsilon$-Net 过滤 -> 评估新方案并入队。

### 3. 实验设计
*   **数据集与场景**：
    1.  **$\tau$-bench**：优化工具使用智能体的提示词（涉及随机环境和多轮对话）。
    2.  **HotpotQA**：优化多步推理问答的提示词。
    3.  **VeriBench**：将 Python 代码翻译为 Lean 4 形式化验证代码（涉及随机评估器）。
    4.  **KernelBench**：生成高效的 CUDA 内核代码（确定性环境）。
*   **对比方法（Baselines）**：
    *   **DSPy**：经典的提示词优化框架。
    *   **GEPA**：基于帕累托前沿的反射式提示词进化算法。
    *   **OpenEvolve**：AlphaEvolve 的开源实现，基于进化策略。

### 4. 资源与算力
*   **模型使用**：使用了多种先进 LLM，包括 **Gemini 2.0 Flash**、**Gemini 1.5 Flash-lite**、**Claude 3.5 Sonnet** 和 **Claude 3.7 Sonnet**。
*   **硬件支持**：KernelBench 实验在 **NVIDIA L40S GPU** 上运行。
*   **Token 消耗**：论文详细记录了 Token 使用量。例如，$\tau$-bench 的一次完整运行（100 次迭代）消耗约 3100 万个输入 Token。
*   **算力说明**：论文强调了其**并行化**设计，通过异步调用 LLM API 显著降低了挂钟时间（Wall-clock time）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从提示词优化到复杂代码生成的四个异构领域，既有高度随机的场景，也有完全确定性的场景。
*   **消融实验**：针对 $\varepsilon$-Net 机制、总结器（Summarizer）以及 $\varepsilon$ 值的敏感度进行了详细的消融研究。
*   **客观性**：实验采用了多个随机种子（如 $\tau$-bench 使用了 6 个种子），并提供了标准差阴影图，结果具有统计学意义。对比基准均设定了相同的评估预算（Metric calls），确保了公平性。

### 6. 主要结论与发现
*   **性能卓越**：POLCA 在所有基准测试中均优于 GEPA 和 OpenEvolve，尤其在随机性较强的任务中优势更明显。
*   **收敛性证明**：论文在理论上证明了在满足一定假设的前提下，POLCA 能够收敛至近优解，且收敛速度受优化器能力和环境随机性共同影响。
*   **多样性的重要性**：$\varepsilon$-Net 过滤不仅节省了算力，还通过保持内存中的方案多样性，提升了总结器提取全局规律的质量。

### 7. 优点（亮点）
*   **理论与实践结合**：不仅有扎实的数学证明（收敛性分析），还在复杂的真实世界基准上验证了有效性。
*   **处理随机性的能力**：通过持续更新的优先级队列，有效解决了 LLM 优化中常见的“因单次评估噪声而错误丢弃好方案”的问题。
*   **高效率**：支持大规模并行评估和生成，显著优于传统的顺序迭代方法。

### 8. 不足与局限
*   **嵌入质量依赖**：$\varepsilon$-Net 的效果高度依赖于 Embedding 模型捕捉语义的能力，若 Embedding 无法区分关键的功能差异，过滤机制可能会失效。
*   **超参数敏感性**：虽然论文做了消融，但 $\varepsilon$ 值的选择在不同领域仍需手动调整以平衡成本与精度。
*   **计算成本**：尽管比基准更高效，但大规模搜索（如 100 次迭代）仍需消耗数千万 Token，对于资源有限的开发者来说成本依然较高。
*   **假设限制**：理论证明依赖于优化器具有“严格改进概率”的假设，但在极难的任务中，LLM 优化器可能长时间无法产生任何改进。

（完）
