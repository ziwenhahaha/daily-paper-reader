---
title: "Retrieve, Schedule, Reflect: LLM Agents for Chip QoR Optimization"
title_zh: 检索、调度、反思：用于芯片 QoR 优化的 LLM 智能体
authors: "Yikang ouyang, Yang Luo, Dongsheng Zuo, Yuzhe Ma"
date: 2026-03-14
pdf: "https://arxiv.org/pdf/2603.13767v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 用于芯片设计优化与调度的LLM智能体
tldr: 针对现代芯片设计中高质量结果（QoR）优化高度依赖人工专家调度的问题，本文提出了一种基于大语言模型（LLM）的智能体框架。该框架通过检索增强生成（RAG）将自然语言知识转化为搜索树，并结合帕累托驱动的语言反思机制与EDA工具交互。实验表明，该方法在性能、功耗和面积优化上优于强化学习等黑盒搜索方法，且效率提升4倍以上，达到了专家级水平。
selection_source: fresh_fetch
motivation: 现代芯片设计中的长周期优化调度高度依赖人工干预，缺乏高效的自动化手段来平衡时序、功耗和面积等多目标优化。
method: 提出一种集成RAG搜索树和帕累托驱动语言反思机制的LLM智能体框架，通过直接与EDA工具交互实现自动化优化调度。
result: "与强化学习相比，该框架在时序优化上提升了10%，同时降低了功耗和面积，且运行速度提升了4倍以上。"
conclusion: 该LLM智能体框架能有效替代人工专家进行芯片QoR优化，并支持通过自然语言定制复杂的多目标权衡任务。
---

## 摘要
现代芯片设计需要在严苛的上市时间约束下，对时序、功耗和面积进行多目标优化。尽管 EDA 工具中集成了强大的优化算法，但实现高 QoR（设计质量）取决于有效的长周期调度，而这在很大程度上依赖于人工专家的干预。为了解决这一问题并实现芯片设计自动化，我们提出了一种基于大语言模型（LLM）的智能体框架，通过与 EDA 工具直接交互来调度芯片优化。该智能体以自然语言形式的专业知识为基础，通过检索增强生成（RAG）将其表示为搜索树。我们通过语言反思引入帕累托驱动的 QoR 反馈，进一步提升了调度质量。实验结果表明，与强化学习等黑盒搜索方法相比，我们的框架在功耗和面积更低的情况下，时序优化提升了 10%，且速度提高了 4 倍以上。优化后的 QoR 与人类专家的水平相当。最后，该智能体支持以自然语言表达的定制化任务，从而实现偏好性的 QoR 权衡。代码和芯片设计数据将在 https://github.com/YiKangOY/Open-LLM-ECO 公开。

## Abstract
Modern chip design requires multi-objective optimization of timing, power, and area under stringent time-to-market constraints. Although powerful optimization algorithms are integrated into EDA tools, achieving high QoR hinges on effective long-horizon scheduling, which relies heavily on manual expert intervention. To address this issue and automate chip design, we propose an agentic LLM framework that schedules chip optimizations through direct interaction with EDA tools. The agent is grounded in natural language expertise expressed as a search tree through retrieval-augmented generation (RAG). We further improve scheduling quality with Pareto-driven QoR feedback through language reflection. Experimental results show that, compared with black-box search methods such as reinforcement learning, our framework achieves 10% greater timing improvement while consuming less power and area, with more than 4x speedup. The post-optimization QoR is also comparable to that achieved by human experts. Finally, the agent supports customized tasks expressed in natural language, enabling preferential QoR trade-offs. The code and chip design data will be publicly available at https://github.com/YiKangOY/Open-LLM-ECO.

---

## 论文详细总结（自动生成）

以下是对论文《Retrieve, Schedule, Reflect: LLM Agents for Chip QoR Optimization》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代芯片设计需要在极短的上市时间内实现功耗、性能和面积（PPA）的多目标优化。虽然 EDA 工具集成了多种优化算法（如门级尺寸调整、缓冲器插入等），但如何**调度**这些算法（即在什么阶段、以什么顺序、配置什么参数）是一个 NP-Hard 问题，目前高度依赖资深工程师的手工干预。
*   **研究背景**：现有的自动化方法（如强化学习 RL）通常将调度视为黑盒搜索，仅依赖数值反馈，忽略了工程师积累的自然语言形式的专业知识，导致搜索效率低、效果不佳，且难以处理用户自定义的复杂优化偏好。

### 2. 方法论：核心思想与关键技术
论文提出了一个名为 **Retrieve-Schedule-Reflect** 的多智能体 LLM 框架，模拟人类工程师的“调度-执行-反思”循环：
*   **检索（Retrieve）**：利用检索增强生成（RAG）技术。研究者将专家经验和 EDA 手册构建成一棵**搜索树（Search Tree）**，节点包含设计状态描述、优化策略和工具手册片段。智能体根据当前芯片的 QoR 报告检索最相关的路径作为调度模板。
*   **调度（Schedule）**：**调度智能体**根据检索到的专家知识和当前设计状态，决定下一阶段的优化目标（时序/功耗/面积）和宏观策略。
*   **执行（Execute）**：**执行智能体**将宏观策略转化为具体的 EDA 工具命令（如 Synopsys PrimeECO 命令），并参考工具手册确保语法和参数的合法性。
*   **反思（Reflect）**：引入**帕累托驱动（Pareto-driven）的反思机制**。在多轮优化中，**反思智能体**识别出处于帕累托前沿（即综合表现最好）的优化轨迹，总结成功经验和失败教训，并将其作为上下文反馈给调度和执行智能体，实现跨轮次的自我进化。

### 3. 实验设计
*   **数据集/场景**：采用 ICCAD'24 竞赛数据集中的 8 个开源芯片设计（基于 ASAP7 7nm 工艺），涵盖 NVDLA 模块、Ariane 处理器内核、AES 加密模块等。
*   **Benchmark（基准）**：
    *   **Original**：未经过 QoR 优化的初始设计。
    *   **Human**：资深工程师手动调度的优化结果。
    *   **RL (A3C)**：基于强化学习（A3C 算法）的黑盒搜索基准。
*   **对比维度**：总负时序裕量（Total Slack）、最差时序裕量（Worst Slack）、功耗（Power）、面积（Area）以及运行时间。

### 4. 资源与算力
*   **硬件环境**：使用了一台配备 2 颗 AMD EPYC 7543（32核）CPU 和 1 块 **NVIDIA RTX 3090 GPU** 的服务器。
*   **模型使用**：默认使用 **GPT-5-mini**（2025-08-07版本）作为核心 LLM，并在消融实验中对比了 GPT-4o-mini 和 DeepSeek-V3.2。
*   **训练/运行时间**：框架平均运行时间为 3.54 小时（包含多轮迭代），相比 RL 基准（15.38 小时）有显著优势。

### 5. 实验数量与充分性
*   **实验规模**：在 8 个不同规模和复杂度的设计上进行了完整测试。
*   **消融实验**：针对 RAG 模块和反思（Reflection）模块分别进行了剔除实验，验证了各组件的必要性。
*   **定制化测试**：额外进行了“自定义偏好”实验，验证智能体能否理解自然语言指令（如“允许 5% 的时序损失以换取更低功耗”）。
*   **客观性**：实验使用了商业级 EDA 工具（PrimeECO）进行评估，指标客观；对比了人类专家和主流 RL 算法，基准设置较为公平。

### 6. 主要结论与发现
*   **性能卓越**：该框架在时序优化上比 RL 基准提升了 **10%**，且在功耗和面积上也表现更好，达到了与人类专家相当的水平。
*   **效率极高**：相比 RL 方法，实现了 **4 倍以上** 的加速，且 Token 成本较低（平均每个设计约 1.25 美元）。
*   **灵活性强**：能够直接处理自然语言描述的复杂权衡任务，这是传统数值优化方法难以实现的。
*   **组件协同**：消融实验证明，反思机制对提升时序质量至关重要，而 RAG 则保证了工具调用的准确性和策略的专业性。

### 7. 优点（亮点）
*   **知识驱动**：首次将芯片优化调度从“盲目搜索”转变为“知识引导”，通过 RAG 引入了人类专家的启发式逻辑。
*   **闭环进化**：帕累托驱动的反思机制解决了 LLM 在长周期任务中容易迷失方向的问题，通过历史轨迹学习优化路径。
*   **端到端自动化**：实现了从读取报告到生成可执行脚本的全流程自动化，显著降低了人力成本。

### 8. 不足与局限
*   **工具依赖**：实验高度依赖特定的商业 EDA 工具（PrimeECO），虽然原理通用，但在其他工具链上的迁移可能需要重新构建工具手册库。
*   **设计规模限制**：虽然测试了多个模块，但对于超大规模（SoC 级别）的全芯片优化，LLM 处理超长上下文报告的能力仍面临挑战。
*   **闭源模型风险**：核心性能依赖于 GPT-5 等顶尖闭源模型，虽然测试了 DeepSeek 等开源模型，但在极端时序修复场景下仍存在性能差距。

（完）
