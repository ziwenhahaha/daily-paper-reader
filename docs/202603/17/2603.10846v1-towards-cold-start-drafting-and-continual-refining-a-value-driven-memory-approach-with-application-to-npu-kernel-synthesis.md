---
title: "Towards Cold-Start Drafting and Continual Refining: A Value-Driven Memory Approach with Application to NPU Kernel Synthesis"
title_zh: 迈向冷启动草拟与持续精炼：一种价值驱动的记忆方法及其在 NPU 算子合成中的应用
authors: "Yujie Zheng, Zhuo Li, Shengtao Zhang, Hanjing Wang, Junjie Sheng, Jiaqian Wang, Junchi Yan, Weinan Zhang, Ying Wen, Bo Tang, Muning Wen"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.10846v1"
tags: ["query:llm-ec"]
score: 9.0
evidence: 基于LLM的自动化算子合成自进化智能体框架
tldr: 针对新兴NPU架构中编程数据稀缺导致的“数据墙”问题，本文提出EvoKernel框架。该框架通过基于价值驱动的记忆增强强化学习，实现了从冷启动草图编写到持续性能优化的自动化内核合成。通过跨任务记忆共享和Q值检索机制，EvoKernel显著提升了通用大模型在特定硬件生态下的代码生成正确率和执行效率。
selection_source: fresh_fetch
motivation: 针对新兴NPU架构编程数据极度匮乏导致的“数据墙”挑战，旨在解决大模型在冷启动环境下内核合成性能低下的问题。
method: 提出EvoKernel框架，利用价值驱动的记忆检索机制和强化学习，通过阶段性Q值评估来引导内核的初始草绘与迭代优化。
result: "在NPU版KernelBench上，将模型生成正确率从11.0%提升至83.0%，并实现了中位数3.60倍的性能加速。"
conclusion: 实验证明，通过价值引导的经验积累和记忆共享，通用大模型能够有效掌握并优化小众硬件生态下的内核合成任务。
---

## 摘要
将大语言模型部署到数据稀缺的编程领域面临着重大挑战，特别是在新兴的领域特定架构（DSA）上的算子合成中，其中“数据墙”限制了可用的训练数据。虽然模型在 CUDA 等数据丰富的平台上表现出色，但在 NPU 编程等数据稀缺的生态系统中，其性能会出现灾难性的下降。为了在不进行昂贵微调的情况下克服这一冷启动障碍，我们引入了 EvoKernel，这是一个自进化的智能体框架，能够自动化从初始草拟到持续精炼的算子合成全生命周期。EvoKernel 通过将合成过程建模为基于记忆的强化学习任务来解决这一问题。通过一种新颖的价值驱动检索机制，它学习特定阶段的 Q 值，根据经验对当前目标的贡献（无论是引导生成可行的草案还是迭代优化延迟）来确定经验的优先级。此外，通过启用跨任务记忆共享，该智能体能够将见解从简单算子泛化到复杂算子。通过构建 KernelBench 的 NPU 变体并进行评估，EvoKernel 将前沿模型的正确率从 11.0% 提升至 83.0%，并通过迭代精炼实现了相对于初始草案 3.60 倍的中位数加速。这表明，价值引导的经验积累使通用模型能够掌握小众硬件生态系统中的算子合成任务。我们的官方页面位于 https://evokernel.zhuo.li。

## Abstract
Deploying Large Language Models to data-scarce programming domains poses significant challenges, particularly for kernel synthesis on emerging Domain-Specific Architectures where a "Data Wall" limits available training data. While models excel on data-rich platforms like CUDA, they suffer catastrophic performance drops on data-scarce ecosystems such as NPU programming. To overcome this cold-start barrier without expensive fine-tuning, we introduce EvoKernel, a self-evolving agentic framework that automates the lifecycle of kernel synthesis from initial drafting to continual refining. EvoKernel addresses this by formulating the synthesis process as a memory-based reinforcement learning task. Through a novel value-driven retrieval mechanism, it learns stage-specific Q-values that prioritize experiences based on their contribution to the current objective, whether bootstrapping a feasible draft or iteratively refining latency. Furthermore, by enabling cross-task memory sharing, the agent generalizes insights from simple to complex operators. By building an NPU variant of KernelBench and evaluating on it, EvoKernel improves frontier models' correctness from 11.0% to 83.0% and achieves a median speedup of 3.60x over initial drafts through iterative refinement. This demonstrates that value-guided experience accumulation allows general-purpose models to master the kernel synthesis task on niche hardware ecosystems. Our official page is available at https://evokernel.zhuo.li.

---

## 论文详细总结（自动生成）

以下是对论文《Towards Cold-Start Drafting and Continual Refining: A Value-Driven Memory Approach with Application to NPU Kernel Synthesis》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：大语言模型（LLM）在数据丰富的领域（如 CUDA 编程）表现优异，但在新兴的领域特定架构（DSA，如 NPU）上由于缺乏训练数据（即“数据墙”问题），性能会出现断崖式下跌。
*   **研究动机**：现有的监督微调（SFT）需要大量专家标注数据，成本极高；而传统的检索增强生成（RAG）在数据库稀疏时效果不佳。
*   **整体含义**：本文提出了 **EvoKernel**，一个自进化的智能体框架。它将算子合成视为一个基于记忆的强化学习任务，旨在不进行模型微调的情况下，通过自主积累经验，解决从“冷启动草拟”到“持续性能精炼”的全生命周期自动化合成问题。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将算子合成建模为**基于记忆的马尔可夫决策过程（M-MDP）**。智能体不更新模型权重，而是通过学习“检索策略”来选择高价值的过往经验（代码片段、错误日志、优化技巧）来引导生成。
*   **关键技术细节**：
    *   **两阶段流水线**：
        1.  **冷启动草拟（Drafting）**：目标是生成功能正确的初始代码。
        2.  **持续精炼（Refining）**：在正确代码基础上，通过迭代优化降低执行延迟。
    *   **价值驱动检索（Value-Driven Retrieval）**：引入特定阶段的 **Q 值函数** $Q(s, m)$。不同于传统的语义相似度检索，Q 值衡量的是某个记忆项 $m$ 对当前目标（正确性或低延迟）的预期贡献。
    *   **统一价值更新**：使用蒙特卡洛（MC）更新规则：$Q(s, m) \leftarrow Q(s, m) + \alpha [r - Q(s, m)]$。奖励 $r$ 在草拟阶段基于正确性（+1/-1），在精炼阶段基于延迟缩减量（使用 tanh 归一化）。
    *   **多门控验证器（Multi-Gate Verifier）**：包含防作弊检查（防止模型调用现成库）、编译检查、正确性比对和硬件原生性能分析（Profiling）。

### 3. 实验设计
*   **数据集/场景**：
    *   **NPU 版 KernelBench**：将原有的 GPU 算子基准适配至华为昇腾（Ascend C）架构，包含 L1（基础）和 L2（融合算子）两个难度等级。
    *   **扩展测试**：Attention Set（70个注意力机制相关算子）和最新的 mHC（流形约束超连接）内核。
*   **对比方法**：
    *   **Pass@k**：无状态的多次采样基准。
    *   **Refinement**：仅针对单个算子进行迭代修复的基准（无跨任务记忆共享）。
    *   **Codex (OpenAI)**：赋予 GPT-5.2 自主 Shell 和文件系统访问权限的进化循环。
*   **评估指标**：编译率（CR）、正确率（Acc）、加速比（Speedup）。

### 4. 资源与算力
*   **算力说明**：论文未详细列出运行 LLM 智能体所使用的具体 GPU 型号和数量。
*   **验证环境**：提到了使用远程服务器进行验证，每个验证调用超时时间为 65 分钟，并行度为 16。
*   **硬件后端**：性能分析在华为 **Ascend 910B NPU** 上完成，使用 `msprof` 工具进行硬件级延迟测量。

### 5. 实验数量与充分性
*   **实验规模**：
    *   主实验涵盖了 KernelBench 的 250 个算子。
    *   进行了跨任务记忆共享、跨模型（GPT-5.2 到 DeepSeek/Qwen）的记忆迁移实验。
    *   针对 Q 值检索、候选池大小等关键变量进行了消融实验。
*   **充分性评价**：实验设计较为全面，不仅验证了正确性的提升，还深入探讨了“经验”如何在不同难度的任务间迁移（如 L1 经验如何加速 L2 任务），证明了框架的泛化能力。实验对比了当前最强的闭源和开源模型，具有较高的客观性。

### 6. 主要结论与发现
*   **性能飞跃**：在 GPT-5.2 基础上，EvoKernel 将 NPU 算子的正确率从 **11.0% 提升至 83.0%**。
*   **性能优化**：在获得正确代码后，通过持续精炼实现了中位数 **3.60 倍** 的加速，部分算子甚至超过了官方 Torch-NPU 的性能。
*   **记忆迁移**：强模型（GPT-5.2）积累的记忆可以显著提升弱模型（DeepSeek-V3.2）的性能（正确率从 6% 升至 58%），证明了经验的通用性。
*   **涌现课程学习**：智能体在没有人工干预的情况下，会自动先学习简单算子，并将其作为解决复杂算子的参考，形成了自发的“由易到难”学习路径。

### 7. 优点
*   **无需微调**：通过非参数化的记忆进化解决了冷启动问题，避免了昂贵的训练成本。
*   **价值导向**：突破了传统 RAG 仅靠语义相似度的局限，利用强化学习信号精准筛选“有用”的经验。
*   **闭环验证**：设计了严密的防作弊和硬件级分析流程，确保生成的代码不仅正确且在真实硬件上高效。

### 8. 不足与局限
*   **模型依赖性**：实验显示，该方法在强模型（如 GPT-5.2）上效果极佳，但在较弱的开源模型上提升幅度相对有限，说明框架性能仍受限于底座模型的上下文学习能力。
*   **延迟瓶颈**：虽然实现了显著加速，但在许多复杂算子上，生成的代码仍慢于经过高度人工优化的官方库（Torch-NPU）。
*   **应用限制**：目前主要聚焦于算子合成这一具有明确二元验证信号（对/错）的领域，在缺乏自动验证器的其他编程领域可能难以直接推广。

（完）
