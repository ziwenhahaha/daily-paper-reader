---
title: "SI-ChainFL: Shapley-Incentivized Secure Federated Learning for High-Speed Rail Data Sharing"
title_zh: SI-ChainFL：面向高铁数据共享的 Shapley 激励安全联邦学习
authors: "Mingjie Zhao, Cheng Dai, Fei Chen, Xin Chen, Kaoru Ota, Mianxiong Dong, Bing Guo"
date: 2026-03-09
pdf: "https://arxiv.org/pdf/2603.07992v1"
tags: ["query:urt-pf"]
score: 10.0
evidence: 用于高铁客流预测的联邦学习框架
tldr: 针对高铁数据共享中联邦学习存在的激励不足和单点故障问题，本文提出SI-ChainFL框架。该框架利用Shapley值量化客户端贡献，并结合稀疏正样本驱动的聚类策略降低计算开销。同时，设计了基于区块链的共识协议实现去中心化聚合，将聚合权限与激励挂钩。实验证明，该方法在恶意攻击下表现稳健，显著提升了模型精度。
selection_source: fresh_fetch
motivation: 解决高铁跨部门流量预测中联邦学习面临的搭便车、模型投毒及中心化聚合带来的单点故障风险。
method: 提出基于Shapley值的贡献度量化指标，并结合客户端聚类加速评估，同时利用区块链共识协议实现与激励挂钩的去中心化聚合。
result: "在多种数据集及高铁流量数据上，SI-ChainFL在90%恶意客户端攻击下仍能保持有效，准确率比RAGA高出14.12%。"
conclusion: SI-ChainFL通过结合贡献感知激励与去中心化聚合，为高铁等关键基础设施的安全高效数据共享提供了可靠方案。
---

## 摘要
在高速铁路（HSR）系统中，联邦学习（FL）能够在不共享原始数据的情况下实现跨部门的流量预测。然而，现有方案存在两个关键局限性：（1）激励机制不足，导致搭便车行为和模型投毒；（2）中心化聚合，引入了单点故障。我们提出了一种安全高效的框架 SI-ChainFL，通过将贡献感知的激励机制与去中心化聚合相结合来解决这些问题。首先，我们使用 Shapley 值指标量化客户端贡献，该指标综合考虑了稀有事件效用、数据多样性、数据质量和及时性。为了降低计算开销，我们进一步开发了一种稀有正样本驱动的客户端聚类策略，以加速 Shapley 值的估算。此外，我们设计了一种基于区块链的去中心化聚合共识协议，其中聚合资格与 Shapley 激励挂钩。这一设计激励客户端提交高质量的更新，并实现了高效且安全的全局聚合。在 MNIST、CIFAR 10、CIFAR 100 以及高铁流量数据集上的实验表明，SI-ChainFL 在 PA 攻击下即使存在 90% 的恶意客户端仍能保持有效，其准确率比 RAGA 高出 14.12%。理论分析进一步保证了性能的上界。

## Abstract
In high-speed rail (HSR) systems, federated learning (FL) enables cross-departmental flow prediction without sharing raw data. However, existing schemes suffer from two key limitations: (1) insufficient incentives, leading to free-riding and model poisoning; and (2) centralized aggregation, which introduces a single point of failure. We propose a secure and efficient framework SI-ChainFL that addresses these issues by combining contribution-aware incentives with decentralized aggregation. First, we quantify client contributions using a Shapley value metric that jointly considers rare-event utility, data diversity, data quality, and timeliness. To reduce computational overhead, we further develop a rare positive driven client clustering strategy to accelerate Shapley estimation. Moreover, we design a blockchain-based consensus protocol for decentralized aggregation, where aggregation eligibility is tied to Shapley incentives. This design motivates clients to submit high-quality updates and enables efficient and secure global aggregation. Experiments on MNIST, CIFAR 10 and CIFAR 100, and a HSR flow dataset show that SI ChainFL remains effective under 90% malicious clients in PA attacks, achieving 14.12% higher accuracy than RAGA. Theoretical analysis further guarantees an upper bound on performance

---

## 论文详细总结（自动生成）

以下是对论文《SI-ChainFL: Shapley-Incentivized Secure Federated Learning for High-Speed Rail Data Sharing》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：中国高铁（HSR）网络规模巨大，精准的客流预测对调度和预警至关重要。这需要跨部门（车站、票务、气象）的数据共享。
*   **核心挑战**：
    1.  **隐私与合规**：原始数据无法直接共享（受GDPR等法规限制）。
    2.  **激励不公**：现有联邦学习（FL）激励机制多基于样本量，忽略了稀有样本（如突发客流）的高价值，易导致“搭便车”行为。
    3.  **安全风险**：中心化聚合服务器存在单点故障风险，且易受模型投毒攻击。
*   **整体含义**：本文提出 SI-ChainFL 框架，旨在通过 Shapley 值量化贡献并结合区块链技术，构建一个公平、安全且去中心化的高铁数据共享预测模型。

### 2. 方法论
SI-ChainFL 框架由三个核心阶段组成：
*   **多目标 Shapley 值建模**：
    *   **维度量化**：综合考虑**稀有事件预测效用**（针对高铁突发流量）、**数据多样性**（特征表示相似度）、**数据质量**（清洁度与标签可靠性）以及**及时性**（时间衰减权重）。
    *   **公式逻辑**：通过加权融合上述指标构建价值函数，利用 Shapley 值公平分配每个客户端的边际贡献。
*   **稀有正样本驱动的近似计算**：
    *   **核心思想**：针对 Shapley 值计算复杂度随参与者数量呈指数级增长（$O(2^n)$）的问题，提出聚类策略。
    *   **技术细节**：识别对稀有正样本（突发流量）影响显著的关键客户端，将影响较小的客户端进行聚类合并，将计算复杂度降至近线性。
*   **基于区块链的共识聚合协议**：
    *   **去中心化聚合**：弃用中心服务器，由验证节点委员会通过共识协议进行聚合。
    *   **激励挂钩**：客户端的聚合权重和获取全局模型的权限直接与其 Shapley 贡献分挂钩，只有通过验证的更新才会被记入区块。

### 3. 实验设计
*   **数据集**：
    *   **基准数据集**：MNIST、CIFAR-10、CIFAR-100（用于验证分类任务的鲁棒性）。
    *   **交通数据集**：PeMS07（高速公路传感器数据）。
    *   **真实高铁数据集（HSR）**：来自成都铁路科研机构，包含108个车站、跨度731天的到发客流及对应气象数据。
*   **对比方法（Benchmark）**：
    *   经典 FL：FedAvg, FedProx。
    *   边缘/图 FL：FedEdge, FedSage。
    *   鲁棒/安全 FL：FLTrust, RAGA, P2FMS。
*   **攻击场景**：模拟了**搭便车攻击（FR）**（上传旧更新或零向量）和**投毒攻击（PA）**（标签翻转）。

### 4. 资源与算力
*   **硬件环境**：实验使用了配备 **NVIDIA RTX 5090 (32GB)** GPU 和 25 vCPU 的 Intel Xeon Platinum 8470Q 处理器的工作站。
*   **软件环境**：基于 PyTorch 2.8.0 框架。
*   **训练细节**：设置了 100 个客户端，每轮随机选择 70% 参与，共进行 100 轮通信。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了收敛性分析、不同客户端数量下的可扩展性测试、隐私预算（DP）影响测试、验证集规模影响测试等。
*   **充分性评价**：
    *   **极端测试**：在恶意客户端比例高达 **90%** 的极端环境下进行了鲁棒性对比，这在同类研究中非常罕见。
    *   **消融实验**：分别验证了贡献感知激励（SI）和区块链聚合（ChainFL）各自的有效性。
    *   **客观性**：使用了真实世界的高铁数据集，证明了方法在非独立同分布（Non-IID）数据下的实用性。

### 6. 主要结论与发现
*   **高鲁棒性**：在 90% 恶意节点投毒攻击下，SI-ChainFL 的准确率比最先进的鲁棒聚合算法 RAGA 高出 **14.12%**。
*   **高效计算**：提出的近似 Shapley 计算方法在 HSR 数据集上将计算时间缩短至随机采样法的 **1/8**。
*   **收敛性**：尽管引入了差分隐私（DP）噪声和复杂的评估机制，其收敛速度仍与 FedAvg 相当，且在回归任务（MAE指标）上表现优异。

### 7. 优点与亮点
*   **精细化估值**：不同于仅看样本量的传统方法，该模型能识别并奖励那些能预测“稀有突发客流”的高价值数据。
*   **内生安全性**：将 Shapley 值作为共识协议的“入场券”，从机制设计层面解决了恶意节点参与聚合的问题。
*   **理论支撑**：论文提供了详尽的安全性分析，证明了恶意更新对全局模型影响的上界。

### 8. 不足与局限
*   **区块链开销**：虽然优化了 Shapley 计算，但区块链共识过程（尤其是频繁的区块打包和广播）在大规模实时预测场景下可能存在延迟瓶颈。
*   **验证集依赖**：Shapley 值的计算依赖于验证节点持有的验证集，若验证集本身存在偏差，可能影响贡献评估的公正性。
*   **动态性挑战**：高铁网络环境高度动态，论文虽提到了时间衰减，但在极端突发状况（如自然灾害导致的大面积停运）下的模型自适应能力仍有待进一步验证。

（完）
