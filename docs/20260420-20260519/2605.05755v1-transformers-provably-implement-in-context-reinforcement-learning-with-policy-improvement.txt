Title: Transformers Provably Implement In-Context Reinforcement Learning with Policy Improvement

URL Source: https://arxiv.org/pdf/2605.05755v1

Published Time: Fri, 08 May 2026 00:38:31 GMT

Number of Pages: 25

Markdown Content:
# Transformers Provably Implement In-Context Reinforcement Learning with Policy Improvement 

Haodong Liang, Lifeng Lai 

Department of Electrical and Computer Engineering University of California, Davis 

{hdliang,lflai}@ucdavis.edu 

## Abstract 

We investigate the ability of transformers to perform in-context reinforcement learning (ICRL), where a model must infer and execute learning algorithms from trajectory data without parameter updates. We show that a linear self-attention transformer block can provably implement policy-improvement methods, includ-ing semi-gradient SARSA and actor-critic, via explicit parameter constructions. Beyond existence, we design a teacher-mimicking training procedure, analyze its gradient-flow dynamics, and establish the first convergence guarantee in the ICRL literature: under suitable richness conditions on the training MDP distribution, gradient flow converges locally and exponentially to an optimal parameter manifold corresponding to the desired RL update. Empirically, training transformers on randomly generated tabular MDPs confirms these predictions: the learned models recover the parameter structure of our explicit constructions and, when deployed on unseen MDPs, deliver strong in-context control performance. Together, these results illuminate how transformer architectures internalize and execute classical reinforcement learning algorithms in context, bridging mechanistic understanding and training dynamics in ICRL. 

## 1 Introduction 

In-context learning (ICL) refers to a model’s ability to learn and perform a new task by conditioning on examples provided in its input, without any parameter updates [Brown et al., 2020, Garg et al., 2022, Dong et al., 2024]. The transformer architecture [Vaswani et al., 2017] has empirically demonstrated remarkable in-context learning capabilities across a wide range of tasks, among which Large Language Models (LLMs) are the most prominent example [Brown et al., 2020, Chowdhery et al., 2023, OpenAI et al., 2024]. This phenomenon has motivated substantial interest in understanding the mechanisms that enable transformers to learn in-context. A growing body of theoretical work has sought to understand this by asking: which algorithms can a transformer execute in context, and why can it implement them? 

For supervised learning problems, this question now has a fairly complete answer: trained transform-ers can be shown to implement variants of gradient descent and closed-form least squares on the in-context examples, with explicit constructions and, in special cases, end-to-end training-dynamics guarantees [Garg et al., 2022, von Oswald et al., 2023, Akyürek et al., 2023, Ahn et al., 2023, Mahankali et al., 2024, Bai et al., 2023, Zhang et al., 2024, Liang et al., 2025]. Beyond the supervised learning scenario, recent works have begun to extend this lens to in-context reinforcement learning (ICRL)[Laskin et al., 2023, Lin et al., 2024, Moeini et al., 2025], where an agent is given a trajectory in its context and must internally execute a policy evaluation or policy improvement step — core operations in the reinforcement learning framework [Sutton and Barto, 2018]. A central question in this line of work is: 

> Preprint.
> arXiv:2605.05755v1 [stat.ML] 7 May 2026

Can a transformer block implement – and be trained to implement standard RL algorithms such as temporal-difference (TD) methods, SARSA, or actor-critic? 

While these works, including Laskin et al. [2023], Lee et al. [2023], Brooks et al. [2023], and Raparthy et al. [2023], primarily focus on empirical experiments and demonstrate strong performance in various ICRL settings, they lack theoretical guarantees on the underlying mechanisms. Toward a theoretical answer, Wang et al. [2025] take a first step by showing that a linear-attention block admits a closed-form structure that implements policy evaluation via TD learning. Two questions, however, remain open. First, their analysis is restricted to policy evaluation, leaving open whether a transformer can also implement policy-improvement algorithms – a component that is no less central to RL. Second, existing analyses establish only the existence of an invariant set: once the parameters lie in the set, the expected update keeps them there. They do not show whether the training actually drives the parameters into this set in the first place, nor identify the conditions under which it does. In this work, we close both gaps: we move from policy evaluation to policy improvement by showing that a single linear-attention block can implement classical on-policy control updates – SARSA and actor-critic – and we move from invariance to convergence by identifying sufficient conditions under which gradient training provably drives the transformer parameters into the theoretical manifold that exactly realizes the target update. Our existence theorem is similar in form to Wang et al. [2025] in that both exhibit a closed-form weight configuration (P⋆, V⋆) for a single linear-attention block, but there are substantial differences. In Wang et al. [2025], their transformer outputs only the value estimate at the query sample, so the construction is tailored to a fixed policy evaluation problem and cannot be reused to drive further interaction. Our construction instead directly outputs the updated parameters themselves – the critic parameter w for SARSA, and both the critic w and the actor λ for actor-critic – which can then be utilized to update policy, sample new trajectories, and close the in-context policy-improvement loop. On the training-dynamics side, the optimum is no longer a single point but a continuous manifold of equivalent constructions, so global strong-convexity arguments do not apply directly; we instead establish a local Polyak–Łojasiewicz inequality on a tubular neighborhood of the manifold which, together with explicit richness conditions on the training-MDP distribution, yields exponential convergence of the gradient flow to a single point on the manifold. Our contributions are summarized as follows: • Existence Proof. We construct explicit parameters θ⋆ = ( P⋆, V⋆) for a single-head linear self-attention block such that TF θ⋆ exactly realizes the batch SARSA semi-gradient update (Theorem 3.1) and the batch actor-critic update (Theorem 3.3). • Local convergence. Under explicit richness assumptions on the training-MDP distribution (Assumption 1), we prove that by mimicking the teacher updates, the gradient flow on the population loss converges locally to a single point on the manifold (Theorem 3.2). To our knowledge this is the first convergence guarantee in the ICRL literature. • Empirical validation. We design ICRL training algorithms (Algorithm 1, Algorithm 2) for both the SARSA and actor-critic settings, and show that the resulting trained transformers recover the theoretical block structure of the attention parameters (P⋆, V⋆) (Section 4.1). Furthermore, when deployed on unseen MDPs, the trained transformers closely track their analytical teachers and drive policies toward oracle-level returns (Section 4.2). 

Additional related work: We provide a detailed discussion of more related work in Appendix A. 

## 2 Preliminaries 

2.1 Transformer architecture 

We consider a one-layer, single-head, linear-attention transformer block with parameters θ =

{(P, V)} ⊆ RD×D. Given input H ∈ RD×n, the output of the attention layer is given by 

Hout := H + 1

n (VH )( H⊤PH ) ∈ RD×n, (1) where P := Q⊤K absorbs the query and key projections. The transformer output is given by 

TF θ (H) := fread (Hout ) for a fixed readout fread : RD×n → RDout .22.2 Markov decision process 

A Markov decision process (MDP) is a tuple (S, A, P, R, γ ) with state space S, action space A,transition kernel P : S × A → P (S), reward R : S × A → R, and discount γ ∈ [0 , 1) , where 

P(X ) denotes the set of probability distributions over X . The goal is to find a policy π : S → P (A)

maximizing the (expected) return Gt := P∞ 

> k=0

γkrt+1+ k. The state- and action-value functions under π are 

vπ (s) := Eπ [Gt | st = s], qπ (s, a ) := Eπ [Gt | st = s, a t = a].

2.3 SARSA 

SARSA is an on-policy value-based algorithm. Given a feature map ϕ : S × A → Rd and parameter 

w ∈ Rd, we consider linear function approximation qw(s, a ) = w⊤ϕ(s, a ), and the batch semi-gradient SARSA update over an n-step trajectory is 

wSARSA = w + αn

> n−1

X

> i=0

ri+1 + γw⊤ϕ(si+1 , a i+1 ) − w⊤ϕ(si, a i)ϕ(si, a i), (2) where α > 0 is the step size and the bracketed quantity is the temporal-difference (TD) error. 

2.4 Actor-Critic 

Actor-Critic combines a parametric policy (the actor) and a parametric value function (the critic). Given a policy feature map ϕπ : S × A → Rm, a value feature map ϕV : S → Rd, and parameters 

λ ∈ Rm, w ∈ Rd, with softmax policy πλ(a | s) ∝ exp( λ⊤ϕπ (s, a )) and linear critic vw(s) = 

w⊤ϕV (s), the batch update over an n-step trajectory is 

wAC = w + βn

> n−1

X

> i=0

ri+1 + γw⊤ϕV (si+1 ) − w⊤ϕV (si)ϕV (si),

λAC = λ + αn

> n−1

X

> i=0

γiri+1 + γw⊤ϕV (si+1 ) − w⊤ϕV (si)gλ(si, a i),

(3) where α, β > 0 are step sizes, and gλ(s, a ) := ϕπ (s, a )−P 

> b

ϕπ (s, b )πλ(b | s) is the score function. 

## 3 Main Results 

We demonstrate that a single-head transformer block can learn to perform in-context policy im-provement using classical on-policy RL algorithms. Figure 1 illustrates the ICRL pipeline. Given a formulated input prompt H(z) constructed from the training sample z, we show that there exists θ⋆

such that the transformer block TF θ⋆ directly outputs the updated parameter w, which is then used to update the value function or policy. In Section 3.1 we establish the existence of such implementations of SARSA, and in Section 3.2 we analyze the training dynamics of the proposed training procedure and identify sufficient conditions for convergence to the optimum parameter manifold. We then extend these results to actor-critic in Section 3.3. 

3.1 Transformers can implement SARSA 

Let z = ( M, ϕ, w, τ ) denote a training sample, where M is the training MDP task, ϕ : S × A → Rd

is the feature map, w ∈ Rd is the parameter of the behavior policy π, and τ = ( s0, a 0, r 1, . . . , s n, a n)

is the resulting n-step trajectory in M. For notational convenience, we define the following quantities for i = 0 , . . . , n − 1:

ϕi := ϕ(si, a i) ∈ Rd, ϕ+ 

> i

:= ϕ(si+1 , a i+1 ) ∈ Rd, δi := ri+1 + γw⊤ϕ+ 

> i

− w⊤ϕi ∈ R,

xi := 

 ϕi

γϕ+

> i

ri+1 

 ∈ R2d+1 , ˜w := 

 1

w



∈ Rd+1 .

3TF θ    

> Attn (P,V)fread
> Agent
> HHout w

Figure 1: A simplified pipeline of transformer’s ICRL implementation. The agent samples a trajectory under the current policy, which is fed into the transformer block. The transformer outputs an updated parameter w, which is then used to update the value function and policy. We also define the following trajectory statistics: 

ˆΣ(z) := 1

n

> n−1

X

> i=0

xix⊤ 

> i

, R(z) := 1

n

> n−1

X

> i=0

ϕix⊤ 

> i

, b(z) := 1

n

> n−1

X

> i=0

xi δi.

Consider the training prompt as follows: 

H(z) = 

x0 · · · xn−1 00 · · · 0 ˜w



∈ RD×(n+1) , (4) where D = 3 d + 2 . We now establish the existence of a transformer block that implements the batch semi-gradient SARSA update (2). 

Theorem 3.1 (Implement semi-gradient SARSA with a transformer block) . Let the output projection function be fread (·) = ·−d:,−1, which extracts the last d elements of the last column. Given formulated input prompt (4) , for any fixed α > 0, there exists θ⋆ = {(cP⋆, c −1V⋆)} ⊆ RD×D, where c̸ = 0 

and 

P⋆ =

∗(2 d+1) ×(2 d+1) 

"0d×1 −Id

0d×1 Id

1 01×d

#

∗(d+1) ×(2 d+1) 0(d+1) ×(d+1) 

 , V⋆ =



∗(2 d+1) ×(2 d+1) ∗(2 d+1) ×(d+1) ∗1×d ∗1×d ∗

αId 0d×d 0d×1

 ∗1×(d+1) 

0d×(d+1) 

 ,

(5) 

such that transformer TF θ⋆ (H(z)) directly implements batch semi-gradient SARSA update (2) .

The proof is deferred to Appendix E. Theorem 3.1 guarantees the existence of a parameter configura-tion θ⋆ that realizes the semi-gradient SARSA update, but does not prescribe how to obtain it. To bridge this gap, we need to develop a training algorithm. 

3.2 Algorithm and training dynamics analysis 

We now design an ICRL algorithm to train the transformer to implement SARSA, as outlined in Algorithm 1. This procedure is inspired by Laskin et al. [2023]. At each iteration, trajectory data are sampled from the MDP, the corresponding SARSA update is computed as a target, and the transformer parameters are updated to minimize the discrepancy between the transformer’s prediction and the target update. The empirical loss at each step approximates the population loss defined below: 

L(θ) = Ez

 12 TF θ

 H(z) − wSARSA (z) 2



, (6) where wSARSA (z) is the target parameter given by the batch semi-gradient SARSA update (2) . We now analyze the training dynamics under this loss objective. We consider gradient flow, which captures the behavior of gradient descent with infinitesimal step size, and is described by the following ordinary differential equation (ODE): 

dθ(t)

dt = −∇L (θ(t)) . (7) We first identify the invariant parameter groups during training. 4Algorithm 1 IC-SARSA Transformer Training  

> 1:

Input: Transformer parameter θ(0) = ( P (0) , V (0) ), trajectory window length n, number of frames per MDP T , number of MDPs K, ϵ-greedy parameter ϵ, learning rate η, and SARSA step size α 

> 2:

for k = 0 , . . . , K − 1 do  

> 3:

Sample a MDP M(k) = ( S, A, P (k), R (k), γ (k)), and generate corresponding feature map 

ϕ(k) : S × A → Rd. Initialize parameter w(0) and state s(0) 0 randomly  

> 4:

for t = 0 , . . . , T − 1 do  

> 5:

Estimate state-action value function Q(k) 

> w(t)

(s, a ) = w(t)⊤ϕ(k)(s, a ), ∀(s, a ) ∈ S × A  

> 6:

Sample a n-step trajectory (s(t)0 , a (t)0 , r (t)1 , s (t)1 , a (t)1 , . . . , r (t) 

> n

, s (t) 

> n

, a (t) 

> n

) from the MDP 

M(k), according to ϵ-greedy policy derived from Q(k) 

> w(t)
> 7:

Formulate H(t) as in (4) with the sampled trajectory and current parameter w(t) 

> 8:

Compute w(t+1) = TF θ(t) (H(t)) := w(t) + 1

> n

V(t)H(t)H(t)⊤P(t)H(t) 

> −d:,−1
> 9:

Compute w(t+1)  

> SARSA

according to (2)  

> 10:

Compute loss ˆL(t) = 12 ∥w(t+1) − w(t+1) 

> SARSA

∥2 

> 11:

Update θ(t+1) ← θ(t) − η∇θ ˆL(t) 

> 12:

Update s(t+1) 0 ← s(t) 

> n

, w(t+1) ← w(t+1)  

> SARSA
> 13:

end for  

> 14:

Update θ(0) ← θ(T ) 

> 15:

end for  

> 16:

Return Trained transformer parameter θ

Proposition 3.1 (Invariant parameter groups) . Partition the attention parameters into four sub-blocks: 

P =

P11 P12 

P21 P22 



, V =

V11 V12 

V21 V22 



, (8) 

where P11 , V11 ∈ R(2 d+1) ×(2 d+1) , P12 , V12 ∈ R(2 d+1) ×(d+1) , P21 , V21 ∈ R(d+1) ×(2 d+1) , and 

P22 , V22 ∈ R(d+1) ×(d+1) . Let ¯V21 and ¯V22 denote the last d rows of V21 and V22 , respectively. Under gradient flow (7) , the following parameters are invariant: (i) The blocks P11 , P21 , V11 , V12 and the first rows of V21 , V22 do not affect the transformer output and remain unchanged throughout training. (ii) If P(0) 22 = 0 and ¯V(0) 22 = 0, then P(t)22 = 0 and ¯V(t)22 = 0 for all t ≥ 0.

The proof is deferred to Appendix F. Throughout the remainder of this section, we restrict our analysis to initializations satisfying P(0) 22 = 0 and ¯V(0) 22 = 0. By Proposition 3.1(ii), this constraint is preserved by gradient flow, and combined with (i) it reduces the training dynamics on (P, V) to dynamics on the effective parameter θeff := ( P12 , ¯V21 ) ∈ Θeff , where Θeff = R(2 d+1) ×(d+1) × Rd×(2 d+1) is the effective parameter space. From (5), the optimum structures of these sub-blocks are 

P⋆ 

> 12

:= 

"0d×1 −Id

0d×1 Id

1 01×d

#

∈ R(2 d+1) ×(d+1) , ¯V⋆ 

> 21

:= [ αId 0d×d 0d×1] ∈ Rd×(2 d+1) ,

subject to the scaling freedom (P⋆

> 12

, ¯V⋆

> 21

) 7 → (cP⋆

> 12

, c −1 ¯V⋆

> 21

) for any c̸ = 0 . Without loss of generality we restrict to c > 0 (the sign-flipped branch is handled by symmetry), and to a compact sub-interval I = [ c−, c +] ⊂ (0 , ∞) bounded away from the degenerate limits c → 0, ∞. On this interval, we define the optimal parameter manifold 

MI :=  ¯θeff (c) := ( cP⋆

> 12

, c −1 ¯V⋆

> 21

) : c ∈ I ⊂ Θeff , (9) and the normal space at ¯θeff (c) as 

NcMI := (U, W) ∈ Θeff : ⟨U, P⋆

> 12

⟩F − c−2⟨W, ¯V⋆

> 21

⟩F = 0 .

We now state the richness assumptions on the training-MDP distribution that ensure local convergence to the optimal parameter manifold (9). 5Assumption 1 (Richness of training MDPs) . Let Π denote the class of behavior policies used to generate training trajectories. There exist positive constants Bϕ, B r , B ˜w, κ ˜w, κ R, κ q and ρ ∈ [0 , 1) 

such that, for every π ∈ Π:(1) Boundedness. Almost surely, ∥ϕ(s, a )∥ ≤ Bϕ, |r| ≤ Br , and ∥ ˜w∥ ≤ B ˜w.(2) Parameter excitation. E[ ˜ w ˜w⊤] ⪰ κ ˜wId+1 .(3) Bellman-regressor excitation. E[R(z)⊤R(z) | w] ⪰ κRI2d+1 for every w ∈ Rd.(4) TD-target excitation. E[b(z)b(z)⊤] ⪰ κq I2d+1 .(5) No destructive cancellation. For every c ∈ I and (U, W) ∈ NcMI ,

Ez ⟨R(z)U ˜w, Wb (z)⟩ ≤ ρpEz ∥R(z)U ˜w∥2 · Ez ∥Wb (z)∥2.

We emphasize that Assumptions (1)–(2) can be enforced by construction: normalize features and clip rewards to achieve boundedness, and sample w from an isotropic distribution to ensure parameter excitation. Assumptions (3)–(5) are structural conditions on the trajectory distribution that are satisfied generically: under a random feature map ϕ, random rewards, and ϵ-greedy exploration ( ϵ > 0), the trajectory distribution provides excitation in the R and b directions, and the cross-correlation bound 

ρ < 1 holds in typical non-degenerate cases. Verification for specific MDP families is deferred to future work. 

Theorem 3.2 (Local convergence to the optimal parameter manifold) . Under Assumption 1, let 

BΣ := 2 B2 

> ϕ

+ B2 

> r

, CQ := 12 BΣB ˜w,m0 := (1 − ρ) min c−2+ α2κRκ ˜w, c 2

> −

κq , M0 := B2ΣB2˜w

 c−2 

> −

α2 + 2 c2+

 .

Define the tube 

Tr (I) :=  ¯θeff (c) + ∆ : c ∈ I, ∆ ∈ NcMI , ∥∆∥ < r , (10) 

where 0 < r < √m0 

> 3CQ

. Fix a compact subinterval I0 ⊂ int( I). Let 

ηI0,r := dist  MI0 , Θeff \ T r (I), (11) 

Suppose the initial parameter θeff (0) satisfies the following condition: 

dist( θeff (0) , MI0 ) < min 

r, ηI0,r 

1 + Kr (√M0+CQr)√2μr

 , (12) 

where Kr := √2BΣB ˜w

 c+

√2d + 1 + r2 +



c−1

> −

√d + r

21/2

. Consider the gradient flow 

(7) with initial point θeff (0) . Then the gradient flow solution exists for all t ≥ 0, remains in Tr (I),and satisfies 

L(θeff (t)) ≤ e−2μr t L(θeff (0)) ,

where μr := (m0−3CQ

> √m0r)2

(√M0+CQr)2 . Furthermore, θeff (t) converges to a point θeff (∞) ∈ MI , and 

∥θeff (t) − θeff (∞)∥ ≤ Kr

μr

q

L(θeff (0)) e−μr t.

Theorem 3.2 establishes that, when initialized sufficiently close to the optimal manifold MI , gradient flow provably converges to a point on that manifold at an exponential rate governed by μr . The key insight is that under Assumption 1 on the training MDPs, the loss function exhibits local contractivity: the manifold itself is a level set, but perturbations perpendicular to it decay exponentially. To briefly summarize our proof strategy, we first define the tube (10) , which specifies the region around MI

where a local Polyak–Łojasiewicz (PL) inequality holds (Lemma C.2). Next, the buffer distance (11) quantifies the slackness between the inner manifold patch MI0 and the boundary of the tube, providing the margin a trajectory can travel before risking exit. The initialization condition (12) then bounds the cumulative gradient-flow displacement by this margin ηI0,r , which ensures that the trajectory never leaves the tube and the PL inequality remains active for all t ≥ 0. The complete proof is deferred to Appendix G. 63.3 Transformers can implement Actor-Critic 

Having established both existence and convergence for the SARSA-based ICRL implementation, we now ask whether an analogous construction extends to the actor-critic method (3) , in which the transformer must jointly update a value function and a parameterized policy. To accommodate this, we construct the input prompt as follows: 

H =



ϕV (s0) · · · ϕV (sn−1) 0

γϕV (s1) · · · γϕV (sn) 0

r1 · · · rn 0

γ0gλ(s0, a 0) · · · γn−1gλ(sn−1, a n−1) 0

0 · · · 0 1

0 · · · 0 λ

0 · · · 0 w



∈ RD×(n+1) , (13) where D = 3 d + 2 m + 2 . Compared to the SARSA prompt (4) , the construction in (13) additionally encodes the discounted score function terms γigλ(si, a i) and the current actor parameter λ. The following theorem shows that a single transformer block can still exactly implement the batch actor-critic update. 

Theorem 3.3 (Implementing Actor-Critic with a transformer block) . Let the output projection function be fread (·) = ·−(d+m): ,−1, which extracts the last (d + m) entries of the final column. Given the input prompt (13) , for any α, β > 0 there exist parameters θ⋆ = {(cP⋆, c −1V⋆)} ⊆ RD×D with 

c̸ = 0 and 

P⋆ =



∗(2 d+m+1) ×(2 d+m+1) 



0d×1 0d×m −Id×d

0d×1 0d×m Id×d

1 01×m 01×d

0m×1 0m×m 0m×d



∗(d+m+1) ×(2 d+m+1) 0(d+m+1) ×(d+m+1) 

 ,

V⋆ =



∗(2 d+m+1) ×(2 d+m+1) ∗(2 d+m+1) ×(d+m+1) 

" ∗1×d ∗1×d ∗1×1 ∗1×m

0m×d 0m×d 0m×1 αIm×m

βId×d 0d×d 0d×1 0d×m

#  ∗1×(d+m+1) 

0m×(d+m+1) 

0d×(d+m+1) 

 ,

(14) 

such that the transformer TF θ⋆ directly implements the batch actor-critic update (3) .

The proof of Theorem 3.3 is provided in Appendix H. Similar to SARSA, we design a training procedure to learn the transformer parameters θ in Algorithm 2 in Appendix B. The convergence analysis follows the analogous arguments as in the SARSA setting and is therefore omitted. Empirical validation of convergence is provided in Section 4. 

## 4 Experiments 

We empirically demonstrate that gradient-based training of a single linear self-attention block con-verges to the optimal parameter manifold characterized in Theorem 3.1 and Theorem 3.3, using Algorithm 1 and Algorithm 2 respectively. Furthermore, we evaluate the control performance of the trained transformers, showing their ability to perform policy improvement on unseen MDPs. Additional details and experiments can be found in Appendix I. 

4.1 Training Common Training Setup. Both experiments share the same training MDP family. Each MDP has 

nS = 9 states and nA = 4 actions, with discount γ = 0 .5. For each MDP, the initial-state distribution 

P0 and transition kernel P (· | s, a ) are drawn independently from Dirichlet(1 , . . . , 1) (uniform over the probability simplex on nS states), and the rewards r(a, s ′) ∼ Unif( −1, 1) are sampled i.i.d. per (a, s ′). Trajectories of window length n = 20 are rolled out with horizon T = 1000 . In both experiments, the in-context learner is a one-layer transformer with corresponding embedding dimension D. Following Proposition 3.1, the inert blocks P11 , P21 , V11 , V12 , P22 , V22 are initialized to zero, while the effective sub-blocks (P12 , ¯V21) are initialized with Xavier-normal 7(a) SARSA: learned P. (b) SARSA: learned V.   

> (c) Actor-Critic: learned P.(d) Actor-Critic: learned V.

Figure 2: Final learned P and V matrices after training, for the SARSA transformer (top row) and the Actor-Critic transformer (bottom row). In both settings, the non-zero structure matches the theoretical constructions (P⋆, V⋆) in (5) and (14). entries (gain 0.1). Training is performed over K = 10 ,000 independently sampled MDPs using Adam optimizer [Kingma and Ba, 2015] with initial learning rate η = 10 −3 and exponential decay by a factor of 0.99 every 10 MDPs. 

SARSA experiment specifications. For each MDP, the state–action feature map ϕ : S × A → Rd

(d = nS nA = 36 ) and the initial weight w(0) ∈ Rd have entries drawn i.i.d. from Unif( −1, 1) . The behavior policy is ϵ-greedy ( ϵ = 0 .1) with Q(s, a ) = ϕ(s, a )⊤w, and the SARSA target wSARSA 

is computed from the trajectory window with step size α = 0 .2. The transformer uses embedding dimension D = 3 d + 2 = 110 .

Actor-Critic experiment specifications. For each MDP, the policy feature ϕπ : S × A → Rm

(m = 36 ), value feature ϕV : S → Rd (d = 9 ), and initial weights λ(0) ∈ Rm, w(0) ∈ Rd are drawn i.i.d. from Unif( −1, 1) . The behavior policy follows π(a | s) ∝ exp( ϕπ (s, a )⊤λ), with ϵ = 0 .1

– random exploration. At each step, the teacher applies a one-step critic update to w (step size 

β = 0 .8) and an actor update to λ (step size α = 0 .2). The transformer uses embedding dimension 

D = 3 d + 2 m + 2 = 101 .

Results. In both experiments, the training loss decays approximately exponentially to zero (see Appendix I.1). Figure 2 shows the learned P and V matrices: the upper-right block of P and the relevant rows of the lower-left block of V recover the structure of (P⋆, V⋆) in (5) and (14) (up to scaling), while other blocks remain near zero. This confirms that the ICRL training algorithms 1 and 2 drive the parameters toward the optimal manifold. 

4.2 Evaluation 

Beyond parameter-level fidelity, we evaluate whether the trained transformer, when deployed as an in-context update rule on unseen MDPs, induces high-return policies. We randomly sample 

K = 100 test MDPs with corresponding features, disjoint from the training set. For each MDP, we run closed-loop in-context updates: at each step, the prompt H(t) is fed to the transformer to produce 

w(t+1) (or (λ(t+1) , w(t+1) ) in the actor–critic setting), which defines the behavior policy used to generate the next trajectory window. Every 10 update steps, we freeze the iterate and estimate the discounted return via Monte Carlo using 32 trajectories of length 50, with initial state s0 ∼ P (k)0 .8(a) SARSA transformer. (b) Actor-Critic transformer. 

Figure 3: Closed-loop control performance on 100 held-out random MDPs versus in-context update step t. Curves show the per-MDP return averaged across MDPs (shaded: 25 –75% interquartile band). The transformer curve closely tracks the teacher curve in both settings, indicating that the trained model approximately implements the analytical update rule at deployment. We compare against analytical teacher updates (SARSA (2) and actor–critic (3) ) executed in the same closed loop, the oracle greedy policy under Q⋆ obtained by exact value iteration on (P (k), R (k)), and a uniform random policy. Figure 3 reports the average return across MDPs. In both settings, the transformer closely tracks the teacher: performance improves rapidly from near-random initialization and approaches the oracle as t increases. Crucially, this consistency persists across long in-context horizons, indicating that the transformer faithfully implements the teacher update rule rather than merely mimicking short-horizon behavior. Notably, the remaining gap to the oracle reflects limitations of the linear function approximation rather than imperfect imitation, since the same gap is also observed for the analytical teacher. These results show that convergence to the optimal parameter manifold (Theorem 3.2) translates into strong policy-level performance, with the trained transformer accurately reproducing the analytical update rules at deployment. 

## 5 Conclusion 

In this work, we demonstrate that transformers can provably implement ICRL algorithms that perform policy improvement. Through explicit constructions, we show that a single linear-attention block can realize batch SARSA and actor-critic updates. Furthermore, we provide the first convergence guarantee in the ICRL literature, proving that under suitable richness conditions on the training MDPs, gradient-flow training to mimic the teacher update converges locally to the optimal parameter manifold. Our empirical results validate these theoretical findings, showing that trained transformers recover the predicted parameter structures and achieve strong control performance on unseen MDPs. These results help bridge the gap between the theoretical understanding and practical implementation of ICRL, suggesting that the transformer architecture naturally favors learning and implementing reinforcement-learning algorithmic structures. Overall, this work advances our understanding of how transformers perform RL algorithms in context, with implications for both AI safety and the design of more capable learning systems. 

5.1 Limitations 

Our work has several limitations that need to be addressed in future work. First, Assumption 1 imposes richness conditions on the training MDPs that may be difficult to verify in practice. While such conditions are expected to hold generically under random features, rewards, and sufficient exploration, precisely characterizing the required trajectory diversity remains open. Second, our convergence guarantee is local and requires initialization near the optimal manifold; understanding the global optimization landscape is an important direction for future work. Finally, our analysis focuses on deterministic gradient flow in a simplified setting, leaving extensions to stochastic optimization and more complex transformer architectures for future study. 9References 

Tom Brown, Benjamin Mann, Nick Ryder, Melanie Subbiah, Jared D Kaplan, Prafulla Dhariwal, Arvind Neelakantan, Pranav Shyam, Girish Sastry, Amanda Askell, Sandhini Agarwal, Ariel Herbert-Voss, Gretchen Krueger, Tom Henighan, Rewon Child, Aditya Ramesh, Daniel Ziegler, Jeffrey Wu, Clemens Winter, Chris Hesse, Mark Chen, Eric Sigler, Mateusz Litwin, Scott Gray, Benjamin Chess, Jack Clark, Christopher Berner, Sam McCandlish, Alec Radford, Ilya Sutskever, and Dario Amodei. Language models are few-shot learners. In Proceedings of Neural Information Processing Systems , Virtual, December 2020. URL https://arxiv.org/abs/2005.14165 .Shivam Garg, Dimitris Tsipras, Percy S Liang, and Gregory Valiant. What can transformers learn in-context? a case study of simple function classes. In Proceedings of Neural Information Processing Systems , New Orleans, LA, USA, December 2022. URL https://arxiv.org/abs/ 2208.01066 .Qingxiu Dong, Lei Li, Damai Dai, Ce Zheng, Jingyuan Ma, Rui Li, Heming Xia, Jingjing Xu, Zhiyong Wu, Tianyu Liu, Baobao Chang, Xu Sun, Lei Li, and Zhifang Sui. A survey on in-context learning, 2024. URL https://arxiv.org/abs/2301.00234 .Ashish Vaswani, Noam Shazeer, Niki Parmar, Jakob Uszkoreit, Llion Jones, Aidan NGomez, Lukasz Kaiser, and Illia Polosukhin. Attention is all you need. In Pro-ceedings of Neural Information Processing Systems , Long Beach, CA, USA, Decem-ber 2017. URL https://proceedings.neurips.cc/paper_files/paper/2017/file/ 3f5ee243547dee91fbd053c1c4a845aa-Paper.pdf .Aakanksha Chowdhery, Sharan Narang, Jacob Devlin, Maarten Bosma, Gaurav Mishra, Adam Roberts, Paul Barham, Hyung Won Chung, Charles Sutton, Sebastian Gehrmann, et al. PaLM: Scaling language modeling with pathways. Journal of Machine Learning Research , 24(240):1–113, 2023. OpenAI, Josh Achiam, Steven Adler, Sandhini Agarwal, Lama Ahmad, Ilge Akkaya, Florencia Leoni Aleman, Diogo Almeida, Janko Altenschmidt, Sam Altman, Shyamal Anadkat, et al. GPT-4 technical report. arXiv preprint arXiv:2303.08774 , 2024. URL https://arxiv.org/abs/2303. 08774 .Johannes von Oswald, Eyvind Niklasson, Ettore Randazzo, João Sacramento, Alexander Mordvintsev, Andrey Zhmoginov, and Max Vladymyrov. Transformers learn in-context by gradient descent. In Proceedings of the 40th International Conference on Machine Learning , 2023. URL https: //arxiv.org/abs/2212.07677 .Ekin Akyürek, Dale Schuurmans, Jacob Andreas, Tengyu Ma, and Denny Zhou. What learning algorithm is in-context learning? Investigations with linear models. In The Eleventh International Conference on Learning Representations , 2023. URL https://openreview.net/forum?id= 0g0X4H8yN4I .Kwangjun Ahn, Xiang Cheng, Hadi Daneshmand, and Suvrit Sra. Transformers learn to imple-ment preconditioned gradient descent for in-context learning. In Thirty-seventh Conference on Neural Information Processing Systems , 2023. URL https://openreview.net/forum?id= LziniAXEI9 .Arvind Mahankali, Tatsunori B. Hashimoto, and Tengyu Ma. One step of gradient descent is provably the optimal in-context learner with one layer of linear self-attention. In The Twelfth International Conference on Learning Representations , 2024. URL https://arxiv.org/abs/2307.03576 .Yu Bai, Fan Chen, Huan Wang, Caiming Xiong, and Song Mei. Transformers as statisticians: Provable in-context learning with in-context algorithm selection. In Thirty-seventh Conference on Neural Information Processing Systems , 2023. URL https://openreview.net/forum?id= liMSqUuVg9 .Ruiqi Zhang, Spencer Frei, and Peter L. Bartlett. Trained transformers learn linear models in-context. In Journal of Machine Learning Research , 2024. URL https://arxiv.org/abs/2306.09927 .10 Haodong Liang, Krishna Balasubramanian, and Lifeng Lai. Transformers handle endogeneity in in-context linear regression. In The Thirteenth International Conference on Learning Representations ,2025. URL https://openreview.net/forum?id=QfhU3ZC2g1 .Michael Laskin, Luyu Wang, Junhyuk Oh, Emilio Parisotto, Stephen Spencer, Richie Steigerwald, DJ Strouse, Steven Stenberg Hansen, Angelos Filos, Ethan Brooks, Maxime Gazeau, Himanshu Sahni, Satinder Singh, and Volodymyr Mnih. In-context reinforcement learning with algorithm distillation. In The Eleventh International Conference on Learning Representations , 2023. URL 

https://openreview.net/forum?id=hy0a5MMPUv .Licong Lin, Yu Bai, and Song Mei. Transformers as decision makers: Provable in-context reinforce-ment learning via supervised pretraining. In The Twelfth International Conference on Learning Representations , 2024. URL https://arxiv.org/abs/2310.08566 .Amir Moeini, Jiuqi Wang, Jacob Beck, Ethan Blaser, Shimon Whiteson, Rohan Chandra, and Shangtong Zhang. A survey of in-context reinforcement learning, 2025. URL https://arxiv. org/abs/2502.07978 .Richard S. Sutton and Andrew G. Barto. Reinforcement Learning: An Introduction . MIT Press, Cambridge, MA, 2 edition, 2018. Jonathan Lee, Annie Xie, Aldo Pacchiano, Yash Chandak, Chelsea Finn, Ofir Nachum, and Emma Brunskill. Supervised pretraining can learn in-context reinforcement learning. In Thirty-seventh Conference on Neural Information Processing Systems , 2023. URL https://openreview.net/ forum?id=dCYBAGQXLo .Ethan Brooks, Logan Walls, Richard L. Lewis, and Satinder Singh. Large language models can implement policy iteration. In Thirty-seventh Conference on Neural Information Processing Systems , 2023. URL https://arxiv.org/abs/2210.03821 .Sharath Chandra Raparthy, Eric Hambro, Robert Kirk, Mikael Henaff, and Roberta Raileanu. Learn-ing to solve new sequential decision-making tasks with in-context learning. In NeurIPS 2023 Foundation Models for Decision Making Workshop , 2023. URL https://openreview.net/ forum?id=JB3H6LS1PY .Jiuqi Wang, Ethan Blaser, Hadi Daneshmand, and Shangtong Zhang. Transformers can learn temporal difference methods for in-context reinforcement learning. In The Thirteenth International Conference on Learning Representations , 2025. URL https://openreview.net/forum?id= Pj06mxCXPl .Diederik P. Kingma and Jimmy Ba. Adam: A method for stochastic optimization. In International Conference on Learning Representations (ICLR) , 2015. URL https://arxiv.org/abs/1412. 6980 .Lili Chen, Kevin Lu, Aravind Rajeswaran, Kimin Lee, Aditya Grover, Michael Laskin, Pieter Abbeel, Aravind Srinivas, and Igor Mordatch. Decision transformer: Reinforcement learning via sequence modeling. In A. Beygelzimer, Y. Dauphin, P. Liang, and J. Wortman Vaughan, editors, Advances in Neural Information Processing Systems , 2021. URL https://openreview.net/forum?id= a7APmM4B9d .Michael Janner, Qiyang Li, and Sergey Levine. Offline reinforcement learning as one big se-quence modeling problem. In M. Ranzato, A. Beygelzimer, Y. Dauphin, P.S. Liang, and J. Wort-man Vaughan, editors, Advances in Neural Information Processing Systems , volume 34, pages 1273–1286. Curran Associates, Inc., 2021. URL https://proceedings.neurips.cc/paper_ files/paper/2021/file/099fe6b0b444c23836c4a5d07346082b-Paper.pdf .Jake Grigsby, Linxi Fan, and Yuke Zhu. AMAGO: Scalable in-context reinforcement learning for adaptive agents. In The Twelfth International Conference on Learning Representations , 2024. URL 

https://openreview.net/forum?id=M6XWoEdmwf .Akshay Krishnamurthy, Keegan Harris, Dylan J Foster, Cyril Zhang, and Aleksandrs Slivkins. Can large language models explore in-context? In The Thirty-eighth Annual Conference on Neural Information Processing Systems , 2024. URL https://openreview.net/forum?id= OWPzhVqIux .11 Yu Huang, Yuan Cheng, and Yingbin Liang. In-context convergence of transformers, 2023. URL 

https://arxiv.org/abs/2310.05249 .Siyu Chen, Heejune Sheen, Tianhao Wang, and Zhuoran Yang. Training dynamics of multi-head softmax attention for in-context learning: Emergence, convergence, and optimality, 2024. URL 

https://arxiv.org/abs/2402.19442 .Yuandong Tian, Yiping Wang, Beidi Chen, and Simon Du. Scan and snap: Understanding training dynamics and token composition in 1-layer transformer. In Thirty-seventh Conference on Neural Information Processing Systems , 2023. URL https://arxiv.org/abs/2305.16380 .Eshaan Nichani, Alex Damian, and Jason D. Lee. How transformers learn causal structure with gradient descent. In Forty-first International Conference on Machine Learning , 2024. URL 

https://openreview.net/forum?id=jNM4imlHZv .

## A Additional Related Works 

In this appendix, we discuss additional works that are related to our paper. 

In-context learning theory for supervised tasks. Garg et al. [2022] initiate this discussion on simple function classes, showing empirically that a transformer trained from scratch can in-context learn linear regression, two-layer neural networks, and decision trees. A subsequent line of mecha-nistic work then asks how such behavior arises. von Oswald et al. [2023] and Akyürek et al. [2023] argue that transformers implement gradient descent or closed-form least squares on the in-context examples, and Ahn et al. [2023] and Mahankali et al. [2024] prove that, on linear regression, the optimal one-layer linear self-attention block is exactly one step of preconditioned gradient descent. Bai et al. [2023] broaden the picture by showing that transformers can implement a wide family of statistical estimators – including ridge regression, Lasso, and gradient descent on generalized linear models – and can even perform algorithm selection in context. Liang et al. [2025] further extend this line to in-context linear regression with endogenous covariates, constructing a multi-layer transformer that implements two-stage least squares and showing that it consistently estimates the structural parameter under standard instrumental-variable assumptions. 

In-context reinforcement learning. The view of RL as in-context sequence modeling was popular-ized by the Decision Transformer [Chen et al., 2021] and the Trajectory Transformer [Janner et al., 2021], which cast policy improvement as autoregressive prediction over offline trajectories. Building on this paradigm, Laskin et al. [2023] introduce algorithm distillation , in which a causal transformer trained on RL learning histories reproduces the underlying learning algorithm at inference time, and Lee et al. [2023] show that supervised pretraining over offline trajectories suffices to elicit ICRL on new tasks. Subsequent empirical work demonstrates that large language models can in-context execute policy iteration on tabular MDPs [Brooks et al., 2023]; that pretraining on sufficiently diverse trajectories enables transformers to generalize to unseen sequential decision-making tasks purely from their context [Raparthy et al., 2023]; that scalable long-context architectures such as AMAGO learn adaptive policies in context across challenging environments [Grigsby et al., 2024]; and that LLMs can perform nontrivial exploration in bandit and small MDP problems even without exter-nal scaffolding [Krishnamurthy et al., 2024]. On the theoretical side, Lin et al. [2024] prove that supervised pretraining of transformers on offline trajectories yields near-optimal posterior-sampling policies, establishing sample-complexity guarantees for ICRL pretraining. The work most closely related to ours is Wang et al. [2025], which shows that a linear-attention block admits an invariant set under the expected gradient update of a value-prediction loss, thereby implementing policy evaluation via TD methods. Our work extends this line in two directions: from policy evaluation to policy improvement (SARSA and actor-critic), and from invariance to convergence of gradient training. 

Training Dynamics. A complementary line of work studies the optimization landscape and training dynamics of attention itself, asking whether the in-context algorithms identified by mechanistic con-structions are actually recoverable by gradient-based learning. Zhang et al. [2024] prove that gradient flow on a single linear self-attention block trained on random linear-regression tasks converges to the global minimum and recovers the preconditioned-gradient-descent solution predicted by Ahn et al. [2023], Mahankali et al. [2024]. Huang et al. [2023] extend such convergence guarantees to softmax 12 attention under orthogonal feature assumptions, and Chen et al. [2024] analyze multi-head softmax attention in the in-context regression setting, identifying an emergent task-specialization phenomenon en route to the global minimizer. From a different angle, Tian et al. [2023] dissect token-level dynamics of one-layer attention during language pretraining, while Nichani et al. [2024] show that transformers can recover hidden causal structure via gradient descent on synthetic tasks. These results are confined to supervised regression-style objectives. Our work pushes the training-dynamics analysis into ICRL, where the loss is non-convex and the optima form a one-dimensional scaling manifold rather than an isolated point. 

## B Training Algorithm 

B.1 Actor-Critic Transformer Algorithm 2 Actor Critic Transformer Training  

> 1:

Input: Transformer parameter θ(0) = ( P (0) , V (0) ), trajectory window length n, number of frames per MDP T , number of MDPs K, ϵ-greedy parameter ϵ, learning rate η, actor step size α,and critic step size β 

> 2:

for k = 0 , . . . , K − 1 do  

> 3:

Sample a MDP M(k) = ( S, A, P (k), R (k), γ ), and generate corresponding feature map for policy ϕ(k) 

> π

: S × A → Rm, and feature map for state value function ϕ(k) 

> V

: S → Rd.Initialize parameters λ(0) , w(0) , and state s(0) 0 randomly  

> 4:

for t = 0 , . . . , T − 1 do  

> 5:

Calculate policy π(k) 

> λ(t)

(a|s) = exp {λ(t)⊤ϕ(k)    

> π(s,a )}
> P
> bexp {λ(t)⊤ϕ(k)
> π(s,b )}

, ∀s ∈ S , a ∈ A  

> 6:

Estimate state value function v(k) 

> w(t)

(s) = w(t)⊤ϕ(k) 

> V

(s), ∀s ∈ S  

> 7:

Compute policy gradient g(k) 

> λ(t)

(s, a ) = ϕ(k) 

> π

(s, a ) − P 

> b

ϕ(k) 

> π

(s, b )π(k) 

> λ(t)

(b|s), ∀(s, a ) ∈S × A  

> 8:

Sample n steps (s(t)0 , a (t)0 , r (t)1 , s (t)1 , a (t)1 , . . . , r (t) 

> n

, s (t) 

> n

, a (t) 

> n

) from the MDP M(k), accord-ing to policy π(k) 

> λ(t)

with ϵ-greedy exploration  

> 9:

Formulate H(t) as in (13) with the sampled trajectory and current parameters λ(t), w(t) 

> 10:

Compute 

λ(t+1) 

w(t+1) 



= TF θ(t) (H(t)) := 

λ(t)

w(t)



+ 1

> n

V(t)H(t)H(t)⊤P(t)H(t) 

> (d+m): ,−1
> 11:

Compute w(t+1)  

> AC

and λ(t+1)  

> AC

according to (3)  

> 12:

Compute loss ˆL(t) = 12 ∥w(t+1) − w(t+1)  

> AC

∥2 + 12 ∥λ(t+1) − λ(t+1)  

> AC

∥2 

> 13:

Update θ(t+1) ← θ(t) − η∇θ ˆL(t) 

> 14:

Update s(t+1) 0 ← s(t) 

> n

, λ(t+1) ← λ(t+1)  

> AC

, w(t+1) ← w(t+1)  

> AC
> 15:

end for  

> 16:

Update θ(0) ← θ(T ) 

> 17:

end for  

> 18:

Return Learned transformer parameter θ

Algorithm 2 extends Algorithm 1 to jointly learn both the value and policy parameters. At each iteration, trajectories are sampled from the MDP, and the corresponding actor-critic updates are computed as teacher signals for both the critic parameter w and the actor parameter λ. The transformer parameters are then updated via gradient descent to match these targets. The empirical loss combines the squared errors for both parameter updates, encouraging the transformer to approximate the full actor-critic learning rule. 13 C Supporting Lemmas 

Lemma C.1 (Transformer output decomposition) . Consider a transformer block with parameters 

θ = {(P, V)} ⊆ RD×D. We decompose the parameters into blocks as follows: 

P =

P11 P12 

P21 P22 



, V =

V11 V12 

V21 V22 



,

where P11 , V11 ∈ R(2 d+1) ×(2 d+1) , P12 , V12 ∈ R(2 d+1) ×(d+1) , P21 , V21 ∈ R(d+1) ×(2 d+1) , and 

P22 , V22 ∈ R(d+1) ×(d+1) . Let the output projection function be fread (·) = ·−d:,−1, which extracts the last d elements of the last column. Denote ˜w := [1 w]⊤. Given formulated input prompt 

H =

x0 · · · xn−1 00 · · · 0 ˜w



∈ RD×(n+1) ,

the transformer output can then be written as: 

TF θ (H) = w + ¯V21 ˆΣP 12 ˜w + 1

n ¯V22 ˜w ˜w⊤P22 ˜w, (15) where ˆΣ := 1

> n

Pn−1 

> i=0

xix⊤ 

> i

, and ¯V21 , ¯V22 denote the last d rows of V21 and V22 , respectively. 

Lemma C.2 (Local Polyak–Łojasiewicz inequality) . Suppose Assumption 1 holds, and let the optimal manifold MI , the constants B ˜w, B Σ, C Q, m 0, M 0, and the tube Tr (I) be as defined in Theorem 3.2. Then L attains its minimum value L⋆ = 0 on MI , and for every 0 < r < √m0 

> 3CQ

the population loss satisfies 

12 ∥∇L (θeff )∥2 ≥ μr

 L(θeff ) − L ⋆, ∀ θeff ∈ T r (I), (16) where 

μr := 

 m0 − 3CQ

√m0 r2

 √M0 + CQr2 .

## D Proof of Supporting Lemmas 

D.1 Proof of Lemma C.1 

Proof. From (4), the input matrix is 

H =

x0 · · · xn−1 00 · · · 0 ˜w



∈ RD×(n+1) .

By the definition of the linear self-attention block in (1), 

Hout = H + 1

n (VH )( H⊤PH ) ∈ RD×(n+1) ,

and the readout extracts the last d entries of the last column of Hout . Denote the last column of the input by 

hn+1 := 

02d+1 

˜w



∈ RD .

Then we have 

Hout [: , n + 1] = hn+1 + 1

n (VH )( H⊤Ph n+1 ). (17) Using the block partition (8), 

Ph n+1 =

P11 P12 

P21 P22 

  0

˜w



=

P12 ˜wP22 ˜w



.

14 Hence  H⊤Ph n+1 

 

> i+1

= x⊤ 

> i

0 P12 ˜wP22 ˜w



= x⊤ 

> i

P12 ˜w, i = 0 , . . . , n − 1,

 H⊤Ph n+1 

 

> n+1

= 0 ˜w⊤ P12 ˜wP22 ˜w



= ˜ w⊤P22 ˜w.

(18) Similarly, 

(VH )[: , i + 1] = V

xi

0



=

V11 xi

V21 xi



, i = 0 , . . . , n − 1,

(VH )[: , n + 1] = V

 0

˜w



=

V12 ˜wV22 ˜w



.

(19) Expanding the matrix-vector product as a sum over columns and combining (18) and (19), we have 

(VH ) H⊤Ph n+1 

 =

> n−1

X

> i=0

V11 xi

V21 xi



x⊤ 

> i

P12 ˜w +

V12 ˜wV22 ˜w



˜w⊤P22 ˜w

=

V11 

 P n−1 

> i=0

xix⊤

> i

P12 ˜w + V12 ˜w ˜w⊤P22 ˜wV21 

 P n−1 

> i=0

xix⊤

> i

P12 ˜w + V22 ˜w ˜w⊤P22 ˜w



.

Thus, rescaling by 1 

> n

, the lower (d + 1) -dimensional block of (17) reads 

˜w + V21 ˆΣP 12 ˜w + 1

n V22 ˜w ˜w⊤P22 ˜w.

Since ˜w = [1 , w⊤]⊤, the last d entries of ˜w equal w. The readout fread (·) = ·−d:,−1 extracts the last d rows of the lower block, which selects the last d rows of V21 and V22 , namely ¯V21 and ¯V22 .Therefore, 

TF θ (H) = w + ¯V21 ˆΣP 12 ˜w + 1

n ¯V22 ˜w ˜w⊤P22 ˜w,

which completes the proof. 

D.2 Proof of Lemma C.2 

Proof. For any effective parameter θeff = ( P12 , ¯V21 ), define the auxiliary predictor 

fθeff (z) := ¯V21 ˆΣ(z)P12 ˜w ∈ Rd.

By Theorem 3.1, (P⋆

> 12

, ¯V⋆

> 21

) is a global minimizer of L with L⋆ = 0 , and the scaling identity 

f(cP⋆  

> 12 ,c −1¯V⋆
> 21 )

= f(P⋆  

> 12 ,¯V⋆
> 21 )

for every c > 0 implies that every point of MI is a global minimizer. Consequently, 

L(θeff ) − L ⋆ = 12 Ez fθeff (z) − f ¯θeff (c)(z) 2, ∀c ∈ I. (20) Throughout the proof, we denote ∆ = ( U, W) ∈ NcMI with ∥∆∥ ≤ r, where ∥∆∥2 := ∥U∥2 

> F

+

∥W∥2 

> F

, and set θeff := ¯θeff (c) + ∆ . Write ∥ · ∥ L2 := pEz ∥ · ∥ 2.

Step 1: Decomposition of the prediction error. By the definition of fθeff together with θeff =(cP⋆ 

> 12

+ U, c −1 ¯V⋆ 

> 21

+ W),

fθeff (z) = ( c−1 ¯V⋆ 

> 21

+ W) ˆΣ(z)( cP⋆ 

> 12

+ U) ˜ w,f ¯θeff (c)(z) = c−1 ¯V⋆ 

> 21

ˆΣ(z) cP⋆ 

> 12

˜w = ¯V⋆ 

> 21

ˆΣ(z)P⋆ 

> 12

˜w.

Expanding the product and cancelling the matched leading term, 

fθeff (z) − f ¯θeff (c)(z) = c−1 ¯V⋆ 

> 21

ˆΣ(z)U ˜w + cW ˆΣ(z)P⋆ 

> 12

˜w + W ˆΣ(z)U ˜w.

We now identify the first two terms with the trajectory statistics R(z) and b(z). Since ¯V⋆ 

> 21

=[αId 0d×d 0d×1], we have ¯V⋆

> 21

xi = αϕi, so 

¯V⋆ 

> 21

ˆΣ(z) = 1

n

> n−1

X

> i=0

( ¯V⋆

> 21

xi)x⊤ 

> i

= αn

> n−1

X

> i=0

ϕix⊤ 

> i

= α R(z).

15 Similarly, P⋆ 

> 12

˜w = [ −w; w; 1] , hence x⊤ 

> i

P⋆ 

> 12

˜w = −ϕ⊤ 

> i

w + γ(ϕ+ 

> i

)⊤w + ri+1 = δi, so 

ˆΣ(z)P⋆ 

> 12

˜w = 1

n

> n−1

X

> i=0

xi(x⊤ 

> i

P⋆ 

> 12

˜w) = 1

n

> n−1

X

> i=0

xiδi = b(z).

Substituting these identities, 

fθeff (z) − f ¯θeff (c)(z) = c−1α R(z)U ˜w + c Wb (z) + W ˆΣ(z)U ˜w

=: X(z) + Y(z), (21) where 

X(z) := c−1α R(z)U ˜w + c Wb (z), Y(z) := W ˆΣ(z)U ˜w.

Step 2: Lower bound on ∥X∥L2 . We first lower-bound the two summands of X(z) separately. Expanding the squared norm and conditioning on ˜w,

Ez ∥R(z)U ˜w∥2 = Ez

 ˜w⊤U⊤R(z)⊤R(z) U ˜w

= Ez

 ˜w⊤U⊤E[R(z)⊤R(z) | ˜w] U ˜w

≥ κR Ez ∥U ˜w∥2

= κR tr  U Ez [ ˜ w ˜w⊤] U⊤

≥ κRκ ˜w∥U∥2 

> F

, (22) where the third line uses Assumption 1 (3), and the last inequality uses Assumption 1 (2). Similarly, 

Ez ∥Wb (z)∥2 = Ez

b(z)⊤W⊤Wb (z)

= Ez

tr  Wb (z)b(z)⊤W⊤ 

= tr  W Ez [b(z)b(z)⊤] W⊤

≥ κq tr( WW ⊤)= κq ∥W∥2 

> F

, (23) where the fourth line uses Assumption 1 (4). Set A := Ez ∥c−1αR(z)U ˜w∥2 and B := 

Ez ∥cWb (z)∥2. Assumption 1 (5) bounds the cross term as 

2Ez ⟨c−1αR(z)U ˜w, c Wb (z)⟩ ≤ 2ρ√AB, 

so expanding ∥X(z)∥2 and applying the cross-term bound, 

Ez ∥X(z)∥2 = A + B + 2 Ez c−1αR(z)U ˜w, c Wb (z)

≥ A + B − 2ρ√AB 

= (1 − ρ)( A + B) + ρ(√A − √B)2

≥ (1 − ρ)( A + B), (24) The two summands A, B are bounded below using the bounds (22) (23) and |c| ∈ [c−, c +]:

A = c−2α2 Ez ∥R(z)U ˜w∥2 ≥ c−2α2κRκ ˜w∥U∥2 

> F

≥ c−2+ α2κRκ ˜w∥U∥2 

> F

,B = c2 Ez ∥Wb (z)∥2 ≥ c2κq ∥W∥2 

> F

≥ c2

> −

κq ∥W∥2 

> F

.

Plugging into (24), with ∥∆∥2 = ∥U∥2 

> F

+ ∥W∥2 

> F

,

Ez ∥X(z)∥2 ≥ (1 − ρ) c−2+ α2κRκ ˜w∥U∥2 

> F

+ c2

> −

κq ∥W∥2

> F



≥ (1 − ρ) min c−2+ α2κRκ ˜w, c 2

> −

κq

 ∥U∥2 

> F

+ ∥W∥2

> F



= m0∥∆∥2.

Hence 

∥X∥L2 ≥ √m0 ∥∆∥. (25) 16 Step 3: Upper bounds on ∥X∥L2 and ∥Y∥L2 . By Assumption 1 (1), 

∥xi∥2 = ∥ϕi∥2 + γ2∥ϕ+ 

> i

∥2 + r2 

> i+1

≤ 2B2 

> ϕ

+ B2 

> r

= BΣ,

hence, applying the triangle inequality to ˆΣ(z) = 1

> n

Pn−1 

> i=0

xix⊤ 

> i

,

∥ ˆΣ(z)∥op ≤ 1

> nn−1

X

> i=0

∥xix⊤ 

> i

∥op = 1

> nn−1

X

> i=0

∥xi∥2 ≤ BΣ.

Writing ϕi = Sx i with S = [ Id, 0d×d, 0d×1] ∈ Rd×(2 d+1) , we have 

R(z) = 1

> n

X

> i

Sx ix⊤ 

> i

= S ˆΣ(z),

and SS ⊤ = Id gives ∥S∥op = 1 . Submultiplicativity then yields 

∥R(z)∥op = ∥S ˆΣ(z)∥op 

≤ ∥ S∥op ∥ ˆΣ(z)∥op 

≤ BΣ.

Also ∥ ˜w∥ ≤ B ˜w. Since P⋆⊤ 

> 12

P⋆ 

> 12

= diag(1 , 2Id), ∥P⋆

> 12

∥op = √2, hence 

∥b(z)∥ ≤ ∥ P⋆

> 12

∥op ∥ ˆΣ(z)∥op ∥ ˜w∥≤ √2 BΣB ˜w.

By the triangle inequality and submultiplicativity, 

∥X∥L2 ≤ c−1αR(z)U ˜w L2 + c Wb (z) L2

≤ | c|−1α ∥R(z)∥op ∥U∥op ∥ ˜w∥ + |c| ∥ W∥op ∥b(z)∥≤ c−1 

> −

αB ΣB ˜w∥U∥F + √2 c+BΣB ˜w∥W∥F .

Applying Cauchy–Schwarz to the right-hand side, 

∥X∥L2 ≤ BΣB ˜w

q

c−2 

> −

α2 + 2 c2+

q

∥U∥2 

> F

+ ∥W∥2 

> F

(26) 

= pM0 ∥∆∥. (27) For the quadratic term, 

∥Y(z)∥ = ∥W ˆΣ(z)U ˜w∥≤ ∥ W∥op ∥ ˆΣ(z)∥op ∥U∥op ∥ ˜w∥≤ BΣB ˜w∥U∥F ∥W∥F

≤ BΣB ˜w

  12 ∥U∥2 

> F

+ 12 ∥W∥2

> F



= CQ∥∆∥2.

Thus 

∥Y∥L2 ≤ CQ∥∆∥2. (28) 

Step 4: Excess-loss upper bound. By (20), (21), and (26)–(28), 

L(θeff ) − L ⋆ = 12 ∥X + Y∥2

> L2

≤ 12

 p M0 ∥∆∥ + CQ∥∆∥22

≤ 12

 p M0 + CQr2∥∆∥2, (29) where the last line uses ∥∆∥ ≤ r.17 Step 5: Directional derivative lower bound. Define 

g(t) := L( ¯θeff (c) + t∆) .

By the chain rule, 

g′(1) = ⟨∇L (θeff ), ∆⟩,

so Cauchy–Schwarz gives 

∥∇L (θeff )∥ ∥ ∆∥ ≥ g′(1) . (30) Thus, to establish the PL inequality, it suffices to lower-bound g′(1) with respect to ∥∆∥2. We now compute g′(1) . Replacing ∆ = ( U, W) by t∆ = ( tU, t W) in the decomposition (21), 

f ¯θeff (c)+ t∆(z) − f ¯θeff (c)(z) = c−1α R(z)( tU) ˜ w + c (tW)b(z) + ( tW) ˆΣ(z)( tU) ˜ w

= tc−1α R(z)U ˜w + c Wb (z) + t2 W ˆΣ(z)U ˜w

= tX(z) + t2Y(z),

hence 

g(t) = 12 Ez ∥tX(z) + t2Y(z)∥2.

Differentiating and evaluating at t = 1 ,

g′(1) = ∥X∥2 

> L2

+ 3 Ez ⟨X(z), Y(z)⟩ + 2 ∥Y∥2

> L2

≥ ∥ X∥2 

> L2

− 3∥X∥L2 ∥Y∥L2 .

Set u := ∥X∥L2 and v := ∥Y∥L2 . The hypothesis r < √m0/(3 CQ) together with (25) and (28) gives 

u ≥ √m0 ∥∆∥ > 3CQ∥∆∥2 ≥ 3v, 

so u 7 → u2 − 3uv is increasing at u. Substituting the lower bound on u (25) and upper bound on v

(28), 

g′(1) ≥ u2 − 3uv 

≥ m0∥∆∥2 − 3CQ

√m0 ∥∆∥3

≥ m0∥∆∥2 − 3CQ

√m0r ∥∆∥2

=  m0 − 3CQ

√m0 r∥∆∥2. (31) 

Step 6: PL inequality. Combining (30) with (31), 

∥∇L (θeff )∥ ∥ ∆∥ ≥ g′(1) ≥  m0 − 3CQ

√m0 r∥∆∥2.

Squaring (and using (29) to convert ∥∆∥2 into the excess loss),  

> 12

∥∇L (θeff )∥2 ≥

 m0 − 3CQ

√m0 r2

 √M0 + CQr2

 L(θeff ) − L ⋆ = μr

 L(θeff ) − L ⋆,

which is the claimed PL inequality on Tr (I).

## E Proof of Theorem 3.1 

Proof. We construct θ⋆ = ( P⋆, V⋆) explicitly in four steps: (i) reduce the readout to four blocks of 

θ; (ii) cancel the cubic-in-w contribution; (iii) match the affine contribution to the SARSA target; (iv) assemble θ⋆ and verify its scaling invariance. 18 Step 1: Reduction to ¯V21 , ¯V22 , P12 , P22 . Partition V, P as in (8) , conformal with D = (2 d +1) + ( d + 1) , and write ˜w = [1; w] ∈ Rd+1 . Let ¯V21 , ¯V22 denote the last d rows of V21 , V22 ,respectively. By Lemma C.1, the transformer update increment simplifies to 

∆wTF (θ) := TF θ (H) − w = ¯V21 ˆΣP 12 ˜w + 1 

> n

¯V22 ˜w ˜w⊤P22 ˜w, (32) where ˆΣ ∈ R(2 d+1) ×(2 d+1) is the empirical second-moment matrix of the trajectory features, with the block decomposition 

ˆΣ =

         

> Pn−1
> i=0 ϕ(si,a i)ϕ(si,a i)⊤
> n
> Pn−1
> i=0 γϕ(si,a i)ϕ(si+1 ,a i+1 )⊤
> n
> Pn−1
> i=0 ri+1 ϕ(si,a i)
> nPn−1
> i=0 γϕ(si+1 ,a i+1 )ϕ(si,a i)⊤
> n
> Pn−1
> i=0 γ2ϕ(si+1 ,a i+1 )ϕ(si+1 ,a i+1 )⊤
> n
> Pn−1
> i=0 γr i+1 ϕ(si+1 ,a i+1 )
> nPn−1
> i=0 ri+1 ϕ(si,a i)⊤
> n
> Pn−1
> i=0 γr i+1 ϕ(si+1 ,a i+1 )⊤
> n
> Pn−1
> i=0 r2
> i+1
> n



:= 



ˆΣϕϕ γ ˆΣϕϕ +

ˆΣϕr 

γ ˆΣϕ+ϕ γ2 ˆΣϕ+ϕ+ γ ˆΣϕ+r

ˆΣrϕ γ ˆΣrϕ +

ˆΣrr 

 .

In particular, the blocks V11 , V12 , P11 , P21 and the first rows of V21 , V22 do not enter the readout, so they are unconstrained. 

Step 2: Cancel the cubic term. The batch SARSA update increment (2) can be written compactly as 

∆wSARSA = α ˆΣϕr + γ ˆΣϕϕ + w − ˆΣϕϕ w, (33) which is affine in w. The first term in (32) is also affine via ˜w = [1; w], while the second is cubic in 

w. Enforcing ∆wTF ≡ ∆wSARSA for every w therefore requires the cubic term to vanish, for which we set ¯V⋆ 

> 22

= 0, P⋆ 

> 22

= 0.

Step 3: Match the affine term. It remains to choose ¯V⋆

> 21

, P⋆ 

> 12

so that 

¯V⋆ 

> 21

ˆΣP ⋆ 

> 12

˜w = α ˆΣϕr + γ ˆΣϕϕ + w − ˆΣϕϕ w.

Take ¯V⋆ 

> 21

to extract the first d rows of ˆΣ with prefactor α:

¯V⋆ 

> 21

= αId, 0d×d, 0d×1

 ⇒ ¯V⋆ 

> 21

ˆΣ = α ˆΣϕϕ , γ ˆΣϕϕ + , ˆΣϕr 

.

The matching condition then reduces to P⋆ 

> 12

˜w = [ −w; w; 1] , which holds for every w ∈ Rd when 

P⋆ 

> 12

=

"0d×1 −Id

0d×1 Id

1 01×d

#

.

Step 4: Assembly and scaling. Leave the unconstrained blocks identified in Step 1 as arbitrary entries (denoted ∗). This yields the structure (P⋆, V⋆) in (5), and by construction 

TF θ⋆ (H) = w + ∆ wSARSA .

Finally, both terms in (32) are bilinear in (V, P), so the rescaled pair (cP⋆, c −1V⋆) produces the same readout for every c̸ = 0 , completing the construction. 

## F Proof of Proposition 3.1 

Proof. Let δ(θ, z ) := TF θ (H(z)) − wSARSA (z) denote the per-sample residual, so that 

L(θ) = 12 Ez ∥δ(θ, z )∥2.

By Lemma C.1, 

δ(θ, z ) = ¯V21 ˆΣ(z)P12 ˜w(z) + 1 

> n

¯V22 ˜w(z) ˜ w(z)⊤P22 ˜w(z) +  w(z) − wSARSA (z), (34) so L depends on (V, P) only through the four sub-blocks ¯V21 , ¯V22 , P12 , P22 .19 Part (i). Since V11 , V12 , P11 , P21 do not appear in (34), 

∇V11 L = ∇V12 L = ∇P11 L = ∇P21 L = 0,

so each is stationary under the gradient flow (7) . For V21 , the loss factors through ¯V21 = SV 21 with 

S = [ 0d×1, Id], and the chain rule yields 

∇V21 L = S⊤∇ ¯V21 L.

Since S⊤ = [ 01×d; Id] has zero first row, so does ∇V21 L, and the first row of V21 remains unchanged. The same argument applies to V22 .

Part (ii). A direct computation from (34) gives 

∇ ¯V22 L(θ) = 1 

> n

Ez

h

δ(θ, z )  ˜w ˜w⊤P22 ˜w⊤i

, (35) 

∇P22 L(θ) = 1 

> n

Ez

  δ(θ, z )⊤ ¯V22 ˜w ˜w ˜w⊤ . (36) Both (35) and (36) vanish whenever P22 = 0 and ¯V22 = 0. Hence the gradient flow (7) admits a trajectory in which P(t)22 ≡ 0 and ¯V(t)22 ≡ 0, with the remaining blocks evolving freely. 

## G Proof of Theorem 3.2 

Proof. We recall the local PL inequality (16) from Lemma C.2: for every θeff ∈ T r (I),

12 ∥∇L (θeff )∥2 ≥ μr

 L(θeff ) − L ⋆.

We keep the L⋆ term explicit for clarity, but it can be dropped since L⋆ = 0 .

Step 1: Bounding the parameter distance to the optimum manifold. Fix any θeff ∈ T r (I) and write its normal decomposition as 

θeff = ¯θeff (c) + ∆ , ∆ = ( U, W) ∈ NcMI , ∥∆∥ < r. 

As in the proof of Lemma C.2, the residual decomposes as 

fθeff (z) − f ¯θeff (c)(z) = X(z) + Y(z),

where, by (21), 

X(z) := c−1α R(z)U ˜w + c Wb (z),

Y(z) := W ˆΣ(z)U ˜w.

By (25), 

∥X∥L2 ≥ √m0 ∥∆∥,

and by (28), 

∥Y∥L2 ≤ CQ ∥∆∥2.

Therefore, applying the reverse triangle inequality, 

q

2 L(θeff ) − L ⋆ = ∥X + Y∥L2

≥ ∥ X∥L2 − ∥ Y∥L2

≥  √m0 − CQ∥∆∥∥∆∥≥  √m0 − CQr∥∆∥,

where the last inequality uses ∥∆∥ < r . Squaring both sides, 

L(θeff ) − L ⋆ ≥ 12

 √m0 − CQr2∥∆∥2 = λr ∥∆∥2, (37) 20 where we set 

λr := 12

 √m0 − CQr2.

By definition of the tube (10), every point θeff ∈ T r (I) admits a unique normal decomposition 

θeff = ¯θeff (c) + ∆ , ∆ ∈ NcMI ,

so the normal displacement equals the distance to the manifold patch: 

∥∆∥ = dist( θeff , MI ).

Combining this identity with (37), for all θeff ∈ T r (I),

L(θeff ) − L ⋆ ≥ λr dist( θeff , MI )2,

and hence 

dist( θeff , MI ) ≤ 1

√λr

pL(θeff ) − L ⋆. (38) 

Step 2: Local upper bound on the gradient norm. Define the prediction error 

eθeff (z) := fP12 , ¯V21 (z) − fP⋆  

> 12 ,¯V⋆
> 21

(z)= ¯V21 ˆΣ(z)P12 ˜w − ¯V⋆ 

> 21

ˆΣ(z)P⋆ 

> 12

˜w.

Then 

L(θeff ) − L ⋆ = 12 Ez ∥eθeff (z)∥2,

so that 

Ez ∥eθeff (z)∥2 = 2  L(θeff ) − L ⋆.

Differentiating the bilinear predictor with respect to ¯V21 and P12 gives 

∇ ¯V21 L(θeff ) = Ez

h

eθeff (z)   ˆΣ(z)P12 ˜w⊤i

,

∇P12 L(θeff ) = Ez

h ˆΣ(z)⊤ ¯V⊤ 

> 21

eθeff (z) ˜ w⊤i

.

For every θeff ∈ T r (I), the triangle inequality together with ∥∆∥ < r yields 

∥P12 ∥F ≤ c+∥P⋆

> 12

∥F + r

= c+

√2d + 1 + r

=: Pmax ,r ,

and 

∥ ¯V21 ∥F ≤ c−1 

> −

∥ ¯V⋆

> 21

∥F + r

= c−1

> −

√d + r

=: Vmax ,r .

Moreover, by Assumption 1 (1), 

∥ ˆΣ(z)∥op ≤ BΣ,

and 

∥ ˜w∥ ≤ B ˜w.

Therefore, by Cauchy–Schwarz and submultiplicativity, 

∥∇ ¯V21 L(θeff )∥F ≤ BΣB ˜w Pmax ,r 

 Ez ∥eθeff (z)∥21/2

= √2 BΣB ˜w Pmax ,r 

pL(θeff ) − L ⋆,

and similarly, 

∥∇ P12 L(θeff )∥F ≤ √2 BΣB ˜w Vmax ,r 

pL(θeff ) − L ⋆.

Combining the two estimates and recalling that 

Kr = √2 BΣB ˜w

q

P 2max ,r + V 2max ,r ,

we obtain the gradient upper bound 

∥∇L (θeff )∥ ≤ Kr

pL(θeff ) − L ⋆, ∀ θeff ∈ T r (I). (39) 21 Step 3: Invariance of the tube. Let θeff (t) denote the maximal solution of the gradient flow (7) starting from θeff (0) . Since ∇L is locally Lipschitz, a unique local solution exists. Define the exit time 

τ := inf t > 0 : θeff (t) /∈ T r (I) .

We will show that τ = ∞. For all t < τ , the local PL inequality (16) holds, and along the gradient flow, 

ddt L(θeff (t)) = ∇L (θeff (t)) , ˙θeff (t)

= −∥∇L (θeff (t)) ∥2

≤ − 2μr

 L(θeff (t)) − L ⋆.

Since L⋆ = 0 , Gronwall’s inequality yields the loss decay bound 

L(θeff (t)) − L ⋆ ≤ e−2μr t L(θeff (0)) − L ⋆, ∀ t < τ. (40) Combining (39) with (40), for every t < τ ,

∥ ˙θeff (t)∥ = ∥∇L (θeff (t)) ∥≤ Kr

pL(θeff (t)) − L ⋆

≤ Kr

pL(θeff (0)) − L ⋆ e−μr t.

Integrating, for every t < τ ,

∥θeff (t) − θeff (0) ∥ ≤ 

Z t

> 0

∥ ˙θeff (s)∥ ds 

≤ Kr

pL(θeff (0)) − L ⋆

Z t

> 0

e−μr s ds 

≤ Kr

μr

pL(θeff (0)) − L ⋆. (41) We next upper-bound the initial excess loss in terms of the distance to MI0 . From (29) , since 

∥∆∥ = dist( θeff , MI ) and MI0 ⊂ MI ,

L(θeff ) − L ⋆ ≤ 12

 p M0 + CQr2∥∆∥2

= 12

 p M0 + CQr2 dist( θeff , MI )2

≤ 12

 p M0 + CQr2 dist( θeff , MI0 )2. (42) Combining (41) and (42), for every t < τ ,

dist( θeff (t), MI0 ) ≤ dist( θeff (0) , MI0 ) + ∥θeff (t) − θeff (0) ∥≤ dist( θeff (0) , MI0 ) + Kr

μr

pL(θeff (0)) − L ⋆

≤ dist( θeff (0) , MI0 ) 1 + Kr

 √M0 + CQr

√2 μr

!

< η I0,r ,

where the last inequality follows from the initialization condition (12) . By the definition of ηI0,r 

in (11), this implies 

θeff (t) ∈ T r (I), ∀ t < τ. 

Hence the solution cannot exit Tr (I) at any finite time, so τ = ∞.22 Step 4: Convergence to the optimum manifold. Since the trajectory is confined to the bounded neighborhood θeff ∈ Θeff : dist( θeff , MI0 ) ≤ ηI0,r ,

no finite-time blow-up occurs and the gradient flow is well defined for all t ≥ 0. The exponential loss decay (40) therefore holds globally: 

L(θeff (t)) − L ⋆ ≤ e−2μr t L(θeff (0)) − L ⋆, ∀ t ≥ 0. (43) Combining (38) with (43), 

dist( θeff (t), MI )2 ≤ 1

λr

 L(θeff (t)) − L ⋆

≤ 1

λr

e−2μr t L(θeff (0)) − L ⋆. (44) It remains to show that the parameters themselves converge to a point on MI . By the same argument as in (41), for any 0 ≤ t1 < t 2,

∥θeff (t2) − θeff (t1)∥ ≤ 

Z t2

> t1

∥ ˙θeff (s)∥ ds 

≤ Kr

pL(θeff (0)) − L ⋆

Z t2

> t1

e−μr s ds 

≤ Kr

μr

pL(θeff (0)) − L ⋆ e−μr t1 . (45) Hence θeff (t) is a Cauchy curve as t → ∞ . By completeness of Θeff , there exists θeff (∞) ∈ Θeff 

such that 

lim  

> t→∞

θeff (t) = θeff (∞).

Letting t2 → ∞ in (45), 

∥θeff (t) − θeff (∞)∥ ≤ Kr

μr

pL(θeff (0)) − L ⋆ e−μr t.

By continuity of L together with (43), 

L(θeff (∞)) = L⋆.

Moreover, by (44), 

dist( θeff (t), MI ) → 0 as t → ∞ ,

and since MI is closed, we conclude that 

θeff (∞) ∈ MI .

This completes the proof. 

## H Proof of Theorem 3.3 

Proof. The argument follows the same four-step structure as the proof of Theorem 3.1; only the dimensions and the affine target change. 

Step 1: Reduction. The actor-critic prompt (13) has the same block-diagonal form as the SARSA prompt (4) , except the trajectory columns hi+1 := [ xi; 0], i = 0 , . . . , n − 1 occupy the upper 

(2 d + m + 1) -block, while the parameter column hn+1 = [ 02d+m+1 ; ˜ w] occupies the lower (d +

m + 1) -block, with 

xi =



ϕV (si)

γϕV (si+1 )

ri+1 

γigλ(si, a i)

 ∈ R2d+m+1 , and ˜w =

" 1

λ

w

#

∈ Rd+m+1 .

23 Partitioning V, P conformally with D = (2 d + m + 1) + ( d + m + 1) as in (8) , and letting ¯V21 , ¯V22 

now denote the last d + m rows of V21 , V22 , the same block computation as in Lemma C.1 yields 

∆λTF (θ)∆wTF (θ)



:= TF θ (H) −

λ

w



= ¯V21 ˆΣP 12 ˜w + 1 

> n

¯V22 ˜w ˜w⊤P22 ˜w, (46) where ˆΣ ∈ R(2 d+m+1) ×(2 d+m+1) is the empirical second-moment matrix of the trajectory features: 

ˆΣ = 1

n

> n−1

X

> i=0

xix⊤ 

> i

:= 



ˆΣϕV ϕV γ ˆΣϕV ϕV +

ˆΣϕV r ˆΣϕV g

γ ˆΣϕV +ϕV γ2 ˆΣϕV +ϕV + γ ˆΣϕV +r γ ˆΣϕV +g

ˆΣrϕ V γ ˆΣrϕ V +

ˆΣrr ˆΣrg 

ˆΣgϕ V γ ˆΣgϕ V +

ˆΣgr ˆΣgg 

 .

The blocks V11 , V12 , P11 , P21 and the first row of V21 , V22 are unconstrained. 

Step 2: Cancel the cubic term. The actor-critic update (3) can be written as 

∆λAC 

∆wAC 



=

 α ˆΣgr + γ ˆΣgϕ V + w − ˆΣgϕ V w

β ˆΣϕV r + γ ˆΣϕV ϕV + w − ˆΣϕV ϕV w

, (47) which is affine in (λ, w). Setting ¯V⋆ 

> 22

= P⋆ 

> 22

= 0 removes the cubic term in (46). 

Step 3: Match the affine term. We require ¯V⋆ 

> 21

ˆΣP ⋆ 

> 12

˜w = ∆λAC ; ∆ wAC 

 for every (λ, w).Take 

¯V⋆ 

> 21

=

0m×d 0m×d 0m×1 αIm

βId 0d×d 0d×1 0d×m



,

which extracts the bottom m rows (prefactor by α), and the top d rows (prefactor by β) of ˆΣ:

¯V⋆ 

> 21

ˆΣ =

 α ˆΣgϕ V αγ ˆΣgϕ V + α ˆΣgr α ˆΣgg 

β ˆΣϕV ϕV βγ ˆΣϕV ϕV + β ˆΣϕV r β ˆΣϕV g



.

The matching condition then reduces to P⋆ 

> 12

˜w = [ −w; w; 1; 0m]. With ˜w = [1; λ; w], this holds for every (λ, w) when 

P⋆ 

> 12

=



0d×1 0d×m −Id

0d×1 0d×m Id

1 01×m 01×d

0m×1 0m×m 0m×d

 .

Step 4: Assembly and scaling. Leaving the unconstrained blocks as ∗, we recover (P⋆, V⋆) as displayed in (14) . Bilinearity of (46) in (V, P) gives the same scaling invariance under (P⋆, V⋆) 7 →

(cP⋆, c −1V⋆) for any c̸ = 0 , completing the construction. 

## I Additional Experiments 

I.1 Training loss curves 

We plot the training loss ˆL(θ(t)) over training iterations for both the SARSA transformer and the Actor-Critic transformer in our experiments presented in Section 4. The results are shown in Figure 4. 24 (a) SARSA transformer. (b) Actor-Critic transformer. 

Figure 4: Training loss ˆL(θ(t)) over training iterations for the SARSA transformer (left) and the Actor-Critic transformer (right). In both settings, the loss decays approximately exponentially to near zero, consistent with the local convergence guarantee in Theorem 3.2. 

I.2 Experimental Detail 

The trainings and evaluations of the transformers used in our experiment were conducted on a Windows 11 machine with the following specifications: • GPU: NVIDIA GeForce RTX 4090 • CPU: Intel Core i9-14900KF • Memory: 32 GB DDR5, 5600MHz The training procedure takes approximately 20 minutes for the IC-SARSA transformer and 30 minutes for the IC-AC transformer; evaluation takes approximately 5 and 10 minutes, respectively. 25