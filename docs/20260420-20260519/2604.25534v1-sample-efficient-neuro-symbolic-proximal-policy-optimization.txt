Title: Sample-efficient Neuro-symbolic Proximal Policy Optimization

URL Source: https://arxiv.org/pdf/2604.25534v1

Published Time: Wed, 29 Apr 2026 01:03:19 GMT

Number of Pages: 21

Markdown Content:
> Proceedings of Machine Learning Research vol 284:1–21, 2026 20th Conference on Neurosymbolic Learning and Reasoning

# Sample-efficient Neuro-symbolic Proximal Policy Optimization 

Simone Murari ∗ simone.murari@univr.it 

Celeste Veronese ∗ † celeste.veronese@univr.it 

Daniele Meli daniele.meli@univr.it 

Dept. of Computer Science, University of Verona, Italy 

Editors: Alessandra Mileo, Andrea Passerini and Cogan Shimizu 

Abstract 

Deep Reinforcement Learning (DRL) algorithms often require a large amount of data and struggle in sparse-reward domains with long planning horizons and multiple sub-goals. In this paper, we propose a neuro-symbolic extension of Proximal Policy Optimization (PPO) that transfers partial logical policy specifications learned in easier instances to guide learning in more challenging settings. We introduce two integrations of symbolic guidance: (i) H-PPO-Product , which biases the action distribution at sampling time, and (ii) H-PPO-SymLoss , which augments the PPO loss with a symbolic regularization term. We evaluate our methods on three benchmarks (OfficeWorld, WaterWorld, and DoorKey), showing consistently faster learning and higher return at convergence than PPO and a Reward Machine baseline, also under imperfect symbolic knowledge. 

1. Introduction 

Deep Reinforcement Learning (DRL) (Sutton and Barto, 2018) is widely applied to solve sequential decision-making problems. It offers significant benefits for complex domains, ranging from robotics (Ibarz et al., 2021) to sustainability (Zuccotto et al., 2024; K˚ age et al., 2025). Despite these successes, scaling DRL to more challenging configurations remains difficult. First, neural policies act as black boxes, limiting their interpretability and making it hard to extract formal guarantees (Vouros, 2022). Furthermore, DRL suffers from sample inefficiency (Dulac-Arnold et al., 2021). Agents often require a huge number of interactions to converge to an optimal policy. This drawback becomes especially evident in environments featuring long horizons, sparse rewards, and complex sequences of sub-goals. Standard methods attempt to solve this by applying architectural inductive biases (Wang et al., 2019) or by injecting domain knowledge through Reward Machines (RMs) (Icarte et al., 2022). However, reliance on scalar reward shaping is often computationally expensive and can degrade performance if the provided heuristics are inaccurate (Cheng et al., 2021). We propose a neuro-symbolic approach to improve the sample efficiency and scalability of Proximal Policy Optimization (PPO) algorithms (Schulman et al., 2017). Our framework uses logical policy representations as an action-level prior to guide PPO training in harder configurations, where standard methods typically fail.    

> ∗These authors contributed equally
> †Corresponding author
> ©2026 S. Murari, C. Veronese & D. Meli.
> arXiv:2604.25534v1 [cs.AI] 28 Apr 2026 Murari Veronese Meli

During training, an online reasoning mechanism translates the agent’s raw observations into high-level logical predicates, capturing object states and spatial relationships. These predicates are then evaluated against the previously extracted rules to deduce the most promising actions at each step. We integrate these preferences into PPO via two distinct methods: H-PPO-Product , which biases the learned policy towards the suggested actions, hence modifying the action sampling distribution; and H-PPO-SymLoss , which adds a symbolic penalty during optimization updates. Our approach is designed to work with imperfect logical rules (Hazra and De Raedt, 2023) and prevents the need to retune PPO hyperparameters when the domain scale increases. Our main contributions are the following: 

• A sample-efficient neuro-symbolic PPO framework that leverages partial logical rules to guide the agent toward logically entailed actions, employing either sampling distri-bution biases or symbolic optimization penalties. 

• An empirical validation on three well-established gridworld domains: OfficeWorld ,

WaterWorld by Icarte et al. (2022), and DoorKey by Chevalier-Boisvert et al. (2023). We show substantial improvements in sampling efficiency and return at convergence over standard baselines and RMs, even in the hardest task instances and without hyperparameter tuning. 

2. Related Works 

Sample inefficiency consistently restricts the scalability and applicability of algorithms like PPO, particularly in sparse-reward environments with sub-goals and long planning horizons. Traditional approaches to mitigate this issue remain purely neural, often improving data efficiency by explicitly decoupling the training of the policy and the value function to enable more aggressive sample reuse (Cobbe et al., 2021; Raileanu and Fergus, 2021). However, these frameworks suffer from inherent opacity, lack a natural mechanism to directly integrate human-readable domain rules and require careful tuning of the hyperparameters. Neuro-symbolic integrations offer a way to combine logical reasoning with neural learning to augment interpretability and generalization. Many methods translate formal task spec-ifications into sequential automata, resulting in policy-correction mechanisms built from trajectories (Yang et al., 2024) or automated reward shaping (De Giacomo et al., 2019; Furelos-Blanco et al., 2021). Along this line, plan-based reward shaping and reward ma-chines can still help when plans are incomplete or partly incorrect, using potential-based signals to speed up learning in long-sequence sparse-reward tasks (M¨ uller et al., 2025) or guiding exploration (Icarte et al., 2022; Furelos-Blanco et al., 2021). Yet reward-based in-terventions are highly sensitive to poorly-specified heuristics (Cheng et al., 2021), which has pushed recent work toward mining logical rules directly from past trajectories (Meli et al., 2024; Sreedharan and Katz, 2023; Veronese et al., 2024, 2025). Our method builds upon the principle of semantic loss presented in Xu et al. (2018), which encodes symbolic constraints over a neural network’s output as a differentiable penalty. In our setting, we leverage the same general principle but target deep reinforcement learning and policy-gradient optimization. We design a term defined through a symbolic policy, composed of logical rules over predicates, and we study the role of a dedicated 

> 2Sample-efficient Neuro-symbolic Proximal Policy Optimization

weight Θ to balance symbolic guidance and neural learning during training. Finally, our methods share some similarities with Statistical Relational Learning (SRL) (Marra et al., 2024) and the neuro-symbolic framework proposed in Hazra and De Raedt (2023). How-ever, SRL and pure logical learning methods struggle to scale to complicated domains, as they require an accurate definition of the policy search space. By combining the advantages of symbolic reasoning and DRL, our approach overcomes the scalability bottlenecks of the logic-driven policy learning proposed by Hazra and De Raedt (2023). As a result, our neuro-symbolic agent can successfully navigate sparse-reward tasks involving extended sequences of sub-goals, where standard DRL algorithms and purely symbolic methods typically fail. Action-level guidance beyond reward modification is inspired from Veronese et al. (2026); Meli et al. (2024); Sreedharan and Katz (2023), which however focused on model-based RL or ϵ-greedy algorithms. Instead, our method is designed for the most advanced and popular class of PPO algorithms for RL. 

3. Background 

We work in the standard Markov Decision Process (MDP) setting, defined by the tu-ple ⟨S, A, T, R, γ ⟩: S is the state space, A is the action set, T (s, a, s ′) is the transition model, R(s, a ) is the immediate reward, and γ ∈ [0 , 1] is the discount factor. A (stochas-tic) policy π(a | s) specifies how actions are sampled given states. For a trajectory 

τ = ( s0, a 0, s 1, a 1, . . . ) generated by π, we define the (discounted) return from time t as 

Gt =

> ∞

X

> k=0

γkrt+k, (1) where rt = R(st, a t). The objective is to maximize the expected discounted return from the initial state distribution: 

Eτ ∼π[G0] (2) 

3.1. Proximal Policy Optimization 

Proximal Policy Optimization (PPO) (Schulman et al., 2017) is a policy-gradient algorithm that constrains updates through a clipped surrogate objective, yielding stable improve-ments in practice. Given a batch of trajectories collected under an older policy πold , PPO optimizes: 

LCLIP (θ) = Et

h

min 



ρt(θ) ˆAt, clip( ρt(θ), 1 − ϵclip , 1 + ϵclip ) ˆAt

i 

(3) where ρt(θ) = πθ (at|st) 

> πold (at|st)

is the probability ratio, ϵclip is the clipping coefficient (typically 0.2), and ˆAt is the estimated advantage. The advantage is computed using Generalized Advantage Estimation (GAE, Schulman et al. (2018)): ˆAt =

> ∞

X

> l=0

(γλ GAE )lδt+l, where δt = rt + γV (st+1 ) − V (st) (4) 

> 3Murari Veronese Meli

with λGAE ∈ [0 , 1] controlling the bias-variance trade-off. The complete PPO loss combines the policy loss with a value function loss LVF and a bonus on the policy entropy H[πθ]: 

LPPO (θ) = −L CLIP (θ) + c1LVF (θ) − c2H[πθ] (5) where c1, c 2 are scalar coefficients. 

4. Methodology 

Before describing our algorithms, we introduce the notion of action-level guidance. In contrast with the state heuristics adopted in classical planning (Bonet and Geffner, 2001), we consider a logical preference over actions, which is more convenient for integration in RL (Veronese et al., 2026; Meli et al., 2024). Specifically, the action-level guidance is provided via a symbolic policy , expressed in a fragment of first-order logic (namely, Horn clauses). 

Definition 1 (Symbolic Policy) Consider two sets of logical predicates F, A, represent-ing abstractions for environmental (state) features and actions in RL, respectively. A sym-bolic policy πsym : F → A is a set of logical rules over predicates that map environmental features to action terms. 

F, A are defined by the task designer. Formally, we follow classical works in symbolic and neuro-symbolic AI (Coradeschi et al., 2013; Veronese et al., 2026; Sreedharan and Katz, 2023) and assume that there exist functions that map the MDP states and actions into sets of grounded logical predicates: GF : S → G (F) and GA : A → G (A). We require GA to be surjective, since we need each symbolic action to correspond to at least one MDP action. The logical policy encodes rules in the form a ← f1, . . . , fn, where fi are environmental predicates and a represents an action. For example, consider the DoorKey domain, where an agent navigates a grid with walls, keys and doors; it must pick up the correct key and open ( toggle ) the door to reach a goal location. In this setting πsym might contain logical rules like: 

toggle (X) ← door (X), locked (X), carryingKey (Z), sameColor (X, Z)To integrate this symbolic knowledge into the RL loop, we define an indicator function 

Iπsym : S × A → { 0, 1}, such that: 

Iπsym (s, a ) = 1 ⇐⇒ πsym (GF (s)) |= GA(a)We now detail the two proposed neuro-symbolic integrations: H-PPO-Product and 

H-PPO-SymLoss . Both methods leverage the logical guidance provided by Iπsym (s, a ), but they integrate this knowledge into PPO training differently. 

4.1. H-PPO-Product 

H-PPO-Product (Algorithm 1) injects symbolic guidance at the action sampling stage. At each time step during training, we reshape the current action distribution by reweighting 

> 4

Sample-efficient Neuro-symbolic Proximal Policy Optimization 

the neural policy toward actions logically entailed by πsym , and then we sample from this distribution. Specifically, we define the following reweighting term (Lines 3-8): 

mt(a, s ) = 

(

1 + λ · εt if Iπsym (s, a ) = 1 1 otherwise (6) where λ ∈ (0 , 1] controls how strongly we trust the symbolic policy, and εt is a scalar that reduces symbolic influence as training progresses following a linear decay: εt = max( εi − t ·

εr, ε f ). The guided sampling policy is then the normalized product (Line 9): ˜πt(a | s) = πθ(a | s) mt(a, s )

P 

> a′

πθ(a′ | s) mt(a′, s ) (7) For εt ≈ 1, the policy is noticeably biased toward actions with Iπsym (s, a ) = 1; for εt → 0, we recover the original PPO policy πθ. This incentivizes early-stage exploration, crucial for robust generalizable learning (Ladosz et al., 2022). In this way, the agent progressively ignores the symbolic policy, and eventually behaves according to the classical PPO neural policy (mitigating the effect of bad or imperfect suggestions). 

Algorithm 1: H-PPO-Product - Guided Action Sampling 

Require: State st, policy πθ, timestep t

Require: Logical indicator Iπsym (st, a ), confidence λ ∈ [0 , 1] 

Require: Annealing parameters ( εi, ε r, ε f )

Output: Action at, log-probability lt, entropy Ht, value Vt 

> 1

πθ(· | st) ← PolicyNetwork( st; θ) 

> 2

εt ← max( εi − t · εr, ε f ) 

> 3

for a ∈ A do  

> 4

mt(a, s t) ← 1 

> 5

if Iπsym (st, a ) = 1 then  

> 6

mt(a, s t) ← mt(a, s t) + λ · εt 

> 7

end  

> 8

end  

> 9

˜πt(· | st) ← Normalize  πθ(· | st), m t(·, s t) 

> 10

at ∼ ˜πt(· | st) 

> 11

lt ← log ˜ πt(at | st) 

> 12

Ht ← Entropy  ˜πt(· | st) 

> 13

Vt ← Vϕ(st) 

> 14

return at, l t, Ht, V t

4.2. H-PPO-SymLoss 

The H-PPO-SymLoss method (Algorithm 2) injects symbolic guidance at the optimization 

stage, by augmenting the standard PPO loss from Equation (3) with a symbolic term that encourages alignment with a reference policy πref derived from the symbolic policy πsym .

5Murari Veronese Meli 

We define probabilistic weights for each action based on the logical indicator using a confidence level η ∈ (0 , 1) (Line 5) 1:

wsym (a | s) = 

(

η if Iπsym (s, a ) = 1 1 − η otherwise (8) and then normalize over actions to have a novel policy distribution (Line 6): 

πref (a | s) = wsym (a | s)

P 

> a′

wsym (a′ | s) . (9) In this way, logically entailed actions receive higher probability mass, while all actions keep non-zero probability. Then we calculate ρsym t (θ) = πθ (at|st)  

> πref (at|st)

at Line 7. In standard PPO, the reference distribution in the ratio is πθold ; here, it is replaced by πref . We then define a symbolic auxiliary loss Lsym which follows the same clipped structure as the PPO surrogate in Eq. (3) (Line 8): 

Lsym = Et

h

min 



ρsym t (θ) ˆAt, clip( ρsym t (θ), 1 − ϵclip , 1 + ϵclip ) ˆAt

i 

(10) The ratio ρsym t measures the similarity between the neural and reference policies, ˆAt

weighs the update by estimated utility, and clipping limits large updates in the same way as LCLIP .We finally combine Lsym with the PPO loss from Equation (5), via a linear parameter Θ (Line 9): 

L(θ) = LPPO (θ) − Θ · L sym (11) 

Algorithm 2: H-PPO-SymLoss - Optimization Step 

Require : Batch of transitions ( st, a t, r t, s t+1 ), timestep t

Require : Current policy πθ , Old policy πθold 

Require : Logical indicator Iπsym (st, a t), symbolic weight Θ (constant) or annealing parameters (Θ i, Θr , Θf ) 

> 1

ˆAt ← ComputeGAE( rt, V ϕ(st), V ϕ(st+1 ))  

> 2

ρt ← πθ (at|st)  

> πθold (at|st)
> 3

LCLIP ← min 



ratio · ˆAt, clip(ratio , 1 − ϵclip , 1 + ϵclip ) · ˆAt

 

> 4

Θt = max(Θ i − t · Θr , Θf ) or Θ 

> 5

wsym (at | st) ← η if Iπsym (st, a t) = 1 else 1 − η 

> 6

πref (at | st) ← wsym (at|st)  

> P
> a′wsym (a′|st)
> 7

ρsym t ← πθ (at|st)  

> πref (at|st)
> 8

Lsym ← Et

h

min 



ρsym t · ˆAt, clip( ρsym t , 1 − ϵclip , 1 + ϵclip ) · ˆAt

i  

> 9

L ← −L CLIP + c1LV F − c2H(πθ (· | st)) −Θt · L sym  

> 10

θ ← θ − α∇θ L

6Sample-efficient Neuro-symbolic Proximal Policy Optimization 

(a) DoorKey 8 × 8 - 1 key (b) OfficeWorld (c) WaterWorld Figure 1: Benchmark domains 

5. Experiments 

We evaluate our methodologies on three benchmarks 2 (Figure 1): DoorKey from Chevalier-Boisvert et al. (2023) and OfficeWorld and WaterWorld from Icarte et al. (2022). DoorKey 

is a partially observable grid navigation task where the agent must retrieve a key that matches the door color before reaching the final goal. OfficeWorld takes place in a discrete grid containing labeled locations such as coffee, mail, plants, an office, and various rooms; the agent must navigate through a sequence of sub-goals, while avoiding the plants. Wa-terWorld is a continuous 2D box containing differently colored balls that move at constant velocities and bounce off walls; the agent controls a white ball and must change its velocity to touch specific color sequences in order, while avoiding out-of-sequence collisions. All settings feature sparse rewards and (in some variants) long planning horizons, making them suitable to test whether symbolic action preferences can improve exploration and policy learning. We compare against vanilla PPO and an RM baseline (Icarte et al., 2022). In our RM setup, rewards are shaped using (i) a small action-level bonus/penalty depending on whether the chosen action is entailed by πsym , and (ii) additional rewards/penalties for progressing or regressing along the states of the RM automaton equivalent to πsym . We tune the main PPO hyperparameters only on the smallest domain instances, and keep them fixed when moving to larger and more complex instances, except for the episode length and the number of parallel environments to accommodate longer horizons. The hyperparameters are reported in Appendix A.2. Crucially, the symbolic policies are not perfect nor complete, but they are induced from the agent’s experience in small and simple task instances and then transferred unchanged to harder ones (see Appendix A.1). In particular, in DoorKey we induce the symbolic policy on the smallest setting (8 × 8 with 1 key) and reuse it for all larger variants (8 × 8 with 2/4 keys and 16 × 16 with 1/2/4 keys). In OfficeWorld , rules induced in DeliverCoffee are reused in 

DeliverCoffeeAndMail , while PatrolABC uses its own policy including guidance for rooms 

A, B, and C. In WaterWorld , we induce the symbolic policy on RedGreen and reuse the   

> 1. η= 1 would introduce shielding, hence incomplete and sub-optimal search over the action space. 2. Domain formalizations and symbolic policies are in Appendix A.1.
> 7Murari Veronese Meli

(a) DoorKey 8 × 8 - 1 key (b) DoorKey 8 × 8 - 2 keys (c) DoorKey 8 × 8 - 4 keys 

(d ) DoorKey 16 × 16 - 1 key (e) DoorKey 16 × 16 - 2 keys (f ) DoorKey 16 × 16 - 4 keys 

Figure 2: DoorKey results same policy in RedGreenAndBlueCyan and RedGreenAndBlueCyanAndMagentaYellow . In the charts, learning curves include H-PPO-Product with εr = 0 .4 and εf = 0 (blue), 

H-PPO-SymLoss with linearly decaying Θ = Θ t = max(Θ i − t · Θr, Θf ), where Θ r = 0 .4and Θ f = 0 (pink), PPO (yellow) and PPO RM (green). For the RM baseline, we exclude the additional shaping reward from the plotted return for a fair comparison. We further report an ablation study on the symbolic weight Θ for H-PPO-SymLoss in Appendix A.3 and on εf for H-PPO-Product in Appendix A.4. 

5.1. DoorKey Results 

Figure 2 reports the DoorKey results across varying grid sizes (8 × 8 and 16 × 16) and number of keys (1, 2, 4). PPO hyperparameter tuning was performed in the 8 × 8 grid with 1 key. In the 8 ×8 grid (top row), we observe consistent gains from both H-PPO-Product and 

H-PPO-SymLoss across all key configurations. With 1 key, all methods converge rapidly, though RMs have more variable performance. However, as the number of keys increases, our algorithms become increasingly beneficial. In particular, H-PPO-Product clearly demonstrates its strength in driving exploration, reaching high return with faster growth in the earliest episodes. Meanwhile, H-PPO-SymLoss reaches the same high return, but more slowly. Interestingly, in the 4-key settings the RM baseline remains at zero return throughout training, so its curve lies on the horizontal axis and is barely visible in the charts, making it the weakest performer. Scaling to the 16 × 16 grid shows the crucial role of exploration, with a longer plan-ning horizon. Indeed, H-PPO-Product is the only algorithm reaching near-optimal per-formance even in the most challenging setting with 4 keys, having the longest planning horizon. 

> 8Sample-efficient Neuro-symbolic Proximal Policy Optimization

(a) DeliverCoffee (b) PatrolAB 

(c) DeliverCoffeeAndMail (d ) PatrolABC 

Figure 3: OfficeWorld results 

5.2. OfficeWorld Results 

Figure 3 reports the OfficeWorld results for DeliverCoffee , PatrolAB , DeliverCoffeeAndMail 

and PatrolABC .In the simpler tasks DeliverCoffee and PatrolAB , H-PPO-Product exhibits the fastest rise and reaches near-optimal return early, while PPO and the RM baseline follow closely; 

H-PPO-SymLoss lags in the first phase but catches up later, ending slightly below the top curves. In PatrolAB , the same pattern holds: H-PPO-Product leads the early learning, whereas H-PPO-SymLoss steadily improves and attains the best return at convergence. Moving to the harder DeliverCoffeeAndMail and PatrolABC tasks, H-PPO-Product 

achieves the fastest initial learning due to its exploration bias, but its average return plateaus at a lower level (slightly above 0 .65 in DeliverCoffeeAndMail ). In contrast, H-PPO-SymLoss improves more slowly at the start (as it provides less exploration pull), but its symbolic regularization strongly shapes the final policy, reaching the highest re-turns in both tasks. The RM baseline shows moderate performance but remains below 

H-PPO-SymLoss . These results demonstrate that H-PPO-SymLoss is highly effective at maximizing policy returns, but does not yield a good initial exploration. 

5.3. WaterWorld Results 

Figure 4 reports the WaterWorld results for RedGreen , RedGreenAndBlueCyan , and Red-GreenAndBlueCyanAndMagentaYellow .

> 9Murari Veronese Meli

(a) RedGreen (b) RedGreenAndBlueCyan (c) RedGreenAndBlueCyan AndMagentaYellow 

Figure 4: WaterWorld results In the RedGreen task (where PPO hyperparameters were tuned), all methods easily learn the optimal behavior. However, as the tasks become more complex, the differences in performance become clear. In RedGreenAndBlueCyan , H-PPO-Product emerges as the most robust approach, actively driving the exploration toward trajectories that follow the required ordering and reaching the highest return close to 0 .6. The RM baseline also performs better than standard PPO. At the same time, H-PPO-SymLoss struggles more than standard PPO, as modifying the policy loss early in training may overly constrain the exploration dynamics. In the hardest task ( RedGreenAndBlueCyanAndMagentaYellow ), these differences are amplified. As the required sequences become longer, only H-PPO-Product successfully achieves the required exploration to reach higher returns. H-PPO-SymLoss is not effective here, confirming that its primary strength lies in policy consolidation after successes are encountered naturally, rather than in initiating complex exploration sequences out of sparse-reward spaces. 

6. Conclusion 

In this paper, we proposed a neuro-symbolic framework for improving the sample efficiency of PPO in environments characterized by sparse rewards, multiple sub-goals, and long plan-ning horizons. Our two integration strategies, H-PPO-Product and H-PPO-SymLoss ,inject logical rules into the learning process at the level of action selection and policy opti-mization, respectively, while progressively annealing their influence so that training recovers standard PPO, without hyperparameter re-tuning when scaling to harder instances. Across three benchmark environments and multiple variants, our methods consistently outperform the baselines, including state-of-the-art reward machines, fulfilling distinct roles. 

H-PPO-Product proves to be an exploration driver, achieving the fastest convergence and highest final returns in the long, sparse spaces of DoorKey and WaterWorld. Conversely, H-PPO-SymLoss does not significantly aid in early exploration, but acts as a powerful prior for policy regularization; this is clear in OfficeWorld, where it results in the highest returns despite slower initial exploration. Taken together, these results demonstrate that integrating symbolic reasoning directly into the algorithmic structure of DRL offers an effective path towards interpretable, and sample-efficient policy learning and generalization. Future work will investigate extensions to continuous action spaces, more challenging domains, and the potential for zero-shot generalization in DRL, towards actual real-world application. 

> 10 Sample-efficient Neuro-symbolic Proximal Policy Optimization

References 

Blai Bonet and H´ ector Geffner. Planning as heuristic search. Artificial Intelligence , 129 (1-2):5–33, 2001. Ching-An Cheng, Andrey Kolobov, and Adith Swaminathan. Heuristic-guided reinforce-ment learning. Advances in Neural Information Processing Systems , 34:13550–13563, 2021. Maxime Chevalier-Boisvert, Bolun Dai, Mark Towers, Rodrigo de Lazcano, Lucas Willems, Salem Lahlou, Suman Pal, Pablo Samuel Castro, and Jordan Terry. Minigrid & miniworld: Modular & customizable reinforcement learning environments for goal-oriented tasks. 

CoRR , abs/2306.13831, 2023. Karl W Cobbe, Jacob Hilton, Oleg Klimov, and John Schulman. Phasic policy gradient. In 

International Conference on Machine Learning , pages 2020–2027. PMLR, 2021. Silvia Coradeschi, Amy Loutfi, and Britta Wrede. A short review of symbol grounding in robotic and intelligent systems. KI-K¨ unstliche Intelligenz , 27(2):129–136, 2013. Giuseppe De Giacomo, Luca Iocchi, Marco Favorito, and Fabio Patrizi. Foundations for restraining bolts: Reinforcement learning with ltlf/ldlf restraining specifications. In Pro-ceedings of the international conference on automated planning and scheduling , volume 29, pages 128–136, 2019. Gabriel Dulac-Arnold, Nir Levine, Daniel J Mankowitz, Jerry Li, Cosmin Paduraru, Sven Gowal, and Todd Hester. Challenges of real-world reinforcement learning: definitions, benchmarks and analysis. Machine Learning , 110(9):2419–2468, 2021. Daniel Furelos-Blanco, Mark Law, Anders Jonsson, Krysia Broda, and Alessandra Russo. Induction and exploitation of subgoal automata for reinforcement learning. Journal of Artificial Intelligence Research , 70:1031–1116, 2021. Rishi Hazra and Luc De Raedt. Deep explainable relational reinforcement learning: a neuro-symbolic approach. In Joint European Conference on Machine Learning and Knowledge Discovery in Databases , pages 213–229. Springer, 2023. Julian Ibarz, Jie Tan, Chelsea Finn, Mrinal Kalakrishnan, Peter Pastor, and Sergey Levine. How to train your robot with deep reinforcement learning: lessons we have learned. The International Journal of Robotics Research , 40(4-5):698–721, 2021. Rodrigo Toro Icarte, Toryn Q Klassen, Richard Valenzano, and Sheila A McIlraith. Reward machines: Exploiting reward function structure in reinforcement learning. Journal of Artificial Intelligence Research , 73:173–208, 2022. Linus K˚ age, Vlatko Mili´ c, Maria Andersson, and Magnus Wall´ en. Reinforcement learning applications in water resource management: a systematic literature review. Frontiers in Water , 7:1537868, 2025. 

> 11 Murari Veronese Meli

Pawel Ladosz, Lilian Weng, Minwoo Kim, and Hyondong Oh. Exploration in deep rein-forcement learning: A survey. Information Fusion , 85:1–22, 2022. Giuseppe Marra, Sebastijan Dumanˇ ci´ c, Robin Manhaeve, and Luc De Raedt. From statis-tical relational to neurosymbolic artificial intelligence: A survey. Artificial Intelligence ,page 104062, 2024. Daniele Meli, Alberto Castellini, and Alessandro Farinelli. Learning logic specifications for policy guidance in pomdps: an inductive logic programming approach. Journal of Artificial Intelligence Research , 79:725–776, 2024. Henrik M¨ uller, Lukas Berg, and Daniel Kudenko. Using incomplete and incorrect plans to shape reinforcement learning in long-sequence sparse-reward tasks. Neural Computing and Applications , 37(23):18851–18866, 2025. Roberta Raileanu and Rob Fergus. Decoupling value and policy for generalization in rein-forcement learning. In International Conference on Machine Learning , pages 8787–8798. PMLR, 2021. John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. Proximal policy optimization algorithms. arXiv preprint arXiv:1707.06347 , 2017. John Schulman, Philipp Moritz, Sergey Levine, Michael Jordan, and Pieter Abbeel. High-dimensional continuous control using generalized advantage estimation, 2018. URL 

https://arxiv.org/abs/1506.02438 .Sarath Sreedharan and Michael Katz. Optimistic exploration in reinforcement learning using symbolic model estimates. Advances in Neural Information Processing Systems ,36:34519–34535, 2023. Richard S. Sutton and Andrew G. Barto. Reinforcement Learning: An Introduction . MIT Press, 2nd edition, 2018. Celeste Veronese, Daniele Meli, and Alessandro Farinelli. Online inductive learning from answer sets for efficient reinforcement learning exploration. In International Workshop on Hybrid Models for Coupling Deductive and Inductive Reasoning , pages 93–106. Springer, 2024. Celeste Veronese, Daniele Meli, and Alessandro Farinelli. Learning symbolic persistent macro-actions for pomdp solving over time. In Leilani H. Gilpin, Eleonora Giunchiglia, Pascal Hitzler, and Emile van Krieken, editors, Proceedings of The 19th International Conference on Neurosymbolic Learning and Reasoning , volume 284 of Proceedings of Machine Learning Research , pages 1026–1040. PMLR, 08–10 Sep 2025. URL https: //proceedings.mlr.press/v284/veronese25a.html .Celeste Veronese, Daniele Meli, and Alessandro Farinelli. Sample-efficient neurosymbolic deep reinforcement learning. arXiv preprint arXiv:2601.02850 , 2026. George A Vouros. Explainable deep reinforcement learning: state of the art and challenges. 

ACM Computing Surveys , 55(5):1–39, 2022. 

> 12 Sample-efficient Neuro-symbolic Proximal Policy Optimization

Huan Wang, Stephan Zheng, Caiming Xiong, and Richard Socher. On the generalization gap in reparameterizable reinforcement learning. In International Conference on Machine Learning , pages 6648–6658. PMLR, 2019. Jingyi Xu, Zilu Zhang, Tal Friedman, Yitao Liang, and Guy Broeck. A semantic loss func-tion for deep learning with symbolic knowledge. In International conference on machine learning , pages 5502–5511. PMLR, 2018. Min Yang, Guanjun Liu, Ziyuan Zhou, and Jiacun Wang. Probabilistic automata-based method for enhancing performance of deep reinforcement learning systems. IEEE/CAA Journal of Automatica Sinica , 11(11):2327–2339, 2024. Maddalena Zuccotto, Alberto Castellini, Davide La Torre, Lapo Mola, and Alessandro Farinelli. Reinforcement learning applications in environmental sustainability: a review. 

Artificial Intelligence Review , 57(4):88, 2024. 

> 13 Murari Veronese Meli

Appendix A. 

A.1. Domains 

A.1.1. DoorKey 

DoorKey is a partially observable grid navigation task (Figure 1( a)) where the agent (red arrow) must retrieve a key, unlock a door of the same color, and then reach the goal cell (green). The MDP observation covers a 7 × 7 area (light gray), with each cell encoded by object type, color and state. The discrete action set has five actions: turn left , turn right ,

move forward , pick up and toggle (used to open the door). Rewards are sparse: the agent is rewarded only when it reaches the goal after opening the required door. In that case the reward is r(s, a ) = 1 − 0.9 · step count max steps , with max steps = env size · env size · 10 as defined by the environment, otherwise the reward is zero. The symbolic policy contains the logical rules from Hazra and De Raedt (2023): 1. pickup(X) ← key(X), sameColor(X, Y), door(Y), notCarrying 

2. toggle(X) ← door(X), locked(X), carryingKey(Z), sameColor(X,Z) 

3. goto(X) ← goal(X), unlocked 

Rule 1 recommends picking up a key X when it matches the color of a door Y and the agent is not already carrying a key. Rule 2 suggests unlocking a door X when the agent holds a key Z with the same color and the door is locked. Rule 3 then directs the agent toward the goal X once the door is unlocked. Unlike the first two, this rule is not tied to a single low-level action and is treated as a high-level navigation directive. 

A.1.2. OfficeWorld 

OfficeWorld (Figure 1( b)) is a grid environment with a rectangular layout and static labeled locations such as mail ( m), coffee ( c), office ( o) and decorative plants ( ∗) that serve as obstacles. Extra rooms labeled A through D appear depending on the task variant. The agent (red arrow) moves deterministically with four actions: up , down , left , right .The observation space includes the agent’s grid position, whether it is holding coffee, whether it is holding mail, and the progress through room visitation. Rewards are sparse and given only at goal completion: r(s, a ) = 1 − 0.9 · step count max steps , with max steps = env width ·

env height · 10 by default, otherwise the reward is zero. We consider four variants of the general task: 

• DeliverCoffeeAndMail , where the agent must collect both coffee and mail before reach-ing the office. We use the following rules from Furelos-Blanco et al. (2021): 1. goto(X) ← coffee(X), not HasCoffee, notHittingPlants 

2. goto(X) ← office(X), HasCoffee, notHittingPlants 

These rules cover only the deliver coffee subtask. 

> 14 Sample-efficient Neuro-symbolic Proximal Policy Optimization

• DeliverCoffee , a simplified variant where the agent only needs to collect the coffee and then reach the office. It uses the same rules as DeliverCoffeeAndMail .

• PatrolABC , where the agent must visit rooms A, B and C in order while avoiding plants. The corresponding rules by Furelos-Blanco et al. (2021) are: 1. goto(X) ← room a(X), not visited a, not visited b not visited c, notHittingPlants 

2. goto(X) ← room b(X), visited a, not visited b not visited c, notHittingPlants 

3. goto(X) ← room c(X), visited a, visited b, not visited c, notHittingPlants 

• PatrolAB , which only requires visiting rooms A and B in order while avoiding plants. The symbolic policy follows the same structure as PatrolABC , but includes only the rules for rooms A and B.

A.1.3. WaterWorld 

WaterWorld (Figure 1( c)) is a continuous 2D box populated by moving colored balls that bounce off the walls. The agent controls a white ball by changing its velocity with five actions: up , down , left , right , and none . At every step, the observation is the set of colors currently colliding with the agent, and the task is to touch balls in a prescribed color sequence. Rewards are sparse: the agent receives r(s, a ) = 1 − 0.9 · step count max steps only when the sequence(s) are completed, otherwise zero, with max steps = env size /2 by default. We evaluate three variants where multiple sequences can be interleaved. The logical rules are simple progression constraints over the color-touch events: 

• RedGreen :1. touch(red) ← not touched red, not touched green. 

2. touch(green) ← touched red, not touched green. 

The first rule recommends seeking a red collision until it happens; the second switches to green once red has been touched. They are both not actually tied to a single low-level action and are treated as high-level navigation directives. 

• RedGreenAndBlueCyan and RedGreenAndBlueCyanAndMagentaYellow : the symbolic policy is the same as RedGreen ; the blue →cyan and magenta →yellow sequences are unsuggested. 

> 15 Murari Veronese Meli

A.2. Hyperparameters 

Task total timesteps num envs batch size minibatch size 8x8 1 key 5 000 000 4 512 128 8x8 2 keys 25 000 000 8 1024 256 8x8 4 keys 50 000 000 16 2048 512 16x16 1 key 5 000 000 16 2048 512 16x16 2 keys 25 000 000 32 4096 1024 16x16 4 keys 100 000 000 64 8192 2048 Table 1: Hyperparameters for DoorKey . Fixed across all tasks: learning rate=0.0003, num steps=128, anneal lr=true, gamma=0.99, gae lambda=0.95, num minibatches=4, update epochs=4, norm adv=true, clip coef=0.2, clip vloss=true, ent coef=0.01, vf coef=0.5, max grad norm=0.5. Task total timesteps num envs batch size minibatch size DeliverCoffee 1 000 000 8 1024 256 DeliverCoffeeAndMail 25 000 000 32 4096 1024 PatrolAB 5 000 000 8 1024 256 PatrolABC 10 000 000 8 1024 256 Table 2: Hyperparameters for OfficeWorld . Fixed across all tasks: learning rate=0.0003, num steps=128, anneal lr=true, gamma=0.99, gae lambda=0.95, num minibatches=4, update epochs=4, norm adv=true, clip coef=0.2, clip vloss=true, ent coef=0.01, vf coef=0.5, max grad norm=0.5. Task total timesteps num envs batch size minibatch size RG 5 000 000 8 1024 256 RG&BC 10 000 000 16 2048 512 RG&BC&MY 20 000 000 32 4096 1024 Table 3: Hyperparameters for WaterWorld . Task abbreviations: RG = RedGreen, RG&BC = RedGreenAndBlueCyan, RG&BC&MY =RedGreenAndBlueCyanAndMagentaYellow. Fixed across all tasks: learning rate=0.0003, num steps=128, anneal lr=true, gamma=0.99, gae lambda=0.95, num minibatches=4, update epochs=4, norm adv=true, clip coef=0.2, clip vloss=true, ent coef=0.01, vf coef=0.5, max grad norm=0.5. 

> 16 Sample-efficient Neuro-symbolic Proximal Policy Optimization

A.3. Ablation Study for Θ in H-PPO-SymLoss 

We investigated two regimes for Θ in H-PPO-SymLoss :

• a linearly decaying schedule Θ t = max(Θ i − t · Θr, Θf ) with Θ i = 1, Θ f = 0 and Θr = 0 .4 (standard H-PPO-SymLoss in the main paper); 

• fixed constant weights Θ ∈ { 0.25 , 0.5, 0.75 , 1.0}.Across all domains, our results demonstrate that maintaining a high, constant symbolic penalty often suppresses the necessary initial exploration, leading to suboptimal conver-gence. On the other hand, lower constant weights allow for more exploration but offer weaker eventual regularization. The decaying Θ t schedule consistently achieves the best balance, successfully combining early exploration with strong subsequent policy stabiliza-tion. The results are reported in Figures 5, 6, 7. 

(a) DoorKey 8 × 8 - 1 key (b) DoorKey 8 × 8 - 2 keys (c) DoorKey 8 × 8 - 4 keys 

(d ) DoorKey 16 × 16 - 1 key (e) DoorKey 16 × 16 - 2 keys (f ) DoorKey 16 × 16 - 4 keys Figure 5: Ablation charts for Θ in DoorKey 

> 17 Murari Veronese Meli

(a) DeliverCoffee (b) PatrolAB 

(c) DeliverCoffeeAndMail (d ) PatrolABC Figure 6: Ablation charts for Θ in OfficeWorld 

(a) RedGreen (b) RedGreenAndBlueCyan (c) RedGreenAndBlueCyan AndMagentaYellow Figure 7: Ablation charts for Θ in WaterWorld 

> 18 Sample-efficient Neuro-symbolic Proximal Policy Optimization

A.4. Ablation Study for εf in H-PPO-Product 

We study the effect of the symbolic influence in H-PPO-Product by fixing εf while keeping the linear decay schedule unchanged, i.e., εt = max( εi − t · εr, ε f ) with εi = 0 and 

εr = 0 .4. In particular, we compare three constant terminal values: 

• εf = 0 (standard H-PPO-Product in the main paper); 

• εf = 0 .2; 

• εf = 0 .4. Overall, the impact is modest on easier instances, where all curves quickly reach similar returns (e.g., DoorKey 8 × 8 with 1 key and WaterWorld RedGreen). As the tasks become harder, maintaining a small symbolic bias is helpful: εf = 0 .2 and εf = 0 .4 tend to improve stability and final performance in the largest DoorKey grids, OfficeWorld PatrolABC , and the hardest WaterWorld sequence, while leaving the learning speed mostly unchanged. We observe a mild trade-off in the easiest OfficeWorld task ( DeliverCoffee ), where larger εf

can slightly reduce the final return, suggesting that a nonzero εf is most beneficial in long-horizon, sparse-reward regimes. Given that these differences are limited in most tasks, we keep εf = 0 in Section 5. The results are reported in Figures 8, 9, 10. 

(a) DoorKey 8 × 8 - 1 key (b) DoorKey 8 × 8 - 2 keys (c) DoorKey 8 × 8 - 4 keys 

(d ) DoorKey 16 × 16 - 1 key (e) DoorKey 16 × 16 - 2 keys (f ) DoorKey 16 × 16 - 4 keys Figure 8: Ablation charts for εf in DoorKey 

> 19 Murari Veronese Meli

(a) DeliverCoffee (b) PatrolAB 

(c) DeliverCoffeeAndMail (d ) PatrolABC Figure 9: Ablation charts for εf in OfficeWorld 

(a) RedGreen (b) RedGreenAndBlueCyan (c) RedGreenAndBlueCyan AndMagentaYellow Figure 10: Ablation charts for εf in WaterWorld 

> 20

Sample-efficient Neuro-symbolic Proximal Policy Optimization 

21